# CLAUDE.md — openclaw-model-bridge 项目背景

> 每次新会话开始时自动读取。当前版本：v37.9.251 / 0.37.9.101（2026-07-06）

---

## 项目简介

将任意大模型（primary 由 PROVIDER env 配置，当前 Doubao Seed 2.1 Pro 单模型多模态，V37.9.222 flip；fallback 链 DeepSeek-V4-Pro 满血版 / Doubao 2.0 / Qwen3-235B+VL 等）接入 OpenClaw（WhatsApp AI助手框架）的双层中间件，支持多模态（图片理解）。
运行于 Mac Mini (macOS)，用户：bisdom。

## 系统架构总览

```
┌─────────────────────────────────────────────────────────────────────┐
│                 用户层 (WhatsApp + Discord 双通道)                    │
└────────────────────────────┬────────────────────────────────────────┘
                             │
┌────────────────────────────▼────────────────────────────────────────┐
│  ① 核心数据通路（实时对话 + 多模态）                                  │
│                                                                     │
│  WhatsApp ←┐                                                        │
│  Discord  ←┼→ Gateway (:18789) ←→ Tool Proxy (:5002) ←→ Adapter (:5001) ←→ LLM (7 Providers) │
│  notify.sh ┘  [launchd管理]        [策略过滤+监控]       [认证+VL路由]     [Qwen3/GPT-4o/     │
│               [媒体存储]           [图片base64注入]      [Fallback降级]     Gemini/Claude/     │
│               [双通道推送]         [自定义工具注入]                         Kimi/MiniMax/GLM]  │
│                  │                [自定义工具注入]          │                               │
│                  │                    │                    │                               │
│                  │               /health ──→ /health       │                               │
│                  │               /stats (token监控)        │                               │
│                  │               /data_clean/* (REST)      │                               │
│                                                                     │
│  图片流程：Gateway存储jpg → Proxy检测<media:image> → base64注入       │
│           → Adapter检测image_url → 路由到Qwen2.5-VL → 图片理解回复    │
│  数据清洗：Gateway存储文件 → LLM调用data_clean工具 → Proxy拦截执行     │
│           → data_clean.py本地处理 → 格式化结果返回 → WhatsApp展示      │
└──────────────────┼────────────────────┼────────────────────┼────────────────────────┘
                   │                    │                    │
┌──────────────────▼────────────────────▼────────────────────▼────────────────────────┐
│  ② 定时任务层（system crontab，不经过 LLM 链路）                                     │
│                                                                                     │
│  每3h    ArXiv论文监控 ──→ KB写入 + WhatsApp+Discord推送                              │
│  每3h    HN热帖抓取 ──→ KB写入 + WhatsApp+Discord推送                                 │
│  每天×3  货代Watcher ──→ LLM分析(直接curl) + KB写入 + WhatsApp+Discord推送             │
│  每天    OpenClaw Releases ──→ LLM富摘要 + KB写入 + WhatsApp+Discord推送              │
│  每小时  Issues监控 ──→ KB写入 + WhatsApp+Discord推送                                 │
│  每天    KB晚间整理                                                                  │
│  每天    KB每日摘要 ──→ ~/.kb/daily_digest.md（LLM对话时可查）                          │
│  每周    KB跨笔记回顾 ──→ LLM深度分析 + WhatsApp+Discord推送                            │
│  每周    健康周报 ──→ WhatsApp+Discord推送                                             │
└─────────────────────────────────────────────────────────────────────────────────────┘
                                       │
┌──────────────────────────────────────▼──────────────────────────────────────────────┐
│  ③ 监控层（多级自动告警）                                                            │
│                                                                                     │
│  每30min  wa_keepalive ──→ 真实发送零宽字符 ──→ 失败则记录日志                         │
│  每小时   job_watchdog ──→ 检查所有job状态文件 + 日志扫描 ──→ 超时/失败→WhatsApp+Discord告警│
│  实时     proxy_stats ──→ token用量 + 连续错误计数 ──→ 阈值告警                       │
│  /health  三层健康端点：Gateway(:18789) → Proxy(:5002) → Adapter(:5001)              │
└─────────────────────────────────────────────────────────────────────────────────────┘
                                       │
┌──────────────────────────────────────▼──────────────────────────────────────────────┐
│  ④ DevOps层（自动部署 + 体检）                                                       │
│                                                                                     │
│  GitHub (main) ──→ auto_deploy.sh (每2min轮询)                                      │
│                     ├─ git fetch + pull                                              │
│                     ├─ 单测验证（proxy_filters变更时）                                 │
│                     ├─ 文件同步（仓库→运行时，31个文件映射）                            │
│                     ├─ 每小时漂移检测（md5全量比对）                                   │
│                     ├─ 按需restart（核心服务文件变更时）                                │
│                     └─ preflight_check.sh --full（部署后自动体检 11项）                │
│                         ├─ 单元测试 (proxy_filters + registry)                       │
│                         ├─ 注册表校验                                                │
│                         ├─ 文档漂移检测                                              │
│                         ├─ 脚本语法 + 权限检查                                       │
│                         ├─ Python语法检查                                            │
│                         ├─ 部署文件一致性（仓库 vs 运行时）                            │
│                         ├─ 环境变量检查（bash -lc 模拟cron）                          │
│                         ├─ 服务连通性（5001/5002/18789）                              │
│                         └─ 安全扫描（API key + 手机号泄漏）                           │
│                                                                                     │
│  开发流程：Claude Code → claude/分支 → GitHub PR → main → auto_deploy → Mac Mini     │
└─────────────────────────────────────────────────────────────────────────────────────┘
                                       │
┌──────────────────────────────────────▼──────────────────────────────────────────────┐
│  ⑤ 三方共享状态层（实时同步）                                                         │
│                                                                                     │
│  ┌──────────┐     ┌─────────────────────┐     ┌──────────────┐                      │
│  │ 用户      │     │ ~/.kb/status.json   │     │ Claude Code  │                      │
│  │ (WhatsApp)│────→│  priorities[]       │←────│ 开工: 读全部   │                      │
│  │ 反馈+决策 │ PA  │  feedback[]         │     │ 收工: 写变更   │                      │
│  │          │写入  │  recent_changes[]   │     │ 更新优先级     │                      │
│  └──────────┘     │  health{}           │     └──────────────┘                      │
│                   │  focus              │                                            │
│                   └────────┬────────────┘                                            │
│                            │                                                         │
│                   ┌────────┴────────────┐                                            │
│                   │ Cron 脚本自动更新     │                                            │
│                   │ auto_deploy → deploy │                                            │
│                   │ preflight → health   │                                            │
│                   │ kb_trend → trend     │                                            │
│                   └─────────────────────┘                                            │
│                                                                                     │
│  宪法：用户提供专业深度 + Claude Code 提供高效设计部署 + OpenClaw 提供数据复利            │
│        三者合一成为有生命的闭环系统                                                     │
└─────────────────────────────────────────────────────────────────────────────────────┘
```

### 核心组件详情

| 组件 | 端口 | 文件 | 功能 | 进程管理 |
|------|------|------|------|----------|
| OpenClaw Gateway | 18789 | npm全局安装 | WhatsApp接入、**媒体存储**、工具执行、会话管理 | launchd (KeepAlive) |
| Tool Proxy | 5002 | `tool_proxy.py` + `proxy_filters.py` | 工具过滤(24→12)、**图片base64注入**、**自定义工具注入+拦截执行**（data_clean、search_kb）、Schema简化、SSE转换、截断、token监控、`/data_clean/*` REST端点 | launchd plist |
| Adapter | 5001 | `adapter.py` | 多Provider转发、认证、**能力感知多模态路由**、Fallback降级 | launchd plist |
| LLM Providers (11) | — | `providers.py` | **11 Provider（7 built-in + 4 plugins）**：Qwen3-235B + VL-72B / GPT-4o / Gemini 2.5 / Claude / Kimi / MiniMax / GLM + **Doubao Seed 2.1 Pro（主力，PROVIDER env 配置，V37.9.222 flip）** / Doubao 2.0 / DeepSeek×2（`providers.d/`） | 外部服务 |

## 关键文件（本仓库）

| 文件 | 用途 |
|------|------|
| `tool_proxy.py` | HTTP 层（收发请求、日志、**自定义工具拦截执行**（data_clean本地执行+search_kb混合检索+followup LLM调用）、`/data_clean/*` REST端点、**V37.9.175 `_send_alert()` 收编 notify.sh --topic alerts**（标记+重试+队列+微信/Discord 扇出，退役裸 message send .py 旁路；fallback 仍加 [SYSTEM_ALERT] 标记，防 proxy 自身告警无标记污染 PA 上下文 → NO_REPLY，闭合 Path B .sh-only 盲区）） |
| `proxy_filters.py` | **V27新增→V37.4.3扩展** 策略层（过滤、修复、截断、SSE转换、**自定义工具注入**（data_clean+search_kb）、**V37.4.3 新增 `SYSTEM_ALERT_MARKER` + `filter_system_alerts()` 告警剥离函数**），纯函数无网络依赖 |
| `data_clean.py` | **V30.3新增** 数据清洗 CLI 工具（profile/execute/validate/history，7种操作，支持CSV/TSV/JSON/JSONL/Excel） |
| `test_data_clean.py` | **V30.3新增** 数据清洗单测（80个用例：格式检测/读写/操作/端到端/多格式） |
| `docs/archive/data_clean_poc/` | **V30.3新增→V35归档** Phase 0 验证材料（3个脏数据样本+LLM判断力测试脚本） |
| `SOUL.md` | **V30.4新增→V37.4.3升级** OpenClaw 最高优先级 system prompt（PA身份Wei、三方宪法、行为指令、**规则9批判性思考（反迎合+禁模糊关联+保存验证）**、**规则10告警消息不跟进（2026-04-11 血案规则，主题对齐硬规则+macOS FDA 幻觉防线）**、项目状态实时快照，每小时自动刷新） |
| `ops_soul.md` | **V31新增** Ops Agent 运维助手 SOUL.md（系统健康检查/日志排查/cron诊断/维护操作，部署到 `~/.openclaw/SOUL.md`） |
| `ops_health.sh` | **V31新增** Ops Agent 健康检查包装脚本（Qwen3 拒绝直接 curl localhost，通过脚本包装绕过） |
| `status.json` | **V30.4新增（仓库副本）** 三方共享意识锚点（priorities/feedback/incidents/quality/operating_rules/session_context），Mac Mini 每小时 git push 同步 |
| `providers.py` | **V34新增→V35+扩展→V37.9.146 验证档位字段化** Provider Compatibility Layer（BaseProvider 抽象 + **7 个实现**（Qwen/GPT-4o/Gemini/Claude/Kimi/MiniMax/GLM）+ doubao plugin + ProviderRegistry 动态注册 + 能力声明 + CLI 兼容性矩阵输出）。**V37.9.146（外部评审2 P2(a)）**：ProviderCapabilities 加 `verification_tier`（四档 declared/smoke_tested/feature_verified/production_observed）+ `tier_note`（退役注记）+ `tier_evidence`（依据，declared 走派生默认单一真理源）+ `tier_consistency_violations()` **一致性不变式**（tier 声明与 verified_* 布尔可证一致，防"改了 verified_* 忘改 tier"漂移）+ `tier_table_lines()` 机器表 + `--tier-matrix`/`--check-tiers` CLI + `to_matrix_row` 含 tier（--json 数据模型完整暴露）。**V37.9.222 B1**：BaseProvider 加 `reasoning_off_body` 字段（reasoning provider 声明 per-request 关-reasoning 请求体片段，doubao_21/deepseek_full = `{"thinking":{"type":"disabled"}}` 双 provider 实测同参数）+ to_legacy_dict/from_yaml 暴露 → adapter B1 批量注入读取。**V37.9.223**：全部 doubao + deepseek 四家（doubao_21/doubao/deepseek_full/deepseek）同款声明 `reasoning_off_body`（任何切换独立成 primary）——doubao 2.0 家族推断（同 Ark thinking API，未在此 endpoint 单测，作 primary 前须探针）+ deepseek 量化版**探针实测 2026-07-02 self-host 网关接受 thinking（HTTP 200/reasoning_tokens:0/无 400，400 风险排除）**但仍 non-reasoning→thinking-off 实际 no-op（无害）。docstring 分层标注证据（原则 #23，PENDING 时注入 inert） | 
| `test_providers.py` | **V34新增** providers.py 单测（48个用例：能力声明/模型查找/认证头/注册表/向后兼容/CLI输出） |
| `adapter.py` | API适配层（认证 `$REMOTE_API_KEY`，Fallback降级）。V34: 从 `providers.py` 导入 PROVIDERS。**V37.9.218**：新增 `FALLBACK_ORDER` 显式有序 fallback 链 env（逗号分隔 provider 名，权威覆盖 cap_score 自动排序 + 旧单槽 `FALLBACK_PROVIDER`，一物一形；primary 自动排除 flip-safe；无 key/未知/geo-block 跳过 + 去重保序）+ **capability-aware vision fallback**（fallback entry 带 `vl_model_id`，image 请求跳过纯文本 provider deepseek/deepseek_full、用 vision-capable 的 vl 模型）+ `_entry_from_registry()` helper。**V37.9.221**：`_classify_fast_route()` 纯函数（可单测）workload 路由——**无 tools = 批量/纯推理（规则 #27）→ fast provider（`FAST_PROVIDER`）；有 tools = PA agent → primary（reasoning 质量）**，复用现有 `FAST_ROUTE`（2026-07-02 reasoning-primary 拖垮批量 job 事故的迁移门槛修复，方案 A2 单点规则覆盖全部 24 异构 LLM 消费方，no-op until flip PROVIDER=doubao_21；design `docs/doubao21_workload_routing_design.md`）。**V37.9.222 B1**：`_is_batch_workload()`（批量判定纯函数，独立于 FAST_ROUTE，一物一形）+ `_batch_reasoning_off_body()`（批量在 reasoning provider 注入 `reasoning_off_body` 关 reasoning，服务 provider=FAST/primary）+ do_POST 注入。**A2/B1 互补**：有独立快 provider→A2 路由 / 无→B1 在 reasoning primary 注入 thinking-off（doubao_21 单模型通吃 batch，去 qwen 依赖）。2026-07-02 doubao_21 转正唯一 primary + flip 上线（批量 thinking-off / PA reasoning）。**V37.9.224 B1 fallback 传播**：fallback chain entry 携带 `reasoning_off_body`（registry + legacy 两构造点，镜像 vl_model_id）+ `_fallback_batch_body()` 纯函数——批量 fallback 按 fallback provider **自己的声明**重算注入（剥 serving 注入的 keys + 注入 fb 自己的片段；非批量原样），修两向事故：qwen-primary 回滚态批量 fallback 到 reasoning provider 带 reasoning 跑（V37.9.220 在 fallback 路径重演）/ doubao `thinking` 片段原样发给 qwen vLLM（未测参数可能 400 打断最后兜底） |
| `docs/strategic_review_20260403.md` | **V34新增** 导师战略复盘文档（Stage判断/主战场定位/V1-V3路标/三个高价值模块/话语权输出/三块差距） |
| `docs/technical_charter_20260705.md` | **V37.9.246新增** 中长期技术纲领（2026H2→2028）：三条元约束（日落法自适用/一物一形/诚实）+ 现状盘点（Stage 2 大部分达成→Stage 3 进行中，护城河=纵向证据语料库非代码）+ 10 条趋势判断（置信度标注）+ 5 需求段定位 + **双轨战略**（研究轨 Reliability Science × 工程轨 Portable Governance Plane，PA 重定义为活体实验室）+ 反目标清单 + **三地平线**（H1 收敛解耦含第二实例 / H2 产品化生态 / H3 标准影响力，每 H 带退役清单）+ 四业务线（B0 个人中枢为根）+ 7 贯穿性技术决策 + **8 判据门控**（G1-G8 数据驱动非日历）+ 8 风险登记 + 季度粒度落实建议 + 维护契约（季度复核/只追加修订/与既有文档一物一形分工） |
| `docs/pa_coupling_inventory.md` | **V37.9.249新增（H1-B / B1）** PA（Wei）耦合机器化盘点（纲领 §4.3「边界管理」哲学：边界内标注+config 化，边界外零 PA 依赖）。四类别机器扫描（个人路径/占位号码/macOS 假设/中文 prompt+TZ）逐项 blast-radius 分级（🔴高 launchd/🟡中 KB 路径·内容 prompt·TZ/🟢低 diagnose·homebrew·SOUL/✅ 已 config 号码·expanduser·md5）+ 边界完整性正面证据（examples/引擎核心零 PA 泄漏 = 可抽取产品已 PA-free）+ 既有扫描器信用登记 + B2 config 化 backlog 分级表 + §5 第一批落地（A1 kb_*.sh 默认路径）+ 诚实边界。结论: ~80% 耦合已 env 参数化，残余可枚举，第二实例真门槛=launchd（非无处不在个人痕迹）。 |
| `test_pa_coupling.py` | **V37.9.249新增→V37.9.250 扩展重命名→V37.9.251 全仓漂移守卫（B3 收尾）** PA 硬编码守卫（9 单测 5 类）: A1 断言 kb_*.sh 用 `$HOME/.kb` 默认 + D3 断言运行时 .sh 无裸 `TZ=Asia/Hong_Kong`（须 `${SYSTEM_TZ:-…}`，广度 ≥30）+ A2 断言 diagnose 无 `/Users/bisdom/.openclaw` + **V37.9.251 `TestNoPersonalPathHardcode` 扫全部运行时 .py+.sh（122 文件）拦截任意新写 `/Users/<真实用户名>` 个人路径（示例名 foo/test 豁免）+ 非空验证 ≥80 文件**。兑现纲领执行计划 §1 **B3 done-criteria「新增 PA-specific 硬编码有 scanner 拦截」**（轻量版：扩守卫为全仓扫描，不造独立 `*_scanner.py` 新模块，日落法）。sabotage 反向验证真有效（任一硬编码回退/任意文件新写个人路径立即 FAIL）。 |
| `docs/charter_execution_plan_20260705.md` | **V37.9.248新增** 纲领配套执行文档（一物一形：纲领管方向判据，本文档管执行+复核）。**Part 1 具体可执行计划**：§0 执行基线快照（复杂度四维度 files 286/env 43/jobs 40-46/state 25 + 徽章事实 + 8 门控当前态）+ §1 H1 地平线**15 任务分解**（H1-A Observer 收官 A1-A4 / H1-B PA 解耦 B1-B3 / H1-C 第二实例 C1-C3 / H1-D 证据刷新 / H1-E 年度复盘，每任务带目标/交付物/done-criteria/依赖门控/优先级/季度）+ §2 H2/H3 粗粒度里程碑（门控驱动不过度规划）+ §3 依赖链图。**Part 2 每季度五项固定复核可运行协议**（零新增常驻机器，日落法自约束，刻意无守卫测试）：R1 影响力指标（采集命令+采集环境说明+诚实复盘规则）/ R2 复杂度账本（四维度只读命令块+判断问题）/ R3 判据门控核对（G1-G8 逐门信号源表）/ R4 纲领增量修订（只追加规则）/ R5 反目标自查（6 条逐条问题）+ §10 记录模板 + §11 2026 Q3 首次实例预填 |
| `docs/llm_observer_design.md` | **V37.9.193新增（研究攻关 #1 Stage 0 design doc）** 机械化人眼 LLM-Observer 设计文档 — 锚定论文 arXiv:2606.14589 §5.2 开放问题（~70% 静默故障靠人眼/测试 ≈0%，机械化人眼）。拆解 unfinished #0 的 5 子目标（附录 A 对应表）：**评分函数**=两层管道（Layer 1 确定性 S1-S5 pre-filter [pollution-signal/credibility-mismatch/fabrication-phrase/provenance-gap/coherence] 复用 source_credibility+hallucination_guards seeds → Layer 2 LLM-judge，Layer 1 在前=成本+可解释+防 Observer 自身幻觉 V37.9.93）/ **ground-truth 标注协议**=22 corpus 已半结构化（trigger/amplifier/concealer）加机器标签（taxonomy_class/fail_plausible/observable_artifact/expected_signal/observer_in_scope 诚实划界，单表一物一形）+ 5 黄金种子 case 划 Category A/B / **自验证回路**=复用 adversarial_chaos_audit 框架（注入已知 fail-plausible 必抓 + sabotage S 规则即漏检）+ Observer 自身 read-only/sampling-aware/path-robust（§5.2 铁律 Observer 是 LLM 组件继承全 taxonomy）/ **与 daily_observer 关系**=扩展非另起（detect_anomalies 后插 detect_fail_plausible，anomalies[] 多态 + score/status 向后兼容，一物一形）/ **日落法第一问**=退役"人工逐条扫已知 fail-plausible 模式"不退役"人发现 novel 模式"（audit-as-regression 自我应用）。三锚点各司其职 + 双产出（论文#2 描述→预测 + 社区可跑 bench 种子 reliability_bench，深度+广度回应两评审同一对短板）+ 实施路径 Stage 0-6 + 风险诚实登记（主开放问题=held-out recall 能否非平凡=描述→预测真考验）。design only 零代码。**V37.9.210（门控收敛日）新增 §9.1 Stage 5.1 flip 决策框架**（预注册 shadow→on 决策规则：观察对象 score_history fp_high/med + 报告 fail_plausible 段 L1+L2 证据人工判 TP/FP + live precision 度量 + C1 零未修复系统性 FP/C2 ≥1 TP 或干净周/C3 cheap-path 成本可持续 + flip vs extend 逻辑 + rollback 即时可逆 env flip + small-N 诚实边界 + force_judge 不与 flip 捆绑）+ Stage 表进度更新（Stage 0-5 ✅ 各标 V 版本 + 6 chunk 1 ✅ + Stage 5.1 🟡 shadow 周）。一物一形：flip 标准从散落 V37.9.198/199 changelog prose 收敛到 design doc 单一 canonical（镜像论文 #2 pre-registration 方法学防 motivated reasoning） |
| `docs/llm_observer_ground_truth.yaml` | **V37.9.194新增（研究攻关 #1 Stage 1 ground-truth 验证集）** Observer fail-plausible 检测器的带标注验证集（单 YAML 表一物一形，避免 27 处 front-matter 漂移）。**24 个 incident postmortem** 逐 case 机器标签（design doc §4.2 schema）：taxonomy_class(A-E) / fail_plausible(yes/no/partial) / llm_fabrication / observable_artifact{desc,user_facing} / discovery_channel / **observer_in_scope 诚实划界**(yes 3/partial 5/no 16) / expected_signal[]（引用 S1-S5+layer2 信号字典）/ category(A 回归/B 探索/null out-of-scope) / golden_seed / paper_canonical / silence_span。**计数自洽**：24 labeled − 2 post-cutoff = 22 paper-canonical（data_inventory.md 对齐）；fail_plausible yes=4(D1 dream/D2 PA告警/D3 kb_review/dream_quota) + partial=4。**5 黄金种子**(Category A 回归核心，覆盖论文 §4.4 D1/D2/D3)。**诚实边界登记**：无面向用户语义 artifact 的事故（60-day TCC sandbox / gateway 静默死 / 部署遗漏）observer_in_scope=no（Observer 读内容结构上抓不到，不假装能抓）；pa_alert/pa_echo 机制在 scope 但 artifact 是 live PA 对话出 daily_observer 扫描集 → partial。**known_gaps** 登记 D4 编造发布（无独立 case 文件，ground-truth = hallucination_guards blocked_phrases）。无独立标注者不报 κ（论文 §3.3/§8 继承，标签 load-bearing 驱动客观 sabotage 测试缓解）|
| `test_llm_observer_ground_truth.py` | **V37.9.194新增** ground-truth YAML schema 守卫（21 单测 / 2 类）：TestGroundTruthSchema(19 — 必填字段/enum 校验/**tri-state 字段必须是 str 非 YAML-bool**（YAML 1.1 yes/no→bool footgun 防线）/case id 唯一/每个 case 文件真存在/**expected_signal 必须在 signals 字典定义**(MR-8 drift guard)/in-scope→有信号 out-of-scope→空(诚实边界契约)/golden 恰 5 个全 Category A 全 fail_plausible≠no/summary 计数自洽/paper_canonical 24-2=22 对账/known_gaps well-formed) + TestGroundTruthFilePresence(2 yaml-free 守卫)。**反向验证守卫真有效**：sabotage golden seed fail_plausible yes→no → 3 守卫 FAIL（category/golden/summary）；expected_signal 加未定义信号 → drift guard FAIL；还原 21/21 全过。skipUnless(PyYAML) 依赖边界（镜像 V37.9.144） |
| `llm_observer.py` | **V37.9.195新增（Stage 2 Layer 1）→V37.9.196 扩展（Stage 3 Layer 2 LLM-judge）** 机械化人眼两层管道 fail-plausible 检测。**Layer 1（S1-S5 确定性，零 LLM）**：**S1 pollution_signal**（错误码/HTTP/'Bad JSON'/告警 artifact 强指纹，D1/D2）/ **S2 credibility_mismatch**（低档来源被高档措辞包装，**复用 source_credibility** rank≥4）/ **S3 fabrication_phrase**（**复用 hallucination_guards.get_blocked_phrases()** 血案精确字眼，D4）/ **S4 provenance_gap**（牵强等价"异曲同工"无 [强证据]/[弱关联] 标注，**复用 LEVEL_6 契约**）/ **S5 coherence_structural**（boilerplate ×≥3 + 全标题无正文 + 字段=分隔符，D1/D3）。**V37.9.196 Layer 2（LLM-judge，design §3.1）**：`FAIL_PLAUSIBLE_SYSTEM` prompt（4 维 grounding/intent_alignment/pollution_evidence/fabricated_success，区别于 daily_observer CRITIQUE_SYSTEM 5 维质量评分）+ `build_fail_plausible_system()` 运行时注入 **LEVEL_6 守卫 + credibility 块**（复用 seed 非硬编码）+ sampling-aware（防 V37.9.93 truncation 误判）。`run_llm_judge()` 调 LLM（DI `llm_caller`，默认 lazy-bind `daily_observer.call_llm_critique` 单一 HTTP 真理源）→ `parse_fp_verdict()` 鲁棒解析（```json 围栏/裸 JSON/无法解析→clean）→ **🔴 `_evidence_grounded()` 反幻觉铁律**（LLM 引用证据必须在原文逐字 ground，否则 drop；verdict=fail_plausible 但无 grounded 证据 → 降级 clean，因 Observer 自己也是 LLM 组件继承全 taxonomy §5.2）。`detect_fail_plausible()` orchestrator：Layer 1 命中 OR `force_judge` → Layer 2；干净+no force → **便宜路径不调 LLM**；产 consolidated verdict（severity/category/evidence[L1+L2]/confidence/fired，对齐 daily_observer anomalies）。**FAIL-OPEN**（缺 seed / caller 错误 → [] 不阻塞）+ **FP 纪律**（不确定→clean）+ MR-11 log→stderr。**V37.9.198（Stage 5）**：`scan_fail_plausible(push_outputs, source_sections, llm_caller, enable_layer2)` collection 级 wrapper（daily_observer.run() 面向接口，§6.1）逐 artifact 跑 orchestrator + cheap-path（force_judge=False → Layer 2 仅 Layer 1 命中触发）+ sampled 判定（length>len(content) 传 sampled=True 防 V37.9.93 误判）+ FAIL-OPEN（单 artifact 异常 skip）+ `enable_layer2=False`（dry_run 仅 Layer 1 零 LLM）。**已 wire 进 daily_observer**（OBSERVER_FP_MODE shadow 默认，首次 runtime 变更 → VERSION bump 0.37.9.72） |
| `test_llm_observer.py` | **V37.9.195新增（39）→V37.9.196 +36（75 单测 / 16 类）** Stage 2+3 守卫。Layer 1：S1-S5 各单测 + TestRunPrefilter（**干净 synthesis FP=0** 控制）+ **TestGoldenCasesAgainstGroundTruth**（Layer 1 绑回 ground-truth yaml 确定性子集 S1-S5）+ D4 known_gap 经 S3。**V37.9.196 Layer 2**：TestFailPlausibleSystemPrompt（**注入 LEVEL_6+credibility 复用非硬编码** / 4 judge 维 / sampling 规则 / seed 缺失 FAIL-OPEN）+ TestParseFpVerdict（围栏/裸 JSON/无法解析→clean/confidence 归一化）+ **TestEvidenceGrounding（反幻觉核心）**+ TestRunLlmJudge（grounded 留/**ungrounded drop**/caller FAIL-OPEN）+ TestDetectFailPlausible（**cheap-path clean+no force 不调 LLM** / L1+L2 consolidated / force_judge L2-only / caller 抛异 L1 存活）+ **TestGoldenCasesLayer2（把 Layer 2 绑回 ground-truth：每 golden case 的 layer2_* judge 经 grounded 证据报出时 orchestrator 必 surface + MR-8 drift guard _JUDGE_TO_CATEGORY 覆盖全 layer2_ 信号）** + **TestStage3SabotageReverseValidation（monkeypatch 关掉 grounding → 幻觉证据泄漏，证守卫 load-bearing）** + Stage3SourceGuards。**反向验证守卫真有效**：sabotage 破坏 grounding → 3 FAIL；移除 LEVEL_6 注入 → 2 FAIL；还原 75/75 全过 |
| `llm_observer_selfcheck.py` | **V37.9.197新增（研究攻关 #1 Stage 4 — 自验证 harness + scorecard）** Observer 的 sabotage-validate 回路（论文 §5.2 铁律：Observer 自己是 LLM 组件须同等 sabotage 验证）。**复用 adversarial_chaos_audit 概念框架**（Category A 回归 / B 探索 / expected_catch / sabotage）但独立模块（机制不同：fixture 评估 `run_prefilter` vs 文件突变+governance subprocess）。**CORPUS bench 种子**（绑回 ground_truth.yaml 单一真理源）：Category A（6 golden 回归，必抓）/ clean（4 FP 控制）/ Category B（4 novel 持留/探索，无专设检测）。**scorecard 全离线可测**（零 LLM，Layer 1 确定性）：`defense_rate`（A 被抓 →100%）/ `fp_rate`（clean 误 flag →0%）/ **`fn_rate_B`（held-out recall，design §11 核心开放问题）** + `detection_latency`/`calibration` 诚实标 N/A（需生产/live LLM）。`run_sabotage_suite()` 每 A case 关掉 only_signal detector → 必漏检（证 load-bearing 非 tautology）。`build_scorecard_markdown` 产 `docs/llm_observer_scorecard.md`。CLI `--json/--save/--sabotage`。**实测**: defense 100%(6/6) / FP 0%(0/4) / **Category B FN 100%(4/4)=诚实 held-out 盲区**（印证 audit-as-regression 对 Observer 自身：Layer 1 是回归引擎非预测引擎，novel 检出须来自 Layer 2/人眼/新 S 规则）/ 6 detector 全 load-bearing。**Stage 边界**：自验证库 + scorecard（未 wire 进 daily_observer，Stage 5 才 runtime）。**V37.9.200（Stage 6 bench 化）**：加 `BENCH_ID`/`BENCH_VERSION`（数据集+指标契约版本，独立项目 VERSION）+ `build_bench_manifest()`（稳定无时间戳机器可读 datasheet：corpus 分类计数/detector 清单/每指标定义+target+current_value/sabotage 摘要/诚实边界，**跨机逐字可复现** → 外部贡献者 diff 改动前后判断 bench 是否退步，复用 build_scorecard 一物一形非新引擎）+ `--manifest` CLI。社区门面 = `docs/fail_plausible_bench.md`（含贡献协议）|
| `test_llm_observer_selfcheck.py` | **V37.9.197新增** Stage 4 harness 守卫（26 单测 / 7 类）：CORPUS schema（A 有 only_signal 指真 detector）+ TestScorecard（defense 100% / FP 0% / **Category B FN 是诚实预期非门禁** / 离线指标标 N/A / 空类 None）+ **TestSabotageSuite（覆盖全 A case + 全 load-bearing + 反向验证配错 detector → load_bearing=False 被抓）** + **TestCorpusBoundToGroundTruth（A 的 gt_id 是 golden_seed + only_signal 对应 expected_signal 确定性子集）** + scorecard markdown/saved artifact + Stage4 marker（复用 Observer 不 import adversarial_chaos_audit / Category B 不当回归门禁）+ CLI。**反向验证守卫真有效**：sabotage 把 golden case text 换成 Layer 1 抓不到的 → 5 FAIL（defense/load_bearing/cli）；还原 26/26 全过。**V37.9.200 +15（41 单测）**：TestBenchManifest(7) + TestBenchDocDriftGuard(5 doc-drift) + TestStage6SourceGuards(3)，sabotage 反向验证 current_value-绑活-scorecard + doc-指标-锚点 两 guard load-bearing |
| `docs/llm_observer_scorecard.md` | **V37.9.197新增** Observer scorecard artifact（llm_observer_selfcheck.py --save 生成）：核心指标表 + audit-as-regression 自我应用解读 + Category B 盲区登记（反哺新 S 规则/Layer 2）+ sabotage 验证表（6 detector load-bearing）。论文 #2 描述→预测的核心数据种子 |
| `docs/fail_plausible_bench.md` | **V37.9.200新增（研究攻关 #1 Stage 6 — 社区可跑 bench 门面）** fail-plausible / silent-failure 检测 bench 的社区 README（`reliability_bench` 故障注入的语义检测姊妹篇 + 论文 arXiv:2606.14589 配套）：why（~70% 静默故障靠人眼/测试治理 ≈0%）+ 指标表（defense 100%/FP 0%/held-out FN 诚实非门禁/latency·calibration 标 N/A）+ how to run（clone + `llm_observer_selfcheck.py --json/--sabotage/--manifest/--save`，零依赖 Layer 1 离线）+ corpus 结构（A 回归/clean FP 控制/B held-out 绑 ground_truth 单一真理源）+ **贡献协议**（外部加 case 的 schema + CI 守卫 + 新 fail-plausible 模式归 Category B 诚实登记 = 广度交付直接回应两评审"可迁移性"短板）+ 诚实边界（held-out recall 开放问题）。守卫 test_llm_observer_selfcheck.py +15（TestBenchManifest 7 含 current_value 绑活 scorecard + 无时间戳稳定 + detector=Category A only_signal / TestBenchDocDriftGuard 5 含运行命令+指标名+论文+ground_truth 引用不漂移 / TestStage6SourceGuards 3 含 marker + 复用 build_scorecard 一物一形 + bench_version≠项目 VERSION），sabotage 反向验证两 guard load-bearing |
| `docs/paper/silent_failures_taxonomy/` | **V37.9.139新增→V37.9.154 已发表→V37.9.187 IEEE Software 投稿** ArXiv 论文 *When Errors Become Narratives: A Longitudinal Taxonomy of Silent Failures in a Production LLM Agent Runtime*（Stage 2→3 话语权 unfinished #5 启动）：`draft.md` ~490 行英文完整第一稿（5 类 taxonomy + three cross-cutting findings（摘要口径，§5 六小节展开）+ **fail-plausible** 概念贡献 + Defense Framework 5 支柱 + 接缝论点）+ `data_inventory.md` 论文每个数字→仓库溯源表（投稿前对表清单）+ `DECISIONS_NEEDED.md` 7 项用户决策（标题/署名 AI 披露/venue 推荐 arXiv 直发/匿名度/LaTeX 时机/内容取舍/figures）+ `latex/` 9 个 .tex 投稿源码。**✅ 已发表 [arXiv:2606.14589](https://arxiv.org/abs/2606.14589)（2026-06-15, cs.SE 直发 + cross-list cs.AI/cs.DC）** — 时间线: DECISIONS_NEEDED 7 项 6/11 全决策 → 引用终核 6/11 清零（6 篇 arXiv 官方 API 作者列表 100% 一致零修改）→ 投稿 6/12 (submit/7702567) → announcement 6/15 周一香港早 ID 落地 → 发布配套 V37.9.154。**V37.9.187 投稿包** `submissions/`（5 文件）：`ieee_software_experience_report.md`（IEEE Software 经验报告版 ~3,400 词 + 3 takeaway + Index Terms）+ `threats_to_validity_and_case_study_method.md`（Runeson-Höst 方法学化解 n=1）+ `ai_disclosure_and_acknowledgments.md`（合规 AI 披露）+ `statement_of_differences.md`（预印本差异说明）+ `SUBMISSION_GUIDE.md`（逐 venue 清单+cover letter）。**✅ 2026-06-25 正式投稿 IEEE Software**（ORCID 0009-0009-1176-7817 / arXiv 预印本透明披露 / Claude 不列作者）进入编辑评审。**V37.9.190 投稿包扩至 6 文件**：新增 `issre_industry_track_paper.md`（ISSRE Industry Track 平行第二投投稿稿 = 完整论文 + Runeson-Höst 纵向单案例方法作 §3 + 四类威胁有效性 schema 作 §8 化解 n=1 + AI 披露整合 + Index Terms + ref[15] Runeson&Höst 2009 + 自含 Trim priorities 附录；数字冻结 study-cutoff 与 arXiv 一致；区别于 IEEE 杂志经验报告版的不同文档无双发冲突）。**V37.9.191（2026-06-28）启动投稿**：WebFetch/WebSearch 实地确认 ISSRE 2026 CFP（官方页 403→EAPLS/EasyChair 镜像交叉验证）= 6 页**含** refs(full)/4(short) IEEE CS 2 栏 / 非双盲 / 摘要≤150 词+≤4kw / 摘要截止 6-28→7-3 / 论文 7-5→7-12 / 通知 8-12 / EasyChair；新建 `issre_6page_submission.md`（按 Trim 附录把 ~14 页裁到 6 页含 refs + 150 词摘要 wc 验 + Index Terms 10→4 + 冻结 study-cutoff 数字 + typeset 超页下一步裁剪指针）+ SUBMISSION_GUIDE §3 加 confirmed CFP 表 + EasyChair 摘要注册块 + author runbook。论文三 venue 状态：arXiv 已发表 / IEEE Software in-review / **ISSRE 已投递（Submission 395, Full Paper, 2026-06-28, in-review, 通知 8-12）**——交互式投递: EasyChair 注册 + pdflatex+IEEEtran 本地真编译 6 页含 refs（`latex/issre_6page_trimmed.tex` 投稿版 / `issre_6page.tex` 完整版 7 页弃用）+ PDF 上传 15:50；截止前(论文 7-12)可 Update 重传 |
| `docs/compatibility_matrix.md` | **V34新增→V37.9.146 验证档位字段化** Provider 兼容性矩阵（**含 Doubao×2 + DeepSeek×2(量化/满血)** + **验证四档诚实标注** declared/smoke_tested/feature_verified/production_observed + gemini geo-block 退役现状 + fallback 链 V37.9.129 现实 + 插件优先接入指南）。**V37.9.146（外部评审2 P2(a)）**：验证档位升级为**第 3 张机器表**（providers.py `verification_tier` 字段 `--tier-matrix` 直出，退役 V37.9.142 手写表 = 一物一形），`gen_compat_matrix.py --check` 守卫**三张表**（支持的 Provider + 验证档位 + 能力矩阵）；人工段落（Fallback 路径/工具模式验证）不参与比对 |
| `gen_compat_matrix.py` | **V37.9.143新增→V37.9.146 三张表** compatibility_matrix 漂移防护（外部评审2 P0(b)+P2(a)）：`--check` 比对 providers.py 直出**三张表**（支持的 Provider + **验证档位** + 能力矩阵）vs doc 表格段（exit 1 = drift）+ `--fix` 一键重写（人工段落绝不触碰）。镜像 gen_jobs_doc --check 模式。**首跑即抓真漂移**（V37.9.142 手动刷新漏 doubao json_mode=True 手写为 —）。**V37.9.146**：验证档位字段化后 TABLE_SPECS 2→3 张 |
| `test_gen_compat_matrix.py` | **V37.9.143新增→V37.9.146 +11** 漂移防护 38 单测：providers 三张表直出纯函数（含 V37.9.146 TestTierTableLines 6 — 8 行/退役注记渲染/declared 派生依据/全名归一化/CLI）+ verifiable_features 分母修复守卫（"5/4 verified" 超界 bug → Qwen 5/5 + Doubao 5/6 诚实语义）+ extract/check/fix 三件套（含档位表提取/sabotage）+ 人工段落保护契约（验证档位机器化后改用 Fallback prose）+ 真仓库端到端 + TABLE_SPECS 3 表守卫 + 反向 sabotage（分母回退 2 fail / doc 表行/档位行篡改 check exit 1） |
| `slo_benchmark.py` | **V35新增→V37.9.143 四态/双窗口/阈值正文** SLO Benchmark 报告生成器（读取 proxy_stats.json 真实数据→Markdown/JSON 报告：延迟p50/p95/p99、成功率、错误分类、降级率、恢复率）。**V37.9.99** 三态 OBSERVING 样本门槛；**V37.9.143**（外部评审2 P0(a) dev 侧）：四态 verdict（tool_calls=0 → `N_A_NO_TOOL_CALLS` 不参与汇总，镜像 slo_dashboard V37.9.79 N/A）+ 24h/7d 双窗口趋势段（复用 slo_dashboard 历史快照 MR-8，无历史显提示行）+ Threshold Rationale 正文段（config.yaml V37.9.79 注释升级为报告正文 + 阈值恢复后文案自适应）+ Methodology 阈值行动态读 config（修 V37.9.79 后硬编码 "≤30s" 漂移） |
| `test_slo_benchmark.py` | **V35新增** SLO Benchmark 单测（17个用例：全通过/各类违规/零请求/格式化/文件读取） |
| `quickstart.sh` | **V35新增** 一键 Quick Start（4阶段：前置检查→启动服务→健康验证→Golden Test Trace，10分钟跑通全栈） |
| `docs/golden_trace.json` | **V35新增** Golden Test Trace（quickstart.sh 生成的真实请求/响应/延迟记录，可复现证据） |
| `docs/slo_benchmark_report.md` | **V35新增** 首份 SLO Benchmark 实验报告（5/5 PASS，p95=459ms） |
| `openclaw_backup.sh` | **V29.1新增** 每日Gateway state备份到外挂SSD（保留7天） |
| `jobs_registry.yaml` | **V27新增** 统一任务注册表（system + openclaw 双 cron） |
| `check_registry.py` | **V27新增→V37.9.206 providers.d 覆盖守卫** 注册表校验脚本。**V37.9.206**：`check_filemap_completeness` 加 `providers.d/*.py` 覆盖扫描（真 provider 插件漏进 auto_deploy FILE_MAP → 硬 error，下划线前缀模板豁免），闭合"provider 插件不是 job 绕过原 job 循环"的部署盲区（deepseek 双插件漏部署致 fallback 静默失效血案） |
| `ROLLBACK.md` | **V27新增** 回滚指南（30秒恢复到V26） |
| `upgrade_openclaw.sh` | Gateway升级SOP脚本（必须SSH直连执行，禁止WhatsApp触发） |
| `restart.sh` | 一键重启 Proxy + Adapter + Gateway（含 PATH 修复，可在 cron 环境使用）。**V37.9.13 架构清理**：Adapter + Proxy 改用 `launchctl kickstart -k` 走 launchd 管理，消除 V37.9.12.1 双管理血案；plist 缺失时 fallback 到 nohup；V37.8.13 Gateway 健康验证模式扩展到 adapter/proxy（5×2s curl /health 探测）；**V37.9.140 日落法**：#48703 listeners-Map 自动补丁段退役（上游 2026.3.23 修复 + Gateway 已升 v2026.4.27，移除本脚本唯一 sudo 依赖；上游回归监控由 preflight 12/19 保留承担） |
| `health_check.sh` | **V27→V37.9.78 重构** 每周一 09:00 健康周报（v2.0 "系统证据周报"）— 9 段证据：🖥服务+🤖模型+📊SLO+🛡安全+🏛治理+🛟MOVESPEED+🐦X监控+📚KB+💾SSD。MR-8 single-source-of-truth 全部走外部工具 (slo_dashboard/security_score/.audit_metrics.jsonl/movespeed_incident_monitor)，safe_call helper 三层 FAIL-OPEN，推送优先 notify.sh --topic daily（重试+队列）fallback openclaw 直推 |
| `test_health_check_v37_9_78.py` | **V37.9.78新增** health_check v2.0 单测（32 个用例 / 5 类）：TestSourceGuards(15 含 V37.9.78 marker / safe_call helper / 9 段 emoji / SLO 字段名校对 / notify.sh 优先 / v1.1 反模式移除) + TestSafeCallHelper(5 含 timeout / fallback / set -e 安全) + TestRuntimeBehavior(3 含 dev 全 8 段 + FAIL-OPEN 不抛 Traceback + JSON schema) + TestReverseValidation(4 sabotage 字面量/段移除/字段名/helper 真有效) + TestGovernanceLineage(6 V36/V30.2/V37.1/V37.9.27/V37.8.4/V37.8.13 引用) |
| `kb_write.sh` | KB写入脚本（含目录锁+原子写） |
| `kb_review.sh` | **V29升级→V37.5 重写** KB跨笔记回顾 thin wrapper（调用 kb_review_collect.py，fail-fast + [SYSTEM_ALERT]，不再机械 fallback） |
| `kb_review_collect.py` | **V37.5新增** KB 周度回顾数据采集器 + LLM 调用（纯 Python 模块：load_sources_from_registry 从 jobs_registry.yaml 读源 + extract_recent_sections H2 drill-down + call_llm 80-char 最小内容阈值 + run() 接受 llm_caller 注入可单测） |
| `test_kb_review.py` | **V37.5新增** kb_review_collect 单测（44 个用例：registry 发现 / H2 parser 边界 / collect_notes 时间过滤 / call_llm 空短失败 / run() 失败不伪装成功 / 输出契约 / shell 源文件锁） |
| `kb_deep_dive.py` | **V37.9.16新增→V37.9.17 picker tier-aware fallback→V37.9.183 OA 全文解析→V37.9.186 pwc 死路径退役** 每日深度分析 collector（纯 Python 模块：`import kb_review_collect as rc` 复用 load_sources_from_registry + extract_recent_sections + call_llm；picker 算法 ⭐≥4 门槛 + TOPIC_WEIGHTS 主题加权 + 摘要长度 tie-breaker + **V37.9.17 方案 C tier-aware fallback**（TIER 1/2 优先于 TIER 3，避免 X tweet ⭐5 挤掉 arxiv ⭐4 走 abstract_only）；三档 fetch：一档 arxiv/acl/hf/s2/dblp PDF（**V37.9.186 移除 pwc 死路径**：job 停用 V31 + 脚本删 V37.8.13 + registry enabled=false，镜像 V37.9.88 daily_observer JOBS_SUBDIRS 残留 pwc 血案；pdfplumber lazy import + References/Acknowledgments 切割 + 图表 caption 去噪 + 30K 截断）/ 二档 rss_blogs/ontology_sources/github_trending HTML（bs4 lazy import + article/main 标签优先）/ 三档 degrade；**V37.9.183 OA 全文解析**（`resolve_oa_pdf_url`/`_s2_lookup_oa_pdf`/`_s2_fetch`：fetch_pdf_text 直接派生失败时用 URL 里确定性标识符 HF arxiv-id 改写/S2 paper-id/DOI → S2 graph API openAccessPdf+externalIds.ArXiv → arxiv/OA PDF，复用 S2_API_KEY，FAIL-OPEN，零选错论文风险；根治 77% abstract_only 结构 gap：picker 选高对齐论文集中在 DOI-only/付费墙场馆，V37.9.132 KB-write arxiv-优先只覆盖有 arxiv 版的论文。**V37.9.184**：ScienceDirect（PII URL 无标识符）由 ontology_sources KB-write 时捕获 prism:doi 改写 doi.org link 覆盖，deep_dive DOI→S2 解析自动接管，零本模块改动）；双 prompt 分支 build_full_text_prompt（论证链+实验对比+局限性）vs build_abstract_only_prompt（禁推测+摘要级标注），两者都含 GROUNDING_CONSTRAINT；fail-fast run() 返回 status: ok/no_candidates/llm_failed/collector_failed） |
| `kb_deep_dive.sh` | **V37.9.16新增** 每日深度分析 thin wrapper（镜像 kb_review.sh V37.5.1 env-var heredoc 模式）：set -euo pipefail + notify.sh source + `--topic deep_dive` 双通道推送（WhatsApp 简版 + Discord #daily 完整版，内容不同）+ `[SYSTEM_ALERT]` fail-fast + last_run_deep_dive.json status 写入 + rsync 备份带事故取证（V37.9.14 INV-BACKUP-001 契约）+ no_candidates 告警但不 exit 1（允许明日再试） |
| `test_kb_deep_dive.py` | **V37.9.16新增→V37.9.17 +6 tier fallback** kb_deep_dive 单测（61 个用例 / 11 类）：TestClassifyTier(7) + TestParseEntriesFromSection(6) + TestScoreEntry(6) + **TestPickTopTierFallback(6) — V37.9.17 方案 C 守卫含 `test_v37_9_16_blood_lesson_scenario` 直接复现 X tweet ⭐5 + arxiv ⭐4 必选 arxiv** + TestPdfUrlDerivation(5) + TestPreprocessPdfText(5) + TestFetchDegrade(3) + TestPromptBuilders(4) + TestOutputBuilders(5) + TestRunOrchestrator(5 端到端：no_candidates/ok full_text/llm_failed 不产出 artifacts/degrade abstract_only/collector_failed) + TestKbDeepDiveShellGuards(9 源码级守卫：[SYSTEM_ALERT]/env-var heredoc 反模式/notify.sh 引用/set -euo pipefail/last_run/deep_dive topic/rsync forensics/fail-fast 顺序锁) |
| `cross_source_signal_aggregator.py` | **V37.9.46新增** Opportunity Radar Stage 1 PoC — 跨 source 弱信号聚合器（纯 Python，6 核心函数：scan_today_notes/embed_notes/cluster_dbscan/filter_cross_source/rank_signals/emit_radar_json + run/main CLI）。**算法管道**：扫今日 ~/.kb/notes/*.md → sentence-transformer 384 维 embedding（lazy import local_embed + numpy）→ DBSCAN 聚类（min_samples=3 / eps=0.35 / metric=cosine, lazy import sklearn）→ filter_cross_source（unique_sources≥2 跨源契约）→ rank_signals（score=source_count×2+note_count+avg_intra_similarity×5, top 10）→ emit ~/.kb/radar/daily_signals_{date}.json。**FAIL-OPEN 契约**（V37.4 同款 lazy import）：dev 无 numpy/sklearn/sentence-transformers 时模块仍可 import + 测试可跑（mock 隔离），run() 缺依赖 → status=fail_open_no_deps + 写空 signals 不阻塞下游。**Cache 策略**：content_hash 16 字符 cache key（V37.4 mtime-stable 同款），命中跳过 sentence-transformer 重算。**MR-11 兑现**：log() 写 stderr 防 $(...) 命令替换污染。**suggested_topic Stage 1 简化版**：cluster 内 titles 最高频关键词（中英文 regex），Stage 4 升级 LLM 一句话总结。**设计文档** docs/opportunity_radar_design.md 第三节锁定常量与算法。 |
| `kb_trend_acceleration.py` | **V37.9.48新增** Opportunity Radar Stage 3 — 趋势加速检测（设计文档 5 节）。5 函数：extract_keywords_per_week (复用 kb_trend.py extract_period_text + extract_keywords, lazy import + FAIL-OPEN)、compute_acceleration (输入 dict {1: w1_kw, 2: w2_kw, 3: w3_kw} 输出加速度 metrics — 用各周占比 pct 而非原始 freq 防总数差距失真 + w2=0 新词 skip + min_freq_pct=0.005 小基数门槛过滤噪声)、classify (5 档锁定 — 🚀 strong: a1≥1.5 AND a2≥1.5 / ⚰️ obs: a1<0.5 AND a2<0.5 / 📈 mild: a1≥1.5 / 💧 decel: a1<0.7 / 📊 stable: 其他 / 顺序锁定 strong > obs > mild > decel > stable / 非数字输入安全降级 STABLE)、rank_signals (按 archetype priority 分桶 + 同桶按 \|a1-1\| desc 排 / top_strong=10 strong+mild 共 / top_stable=5 / top_obs=3 / 总 max 18 signals)、emit_radar_json (~/.kb/radar/weekly_trends_{week}.json + archetype_summary 计数). **不集成 kb_dream/kb_evening** (Stage 4 才集成). **向后兼容**: kb_trend.py 旧接口完全不动. **设计文档 11.2 风险表登记**: 趋势加速度噪声大 (小基数) → MIN_FREQ_PCT 门槛防 0.5% 占比以下噪声参与计算. CLI: `--kb-dir X --output-dir Y --json`. |
| `test_kb_trend_acceleration.py` | **V37.9.48新增** Stage 3 PoC 28 单测（7 测试类）：TestExtractKeywordsPerWeek(4 含 invalid week_offset / dict 类型契约 / FAIL-OPEN 缺 kb_trend / 日期范围注入) + TestComputeAcceleration(5 含 3 周历史正常 / w2=0 新词 skip / w3=0 → accel_2w=None / min_freq_pct 噪声门槛 / 空输入安全) + TestClassify(6 含 strong 必须 a1+a2 / obs 必须 a1+a2 / mild 仅要 a1 / decel 仅要 a1 / stable 中间区 / 非数字 fallback STABLE) + TestRankSignals(4 含 archetype 优先级 strong>mild>decel>obs>stable / top_k 截断 per bucket / 空输入 / 桶内 \|a1-1\| desc) + TestEmitJson(3 含 JSON 完整性 + archetype_summary / weekly_trends_{week}.json 路径 / 嵌套自动 mkdir) + TestBackwardCompat(3 含 kb_trend.extract_period_text/tokenize/extract_keywords 旧接口仍 callable 不 break) + TestSourceLevelGuards(3 V37.9.48 marker / 5 档阈值锁定 1.5/0.7/0.5 + 5 archetype 常量 + 5 emoji / log() file=sys.stderr MR-11). **反向验证守卫真有效**: sed `ACCEL_STRONG_THRESHOLD = 1.5` → `0.5` → test_design_locked_thresholds 立即 AssertionError. 还原后 28/28 全过. |
| `project_alignment_scorer.py` | **V37.9.47新增** Opportunity Radar Stage 2 — 项目对齐度评分 rule_check 验证层（设计文档 4 节）。7 函数：load_project_concepts (lazy YAML parse + 行尾注释剥离 V37.9.47 修)、count_keyword_hits (大小写不敏感 + 中英 substring + dedup + excluded_topics weight × -2 强降权)、compute_expected_range (5 档锁定: <0/0/1-2/3-4/5+ → ⭐1-2/2-3/3-4/4-5)、validate_alignment_score (LLM ⭐ 数 vs rule keyword 一致性 + 偏高/偏低 reason 解释)、parse_6field_output (MR-8 与 hf_papers.sh inline 行为一致 — 仅 📌 字段 inline 提取, 其他字段 LLM 多行格式)、extract_star_count (取最大连续 ⭐ 段)、format_validation_marker (validated=False 时返回 ⚠️ <reason>)。**集成 hf_papers.sh emit-stage**: lazy import + load_project_concepts 一次, 每 paper alignment 段后调 validate_alignment_score(title+tldr+abstract, llm_stars), 不一致时推送加 ⚠️ 标记前缀给用户. **FAIL-OPEN**: project_alignment_scorer 缺失 → log WARN 跳过 rule_check 不阻塞 hf_papers cron. CLI: `--content X --llm-score N --json`. |
| `test_project_alignment_scorer.py` | **V37.9.47新增** Stage 2 PoC 31 单测（7 测试类）：TestProjectConceptsYaml(5 含 yaml schema / weight 范围 [-5,5] / keywords 非空 / FAIL-OPEN 缺文件 → 默认 dict) + TestKeywordHitCounting(6 含多关键词 / 大小写不敏感 / 中英混合 substring / excluded × -2 降权 / 同 keyword dedup / 空内容) + TestExpectedScoreRange(4 含 0/5/边界 1-4/负数命中) + TestValidateAlignmentScore(4 含血案场景 LLM=5+0 命中 fail / LLM in range pass / LLM 偏低 / invalid score type) + TestPromptInjection(3 hf_papers.sh prompt 含 🎚️ + 5 档评分指南 + 一句话原因 ≤30 字) + TestParse6FieldOutput(5 完整 6 字段 / 缺字段 / 顺序乱序 / 空 / **MR-8 grep hf_papers inline 守卫**) + TestSourceLevelGuards(4 V37.9.47 marker / _SCORE_RANGES 5 档锁定 / FAIL-OPEN 注释 / log stderr MR-11). **反向验证已确认守卫真有效**: sed `(5, 999, 4, 5)` → `(5, 999, 1, 2)` → test_high_hits_high_stars 立即 fail. 还原后 31/31 全过. |
| `test_cross_source_signal_aggregator.py` | **V37.9.46新增** Stage 1 PoC 33 单测（8 测试类）：TestScanTodayNotes(5 含 YYYYMMDD 日期过滤 + frontmatter parse + tags 单值/list 双格式 + 缺字段 default unknown + 无效 date) + TestEmbedNotes(4 含 cache hit os.path.isfile mock + force_recompute bypass + 文本截断 ≤500 chars + empty 输入 numpy.zeros (0,384)) + TestClusterDbscan(5 含 lazy import sklearn ImportError + N<min_samples short-circuit 在 import 之前 + 多 cluster + noise -1 + DBSCAN 参数 design-locked 验证) + TestFilterCrossSource(4 含单 source 拒绝 + 跨 2 源保留 + 跨 4 源高信号 + noise label 排除) + TestRankSignals(3 含公式正确性 + top_k 截断 + 空输入) + TestEmitRadarJson(3 含 JSON 字段完整性 + daily_signals_{date}.json 路径 + 自动 mkdir 嵌套目录) + TestFailOpenContract(4 含 no_notes 状态 + numpy 缺失 fail_open + sklearn 缺失 fail_open + 单笔记 OSError 不致命) + TestSourceLevelGuards(5 V37.9.46 marker / DBSCAN_MIN_SAMPLES=3 + DBSCAN_EPS=0.35 设计锁定值 / FAIL-OPEN 契约文档 / lazy import 反 module-top-import 守卫前 60 行禁 numpy/sklearn/sentence_transformers/local_embed / log() 写 stderr MR-11)。**反向验证已确认守卫真有效**: sed 把 `DBSCAN_MIN_SAMPLES = 3` → `1` → test_design_locked_constants 立即 AssertionError; 模块顶部加 `import numpy` → ModuleNotFoundError 立即触发（dev 无 numpy 强证守卫）。还原后 33/33 全过。 |
| `project_concepts.yaml` | **V37.9.45新增** Opportunity Radar #2 PoC 项目本体清单（V37.9.46 Stage 1 复用即将由 project_alignment_scorer.py 完整 Stage 2 消费）：3 core_planes (control/capability/memory plane keywords + weights 5/4/5) + 3 active_research_directions (ontology/agent_reliability/data_compounding) + 2 excluded_topics (pure_finetuning/pure_hardware weight=-2 命中扣分) + scoring_guide 5 档评分指南（⭐⭐⭐⭐⭐=control plane/agent runtime/ontology 直接相关）+ meta_rules（收工时同步本周新增概念）。version=0.1-poc。 |
| `hallucination_guards.py` | **V37.9.57新增** 公共反幻觉守卫 5 档模板 (MR-8 single-source-of-truth, 替代 9 task 各自 inline 守卫). 5 档累积式严格度: **LEVEL_1_MINIMAL** (kb_harvest_chat 闭环数据, 仅"严禁虚构") / **LEVEL_2_STANDARD** (freight/finance/ontology 单源新闻, + 来源标签校验) / **LEVEL_3_STRICT** (kb_review/kb_deep_dive 跨域回顾, + 反链式推论 "X 段提到 Y → 推论 Z" 模式禁止) / **LEVEL_4_PROJECT_AWARE** (8 ALIGNED jobs per-paper alignment 评分, + V37.9.56-hotfix3 6 个血案精确字眼 "OpenClaw 社区发布"/"v26"/"v26/v27/v37 版本更新"/"项目里程碑"/"开源 X 上线"/"[openclaw]") / **LEVEL_5_RADAR_AWARE** (kb_dream Reduce + kb_evening, + Opportunity Radar 三件套信号源契约 + Top 5/cross_source/trend 显式声明"跨多天累积非今日事件"). **公开 API**: `get_guard(level)` / `list_levels()` / `get_blocked_phrases()` / `get_radar_signal_types()`. 未知 level fallback LEVEL_3_STRICT 安全中位数. CLI `python3 hallucination_guards.py --list/--blocked-phrases/--level X`. 纯 stdlib 零外部依赖. |
| `test_hallucination_guards.py` | **V37.9.57新增** 38 单测 / 7 类: TestGuardLevels(8 含 5 档累积式 + V37.9.57 marker + LEVEL_N 不含 LEVEL_(N-1) 之外特性) + TestGetGuardAPI(6 默认/未知/None/非string fallback 全过) + TestBloodLessonBlockedPhrases(5 V37.9.56-hotfix3 5 个核心字眼必须在 LEVEL_4+) + TestRadarSignalTypes(3 三件套 #1/#2/#3 必须在 LEVEL_5 + 不在 LEVEL_4) + TestCliInterface(4 subprocess --list/--blocked-phrases/--level) + TestSourceLevelGuards(9 V37.9.57 marker / V37.9.56-hotfix3 lineage / GUARDS dict 5 key locked / _DEFAULT_FALLBACK 锁 LEVEL_3 / MR-8 documented) + TestV37957Contracts(2 模块顶部 import / 无外部依赖). **反向验证已确认守卫真有效**: sabotage _LEVEL_4 整段赋空 → 4 failures; sabotage _DEFAULT_FALLBACK 改 LEVEL_5 → 1 failure; sabotage _LEVEL_5 整段赋空 → 4 failures. 还原后 38/38 全过. |
| `source_credibility.py` | **V37.9.98新增** 来源可信度评级（observer 5/28 LLM critique proposal #2 兑现，与 hallucination_guards.py 互补：HG 守"内容真实性 + 跨域推论可信度"，credibility 守"出处权威性"）。纯 stdlib 零外部依赖。**5 档 tier**（按"引用断言无需独立验证的可信程度"递减）：🥇 学术同行评审 (rank1: S2/DBLP/ACL/ontology_sources 已发表有DOI) / 🥈 会议预印本 (rank2: arxiv/hf_papers 公开可引未评审) / 🥉 工业实践 (rank3: github_trending/openclaw_run 官方代码可验证) / 📝 博客 (rank4: rss_blogs/**chaspark observer 点名的非主流源**) / 💬 社媒 (rank5: ai_leaders_x/hn/freight/finance 时效强可靠性参差)。**SOURCE_CREDIBILITY** 14 源映射 = MR-8 单一真理源（test drift guard 守 ↔ jobs_registry 14 内容源 id+label 双匹配）。**公开 API**: `get_credibility(source_id)` (FAIL-OPEN 未知源→社媒最低档+reason=未分类来源) / `format_credibility_block()` (prompt 注入块, source 列表动态生成) / `list_tiers()` / `list_sources()`. CLI `--list/--sources/--block/--source X`. |
| `test_source_credibility.py` | **V37.9.98新增** 56 单测 / 10 类: TestCredibilityTiers(6 含 5 档 + rank 1-5 唯一 + list_tiers rank 升序 + _DEFAULT_TIER=社媒最保守) + TestGetCredibility(7 含已知学术/预印本/chaspark博客 + 未知→社媒FAIL-OPEN + 非string + 字段完整 + label 匹配) + **TestSourceCoverage(7 MR-8 drift guard — registry 14 源 + 每源有 credibility + 无 orphan + label 匹配 + tier 有效)** + TestChasparkFlagged(4 observer proposal #2 锚点 — chaspark=博客 + block 含"非主流学术源"+"需独立验证"+"proposal #2") + TestFormatBlock(9 \\n\\n 开头 + ⚠️ + V37.9.98 + 5 tier + 14 label + [可信度:<等级>] 标注指令 + 未分类默认规则) + TestCli(5 subprocess) + TestDreamWiring(6 含 5 注入点 + import + 反 inline 守卫) + TestEveningWiring(5 含 build_evening_prompt 渲染 + 位置在今日笔记 section 之前) + TestAutoDeployFileMap(2) + TestSourceLevelGuards(7 V37.9.98 marker + MR-7/MR-8 + FAIL-OPEN + chaspark tier 锁定). **反向验证已确认守卫真有效**: sabotage chaspark→学术同行评审 → 4 failures; 删 ontology_sources 源 → drift guard fail. 还原后 56/56 全过. |
| `top_alignment_picker.py` | **V37.9.56新增** Opportunity Radar Sub-Stage 4c Top 5 高对齐推送 picker（设计文档第 7 节 Stage 4c 兑现）。纯 Python 模块 8 个核心函数：scan_source_results / parse_alignment_from_content / collect_all_picks / rank_picks / format_top_picks_block / pick_top_aligned + log()(MR-11 stderr) + main CLI（--json/--block-only/--repo-root/--min-stars/--top-n）。**ALIGNED_SOURCES** **V37.9.112 → 10 个 source** (hf_papers prio 1 / s2 prio 2 / arxiv+dblp prio 3 / github_trending prio 4 / rss_blogs+ai_leaders_blogs prio 5 博客档 / ai_leaders_x+**ai_leaders_bsky** prio 6 社媒档 / hn prio 7, **V37.9.108 +ai_leaders_blogs 大神长文 / V37.9.112 +ai_leaders_bsky 大神实时短帖**) 来自 V37.9.45/50/51 全量迁移 6 字段 + 🎚️ 项目对齐度评分 + V37.9.108/112 ai_leaders 双渠道大神观点. **解析**: lazy import project_alignment_scorer.extract_star_count → _fallback_extract_star_count (clamp [0,5]) → key-based 6-field parser 复用 hf_papers/V37.9.51 同款 (📌/🔑/💡/🎯/⭐/🎚️ 字段头识别 + 🎚 fallback no variation selector)。**排序契约**: ⭐ 数 desc + source_priority asc + cn_title 长度 desc 三档 tie-break。**FAIL-OPEN 契约 (V37.9.46 同款 lazy import 模式)**: cache dir/file 缺失 → 该 source 0 picks 不阻塞其他 source / llm_results.jsonl 损坏行 → skip 继续 / failed=True 跳过 / alignment scorer 缺失 → fallback regex / 无 picks → block="" 调用方静默不推送 / 任何异常 → pick_top_aligned 顶层 try/except 返回 no_picks。**默认配置**: DEFAULT_MIN_STARS=4 / DEFAULT_TOP_N=5 (V37.9.51 收工承诺锁定)。**集成路径** kb_dream.sh Phase 1.5+ + kb_evening_collect.py build_evening_prompt 双处。 |
| `test_top_alignment_picker.py` | **V37.9.56新增** Sub-Stage 4c 55 单测 / 8 类 + 反向验证三层守卫真有效 (sabotage DEFAULT_MIN_STARS=3 / 删 🎚 fallback / log 写 stdout 都立即多 fail，还原后全过). TestParseAlignmentFromContent(11 含 6 字段完整 / ⭐≥4 vs ⭐3 阈值 / 🎚 emoji fallback / 中文标题 / 5 字段向后兼容 / 空内容 / 非 string / ⭐ clamp 到 5 / rating vs alignment 不污染 / reason truncate 60 字) + TestFallbackExtractStarCount(5 含多段取最长 / 空输入 / clamp) + TestScanSourceResults(6 含 missing dir + corrupted JSON skip + failed entries skip + empty content skip) + TestCollectAllPicks(3 含 dev 空环境 / min_stars 过滤 / source metadata 注入) + TestRankPicks(6 含 stars desc / priority tie-break / title len tie-break / top_n truncate / 空输入 / 缺字段安全) + TestFormatTopPicksBlock(6 含空输入 / 一行一 pick / 标题 40 字截断 / 无 reason 不加分隔符 / ⭐ clamp) + TestPickTopAlignedOrchestrator(3 含端到端 dev no_picks / ok with picks / multi-source 排序) + TestCliInterface(3 subprocess 真跑 CLI --json + --block-only) + TestSourceLevelGuards(10 V37.9.56 marker / 8 source 全部注册 / DEFAULT_MIN_STARS=4 锁定 / DEFAULT_TOP_N=5 锁定 / log 写 stderr MR-11 / FAIL-OPEN 文档 / 🎚️ + 🎚 双 emoji 兼容 / priority 论文优先 / 反向验证守卫 / 模块顶部反 lazy import 守卫) + TestV9_56IntegrationContracts(2 picks JSON 可序列化 + bash 注入安全契约). |
| `kb_search.sh` | **V29新增** KB按需查询工具（关键词/标签/来源/统计概览） |
| `kb_inject.sh` | **V29新增→V29.4升级** 每日KB摘要+运维知识精华注入workspace CLAUDE.md；文档按需查阅路径 `~/.kb/docs/` |
| `mm_index.py` | **V29.1新增** Multimodal Memory 索引器（Gemini Embedding 2，支持图片/音频/视频/PDF） |
| `mm_search.py` | **V29.1新增** Multimodal Memory 语义搜索（文本查询→cosine similarity→匹配媒体） |
| `mm_index_cron.sh` | **V29.1新增** MM 索引定时任务包装脚本（每2小时） |
| `local_embed.py` | **V29.3新增** 本地 Embedding 引擎（sentence-transformers，中英双语，零API调用） |
| `kb_embed.py` | **V29.3新增** KB 文本向量索引器（notes+sources 分块→本地 embedding→~/.kb/text_index/） |
| `kb_rag.py` | **V29.3新增** KB RAG 语义搜索（--context LLM注入 / --json 脚本调用） |
| `kb_trend.py` | **V29.5新增→V37.9.178 收编 notify** KB周趋势报告（本周vs上周关键词+LLM分析+推送）。**V37.9.178**：`push_whatsapp` 收编 `notify.sh --topic daily`（微信+Discord#daily+重试+队列），退役裸 `os.system('openclaw message send')`（不走 notify 漏微信 + shell 注入风险），fallback 用 subprocess argv（Path B 最后一个 .py 内容旁路） |
| `status_update.py` | **V29.5新增** 三方共享项目状态工具（原子读写 ~/.kb/status.json，Claude Code + PA + cron 共用） |
| `kb_save_arxiv.sh` | ArXiv监控结果写入KB + rsync备份 |
| `auto_deploy.sh` | **V27.1新增** 仓库→部署自动同步 + 漂移检测（md5全量比对+WhatsApp+Discord告警） |
| `test_tool_proxy.py` | proxy_filters 单测（118个用例，含自定义工具注入 + **V37.4.3 新增 21 个告警隔离回归：TestFilterSystemAlerts(14) + TestNotifyShAlertMarker(5) + TestToolProxyImportsAlertFilter(2)**） |
| `test_check_registry.py` | **V28新增** check_registry.py 单测（18个用例） |
| `gen_jobs_doc.py` | **V28新增** 从 registry 自动生成任务文档 + 漂移检测 |
| `smoke_test.sh` | **V28新增** 端到端 smoke test（单测+注册表+连通性） |
| `wa_keepalive.sh` | **V28新增** WhatsApp session 保活（每30分钟真实发送验证） |
| `preflight_check.sh` | **V28新增→V30.3升级→V37.9.177 推送测试环境保真** 收工前全面体检（16项检查：单测+注册表+语法+部署一致性+环境变量+连通性+安全扫描+数据流+货代监控+crontab路径一致性+推送通道E2E）。**V37.9.177**：check 16 推送 smoke test 前先 `source .env_shared`（再 source notify.sh）→ 测 cron 真实推送配置而非启动 shell 碰巧的环境，免疫 shell 会话污染（2026-06-18 export WEIXIN_TARGET 占位符泄漏血案） |
| `job_smoke_test.sh` | **V30.3新增** 全量 job smoke test（20个启用任务×6维度：脚本存在/crontab注册/运行时路径/日志活跃/状态文件/锁文件+KB完整性+crontab条目数） |
| `docs/config.md` | 完整系统配置文档（含所有历史变更） |
| `docs/GUIDE.md` | 完整中英文集成指南 |
| `docs/openclaw_architecture.md` | **V28.2新增** OpenClaw 开源架构完整参考（每日开工自动刷新） |
| `cron_doctor.sh` | **V30新增** 定时任务全面诊断工具（7项检查：crontab/锁文件/心跳/服务/环境/时效/系统） |
| `cron_canary.sh` | **V30新增** Cron 心跳金丝雀（每10分钟，零依赖，原子写入） |
| `crontab_safe.sh` | **V30新增** 安全 crontab 操作（自动备份+条目数验证+回滚保护） |
| `test_cron_health.py` | **V30新增** 定时任务健康检测单测（94个用例：锁/心跳/告警/原子写入/损坏恢复/daemon检测/完整性/状态刷新） |
| `kb_status_refresh.sh` | **V30.1新增** 每小时刷新 status.json 系统健康字段（三层服务/模型ID/KB统计/过期job），补齐三方宪法实时同步 |
| `kb_integrity.py` | **V30.1新增** KB 文件完整性校验器（SHA256 指纹比对、目录文件数监控、权限检查、status.json 结构验证） |
| `test_status_update.py` | **V30.1新增** status_update.py 全量单测（33个用例：原子读写/嵌套字段/数组操作/优先级CRUD/CLI接口） |
| `test_adapter.py` | **V30.1新增** adapter.py 全量单测（36个用例：Provider注册表/认证头/多模态路由/Fallback/智能路由/健康端点） |
| `test_kb_business.py` | **V30.1新增** KB全业务逻辑单测（44个用例：kb_embed/kb_rag/kb_trend/mm_index/mm_search/kb_integrity/安全模式） |
| `full_regression.sh` | **V30.1新增** 全量回归测试一键运行器（四层：单元测试+注册表文档+安全扫描+代码质量，393个用例，100%通过才允许推送） |
| `audit_log.py` | **V30.2新增** 链式哈希审计日志（JSONL append-only，SHA256 链式校验，篡改/删除可检测） |
| `test_audit_log.py` | **V30.2新增** 审计日志单测（19个用例：写入/链式哈希/篡改检测/删除检测/统计） |
| `governance_audit_cron.sh` | **V37.1新增** 每日定时治理审计（governance_checker --full + engine --check，失败推送 alerts），ontology 从被动验证升级为主动监控 |
| `security_score.py` | **V30.2新增** 系统安全评分（7维度100分：密钥/测试/完整性/部署/传输/审计/可用性） |
| `reliability_bench.py` | **V36新增→V37.9.147 +10 场景** Agent Reliability Bench（**17场景103检查**：原 7（Provider宕机/工具超时/畸形参数/超大请求/KB未命中/Cron漂移/状态损坏）+ **V37.9.147 外部评审2 P2(b)** 10（Provider schema 漂移/SSE 流中断/tool 结果超大/JSON 畸形修复/全 fallback 失败/记忆索引陈旧/cron 重复触发/config 部分损坏/DNS 失败/长上下文截断质量），mock-based 可 dev 运行，朝行业可引用测试集方向） |
| `test_reliability_bench.py` | **V36新增→V37.9.147 +23** Reliability Bench 单测（59个用例：17场景×独立验证+报告格式+CLI+场景计数/ID 守卫；真行为 sabotage 证 bench 测真控制平面非 tautology） |
| `docs/reliability_bench_report.md` | **V36新增→V37.9.147 17 场景** Reliability Bench 实验报告（17/17 PASS，103/103 checks，V37.9.147 +10 场景重生成） |
| `memory_plane.py` | **V36新增** Memory Plane v1 统一接口（4层：KB语义/多媒体/偏好/状态，统一 query/context/stats） |
| `test_memory_plane.py` | **V36新增** Memory Plane 单测（45个用例：4层×可用性/搜索/统计+统一查询+优雅降级+CLI） |
| `docs/memory_plane.md` | **V36新增** Memory Plane 架构文档（分层设计/API/数据流/CLI） |
| `docs/security_boundaries.md` | **V36新增** 安全边界文档（8节：认证/网络/输入验证/数据保护/LLM安全/运维/评分/已知风险+Checklist） |
| `docs/resilience_report.md` | **V36新增** 运维韧性实验报告（7场景故障注入+Recovery Time+GameDay对比+改进建议） |
| `docs/ontology/` | **V36新增** Ontology KB — 本体论驱动的企业智能架构知识库（16文件：三角架构论述/核心概念/AI治理/OpenClaw本体审视/文献/人物/README/流派对比/供应链本体） |
| `ontology/` | **V36.1新增→V37.9.15 Phase 4 P3 三阶段门控 shadow wiring** Ontology 独立子项目 — Tool Engine（81条声明式规则+推理引擎+**classify_tool_call语义分类**）+ **Governance Ontology v3.32**（**61不变式**+**297 可执行检查**+**16元规则**+**MR-6 critical-invariants-need-depth 从警告升级为 hard-enforcing via INV-LAYER-001**+MR-7 治理自观察+MR-8 copy-paste-is-a-bug-class+MR-9 state-writes-go-through-helper+**INV-GOV-001 summary 不吞 error**+**INV-DREAM-001/002 + INV-CACHE-002 Dream Map 预算+缓存契约**+**INV-PA-001/002 告警污染+SOUL.md 规则 10 双防线**+**INV-LAYER-001 治理深度自我强制**+**INV-GATE-001 三阶段门控 wiring observability (V37.9.15)**+验证深度三层模型+Phase 0元规则自主发现）+ governance_checker.py 执行引擎 + 宪法6条 + **Phase 2 shadow模式**(off→shadow→on) + 立场文章(EN+ZH) + **V37.9.12 engine.py 3 个 Phase 4 P1 纯函数** (`load_domain_ontology()` / `find_by_domain(domain)` / `evaluate_policy(policy_id, context)`) + `proxy_filters.py::filter_tools` 切换读 policy.max-tools-per-agent.limit + **V37.9.13 Phase 4 P2 context evaluator** (6 matcher + `_CONTEXT_EVALUATORS` dispatch + `evaluate_policy()` 三档扩展) + 第二条 policy `_resolve_max_tool_calls_per_task_limit()` 镜像 5 档 safe-fallback + **V37.9.15 Phase 4 P3 `ontology/three_gate.py` 三阶段门控 shadow wiring**：`pre_check/runtime_gate/post_verify` 三纯函数 + `GateFinding` namedtuple + `ONTOLOGY_GATES_MODE` env (off/shadow/on 默认 shadow) + 静态 policy signal 比对（tool_count/tool_call_count/body_bytes vs limit 产 pass/flag verdict） + 与 `ONTOLOGY_MODE` 解耦 + FAIL-OPEN 契约（engine 异常不冒泡，fall back to pass+reason=engine_unavailable）。tool_proxy.py lazy-load three_gate + `_run_gate` 帮助函数（Exception 兜底 + log [gate:*]）+ do_POST 两处调用点（raw 后 pre_check+runtime_gate / rj 后 post_verify 必须在 fix_tool_args 之前）|
| `ontology/domain_ontology.yaml` | **V37.9.9新增（骨架）→V37.9.12 P1 wiring 依据** Phase 4 Layer 1 领域模型 — 六域概念 Actor/Tool/Resource/Task/Provider/Memory + 域间关系模板 + Phase 4 实施路径 P1/P2/P3。V37.9.12: `find_by_domain()` 纯函数读此文件, Actor 6/Resource 7/Task 6/Memory 4 层/Provider 4 type 可查。|
| `ontology/policy_ontology.yaml` | **V37.9.9新增（骨架）→V37.9.15 Phase 4 P3 gates 接入** Phase 4 Layer 2 策略声明 — 10 条 policy 按 static/temporal/contextual 三类分。**V37.9.15 meta 升级** version 0.2-p2-partial → 0.3-p3-partial，status → gates_wired_shadow_mode，新增 `policies_observed_by_three_gate` 清单（pre_check 2 / runtime_gate 3 / post_verify 1）+ `gates_feature_flag` 声明（env_var=ONTOLOGY_GATES_MODE / values=["off","shadow","on"] / default=shadow / decoupled_from=ONTOLOGY_MODE）+ `next_step` 指向 P3.1 (shadow→on 一周观察) / P3.2 (第三条 policy) / P4 (真正 enforcement 路径)。|
| `ontology/three_gate.py` | **V37.9.15新增** Phase 4 P3 三阶段门控纯函数模块（~280 行，零 I/O 零状态）— `pre_check(context)` 评估 alert-context-isolation + quiet-hours-00-07；`runtime_gate(context)` 评估 max-tools-per-agent + max-tool-calls-per-task + max-request-body-size（静态 policy 提取 signal 与 limit 数值比对产 pass/flag verdict）；`post_verify(context, response)` 扫 LLM 响应检测 [SYSTEM_ALERT] echo。`GateFinding` namedtuple (gate/policy_id/verdict/action/reason/enforced)。`ONTOLOGY_GATES_MODE` env 三档 off/shadow/on（默认 shadow，未知值 fall back shadow 观察优先）+ `_VALID_GATE_MODES = ("off", "shadow", "on")` 常量。与 `ONTOLOGY_MODE` 解耦（允许 ONTOLOGY_MODE=on 但 gates=off 的灰度组合）。FAIL-OPEN 契约: `_safe_evaluate_policy` 捕获 engine 所有异常返回 None → gate 降级为 verdict=pass + reason="engine_unavailable"，绝不冒泡到 tool_proxy。`format_findings_for_log()` 产出 `[gate:pre_check] N findings: policy_id=verdict(reason) ...` 单行便于 `grep [gate:]` 运维排查。|
| `ontology/tests/test_three_gate.py` | **V37.9.15新增** three_gate 契约守卫（57 单测）：TestGatesMode(7 含默认 shadow + case-insensitive + whitespace + 未知 fall back shadow)、TestGateFinding(3 含 immutable + 字段顺序稳定)、TestOffModeShortCircuit(4 含 off 模式不调 engine)、TestShadowModeNeverEnforces(2 反证 on 模式会 enforced=True)、TestPreCheck(8 含 alert 检测 + quiet-hours 上下文全分支)、TestRuntimeGate(6 含 signal≤limit boundary 合规 / signal>limit flag / 缺信号 tolerant)、TestPostVerify(5 含 assistant [SYSTEM_ALERT] 回声检测 + malformed response tolerant)、TestFailOpen(4 monkey-patch engine.evaluate_policy 抛异常契约)、TestExtractSignal(7 含 bool 排除于 int) + TestExtractAssistantText(6) + TestFormatFindingsForLog(3) + TestModuleIntegration(2 公开 API + gate name 稳定性) |
| `test_phase4_ontology_skeleton.py` | **V37.9.9新增→V37.9.12扩展** Phase 4 骨架守卫（17 单测）：六域声明/description 长度/Actor 核心实例/source_of_truth 对齐/Memory 四层匹配 memory_plane/policy 三类分类/policy 必填字段/hard_limit 仅 static/alert-isolation ordering_constraint/跨 ontology 引用一致性 + **V37.9.12 hard_limit 必须有 `limit` 字段 + max-tools-per-agent.limit == 12 横向守卫** |
| `ontology/tests/test_engine_phase4.py` | **V37.9.12新增** Phase 4 P1 wiring 契约（38 单测）：`load_domain_ontology()` 加载 + path 参数 + 缺文件抛错；`find_by_domain()` Actor/Resource/Task/Memory 归一化 + Provider.types 字符串包装 + Tool 主动返回 []（source_of_truth=tool_ontology）+ 未知域返回 [] + 预加载 ontology 注入 + 纯函数不改输入；`evaluate_policy()` 返回 12-key 稳定结构 + unknown found=False + static applicable=True + contextual/temporal applicable=None+reason=needs_context_evaluator + load_failed 不抛异 + context 参数兼容 + pre-loaded policy_data 注入；**max-tools-per-agent 契约**: limit=12, hard_limit=True, type=static, governance_invariant=INV-TOOL-001, 横向一致 config_loader.MAX_TOOLS; **_parse_limit_from_rule**: ≤/<=/< 提取 + 下划线千位分隔 + None 回退 |
| `ontology/tests/test_governance_cron_matcher.py` | **V37.2+新增** INV-CRON-003/004 匹配器回归测试（18 单测）：① endswith+word-boundary 精确匹配 ② Map-Reduce split 不误报 ③ prefix-subset entries 不混淆 ④ exec() 作用域陷阱正向对照 ⑤ YAML-matcher in-sync guard |
| `ontology/tests/test_governance_summary.py` | **V37.3新增** INV-GOV-001 silent error bug 回归测试（7 单测）：① error 状态注入→汇总不说"所有不变式成立" ② error 状态 exit_code≠0 ③ error 汇总用 💥 ④ pure fail 仍用 ❌ ⑤ all-pass 仍说✅ ⑥ mixed fail+error 各自计数 ⑦ 源码级 grep 守卫 `r["status"] in ("fail","error")` |
| `ontology/docs/cases/governance_silent_error_case.md` | **V37.3新增** Governance 自身三层嵌套盲区案例（子串匹配→exec 作用域→silent error summary）— 观察者的自我盲区 + MR-7 提案 + INV-GOV-001 提案 |
| `ontology/docs/cases/dream_map_budget_overflow_case.md` | **V37.4新增** Dream Map 预算溢出 + cache key 漂移案例（workload 增长 × cache key 对 mtime/sort 敏感 × Reduce 不相信缓存 → 连续超时，Reduce 从未执行）完整因果链架构图 + 三层根因 + 预算/契约/稳定三层修复 |
| `ontology/docs/cases/pa_alert_contamination_case.md` | **V37.4.3新增** PA 告警污染血案案例（系统告警经 Gateway sessions.json 污染对话上下文 × Proxy truncate 无语义理解 × Qwen3 attention 跨主题错误关联 × LLM 编造 macOS FDA 指令）六层根因深挖 + 完整因果链架构图 + 六条件组合爆炸分析 + 结构+行为双防线修复论证 |
| `ontology/docs/cases/heartbeat_md_pa_self_silencing_case.md` | **V37.8.16新增** PA 自残 HEARTBEAT.md 血案案例（2026-04-19 09:09 PA 把"任务完成"写进 OpenClaw 保留文件 HEARTBEAT.md → 13h 潜伏 → OpenClaw heartbeat 机制激活让 LLM 对所有用户消息回 HEARTBEAT_OK → Gateway stripTokenAtEdges 剥离 → 13h 完全静默）完整时间线 + 三层根因 + 六条件组合爆炸分析 + MR-15 元规则提炼 + 4/19 深夜原则 #28 违规复盘（Bad MAC 错归因让用户 auth reset）+ 6 轮假说错五轮的教训 |
| `ontology/tests/test_dream_cache_stability.py` | **V37.4新增→V37.4.2扩展** Dream Fix A+B+C+retry 回归测试（32 单测）：① Fix A cache-only fast path + SKIP_MAP_LOOPS 三处门控 ② Fix B 批次阈值 30/24000 ③ Fix C per-note content_hash 缓存 + 跨平台 md5 fallback + signal dedup ④ 动态 DREAM_TIMEOUT_SEC 预算 ⑤ flush_pending_batch 辅助函数语义 ⑥ Sources 缓存键保持不变 ⑦ **V37.4.1 Retry fallback**：MIN_DREAM_CHARS=3000 / MIN_ACCEPTABLE_CHARS=1500 / BEST_RESULT 追踪 / 温度单调递减 / 致命错误门槛用 MIN_ACCEPTABLE_CHARS ⑧ **V37.4.2 结构+变体**：UNIQ_NOTE_SIG_BLOCKS 数组 / 覆盖统计 header / 编号 cluster header / bland header 已移除 / retry 2+ 变体 prompt / llm_call 引用 \$cur_prompt |
| `test_kb_dream_helpers.py` | **V37.9.68新增→V37.9.68-hotfix +12** Dream 三阶推送 helper + shell 集成 **80 单测 / 11 类** (V37.9.68 68 + V37.9.68-hotfix +12): TestSplitDreamIntoChunks(8 V37.9.68-hotfix 新类 — empty/非string/血案场景回归 4 段必产 4 chunks/header 合并/超大段内部切/无 ## 段单 chunk/max_chunk 防御/空段跳过 + 5/15 真实 layout 模拟) + TestKbDreamShellGuards(12 → V37.9.68-hotfix +4: helper import 守卫 + 真调用 + **反向消除旧合并算法**`if len(current) + len(piece) + 1 <= max_chunk:` python_assert + V37.9.68-hotfix marker) + 其他 9 类 V37.9.68 原有 60 单测保留. **反向验证两层守卫真有效**: sabotage helper while 循环 → 1 fail / sabotage 回退到旧合并算法 → 5 fail (含血案场景回归 + 5/15 真实 layout). 接近真实数据模拟 (DEEP 含 ### 三子段 + WIDE 1700 + RADAR 900 + 总览 350) → V37.9.68-hotfix 后产 4 chunks: [1/4] DEEP+header 2132 chars / [2/4] WIDE 1417 / [3/4] RADAR 615 / [4/4] 总览 141 完美匹配 V37.9.68 设计预期。：TestNormalizeThemeKeywords(8 含 stopword 过滤 / 小写化 / 中文 2-gram+3-gram / 多层 prefix strip) + TestThemesOverlap(5 含 anchor 单独命中 / ≥2 共同 / ≥50% 短主题 / 空集) + TestExtractRecentThemes(7 含 14 天默认 / 日期降序 / 缺目录返回空 / 非法文件名跳过 / 缺主题行跳过) + TestFormatBannedThemesBlock(3 含血案 marker 字眼) + TestExtractDeepThemeFromChunk(6 含 V37.9.68 新格式 + V37.4 老格式兼容) + TestSplitWideRadarOutput(6 含 emoji 变体识别) + TestBuildOverviewBlock(5 含 H2 header 守卫 - 依赖 V37.9.21 分窗机制) + TestExtractSectionTitles(7) + TestBloodLessonQwenBimRegression(2 端到端复现 14 天 ban-list 阻止 Qwen-BIM 变体 + Memory Plane 新主题不被错误拦截) + **TestKbDreamShellGuards(12)** kb_dream.sh 源码级守卫: V37.9.68 marker / 14 天窗口字面量 / BANNED_THEMES_BLOCK 注入 / 2 LLM 调用不 3 chunks / DEEP fail-fast 顺序锁 / WIDE+RADAR 独立 [DEGRADED] / 总览段 helper 调用 / status.json schema 升级 / 4 段拼接 / MIN_DEEP_CHARS=1200 阈值 / V37.8.6 反污染守卫在 DEEP+WIDE_RADAR 两 system prompt 都保留 + TestSourceLevelGuards(5 含 V37.9.68 marker + 默认 14 days + overlap ≥2 阈值 + 血案注释 + overview H2 header 守卫) + TestCli(2 含 --show-recent-themes / help)。 |
| `kb_dream.sh` | **V36新增→V37.8.6→V37.9.68→V37.9.68-hotfix 多窗口 4 段独立** Agent Dream v2 MapReduce 全量 KB 探索引擎（Phase1 Map 14源+293笔记逐一提取信号 → Phase2 V37.9.68 三阶推送 (DEEP+WIDE+RADAR+总览) 跨域分析+推送 4 个独立 WhatsApp/Discord 窗口）。**V37.9.68 设计**（用户视角原则 #13 兑现）：(a) PREV_THEMES 3 → **14 天** + Python helper 提取归一化主题关键词作 ban-list 防 Qwen-BIM 类连续几周重复 (b) Phase 2 拆 V37.8.3 chunked 3 段 → **2 LLM 调用**：DEEP_SYSTEM 单主题深挖 (1500 字 + 14 天 ban-list 硬规则 + fail-fast 顺序锁) / WIDE_RADAR_SYSTEM 单调用产 2 段（WIDE 5 跨领域鲜人知 × 200 字 + RADAR 5 准期信号 × 300 字） (c) **总览段**用 build_overview_block() 规则提取不调 LLM (d) **失败降级**：DEEP 必过 (MIN_DEEP_CHARS=1200 不及格 exit 1) / WIDE+RADAR 独立 [DEGRADED] 不阻塞 (e) **status.json schema 升级** multitheme:true + deep_chars + wide_status + radar_status 段独立可观测 (f) **4 段 ## header** 自然复用 V37.9.21 多窗口分片，推送 4 个独立 WhatsApp 窗口。**V37.4 Fix A+B+C** / **V37.4.1 retry** / **V37.4.2 结构+变体**（见下方 changelog）。**V37.8.6 自引用幻觉防御保留**（log→stderr / heredoc _sanitize / file errors='replace' / DEEP_SYSTEM + WIDE_RADAR_SYSTEM 双 system prompt 反污染守卫）|
| `kb_dream_helpers.py` | **V37.9.68新增→V37.9.68-hotfix +9 公开函数** Dream 三阶推送辅助纯函数模块（~470 行，零 I/O at import，9 公开函数）：(1) `normalize_theme_keywords(text)` — 主题归一化为关键词集合（剥 markdown header + 中文 2-gram+3-gram 滑动窗口 + 英文 token 小写+停用词过滤）(2) `themes_overlap(a, b)` — 三层判定（anchor 长 token 单独命中 / ≥2 共同 / ≥50% 短主题），anchor 阈值实测调优：英文复合标识符 (含 hyphen/digit) ≥4 字 / 英文纯字母 ≥8 字 / 中文 ≥3 字 — 避开 "plane" 类 5 字纯字母 false positive (3) `extract_recent_themes(dream_dir, days=14)` — 扫 dream 目录提取最近 N 天主题（含日期降序 + 关键词归一化）(4) `format_banned_themes_block(themes)` — DEEP prompt ban-list 段渲染（含硬规则文字 + "违反整份输出作废"）(5) `extract_deep_theme_from_chunk(chunk)` — 从 DEEP LLM 输出抓主题标题（兼容 V37.9.68 新格式 + V37.4 老格式）(6) `split_wide_radar_output(content)` — 单 LLM 调用 WIDE+RADAR 拆分为两段（识别 🌐/📡 emoji header）(7) `build_overview_block(deep_theme, wide_themes, radar_themes, kb_stats)` — 总览段规则提取（不调 LLM，含明日关注规则模板）(8) `extract_section_titles(section_md, max_n)` — 从 WIDE/RADAR 段抽各子主题标题供总览段 (9) **`split_dream_into_chunks(text, max_chunk=4000)` (V37.9.68-hotfix)** — WhatsApp 多窗口分片纯函数：每个 ## header 段独立成 chunk (不合并小段) + header 段合并到第一个 ## 段作 prefix + 单段超 max_chunk 时按 \\n 内部切分 + 空段静默跳过. **修复 V37.9.68 设计假设错配** (V37.9.66 类别 B): V37.9.68 changelog 说"4 段 ## header 自然复用 V37.9.21 多窗口分片", 但 V37.9.21 实际是"current+piece <= max_chunk 就合并"算法. V37.9.68 4 段大小不均匀 (DEEP ~5000 / WIDE ~1700 / RADAR ~900 / 总览 ~400) 让 WIDE+RADAR (~2600) 被合并成 1 chunk. Mac Mini 5/15 03:00 cron 实测确认: 用户收到 [3/3] 而非 [4/4]. CLI `--show-recent-themes DREAM_DIR` 运维查询。 |
| `gameday.sh` | **V33新增** GameDay 故障演练（5场景：GPU超时/断路器/快照/SLO/Watchdog，`bash gameday.sh --all`） |
| `jobs/dblp/run_dblp.sh` | **V30.5新增** DBLP CS论文监控（多关键词搜索、免费API、每日12:00推送+KB写入） |
| `jobs/hf_papers/run_hf_papers.sh` | **V30.5新增** HuggingFace Daily Papers 监控（热门AI论文、每日10:00推送+KB写入） |
| `jobs/semantic_scholar/run_semantic_scholar.sh` | **V30.5新增** Semantic Scholar 论文监控（引用量排序、每日11:00推送+KB写入） |
| `jobs/acl_anthology/run_acl_anthology.sh` | **V30.5新增** ACL Anthology NLP论文监控（顶会论文、每日09:30推送+KB写入） |
| `jobs/pwc/run_pwc.sh` | **V31新增** Papers with Code 论文+代码监控（免费API、每日13:00推送+KB写入，核心价值：论文+代码仓库关联） |
| ~~`jobs/openreview/`~~ | **V35 已移除**（API 403 post-security-incident，S2 已覆盖顶会论文） |
| `preference_learner.py` | **V30.4新增** 用户偏好自动学习器（从对话历史推断偏好，写入 status.json） |
| `activate_openclaw_features.py` | **V30.5新增** OpenClaw 功能激活脚本（检查+启用 agent 工具配置） |
| `notify.sh` | **V33新增→V37.9.170 微信接入→V37.9.177 队列淘汰→V37.9.179 默认退回 discord→V37.9.180 冷调用去重→V37.9.182 微信机制修正** 统一消息推送（**默认 `NOTIFY_CHANNELS=discord`** + **自动重试3次指数退避** + **失败队列持久化+自动重放**，`source notify.sh && notify "msg" --topic X`）。**V37.9.180**：`_notify_is_coldcall_timeout`（发送+重放两路径）—— 4.27 CLI 冷调用 10s 超时（`gateway timeout after Nms`）= gateway 已投递但 CLI 放弃，按已投递处理（不重试/不入队/不重复），修 WhatsApp cron 推送 3 条重复（2026-06-18 实测）。**V37.9.182 微信机制修正（证伪 V37.9.179「永远做不了」）**：微信客服通道 CLI outbound 无 contextToken，但真闸门是**48h 客服窗口**——窗口内（用户近 48h 有交互）无 token 也可投递，窗口外静默丢失（Mac Mini 日志铁证：22:30 deep_dive cron 无 token outbound 投递成功 + weixin in:29h ago）。操作结论不变（微信对无人值守 cron 不可靠，默认仍 discord/whatsapp，weixin 仅交互+尽力投递）；详见 `cases/weixin_contexttoken_push_blocked_case.md` V37.9.182 修正段。WhatsApp 408 缓解后设 `NOTIFY_CHANNELS="whatsapp,discord"`。**V37.9.176**：weixin 在通道但 WEIXIN_TARGET 空时一次性 stderr WARN。**V37.9.177**：`_notify_drain_queue` 错误类型感知淘汰（永久错误立即淘汰+continue 解 poison 队头阻塞，瞬态保留+break 避雪崩） |
| `kb_harvest_chat.py` | **V37新增→V37.1升级→V37.9.130 层级 Reduce→V37.9.163 尾部残行剥除** 对话精华提炼器 MapReduce 版（从 proxy 捕获的每日对话中 LLM 分段提取+去重合并，零数据丢失，每日 06:00 cron）。**V37.9.130 Reduce 超时血案修复**：层级分批 Reduce（`_reduce_hierarchical` 每批 ≤REDUCE_BATCH_SIZE=4 段多轮归并 9→3→1，替代单次 ~40K 大请求）+ REDUCE_TIMEOUT=300s（镜像 V37.9.129 doubao）+ `_llm_call_with_retry`（Map/Reduce 失败重试 1 次）+ `_mechanical_dedup` fail-soft（LLM 失败机械行级去重兜底零丢失，KB 标 `[REDUCE_DEGRADED]`）+ 轮级熔断（整轮全失败不再下一轮）+ Map 全失败返回 None 不伪装"无关键内容"（原 bug 会 mark_processed 永久丢数据）+ `--days` 默认 1→3（失败日自动补提炼，is_processed 幂等）+ last_run 加 degraded/map_failed 可观测字段（status 枚举不变守 V37.9.72 watchdog 契约）。**V37.9.163（[23](a) 生产观察闭环）**：`_strip_trailing_incomplete_line` 保守剥 max_tokens 截断尾部残行（仅剥类型括号未闭合的残 bullet，完整行含 `]` 绝不误剥，≤1 行不剥 MR-4 不返回空）+ emit 前应用 + stderr WARN 可观测 + `REDUCE_MAX_TOKENS` 常量 2000→3000 降截断频率 |
| `test_harvest_chat.py` | **V37.1新增→V37.9.130扩展→V37.9.163 +13** 对话精华提炼器单测（70 个用例 / 10 类：chunk 分块 8 + prompt 3 + MapReduce 5 + process_date 4 + 加载 4 + 语法 4 + **V37.9.130 新增 29**：TestMechanicalDedup(5 含零丢失契约+全段头 fallback) + TestLlmCallWithRetry(3) + TestHierarchicalReduce(6 含 9 段血案场景 3 次小请求 / batch≤4 守卫 / 单批失败 fail-soft / 全失败轮级熔断零丢失) + TestExtractKeyPointsMetaV130(4 含 Map 全失败≠无关键内容 MR-4 修复) + TestProcessDateDegradedV130(3 含 degraded KB 标记+error 不标记已处理) + TestV379130SourceGuards(8 常量锁定+--days=3+status 枚举契约+旧单次 Reduce 已退役)。**反向验证三层守卫真有效**：sabotage Map 全失败旧行为 → 2 fail；删轮级熔断 → 1 fail；--days 回退 1 → 1 fail。还原后 57/57 全过。**V37.9.130 同时补登历史 debt**：V37.1 起该测试从未注册 full_regression，本次接入。**V37.9.163 +13**：TestStripTrailingIncompleteLineV163（剥尾行 helper 行为 8 含血案 `- [` + 完整行/截断内容不误剥 + process_date 集成 2 含 stderr WARN 可观测 + 源码守卫 3：REDUCE_MAX_TOKENS=3000 / 防硬编码 2000 回退 / V37.9.163 marker） |
| `jobs/ontology_sources/run_ontology_sources.sh` | **V37.1新增** Ontology 专属信息源监控（4 RSS: W3C/JWS/DKE/KBS + 两层关键词过滤 + LLM 中文摘要+要点+价值 + Discord #ontology 推送 + KB 归档，cron 10:00/20:00） |
| `jobs/finance_news/run_finance_news.sh` | **V37.8.2新增→V37.8.5→V37.9.103 冻结≠死亡** 全球财经/政策新闻每日汇总（双通道：8 RSS 直连 + X/Twitter Syndication API，72h 窗口覆盖周末，LLM 结构化分析+价值评级+国内外对比+投资建议+风险提示，cron 07:30）。V37.8.5：僵尸检测导入 `finance_news_zombie.classify_zombie` 模块。**V37.9.103**：parser 捕捉 `newest_age_days`，frozen（快照冻结活账号）分流到 `degraded_${DAY}.txt` 仅 🧊 可观测日志不告警（避免错杀 + 接受退化），ZOMBIE_FILE 只收 stub+dead（真可移除），3 天告警措辞 僵尸→可移除 + 澄清 frozen 不在此列 |
| `jobs/finance_news/finance_news_zombie.py` | **V37.8.5新增→V37.9.103「冻结≠死亡」四档重构** Syndication 僵尸检测纯函数模块。**V37.9.103**：V37.9.102 复盘揭示 Syndication 平台级退化把天天发推的活账号（XHNews/CGTN）快照冻结 6-10 月前，V37.8.5 的 "stale=可移除" 在退化下错杀活账号。新增 `newest_age_days` 用最新推文年龄拆四档：**stub**（0 推文 embed-disabled，Syndication 永久无用→可移除）/ **dead**（newest ≥ DEAD_AGE_DAYS=730 天真死/改名→可移除）/ **frozen**（newest<730 或无法判定 = 快照冻结/低频活账号→**绝不移除**，血案核心）/ **alive**。is_zombie_suspect = stub OR dead，frozen 永远 False。DEAD_AGE_DAYS 与 ai_leaders_rotation.py ZOMBIE_DEAD_DAYS 跨模块一致（test 守卫） |
| `jobs/ai_leaders_x/ai_leaders_rotation.py` | **V37.9.101新增→V37.9.103 frozen/dead 拆分** 账号轮换 + 健康分类纯函数模块。`select_batch`（轮换+环绕，11/run 3 run 覆盖 31，防 429）+ `classify_account` **V37.9.103 五态**（rate_limited 瞬态不误杀 / stub embed-disabled / **frozen 7d<newest<730d 冻结活账号勿移除** / dead ≥730d 真死可移除 / alive）+ is_zombie_suspect（仅 stub+dead）。预防 #18 zombie loop-wiring 将来错杀 karpathy 类冻结活账号 |
| `jobs/ai_leaders_blogs/run_ai_leaders_blogs.sh` | **V37.9.108新增→V37.9.108-hotfix 首跑剪枝** ai_leaders 从 X (429+快照冻结退化) 转向**博客/Substack RSS** 非 X 渠道，收集 AI 大神长文观点（13:30 HKT 1x/天）。11 策展大神 feed 偏重"不同意见"（Marcus on AI/AI Snake Oil 怀疑派 + Import AI/Ahead of AI/Interconnects/Eugene Yan/Karpathy/The Gradient/One Useful Thing/Don't Worry About the Vase/HF），不与 rss_blogs 4 源重叠。紧贴 rss_blogs 已验证管道（RSS + LLM 6 字段 + 🎚️ project_alignment_scorer 项目对齐评分 + hallucination_guards LEVEL_4 + 双通道「🧠 AI 大神观点」+ KB + 多窗口 + 每 feed FAIL-OPEN）。prompt 适配"学者立场/不同意见"。**V37.9.108-hotfix（2026-06-04 Mac Mini 首跑验证）**：9/11 feed 活，fast.ai（Quarto 迁移 RSS 坏）+ Anthropic News（无公开 RSS）404→换高置信 Substack One Useful Thing（Ethan Mollick）+ Don't Worry About the Vase（Zvi）。首跑 45 篇全解析成功 + **🎚️ 高对齐 25/45（⭐≥4）= 发现机会点引擎生效**。替代退化的 ai_leaders_x（后者保持 enabled 避免禁用级联 6 依赖，真停用留 follow-up） |
| `jobs/ai_leaders_bsky/run_ai_leaders_bsky.sh` | **V37.9.111新增→V37.9.111-hotfix 首跑剪枝** ai_leaders 加 **Bluesky 实时短观点维度**（17:00 HKT 1x/天）。**V37.9.111-hotfix（2026-06-05 Mac Mini 首跑）**：7/10 handle 活，3 个 HTTP 400=「Profile not found」handle 漂移（高粉大神迁自定义域名）→ `ylecun`→`yann-lecun.bsky.social`（验证可用）/ `timnitgebru`→`timnitgebru.blacksky.app`（searchActors 确认）/ `fchollet` 停用→剪枝。10→9 账号。首跑 17 帖全解析 + 推 4 窗口 + 🎚️ 高对齐 1 = 管道工作。，补足 blogs 长文（V37.9.108）+ ai_leaders_x（X 429+冻结退化产 ~0）的实时短帖维度。Bluesky 公开 AppView `getAuthorFeed` JSON API **无需认证 + 带缓存**（docs.bsky.app 确认），30 万学者 2023-2025 已迁入（arXiv 2505.24801 + Science）。**仅抓取层**从 RSS XML 换为 getAuthorFeed JSON（`filter=posts_no_replies` + skip reasonRepost + MIN_POST_CHARS=50 过短过滤 + MAX_PER_ACCOUNT=3 + at:// URI→`bsky.app/profile/{handle}/post/{rkey}` web URL + 外链 embed 捕获），**下游全复用 blogs 管道**（call_llm_single_with_retry + parse_6field_output + project_alignment_scorer rule_check + hallucination_guards LEVEL_4 + 双通道「🦋 AI 大神实时观点」+ KB + 多窗口 + per-account FAIL-OPEN + fail-fast）= MR-8。10 大神 contrarian 为主（8 confirmed handles：ylecun/fchollet/melaniemitchell/rao2z/randomwalker/timnitgebru/mmitchell/howard.fm + Gary Marcus/Emily Bender 候选，跨 7+ 流派：世界模型/复杂系统怀疑/LLM 推理怀疑/炒作祛魅/伦理/开源派）。prompt 适配短帖防过度膨胀（V37.9.40 ai_leaders_x 过度分析教训）。handle 可达性待 Mac Mini 首跑验证（原则 #33 + 反馈 #1，per-account FAIL-OPEN 死 handle WARN+跳过+剪枝） |
| `test_finance_news_zombie.py` | **V37.8.5新增→V37.9.103扩** 僵尸检测 32 单测：四档 tier（stub/dead/frozen/alive）+ frozen 血案守卫（XHNews/CGTN 99/99 newest<730→frozen 绝不移除）+ dead 730 边界 + 跨模块 DEAD_AGE_DAYS 一致性 + shell 集成（newest_age_days 捕捉 / degraded_file 分流 / 可移除-冻结前缀）+ 反向验证（frozen→suspect sabotage 立即 fail）|
| `test_wa_gateway_resilience.py` | **V37.8.13新增** Gateway 宕机韧性三层修复 21 单测：TestQuietAlertDiscordAlways(4) quiet_alert 静默期 Discord 穿透 + TestWaKeepaliveEscalation(9) 连续 WARN 升级 + TestRestartGatewayVerification(5) post-bootstrap 健康验证 + TestCrossFileGuards(3) 跨文件一致性 |
| `test_restart_launchd.py` | **V37.9.13新增** restart.sh 架构清理 20 单测：TestRestartVialaunchdHelper(6) helper 函数契约（kickstart -k / bootstrap fallback / 5×2s 健康验证 / return 2 / launchctl 可用性检测）+ TestAdapterViaLaunchd(3) + TestProxyViaLaunchd(2) + TestSingleManagerInvariant(5) V37.9.12.1 双管理血案防线（nohup 必在 fallback 分支 + V37.8.13 Gateway 逻辑保留 + #48703 hotfix 保留）+ TestShellExecutability(3) + TestRuntimeHelperBehavior(1) subprocess 实跑 helper 验证 return 2 |
| `providers.d/` | **V37新增** Provider Plugin 目录（YAML/Python 插件自动发现，`_example.yaml` + `_example_provider.py` 示例）。真插件：`doubao_provider.py`（V37.9.52 第 8，Seed 2.0 Pro）+ `deepseek_provider.py`（V37.9.201 第 9，w4a8-mtp 量化，**V37.9.204 PENDING**）+ `deepseek_full_provider.py`（V37.9.204 第 10，满血版 ai-tokenhub）+ **`doubao_seed_21_provider.py`（V37.9.216 第 11，Seed 2.1 Pro 旗舰）** |
| `providers.d/doubao_seed_21_provider.py` | **V37.9.216新增→V37.9.217 E2E 升 feature_verified** 第 11 个 provider Doubao Seed 2.1 Pro 旗舰（model `doubao-seed-2-1-pro-260628`，火山引擎 Ark 托管，区别于第 8 个 doubao_provider.py 的 Seed 2.0 Pro）。🔴 安全（镜像 doubao 2.0 V37.9.52）：**API key 走 env `ARK_21_API_KEY`** + **endpoint ID 走 env `ARK_21_ENDPOINT_ID`**（Volcengine 的 model 字段接收 endpoint ID；dev 无 env → fallback 公开 model 标识符让合约通过 + `available()` 因缺 key 自动排除）+ **base_url 公开域名可入库**。独立 env（非复用 2.0 的 ARK_API_KEY）。**V37.9.216 接入 declared**（能力声明未实测）→ **V37.9.217 Mac Mini E2E 实测 text/vision/tool_calling/streaming/reasoning 5/5 全通过 → 升 `feature_verified`**：🌟 旗舰质量——bat-ball 答对 0.05（content 正确）+ 完整 reasoning_content 通道（reasoning_tokens=255）+ vision 精准描述（湖面/皮划艇/针叶林/覆雪山脉全命中）+ tool_calls get_weather + streaming chunk+[DONE]，**无乱码 token**（优于 deepseek 量化版 w4a8）。verified_fallback 诚实保持 False（未真生产接管）/ json_mode 声明未单测。已进 fallback 链 `['deepseek_full','doubao','doubao_21','deepseek']` |
| `providers.d/deepseek_full_provider.py` | **V37.9.204新增** 第 10 个 provider DeepSeek-V4-Pro 满血版（model `deepseek-v4-pro-260425`，ai-tokenhub 托管）。定位 = Qwen3 迁移**新候选**，替代被 pending 的量化版 deepseek（w4a8-mtp 偶发乱码）。🔴 安全：**API key 走 env `DEEPSEEK_FULL_API_KEY`**（不入库）+ **base_url `https://ai-tokenhub.com/api/v1` 公开域名可入库**（非裸 IP/token）。诚实（原则 #23）：初始 tier=declared 保守仅 text+streaming。**V37.9.205 Mac Mini E2E 实测 text/tool_calling/reasoning 3/3 → feature_verified**：🌟 满血版**有 R1 reasoning_content 通道**（reasoning 字段 + reasoning_tokens=55，量化版无）+ **无乱码 token**（优于量化版 w4a8）；vision 确认不支持（400）/ json_mode 围栏非严格保持 False / streaming 未单测。⚠️ 推理模型先生成 reasoning 占 token 预算，生成任务须给足 max_tokens。缺 key 时 available() 自动排除 |
| `providers.d/deepseek_provider.py` | **V37.9.201新增** 第 9 个 provider DeepSeek-V4-Pro（用户自建 OpenAI 兼容推理端点）。🔴 公开 repo 安全底线（镜像 doubao V37.9.52）：**API key 走 env `DEEPSEEK_API_KEY`**（明文 key 不入库）+ **base_url 走 env `DEEPSEEK_BASE_URL`**（用户端点是裸 IP + 路径 token = 类机密，公开仓库不入库；dev 无 env → 公开 fallback `https://api.deepseek.com/v1` 让合约通过 + `available()` 因缺 key 自动排除）。诚实语义（原则 #23）：初始 `verification_tier=declared` + verified_* 全 False。**V37.9.202 Mac Mini E2E 实测 text/streaming/tool_calling 3/3 通过 → 升 `feature_verified`**；**V37.9.203 能力探针实测**：+json_mode（declare True，response_format E2E 通过）/ vision **确认不支持**（400 非多模态，部署=`DeepSeek-V4-Pro-w4a8-mtp`）/ reasoning **无 R1 reasoning_content 通道**（reasoning:null，content 内能 CoT）。⚠️ 质量：w4a8-mtp 量化推理响应偶发乱码 token（替换 Qwen3 须权衡）。reasoning/vision/fallback 诚实保持 False |
| `docs/provider_plugin_guide.md` | **V37新增** Provider Plugin Extension Guide（60秒添加新 Provider，合约验证，YAML/Python 双模式） |
| `examples/minimal_consumer/` | **V37.9.104新增→V37.9.107 chunk-3a +convergence** ontology-engine 包化第一个真消费方 demo（虚构 WeatherBot Agent Runtime，工具/域/策略/不变式全部不同于 bridge）。`weatherbot.py`（被审计代码）+ **`weatherbot_state.json`（V37.9.107 convergence 源文件）** + `run_demo.sh`/`run_demo.py`（设 ONTOLOGY_CONFIG_DIR+ONTOLOGY_PROJECT_ROOT，练习 ToolOntology/find_by_domain/evaluate_policy/governance run_all **+ V37.9.107 convergence** 五能力，端到端验证 config-injection）+ `ontology/` Layer 2 YAML（tool/domain/policy/governance **+ V37.9.107 convergence_ontology.yaml**）+ `README.md`。引擎的"Doubao 时刻"（真消费方接入才证明可扩展）。 |
| `examples/minimal_runtime/` | **V37.9.144新增** 外部评审2 P1(b) 10 分钟最小 Core Runtime 入口 demo（与 minimal_consumer governance 消费方 demo 互补构成"最小例子+完整生产例子"双入口）：`minimal_runtime.py`（1 provider capability 路由+fallback 链 + 24→12 工具治理（白名单拒绝/硬截断/自定义注入三类语义分开展示）+ 1 policy 查询（无 PyYAML 优雅降级=依赖边界活演示）+ SLO mini-stats + **golden trace 自校验**（MATCH=跨机 control plane 行为一致的可复现证据，--write-golden 维护者重生成））+ `golden_trace.json`（确定性决策参考，时延只进 non_deterministic 不参与比对）+ `README.md`。`test_minimal_runtime.py` 15 单测守卫（端到端/golden schema/无 yaml 环境模拟/sabotage golden 篡改必 exit 1/主 README 双入口链接） |
| `examples/external_dogfood/` | **V37.9.148新增** 外部评审2 P2(c) 仓库外 dogfood — 把 V37.9.137 一次性 PyPI 冒烟升级为 CI 回归守卫（证发布的 `openclaw-ontology-engine` 可被第三方零 monorepo 访问消费，闭合评审"仓库外独立 dogfood" gap）。`run_dogfood.sh`（离线 build wheel 三方法链 → 隔离 `--system-site-packages` venv 装 wheel `--no-deps`，无 PYTHONPATH 回 monorepo → 全新 LibraryBot toy 复制出仓库到 /tmp 跑 → config-injection 审计 + 反向验证无注入读 wheel bundled 默认）+ `project/`（LibraryBot 第三方消费方：`librarybot.py` 被审计源 + `librarybot_state.json` convergence 源 + `run_demo.py` **用 `import ontology_engine` 去泛化名**练 5 能力 + `ontology/` 5 个 Layer 2 YAML，域全异于 bridge/WeatherBot）+ `README.md`（与 minimal_consumer 的 wheel-vs-PYTHONPATH 差异表）。`test_external_dogfood.py` **17 单测**（source guards 恒跑 + wheel E2E `skipUnless` 工具链 setUpClass 一次性 build+venv+install + sabotage 反向验证 toy 源破坏 audit 检出）。与 minimal_consumer（editable/PYTHONPATH）的关键差异：装 wheel + `import ontology_engine`（chunk-2-lite 去泛化对真实消费方生效的唯一验证）。 |
| `docs/ontology_engine_extension_guide.md` | **V37.9.104新增→V37.9.107 chunk-3a 更新** Ontology Engine Extension Guide（60秒接入：pip install → 写自己 YAML → export 双 env → openclaw-ontology-query/audit）。镜像 provider_plugin_guide.md。含 5 种 check_type 参考 + YAML 正则转义陷阱 + **"Config-injected phases"（三阶段 invariant/MRD/convergence 全 config-injected，含写 convergence spec 指南）+ chunk-3b follow-up（MRD 文件名模式参数化）诚实登记**（chunk 3a 已修 convergence 路径耦合）。 |
| `test_ontology_extension_demo.py` | **V37.9.104新增→V37.9.107 +3** chunk 4/3a 端到端守卫（**15 单测 7 类**）：run_demo.sh 真 subprocess + 注入加载 demo 配置非 bridge + **反向验证（不注入 → 引擎读 bridge，证明 demo 通过依赖注入）** + demo↔bridge 工具集相异 + governance run_all 审计 demo 全过 + **TestConvergenceConfigInjectionDemo(3 V37.9.107：注入读 WeatherBot convergence spec 非 bridge / 反向验证不注入读 bridge 5 specs / verify_convergence 零 drift 源文件经 PROJECT_ROOT)** + 源码守卫。 |
| `ontology/docs/architecture/industrial_ai_paradigm.md` | **V37新增** 工业AI范式文档（三平面架构+五项工业需求+范式对比，切断 Dream→PA 链式幻觉） |
| `ontology/docs/architecture/target_architecture.md` | **V37.1新增** Ontology 终态架构（四层设计+六域概念模型+策略引擎+三阶段门控+迁移路径 Phase 3-5） |
| `ontology/docs/cases/pa_echo_chamber_case.md` | **V37.1新增** PA 迎合性回复案例分析（三环反馈陷阱根因+SOUL.md 批判性规则修复+Phase 4 结构性修复路径） |
| `ontology/docs/cases/dream_quota_blast_radius_case.md` | **V37.2新增** Dream MapReduce 配额耗尽跨 Job 爆炸链案例（30+ LLM 调用 × Qwen3 宕机 → Gemini 配额耗尽 → HN 垃圾推送，三层修复+2 新不变式） |
| `ontology/docs/cases/reasoning_model_primary_breaks_batch_jobs_case.md` | **V37.9.220新增** reasoning 模型作 primary 拖垮批量 cron job 事故（doubao_21 primary 切换 → 批量分析 job reasoning 延迟 7-9 分钟 → job client 超时 broken pipe → adapter 级联全 fallback 失败 → 502；PA 短请求正常掩盖 = MR-4 silent-failure 家族 / fail-plausible 变种）。回滚 PROVIDER=qwen 恢复（Plan B）。三层根因 + 因果链 + 迁移真门槛（capability router 分流 batch job）+ 元教训（provider 迁移必须按 workload profile 验证 / reasoning tokens 不受 max_tokens 约束）。LLM-Observer taxonomy 候选 |

## 版本变更历史

> 近期版本变更见下表（V37.9.207+）；**V37.9.206 及更早 268 个版本完整历史见 [`docs/changelog.md`](docs/changelog.md)**（按需 read，已移出每-session-必读路径；2026-06-17 首轮归档 190 版 + 2026-07-05 第二轮归档 78 版）。

| 版本 | 日期 | 关键变更 |
|------|------|----------|
| V37.9.251 | 2026-07-06 | **B3 收尾 — PA 硬编码全仓漂移守卫（`test_pa_coupling.py` 升级：拦截任意运行时文件新写 `/Users/<真实用户名>` 个人路径 + 裸 TZ，兑现 charter B3 done-criteria「新增 PA-specific 硬编码有 scanner 拦截」，用户「继续执行完 B3」，VERSION 不变）** — ① **触发**: V37.9.249/250 落地 B1 盘点 + 第一批 config 化（A1/D3/A2）后，用户问「今天可继续的待办」→ 我诚实分诊（27 项 unfinished 绝大多数 Mac-Mini/生产/评审 gated）→ 唯一实质 dev 项 = B3 done-criteria 的「scanner 拦截」未兑现（既有守卫只盯已修 4 文件，未防其他文件新写 PA 硬编码）→ 用户「继续执行完 B3」。② **设计选择（日落法 #34 复用而非新机器）**: 两方案——重量版独立 `pa_coupling_scanner.py`（对齐 cross_os_quirk_scanner 框架）vs 轻量版扩既有守卫为全仓扫描。选**轻量版**：现有 PA 硬编码点为**零**（A1/A2 已修，D3 已 config 化），无「扫描-修复」循环需求，造独立扫描器模块 = 为零违规造新机制（违复杂度预算）；既有 `test_pa_coupling.py` 已含 `_runtime_shell_scripts()` + 全仓 TZ 扫描，扩一个 `_runtime_code_files()`（py+sh）+ 个人路径正则即闭合。③ **交付**: `TestNoPersonalPathHardcode`（2 单测）扫**全部 122 个运行时 .py+.sh**（排除 test_ fixture），正则 `/Users/([name])` 命中非示例名（foo/test/user 等豁免）即 violation → 应改 `$HOME`/`expanduser("~")`；含非空验证（≥80 文件防 glob 瘫痪 vacuous pass）。**先验证再编码（原则 #28）**: dev 实跑正则确认现仓 122 文件 0 violation（bisdom 已修 / foo 示例豁免 / test fixture 排除）。④ **守卫总量 7→9 单测 5 类**（A1 KB 路径 + D3 TZ×2 + A2 diagnose + **B3 个人路径×2** + marker）。**sabotage 反向验证守卫真有效**: 注入 `/Users/bisdom/.kb` 到 kb_status_refresh.sh → 精确抓 `kb_status_refresh.sh:319 /Users/bisdom` FAIL，byte-identical 还原 9/9 全过。⑤ **诚实边界**: 号码作默认值维度未建守卫（category B 已全 `${OPENCLAW_PHONE:-…}` GREEN，零违规先例，不预防性建机器 日落法）；未来若真有多贡献者或号码硬编码先例再评估重量版扫描器。⑥ **验证**: full_regression 153/0/5457 (+2) / 治理 91/91 EXIT=0 / 安全 95/100 / 零泄漏 / **VERSION 0.37.9.101 不变**（守卫扩展零 runtime，同 V37.9.249/250 惯例）。**复杂度预算账本**: files 不变（扩既有 guard，**刻意不造新 scanner 模块**）/ env/jobs/runtime-state-sources 不变 / tests +2 / **净 runtime 机制 0**（全仓 CI 拦截是既有守卫的扫描面扩展，非新接缝；轻量版明确退役了「造独立 pa_coupling_scanner.py」的选项）。⑦ **🔴 Mac Mini 待用户**: git fetch+reset 同步（纯测试守卫，零重启零业务影响）。⑧ **B3 完成 → H1-B（PA 解耦第一批）全部交付**: B1 盘点（V37.9.249）+ B2 backlog 分级（inventory §4）+ B3 config 化落地 A1/D3/A2 + scanner 拦截（V37.9.251）。剩余 PA 耦合（C1 launchd/C2 homebrew/D2 中文 prompt）按 §4 归 H1-C 或边界内延后。**元价值**: 「继续执行完 B3」在诚实分诊「大部分 gated」后，把唯一实质 dev 项做到收尾——且用轻量版守住日落法（零违规不造扫描器框架），H1-B 一个季度任务在一天内 B1→B2→B3 全链闭合。 |
| V37.9.250 | 2026-07-06 | **PA 耦合 config 化第二批 — D3 TZ config 化（35 脚本 87 处 `TZ=Asia/Hong_Kong` → `TZ=${SYSTEM_TZ:-Asia/Hong_Kong}`）+ A2 diagnose openclaw home 路径可移植（用户「继续全部执行」，接 V37.9.249 B1，全部 Mac Mini 生产 no-op，VERSION 不变）** — ① **触发**: V37.9.249 B1 盘点交付后用户指令「根据建议继续全部执行」= 落地我提议的下一批低-blast-radius config 化（inventory §4 backlog 的 A2 + D3）。② **D3 TZ config 化（先理解再动手，原则 #28）**: 机器扫描确认 35 个运行时 `.sh`（root + jobs/，排除 test/docs）共 87 处 `TZ=Asia/Hong_Kong`，**全部为 `$(TZ=... date ...)` 命令替换上下文**（risky-context grep 单引号/heredoc 字面量 = 空 → 参数展开安全，无误伤）。sed 统一 `TZ=Asia/Hong_Kong` → `TZ=${SYSTEM_TZ:-Asia/Hong_Kong}`（默认仍 HKT → HK 用户 no-op；`.env_shared` 设 `SYSTEM_TZ` 全局覆盖，job 脚本已 `source .env_shared` plumbing 就位）。修潜伏可迁移性 bug：非-HK 实例此前所有 date 强制 HKT → 跨午夜日期命名错位（V37.9.241 曾登记 observer 按日期读文件错位）。**连带更新 2 处测试字面量断言**（test_kb_deep_dive:858 / test_kb_evening:705，V37.9.131 alternation 惯例）。③ **A2 diagnose 路径**: `diagnose.sh:77` `/Users/bisdom/.openclaw/openclaw.json` → `$HOME/.openclaw/openclaw.json`（在 `python3 -c "..."` 双引号 shell 串内 `$HOME` 正常展开）。**不引入新 env**（`$HOME/.openclaw` 对齐代码库 40 个 `expanduser("~/.openclaw")` 惯例，日落法：不为 ops 工具造 OPENCLAW_HOME 新 env）。④ **守卫演进**: `test_pa_coupling_kb_paths.py` → `test_pa_coupling.py`（git mv 扩展，7 单测 4 类）: A1 KB 路径 + **D3 无裸 TZ + config 化广度 ≥30 非空验证**（防 vacuous pass）+ A2 diagnose 路径。run_suite 描述同步 + dark-test guard（V37.9.241）核对通过。**sabotage 反向验证守卫真有效**: 回退 job_watchdog TZ + diagnose 路径 → 2 FAIL，byte-identical 还原 7/7 全过（首轮抓出自己一个假阳性：guard 误把 full_regression.sh run_suite 描述里的 `\${SYSTEM_TZ` 文本当 TZ 用法 → 改为精确全串计数修正）。⑤ **验证**: full_regression 153/0/5455 (+3) / 治理 91/91 EXIT=0 / 安全 95/100 / 零泄漏 / bash -n 35 脚本全 OK / **VERSION 0.37.9.101 不变**（三项全生产 no-op，job/工具级 TZ+路径可移植收敛，镜像 V37.9.241/249 惯例）。**复杂度预算账本**: files 不变（扩既有 guard + inventory）/ env **+1（SYSTEM_TZ，占位默认 Asia/Hong_Kong，未设时 no-op）** / jobs/runtime-state-sources 不变 / suites 不变（重命名非新增）/ tests +3 / **净 runtime 机制 0，净 -1 一物一形隐患**（TZ 从「87 处硬编码个人时区」收敛为「config 化默认 + 单点 env 覆盖」）。⑥ **诚实边界**: 剩余 backlog（inventory §4）C2 /opt/homebrew PATH（无害附加，日落法不为它造机制）/ C1 launchd（归 H1-C 第二实例，非纯 config 化）/ D2 中文 prompt（边界内延后）。⑦ **🔴 Mac Mini 待用户**: git fetch+reset 同步（35 脚本 + diagnose 走 FILE_MAP / job auto_deploy，零重启；SYSTEM_TZ 未设 → 生产行为不变）。⑧ **元价值**: B1 盘点当天即落地第一批全部 config 化（A1+D3+A2），把「PA 耦合深」从清单升级为「已收敛可枚举的三批 + 定向守卫防回退」；D3 是最大一刀（87 处）但机械安全（全命令替换上下文 + 精确守卫），SYSTEM_TZ 单点 env 让第二实例时区一次配置全局生效。 |
| V37.9.249 | 2026-07-06 | **PA 耦合机器化盘点 — `docs/pa_coupling_inventory.md`（H1-B / B1，纲领 §4.3「边界管理」哲学落地，Q3 任务；含第一批 config 化 A1 落地 kb_*.sh 默认路径可移植收敛，VERSION 不变）** — ① **触发**: 周一开工健康基线全绿（153 suites / 5448 tests / 0 fail / 治理 91/91 EXIT=0 / 分支==origin/main #813 / OpenClaw latest 仍 2026.6.11 无新 stable → 沿用 V37.9.244 第六次 hold 决策，仅 beta.1→beta.2 补丁）。从执行计划 §1 本季度四任务（A1 Observer flip/B1 PA 盘点/D1 SLO 重生成/A4 论文#2 数据）中，A1/D1/A4 均 Mac-Mini 或 in-review gated，**B1 是唯一无门控、纯 dev-actionable 的 P0·Q3 任务**，且直击两次外部评审共同点名短板（个人 PA 耦合深/可迁移性不足）。② **盘点方法（机器扫描非人工印象）**: 四类别各自 grep 命令（个人路径/占位号码/macOS 假设/中文 prompt+TZ），排除 docs/test 聚焦运行时代码。③ **关键正面证据（先验证边界外）**: `examples/`（minimal_runtime/minimal_consumer/external_dogfood）+ ontology 引擎核心扫 `/Users\|bisdom\|+852\|Wei\|三方宪法` **零命中** = **可抽取产品（引擎/bench/demo）已 PA-free**（纲领 §4.3「边界外零 PA 依赖」已达成，可迁移性批评的一半反驳）。④ **结论**: 系统约 **80% 耦合已 env 参数化**（占位默认值 + env 覆盖，号码/推送目标/40 个 Python `expanduser` 路径/md5 fallback 全绿），残余耦合集中 4 点分级：🔴高 C1 launchd 进程管理（第二实例真门槛，`minimal_runtime` 不依赖它可增量）/ 🟡中 A1 KB 路径默认·D2 内容中文 prompt·D3 TZ=HKT / 🟢低 A2 diagnose 路径·C2 /opt/homebrew PATH（Linux 无害附加）·D1 SOUL 身份（已 config 部署）。⑤ **第一批 config 化落地（B3 最低 blast-radius demo）**: `kb_write.sh:3`/`kb_search.sh:13`/`kb_inject.sh:12` 默认 `${KB_BASE:-/Users/bisdom/.kb}` → `${KB_BASE:-$HOME/.kb}`——**Mac Mini 上 `$HOME=/Users/bisdom` → 生产逐字节不变**，非-Mac 从坏路径变正确路径，同时消除一物一形违反（对齐 15+ job 脚本 + auto_deploy FILE_MAP 已用 `$HOME`）。⑥ **守卫**: `test_pa_coupling_kb_paths.py`（4 单测源码守卫，断言 3 脚本用 `$HOME/.kb` 默认 + 不含 `/Users/bisdom` 硬编码）注册 full_regression（V37.9.241 dark-test guard 核对通过）。**sabotage 反向验证守卫真有效**: 改回 `/Users/bisdom` → 3 FAIL，byte-identical 还原全过。日落法：定向守卫非整个新扫描器（B3 才框架化）。⑦ **既有扫描器信用登记**: cross_os_quirk/cross_env_path/path_consistency 守 OS-quirk bug 与路径漂移，**缺口 = 无扫描器守「PA-specific 硬编码默认值」**（B3 待补）。⑧ **验证**: full_regression 153/0/5448 (+4) / 治理 91/91 EXIT=0 / 安全 95/100 / 安全扫描零泄漏 / bash -n 3 脚本 OK / **VERSION 0.37.9.101 不变**（KB 路径默认可移植收敛 = 生产 no-op job/工具级，镜像 V37.9.241 TZ 一物一形惯例）。**复杂度预算账本**: files +2（inventory doc + guard test）/ env/jobs/runtime-state-sources 全不变 / suites +1 / tests +4 / **净 runtime 机制 0，净 -1 一物一形违反**（KB base 默认从「$HOME/.kb（job）vs /Users/bisdom（工具）」双形态收敛为单一 `$HOME/.kb`）。⑨ **🔴 Mac Mini 待用户**: git fetch+reset 同步（kb_write/search/inject 走 FILE_MAP，零重启，生产行为不变因 $HOME=/Users/bisdom）。⑩ **元价值**: 把两次评审的「PA 耦合深」从声明式承认升级为逐项、可分级、可追踪清单——最重要发现是边界外已 PA-free（可迁移性底座就位），残余耦合可枚举，最脏的第二实例门槛是 launchd（非无处不在的个人痕迹）；B1 盘点同步落地最安全的一刀（A1 生产 no-op）证明 config 化模式可行。剩余按 §4 backlog 分级排期（多数 Q4 或边界内延后）。 |
| V37.9.248 | 2026-07-05 | **纲领执行计划确立 — `docs/charter_execution_plan_20260705.md`（技术纲领配套运行手册，用户两步指令：先生成具体可执行计划 + 再落地每季度五项固定复核，纯执行文档零业务语义 VERSION 不变，同日第四交付）** — ① **触发**: 用户「根据技术纲领严格生成具体可执行计划（第一步）+ 落地每季度五项固定复核（第二步）」。② **文档定位（一物一形）**: 纲领 §5/§8/§10 是「方向与判据」，本文档是「怎么执行 + 每季度怎么复核」，不复制纲领判断只落成可跑任务/命令；纲领 §11.1 分工表 + CLAUDE.md 战略定位区双向注册指针。③ **Part 1 具体可执行计划**: §0 **执行基线快照**（复杂度四维度机械采集 files 286/env 43/jobs 40-46/state 25 + 徽章事实 tests 5448/checks 839/providers 11 + 8 门控当前态 + 影响力指标诚实标「待采集」）+ §1 **H1 地平线 15 任务分解**（H1-A Observer 收官 A1 flip[G1]→A2 精度→A3 回灌 bench→A4 论文#2数据 / H1-B PA 解耦 B1 盘点→B2 backlog→B3 低风险 config 化 / H1-C 第二实例 C1 前置[G2]→C2 跑通→C3 portability report / H1-D 证据刷新 / H1-E 年度复盘，每任务带目标/交付物/done-criteria/依赖门控/优先级/目标季度）+ §2 H2/H3 粗粒度里程碑（门控驱动不过度规划避免「计划成复杂度源」）+ §3 依赖链 ASCII 图。④ **Part 2 每季度五项固定复核可运行协议（🔴 日落法自约束：零新增常驻机器，不写 quarterly_review.py，复用 gen_readme_badges/governance/git/公开 API，记录落 status.json，刻意无守卫测试 MR-22）**: R1 影响力（采集命令 pypistats/S2/GitHub + **dev 探测实证采集环境说明**：S2/PyPI 从 dev 不可达+GitHub 需连 App→须 Mac Mini/账号侧跑 + 诚实复盘规则连续两季零增长触发反思）/ R2 复杂度（四维度只读命令块 + 对每个净新增机制问「退役了什么/是否新接缝」）/ R3 判据门控核对（G1-G8 逐门信号源表 + 「条件全满足才推进」规则）/ R4 纲领增量修订（只追加修订记录不改写历史判断）/ R5 反目标自查（6 条逐条问题）+ §10 copy-paste 记录模板 + §11 2026 Q3 首次实例预填（基线已采 + Q3 交付清单对应 A1/B1/D1/A4）。⑤ **诚实边界（原则 #23）**: 外部影响力指标（arXiv 引用/PyPI 下载/GitHub star）dev 环境探测证实不可达→绝不编造数字占位，命令+「待采集」标注由真实账号采集填「下季度增长率分母」。⑥ **复杂度自证（元）**: §11 Q3 R5 预判反目标 #6（纲领膨胀）自查——本执行计划退役的是「落实建议散落纲领 §10 无法直接执行」的状态，落成可跑命令 = 可见性提升零常驻机器 = 净负复杂度。⑦ **验证**: full_regression 152/0/5448 全绿 / 治理 91/91 EXIT=0 / 三 doc-drift 无漂移 / 安全扫描零泄漏（执行计划无 key/号码/内网 IP，命令块仅公开 API 端点）/ **VERSION 0.37.9.101 不变**（纯执行文档零业务语义，同 V37.9.246 纲领惯例）。**复杂度预算账本**: files +1（执行文档非 runtime 接缝）/ env/jobs/runtime-state-sources/suites/tests/checks/VERSION 全不变 / **净 runtime 机制 0**（协议明确零新增常驻机器，用可见性执行）。⑧ **Mac Mini 待用户**: git fetch+reset 同步（纯 doc 零重启）；每季度末照 Part 2 协议跑一次五项复核（首次 2026 Q3 末，基线已在 §0）。⑨ **元价值**: 把纲领从「方向文档」补齐为「方向 + 可执行任务 + 可复核协议」的完整闭环——五项固定复核从纲领 §10 的一句话清单落成带命令/判断问题/记录位置/模板的运行手册，且协议以日落法自约束（零新机器）示范「复核制度本身不成为复杂度来源」。 |
| V37.9.247 | 2026-07-05 | **INV-CONVERGENCE-INTEGRATION-001 负载超时硬化 — 07:00 治理 💥 再现触发预注册判别器（V37.9.232 unfinished [23](a) 兑现，按 V37.9.214 模式，VERSION 不变，同日第三交付）** — ① **触发**: 用户转发 07-05 07:00 `[SYSTEM_ALERT] Governance Audit 失败` — INV-CONVERGENCE-INTEGRATION-001 两 runtime check 💥 + 08:30 watchdog CORE「治理审计: 异常状态 (fail)」+ chaspark 418 / NBER 403 两条外部告警。② **诊断（原则 #28 理解再动手，非盲修）**: Q1 之前存在 ✓——V37.9.232（07-03）已诊断此 💥 = 两 runtime check 各 spawn 一个 **FULL 内层 governance_checker**（~758 checks + convergence specs）、07:00 cron 风暴下内层被 CPU 饿死超 `timeout=120` → `subprocess.TimeoutExpired`（非 AssertionError → 执行器 line 190 归为 error → 💥）；**当时预注册**「再现 → 按 V37.9.214 模式硬化」写进 unfinished [23](a)。Q2 非新回归——07-03 见、07-04 干净（V37.9.243 晨绿）、07-05 又现 = 间歇负载诱发（静时重跑全绿）；执行器 `_exec_python_assert` 无外层超时（line 186 裸 exec），内层 `subprocess.run(timeout=120)` 就是 TimeoutExpired 来源。Q3 最小修复 = 镜像 V37.9.214 INV-REVIEW-001 同款 load-timeout 硬化。③ **修复（两 runtime check 对称）**: `proc = None` + `try: subprocess.run(..., timeout=240)` + `except subprocess.TimeoutExpired: print(INCONCLUSIVE, stderr)` + 全部断言移入 `if proc is not None:` 门控（超时 → proc=None → 断言跳过 = inconclusive 非 💥）。timeout 120→240 容忍 ambient 负载降触发频率；真 hang（>240s）仍经 job_watchdog last_run + stderr WARN 浮现。**回归检测逻辑逐字保留**（只是移进门控内）——subprocess COMPLETES 时全部原断言照跑，convergence-wiring 真回归（缺 key / spec≠5）仍快速 manifest。④ **验证（dev 不可跑 requires_full check → 显式补验）**: YAML 解析 OK + dev 治理 EXIT=0 91/91 + **两 check 显式 compile（requires_full 让 dev 跳过，语法错会潜伏到 Mac Mini 07:00 才炸）** + 行为模拟 TimeoutExpired→proc=None→断言跳过零异常。⑤ **8 新守卫单测** test_cron_monitor_fatal_handler.py TestV37_9_247_ConvergenceCheckLoadHardening（50 测试，+8）: INV 找到含恰 2 runtime check / timeout=240 / 旧 120 退役 / catch TimeoutExpired+INCONCLUSIVE / `if proc is not None:` 门控 / marker / **compile 守卫**（dev 跳过的语法漂移 CI 化）/ **gate 保留回归检测行为验证**（timeout→clean + completed-bad-output→断言仍 fire 双向证门控不阉割真检测）。**sabotage 反向验证守卫真有效**: 退回一处 timeout=120 → 2 FAIL（bump 守卫 + 旧值退役守卫），备份还原 byte-identical → 50/50 全过。⑥ **其余告警分诊（不盲改外部波动）**: watchdog CORE 治理 fail = 07:00 💥 downstream（读 stale flag），V37.9.247 修复后下次 audit 转 pass 自愈；**chaspark 418** = 外部反爬/限流（原则 #33 chaspark 波动史）单次非系统性，per-source FAIL-OPEN 已处理不改；**NBER 403** = finance_news 8 RSS 源之一，`WARN: NBER 跳过` = 正确 per-source fail-loud 降级（V37.9.227 仅全源失败才 fetch_failed，其他源 new>0），连续 2 天 403 登记低优观察（持久化才评估换源，不现在加机器 原则 #8）。⑦ **验证**: full_regression 153/0/**5448 (+8)** / governance 91/91 EXIT=0 / 三 doc-drift 无漂移 / 安全 95/100 / 安全扫描零泄漏 / **VERSION 0.37.9.101 不变**（治理 runtime check 负载鲁棒性 = 非核心 runtime，镜像 V37.9.214/232/162 惯例）。**复杂度预算账本**: files 不变（扩既有 yaml check + 既有 test）/ env/jobs/runtime-state-sources/suites 全不变 / tests +8 / **净 runtime 机制 0**（负载鲁棒性修复 = 退役「环境负载超时误报 💥」噪声面，非新接缝；两 check 昂贵重复 spawn 全量内层 audit 登记为更低优观察不现改，会弱化集成验证深度）。⑧ **🔴 Mac Mini 待用户**: 合并 + git fetch+reset 同步（governance_ontology.yaml 走 FILE_MAP）→ 下次 07:00 治理审计 INV-CONVERGENCE-INTEGRATION-001 在负载下不再 💥（240s + inconclusive catch）+ watchdog 治理 CORE 告警随之消失。⑨ **元价值**: 预注册判别器（V37.9.232 写「再现→硬化」于 unfinished）在第二次再现时精确兑现——不是每次 💥 都当新问题从头查，而是首次诊断即预注册决策规则 + gated 在再现信号上，第二次触发直接执行。这是「判据门控决策法」（技术纲领 §7-4）在事故处理层的实例：07:00 治理 flake 的 cold-call（V37.9.157）+ load-timeout（V37.9.214 INV-REVIEW / V37.9.247 INV-CONVERGENCE）两条负载敏感 runtime check 家族收尾。 |
| V37.9.246 | 2026-07-05 | **中长期技术纲领确立 — `docs/technical_charter_20260705.md`（2026H2→2028 架构与业务规划，用户指令"结合趋势与市场做中长期规划并长期保存为技术纲领"，纯战略文档零业务语义 VERSION 不变，同日第二交付）** — ① **触发**: 用户要求结合未来技术趋势 + 市场潜在需求 + 当前进展，产出中长期架构与业务规划作为长期保存的技术纲领。② **文档定位**: 上承 `strategic_review_20260403.md`（导师 12 个月 V1/V2/V3 路线——V1/V2 已完成、V3 收口进纲领），覆盖 2026H2→2028；与既有文档一物一形分工（纲领管方向与判据 / status.json 管执行态 / CLAUDE.md 管操作手册 / design doc 管研究细节，引用不复制）。③ **三条元约束先于内容**: 日落法适用于纲领本身（每个方向必答"退役什么"+ 判据门控替代日历承诺防"计划成为复杂度来源"）/ 一物一形 / 诚实（趋势全部标注置信度 + 不编造市场数字 + 负面结果可发表）。④ **核心战略判断**: (a) **护城河 = 纵向证据语料库非代码**（8+ 月连续记录 28+ postmortem 带因果链 + 91 不变式各绑血案 + sabotage 验证防御，全球唯一，随 agent 可靠性成为行业痛点而增值）(b) **10 条趋势判断**（T1 agent 基础设施分层/T2 SRE-for-AI 成瓶颈/T3 per-request reasoning 控制成路由维度（V37.9.220-222 已实证领先）/T8 LLM-as-judge 走向 runtime observer 等，T1+T2+T8 构成 24 个月顺风主航道）(c) **fail-plausible 是潜在品类定义词**，~1-2 年窗口期内是个人成为该细分"the person"的机会，研究输出 2027 年内完成是硬节奏（R8）(d) **PA 角色重定义**回应两次评审"个人耦合"批评: 不杀 PA 追求纯净框架，PA=长寿活体实验室（语料来源）+ 引擎=可抽取产品（config-injection 唯一抽包路径），"一个系统两种产出：实验室与引擎"。⑤ **双轨战略**: 研究轨（Reliability Science: taxonomy 已发表→Observer 预测性检测→论文#2/#3→社区标准）× 工程轨（Portable Governance Plane: 引擎已发布→PA 解耦/第二实例→引擎家族→参考架构），两轨互为燃料。⑥ **三地平线（每 H 带退役清单）**: H1（2026H2）收敛与解耦——Observer 收官（宪法级最高优先）+ PA config 化第一批（明确不做目录重排/3-engine-merge）+ **第二实例 PoC（Linux 容器最小闭环 = 可迁移性从声明到实证的终局回答）**+ 复杂度年度复盘；H2（2027）产品化与生态——engine 1.0 + bench 社区化 + observer-engine 抽包（G3 需求信号门控）+ 论文#2 投稿；H3（2028）标准与影响力——参考架构白皮书 + 教学/商业化选项（G4 门控）。⑦ **反目标清单**（与做什么同等重要）: 不做通用 agent framework / 不追协议每班车 / 不做 UI/托管 / enforcement Phase D 永久 human-approval / 不假设团队资金 / 纲领自身不膨胀。⑧ **四业务线**: B0 个人智能中枢为根（活体实验室 + 用户第一 KPI"自己都不好用没资格谈治理别人"）/ B1 研究话语权（影响力漏斗: 论文被引→bench 被跑→引擎被装→inbound，季度指标诚实复盘两季度零增长触发反思）/ B2 开源生态（刻意的小: 回 issue+收 PR+写文档）/ B3 商业化选项（G4/G5 双门控不承诺）。⑨ **8 判据门控**（G1 Observer flip §9.1 / G2 第二实例 / G3 observer-engine / G4 商业化 / G5 团队化 / G6 OpenClaw 升级 §17 / G7 MCP 兼容 / G8 engine 1.0 semver）+ 7 贯穿性技术决策（零依赖核心/config-injection 唯一抽包路径/声明式治理三角/判据门控决策法/sabotage 验证/三步跃迁机器化/上游 tripwire 化）+ 8 风险登记（含 R3 Observer 研究失败=负面结果可发表 / R8 窗口期错失=2027 硬节奏）。⑩ **落实建议季度粒度**: 2026Q3 = Observer flip + PA 耦合机器化盘点（产出 pa_coupling_inventory.md + backlog 分级）+ SLO 报告刷新 + 论文#2 数据口径预注册；Q4 = 第二实例 PoC + 论文#2 草稿 + engine 0.2.0 + 年度复杂度复盘；2027H1 = 论文#2 投稿 + bench 推广 + G3/G8 评估；每季度固定动作五项（影响力指标/复杂度小结/门控核对/纲领增量修订/反目标自查）。⑪ **注册**: CLAUDE.md 战略定位区指针（"要不要做 X"以纲领 §8 为准）+ 文件表行 + status.json priorities 纲领条目 + focus/recent_changes。⑫ **验证**: full_regression 153/0/5440 全绿 / 治理 91/91 EXIT=0 / 三 doc-drift 无漂移 / 安全扫描零泄漏（纲领无 key/号码/内网 IP）/ **VERSION 0.37.9.101 不变**（纯战略文档零业务语义，同 V37.9.192 战略记录惯例）。**复杂度预算账本**: files +1（纲领 = 战略文档非 runtime 接缝）/ env/jobs/runtime-state-sources/suites/tests/checks/VERSION 全不变 / **净 runtime 机制 0**（纲领用判据门控与退役清单结构性防止自身成为复杂度来源）。⑬ **Mac Mini 待用户**: git fetch+reset 同步（纯 doc 零重启）。⑭ **元价值**: 把"未来往哪走"从散落的 priorities/focus/导师复盘升级为单一 canonical 纲领——双轨战略给了研究与工程互为燃料的结构，判据门控给了"何时做/何时不做"的机器可观察标准，反目标清单给了拒绝的依据；纲领以日落法三元约束自我约束，是"计划"而非"愿望清单"。 |
| V37.9.245 | 2026-07-05 | **CLAUDE.md changelog 第二轮归档 — 每-session 必读 660KB→332KB（-50%，V37.9.167 谱系第二轮，日落法把 78 版冻结历史移出热路径 + 头注去数字化退役漂移源，纯 doc 移动零业务语义 VERSION 不变）** — ① **触发**: 周六开工健康基线全绿（153 suites / 5440 tests / 0 fail / 治理 91/91 EXIT=0 / 混沌审计完整跑完 sabotage 全还原 / 徽章漂移检测 ✅ / 分支==origin/main #809 / OpenClaw 上游无新 stable（latest 仍 2026.6.11，beta 2026.7.1-beta.1）→ 按原则 #1 沿用 V37.9.244 第六次评估 hold 决策 + 三收敛判据）；gen_readme_badges 偶发失败观察项再次未复现（67 tests ✅）。26 项 unfinished 逐核全部 gated（LLM-Observer shadow 周 ~7/7 flip / doubao_21 观察 N 天 / IEEE·ISSRE in-review / Mac Mini 被动验证 / deferred-by-design）→ 门控收敛日（镜像 V37.9.185/208/209/243 谱系）。**grounded 发现**: V37.9.167（6-17）把 CLAUDE.md 从 1MB 压到 291KB 后，18 天累积 78 版巨型 changelog 行（V37.9.168→244 每行 3-6KB），CLAUDE.md 胀回 **660KB**、内联表 116 行 vs L282 头注声称「近 ~38 版」= stale——每 session 又多烧 ~80K tokens 读冻结历史，外部评审2「文档认知过载」面回潮。② **执行（V37.9.167 同款方法）**: Python 原子脚本（tmp+os.replace + pid 后缀 V37.9.237 惯例 + 边界 assert 防呆：首/末/切点三行版本号逐一断言 + hotfix 行计数 ==1）归档 **V37.9.206→V37.9.130 共 78 行**（含 V37.9.158-hotfix）到 docs/changelog.md 既有归档表（插在 V37.9.129 行之前 = 单表连续降序一物一形，段头/两处注/文件头维护注同步更新为双轮口径），保留 V37.9.244→V37.9.207 共 38 行内联。**行数守恒精确验证**: CLAUDE 129→51 + changelog 190→268 = 319 恒定（零丢失，脚本内 assert）。③ **归档前只读预检（不凭 V37.9.167 旧结论，重新全量核实——18 天间新增测试/治理可能引用被归档行）**: 治理 yaml 结构化解析仅 **1 个** file:CLAUDE.md check（原则 #27「账号健康 ≠ HTTP 200」在原则区不受影响；初版窗口式提取误报 4 个 pattern，精确 yaml 解析证实全部误归属于邻近其他文件的 check）+ 模拟归档后全 pattern 仍匹配 / 8 个引用 CLAUDE.md 的测试逐一核实（changelog-marker 类断言全在 .py 源码非 CLAUDE.md 行 + fixture 用临时文件）/ 引用被归档版本号字面量的 10 个测试无一打开 CLAUDE.md / 归档边界文本（「190 版」「V37.9.129 及更早」「近 ~38」）全仓无外部引用。④ **头注去数字化（日落法退役漂移源，V37.9.152 file-row 同款 method）**: L282「近 ~38 版本变更见下表」（实际已胀到 116 行 = 声称数字必然漂移）→「近期版本变更见下表（V37.9.207+）」——内联行数不再声称具体数字，只标归档边界（冻结值，仅下轮归档时整句更新）；表尾指针块 + docs/changelog.md 段头/头注同步为双轮口径（190+78=268 版）。⑤ **操作纪律**: 全程遵守「不与 full_regression 混沌审计层并发编辑」（V37.9.186/224 教训第 3 次兑现）——基线运行期间只做只读预检，期间观察到 auto_deploy.sh 出现 CHAOS_MUTATED 改动，正确判定为**混沌场景进行中的瞬时突变**（场景自身还原，非泄漏 bug）而非误"修复"；另实录一次 monitor 早触发假完成信号（tasks output 为空时事件不可信），以进程树实证（chaos python 仍在跑）纠正，等真完成通知再动手。⑥ **验证**: 开工基线 + 收工两轮 full_regression 153/0/5440 全绿 / 治理 91/91 EXIT=0（file:CLAUDE.md check 归档后实测仍过）/ 三 doc-drift 机器检查无漂移 / 安全 95/100 / 安全扫描零泄漏 / gen_readme_badges --write 同步 v37.9.245 / **VERSION 0.37.9.101 不变**（纯 doc 结构优化零业务语义，同 V37.9.167 惯例）。**复杂度预算账本**: files/env/jobs/runtime-state-sources 四维度全不变 / suites/tests/checks/VERSION 全不变 / **净 runtime 机制 0，净 -1 巨型热路径接缝（660KB→332KB）+ 净 -1 头注计数漂移源（去数字化）**。⑦ **刻意无守卫测试**（changelog 内容/位置不在机器跟踪范围，boundary assert 在归档脚本内一次性防呆，V37.9.167 同款惯例）。⑧ **Mac Mini 待用户**: git fetch+reset 同步即可（纯 doc 零重启零业务影响）。⑨ **元价值**: V37.9.167 建立的「热路径只放近期活跃版」纪律第二轮兑现——归档从一次性手术变成周期性维护动作（~2.5 月节奏，版本累积 ~70-80 行触发）；本轮同时把头注从「声称固定数字」收敛为「只标边界」，下轮归档只需更新边界值 + 搬行，头注不再随版本累积漂移。每 session 上下文预算重获 ~80K tokens。 |
| V37.9.244 | 2026-07-04 | **OpenClaw Gateway 第六次升级评估 — 4.27→6.11「无损稳定」深评结论 = 继续 hold + 三收敛判据（用户主动要求仅评估不升级，eval doc 新增第十七节，纯评估/文档零升级动作 VERSION 不变）** — ① **触发**: 用户「深入评估基础系统 openclaw 能否无损稳定继续升级（当前 4.27），仅评估风险待确认再决定」。② **方法（V37.9.225「代码即事实」升级版）**: npm packument 全量分析（23 stable 跨度 / engines 门槛 / deprecation）+ **5 版本 tarball 实证**（4.29/5.2/5.28/6.1/6.11 CHANGELOG + dist 源码 grep）+ WebFetch 补 6.5-6.10 窗口 + **Explore agent 全仓 Gateway 依赖面审计**（7 类：CLI 解析点/状态文件/HTTP 端点/schema 假设/dist grep/插件/退出码假设 → top 5 脆弱接缝）。③ **核心发现三个结构性迁移**: **M1 插件外部化**——tarball 目录实证 4.29 whatsapp=77 文件 → 5.2 起 =0，WhatsApp/Discord 移出核心包成外部 npm 包（@openclaw/whatsapp + Baileys rc9→rc11，5.12），升级须经 doctor/update 修复机制网络安装（ClawHub 429 史 #54446 + 修复机制自身 bug 史 #82533/#82813「升级后 Discord 频道消失」5.17 才修 = 4.x→6.x 跳变路径被显式支持过但曾真实断过）；**M2 状态 SQLite 渐进迁移 6.11 仍进行中**——6.5 cron store（jobs.json→SQLite doctor preflight 自动迁）+ auth profiles + session metadata / 6.6 auth verify-before-**cleanup**(#91740) / 6.8 jobs.json 退役 / 6.11 guardrail 仍在修（#95857/#95916）+ **session accessors 正在 refactor（#96182/#96204）而 sessions.json 仍是会话存储（dist 实证）= 迁移弧线中段**；**M3 Gateway HTTP/WebSocket 栈整体换 Proxyline**（5.12 唯一正式 Breaking，正是 Gateway→:5002 通路）。④ **🔴 质变结论 = 回滚单向门**: 上次升级安全网「30 秒无损回滚」退化——SQLite 迁移含 cleanup，回滚到 4.27 读不回 cron/auth 状态，凭据丢失 → WhatsApp 重链撞 6/16 设备链接限流(408)血案类风险；缓解 = 升级前全量 ~/.openclaw tar 快照（backup create 对 SQLite 覆盖语义未实证），回滚变「恢复快照 + 窗口期新状态丢弃」= 可控但非无损。⑤ **风险矩阵**: 🔴 R1 WhatsApp 穿越升级（M1 安装步×Baileys 跳变×M2 auth 迁移三重叠加）/ R2 weixin 第三方插件（好消息=官方外部 catalog 有 @tencent-weixin/openclaw-weixin@2.4.3 实证，坏消息=4.27 装的版本在 6.11 SDK 下兼容性未知+6.8 untrusted 警告机制）/ R3 notify.sh 冷调用超时签名判定（4.27 quirk 被 6.11 根治 → 签名语义变化可能漏投/重复投，最高频解析点）；🟡 R4 CLI 输出/退出码解析面（wa_channel_status FAIL-OPEN 格式变=回 7h 盲区 + 6.8 退出码重分类 + 6.9 cron list 改版）/ R5 openclaw.json 23-stable 多跳 doctor 迁移（暴露已知内部漂移 providers.qwen vs models.providers.qwen-local）/ R6 preflight #48703 dist grep 必失效 / R7 **Node 门槛 22.14→22.19**（Mac Mini 版本待确认=新前置）/ R8 工具注入契约（4.29 tools.exec/fs profile + 6.10 trusted tool policy → 白名单静默丢工具=PA 无声失能 fail-plausible 类）；🟢 收益侧诚实登记（B1 冷调用根治 #93356/#93919/#89628 / B2 WhatsApp 可靠性+socket-timing 透传 #73580 仅 ≥6.x / B3 cron 韧性+doctor #94148 非交互不自动重启 / B4 版本差距复利）。⑥ **建议 = 方案 A 继续 hold + 三收敛判据开窗（数据驱动非日历）**: ①连续 2 stable 无 session/SQLite 迁移 PR（当前明确未收敛，beta 2026.7.1 在跑弧线或数周内完成）②周稳定版 ≤1（6 月 7 stable ≈ 周更，原 G1 不通过）③Mac Mini node ≥22.19 预先就位（可独立提前做）。方案 B 中间版本明确不推荐（≥5.2 都吃 M1+M3 却拿不到 6.11 修复=成本全担收益不全拿，若升一步到位）。方案 C（用户若决定现升）前置清单已写入 eval doc 17.6。⑦ **交付**: eval doc **第十七节**（17.1-17.6：结论/上游现状/三迁移/单向门/风险矩阵/判据）+ check_upgrade.sh LAST_EVAL_DATE 2026-06-11→**2026-07-04**（第六次评估重置时间 tripwire，注释同步）+ config.md 头部 Gateway 行追加第六次评估 hold 决策（原则 #1 每日必读锚点）。**零升级动作**（tripwire 0/6：时间 23/180 / 版本差距 23/50）。⑧ **验证**: bash -n check_upgrade.sh / full_regression 全绿 / 治理 EXIT=0 / 安全扫描零泄漏 / **VERSION 0.37.9.101 不变**（纯评估+监控配置日期零业务语义，同 V37.9.128/225 评估惯例）。**复杂度预算账本**: files/env/jobs/runtime-state-sources 全不变 / 净 runtime 机制 0（评估记录+tripwire 日期重置）。⑨ **Mac Mini 待用户**: git fetch+reset 同步（纯 doc+监控脚本注释/日期，零重启）；可独立提前做判据 ③：`node -v` 确认 ≥22.19（不足先升 Node，与升级窗口解耦）。⑩ **元价值**: 「能否无损升级」的诚实答案是**不能称为无损**——上次 3.13→4.27 的「30 秒回滚」安全网在 6.x SQLite cleanup 下不复存在（单向门），这是本次评估最重要的新结论；升级决策从「等哪天」升级为「等三条可机器观察的收敛信号」，每周一 check_upgrade cron 照常监控 + 2026.7.x stable 发布时核对 changelog 即完成判据跟踪。 |
| V37.9.243 | 2026-07-04 | **doubao_21-primary 现态 prose 一致性收敛 + GUIDE 降级段退役 Gemini/FALLBACK_PROVIDER 误导示例 + unfinished 归账（门控收敛日，纯 prose/元数据零业务语义 VERSION 不变）** — ① **触发**: 周五开工健康基线全绿（153 suites / 5440 tests / 0 fail / 治理 91/91 EXIT=0 / 混沌审计 10/10 全抓 / 三 doc-drift 机器检查无漂移 / 分支==origin/main #807 / OpenClaw 上游无新 stable（latest 仍 2026.6.11）沿用 hold 决策）；status.json 登记的 gen_readme_badges 套件偶发失败以全量日志抓现场**未复现**（67 tests ✅，观察项保留）。25 项 unfinished 逐核全部 gated（LLM-Observer shadow 周 ~7/7 flip / doubao_21 观察 N 天 / IEEE·ISSRE in-review / Mac Mini 被动验证）→ 门控收敛日纯收敛（镜像 V37.9.185/208/209/219 谱系 + 外部评审2「证据刷新与一致性收敛」指令）。② **诊断（原则 #28 grounded 全文件 sweep 非凭推测）**: V37.9.220 事故回滚时把 primary prose 回正为 Qwen3，V37.9.222 当日重新 flip 生产（PROVIDER=doubao_21 唯一 primary + fallback [deepseek_full,doubao,deepseek,qwen]）但该 session code-heavy 未再同步 prose → 现态漂移复活: CLAUDE L9 项目简介「当前主力 Qwen3 + doubao_21 在 fallback 链」/ L116 组件表主力 label / README L71 架构图「Qwen3-235B primary」/ L439 fallback 图示例 / FEATURES L10-11「Qwen3 (主力)」+「doubao_21 (fallback 链首)」双反 / **GUIDE.md 降级段整段主动误导**（「Qwen3 不可用降级到 Gemini 2.5 Flash」+ `FALLBACK_PROVIDER="gemini"` 示例 + Primary/Fallback 角色表——Gemini V37.9.129 geo-block 退役 + 单槽 env 已被 V37.9.218 FALLBACK_ORDER 取代，照抄会配置一个永不工作的降级链）/ **机器表元数据 2 处**: providers.py qwen tier_evidence「主力 provider，全部生产流量（V27 起）」读作当前声明已假 + doubao 2.0 插件「fallback 链第 1 位（V37.9.129 起唯一真 fallback）」（现链第 2 位，deepseek_full 第 1）。③ **修复（whiplash-resistant 机制优先写法，V37.9.220 教训兑现）**: 全部 prose 以「primary 由 PROVIDER env 配置」为主句 + 当前值标注 V37.9.222 flip（未来再切换只改值不改结构）；GUIDE 降级段重写为 FALLBACK_ORDER 现实（含「primary 自动排除=同一顺序串在 flip 时无需改动」语义 + 命令块按原则 #35 零注释行）；qwen evidence 改「V27 起承载全部生产流量至 2026-07（V37.9.222 flip 后转 fallback 兜底）」/ doubao 2.0 改「V37.9.129–V37.9.218 期间唯一真 fallback，现链中第 2 位」——**tier 均不变**（production_observed 是历史挣得的，evidence 加时间边界即诚实，原则 #23）+ `gen_compat_matrix --fix` 同步档位表 + `--check` 三表一致 + `--check-tiers` 11/11 一致。④ **unfinished 归账（V37.9.185 谱系，走 status_lock+save_status helper MR-9）**: [0] 2026-07-02 审计（9 findings 全部处置完毕）与 [23] 2026-07-03 triage（findings 全闭环）两条巨型条目压缩为纯 Mac Mini 被动验证清单（完整分诊留 changelog 单一源），直接缩评审2「文档认知过载」面。⑤ **诚实不动 point-in-time**: 全部 changelog 行 / 论文冻结快照「8 providers Qwen3 primary」/ GUIDE `qwen-local/` 前缀（OpenClaw 路由标签与 provider 切换无关，V37.9.219 已 ground）/ CLAUDE L23 架构图「LLM (7 Providers)」（V37.9.209 决定=built-in 视图）。⑥ **验证**: 开工+收工两轮 full_regression 153/0/5440 全绿 / 定向 test_providers 173 + test_gen_compat_matrix 38 + test_v37_9_52 32 + test_minimal_runtime 15 全过 / 治理 91/91 EXIT=0 / 安全 95/100 / 安全扫描零泄漏 / **VERSION 0.37.9.101 不变**（prose+evidence 元数据刷新零路由语义，同 V37.9.208/219 惯例）。**复杂度预算账本**: files/env/jobs/runtime-state-sources 四维度全不变 / suites/tests/checks/VERSION 全不变 / **净 runtime 机制 0，净 -1 误导面**（GUIDE 降级段从「教读者配置已退役 provider」收敛为现实配置；primary prose 从「值硬编码」收敛为「机制+当前值」抗未来 flip whiplash）。⑦ **刻意无守卫测试**（prose 现态不在机器跟踪范围，V37.9.219 同款；evidence 字符串零测试字面量依赖经 grep 核实）。⑧ **Mac Mini 待用户**: git fetch+reset 同步（纯 doc/元数据零重启；providers.py/doubao 插件走 FILE_MAP 同步但仅 evidence 字符串零行为变化）。⑨ **元价值**: V37.9.219→220→222 的 primary flip whiplash（收敛→事故回正→重新 flip）留下的第三态漂移在生产稳定 2 天后收敛，且这次用机制优先写法让 prose 对未来 flip 免疫——「主力是谁」只活在 PROVIDER env + 机器表，prose 只描述机制与当前标注。 |
| V37.9.242 | 2026-07-03 | **adapter 非 chat 端点回写收编 _deliver — V37.9.231 ⑦ 登记边界闭合（完成态回写零裸写强不变式，VERSION 0.37.9.100→0.37.9.101，同日第六交付）** — ① **背景**: V37.9.231 把 chat 路径 4 个回写点收编 _deliver（client 断开只记 CLIENT GONE，不误记 CB failure/不跑死 socket fallback 链），⑦ 诚实登记"非 chat 端点回写仍裸写（噪声级低优）"。② **修复（3 站点 5 回写全收编）**: /health（raw write 在 try 外，client 断开异常直接泄漏 http.server）+ GET passthrough（成功回写 client-gone 被 except 当 GET 失败 → send_error(502) 再写死 socket → 二次异常泄漏 = 登记的噪声；错误路径 send_error → _deliver JSON 一致化）+ 非 chat POST（成功 + 502 两路径同款）。**收编后强不变式: adapter 完成态回写恰 9 个 _deliver（chat 4 + 非 chat 5），裸 send_response 仅 _send_json 内 1 处**；输入校验类 send_error（400/413，client 刚发数据必活）豁免不动。③ **守卫演进**（V37.9.131 alternation 惯例）: test_v37_9_231 计数守卫 4→9 + 新增 `test_no_raw_send_response_outside_send_json`（regex 计数 == 1 = 完成态零裸写结构性锁死）。**sabotage**: GET 回退裸回写 → 3 FAIL；行为级探针（抽真实 _send_json+_deliver exec）: client-gone → CLIENT GONE + False 零泄漏 / 正常投递不变。④ **验证**: full_regression 152/0/5440 (+1: 231 套件计数守卫改名 + 新增零裸写守卫净 +1) / governance 所有不变式成立 EXIT=0 / 安全 95/100 / **VERSION 0.37.9.100→0.37.9.101**（adapter 投递语义 runtime 变更，镜像 V37.9.231 bump 惯例）。**复杂度预算账本**: files 不变 / env/jobs/runtime-state-sources 不变 / **净 runtime 机制 净 -3 裸写站点**（一物一形: 投递策略从"chat 收编+非 chat 裸写"双形态收敛为单一 _deliver 路径，退役 502-to-dead-socket 噪声面）。⑤ **Mac Mini 待用户**: 合并 + git fetch+reset 同步 → adapter 经 FILE_MAP 同步 + 重启 → 生效后 /health、GET /models、非 chat POST 撞 client 断开时 adapter.log 记 CLIENT GONE 单行（此前 http.server 抛整段 traceback 噪声）。 |
| V37.9.241 | 2026-07-03 | **暗测试三连破案 + dark-test 对账守卫机器化 + 三小项日落清理（用户「彻底检查今天可解决待办」全量 grounded 扫描产物，同日第五交付，VERSION 0.37.9.100 不变）** — ① **触发**: 用户要求彻底再查今天可解决项。补齐 `-m unittest` 模块形式注册口径对账 full_regression，抓到 **3 个从未注册的暗测试，其中 2 个正在静默失败**: (a) `ontology/tests/test_engine.py`（V36.2 引擎基础套件 ~89 用例）**FAILED ×3**——V37.9.91 加第 3 个自定义工具 expert_escalate 后期望腐烂（custom_tools 2→3 / query 列表 / 18→19 tools）近 1 个月无人知 (b) `ontology/tests/test_notify_activity.py` **FAILED ×1**——代码 V37.9.104 时代改走 `_MRD["registry_file"]` config 注入后 literal 守卫 stale（代码正确守卫烂）(c) `test_data_clean.py`（V30.3 起 80 用例）幸运仍绿但纯注册债。**实证「未注册测试必然腐烂」**——与 V37.9.130 harvest_chat 构成 bug 类 4 次演出 = 达项目机器化门槛。② **修复**: test_engine 3 处期望更新（2→3 / +expert_escalate / 18→19，注明血案）+ test_notify_activity 守卫升级（断言 `_MRD["registry_file"]` 注入形式 + 注入源默认值仍指向 jobs_registry.yaml = 语义双锚）+ 三套件注册 full_regression。③ **dark-test 对账守卫机器化** `test_v37_9_241_dark_tests.py`（5 测试）: 枚举 repo 测试全集（根目录 + ontology/tests）对账 full_regression 两种注册形式（文件名/模块名）→ 任何新测试漏注册立即 FAIL（自指: 守卫自己也在全集里）+ EXEMPT 显式豁免清单（当前空，加条目须附理由）+ 幽灵豁免检查 + 防 vacuous（全集 >100 + 双目录锚点）+ 三暗测试防回退。**sabotage**: 删一条注册 → 3 FAIL 精确指名。④ **三小项日落清理**: (a) **CLAUDE.md Phase 3 表 2 行 stale 修正**——"filter_tools()/fix_tool_args() 改用引擎 待启动" 实际 V37.8.14 ONTOLOGY_MODE=on 已完成（proxy_filters L463-469 mode=on 加载全部 7 集合含 TOOL_PARAMS/CLEAN_SCHEMAS 实证，load-once 形态优于每请求 query）→ 标 ✅（评审2 一致性收敛）(b) **TZ 一物一形**（V37.9.213 ⑨ 登记）——kb_evening.sh/kb_deep_dive.sh 的 DATE 从 system-local 统一 `TZ=Asia/Hong_Kong`（同脚本 TS 和 kb_dream DAY 已 HKT；Mac Mini TZ=HKT 时 no-op 潜伏，系统 TZ 漂移时 observer 按日期读文件会错位）+ 2 守卫（含回退反模式 assertNotIn，sabotage 验证 fire）(c) **kb_review.sh OPENCLAW_BIN 死变量退役**（V37.9.145 登记候选，notify.sh 不消费 + 全文件零引用 grep 实证，注释留痕）。⑤ **验证**: full_regression 152 通过/0 失败/**5439 tests (+189: data_clean 80 + engine 89 + notify_activity 9 + 对账守卫 5 + TZ 守卫 2 及既有增量)** / governance 所有不变式成立 EXIT=0 / 安全 95/100 / bash -n 4 脚本 / **VERSION 0.37.9.100 不变**（测试注册+doc 收敛零 runtime；TZ 修复 job 级且生产 no-op，镜像 V37.9.233/234 惯例）。**复杂度预算账本**: files +1（dark-test 守卫）/ env/jobs/runtime-state-sources 不变 / **净 runtime 机制 0**（对账守卫是 CI 机器非 runtime 接缝；4 次演出的 bug 类从「靠记忆注册」升级为机器对账——本次抓出的 2 个失败套件正是该盲区的活证）。⑥ **Mac Mini 待用户**: git fetch+reset 同步（kb_evening/kb_deep_dive/kb_review 走 FILE_MAP，TZ 修复在 Mac Mini 系统 TZ=HKT 下零行为变化；测试变更下次跑 full_regression 生效）。⑦ **元价值**: 「彻底检查」的价值在补口径——之前的对账 grep 漏了 `-m unittest` 模块形式注册，补上后 3 个暗测试现形，其中 test_engine 的 3 个失败正是「暗测试必然腐烂」定律的一个月期实证（V37.9.91 改引擎时 CI 全绿因为烂测试根本不在 CI 里）。对账守卫让这个 bug 类从此结构性绝迹。 |
| V37.9.240 | 2026-07-03 | **observer deep_dive 跨天重复检测 — V37.9.233 ban-list 的回归探测器（item 23(c) 登记的 observer 候选信号兑现，闭合「单日视角盲区」+ ban-list FAIL-OPEN 静默失效风险，VERSION 0.37.9.99→0.37.9.100，同日第四交付）** — ① **触发**: 2026-07-02 审计 9 findings 全部处置完毕后，item 23(c) 登记的 observer 候选信号是剩余唯一非 gated dev 项：「deep_dive 跨天重复主题检测——dream 有 ban-list 时 observer 没抓到 20 天重复 = 单日视角盲区」。② **为什么值得做（非重复机制）**: V37.9.233 的 picker ban-list 是**预防**，但它 **FAIL-OPEN**（目录缺失/frontmatter 格式漂移 → `load_recent_analyzed_links` 返回空集 = 无 ban）——生产路径问题会**静默禁用**它数周无人察觉（正是 MR-4 + fail-plausible 家族：防御自己静默失效）。observer 端独立检测 = ban-list 失效的告警器（audit-as-regression：已知 fail 模式的机器化回归网，宪法 #1 机械化人眼正业——V37.9.233 血案本靠用户 uniq -c 明细才发现）。③ **实现（扩 V37.9.168/213 既有扫描，零新文件读取机器）**: `scan_deep_dive_modes` 循环内加 frontmatter `link:` 提取（`_DD_LINK_RE` 前 2000 字符窗口 + 尾斜杠归一化 = kb_deep_dive `_normalize_link` 同款）→ 按 link 分组、**相邻日期对差 ≤ `DEEP_DIVE_REPEAT_WINDOW_DAYS=14`** 判违规（`[d,d+20]` 合法 ban 过期 / `[d,d+3]` 违规；窗口与 `kb_deep_dive.DEDUP_WINDOW_DAYS` 跨模块一致——观察窗口大于 ban 窗口会把合法再分析误报）→ `stats["repeats"]` + detect_anomalies MED `deep_dive_repeat`（消息含「ban-list 应已阻止 → 疑似静默失效」+ 样例 link+dates）+ 报告段 ⚠️ 重复分析行（**仅违规时出现，正常日零噪声**——原则 #32）。④ **10 新单测**（test_daily_observer 236→246）: **血案回归**（同 ACM DOI 2 天两次 → 检出精确 link+dates）/ 尾斜杠归一化同链接 / **>14 天合法再分析绝不误报** / 无 link 行不崩 / anomaly 触发+消息 / 无重复无 anomaly / 报告段条件渲染 / **跨模块窗口一致性守卫**（== kb_deep_dive.DEDUP_WINDOW_DAYS，V37.9.103 DEAD_AGE_DAYS 模式）/ **MR-8 跨文件契约**（observer 解析正则与 kb_deep_dive 自己的 `r"^link: (.+)$"` 逐字一致 + 真实匹配 build_deep_dive_markdown 模板渲染行——上游改格式守卫先 fail，防重复检测自己也静默失效）/ marker。**sabotage 反向验证**: 窗口改 -1 检测瘫痪 → 4 FAIL（血案回归+归一化+跨模块一致性），还原全过。⑤ **验证**: full_regression 148/0/**5250 tests (+10)** / governance 所有不变式成立 EXIT=0（V37.9.213 函数边界 slice check 无破坏）/ 安全 95/100 / **VERSION 0.37.9.99→0.37.9.100**（daily_observer 06:30 live cron 组件行为变更 = runtime，镜像 V37.9.213/235 bump 惯例）。**复杂度预算账本**: files 不变（扩 daily_observer + 既有 test）/ env/jobs/runtime-state-sources 不变 / **净 runtime 机制 ≈ 0**（复用既有 deep_dives 扫描循环加一个字段提取+分组判定，零新状态源/零 LLM 调用；闭合「防御 FAIL-OPEN 静默失效无人察觉」盲区）。⑥ **🔴 Mac Mini 待用户**: 合并 + git fetch+reset 同步（daily_observer.py 走 FILE_MAP，明日 06:30 cron 自动生效）→ 正常日报告无新增行（零噪声）；若 ban-list 未来因路径/格式问题静默失效，重复分析出现 2 天内即 observer MED 告警（而非再潜伏 20 天等用户发现）。⑦ **元价值**: V37.9.233 修复链的最后一环——picker ban-list（预防）+ banned_recent 字段（picker 侧可观测）+ **observer 重复检测（防御自身失效的告警器）**。「防御本身是事故面」（日落法 #34）的正解不是不建防御，是给 FAIL-OPEN 防御配独立的失效探测器；且探测器用代码确定性信号（frontmatter link 精确匹配）而非 LLM 判断，MR-8 契约测试把它与上游写入格式机器绑定。 |
| V37.9.239 | 2026-07-03 | **审计 B-delivery 兑现 — streaming fallthrough 内容类型修复（SSE client 在 parse 异常时收 text/event-stream 非 application/json 错配，2026-07-02 审计最后一个可修项闭环，VERSION 0.37.9.98→0.37.9.99，同日第三交付）** — ① **触发**: V37.9.238 后审计仅剩 I（half-open 可接受 tradeoff 登记）+ B-delivery（V37.9.228 诚实登记「修法 murky 需专门评估」）。本版做专门评估 + 修复。② **根因（grounded tool_proxy:1243 fallthrough）**: do_POST parse 块抛异后 fall through 到通用回写——无条件发 `application/json` + raw JSON body。client 请求 `stream:true`（was_streaming=True）时期待 text/event-stream 的 Gateway SSE 解析器收到 JSON = **内容类型错配**（transport 层直接 choke，比流内错误更糟）。到达 fallthrough 且 was_streaming=True ⇔ parse 块异常或空 body（成功路径已 return）= 全是异常场景。③ **评估解「murky」**: 读 build_sse_response 实现（纯 dict-walk + json.dumps）确认其现实失败模式 = backend 200 + schema-drift 垃圾（`choices` 非 list 等）→ rj 是「合法 JSON 垃圾」无可投递 completion；但崩溃点在 parse **之后**（fix_tool_args / custom-tool / alerts）时 rj 是**合法 completion**。→ **两级策略**: fallthrough 且 streaming → (1) 先重试 `build_sse_response(rj)`——rj 合法时投递**真实内容**为 SSE（不丢好响应）(2) rj 未绑定（json.loads 失败 → NameError 被 except 捕获）或 build 再崩 → **SSE error frame**（`data: {"error":{type:"proxy_sse_conversion_error"}}` + `data: [DONE]`，客户端 SSE 传输层可解析，错误流内 surface）(3) 非 streaming / 非 chat 端点走原 application/json 路径**零改动**。SSE 分支 return 防双重响应。④ **9 新单测** test_v37_9_239_sse_delivery.py（extract-block + exec-with-fakes，tool_proxy 顶层 serve_forever 不可 import 的 V37.9.132/175/228 惯例；**锚点=V37.9.239 注释**——泛匹配 send_response(resp.status) 会抓到 L297 更早站点跨数千行，开发中实measure）: 行为级 5（rj 未绑定→error frame+DONE / **rj 合法→真实 completion 投递非 error frame** / build 再崩→error frame / 非 streaming json 路径不变 / 非 chat 端点不变）+ 源码守卫 4（marker / gate 在 json 回写前 / **重试优先于 error frame 顺序** / SSE 分支必 return）。**sabotage 反向验证**: 移除整个 streaming gate → 9 FAIL/ERROR（锚点消失+行为复现 bug），还原 9/9 全过。既有 test_tool_proxy / test_v37_9_228 / test_v37_9_175 零破坏。⑤ **验证**: full_regression 148/0/**5240 tests (+9)** / governance 所有不变式成立 EXIT=0 / 安全 95/100 / **VERSION 0.37.9.98→0.37.9.99**（proxy 请求热路径投递语义 = runtime 变更，镜像 V37.9.228/231 bump 惯例）。**复杂度预算账本**: files +1（test）/ env/jobs/runtime-state-sources 不变 / **净 runtime 机制 ≈ 0**（1 个内容类型 gate = 投递契约正确性修复，退役「SSE client 收 JSON」错配面，非新接缝）。⑥ **🔴 Mac Mini 待用户**: 合并 + git fetch+reset 同步 → tool_proxy 经 FILE_MAP 同步 + 重启 → 生效后 streaming 请求撞 parse 异常时 Gateway 收到可解析的 SSE（错误流内可见）而非 content-type 错配。⑦ **元价值**: **2026-07-02 多镜头审计 9 findings 全部处置完毕**——8 个修复闭环（SEC/F 全含 follow-up/B-stats/B-delivery/A/C 全三件/D/E/G/H）+ 1 个显式登记为可接受设计 tradeoff（I half-open 自纠正）。「murky 需专门评估」的正确处置不是搁置而是评估——读实现分清「无可投递」与「有可投递」两类崩溃场景后，murky 变成两级策略各自显然。 |
| V37.9.238 | 2026-07-03 | **审计剩余项三批兑现 — finding C 全闭环（status_lock 跨写者 RMW 锁 + audit 链 fork 锁）+ F follow-up（rss_blogs/ai_leaders_blogs/bsky FETCH_ERRORS fail-loud）+ MR-9 ProxyStats 测试污染隔离（VERSION 0.37.9.97→0.37.9.98，同日续 V37.9.237，用户「预算用完前继续待办」）** — ① **(C-lock) status.json lost-update 跨写者锁**：V37.9.237 修了损坏半边（唯一 tmp），本版完成 lost-update 半边——`status_lock()` 上下文管理器，**设计三原则防死锁**（V37.9.226 登记的「勿 rush flock」经谨慎设计兑现）: (1) fcntl.flock **独立锁文件** `status.json.lock`（不能 flock 目标本身——os.replace 换 inode 使锁失效；flock 由内核管理，进程死亡含 SIGKILL 自动释放 = 无 mkdir 目录锁的 stale-lock 永久阻塞问题）(2) **有界获取**（LOCK_NB 轮询 ≤5s 绝不无限阻塞 cron）(3) **FAIL-OPEN**（超时/fcntl 不可用 → stderr WARN 无锁继续 = 修复前行为，锁是增强非闸门）。**全写者覆盖**: CLI main() RMW 提取为 `_run_rmw(args, parser)` 锁内执行（~40 cron shell 写者全走 CLI，grep 核实无 shell 直写）+ 3 个库 RMW 点（daily_observer `_write_observer_to_status` / preference_learner `apply_preferences` / security_score `--update`）各包锁 + **部署窗口 fallback**（`try: from status_update import status_lock except ImportError: nullcontext`——auto_deploy 逐文件同步窗口内旧 status_update 无此符号不崩溃）。save_status 自身**不**内嵌锁（同进程二次 flock 自阻塞→假超时，源码守卫锁死）。② **(C-audit) audit_log 链 fork 锁**：`audit()` 的 read-last-hash→append 是 RMW，并发两写者读同 last-hash → 同 prev → 链 fork → verify_chain 假 tamper。同款 flock（`audit.jsonl.lock` + 有界 + FAIL-OPEN）；**锁序无环**（audit 锁只在 status 锁内或独立获取，反向从不发生）。③ **(F follow-up) 3 个 content job fail-loud**（V37.9.227 诚实登记的 gap 兑现，镜像 ontology_sources 三件套）: rss_blogs / ai_leaders_blogs + **ai_leaders_bsky**（V37.9.227 未点名但同 shape，原则 #31 全量同步）各加 `FETCH_ERRORS=0` init + skip 分支 incr + new==0 出口 consult（≥源数 → `status:"fetch_failed"` + exit 1 → watchdog 显式 case 告警；平静日 ok 带 errors 字段可观测）。全源宕从「与平静日不可区分的静默 ok」升级为「4h 内 watchdog 告警」（MR-4 家族收敛）。④ **(MR-9) ProxyStats 测试污染隔离**（V37.9.229 登记观察兑现，test-pollutes-production 家族第 6 实例）: `ProxyStats()` 首次 record_* 必 flush（`_last_flush=0.0`）→ test_config_slo.TestProxyStatsSLO / test_tool_proxy.TestProxyStats / test_v37_9_228.TestParseStatsBehavior 三个类直写真实 `~/proxy_stats.json`（Mac Mini 回归时瞬态污染 live proxy 统计）→ 三类 setUp monkeypatch STATS_FILE 到临时目录（镜像 test_v37_9_229 sanctioned 模式）。**行为级证明**: HOME 隔离子进程跑三套件 → 无 proxy_stats.json 创建；sabotage 移除隔离 → 污染复现 = 检查 load-bearing。⑤ **50 新单测**（237 套件 14→20 +6 锁测试 / 新 238 套件 22 / 相关断言更新）: **真跨进程互斥**（子进程 flock holder 1.2s → status_lock 真等待后获取）+ **fail-open 超时不阻塞**（10s holder + 0.5s timeout → acquired=False 立即继续 = 结构性无死锁行为证明）+ **lost-update 端到端确定性**（8 并发 CLI --add 8/8 全保留——V37.9.237 时不可能断言，锁串行化后确定性成立）+ **10 并发 audit() → verify_chain ok:True errors:0**（chain-fork 修复端到端）+ 三 job fail-loud 契约（mixin 复用: counter init / skip incr / consult+exit 1 / ok 带 errors / 旧无条件 ok 退役 / watchdog fetch_failed case 防退役）+ MR-9 隔离源码守卫。**Sabotage 反向验证四轮**（备份副本还原 + 清 __pycache__）——**首轮抓出自己两个弱守卫**: `with status_lock():` 断言被 status_lock docstring 用法示例满足（V37.9.178「守卫被自己的注释咬」同族）→ 改断言两行连续代码形态；`fcntl.flock` 泛断言被 finally 块 LOCK_UN 释放调用满足 → 改断言获取形态 `LOCK_EX \| LOCK_NB`。修强后 4 轮 sabotage 全部正确 FAIL，还原全过。⑥ **连带演进 2 个既有守卫**（V37.9.131 alternation 惯例）: test_ai_leaders_blogs/bsky 的 FAIL-OPEN 守卫 regex `抓取失败.*\n\s*continue` 允许 FETCH_ERRORS 计数行插中间（FAIL-OPEN 语义不变仅可观测化）+ V37.9.227 套件「诚实边界」docstring 更新为已闭合。⑦ **验证**: full_regression 147/0/**5231 tests (+28)** / governance EXIT=0 所有不变式成立 / 安全 95/100 / 三 job bash -n / **VERSION 0.37.9.97→0.37.9.98**（status_update/audit_log 写路径并发语义 + 3 job status 语义 = runtime 变更，镜像 V37.9.227/237 bump 惯例）。**复杂度预算账本**: files +1（238 测试）/ env/jobs/runtime-state-sources 不变（.lock 文件是既有状态文件的伴生锁非新状态源）/ **净 runtime 机制 = 2 个 flock 锁**（真新增能力——lost-update/chain-fork 此前无任何机制防护；FAIL-OPEN 设计使其绝不成为新故障闸门；audit finding C 全部三件闭环退役整个「跨进程 RMW 无锁」故障面）。⑧ **🔴 Mac Mini 待用户**: 合并 + git fetch+reset 同步（status_update/audit_log/daily_observer/preference_learner/security_score + 3 job 脚本走 FILE_MAP，零重启）→ 生效后 `ls ~/.kb/status.json.lock ~/.kb/audit.jsonl.lock` 出现伴生锁文件（0 字节正常）+ 并发 cron 写 status.json 不再互相覆盖 recent_changes + 全源宕的 rss_blogs/ai_leaders_blogs/bsky 会 watchdog 告警而非静默。⑨ **元价值**: 2026-07-02 审计 9 findings 至此 **8 个闭环**（SEC/F 全部/B-stats/A/D/E/G/H + C 全部三件），仅剩 I（half-open 设计 tradeoff 自纠正，登记为可接受）+ B-delivery（streaming content-type murky 需专门评估）。「勿 rush flock 防死锁」的登记警告以「独立锁文件 + 内核自动释放 + 有界获取 + FAIL-OPEN」四重设计兑现——锁的每一种失败模式（持锁者死亡/超时/fcntl 缺失/锁文件不可创建）都降级为修复前行为而非新故障。 |
| V37.9.237 | 2026-07-03 | **审计 finding C 安全半边 — 共享状态原子写唯一 tmp（pid 后缀）消除并发交错损坏→系统失忆（status.json + ~/.kb/index.json，VERSION 0.37.9.96→0.37.9.97）** — ① **触发**: 周五开工健康基线全绿（VERSION 0.37.9.96 / 5189 tests / 0 fail / governance 91/91 EXIT=0 / 安全 95 / 工作树干净同步 origin/main #804 / OpenClaw v2026.4.27 hold 决策清晰无需重评估）。22 项 unfinished 逐核多数 Mac-Mini-gated/in-review/design-tradeoff-deferred；从 2026-07-02 多镜头审计剩余登记项（C 跨进程锁 / I half-open / B-delivery streaming）选 **finding C 的安全半边**——最有价值 grounded dev（影响最关键共享状态 status.json = 三方意识锚点，~40 并发写者，损坏即系统失忆）且有零死锁风险的可切分半边。② **根因（grounded，原则 #28，Explore agent 全仓核实 + 亲读代码）**: `status_update.py:189 save_status` / `kb_dedup.py:46 save_index` / `kb_autotag.py:215 save_index` 全用**固定** tmp 名 `TARGET + ".tmp"`。os.replace 保证 atomic **publish**（读者never 见半截），但**两个并发写者写同一固定 tmp 路径**（status.json 由 ~40 cron 进程 + kb_status_refresh + preflight + security_score 并发写；index.json 由 kb_dedup / kb_autotag **不持** kb_write.sh 的 .write.lockdir 却写同款 index.json.tmp）→ 字节交错 → os.replace 发布损坏文件 → 读者 `json.JSONDecodeError` → fallback DEFAULT_STATUS / `{"entries": []}` = **系统失忆 / KB 索引失忆**。**docstring L4「支持并发安全」本身是 fail-plausible 声明**（atomic publish ≠ 并发安全）。③ **修复（唯一 tmp，零死锁）**: 三处 tmp 改 `"{}.tmp.{}".format(TARGET, os.getpid())`——并发写者（不同进程 = 不同 pid）写各自 tmp 路径绝不共用 → 消除交错损坏；os.replace 仍 atomic publish（并发下最坏 last-writer-wins 良性丢更新，index 可由 kb_embed 重建）+ `try/finally` 清理未发布 tmp（防 orphan 累积，异常时 os.remove）+ docstring 改诚实（退役「支持并发安全」→ 标 non-lost-update）。**kb_write.sh 不动**（已持 mkdir 目录锁，且三处修复后 index.json.tmp 仅它独用 → 无跨写者共用路径）。④ **诚实范围**: 只做 finding C **损坏安全半边**（唯一 tmp），**不做 lost-update 跨写者锁**（需谨慎锁设计防死锁 + audit_log.jsonl chain-fork 同族，保持登记）——把「损坏→系统失忆」降级为「last-writer-wins 良性丢更新」，绝不 rush flock。日落法不建新共享 helper（`+".tmp"` 已在 15 模块 copy-paste，新 helper=15 import 接缝违复杂度预算；targeted 修 3 个并发关键路径非新 copy-paste）。⑤ **14 新单测** test_v37_9_237_atomic_tmp.py（3 类）: TestStatusTmpUniqueness(6: tmp 含 pid / **核心属性两 pid 不同 tmp 路径→不可能交错** / 发布 valid JSON / 写异常清理 tmp orphan / **真 8 子进程跨进程并发 --add 最终始终 valid JSON**（诚实只断言无损坏非全保留=未修 lost-update）) + TestKbIndexTmpUniqueness(3) + TestSourceGuards(5: 三处 pid-tmp 在位+固定名反模式退役 / finally 清理 / **fail-plausible docstring 退役** / marker)。**sabotage 反向验证守卫真有效**（备份副本还原 byte-identical + 清 __pycache__，V37.9.134 教训）: 回退 status_update 固定 tmp → 3 FAIL（源码守卫+行为级 tmp-path+核心 distinct-path 属性），还原 14/14 全过。既有 42+26+19+27 测试零破坏（test_status_update guard `STATUS_FILE + ".tmp"` → 更新为 pid-tmp 断言）。⑥ **验证**: full_regression 0 fail / 5189→**5203 tests (+14)** / governance 91/91 EXIT=0 / 安全 95/100 / **VERSION 0.37.9.96→0.37.9.97**（status.json 写路径并发正确性 = live runtime 组件变更，~40 cron writer 消费，镜像 V37.9.231 delivery 语义 bump 惯例）。**复杂度预算账本**: files +1（test）/ env/jobs/runtime-state-sources 不变 / **净 runtime 机制 ≈ 0**（唯一 tmp 是既有原子写的正确性修复退役「共用固定 tmp 交错损坏」故障面，非新接缝）。⑦ **🔴 Mac Mini 待用户**: 合并 + git fetch+reset 同步（status_update/kb_dedup/kb_autotag 走 FILE_MAP，零重启）→ 生效后并发 cron 写 status.json/index.json 时每写者用 `<file>.tmp.<pid>`（`ls ~/.kb/*.tmp.*` 短暂可见并发写者各自 tmp），不再有交错损坏致的 JSONDecodeError→系统失忆。⑧ **剩余登记（保持不修，需锁设计）**: (C-lock) status.json/index.json lost-update 跨写者锁（并发 RMW 静默覆盖 insert，勿 rush flock 防死锁）+ audit_log.jsonl append chain-fork（read-last-hash-then-append RMW 无锁，假 tamper）+ kb_write.sh 已 lock-protected 低风险。 |
| V37.9.236 | 2026-07-03 | **notify.sh $() 子 shell errtrace 假 FATAL 根治 — "line=101" 三脚本之谜破案（哨兵守卫 2 发送站点 + 内层守卫 5 解析/find 站点，修正 V37.9.214 "LINENO 误报" 结论，VERSION 0.37.9.96 不变）** — ① **触发**: 用户贴 12:30 `[SYSTEM_ALERT] auto_deploy FATAL abort exit=1 line=101 — 部署同步死亡!` + watchdog 12:30 WARNING 7 项。数据驱动取证（原则 #28，用户 Mac Mini 三条命令）: auto_deploy.log 显示**所有 run 全部正常完成**（11:22 部署成功 / 12:24+13:58 漂移修复 / 15:00 heartbeat 正常）——"部署同步死亡"是假告警；但 alerts.log 揭示**三条 FATAL**: auto_deploy 12:25:47 + **watchdog 12:31:57** + auto_deploy 14:00:02，全部 line=101。② **破案（三脚本同报 line=101 的真相）**: 各自的第 101 行全部无辜（auto_deploy=`git rev-parse HEAD` 且 FATAL 时刻早已跑过 / watchdog=心跳 echo / governance_audit=已处理路径）→ **LINENO 报的是 sourced 文件 notify.sh 内的行号**，notify.sh:101 正是 `_notify_send_with_retry` 的 openclaw 发送行 `send_err=$("$OPENCLAW" message send ...) && {`。**机制**: governed caller（set -eE + trap ERR，7 个脚本）source notify.sh 后，其 ERR trap 在 macOS bash 3.2 下被 `$()` 子 shell 继承（V37.9.105/131 quirk 同族），外层 `&&`/`\|\| true` 救不了子 shell 内部 → openclaw **设计性非零退出**（4.27 冷调用超时 = 重试循环的日常输入）在子 shell 内误触发 caller 的 ERR trap → 推送假 "<label> FATAL abort exit=1 line=101"。**时间线完美吻合**: 三条 FATAL 各距一次告警推送启动 ~100-115s（12:24:06 漂移告警 / 12:30 watchdog 告警 / 13:58:07 漂移告警，冷调用 40-77s + 队列 drain）；heartbeat run（无推送）从不 FATAL。dev bash 5.x 复现实验证实 quirk 是 bash 3.2 独有（trap 不触发）。③ **修正 V37.9.214 结论（诚实追加非改写）**: 当时 governance_audit "line=101" 被诊断为 "bash 3.2 trap LINENO 误报"——行号其实一直是真的，指向 notify.sh:101；其 set +E 修复有效的真正原因 = 关掉了 notify $() 子 shell 的 trap 继承。④ **修复（哨兵守卫，一物一形——修在 notify.sh 源头，7 个 governed caller 一次全愈）**: 2 个发送站点（send + replay）`\|\| printf '\n%s' '__NOTIFY_SEND_RC_FAIL__'` 让子 shell 末命令永远成功，失败经哨兵带出、主 shell 剥哨兵判 rc（`\|\|` 在子 shell 内层 = 所有 bash 版本的常规守卫语义，生产证据: 各脚本内层 `\|\| true` 从不误触发）+ 3 个 json 解析站点守卫移入内层（`\|\| true` + `[ -n ]` 判空，损坏 claim 防同族假 FATAL）+ 3 个 find/cat pipefail 站点内层 `\|\| true`。**刻意不用 set +E/-E 切换**（V37.9.214 教训: re-enable 是 landmine，且会泄漏改变 caller 的 MR-19 覆盖面）。⑤ **11 新守卫单测**（test_notify_weixin 46→57，TestV379236SubshellErrtraceSentinel）: 源码守卫 8（两站点哨兵在位 / 旧无守卫形态退役 / 哨兵恰 2 处 / 剥除 4 处 / 解析+find 内层守卫 / marker）+ 行为级 3（失败 stderr 干净无哨兵泄漏 / 损坏 claim 优雅处理不阻塞合法条目 / **governed caller 契约**: set -eEuo+trap ERR 下调 notify 失败主脚本完整走完 trap 不触发——dev bash 5 非判别器，诚实标注真判别 = Mac Mini 观察）。**两轮 sabotage 反向验证守卫真有效**（备份副本还原）: send 站点回退旧形态 → 4 FAIL / replay 站点回退 → 4 FAIL；还原 byte-identical 57/57 全过。**现有 46 测试零破坏** = 发送/重试/冷调用/队列/claim/淘汰语义完全不变。⑥ **同报告其余分诊（零代码）**: 治理审计 fail = 07:00 陈旧 flag（手动重跑已绿，不写 watchdog 状态文件，下次 cron 清除）/ observer 状态文件格式异常 = V37.9.230 预期一天过渡（今晨旧代码写 ISO，明早自愈；恰证明 watchdog 注册生效）/ freight = 今日 14:00 自愈判定 / 429/403/github_trending/hn = 已知噪声与 07-02 残留。⑦ **验证**: full_regression 0 fail / **5189 tests (+11)** / governance 91/91 EXIT=0 / cross_os_quirk_scanner 0 violations / 安全 95/100 / **VERSION 0.37.9.96 不变**（notify 推送层可靠性修复零业务语义，镜像 V37.9.177/180 同文件同类惯例）。**复杂度预算账本**: files 不变（扩 notify.sh + 既有 test）/ env/jobs/runtime-state-sources 不变 / **净 runtime 机制 ≈ 0**（哨兵是既有 $() 站点的内层守卫形态收敛，退役"告警推送制造假 FATAL 告警"的自激噪声源，非新接缝）。**scanner 第 8 pattern 候选诚实登记不现建**（sourced-lib $() 需跨文件 source-graph 分析 = 新机器，11 个 targeted 守卫已封死唯一日常失败站点，日落法；若未来再有 sourced-lib 假 FATAL 实例再评估）。⑧ **🔴 Mac Mini 待用户**: 合并 + git fetch+reset 同步（notify.sh 走 auto_deploy FILE_MAP，零重启）→ 验证 = 下次漂移/watchdog 告警推送撞上冷调用超时后 `tail ~/.openclaw_alerts.log` **不再出现新的 FATAL line=101**（告警推送功能本身不变，用户仍正常收到真告警）。⑨ **元价值**: 一条"部署同步死亡"的假告警，牵出潜伏三周、跨三个脚本、曾被误诊过一次（V37.9.214）的同一根因——**告警系统自己的发送路径在制造假 FATAL 告警**（自激噪声，防御本身是事故面的教科书案例，日落法 #34）。破案钥匙是"三个不同脚本报同一行号"这个反常细节 + 用户取证的 alerts.log 时间戳与推送窗口逐条对齐（原则 #28 数据驱动，零盲改）。 |
| V37.9.235 | 2026-07-03 | **observer 两 LOW 项兑现 — arxiv 样本截断（source sections 硬切片 = 采样假阳性家族第 3 位点）+ dream 远期信号时效标注（VERSION 0.37.9.95→0.37.9.96）** — ① **触发**: 用户「趁最强模型还有预算，继续执行: arxiv 样本截断、dream 远期信号标注」（observer 2026-07-03 日报 [LOW] #3/#4，今日 observer findings 全部清零收官）。② **(#3) arxiv 样本截断根因（数据驱动排查链）**: 观察到的 \"⚠️ LLM 评 ⭐4 偏高 (ru\" = project_alignment_scorer 校验标记切在 \"(rule 预期…\" 第 2 字符。逐层排除: arxiv 推送组装无截断 / KB note 写入全文 / sources 归档全文 → 真凶 = **daily_observer.scan_source_sections 的硬切片 `[:max_chars_per=500]` 无换行对齐**（V37.9.93/213 采样假阳性家族第 3 个位点: V37.9.93 修 tail 可见性 → V37.9.213 修 head-snap → 本版修 source sections——同一 bug 类三次演出在三条采样路径）。**修复**: 复用 SMART_SAMPLE_HEAD_SNAP_MAX 就近换行收尾 + 显式采样标注「…(样本截断于行边界, 完整 section 共 N 字)」（sampling-aware, LLM assessor 不再把采样痕迹误报为内容截断）; char_count 保持完整长度（报告统计语义不变）。③ **(#4) dream 远期信号标注**: dream 读全量 KB（多天累积），引用 ~3 个月前信号（2026-04-04/03-07/04-20）作「今日深度」印证无时效标注 → 读者误当当日新闻。**修复**: DEEP_SYSTEM + WIDE_RADAR_SYSTEM 两 system prompt 各加硬规则「引用日期距今超过 14 天的信号必须紧跟标注『(长期背景, 非近期新增)』，不得呈现为当日新增」（原则 #31 双 prompt 全量同步; 14 天阈值与 dream 主题 ban-list/deep_dive V37.9.233 同窗口一物一形）。④ **8 新守卫**: test_daily_observer +5（TestV37_9_235_SectionSnap: 超限 snap 到行边界+标注 / 未超限无标注 / **血案回归**（校验标记落 500 边界 → 要么完整要么不出现绝不半截, 含防空转断言）/ char_count 完整长度 / marker）+ test_kb_dream_helpers +3（TestV37_9_235_SignalFreshness: 双 prompt 各一条 count==2 / 长期背景字面量 / 14 天阈值对齐）。**两轮 sabotage 反向验证守卫真有效**（含一次测试自身校准修正: 初版血案 padding 算错 CJK 字数致空转 → 加防空转断言后 sabotage 精确抓 \"(ru\" 形态 2 FAIL / 删 WIDE_RADAR 规则 → 3 FAIL）。⑤ **操作教训（同 session 第 2 次演出即免疫）**: tmp+os.replace 原子写又丢 kb_dream.sh 755 位——V37.9.234-hotfix 刚踩过, 本次**提交前** git ls-tree 对比抓到并恢复（收工清单候选: 原子写后核对 mode）。⑥ **验证**: full_regression 145 suites/0 fail/**5178 tests (+8)** / governance 91/91 EXIT=0 / bash -n + py 双语法 / 安全 95/100 / **VERSION 0.37.9.95→0.37.9.96**（daily_observer 喂 LLM 的采样内容 + dream live prompt = live cron 组件 runtime 变更，镜像 V37.9.213 bump 惯例）。**复杂度预算账本**: files 不变（扩 2 组件 + 2 既有 test）/ env/jobs/runtime-state-sources 不变 / **净 runtime 机制 ≈ 0**（采样精度修复退役假阳性刺激源 + prompt 时效规则纯加一行硬规则）。⑦ **🔴 Mac Mini 待用户**: 合并 + git fetch+reset 同步（两文件走 FILE_MAP，零重启 cron 生效）→ 验证: 明日 06:30 observer 报告 arxiv 截断 finding 应消失 + 明日 03:00 dream 引用远期信号时带「长期背景」标注（用户视角抽查）。⑧ **元价值**: 2026-07-03 observer 日报 4 findings 当日全清（F1 deep_dive→V37.9.233 / DBLP→234 / #3+#4→235）——机械化人眼（宪法级 #1）的发现→修复闭环第一次完整跑通一整份日报。#3 的讽刺: observer 自己的采样管道第三次制造它自己报告的假阳性（V37.9.93→213→235 同类三修），印证论文 §5.2「Observer 也是 LLM 组件继承全 taxonomy」。 |
| V37.9.234 | 2026-07-03 | **DBLP 标题推断粒度收敛 — observer 提案 #1 兑现（退役结构性编造逼迫点，VERSION 0.37.9.95 不变）** — ① **触发**: 用户「继续执行: DBLP 标题推断粒度收敛」（observer 2026-07-03 日报 [MED] finding: DBLP 条目对论文核心贡献/关键方法/技术路径全标 \"基于标题推断\" 但仍给出过细方法描述——\"推测采用预训练的基础模型\"\"可能引入对比学习或适配器模块\"，无摘要支撑下推断粒度偏细存在误导风险）。② **根因（结构性逼迫点，非 LLM 不听话）**: `💡 关键方法` 字段要求**长度按评级动态调整 100-800 字**（⭐⭐⭐⭐⭐→500-800 字），但 DBLP 输入只有标题+会议名——标题支撑不了 800 字真实分析，LLM 被长度要求结构性逼迫编造细粒度细节（诚实标注挡不住长度硬指标）。V37.9.213 F4 判定 \"(基于标题推断)\" 标注本身是正确诚实行为（保留）；本次收敛的是**粒度**。③ **修复（prompt 三处 + 谱系注释）**: 💡 字段改「方向性解读（研究领域/要解决的问题/标题明确点名的方法大类），**长度固定 80-150 字不随评级扩展**——标题无法支撑更长的真实分析」+ 严格约束新增「推断粒度收敛: 严禁细粒度技术路径猜测（\"推测采用XX模型/架构\"\"可能引入XX模块/机制/损失函数\" 类表述一律禁止）」+ 输出格式模板同步（\"长度按评级规则\"→\"80-150 字方向性解读\"）+ 🔑 核心贡献收敛为「只写标题字面可支持的方向性判断」。评级扩展长度规则在 hf_papers/s2 等**有摘要**的源保持不变（那里输入支撑得起, 不改）。④ **5 新守卫**（test_v37_9_40_dblp_ailead.py TestV37_9_234_GranularityConvergence）: 评级扩展长度退役（3 个旧字面量 assertNotIn 含格式模板）/ 固定 80-150 字+不随评级扩展在位 / 细粒度猜测禁令在位（含观察者点名字眼形态）/ **(基于标题推断) 诚实标注保留**（防误删 V37.9.213 F4 正确行为）/ marker。**sabotage 反向验证守卫真有效**: 恢复评级扩展长度 → 2 FAIL，备份还原全过。⑤ **验证**: full_regression 145/0/**5170 tests (+5)** / governance 91/91 EXIT=0 / bash -n 通过 / 安全 95/100 / **VERSION 0.37.9.95 不变**（source prompt 配置变更，job 级零核心 runtime，镜像 V37.9.108-112/132 惯例）。**复杂度预算账本**: files 不变（扩 run_dblp.sh + 既有 test）/ env/jobs/runtime-state-sources 不变 / **净 runtime 机制 0**（prompt 收敛 = 退役一个结构性编造逼迫点，反幻觉纯减法）。⑥ **🔴 Mac Mini 待用户**: 合并 + git fetch+reset 同步（run_dblp.sh 走 auto_deploy 同步到 ~/.openclaw/jobs/）→ 明日 12:00 dblp cron 起生效；**验证 = 用户视角**（原则 #13）: 明日 DBLP 推送的 💡 关键方法应为短方向性解读，不再出现 \"推测采用XX模型\" 类细粒度猜测；observer 后续日报该 finding 应消失。⑦ **元价值**: 观察者(机械化人眼) → 用户转发 → 修复的完整闭环第二例（V37.9.213 F2 第一例修 observer 自己，本例修被观察的 job）。反幻觉的正解不是加更多守卫 prompt，是**找到并拆掉逼迫编造的结构性要求**（长度硬指标 vs 输入信息量的错配）——顺势设计原则 #22 的又一次兑现。 |
| V37.9.233 | 2026-07-03 | **deep_dive 77% 降级率真根因修复 — 已分析论文 14 天 ban-list（同一 ACM 付费墙 DOI 被 picker 连选 ~20 天，用户数据驱动确诊，VERSION 0.37.9.95 不变）** — ① **触发**: V37.9.232 收工后用户 Mac Mini 取回两组数据: freight last_run 停在 07-02 14:00:01（= V37.9.220 事故窗口残留，freight 每天 1 次 14:00 调度 V37.9.64 → 今日 14:00 run 预期自愈）+ **deep_dive 降级原因明细铁证**: `doi.org/10.1145/3774904.3792985`（ACM 付费墙 DOI，无 arXiv/OA 版）出现 **16+4=20 次** — 同一篇论文被 picker 连选 ~20 天。② **真根因（比 observer 的「fetch-path 退化」精确得多）**: 高对齐论文停留在 KB 源文件 recent 窗口内 + picker 无「已分析」去重 → 每天重新胜出 → 每天霸占唯一深度分析位且必然 abstract_only（30 天 77% 降级率的主体是**一篇重复论文**）+ 用户连收 ~20 天近似重复分析（dream 有 V37.9.68 14 天主题 ban-list，deep_dive 没有 = 惯例覆盖缺口）。V37.9.183 OA 解析没把率压下来的原因也在此: OA lookup 对这篇无 OA 版论文（新格式 4 次 incl. OA lookup 仍失败）无能为力。③ **修复（镜像 dream V37.9.68 惯例）**: `load_recent_analyzed_links(deep_dive_dir, days=14)`（扫 deep_dives/YYYY-MM-DD.md frontmatter `link:` 行 → 已分析链接集合；FAIL-OPEN 目录缺失/文件损坏→空集；非日期文件跳过）+ `filter_recently_analyzed(candidates, recent_links)`（link 两侧归一化精确匹配；空 link 候选永不 ban；**全部被 ban → 走 no_candidates 明日再试，绝不回退重复分析**）+ run() 在 pick_top **之前** wire + `banned_recent` 字段进 ok/no_candidates 两路 result（生产验证可观测: 明日 22:30 run 的 last_run 即可见 ban 生效）。`DEDUP_WINDOW_DAYS=14` 与 dream 同窗口。④ **11 新单测** TestV37_9_233_AnalyzedBanList: 窗口内加载+尾斜杠归一化 / >14 天排除 / 缺目录 FAIL-OPEN / 非日期文件跳过 / ban 匹配+保留其他 / 空集不变 / 空 link 永不 ban / **血案端到端（昨天分析过的 ACM DOI 今天再入候选 → 被 ban，picker 选新论文）** / run() wiring 顺序守卫（filter 在 pick_top 前）/ 14 天设计锁定 / marker。**两轮 sabotage 反向验证守卫真有效**（备份副本还原）: filter 瘫痪永不 ban → 2 FAIL（含血案端到端）/ run() 去 wiring → 1 FAIL。⑤ **验证**: full_regression 145/0/**5165 tests (+11)** / governance 91/91 EXIT=0 / 安全 95/100 / **VERSION 0.37.9.95 不变**（deep_dive picker 增强 = job 级非核心 runtime，镜像 V37.9.183 惯例）。**复杂度预算账本**: files 不变（扩 kb_deep_dive + test）/ env/jobs/runtime-state-sources 不变（ban-list 读既有归档文件零新状态源）/ **净 runtime 机制 ≈ 0**（2 纯函数复用 dream ban-list 惯例，退役「重复霸位」盲区）。⑥ **预期效果（登记验证项）**: 重复占位释放后 full-text 比例应显著回升（可抓的 arxiv 论文重新有机会被选）+ 剩余降级 = 真付费墙结构性（ACM/MDPI/Elsevier 无 OA 版，诚实接受）+ 用户不再连收重复分析。一周后 observer 降级率复核。⑦ **🔴 Mac Mini 待用户**: 合并 + git fetch+reset 同步（kb_deep_dive.py 走 FILE_MAP）→ 今晚 22:30 deep_dive 起 ban 生效（`cat ~/.kb/last_run_deep_dive.json` 可见 banned_recent 字段）。freight 今日 14:00 run 后 `cat ~/.openclaw/jobs/freight_watcher/cache/last_run.json` 应转 ok（07-02 事故残留自愈确认）。⑧ **元价值**: 观察者报了「率」（77%），用户取回的「原因明细」揭示率的主体是**一篇论文 × 20 天**——聚合指标掩盖了重复个体（V37.9.213 F1 降级原因分解 + 本次 uniq -c 明细两级下钻才见真相）。数据驱动把「加备用 PDF 通道」的猜测方向纠正为「补 picker 去重」的真修复（原则 #28: 理解再动手避免修错方向）。 |
| V37.9.232 | 2026-07-03 | **治理 07:00 红灯根治 — 治理 runtime 所跑测试对生产 env 不 hermetic（FALLBACK_ORDER/PROVIDER 泄漏，dev 完整复现 + 判别器固化为 CI 守卫，VERSION 0.37.9.95 不变）** — ① **触发**: 用户贴 07-03 07:00 治理审计告警（INV-FALLBACK-EXCLUDE-001 ❌ + INV-CONVERGENCE-INTEGRATION-001 💥×2）+ 08:30 watchdog + observer 日报。数据驱动 triage（原则 #28）: dev 用生产同款 env（FALLBACK_ORDER + PROVIDER=doubao_21 + 假 key）**完整复现 ❌ 且多抓出一个 Mac Mini 尚未触发的潜伏项**（INV-ROUTER-001 ❌）。② **根因（「生产 env 污染测试」= MR-9 测试污染生产的镜像形态）**: 治理 runtime python_assert 以 subprocess 跑测试文件、子进程继承生产 env——(a) `test_adapter._exec_build` 的 mock env 用 `_os.environ.copy()` 起步 → FALLBACK_ORDER 泄漏进被测 `_build_fallback_chain` → 走 V37.9.218 新分支调 `reg.get()` → V37.9.129 时代 MockReg 无 `.get` → AttributeError（该 check 声明「mock 路径 env-independent」为假）(b) `TestAdapterResolvePrimaryProvider.setUp` 用 `os.environ.setdefault(\"PROVIDER\", \"qwen\")` → 生产已有 PROVIDER=doubao_21 时 setdefault 无效 → import adapter 得 doubao_21 → 6 个断言 name==qwen 的测试全挂。**时间线吻合**: V37.9.218/222 flip 是 07-02 白天，07-03 07:00 是第一次带此 env 的审计 = 首次红灯；dev 无此 env 永远绿（dev-production 接缝，日落法 #34）。③ **修复（测试 hermetic 化）**: `_exec_build` mock env 空字典起步（`env = {}` + overrides，不再 copy 进程 env）+ setUp 改 `patch.dict` 强制基线（PROVIDER=qwen + REMOTE_API_KEY）+ 显式 pop 路由类 env（FALLBACK_ORDER/FALLBACK_PROVIDER/FAST_PROVIDER/MODEL_ID/VL_MODEL_ID/ROUTER_ENFORCE），stop 时整体还原不污染进程。④ **判别器固化** test_v37_9_232_env_hermetic.py（5 测试）: **生产同款 env 注入真 subprocess 跑两套件必须绿**（INV-FALLBACK 的 3 测试精确命令 + V37.9.77 全套件 = 血案 ①② 的复现命令各自固化）+ 源码守卫（`env = {}` 在位 / `env = _os.environ.copy()` 退役——精确断言含赋值前缀防守卫被自己 docstring 咬 V37.9.178 教训 / patch.dict+pop 在位 / setdefault 形态退役 / markers）。**两轮 sabotage 反向验证守卫真有效**（备份副本还原）: 回退 environ.copy() → 2 FAIL / 回退 setdefault → 4 FAIL。**根治铁证**: 生产同款 env 下完整 governance **EXIT=0 所有不变式成立**（修复前同 env: 2 INV ❌）。⑤ **💥 部分诚实分诊（不盲修）**: INV-CONVERGENCE-INTEGRATION-001 两 runtime check 是 requires_full（仅 Mac Mini）+ 各 spawn 完整内层 `governance_checker --json`（timeout=120s）→ 07:00 cron 风暴下内层超时 TimeoutExpired = 💥（V37.9.214 登记的「治理 runtime check 对负载敏感」家族，dev 不可复现）。**判别器**: Mac Mini 同步后静时重跑 `governance_checker --full`——若 ❌ 消失（本版修）且 💥 也消失 = 负载瞬态；若 💥 仍在 = 下 session 按 V37.9.214 模式硬化（TimeoutExpired→inconclusive + 加大 timeout），登记 unfinished。⑥ **同报告其余分诊**: github_trending 41 错/hn 13 错/SLO p95 69s = **07-02 14:00 事故残留**（V37.9.220 已诊断回滚，时间戳吻合，随新 run/窗口冲刷自愈，无需动作）；freight llm_failed 待确认今日 run 是否自愈；observer 日报 4 findings 登记（DBLP 标题推断粒度收敛 prompt 修 dev-fixable / deep_dive 降级原因分解首次可用: PDF 抓取失败 20/24 = fetch-path 类非付费墙 → Mac Mini 取具体错误定向修 / arxiv 样本截断 LOW / dream 远期信号标注 LOW）。⑦ **验证**: full_regression 146 suites/0 fail/**5154 tests (+5)** / governance 干净+生产两 env 双 EXIT=0 / 安全 95/100 / **VERSION 0.37.9.95 不变**（纯测试隔离+守卫零业务语义，镜像 V37.9.157/158-hotfix 惯例）。**复杂度预算账本**: files +1（判别器守卫 test）/ env/jobs/runtime-state-sources 不变 / **净 runtime 机制 0**（测试 hermetic 化收窄 dev-production 接缝，退役两个假 hermetic 漂移源）。⑧ **🔴 Mac Mini 待用户**: 合并 + git fetch+reset 同步 → **静时重跑 `python3 ontology/governance_checker.py --full`**: INV-FALLBACK-EXCLUDE/INV-ROUTER 应转 ✅（明日 07:00 起每日审计不再红）+ 观察 INV-CONVERGENCE 💥 是否消失（判别负载瞬态 vs 需硬化）。⑨ **元价值**: V37.9.159 的 scanner 管「测试污染生产」，本版补上镜像盲区「生产污染测试」——治理在生产 env 里跑 dev 时代写的测试，env 每次 flip（V37.9.218/222）都可能让假 hermetic 测试翻红。判别器固化后，未来任何测试重新依赖进程 env 会在 dev CI 立即红灯，不再等 Mac Mini 07:00。 |
| V37.9.231 | 2026-07-03 | **审计 finding E 兑现 — client 断开 ≠ backend 失败（_deliver 投递 helper 收编四回写点，不误开断路器/不对死 socket 跑 fallback 链，VERSION 0.37.9.94→0.37.9.95）** — ① **触发**: 用户「还有预算，基于专业评判继续待办」。剩余审计四项（C 锁设计/E 热路径/I tradeoff/B-delivery murky）中 E 是唯一设计明确、可单 session 谨慎完成的，且正好补完 V37.9.224（fallback 请求体正确性）+ V37.9.229（fallback 可观测）的 fallback 韧性弧线。② **血案机制（V37.9.220 adapter.log 实录）**: 批量 job 的 HTTP client 超时先断开 → adapter 上游成功后回写响应撞 BrokenPipeError；此前回写在 `_forward_request` 的 try 内 → **两 defect**: (a) primary 路径 client 断开被 except 当 backend 失败 → 健康 backend 被记 `_circuit_breaker.record_failure()`（误开断路器加速器）+ 触发 fallback 链 (b) fallback 路径 client 断开被 per-fallback except 当 fallback 失败 → 继续对死 socket 试下一个（每个都「FALLBACK OK ... 1ms 后 FAILED Broken pipe」）→ 链耗尽浪费 ~500s tail + 上游配额。③ **修复（一物一形投递策略）**: `_deliver(status, body, extra_headers, tag)` helper — 回写单独兜 **OSError**（BrokenPipe/ConnectionReset 写 socket 失败族；编程错误 TypeError 等照常传播可见），client 断开只记 `CLIENT GONE` 日志绝不冒泡；**四个回写点全收编**（primary 成功 / no-chain 502 / fallback 成功含 V37.9.229 header / all-failed 502），do_POST 内裸 `_send_json` 调用退役（唯一调用点在 _deliver 内部）。CB 语义不变: record_success 在投递前（backend 健康度只看 backend 调用）。④ **14 新单测** test_v37_9_231_client_disconnect.py（3 类）: TestDeliverHelper(5 handler 无 socket: BrokenPipe/ConnectionReset→False 不冒泡 / **非 OSError 传播**（except 范围严格）/ header 透传) + **TestClientGoneE2E(2 真 ThreadedServer + SpyCB + 计数 forward + class-level monkeypatch _send_json 模拟 client 断开**: primary 成功+client gone → 上游只调 1 次不跑 fallback + CB failures=0 / primary 挂+fb1 成功+client gone → fb2 绝不被调链停止 + primary 真失败仍正常记 CB) + TestSourceGuards(7: helper 在位 OSError-scoped / CLIENT GONE / 四回写点 count==4 / **裸 _send_json 调用退役 count==1** / V37.9.229 header 保留 / record_success 在投递前 / marker)。连带演进 1 个 V37.9.229 守卫（primary 投递形态 _send_json→_deliver, 无 extra_headers 语义不变）。**两轮 sabotage 反向验证守卫真有效**（备份副本还原 byte-identical + 清 __pycache__）: primary 退回 try 内裸 send → 3 FAIL+1 ERROR（E2E 精确复现血案: fallback 误跑+CB 误记）/ fallback 退回裸 send → 4 FAIL（链不停止被抓）。⑤ **验证**: full_regression 145 suites/0 fail/**5149 tests (+14)** / governance 91/91 EXIT=0 / 安全 95/100 / 安全扫描零泄漏 / **VERSION 0.37.9.94→0.37.9.95**（adapter 请求热路径投递语义 = runtime 变更，镜像 V37.9.229 bump 惯例）。**复杂度预算账本**: files +1（test）/ env/jobs/runtime-state-sources 不变 / **净 runtime 机制 ≈ 0**（1 投递 helper 替代 4 处裸回写 = 投递错误策略一物一形，退役「client 断开污染 backend 健康度/fallback 路由」盲区，非新接缝）。⑥ **🔴 Mac Mini 待用户**: 合并 + git fetch+reset 同步 → adapter.py 经 auto_deploy FILE_MAP 同步 + 按需 restart → 生效后 V37.9.220 类场景（client 超时先走）adapter.log 出现 `CLIENT GONE` 行而非「FALLBACK OK→FAILED Broken pipe」链爆；断路器不再被 client 断开污染。⑦ **诚实边界（登记不捆绑）**: 非 chat 端点（/embeddings 等无 fallback 路径）的回写仍裸写（client 断开会 502-to-dead-socket 传播到 http.server 日志，无 CB/fallback 语义影响 = 噪声非缺陷，低优）。⑧ **元价值**: 「fallback 是故障保险非延迟保险」（V37.9.220 洞察）的最后一块拼图——V37.9.221 A2 分流批量、V37.9.224 fallback 体正确性、V37.9.229 fallback 可观测、**V37.9.231 投递失败与 backend 健康度解耦**。2026-07-02 审计 9 findings: 明确+机械+可设计项全清（SEC/F/B-stats/A/D/G/H/E），剩 C（跨进程锁）/ I（tradeoff）/ B-delivery（murky）三项登记。 |
| V37.9.230 | 2026-07-03 | **审计 LOW 批次三项兑现 — D notify 并发重复投递 TOCTOU（mv-to-own claim）+ G daily_observer 注册 watchdog（时间格式对齐，堵观察者自己的观察盲区）+ H kb_deep_dive push 全失败 fail-loud（VERSION 0.37.9.93→0.37.9.94）** — ① **触发**: V37.9.229 收工后用户「继续处理待办事项」，unfinished[0] 剩余分诊中三个机械可修项（D mv-claim / G dev 可修 / H 机械）一批兑现（V37.9.227「cron 静默状态」主题延续 + V37.9.140 多小修同 session 惯例）。② **(D) notify.sh drain mv-to-own claim**: 两并发 notify 同时 drain 都 find 到同一队列文件 → 双双重放 = 用户收重复消息（TOCTOU）。修复: 处理前 `mv $f $f.claim.$$`（rename 原子只有赢家成功，输家 continue）+ 瞬态失败 mv 回原名（at-least-once 语义不变）+ **孤儿 claim 回收**（进程在 claim 窗口内被杀留下 *.claim.<pid> 不再匹配 *.json 永不重放=消息丢失 → drain 开头把 >10min 的 claim 恢复回 .json；极端「发送成功后被杀」恢复重放=重复，与修复前语义相同不劣化）。V37.9.177 淘汰语义（poison EVICT+解队头阻塞）+ V37.9.180 冷调用判定在 claim 机制下完整保留。③ **(G) daily_observer 注册 watchdog**: 审计发现**宪法级 #1 LLM-Observer 的宿主 job**（06:30 每日自评）不在 watchdog——last_run_self_critique.json 的 time 用 ISO8601 UTC（date -u +%Y-%m-%dT%H:%M:%SZ）而 watchdog strptime('%Y-%m-%d %H:%M:%S') 解析不了 → 无法注册 → observer 自己静默死无人发现（观察者的观察盲区，MR-4 家族讽刺闭环）。修复: 时间格式对齐本地 HKT（watchdog 内部 -8h 转 UTC 契约）+ JOB_STATUS 注册（180000s=50h 阈值镜像其他 daily job，core tier；状态语义: ok 正常 / llm_failed/parse_error/no_outputs 走 catch-all 告警——上游 evening/dream/deep_dive 天天跑，「无产出」本身就是异常非安静日）。④ **(H) kb_deep_dive push 全失败 fail-loud**: 此前 WA 段失败/Discord 失败都只 WARN，status 恒写 ok → 用户什么都没收到但 watchdog 静默。修复: WA 0 段 且 Discord 失败 → `write_status \"push_failed\"`（watchdog catch-all「异常状态」告警）；任一通道送达 → 内容已到用户保持 ok（部分失败已 WARN 不过度告警）；**不 exit 1**（产出已归档，rsync 取证备份必须继续跑，镜像 V37.9.227 取舍）。⑤ **21 新单测**: test_notify_weixin +10（TestNotifyQueueClaimV230 行为级 5: 成功无孤儿/瞬态恢复原名/**他进程新鲜 claim 不碰不重放=防重复核心**/孤儿 claim 回收后重放不丢失/V37.9.177 淘汰语义保留 + SourceGuards 5；_run_drain harness 加 age 参数）+ test_daily_observer +5（格式对齐/ISO 形态退役/watchdog 注册/**跨文件契约 MR-8: 两侧源码各自提取格式字面量真实渲染+strptime 解析，防未来单侧改格式漂移**/markers）+ test_kb_deep_dive +6（push_failed 在位/全失败判定/双分支 DISCORD_SEND_OK 原则 #31/**无条件 ok 退役 count==1**/push_failed 到 rsync 间无 exit/marker）。连带演进 1 旧守卫（test_transient_still_breaks 正则接受 mv-back 行，瞬态仍 break 语义不变）。**四轮 sabotage 反向验证守卫真有效**（备份副本还原 byte-identical）: 删瞬态 mv-back → 3 FAIL / 孤儿阈值改 999999min → 2 FAIL / 时间格式退回 ISO → 3 FAIL / push_failed 退回无条件 ok → 3 FAIL。⑥ **验证**: full_regression 144/0/**5135 tests (+21)** / governance 91/91 EXIT=0 / cross_os_quirk_scanner 0 violations / 安全 95/100 / 安全扫描零泄漏 / **VERSION 0.37.9.93→0.37.9.94**（notify 队列语义 + observer 状态文件格式 + deep_dive 状态语义 = job/推送层可观测性与可靠性变更，镜像 V37.9.227 status 语义 bump 惯例）。**复杂度预算账本**: files 不变（扩 3 脚本 + 3 既有 test）/ env/jobs/runtime-state-sources 不变（.claim 是队列条目的瞬态改名非新状态源）/ **净 runtime 机制 ≈ 0**（claim=既有队列的原子化处理、watchdog 注册=既有监控网补一格、push_failed=既有 write_status 的诚实化——三项全是退役静默盲区非新接缝）。⑦ **🔴 Mac Mini 待用户**: 合并 + git fetch+reset 同步 → notify.sh/daily_observer.sh/job_watchdog.sh/kb_deep_dive.sh 经 auto_deploy FILE_MAP 同步（零服务重启，cron 下次运行自动生效）→ 验证: (a) 明日 06:30 后 `cat ~/.kb/last_run_self_critique.json` 的 time 应为本地格式 + 下个 watchdog 周期（4h）对 daily_observer 不再盲 (b) notify 并发重复投递消失（低频靠机制保证）(c) deep_dive 全通道推送失败时 watchdog 告警「异常状态 (push_failed)」。⑧ **元价值**: 2026-07-02 审计 9 项 finding 中可机械修复的全部清零（SEC→V37.9.226 / F→227 / B-stats→228 / A→229 / **D+G+H→230**），剩 C（跨进程锁需设计）/ E（热路径需谨慎）/ I（设计 tradeoff）/ B-delivery（murky）诚实登记待专门评估。G 的讽刺最深: 造出来盯全系统静默故障的 observer，自己就是 watchdog 网里最后一个静默盲区。 |
| V37.9.229 | 2026-07-03 | **审计 finding A 兑现 — SLO fallback 可观测（record_fallback 复活 + slo 原始计数落盘，修 fail-plausible SLO，VERSION 0.37.9.92→0.37.9.93）** — ① **触发**: V37.9.226 审计登记 finding A（MED-HIGH 静默 SLO）——`record_fallback` 死代码零生产调用 → `degradation_rate_pct` 结构性永远 0% → slo_benchmark 永远 PASS / slo_dashboard 永远 Fallbacks:0。**根因架构性**: proxy_stats 在 tool_proxy（adapter 之前），fallback 发生在 adapter 内部，proxy 只见正常 200 无从得知。V37.9.220 场景（primary 全宕 100% fallback）时 SLO 仍报 healthy = **fail-plausible SLO**（系统自己的仪表盘上演论文 arXiv:2606.14589 批判的模式）。② **设计（复用既有 proxy↔adapter HTTP 接缝，零新状态源，日落法）**: 审计给的两修法（header surface / adapter 侧独立采样）选 **header**——adapter fallback 成功响应加 `X-Adapter-Fallback: <provider>` 响应头（`_send_json` 加可选 `extra_headers`；**primary 成功/502 全败不带**——前者防误标降级虚高、后者是错误由 record_error 记非降级服务）→ tool_proxy 在 Backend log 后、parse 块前读 header 调 `proxy_stats.record_fallback()`（放 parse 前 = 请求被 fallback 服务这一事实与后续 parse 成败无关；header 在 proxy 消费不下传 gateway，proxy 自建响应头）。弃选 adapter 侧独立采样（新状态源+新机器，违复杂度预算）。③ **同 finding 第二个潜伏 gap 一并修（grounding 时发现）**: `_write_stats`/`get_stats_dict` 的 slo 字典**从未落盘** 4 个原始计数（`fallback_count`/`tool_calls_success`/`recovery_total`/`failure_streaks`），而 slo_benchmark（degradation.fallback_count / tools.success_calls / recovery.*）与 slo_dashboard（Fallbacks 行）一直在读它们 → 报告恒 0；补齐后 recovery verdict 的样本基数（failure_streaks）也从恒 0 变真值（此前 recovery 判定结构性永远 OBSERVING）。④ **20 新单测** test_v37_9_229_slo_fallback_surface.py（6 类）: TestAdapterSendJsonExtraHeaders(3 handler 无 socket 实例化) + **TestAdapterFallbackHeaderE2E(3 真 ThreadedServer 驱动完整 do_POST**——V37.9.224 的 E2E 方法首次固化为回归测试: fallback 服务带 header / primary 不带 / 502 不带) + TestProxyHeaderConsumptionBehavior(2 literal-as-guard 抽 tool_proxy 真源码块 exec + 真 ProxyStats 行为级) + TestProxyStatsSloRawCounters(2) + **TestConsumerChainSeesFallback(2 消费链贯通**: ProxyStats→slo_benchmark degradation.fallback_count=2/20% + slo_dashboard snapshot 非零) + TestSourceGuards(8 wiring 位置/双写点 count==2/marker/header 不下传/primary 不带)。**三轮 sabotage 反向验证守卫真有效**（备份副本还原 byte-identical + 清 __pycache__，V37.9.134 教训）: 删 adapter header → 2 FAIL / 删 proxy record_fallback → 3 FAIL+1 ERROR / 删 slo dict 一字段 → 3 FAIL+1 ERROR（两个消费链测试的失败形态正是修复前的盲区）。⑤ **测试隔离（MR-9）**: 新测试全部 monkeypatch STATS_FILE 到临时目录绝不写真实 ~/proxy_stats.json；**顺带登记既有观察（低优不捆绑）**: test_config_slo TestProxyStatsSLO 直接 ProxyStats()+record_success 触发首次 _maybe_flush 写真实 ~/proxy_stats.json（Mac Mini 跑回归时瞬态污染、被 live proxy 下个 FLUSH_INTERVAL 覆盖自愈）。⑥ **验证**: full_regression 144 suites/0 fail/**5114 tests (+20)**（run1 抓徽章漂移 → --write → run2 全绿，V37.9.189 流程）/ governance 91/91 EXIT=0 / 安全 95/100 / 安全扫描零泄漏 / **VERSION 0.37.9.92→0.37.9.93**（proxy+adapter 请求热路径可观测性 = runtime 变更，镜像 V37.9.228 bump 惯例）。**复杂度预算账本**: files +1（test）/ env/jobs/runtime-state-sources 不变 / **净 runtime 机制 ≈ 0**（1 响应 header + 1 header 读 + 4 落盘字段 = 复活既有死代码、退役"degradation 恒 0"盲区，复用既有 HTTP 接缝非新接缝）。⑦ **🔴 Mac Mini 待用户**: 合并 + git fetch+reset 同步 → adapter/tool_proxy/proxy_filters 经 auto_deploy FILE_MAP 同步 + 按需 restart 两服务 → 生效后任何真 fallback 服务的请求: proxy 日志出现 `DEGRADED: served via adapter fallback (<provider>)` + proxy_stats.json 的 slo.fallback_count/degradation_rate_pct 变真值 + slo_dashboard/slo_benchmark/健康周报可见降级率。⑧ **元价值**: 系统自己的 SLO 仪表盘曾有一个永远绿的静默指标——修复后 V37.9.220 类"primary 全宕 100% fallback"场景从"SLO 报 healthy"变为"degradation_rate 立即可见"。2026-07-02 审计三个明确项全部闭环（F→V37.9.227 / B-stats→V37.9.228 / A→V37.9.229），剩 C/D/E/G/H/I + B-delivery 登记在案待深入。 |
| V37.9.228 | 2026-07-02 | **审计 finding B 部分兑现 — proxy parse-block 抛异未记 success 时 record_error（修零 stats 信号监控盲区，VERSION 0.37.9.91→0.37.9.92）** — ① **触发**: V37.9.226 审计登记的 finding B（明确热路径静默失败）——tool_proxy do_POST 的 200-response 处理块包在 `try/except Exception`，except **不 re-raise 直接 fall through** 发 `resp.status`(200) + raw body。若 `json.loads(resp_body)` 抛异（**backend 返 200+garbage**）：`record_success` 未到 + `record_error` 在**外层** except（永不达）→ 请求**零 stats 信号** → `consecutive_errors`/告警/incident snapshot 永不触发 → 监控对 backend 200+乱码完全失明 + forward 给 gateway 作 plausible 200 = **fail-plausible**（正是论文批判的模式）。② **最小安全修复（原则 #28，热路径 contained）**: parse block 前置 `_recorded_success = False` flag，两处 `record_success` 后置 True，parse-except 里 `if not _recorded_success: record_error(502, ...) + pop_alerts`。**happy 路径零改动**（flag 仅在 except 读）；已合法记 success 的路径（如 build_sse_response 后崩）不重复记（success 真实发生）。③ **9 新单测** test_v37_9_228: 6 源码守卫（flag 初始化/置 True 顺序/except record_error/pop_alerts/门控非无条件/marker）+ **3 行为级用真 `ProxyStats`（proxy_filters 可 import）复现确切 flag 控制流**: backend 200+garbage → `consecutive_errors=1`（监控可见，原为 0 盲区）/ transform 抛异 → error 记 / happy 路径 → `consecutive_errors=0`（不误记）。**两轮 sabotage 反向验证守卫真有效**: 移除 except record_error 分支 → 3 FAIL / 移除 flag=True → 1 FAIL；备份还原 9/9 全过。④ **诚实边界 → 剩 follow-up（登记 audit TODO）**: streaming 序列化失败（build_sse_response 抛异，success 已合法记）后仍发 `application/json` 给 SSE client = **内容类型错**（独立 delivery bug，修法 murky 需专门评估，非本版 stats 信号修复范围）。⑤ **验证**: full_regression 142/0/**5094 (+9, run1 抓 badge 漂移 → --write → run2 全绿)** / governance 91/91 EXIT=0（MRD-SILENT-EXCEPT 该块本就 log 非 silent，加 record_error 仍非 silent）/ 安全 95/100 / **VERSION 0.37.9.91→0.37.9.92**（proxy 请求热路径 stats 语义 = 可观测性 runtime 变更）。**复杂度预算账本**: files +1（test）/ env/jobs/runtime-state-sources 不变 / **净 runtime 机制 ≈ 0**（1 flag + except record_error，退役"零信号盲区"，非新接缝）。⑥ **🔴 Mac Mini 待用户**: 合并 + git fetch+reset 同步 → tool_proxy 经 FILE_MAP 同步 + 重启 → backend 返 200+garbage 时 proxy_stats 记 error（consecutive_errors 累加 → 达阈值触发 incident snapshot + 告警），不再静默透传。⑦ **元价值**: 修复系统自己的监控盲区——一个 backend 返 200+乱码的请求原本对 SLO/告警完全隐形（fail-plausible），现在被计入错误统计。用真 ProxyStats 行为级证明（consecutive_errors 0→1）而非仅源码守卫，把"修了盲区"从声明升级为机器可证。热路径改动严守最小 contained（flag 仅 except 读，happy 路径零改）。 |
| V37.9.227 | 2026-07-02 | **审计 finding F 兑现 — cron 静默状态 fail-loud（ontology_sources/finance_news 全源抓取失败 + finance_news LLM 全失败不再伪装 status:ok，VERSION 0.37.9.90→0.37.9.91）** — ① **触发**: V37.9.226 审计登记的 finding F（明确的静默失败，机械可批修）第一时间处理。**根因**: ontology_sources + finance_news 有 `FETCH_ERRORS` 计数器（每源失败 +1），但 `new==0` 出口**从不 consult 它** → 全部 RSS 源抓取失败时仍写 `status:"ok"` → watchdog 状态检查静默（与"平静无新文章"不可区分，MR-4 家族）。finance_news 额外：LLM 3 次全失败时**无条件**写 `status:"ok"`（唯一非 fail-loud 的 LLM-content job，兄弟 job 都写 llm_failed + 告警）。② **理解再动手（原则 #28，纠正审计 agent 过度泛化）**: 审计 agent 称 rss_blogs:217/ai_leaders_blogs:236「same shape」，但核实**它们无 FETCH_ERRORS 计数器**（每 feed FAIL-OPEN WARN+skip 无计数）→ 只有 ontology_sources + finance_news 是「consult 既有计数器」的干净低风险修复。③ **交付**: (a) ontology_sources new==0 块: `FETCH_ERRORS >= ${#RSS_FEEDS[@]}`（全 RSS 源失败=系统性）→ `status:"fetch_failed"` + `exit 1`（watchdog fetch_failed 显式 case 告警）；否则 ok+new=0（真平静日）。(b) finance_news new==0 同款（FETCH_ERRORS 含 RSS+X，>= RSS 源数即系统性）。(c) finance_news 最终状态记录: `LLM_OK != true → RUN_STATUS="llm_failed"`（参数化，退役无条件 `status:"ok"` 硬编码）→ watchdog catch-all 告警。④ **watchdog 契约验证**: `fetch_failed` 有显式 case（"最近一次抓取失败"）+ 在 log-scan `err_pattern`；`llm_failed` 走 catch-all `*)`（非 ok/unknown 即告警）。两状态都被告警，非静默非崩溃。⑤ **8 新单测** test_v37_9_227（源码级 fix 模式在位 + 旧无条件 ok 退役守卫 + watchdog 状态兼容契约 + 诚实登记 rss_blogs/ai_leaders_blogs 无计数器）。**sabotage 反向验证守卫真有效**: finance_news 退回无条件 ok → guard FAIL，备份还原 8/8 全过。⑥ **诚实边界 → follow-up（未修，登记 audit TODO）**: rss_blogs/ai_leaders_blogs 加 FETCH_ERRORS 计数器（增量 invasive，且 auxiliary tier + 48h KB-staleness watchdog 网部分覆盖）。⑦ **验证**: full_regression 141/0/**5085 (+8, run1 抓 badge 漂移 → --write → run2 全绿)** / governance 91/91 EXIT=0 / 安全 95/100 / bash -n 两 job 通过 / **VERSION 0.37.9.90→0.37.9.91**（cron job 状态语义 = 可观测性修复，job 级非核心 runtime，同 V37.9.78/147 惯例但含 status 语义变更故 bump）。**复杂度预算账本**: files +1（test）/ env/jobs/runtime-state-sources 不变 / **净 runtime 机制 ≈ 0**（consult 既有 FETCH_ERRORS 计数器 + 参数化 status，退役 2 处静默 ok 伪装，非新接缝）。⑧ **🔴 Mac Mini 待用户**: 合并 + git fetch+reset 同步 → 两 job 经 auto_deploy FILE_MAP 同步 → 下次 cron（ontology 10:00/20:00 / finance 07:30）起全源宕/LLM 全失败会写 fetch_failed/llm_failed → watchdog（每 4h）告警（不再静默）。⑨ **元价值**: MR-4 静默失败在 cron 状态层的又一次收敛——"全源宕看起来像平静日"从「靠肉眼/48h staleness 发现」升级为「watchdog 4h 内状态告警」。理解再动手纠正了审计 agent 的过度泛化（4 job 声称同 shape，实 2 job 有计数器），只修干净的、诚实登记需加计数器的。 |
| V37.9.226 | 2026-07-02 | **多镜头对抗审计驱动安全加固 — data_clean 路径限制（HIGH，防提示注入读密钥）+ ingest cap/Content-Length guard（MOD/LOW），9 个潜在威胁登记待办（VERSION 0.37.9.89→0.37.9.90）** — ① **触发**: 用户「用最强能力彻底检查系统漏洞/bug/威胁，明确的第一时间处理，潜在的登记待办」。先体检（干净树 full_regression 140/0/**5053**，混沌审计层真跑 Category A **10/10 全抓**，安全 95/100）→ 三镜头并行审计 agent（并发·共享状态 / 输入验证·注入 / 静默失败·fail-plausible，各读真实代码非假设）。② **明确威胁 → 第一时间修（本版，安全主题）**: **[SEC-1 HIGH] data_clean 任意文件读**——`data_clean` 自定义工具注入到**每个** tool-enabled 请求（CUSTOM_TOOLS），PA LLM 可用任意 `file` 参数调用 → `os.path.expanduser` → data_clean.py profile/execute **读文件内容返回给用户**，**无路径限制**。untrusted RSS/HN 内容在 PA 上下文（提示注入，CLAUDE.md 明确的威胁面）可诱导它读 `~/.env_shared`（API keys）/ `~/.ssh/*` / `/etc/passwd`，解析行直接泄漏进 chat。**修复 = allowlist confinement**（`_confine_data_path`）：resolved realpath（symlink + `..` 逃逸经 `os.path.realpath` 归一）必须在允许数据目录下（`~/.openclaw/media/inbound` Gateway 上传 + `~/.data_clean` 工作区），os.sep 后缀防 prefix 混淆（`.data_clean` vs `.data_clean_evil`），default-deny + **可见拒绝日志**（非静默）+ env 逃生舱 `DATA_CLEAN_ALLOWED_DIRS`。三执行点全覆盖（custom tool profile/execute + REST profile/execute/validate 的 file+cleaned）。**[SEC-2/3 MOD/LOW] ingest cap**——tool_proxy/adapter do_POST 的 `int(Content-Length)` 裸调在 try 外（畸形值崩线程）+ 无 body 上限（超大 Content-Length 读入内存前不拒 → OOM）→ 优雅 400 + 读入前拒超 `_MAX_INGEST_BYTES`（32MB DoS backstop，legit 流量远低于此）。③ **24 新单测** test_v37_9_226（extract-source + exec-with-mocks，tool_proxy 顶层 serve_forever 不可 import 的项目惯例 V37.9.132/175）: TestConfinementBehavior（血案核心敏感文件拒 + `..`/prefix 逃逸拒 + 合法目录通过不破坏功能 + 空/None/非 str FAIL-CLOSE + env 逃生舱 + 返回 resolved realpath 契约）+ TestConfinementWiring（三执行点源码守卫 + 旧裸 expanduser 直连退役 + marker）+ TestIngestCap（双 server Content-Length guard + 413 + `_MAX_INGEST_BYTES`）。**三轮 sabotage 反向验证守卫真有效**（备份副本还原 + 清 __pycache__）: 移除 confine startswith → 8 FAIL / 移除 proxy ingest cap → 1 FAIL / REST validate cleaned 不 confine → 1 FAIL；还原 byte-identical 24/24 全过。④ **潜在系统威胁 → 登记待办（9 项，全经代码验证真实非误报，unfinished[0]）**: (A) **record_fallback 死代码零生产调用 → SLO degradation_rate 结构性永远 0%**（架构性：proxy_stats 在 adapter 前，看不到 adapter 内部 fallback；V37.9.220 全 fallback 场景 SLO 仍报 healthy = fail-plausible SLO，正是论文主题）(B) tool_proxy parse-block try/except 不 re-raise → 200 透传 + 零 stats 信号 / streaming 序列化失败发 application/json 给 SSE client (C) status.json/kb index.json/audit_log 跨进程 RMW 无锁+固定 tmp → 丢更新/损坏/假 tamper 共享根因 (D) notify drain claim TOCTOU 重复投递 (E) adapter broken-pipe 后误记 CB failure+跑死 fallback 链 (F) finance_news status=ok 无条件写 + dead FETCH_ERRORS 4 job（全源宕=status ok watchdog 不告警，机械可批修 = 下一批候选）(G) daily_observer 不在 watchdog + ISO8601 时间格式 watchdog 解不了 (H) kb_deep_dive push 全失败 status 仍 ok (I) CircuitBreaker half-open thundering herd（设计 tradeoff）。⑤ **验证**: full_regression 140/0/**5077 (+24, run1 抓 badge 漂移 → --write → run2 全绿 V37.9.189 流程)** / governance 91/91 EXIT=0 / 安全 95/100 / 安全扫描零泄漏 / **VERSION 0.37.9.89→0.37.9.90**（tool_proxy/adapter 请求入口安全 guard = runtime 变更）。**复杂度预算账本**: files +1（test）/ env +1（DATA_CLEAN_ALLOWED_DIRS 逃生舱，未设时用默认 allowlist）/ jobs/runtime-state-sources 不变 / **净 runtime 机制 = 1 路径 confinement guard + 1 ingest guard**（安全正确性 default-deny，非新框架；退役"任意文件读"攻击面）。⑥ **🔴 Mac Mini 待用户**: 合并 + git fetch+reset 同步 → tool_proxy/adapter 经 FILE_MAP 同步 + 重启（launchctl kickstart）→ **验证 data_clean 合法工作流未破坏**：若上传的数据文件不在 `~/.openclaw/media/inbound`/`~/.data_clean` 下被拒（日志 `[data_clean] REJECT`），把其目录加进 `.env_shared` 的 `DATA_CLEAN_ALLOWED_DIRS`（冒号分隔）。⑦ **元价值**: 用户「彻底检查」催生三镜头并行审计——最严重的是一个提示注入 → 密钥泄漏链（data_clean 无 confinement），修复方式是标准 default-deny allowlist（realpath 归一防逃逸）。9 个潜在威胁诚实登记而非 rush（架构性 SLO 盲区/跨进程锁/热路径断路器误记需设计，不投机改热路径），其中 record_fallback 死代码致 SLO 永远绿是最讽刺的 fail-plausible 实例（系统自己的 SLO 仪表盘有一个永远绿的静默指标，正是它论文批判的东西）。 |
| V37.9.225 | 2026-07-02 | **上游情报收集 — unfinished [5](c) 销项 + 🔴 [9] Option A 复活（V37.9.180 误判修正）+ [0](B) 核实销项（npm tarball 实证方法，纯情报/doc/账本零代码 VERSION 不变）** — ① **触发**: 用户「处理下一个待办事项」。22 项 unfinished 逐核，绝大多数 Mac-Mini-gated/in-review/deferred-by-design，真 dev 可执行 = [5](c)「上游 releases 查 CLI 冷启动/staging 修复」（tripwire 证据收集，兼覆盖原则 #1 每日 release 检查）+ [0](B) V37.9.212 合并核实。② **方法（api.github.com 对 session 外仓被代理拦 → 改道 npm registry tarball 实证）**: registry.npmjs.org 可达 → 拉 2026.6.11 + 2026.4.27 两个 tarball，直接读 CHANGELOG.md + dist zod-schema + whatsapp connection-controller 源码——**代码即事实，比 release notes 转述更权威**。③ **[5](c) 完成**: CLI 冷调用/plugin staging 修复定位上游 **2026.6.11**（changelog 覆盖 v2026.6.10..HEAD 308 PRs：#93356 plugin-setup-registry **CPU storm** 修复=V37.9.145 冷调用 ~40s user CPU 直接对应 / #93919 bundle manifest-scan existence-probe cache=staging 扫描面 / #89628 help startup 提速），全部 6/24-6/30 窗口合并 → **构造上不可能在 4.27** → 现状 notify.sh 重试+队列可接受，根治随未来升级 ≥6.11 获得（升级评估收益侧新筹码）。④ **🔴 [9] Option A 复活（本次最大发现）**: #56365 已 **closed via PR #73580**（makeWASocket socket-timing 透传）；schema 对比实证 `web.whatsapp.{keepAliveIntervalMs,connectTimeoutMs,defaultQueryTimeoutMs}` 仅 ≥6.x（4.27 无）；**但 4.27 本就有顶层 `web.reconnect.{initialMs,maxMs,factor,jitter,maxAttempts}`**——消费方实证 connection-controller `resolveReconnectPolicy(cfg)` 读 `cfg.web?.reconnect`，默认 `{2000,30000,1.8,0.25,12}` 与 V37.9.162 血案「Retry 1/12」吻合。**V37.9.180「4.27 strict schema 拒绝」实为路径错误**（试的 `channels.whatsapp.web.reconnect`，channels.whatsapp 下无 web 键被 strict 拒 ≠ 配置不存在）——绝对化结论需跨条件证据的教训（V37.9.182 同款）再次演出。对症杠杆=拉长退避（血案 6h 风暴=多轮 12-attempt 循环，`maxMs` 30s→300s 降请求密度一个量级），风暴中断后兜底仍 Option C（V37.9.162 检测+Discord escalation MR-14）。⑤ **tripwire 情报**: 最新 stable 2026.6.11（6-30）/ beta 2026.7.1-beta.1（今日）；6.x SQLite guardrail 仍活跃改动（#95857/#95916）→ hold 决策的 6.x 风险面仍真实，**无升级动作**（tripwire 不自动升级）。⑥ **[0](B) 销项**: V37.9.212（diagnose/health_check/install env 化）经 git log 核实已并 main（PR #774），item [0] 剩纯用户 env 动作。⑦ **交付**: eval doc 新增 **§16**（16.1 上游状态 / 16.2 冷调用修复定位表 / 16.3 #56365+schema 对比 / 16.4 Option A 复活+Mac Mini 验证项）+ unfinished [0][5][9] 账本更新 + recent_changes。**复杂度预算账本**: files/env/jobs/runtime-state-sources 全不变 / suites/tests/checks 不变 / **VERSION 0.37.9.89 不变**（纯情报收集+文档+待办账本，零代码零业务语义，同 V37.9.219/220 惯例）/ 净 runtime 机制 0。⑧ **🔴 Mac Mini 待用户（[9] Option A 落地，写入 unfinished）**: `openclaw config set web.reconnect.maxMs 300000` → config set 接受即 schema 证明 → 重启后 `openclaw config get web.reconnect` 确认 canonicalize 保留（V37.9.180 教训：不可手改 openclaw.json）→ 后续重连日志观察退避拉长。⑨ **元价值**: 一次「查上游 releases」的例行待办，用 tarball 实证法（schema+消费方源码双确认）推翻了一个已登记 20 天的「证伪」结论——Option A 从「4.27 做不了等上游」变回「今天就能配」；同时把 V37.9.145 冷调用回归的上游收敛点钉到具体版本+具体 PR，未来升级评估的收益侧从「可能修了」升级为「确认在 6.11」。 |
| V37.9.224 | 2026-07-02 | **B1 fallback 传播 — 批量 fallback 按 fallback provider 自己的 reasoning_off_body 重算注入（focus 登记 follow-up adapter:695 兑现，堵 V37.9.220 事故的 fallback 路径变体 + qwen 未测参数风险，VERSION 0.37.9.88→0.37.9.89）** — ① **触发**: V37.9.222 B1 flip 上线时 focus 登记 follow-up「B1 fallback 传播 (adapter:695)」，V37.9.223 全四家声明 `reasoning_off_body` 正是本修复前置。诊断（原则 #28 先 ground）: fallback 循环 `fb_clean = dict(clean)` **原样继承 serving provider 的注入结果**，且 chain entry 不携带 `reasoning_off_body` → 两向真实事故面: **(a) qwen-primary 回滚态**（Plan B）批量无注入 → fallback 到 deepseek_full/doubao_21 带 reasoning 跑 → reasoning 7-9min >> client 超时 → broken pipe → **V37.9.220 事故在 fallback 路径重演**；**(b) 当前 B1 flip 态**批量注入 doubao 的 `thinking:{disabled}` → fallback 到 qwen 时该参数**原样发给 vLLM 端点**（未测参数，可能 400 打断最后兜底）。② **交付（adapter.py）**: chain entry 携带 `reasoning_off_body`（**registry `_entry_from_registry` + legacy `FALLBACK_PROVIDER` 手工 dict 两个构造点全量同步**，原则 #31，镜像 V37.9.218 vl_model_id）+ **`_fallback_batch_body(clean, fb_entry, is_batch, serving_rob)` 纯函数**——批量 fallback 先剥 serving 注入的 keys 再注入 fb 自己的片段（有声明才注）；非批量（PA/多模态/override）原样浅拷贝现行为不变（PA fallback 保留 reasoning 质量）+ do_POST fallback 循环改用 helper + 可观测 log（`B1 REASONING-OFF: fallback X -> inject/strip [...]`）。③ **ultracode 三镜头对抗审查 workflow 抓出 2 个真 finding 上线前修复**: (F1 correctness) 畸形 YAML 插件声明非 dict `reasoning_off_body`（字符串/list，PluginLoader 无类型校验）→ `update()` 抛 ValueError 且调用点在 per-provider try **之前** → **整条 fallback 链中断 = 故障时刻摧毁兜底** → 注入/剥离两侧 + `_batch_reasoning_off_body` 全部 `isinstance(dict)` FAIL-OPEN 守卫；(F2 conventions) 既有守卫 `test_fallback_uses_same_clean_body` 断言 `fb_clean = dict(clean)` 存在——修改后仅因 helper 内部同名变量**巧合通过**且叙事已被有意反转 → 按房规翻转为 `test_fallback_body_via_helper`（V37.9.223 test_doubao2 翻转先例）；(第三镜头 6 生产场景回放零 finding)；(F3 verify 阶段追加 confirmed) `docs/doubao21_workload_routing_design.md` 三处 current-state prose 停在 pre-V37.9.222（§6:69「fallback 链全是慢 reasoning 仍可能 502」被本版修复反转 / §8 状态停在 no-op-until-flip / §10 头「待 qwen 退役排期时实现」而 B1 已上线）→ 按一致性收敛谱系（V37.9.219）同 change 修复 4 处（该 doc 是 B1 家族 living canonical，V37.9.221-223 每版维护）。④ **13 新单测**（test_adapter 93→106: TestB1FallbackPropagation 12 + TestB1ReasoningOff +1）: entry 双构造点携带/缺省 None + 血案(a)注入 + 血案(b)剥离 + 同片段重注（Bifrost 归一化 V37.9.222 实测）+ PA 不动 + 纯函数不改入参 + entry 缺 key 容忍 + **非 dict FAIL-OPEN 双侧**（4 种畸形类型 + serving 侧防御）+ 源码守卫（fallback 循环段用 helper 且旧 `dict(clean)` 直连退役，段锚点 `for fb in FALLBACK_CHAIN:`→`FALLBACKS FAILED` 防误伤 helper 内部合法浅拷贝）+ 双构造点同步守卫。**四轮 sabotage 反向验证守卫真有效**（备份副本还原 + 清 __pycache__，V37.9.134/199 教训）: 删剥离 → 2 FAIL（血案(b)复现）/ 删注入 → 2 ERROR（血案(a)复现）/ 循环退回 dict(clean) → 源码守卫 FAIL / 删 isinstance 守卫 → 1 ERROR+1 FAIL（正是 F1 预言的 ValueError）；每轮还原 byte-identical 全过。⑤ **E2E 行为验证（超越纯函数）**: monkeypatch `_forward_request` + 真起 ThreadedServer 驱动完整 do_POST 三生产场景全过——S1 B1-flip 态批量 primary+ds 挂 → qwen 兜底体**无 thinking key**（primary/ds 均带）/ S2 qwen-primary 回滚态批量 → ds 兜底带**自己的** thinking-off / S3 PA 有 tools → 全链路零注入；日志逐行确认 `fallback deepseek_full -> inject` / `fallback qwen -> strip`。⑥ **消费方审计**: /health 与 hot-reload 只消费 `fb["name"]` 不受新字段影响；FAST_ROUTE 不需带（`_batch_reasoning_off_body` 读 PROVIDERS）；test_v37_9_218_fallback_order 20/20 无形状断言零破坏。⑦ **验证**: full_regression 139/0/**5053 (+13)**（run1 同 run 抓 badge 漂移 → --write 同步 → run2 全绿，V37.9.189 流程；dirty 树混沌层 skip 计数保持 V37.9.140） / governance 91/91 EXIT=0 / 安全 95/100 / 安全扫描零泄漏 / 三 doc-drift 同步后无漂移 / **VERSION 0.37.9.88→0.37.9.89**（adapter fallback 请求体构造 = 路由机制 runtime 变更，镜像 V37.9.221/222 bump 惯例）。**复杂度预算账本**: files 不变（扩 adapter + 既有 test）/ env/jobs/runtime-state-sources 不变 / **净 runtime 机制 ≈ 0**（1 纯函数替代裸 dict(clean)，注入决策从「只看 serving」收敛为「每次实际被调的 provider 自己的声明」= 一物一形，退役 fallback 路径的注入盲区非新接缝）。⑧ **🔴 Mac Mini 待用户**: 合并 + git fetch+reset 同步 → adapter.py 经 auto_deploy FILE_MAP 同步 + 按需 restart → 生效后批量真 fallback 场景 adapter.log 出现 `B1 REASONING-OFF: fallback ... inject/strip`。当前 B1 flip 态下零行为变化直到某次批量真 fallback（doubao_21 挂时）才 load-bearing；回滚 Plan B（PROVIDER=qwen）时本修复让批量 fallback 自动 thinking-off = 回滚更安全。⑨ **元价值**: B1 注入决策补全最后一条路径——serving provider（V37.9.222）+ fallback chain（本版）都按「实际被调 provider 自己的声明」重算，V37.9.220「fallback 是故障保险非延迟保险」的洞察在 fallback 路径本身兑现（批量掉到 reasoning provider 不再带 reasoning 跑死，掉到 qwen 不再带未测参数）。开工教训重演并登记: 又在 full_regression 混沌审计运行中并发编辑（V37.9.186 教训第 2 次演出，基线报 DIRTY 假失败，settled 树复跑即净）——多镜头审查 workflow 首次在本仓上线前抓出 2 个真 finding（类型守卫 + stale 守卫翻转），对抗审查作为 pre-merge 例行的价值实证。 |
| V37.9.223 | 2026-07-02 | **全部 doubao + deepseek 四家同款 `reasoning_off_body` 声明（任何切换独立成 primary，用户「未来任何情况的切换都可以独立成为主力模型」，VERSION 0.37.9.87→0.37.9.88）** — ① **触发**: V37.9.222 B1 flip 上线后，用户「最后请把 LLM provider 中所有 doubao 和 deepseek 模型都做同款修改配置，未来任何情况的切换都可以独立成为主力模型」——B1 的 `reasoning_off_body` 此前只声明在 E2E 实测过的 doubao_21 + deepseek_full 两家；扩到全部 doubao/deepseek 四家 = 未来 primary 切到任一家时批量 workload 都能自动 thinking-off 走快路（防重演 V37.9.220 reasoning-primary 批量 502）。② **诚实分层交付（原则 #23，不盲声明）**: 四家非对称——**doubao 2.0**（reasoning 模型，同 Volcengine Ark base_url 家族，doubao_21 thinking:disabled 已 V37.9.221 E2E 实测）→ 家族推断高置信声明，docstring 标「同 Ark API 表面但未在此 endpoint 单测，作 primary 前须探针」；**deepseek 量化版**（reasoning=False 非 R1 通道 + self-host 网关 DEEPSEEK_BASE_URL 裸 IP 非 deepseek_full 的 Bifrost + PENDING）→ 声明但 docstring 强标**双重不确定**（thinking-off 可能 no-op + 网关是否接受 thinking 未知 → 作 primary 前【必须】探针，若 400 拒绝须移除，PENDING 时注入 inert 因 B1 仅 serving provider fire）。③ **交付**: doubao_provider.py + deepseek_provider.py 各加 `reasoning_off_body = {"thinking":{"type":"disabled"}}`（一份四家复用）+ 诚实 docstring 注记。④ **测试更新**（test_providers TestReasoningOffBodyB1 +2 净）: `test_doubao2_no_reasoning_off`→`test_doubao2_declares_reasoning_off`（翻转断言）+ 新 `test_deepseek_quantized_declares_reasoning_off` + 新 `test_qwen_still_no_reasoning_off`（守 gemini 纯文本 built-in 不误声明）+ `test_flows_to_legacy_dict` 加 doubao + 新 `test_all_doubao_deepseek_same_body`（四家同片段守卫）。⑤ **验证**: full_regression 140/0/5040 (+2, 收工干净树混沌审计真跑) / governance 91/91 EXIT=0 / 安全 95/100 / 安全扫描零泄漏 / 三 doc-drift 无漂移 / **VERSION 0.37.9.87→0.37.9.88**（provider 能力声明变更）。**复杂度预算**: files/env/jobs/runtime-state 全不变（扩 2 provider + 既有 test）/ 净 runtime 机制 0（复用 V37.9.222 B1 既有字段+注入路径，纯声明扩展非新接缝）。⑥ **探针结果（同日用户 Mac Mini 实测，收工前闭环）**: **deepseek 量化 ✅ 探针通过**——self-host 网关接受 `thinking:{"type":"disabled"}`（HTTP 200 + finish_reason=stop + reasoning_tokens:0 无 400）→「网关 400 风险」排除声明安全保留（docstring 已刷证据；仍 non-reasoning → thinking-off 实际 no-op 但无害）；**doubao 2.0 探针受 env 位置阻塞**（AuthHeader 错 = ARK_API_KEY/ARK_ENDPOINT_ID 只在 adapter plist 不在 .env_shared，thinking 参数未被评估）→ 家族推断保留（同 Ark base_url + 同族 doubao_21 已 E2E 实测 thinking API，置信度高，直连探针可选）。**剩余门槛**: 切 primary 到 doubao 2.0 前建议补 env 探针确认。当前 primary 仍 doubao_21（V37.9.222 flip），四家新声明在切换前 inert，合并同步后零重启。⑦ **元价值**: B1 从「两家实测 provider」扩到「全 doubao/deepseek 家族 config-ready」——未来 qwen 退役后任一 doubao/deepseek 可独立成 primary 而无需临时改 provider 代码，但严守原则 #23：家族推断 vs 网关未知分层标注，作 primary 前探针门槛清晰（不把「声明」当「已验证」，deepseek 量化的 self-host 网关不确定性显式登记防 V37.9.220 重演）。 |
| V37.9.222 | 2026-07-02 | **B1 落地 + doubao_21 转正唯一 primary flip 上线（batch reasoning-off 注入，doubao_21 单模型通吃 batch，意外发现比 qwen 更快，VERSION 0.37.9.86→0.37.9.87）** — ① **触发**: V37.9.221 A2 flip 上线（doubao_21 primary + 批量→qwen）+ B1 双 provider 实测确认（thinking:disabled）后，用户「建议 doubao_21 作唯一 primary，有无 tools 都由 doubao_21 实际压测」——即撤 qwen 分流、让 doubao_21 亲自接批量（去 qwen 依赖，为退役铺路）。② **安全前提（AskUserQuestion 确认）**: doubao_21 接批量若 reasoning 开着 = 重演上午 502；唯一安全方式 = **B1 注入 `thinking:{"type":"disabled"}` 关 reasoning**（V37.9.221 实测 doubao_21 17.7s vs 166s / deepseek_full 经 Bifrost 网关同参数）。用户选「实现 B1 + 撤 qwen 分流」= real consumer 需求驱动（原则 #18），从「日落法暂不建」转为「现在建」。③ **交付（providers.py）**: BaseProvider 加 `reasoning_off_body` 字段（默认 None）+ to_legacy_dict 暴露 + from_yaml 读取；doubao_21 / deepseek_full 声明 `{"thinking":{"type":"disabled"}}`（一份两家复用，实测同参数）；qwen/doubao 2.0 = None（诚实，未测/非-reasoning）。④ **交付（adapter.py）**: `_is_batch_workload()` 纯函数（批量判定，**独立于 FAST_ROUTE**，一物一形单一真理源，`_classify_fast_route` 重构复用它）+ `_batch_reasoning_off_body()` 纯函数（批量+服务 provider 有 body→返回注入片段；服务 provider=FAST_ROUTE provider 若 use_fast 否则 primary）+ do_POST 批量注入。**A2/B1 互补**: 有 FAST_PROVIDER→A2 路由到独立快 provider（qwen 无 body no-op）；无 FAST_PROVIDER→B1 在 reasoning primary 注入 thinking-off。⑤ **18 新单测**（test_adapter TestB1ReasoningOff 12 + test_providers TestReasoningOffBodyB1 6）: _is_batch_workload（no-tools→True/tools/多模态/override/非默认 model→False）+ _batch_reasoning_off_body（primary=doubao_21 注入 / PA 不注入 / FAST=qwen 不注入 / FAST=deepseek_full 注入 / primary=qwen 不注入 / 多模态不注入）+ 声明流经 to_legacy_dict + 双 provider 同 body + 源码守卫。**sabotage 反向验证守卫真有效**: 破坏 _batch_reasoning_off_body → 2 注入测试 FAIL，备份还原 93+171 全过。⑥ **Mac Mini B1 flip 已部署 + E2E 全过（用户执行，含意外惊喜）**: 合并 PR #790 + git reset 同步 + 撤 FAST_PROVIDER（保持 PROVIDER=doubao_21）+ bootout/bootstrap → /health provider=doubao_21 + fast_route 字段消失 + fallback[deepseek_full,doubao,deepseek,qwen] / **④ `B1 REASONING-OFF: batch on doubao_21 -> inject ['thinking']` 验证** / **⑤ github_trending 10/10 成功 degraded 0**（批量走 doubao_21 thinking-off）。**🌟 意外发现：批量在 doubao_21 thinking-off 上比 qwen 分流时更快**——火山 Ark 托管云高并发 > 自建 qwen W8A8 端点（有 queue_wait）→ qwen 退役从「可行」升级为「有收益」。doubao_21 现单模型通吃全部主流量（PA thinking-on reasoning 质量 / 批量 thinking-off 快），**去 qwen 依赖达成**（qwen 仅 fallback 兜底）。deepseek_full 第一优先级 fallback。回滚 = 重设 FAST_PROVIDER=qwen 恢复 A2 批量→qwen（随时可用）。⑦ **验证**: full_regression 139/0/5038 (+18) / governance 91/91 EXIT=0 / 安全 95/100 / 安全扫描零泄漏 / 三 doc-drift 无漂移 / **VERSION 0.37.9.86→0.37.9.87**（provider 能力字段 + adapter 路由机制变更）。**复杂度预算**: files 不变（扩 providers/adapter/2 test）/ env -1（撤 FAST_PROVIDER，doubao_21 自接批量）/ 净 runtime 机制 = reasoning_off_body 字段 + 1 注入纯函数（复用 _is_batch_workload + 既有转发路径，非新框架）。⑧ **元价值**: 终局架构落地——reasoning 是 per-request 模式非模型属性，doubao_21 单模型 thinking-on(PA 质量)/thinking-off(批量快) 通吃，**去 qwen 依赖**（qwen 退役预演）。B1 从「proven-feasible 待需求」（V37.9.221 §10）转为「real consumer 需求驱动落地」（原则 #18 兑现），deepseek_full 同参数使终局无单点依赖。**B1 flip 已上线**（doubao_21 单模型通吃 + 去 qwen 依赖 + 意外比 qwen 更快）；观察 N 天 cron 周期 → 升 doubao_21 production_observed。 |
| V37.9.221 | 2026-07-02 | **doubao_21 迁移门槛修复 — adapter workload 路由（方案 A2 单点规则：无 tools=批量→fast provider / 有 tools=PA→primary，复用 FAST_ROUTE，VERSION 0.37.9.85→0.37.9.86）** — ① **触发**: V37.9.220 事故回滚后用户问「doubao_21 作 primary 如何设计规避 + qwen 在 fallback 为什么没接住」。② **Q2 深挖（qwen 没接住的真相）**: 读 adapter.log——`FALLBACK OK: 200 (5341B) 526554ms via qwen` 后 `FAILED Broken pipe`：**qwen 其实成功生成了合法响应**，但 fallback 串行、qwen 排第 4，走到它时已 526s，client（cron job）早在 ~2-3 分钟前超时断 socket → adapter 写回撞 Broken pipe → 判 FAILED。**架构真相：fallback 防的是 provider「故障」不是「延迟」**；串行链累积慢-但-成功的 provider，client 超时是 binding constraint，在链到达快 provider 前就断了。③ **先 ground 再动手（原则 #28）**: 读 adapter `_resolve_primary_provider`/`?provider=`/`FAST_ROUTE`/`classify_complexity`/`ROUTER_ENFORCE` + proxy do_POST 转发——发现 (a) 批量 job 全打 **proxy :5002**（非 adapter 直连，规则 #27），proxy line 1062 `url=f"{BACKEND}{self.path}"` 原样转发 query（`?provider=` 穿透，proxy 无需改）(b) adapter **已有 FAST_ROUTE + SMART ROUTE 骨架**（simple→fast）(c) LLM 调用点是 **24 个异构站点**（19 shell curl 硬编码 + 5 变量 + 2 端口 + Python kb_*）。④ **AskUserQuestion ×2 决策**: 方案 A（workload 路由）vs 保持+深挖 vs 破管级联——用户选 A；A 的实现 A1（24 站点显式 `?provider=qwen` + guard）vs **A2 单点 adapter 规则**——用户选 A2（推荐，一物一形，脆弱面小）。⑤ **交付（adapter.py）**: `_classify_fast_route(clean, clean_msgs, has_multimodal, use_model, primary_name)` **纯函数（可单测）** → `"workload"`（无 tools=批量/纯推理→fast，**不依赖 classify_complexity**：批量内容 complex 但延迟敏感应走快路=修复点）/ `"smart"`（有 tools+simple→fast，V37.9.76 向后兼容）/ `None`（PA 复杂 tool-call / 多模态 / `?provider=` override / 非默认 model → 留 primary）。do_POST 改用 helper（一物一形非 inline）。信号可靠性：PA 是 tool-calling agent 每轮带 tools / 所有批量消费方纯推理无 tools（规则 #27）→ **一处规则自动覆盖全部 24 消费方，新 job 无需改**。⑥ **no-op until flip（安全）**: `FAST_ROUTE` 仅 `FAST_PROVIDER≠PROVIDER` 时非空 → PROVIDER=qwen 时 FAST_PROVIDER=qwen 自动 no-op，代码合并零行为变化，flip PROVIDER=doubao_21 后才 load-bearing。A2 用 FAST_ROUTE **不需要 ROUTER_ENFORCE**（那是 `?provider=` 开关）。⑦ **10 新单测** test_adapter.py TestWorkloadRouting（行为级 monkeypatch adapter globals）: no-tools→workload / **不依赖 classifier** / tools+simple→smart / tools+complex→primary / 多模态→primary / 非默认 model→primary / **?provider= override→primary** / FAST_ROUTE 未配置→None / 空 tools→workload / 源码守卫 do_POST 用 helper。**sabotage 反向验证守卫真有效**: 破坏 no-tools 分支 → 3 测试 FAIL，备份还原 81/81 全过。⑧ **交付文档**: `docs/doubao21_workload_routing_design.md`（问题/A2 决策/实现/no-op-until-flip/Mac Mini 阶段 0-2 部署验证+flip runbook/边界取舍/守卫）+ case doc 六段更新（门槛→已实现方案）。⑨ **验证**: full_regression 139/0/5020 (+10) / governance 91/91 EXIT=0 / 安全 95/100 / 三 doc-drift 无漂移 / **VERSION 0.37.9.85→0.37.9.86**（adapter 路由机制 = 能力平面变更，镜像 V37.9.218 bump 惯例）。**复杂度预算**: files +2（design doc + 无新 test 文件扩既有）/ env +1（FAST_PROVIDER，Mac Mini flip 时设，dev/当前 no-op）/ 净 runtime 机制 = 1 纯函数（复用 FAST_ROUTE 既有转发路径，非新框架）。⑩ **🔴 Mac Mini 待用户（flip runbook 见 design doc 第 5 节）**: 阶段 0 同步部署（no-op）→ 阶段 1 临时 FAST_PROVIDER=doubao_21 双 curl 探针验证 WORKLOAD ROUTE 触发后 unset → 阶段 2 正式 flip `FAST_PROVIDER=qwen`+`PROVIDER=doubao_21`+bootout/bootstrap → 重跑 github_trending 应 10/10 快速成功 + PA 走 doubao_21 → 观察几个 cron 周期。回滚 Plan B（PROVIDER=qwen）随时可用。⑪ **元价值**: fallback 是故障保险非延迟保险（Q2 洞察）——doubao_21 作 primary 的正解不是靠 fallback 兜，是按 workload 分流让批量 job 从一开始走快路。骨架已在（V37.9.76/77），事故把它从 shadow 推向转正。单点规则（一物一形）优于 24 站点显式（脆弱高 churn）。 |
| V37.9.220 | 2026-07-02 | **doubao_21 primary 拖垮批量 cron job 事故 → 回滚 PROVIDER=qwen（Plan B）+ 文档 primary prose 回正（V37.9.219 doubao_21-primary 收敛当日被事故反转，运维回滚 Mac Mini + 文档回正 repo，纯运维+doc 零代码 VERSION 不变）** — ① **触发（用户转发生产告警）**: 2026-07-02 14:00–14:45 三条 `[SYSTEM_ALERT]` — 货代/github_trending(10/10 repo)/hn(3/3) 全部 LLM 失败，`last reason` **为空**，raw cache 空。同期 PA WhatsApp 正常。② **数据驱动诊断（原则 #28/#26 异常分析宪法，dev 不可见 Mac Mini → 请用户取 4 组数据零盲改）**: (a) cache 空 (b) `/health` 200 provider=doubao_21 fallback_chain=[deepseek_full,doubao,deepseek,qwen] circuit_breaker=closed (c) 直连 "reply OK" **7.3s 通但产 reasoning_tokens=173**（max_tokens=10 也不约束 reasoning）(d) adapter.log 铁证: 请求走完 4 fallback 每个 `OK 200` 后 1ms 内 `FAILED Broken pipe`（457s/496s/526s），`ALL 4 FALLBACKS FAILED → 502`。③ **根因（三层）**: **触发器**=今日 primary 切 doubao_21（reasoning 模型）→ 批量长请求（10 repos 分析 + 大 max_tokens 结构化输出）reasoning 爆炸 7-9 分钟 >> job HTTP client 超时 → client 断开 socket；**放大器**=fallback chain 也全 reasoning/慢（deepseek_full R1 / doubao 2.0）+ adapter broken-pipe 后仍傻跑完 4 fallback 浪费 500s 到死 socket；**掩护者**=job `last reason` 空（timeout 无 body）+ PA 短请求正常（前一天 E2E「延迟非故障」掩盖批量 job 延迟，不同 workload profile）。**排除**: adapter 宕机（health 200+短请求 7s）/ 端点故障 / auth / env / fallback 配置 全正常 = **纯 reasoning 模型对批量长请求延迟问题**。④ **修复（AskUserQuestion 选「立即回滚」，Plan B V37.9.203 设计）**: 运维回滚 `PROVIDER=doubao_21→qwen`（`~/.env_shared:35` sed + `com.openclaw.adapter.plist` EnvironmentVariables PlistBuddy Set）→ `launchctl bootout/bootstrap` 重载（V37.9.202 教训: kickstart 不重读 plist env）→ 恢复 V37.9.218 设计态: `/health` provider=qwen + fallback_chain=[doubao_21,deepseek_full,doubao,deepseek]（doubao_21 退兜底首位）。**验证**: qwen 直连 **0.412s**（vs doubao_21 7.3s、job 7-9 分钟）根因确认无疑。⑤ **文档 primary prose 回正（V37.9.219 当日收敛到 doubao_21-primary 被事故反转）**: CLAUDE L9/L116 + README L71/L439 + FEATURES body 表 主力 doubao_21→**Qwen3-235B（+ 注明 PROVIDER env 配置减少下次 whiplash）**。V37.9.219 的 provider-count 10→11 + 去数字化 + 能力感知路由改动**保留**（与 primary 无关，正确）。V37.9.219 changelog 行作 point-in-time 记录**不改写**（V37.9.153 纪律），本行记录反转。⑥ **case doc**: `reasoning_model_primary_breaks_batch_jobs_case.md`（异常分析宪法五步 + 因果链 + 三层根因 + workload profile 组合 + 元教训 + LLM-Observer taxonomy 候选）。⑦ **登记 unfinished（doubao_21 迁移真门槛）**: 让 doubao_21 重作 primary 的前提 = 批量 job 走快速路径（capability router V37.9.76 分流 PA→reasoning / batch→快 provider / 或 reasoning 上限 / 或 per-request provider override）。在此前 doubao_21 保持 fallback 链首不作 primary。次生: adapter broken-pipe 级联浪费（低优 robustness 候选）。⑧ **验证**: full_regression 139/0/5010 / 三 doc-drift 无漂移 / governance 91/91 EXIT=0 / 安全 95/100 / **VERSION 0.37.9.85 不变**（运维回滚零 repo 代码 + doc 回正零业务语义）。**复杂度预算**: files +1（case doc = 事件证据）/ env/jobs/runtime-state 不变 / 净 runtime 机制 0。⑨ **元教训**: provider 迁移必须按 **workload profile** 验证——PA 短对话 E2E 通过 ≠ 批量分析 job 可用（同 provider 对不同请求 profile 天差地别，reasoning 模型尤甚）；reasoning tokens 不受 max_tokens 约束 = 延迟不可控，批量/低延迟场景选 provider 必实测 reasoning 开销；优雅降级掩盖结构性失败（三 job 全挂看似正常降级，靠告警才发现，MR-4 家族）。**🔴 Mac Mini 已完成回滚**（用户执行），repo 文档回正待合并同步。 |
| V37.9.219 | 2026-07-02 | **primary=doubao_21 现态 prose 一致性收敛 + provider-count 10→11 漂移补齐（V37.9.216 加 doubao_21 第 11 个 + V37.9.218 primary 切换留下的机器检查覆盖不到的 prose 漂移，纯 prose 零业务语义 VERSION 不变）** — ① **触发**: 周四门控收敛日健康基线全绿（140 suites / 5010 tests / 0 fail / governance EXIT=0 91/91 / 三 doc-drift 机器检查无漂移 / 分支==origin/main #785 / OpenClaw 4.27 无 pending）。22 项 unfinished 逐核实均 gated（Mac Mini 生产数据 doubao_21→production_observed·LLM-Observer shadow 周 / IEEE·ISSRE in-review / deferred-by-design guardrail）→ 从 grounded 纯 dev 项选一致性收敛（外部评审2 核心指令「从加功能切换到证据刷新与一致性收敛」+ 日落法北极星 #34，镜像 V37.9.208/209/215 门控日收敛谱系）。② **诊断（原则 #28 grounded 全文件核实非凭推测）**: 三机器 doc-drift 全绿 → 漂移全在徽章/机器表覆盖不到的 human prose。抓到**两类真漂移**: **(A) provider-count 10→11**（V37.9.216 加 doubao_21 第 11 个 provider，"10→11" prose sweep 遗漏）: CLAUDE.md L116 组件表「LLM Providers (10) / 10 Provider（7 built-in + 3 plugins）」+ L145 file-row「10 providers」+ compatibility_matrix.md L4 human header「10 Providers（7 built-in + 3 plugins）」（与其自身机器表 11 行矛盾）+ FEATURES.md L67 + INDEX.md L35 file-reference 行 —— README 已全 11（V37.9.216 更新过）。**(B) primary-model 现态**（V37.9.218 运维切换 primary qwen→doubao_21）: CLAUDE.md L9 项目简介「当前：Qwen3-235B + Qwen2.5-VL-72B」+ L116 组件表「Qwen3-235B（主力）」+ README L69「text→Qwen3, image→Qwen2.5-VL」路由示例 + L71「Qwen3-235B primary」+ L439 fallback 图「Primary e.g. Qwen3-235B / Fallback e.g. Gemini 2.5 Flash」（Gemini 已 geo-block 退役出链 V37.9.129 = 主动误导）。③ **先 ground adapter 路由（原则 #28 避免用新错误替换旧错误）**: 读 adapter.py 确认 `PROVIDER` env 决定 primary（Mac Mini=doubao_21 / dev 默认 qwen）；`_PRIMARY_TIMEOUT`=300s=5min / `_FALLBACK_TIMEOUT`=60s=1min（README 439 timeout 数值正确仅模型名 stale）；config.md 的 `qwen-local/` 前缀是 **OpenClaw 路由标签**（provider 切换不影响）**不 stale 不动**。④ **修复（一致性收敛 + 日落法去数字化）**: (A) CLAUDE L116 → 11 Provider（7 built-in + 4 plugins）+ 主力 label 移到 Doubao 2.1 / compatibility_matrix L4 → 11 Providers / CLAUDE L145 + FEATURES L67 + INDEX L35 三个 file-reference 行**去数字化**（V37.9.152 file-row 先例：退役漂移源，计数只活在 providers.py 机器表+badges 单一源，比 bump 到 11 更抗未来第 12 个 provider 漂移）(B) CLAUDE L9/L116 主力→Doubao Seed 2.1 Pro / README L71 → doubao_21 primary + [deepseek_full,doubao,qwen] fallback / L69 → capability-aware multimodal（去耦具体 primary，doubao_21 单模型多模态）/ L439 → Primary e.g. Doubao 2.1 + Fallback e.g. DeepSeek-V4-Pro（退役 Gemini 误导示例，timeout 5min/1min 数值不动）。⑤ **诚实不动 point-in-time（V37.9.144/153 纪律）**: why_control_plane.md（已发表文章）+ paper/*.md draft/submissions（冻结 study-cutoff 2026-06-11「8 providers Qwen3 primary」匹配已发表 arXiv）+ 全部 changelog 行 + CLAUDE L23 架构图「LLM (7 Providers)」（V37.9.209 决定=built-in 视图不churn）。⑥ **验证**: full_regression 139/0/5010 / 三 doc-drift 机器检查同步后无漂移 / governance EXIT=0 91/91 / 安全 95/100 / 安全扫描零泄漏 / **VERSION 0.37.9.85 不变**（纯 prose 一致性收敛零业务语义零代码零测试改动，同 V37.9.208/209/215 惯例）。**复杂度预算账本**: files/env/jobs/runtime-state-sources 四维度全不变 / suites/tests/checks/VERSION 全不变 / **净 runtime 机制 0，净 -3 human-prose 漂移源**（L145+FEATURES L67+INDEX L35 三个 file-reference 计数去数字化退役；L116/L4 用真值同步仍是漂移源但组件表/matrix header 需具体计数，诚实保留）。⑦ **刻意无守卫测试**（prose 计数不在 gen_readme_badges 跟踪范围，加守卫=新机器面对罕漂移得不偿失，靠 changelog 留痕，对齐 V37.9.164/165 file-row 去数字化惯例）。⑧ **Mac Mini 待用户**: git fetch+reset 同步（纯 doc 零重启零业务影响）。⑨ **元价值**: 门控收敛日无强 dev 候选（primary→production_observed 需 N 天生产数据 / qwen 移除 FALLBACK_ORDER premature / LLM-Observer flip gated on shadow 周）用于纯收敛——修复 V37.9.216（provider-count sweep 遗漏 5 处）+ V37.9.218（primary 切换现态 prose）留下的 badges⟷prose 不一致（同 repo 并存「10 provider」vs「11 provider」+「Qwen3 primary」vs「doubao_21 primary」→ 收敛）。延续 V37.9.152/164/165/209 doc-stat 谱系：找真漂移，file-row 去数字化退役漂移源（一物一形）而非盲目 bump。 |
| V37.9.218 | 2026-07-02 | **FALLBACK_ORDER 显式有序 fallback 链 + capability-aware vision fallback（用户偏好顺序 doubao_21>deepseek_full>doubao>deepseek>qwen 逐步老化，cap_score 表达不了 + 单槽限制 → 新机制，VERSION 0.37.9.84→0.37.9.85）** — ① **触发**: 用户给出 provider 优先顺序 `doubao_21 > deepseek_full > doubao > deepseek > qwen(逐步老化)` 要落地路由。② **先理解再动手（原则 #28）+ AskUserQuestion 三决策**: 读 adapter `_build_fallback_chain` + fallback 循环 + providers `vl_model_id`——发现**两个约束**: (a) 用户顺序 `deepseek_full > doubao` 与 cap_score 自动排序**相反**（doubao 5 verified > deepseek_full 3），且旧机制只有 **1 个显式槽** `FALLBACK_PROVIDER` → 表达不了完整 5 元顺序 (b) `deepseek_full`/`deepseek` 纯文本无 vision，image 请求 fallback 到它们必失败。用户三选（全推荐）: **先只重排 fallback 链**（qwen 暂留 primary 观察，doubao_21 领衔兜底，逐步老化）+ **新增显式有序机制**（替代单槽一物一形）+ **capability-aware 跳过**。③ **交付（adapter.py）**: **`FALLBACK_ORDER` env**（逗号分隔有序 provider 名，权威覆盖 cap_score + 旧单槽；`primary 自动排除`= 可传完整偏好顺序，切换 primary 无需改 FALLBACK_ORDER 的 flip-safe 设计；无 key/未知/`_FALLBACK_EXCLUDE`(geo-block) 跳过 + 去重保序；与 FALLBACK_PROVIDER 同设时 WARN 优先）+ **`_entry_from_registry()` helper** 让每个 chain entry 带 `vl_model_id`（legacy 路径 entry 也补齐）+ **capability-aware vision fallback**（fallback 循环: `has_multimodal` + 无 vl_model_id → skip+记 reason+continue；vision-capable 用 `fb_vl`=vl 模型 而非 model_id——qwen 用独立 VL 模型/doubao 单模型多模态）。④ **实测验证（真 registry 探针）**: `FALLBACK_ORDER="doubao_21,deepseek_full,doubao,deepseek,qwen"` + PROVIDER=qwen → 链 `[doubao_21,deepseek_full,doubao,deepseek]`（qwen 排除，deepseek_full 在 doubao 前=cap_score 做不到）；PROVIDER=doubao_21 同 order → `[deepseek_full,doubao,deepseek,qwen]`（doubao_21 排除 qwen 进链末=flip-safe）；vl_model_id 仅 doubao_21/doubao 非空（deepseek 空→image 跳过）。⑤ **20 新单测** test_v37_9_218_fallback_order.py（TestFallbackOrderMechanism 9: 精确顺序/defies-cap_score/primary-flip-safe/无 key 跳过/未知跳过/exclude/去重/precedence-over-单槽/空 order 走 legacy + TestVisionCapableEntries 2 + TestCapabilityAwareSkipSourceGuards 4 + SourceGuards 5）。**sabotage 反向验证守卫真有效**: 移除 primary-exclude → 2 test FAIL（精确顺序+flip-safe），备份还原 20/20 全过。⑥ **验证**: full_regression 139/0/5010 / governance EXIT=0 / 安全 95/100 / 安全扫描零泄漏 / VERSION 0.37.9.84→0.37.9.85（adapter 路由机制 = 能力平面 runtime 变更）。**复杂度预算账本**: files +1（test）/ env +1（FALLBACK_ORDER，替代/subsume 旧单槽 FALLBACK_PROVIDER=一物一形非纯加）/ 净 runtime 机制 = 显式有序 fallback（替代单槽）+ vision-aware skip（capability 正确性，非新接缝）。⑦ **🔴 Mac Mini 待用户**: 合并同步后 `.env_shared` + adapter/proxy plist 设 `FALLBACK_ORDER="doubao_21,deepseek_full,doubao,deepseek,qwen"`（PROVIDER 保持 qwen）→ bootout/bootstrap 重载 → `/health` 应显示 `fallback_chain: [doubao_21, deepseek_full, doubao, deepseek]`。逐步老化: 观察 doubao_21 兜底表现成熟后, 切 `PROVIDER=doubao_21`（FALLBACK_ORDER 无需改，qwen 自动进链末尾开始老化）。⑧ **元价值**: 控制平面本就该支持显式优先级（非只 cap_score 自动排）。新机制替代单槽（一物一形）+ flip-safe（primary 自动排除让 primary 切换零 order 改动）+ capability-aware（纯文本 provider 不再对 image 请求浪费失败尝试）= 渐进 provider 迁移的路由基础设施。 |
| V37.9.217 | 2026-07-02 | **Doubao Seed 2.1 Pro Mac Mini E2E 实测 → declared 升 feature_verified（5/5 能力全通过，旗舰质量无乱码，VERSION 0.37.9.83→0.37.9.84）** — ① **触发**: V37.9.216 接入后用户 Mac Mini `git reset` 同步（PR #782）+ 设 `ARK_21_API_KEY`/`ARK_21_ENDPOINT_ID` env + 加进 adapter+proxy plist（PlistBuddy Add，与现有 ARK_API_KEY 同域账号 key，独立 endpoint）+ `bootout`/`bootstrap` 重载 → adapter /health 确认 `doubao_21` 进 fallback 链 `['deepseek_full','doubao','doubao_21','deepseek']`（available()=True）→ 直连 ARK 端点 4 组 curl 探针。② **E2E 实测 5/5 全通过（原则 #11/#13 用户视角真端点验证）**: (a) **text+reasoning** → `content="0.05"` **bat-ball 推理题答对**（多数模型错答 0.10）+ `reasoning_content` 完整推理链 + `reasoning_tokens=255` = **有 R1 风格独立推理通道** (b) **tool_calling** → `finish_reason=tool_calls` + `tool_calls[].get_weather({"city":"东京"})`（先 reasoning 再调工具） (c) **streaming** → `chat.completion.chunk` 流（reasoning_content→content）+ `finish_reason=stop` + `[DONE]` (d) **vision** → 火山 TOS 示例图（火山服务端拉取不受 Mac Mini 网络影响）精准描述「深蓝湖面+橙色皮划艇+针叶林+覆雪山脉+薄云」全元素命中 + 带 reasoning_content。**旗舰质量: 无乱码 token**（对比 deepseek 量化版 w4a8-mtp 偶发乱码，doubao_21 全程干净）。③ **据实 flip（原则 #23 只声明实测过）**: verified_text/vision/tool_calling/streaming/reasoning=True（5/6）+ `verification_tier="declared"→"feature_verified"`（tier 一致性不变式要求非 declared 档必须显式 tier_evidence + ≥1 verified → 写 5/5 E2E 证据串）。**未测/未接管保持 False**: verified_fallback（未真生产 fallback 接管，同 doubao 2.0/deepseek 惯例）/ json_mode（声明 True 但未单测，非 verified_* 跟踪维度）。④ **全量同步**: test_providers TestDoubao21Provider（test_tier_declared_nothing_verified → test_tier_feature_verified_after_e2e：5 verified + fallback False + tier_evidence 含 E2E/5-5 + 一致性 []）+ test_gen_compat（declared 6→5，doubao_21 离开 declared）+ gen_compat_matrix --fix（能力矩阵 doubao_21 verified 列 + 验证档位表 feature_verified）+ minimal_runtime golden 重生成（doubao_21 verified 变化，best(text,prefer reasoning) 仍 doubao 因 demo 不排除；fallback_chain 排序保持）。⑤ **验证**: full_regression 138/0/4990 / governance 91/91 EXIT=0 / tier 一致性 [] / 安全 95/100 / 安全扫描零泄漏（新 key+端点未入库）/ **VERSION 0.37.9.83→0.37.9.84**（能力变更 5 verified + tier 升级影响路由资格+评分，镜像 deepseek V37.9.205 / doubao 2.0 V37.9.53-55 渐进验证分版本 bump）。⑥ **Mac Mini 已完成**: env 持久化 `~/.env_shared` + adapter/proxy plist + 服务重载 + 5/5 E2E。**剩余**: doubao_21 已在 fallback 链兜底（declared→feature_verified 后仍是 fallback，路由默认 primary=qwen 不主动选）；真接入某任务 provider 或 fallback 真接管后 → 可升 production_observed。**安全**: 明文 ark- key（与 doubao 2.0 同账号）对话多次出现，建议轮换。⑦ **元价值**: 渐进验证第三次兑现（doubao 2.0 V37.9.53-55 → deepseek V37.9.201→205 → doubao_21 V37.9.216→217）——旗舰 provider 从「声明」到「分项 E2E 实测」，verified_* 每项都有真 curl 证据，tier 升级有据可查。旗舰 doubao_21 是当前唯一 5/6 verified 的多模态 provider（vision+reasoning 双通道），质量优于 deepseek 量化版。 |
| V37.9.216 | 2026-07-02 | **新增第 11 个 LLM provider Doubao Seed 2.1 Pro 旗舰（火山引擎 Ark，env-驱动 key+endpoint 守公开 repo 安全底线，declared 档位诚实未实测，VERSION 0.37.9.82→0.37.9.83）** — ① **触发**: 用户「最后再增加一个旗舰版 LLM provider」（model `doubao-seed-2-1-pro-260628` / endpoint ep（走 env 不入库）/ 明文 ark- key（走 env 不入库）/ url `https://ark.cn-beijing.volces.com/api/v3`），区别于第 8 个 doubao_provider.py 的 Seed 2.0 Pro。② **🔴 安全第一（公开 repo 底线 + push 前 ark-/sk- 扫描）**: 仓库公开 → 明文 ark- key **绝不入库**；endpoint ID `ep-2026...` 也走 env（Volcengine 的 model 字段接收 endpoint ID）；base_url 是公开 Volcengine 域名（非裸 IP/机密）**可入库**。镜像 doubao 2.0 V37.9.52「即便用户豁免也守公开 repo 安全底线」+ deepseek V37.9.201 env 模式。③ **交付 `providers.d/doubao_seed_21_provider.py`（第 11 provider）**: `api_key_env="ARK_21_API_KEY"`（独立 env 非复用 2.0 的 ARK_API_KEY，让 2.1 旗舰与 2.0 各自持 key）+ `__init__` 从 env `ARK_21_ENDPOINT_ID` 取 endpoint ID（dev 无 env → fallback 公开 model 标识符 `doubao-seed-2-1-pro-260628` 让合约通过 + `available()` 因缺 key 自动排除）+ auth bearer（OpenAI 兼容）。**诚实语义（原则 #23 渐进验证）**: `verification_tier="declared"`（tier_evidence 走 `_DECLARED_TIER_EVIDENCE` 派生守 tier 一致性不变式）+ verified_* 全 False；能力声明 text/vision/tool_calling/streaming/json_mode/reasoning=True 来自 Doubao Seed Pro 家族文档（同 2.0 Pro 家族），与 verified_* 解耦，待 Mac Mini E2E 实测后逐项 flip（镜像 doubao 2.0 V37.9.53-55 / deepseek V37.9.201→205）。④ **registry 10→11**（7 built-in + doubao + deepseek×2 + doubao_21）+ **auto_deploy FILE_MAP 登记**（V37.9.206 教训：provider 插件漏进 FILE_MAP → check_registry 硬 error）。⑤ **全量同步 count 守卫（原则 #31）**: test_providers（多处 10→11 + TestDoubao21Provider 5 含安全守卫 regex 无 ark- key/无 ep- endpoint 字面量 + 独立 env 名 ARK_21_API_KEY 以 _API_KEY 结尾）+ test_gen_compat（matrix/tier 2+10→2+11 + declared 5→6）+ test_v37_9_52（10→11 + CLI 11 providers）+ gen_compat --fix（能力矩阵）+ minimal_runtime golden 重生成（+doubao_21）+ README/CLAUDE prose 10→11。⑥ **验证**: full_regression 138/0/**4990 (+5)** / governance 91/91 EXIT=0 / 安全 95/100 / **安全扫描零泄漏**（全仓 grep ark-/sk-/ep-2026 全空，git diff 无机密）/ tier 一致性 [] / 三 doc-drift 无漂移 / **VERSION 0.37.9.82→0.37.9.83**（新 provider = 能力平面变更，镜像 V37.9.201/204 bump 惯例）。**复杂度预算账本**: files +1（doubao_seed_21_provider.py = 新 provider 接缝，复用 plugin 框架零新机制）/ env +2（ARK_21_API_KEY + ARK_21_ENDPOINT_ID，Mac Mini 设置，dev 优雅降级）/ jobs/runtime-state 不变 / 净 runtime 机制 = 1 provider（复用 plugin 自动发现 + available() 过滤 + doubao 安全模式，非新框架）。⑦ **🔴 Mac Mini 待用户**: 合并 + git fetch+reset 同步 → auto_deploy 把插件 rsync 到 `~/providers.d/` → `.env_shared` 设 `ARK_21_API_KEY`（明文 key）+ `ARK_21_ENDPOINT_ID`（用户旗舰端点 ID）→ 加进 adapter+proxy plist（镜像 ARK/DEEPSEEK）+ bootout/bootstrap 重载 → E2E 实测 text/vision/tool_calling/streaming/reasoning → 通过项 flip verified_* + 升 tier。⑧ **🔴 安全提醒**: 明文 ark- key 在对话出现，建议**用完轮换**（防御纵深，对话历史可能被记录）。⑨ **元价值**: Provider Plugin 框架第四次真实消费（doubao 2.0→deepseek→deepseek_full→doubao_21），60 秒加旗舰 provider + 安全模式（机密全走 env）+ 诚实档位（declared 未实测），全程零硬编码机密守公开 repo 底线。 |
| V37.9.215 | 2026-07-02 | **GitHub 门面文档刷新 — README Latest-milestone/Highlights 从 2026-05 更新到 07 + 10-provider 一致性收敛（纯 doc，VERSION 不变）** — ① **触发**: 用户"很久没刷新 GitHub 文档，很多陈旧"。grounded 审计（原则 #28 + V37.9.153 纪律：只刷真陈旧内容，不 churn point-in-time file-table 版本标签/稳定文件）。机器统计（徽章/provider/jobs）由 gen_readme_badges 自动同步已当前，陈旧只在人工维护 prose。② **刷新 4 项真陈旧**: (a) **Latest milestone**（line 17）从「V37.9.117→121 日落法」（停 5 月）→ **arXiv 论文发表 (arXiv:2606.14589) + LLM-Observer 研究攻关 #1（宪法级机械化人眼）** + 日落法北极星（GitHub 访客头条）(b) **Highlights 段**标题 (2026-05)→**(2026-05 → 07)** + 表头插 4 行 6-7 月里程碑（arXiv 发表+IEEE·ISSRE 投稿 / LLM-Observer #1 Stage 0-6 / DeepSeek→10 providers / 数据驱动根治 arc V37.9.130-214）(c) **10-provider 一致性收敛**: 6 处「8-provider」「+5 more」「Gemini fallback」（V37.9.209 同步 provider-count prose 时**整个漏了 README** 的架构文本+组件表+capability plane+命令注释+adapter file-row，Gemini 已 V37.9.129 退役）→ 10 / DeepSeek·Doubao (d) openclaw_architecture sync note v2026.3.23→v2026.4.27（Gateway 已升 V37.9.138）。③ **诚实不动（V37.9.153 纪律）**: Publications 段（589-597，V37.9.155 已当前含 arXiv/中文科普版/why_runtime 双语）+ file-table 版本标签（point-in-time 记录）+ 稳定文件。④ **验证**: 三 doc-drift 机器检查（gen_readme_badges/gen_jobs_doc/gen_compat_matrix）无漂移（只动 prose 非机器 token）/ governance 91/91 EXIT=0 / 安全 95/100 / **VERSION 0.37.9.82 不变**（纯 GitHub 门面刷新零业务语义，镜像 V37.9.153 门面刷新 + V37.9.164/209 一致性收敛惯例）。**复杂度预算**: files/env/jobs/runtime-state/suites/tests/checks/VERSION 全不变 / 净 runtime 机制 0 / 退役 6 个 README provider-count 漂移点（V37.9.209 遗漏补齐）。⑤ **Mac Mini 待用户**: git reset 同步（纯 doc 零重启零业务影响）。⑥ **元价值**: 用户视角发现 GitHub 门面陈旧（Highlights 停 5 月、8-provider 不一致）——正是原则 #13 的价值（用户看外部呈现，dev 看内部）。刷新聚焦真内容陈旧（arXiv/observer/10-providers），严守 V37.9.153 纪律不 churn point-in-time 记录。 |
| V37.9.214 | 2026-07-02 | **08:30 watchdog 4-finding triage → governance 审计间歇性 fail 根因定位+修复（INV-REVIEW-001 负载超时）+ B1 告警失明修复 + B2 假 FATAL 日落法根治（errtrace re-enable landmine）；VERSION 不变** — ① **触发**: 用户贴 08:30 job_watchdog 告警（4 项：KB晚间整理 llm_failed / 治理审计 fail / dblp bad_json / kb_evening ERROR）。逐条数据驱动 triage（原则 #28 先取数据再动手，全程让用户 Mac Mini 取证不盲改）。② **KB晚间整理 llm_failed + dblp bad_json = 已知瞬态**: evening = V37.9.213 F3 同事件（`last_run_evening.json` 实证 `status:llm_failed reason:"LLM returned empty content"` @22:00，deep_dive 同 proxy 晚 30min 成功=瞬态），今晚 22:00 成功即清；dblp = `attempt 1/3` 自动重试类瞬态。③ **治理审计 fail 诊断（间歇性坐实）**: 用户 `governance_checker --full` grep `❌|💥` **空**（warm 760/760 过），`governance_audit_cron` 却报 **rc=1 759/760**（warm/cold 元规则都 19/23 → 19/23 是常量非失败，冷时恰 1 个 check 💥）。同代码 warm 过 cold 挂 = 间歇性。④ **根因定位（用户 6× 并行压测确定性复现，非 cold-repro）**: `sleep 400` cold 单跑 **rc=0 过**，但 **6× 并行 governance --full → 6/6 全部 💥 `[INV-REVIEW-001]` 的 runtime check "真实 subprocess 执行 kb_review.sh 不触发 JSONDecodeError"** → **负载诱发 subprocess 超时（非 cold-call，V37.9.157 已修 cold-call）**：该 check 真跑 kb_review.sh subprocess `timeout=45`，重负载下（07:00 cron 风暴 / 09:52 我刚跑完 5× loop）被 CPU 饿死超 45s → TimeoutExpired → 💥。⑤ **根因修复（延续 V37.9.145/157 lineage）**: INV-REVIEW-001 runtime check `timeout 45→90` + **捕获 `subprocess.TimeoutExpired` 视为 INCONCLUSIVE**（stderr WARN + skip 断言，不 💥）—— 因为**超时不是这个 check 守的 JSONDecodeError 回归的证据**（回归在 subprocess 完成时快速 manifest），环境负载不该让每日治理审计 💥。⑥ **B1 修复（surface the reason 镜像 F1）**: `governance_audit_cron.sh:89` `GOV_VIOLATIONS=grep "❌"` → `grep -E "❌|💥"` + 告警标签 `不变式违反:` → `不变式违反 / 检查出错 (❌ fail / 💥 error):`。**根因**: 冷失败是 💥 error 不是 ❌ fail，旧 grep ❌ 漏掉 → "Governance Audit 失败" 告警「不变式违反」段**空**（告"失败了"却不告"哪个 check"，与 F1 "报症状不报原因" 同构）。B1 让**未来任何 error-class 失败告警自报 💥 check 名**（belt-and-suspenders 双保险）。⑦ **B2 假 FATAL 日落法根治（errtrace re-enable landmine，第 3 次复发终结）**: `[SYSTEM_ALERT] governance_audit FATAL abort line=101` = ERR trap 在 GOV_RC=1 已处理路径误触发的假"治理审计自身死亡"。3 个 dev repro（结构 / notify-内部失败 / 真脚本强制 GOV_RC=1）**在 dev bash 5.x 全不复现** → 确认是 **bash 3.2 特有 errtrace quirk**（LINENO=101 是 bash 3.2 trap 误报）。V37.9.105 用 `set +E` 包每个 `$()` 再 `set -E` 复原，但**每个 `set -E` re-enable 都是 bash 3.2 landmine**（V37.9.105 line 64 / line 100 / 本次 line 101 三次复发 = whack-a-mole）。**根治**: `set +E` 一次（errtrace 从 governance 块起关到脚本尾），**移除两个 `set -E` re-enable + 冗余 `set +E`**，消除整类 landmine。errexit(`set -e`) 仍在（真 main-shell silent abort 仍触发 trap = MR-19 核心保留）+ 顶部 `set -eEuo` 声明保留（governance check 7526 + cron_monitor_scanner 查声明）+ 两审计 `$()` 都 errtrace-off（子 shell 不继承 trap，V37.9.105 保护保留）+ reporting 全 if/|| 安全。⑧ **验证**: **单跑 governance exit 0（生产-representative，生产 governance 单跑非 6×）** + **6× 并行 INV-REVIEW-001 不再 💥**（grep 空 ✅）+ **B2 修复不破坏 dev 正常路径**（forced GOV_RC=1 → exit 0/无 FATAL/status 写入/overall=fail 正常）/ cron_monitor_scanner 7/0 + governance 91/91 exit 0（MR-19 契约靠顶部声明满足）/ full_regression 138/0/**4985 (+12)** / 安全 95/100 / 零泄漏 / 三 doc-drift 无漂移 / **VERSION 0.37.9.82 不变**（INV-REVIEW-001 负载鲁棒性 + B1/B2 监控告警可观测性/健壮性，均非核心 runtime，镜像 V37.9.157/141/162 惯例）。守卫: +4 GovAuditAlertSurfacesErrors（B1）+ 4 ReviewCheckLoadHardening（INV-REVIEW timeout=90+TimeoutExpired catch）+ 4 B2NoErrtraceReEnable（无 `set -E` re-enable landmine 回归守卫）。**🔴 B2 修复只能 Mac Mini bash 3.2 验证**（dev bash 5.x 不复现该 quirk）。**复杂度预算**: files 不变（扩 governance yaml + test）/ 净 runtime 机制 ≈ 0（负载鲁棒性 + 告警可观测性，非新接缝，退役 alert 失明 + 负载假 💥）。⑨ **连带修（我的 V37.9.214 注释加长 INV-REVIEW-001 块撑爆定长切片守卫）**: test_kb_review + test_v37_9_157 的 `yml[i:i+N]` 定长切片 → 改切到 `shutil.rmtree` 边界（robust，一物一形抗块增长）；顺手恢复 job_watchdog.sh 的对抗混沌审计 sabotage 残留（`LC_ALL=C awk` 未恢复，V37.9.186 教训: 不与 chaos 层并发跑）。⑩ **诚实登记（未盲修，原则 #18）**: 6× 并行还暴露 **INV-WATCHDOG-SELF-001 ❌**（4/6）—— 但那是 **6 个 governance 实例自并行争用共享文件的 artifact，非生产**（生产 governance 单跑，07:00/09:52 都只 INV-REVIEW-001 💥；INV-WATCHDOG 生产单跑从未触发）→ 登记为潜在类（"治理 runtime check 对负载敏感"）待系统性评估，不投机单修。⑪ **🔴 Mac Mini 待用户**: (a) git reset 同步 → 下次 07:00 治理审计 INV-REVIEW-001 负载下不再 💥（90s + inconclusive catch）+ **B2 假 FATAL 不再误触发**（errtrace landmine 移除）。(b) **B2 只能 bash 3.2 验证**（dev bash 5.x 不复现该 quirk）: 同步后观察后续冷/负载失败是否还伴随 `[SYSTEM_ALERT] governance_audit FATAL abort` 假告警（预期消失）；若仍出现，`bash -x ~/governance_audit_cron.sh 2>/tmp/gac_x.txt` 抓 trace 贴我。(c) 清 watchdog 陈旧治理 flag `bash ~/governance_audit_cron.sh`（现全过写 pass）。⑫ **元价值**: 观察者自评（V37.9.213）→ watchdog 告警（本版）连续两天数据驱动根因诊断——全程零盲改（cold-repro 排除 cold-call → 6× 并行确定性复现 → 单跑验证生产修复 → 诚实区分生产问题 vs 6× artifact）。治理审计冷/负载 flake 是 V37.9.145/157 记录的 4.27+负载类的收尾：cold-call V37.9.157 修，load-timeout V37.9.214 修。B1 是让同类失败自诊断的 enabler。 |
| V37.9.213 | 2026-07-02 | **daily_observer 每日自评行动 — F1 deep_dive 降级原因聚合（77% 但为什么 → 自助分解）+ F2 dream 截断假阳性根治（观察者 head 采样切句中，V37.9.93 类复发；VERSION 0.37.9.81→0.37.9.82）** — ① **触发**: 用户贴 2026-07-02 06:30 daily_observer（机械化人眼，宪法级 #1）自评报告，4 个 finding。这正是观察者存在的意义——在用户之前发现静默质量退化。逐条 ground（原则 #28，4 并行 Explore agent 遇 529 过载后主线程直接读代码）判 dev-可修 vs Mac-Mini-gated。② **F1 [HIGH] deep_dive 摘要级降级率 77%（24/31）= unfinished #7（V37.9.183/184 OA 解析 follow-up，~6/29 复核到期）**: 数据说话——OA 解析没把率压下来（76%→77% 几乎没动）。诊断：`scan_deep_dive_modes` 测量可靠（30 天窗口 + `**模式**` marker），77% 真实；根因（哪些论文/为何）需 Mac Mini 每文件降级原因。**但 `kb_deep_dive.build_deep_dive_markdown:656` 已把机器可解析的 `> ⚠️ 抓取降级原因：<reason>` 写进每个 abstract_only 文件**（reason 三类：`PDF fetch failed:` / `HTML fetch failed:` / `tier{N} source`），observer 只分类 full/abstract **没聚合原因**。**交付（dev-fixable 最高杠杆）**: `_DD_DEGRADE_RE` + `_extract_degrade_category`（4 桶：PDF 抓取失败/HTML 抓取失败/非全文来源/未标注原因）+ `scan_deep_dive_modes` 聚合 `degrade_reasons` + `build_deep_dive_mode_section` 渲染「降级原因」分解行（计数降序）→ 把「77% 但为什么」变自助（下次报告直接告诉是付费墙结构性=非全文来源/接受，还是 fetch-path 失败=PDF/HTML 可修），**无需 Mac Mini grep**。同 MR-4 闭真盲区逻辑（V37.9.168 sanctioned）。③ **F2 [MED] dream 截断 = 观察者 head 采样假阳性根治**: `[弱关联]...运力预案生成与多` 中途截断——**dream 文件本身完整**（7284 chars），截断是 `_read_file_sample` 的 head 采样（V37.9.93 head 1400+marker+tail 500）在 char 1400 从句中切断，LLM assessor 无视 marker「File is complete」仍判「实际内容中断」= V37.9.93 假阳性类在 **head 切点**复发（V37.9.93 修了 tail/footer 可见但 head 中途切仍像截断）。**交付**: `SMART_SAMPLE_HEAD_SNAP_MAX=300` + head 先预留 marker+tail 预算（`head_budget`）再 snap 到最后换行（`rfind("\n")`）→ head 干净收尾不切句中，移除假阳性刺激源（顺势设计原则 #22：不靠 LLM 正确解读 marker，直接不给它看句中切）。marker omitted 仍从 nominal head_chars 算（保 `test_marker_reports_correct_omitted_count`）；无就近换行长行保留 raw head（budget 保）。④ **F3 [MED/LOW] evening 缺失 = 真 job 瞬态失败（Mac-Mini-gated 非 observer bug）**: writer/reader 路径日期逐字一致（both `daily/evening_{YYYYMMDD}.md`），deep_dive(22:30 同 proxy)晚 30 分钟成功 → 22:00 evening 撞瞬态 LLM/proxy 失败，`kb_evening.sh` 非零退出于 file-write 之前不产出。observer 已 surface（missing_output 已告警），加 retry=过度工程（原则 #8）不做。⑤ **F4 [MED] 基于标题推断 = NO-ACTION（正确诚实行为，原则 #23）**: `jobs/dblp/run_dblp.sh:356` 明确「DBLP 不提供摘要...必须显式标注 (基于标题推断)」——设计的诚实出处标注，observer 过度谨慎把正确行为当幻觉。绝不"修"（修了反删诚实标注）。⑥ **守卫（+19 单测，daily_observer 207→226）**: TestV37_9_213_DegradeReasons（提取器 4 类 + scan 聚合 + full_text 不贡献 + section 渲染排序 + 向后兼容 + **sabotage 破 `_DD_DEGRADE_RE` → 全落未标注原因证 load-bearing**）+ TestV37_9_213_HeadSnap（血案场景 head 干净边界 + **反向验证 SNAP_MAX=0 → head 切句中证 snap load-bearing** + 无换行保 raw + omitted 稳定）+ 源码守卫。同步更新 test_read_file_sample_uses_smart_path 源码守卫 + **INV-OBSERVER-001 治理 file_contains pattern**（`content[:head_chars]+marker` → `head+marker`，防回退守卫意图不变，不加新治理 check 避 governance bloat 镜像 V37.9.168/159）。⑦ **验证**: full_regression 138/0/**4973 (+19)** / governance 91/91 EXIT=0（INV-OBSERVER-001 ✅）/ 安全 95/100 / 安全扫描零泄漏 / 三 doc-drift 机器检查同步后无漂移 / **VERSION 0.37.9.81→0.37.9.82**（F2 改 daily_observer cron 组件喂 LLM 的采样内容 → 可影响其 LLM 打分输出 = live 组件 runtime 变更，镜像 V37.9.199 detector 行为变更 bump；F1 是 observability V37.9.168 惯例本不 bump，合并版本随 F2 bump）。**复杂度预算账本**: files 不变（扩 daily_observer + test）/ env/jobs/runtime-state-sources 不变 / suites 不变 / tests +19 / **净 runtime 机制 ≈ 0**（F1 聚合复用既写的降级原因行零新状态源；F2 采样精度修 = 退役 head 中途切假阳性刺激；均缩噪声/加自助诊断非新接缝）。⑧ **🔴 Mac Mini 待用户**: git fetch+reset 同步（daily_observer.py 走 FILE_MAP）→ 次日 06:30 报告 deep_dive 段含「降级原因」分解（Discord 通道，不依赖任何失效主体）+ dream 段不再误报截断。**F3 evening 诊断（dev 不可执行）**: `cat ~/.kb/last_run_evening.json`（预期 status=llm_failed/collector_failed）+ `grep SYSTEM_ALERT ~/kb_evening.log` + `ls ~/.kb/daily/evening_20260701.md`（确认真缺）。**F1 根因**: 同步后下次报告的降级原因分解会告诉哪类占主导（付费墙结构性 vs 可修 fetch-path），据此决定是否值得投入 OA 解析加固。⑨ **元价值**: 机械化人眼自评首次驱动真实行动闭环——它抓的 4 个 finding 里，2 个 dev-可修（F1 让「率」升级为「原因」自助诊断 + F2 修观察者对自己的假阳性），1 个 Mac-Mini-gated 真失败（诚实给诊断命令不假装能修），1 个 NO-ACTION（诚实识别正确行为不制造修复）。观察者的第一个真 bug 修复（F2）是它自己的采样假阳性——正印证论文 §5.2「Observer 自己是 LLM 组件须同等审视」。附带发现潜伏隐患（evening/deep_dive 用 system-local date vs dream 用 HKT TZ，一物一形不一致，非本次成因低优登记）。 |
| V37.9.212 | 2026-07-01 | **Qwen3 端点刷新 原则 #31 全量同步 — 直连远端 LLM 的 ops 脚本 env 化（V37.9.211 漏掉的直连消费方，绝不硬编码新裸 IP，VERSION 不变）** — ① **触发**: V37.9.211 adapter 端点刷新后 Mac Mini E2E 诊断确认 adapter 修好（plist env 待用户），但审计（原则 #31 跨消费者全量同步）发现**还有脚本直接 curl 远端 LLM 绕过 adapter 且硬编码旧死域名** `https://hkagentx.hkopenlab.com/v1/models`。② **诊断（grounded grep 全仓直连消费方）**: `diagnose.sh:62`（手动诊断工具 /models 查询）+ `health_check.sh:54`（每周一 09:00 健康周报 CURRENT_MODEL）硬编码旧域名 → 服务器重装后 521 失效；`jobs/finance_news:768` 实际走 `localhost:5001`（经 adapter，**无需改**）；`deploy/install_openclaw_macmini.sh` 用 `QWEN_BASE_URL` env（fresh install，与 adapter 读的 REMOTE_BASE_URL 变量名不一致）。③ **🔴 安全（同 V37.9.211）**: 这些脚本**绝不能改成硬编码新裸 IP**（暴露自建服务器进公开仓库）→ 全部 env 化 `${REMOTE_BASE_URL:-https://hkagentx.hkopenlab.com/v1}/models`（公开域名 fallback，dev/无 env 优雅降级）。④ **交付**: diagnose.sh + health_check.sh 两处 /models curl env 化 + install 脚本加 `REMOTE_BASE_URL = QWEN_BASE_URL` 别名（镜像既有 REMOTE_API_KEY 别名，shell RC + gateway plist EnvironmentVariables 两处，fresh install 可复现性）。⑤ **验证**: full_regression 138/0/**4954 不变** / test_health_check_v37_9_78 45 全过 / bash -n 三脚本 OK / 无测试断言旧硬编码 URL / 安全扫描新 IP+新 key 零硬编码 / VERSION 0.37.9.81 不变（ops/report/deploy 脚本非核心 runtime 服务，同 V37.9.141 preflight 惯例）。**复杂度预算**: files 不变（改 3 脚本）/ 净 runtime 机制 0（env 化直连消费方 = 原则 #31 补齐，非新接缝）。⑥ **🔴 Mac Mini 待用户**: 除 adapter plist（V37.9.211）外，`.env_shared`（cron 脚本 env 源）也须设新 REMOTE_API_KEY + REMOTE_BASE_URL，否则每周健康周报 CURRENT_MODEL 查询失败（走 fallback 旧死域名）。⑦ **元价值**: 原则 #31「跨消费者豁免/配置必须全量同步」再次兑现——base_url 被 adapter + 直连 ops 脚本多方消费，端点刷新必须同步所有消费方，V37.9.211 只改了 adapter（主路径）漏了直连脚本，本版补齐；全程守「机密走 env 不硬编码」公开 repo 底线。 |
| V37.9.211 | 2026-07-01 | **Qwen3 primary provider 端点刷新（服务器重装 → base_url env 化 REMOTE_BASE_URL，裸 IP 类机密不入库，镜像 deepseek V37.9.201 安全模式，VERSION 0.37.9.80→0.37.9.81）** — ① **触发**: 用户报 primary LLM Qwen3 因服务器重装，provider 注册信息变更（旧 `hkagentx.hkopenlab.com/v1` 失效），新 endpoint = 裸 IP `http://<ip>:8000/v1` + 新 key（明文，授权本次 session 使用），model `Qwen3-235B-A22B-Instruct-2507-W8A8`（**未变**）。② **🔴 安全第一（公开 repo 底线 + push 前 sk- 扫描会拦截）**: 仓库公开（README/文章对外发布）→ 明文 key **绝不入库** + 新 base_url 是**裸 IP + HTTP**（暴露用户自建 LLM 服务器基础设施 → 招致扫描/滥用）= 类机密**也不入库**。镜像 deepseek V37.9.201「裸 IP 端点走 env」决策。③ **先 ground（原则 #28）**: 读架构确认 primary provider 真理源 = `providers.py:490 QwenProvider.base_url`（类属性硬编码公开域名，经 `to_legacy_dict()` → PROVIDERS dict → adapter.py `TARGET_BASE`）；base_url 是唯一**不可 env 覆盖**字段（key 走 REMOTE_API_KEY env / model 走 MODEL_ID env 已可覆盖）。④ **de-risk（原则 #28 + feedback #1）**: dev 容器 curl 新端点 20s 超时——但**这是 dev 出站网络策略限制**（裸 IP 是公网 IP 不在 proxy noProxy 私网段 + dev 无 HTTP_PROXY 直连被拦），**非端点死了**（proxy status 证实网络受限）；绝不重蹈 chaspark 403 误判 → 端点功能验证必须 Mac Mini 做（adapter 真运行处 + 防火墙 allowlist 网络路径）。⑤ **交付（安全模式，镜像 deepseek）**: `QwenProvider` 加 `__init__` 从 env `REMOTE_BASE_URL` 解析 base_url + `_QWEN_PUBLIC_FALLBACK_BASE` 公开域名 fallback（历史端点，已公开在 git 历史无新泄漏 + 合约通过 + 文档结构）；env 值经 `to_legacy_dict()` 自动流到 PROVIDERS dict → adapter primary + fallback 两路径，**无需改 adapter.py**（一物一形，env 解析单一处）。key 已走 REMOTE_API_KEY env（无代码改动，用户更新 env 值），model_id 未变。顺手更新 CLAUDE.md「查询远端模型ID」命令用 `${REMOTE_BASE_URL:-...}`（指向死域名 → env 化一致）。⑥ **守卫（4 新单测 TestQwenBaseUrlEnv）**: base_url env 解析（无 env → 公开 fallback / 有 env → 覆盖）+ env 流到 legacy dict（adapter 消费路径）+ model_id 未变 + **🔴 `test_no_bare_ip_or_key_hardcoded_in_repo`**（providers.py 无裸 IPv4 base_url + 无 sk- key 字面量 + 引用两 env 变量 + fallback 是 https 域名，测试本身不嵌明文机密用 regex）。**反向验证守卫真有效**: sabotage 注入假裸 IP 到 fallback 常量 → 守卫 FAIL，备份还原 → PASS。⑦ **验证**: full_regression 138/0/**4954 (+4)** / governance 91/91 EXIT=0 / 安全 95/100 / **安全扫描零泄漏**（全仓 grep 新 key + 裸 IP 14.x + sk- 全空，git diff 无机密）/ 三 doc-drift 机器检查无漂移（base_url 不在矩阵表）/ **VERSION 0.37.9.80→0.37.9.81**（primary provider 端点解析机制 runtime 变更，镜像 deepseek provider 变更 bump 惯例）。**复杂度预算账本**: files 不变（扩 providers.py + test）/ **env +1（REMOTE_BASE_URL，Mac Mini 设置，dev 无 env 优雅降级公开 fallback）** / jobs/runtime-state-sources 不变 / **净 runtime 机制 = base_url env 解析（复用 deepseek 既定安全模式，非新框架）**。⑧ **🔴 Mac Mini 待用户（E2E dev 不可替代，原则 #6/#9-H）**: 合并 + git fetch+reset 同步 → 在 adapter（+ proxy）env 源（`.env_shared` 或 plist，即当前提供 REMOTE_API_KEY 的同一处）**设两个 env**: `REMOTE_API_KEY=<新 key>` + `REMOTE_BASE_URL=http://<新 IP>:8000/v1` → 重启 adapter（launchctl bootout/bootstrap 让 env 重载）→ **E2E 验证**: `curl "$REMOTE_BASE_URL/models" -H "Authorization: Bearer $REMOTE_API_KEY"` 应列 Qwen3 模型 + `curl localhost:5001/health` + WhatsApp 发消息确认 PA 回复（primary Qwen3 真接管）。⑨ **🔴 安全提醒**: 明文 key 在对话出现，建议**用完轮换**（防御纵深，对话历史可能被记录；授权仅本次 session）。⑩ **元价值**: deepseek「裸 IP 端点走 env」安全模式（V37.9.201）复用到 primary provider 端点刷新——服务器重装的运维变更用既定安全模式处理，全程零硬编码机密守公开 repo 底线，dev 网络受限不下「端点死」误判（feedback #1）。 |
| V37.9.210 | 2026-07-01 | **LLM-Observer Stage 5.1 flip 决策框架预注册（design doc §9.1）— 门控收敛日一致性收敛（散落 changelog 的 flip 标准收敛到 design doc 单一 canonical + Stage 表进度反映现实，纯 design/state 零业务语义 VERSION 不变）** — ① **触发**: 周三开工健康基线全绿（139 suites / 4950 tests / 0 fail / governance EXIT=0 91/91 / 三 doc-drift 机器检查 gen_readme_badges/gen_jobs_doc/gen_compat_matrix 全绿无漂移 / OpenClaw 4.27 已升无 pending / 工作树干净同步 origin/main #771 V37.9.209）。核实全部 17 项 unfinished **均 gated/deferred/external**（LLM-Observer shadow 周观察中 / DeepSeek Phase 2 生产观察 / IEEE·ISSRE in-review / 4.27 push 追踪 — 全 Mac-Mini 或外部动作），且今日已有两 prose-convergence commit（V37.9.208 docstring + V37.9.209 provider-count prose）→ 不制造第三个 cosmetic churn（原则 #34 日落法 + V37.9.153 明确警告 churn for cosmetics）。② **诚实核查 evidence current**: live 跑 `llm_observer_selfcheck --json` vs `docs/llm_observer_scorecard.md` **完全一致**（defense 1.0/6-6 + fp 0.0/0-4 + fn_B 1.0/4-4 + 6 detector 全 load_bearing）→ 证据无 stale，无需重生成。系统确处健康门控态。③ **从宪法级 #1（LLM-Observer）选非 gated 非 premature 真实价值项**: shadow 周（6/30 启动 ~7/7）后 flip `OBSERVER_FP_MODE=on` 是**有真实代价决策**（fp verdict `.extend(anomalies)` → 影响 `overall_score` + 触发 Discord #observer 告警），V37.9.199 已证 shadow 抓真 FP（S5 评分字段误报）→ flip 有噪声风险的真决策。**gap（诊断，原则 #28 grounded 全文件核实非凭推测）**: (a) design doc 实施路径表停在 V37.9.193 的「今日仅 Stage 0」进度描述（stale，Stage 1-5 + 6ch1 均已完成）(b) flip criteria 散落 V37.9.198/199 changelog prose（「信号真有用且 FP 噪声低」模糊，无结构化决策规则）。④ **交付（design-only，收敛 = 一物一形 + 一致性收敛）**: **§9.1 Stage 5.1 决策框架**——预注册 shadow→on 决策规则（数据到达前定规则 = pre-registration 防 motivated reasoning，正是论文 #2 自身方法学）: 观察对象（`score_history` fp_high/med 7 天序列 + 每日报告 fail_plausible 段 L1+L2 证据逐条人工判 TP/FP + `status.quality.observer.fail_plausible_*`）+ 核心度量 **live precision**（scorecard §5.4 fp_rate 应用到生产真实数据非 corpus）+ **flip criteria**（C1 零未修复系统性 FP：系统性误报先 shadow 内修 detector 不带噪声 flip / C2 信号有用性：≥1 TP 或干净周均可 flip / C3 cheap-path 成本可持续）+ flip vs extend 逻辑（系统性 FP → shadow 修 + scorecard 复跑 → extend / FP-only 周 → 修 + extend）+ flip 后变化（fp 进 anomalies 影响评分+告警 FP bar 高）+ **rollback 即时可逆**（env flip next cron 生效，flip 是可逆决策非 irreversible）+ small-N 诚实边界（样本小不追统计显著追无系统性噪声，同 detection_latency/calibration 标 N/A 同源诚实）+ force_judge 不与 flip 捆绑（避免一次改两变量）。**Stage 表进度更新**反映现实（Stage 0-5 ✅ 各标 V37.9.193-198 版本 + 6 chunk 1 ✅ V37.9.200 + 新增 Stage 5.1 🟡 shadow 周行，退役「今日仅 Stage 0」stale 描述）。⑤ **日落法一物一形**: flip 标准从散落 V37.9.198/199 changelog prose 收敛到 design doc §9.1 单一 canonical（未来 shadow 周完成后按 §9.1 判 flip/extend，决策结构不再散落）= 镜像论文 pre-registration 方法学 + 外部评审2「一致性收敛」指令。⑥ **验证**: 三 doc-drift 机器检查无漂移（未破坏徽章/机器表）/ governance EXIT=0 91/91 inv / 安全扫描零泄漏（design doc 无 key/手机号/内网 IP）/ **VERSION 0.37.9.80 不变**（设计文档预注册决策规则零业务语义零代码零测试改动，同 V37.9.192/193 design doc 惯例）。**复杂度预算账本**: files 不变（扩既有 design doc + status/CLAUDE 文档同步）/ env/jobs/runtime-state-sources/suites/tests/checks/VERSION 全不变 / **净 runtime 机制 0**（决策框架是研究/设计产物非 runtime 接缝；收敛散落 prose 到单一源 = 退役一个一物多形漂移面）—— 日落法健康态。⑦ **刻意无守卫测试**（design doc 决策框架不在机器跟踪范围，靠 changelog 留痕 + 收工自问，MR-22 示范 + 对齐 §十「design docs 不立机器守卫」）。⑧ **Mac Mini 待用户**: git fetch+reset 同步（纯 doc/state 零重启零业务影响；shadow 周 ~7/7 完成后按 §9.1 criteria 判 flip）。⑨ **元价值**: 门控收敛日无强 dev 候选（所有实质待办 gated + 今日已两 prose-convergence）用于宪法级 #1 的**设计半**推进——把「shadow 周后凭感觉 flip」升级为「数据到达前预注册决策规则」，防 motivated reasoning（"想上线就把噪声说成可接受"），是 design-before-action 的 disciplined 工作而非 cosmetic churn。延续 V37.9.185/188/208/209 门控日收敛谱系。 |
| V37.9.209 | 2026-07-01 | **provider-count 现状 prose 一致性收敛 — 5 处 human-维护 prose 从 stale「8 Provider」收敛到当前 10（doubao 第 8 时改到 8, deepseek×2 后未 bump; 机器表/徽章覆盖不到的 prose 漂移; 外部评审2「文档一致性」弱项, 纯 prose 零业务语义 VERSION 不变）** — ① **触发**: V37.9.208 docstring 收敛后用户问「今天可执行哪些推荐任务 / 还是继续观察」。grounded 核实（非凭推测）: 三 doc-drift 机器检查全绿 → 漂移在徽章/机器表覆盖不到的 human prose。② **诊断（原则 #31 全量 sweep）**: grep provider-count prose 抓到 5 处 stale — 总数已 10（7 built-in + doubao + deepseek×2）但 prose 仍说「8 Provider」: FEATURES.md:66 兼容性矩阵行 / INDEX.md:35 / compatibility_matrix.md:4 human header「8 Providers（7 built-in + 1 plugin）」（该文档 L4 自述「人工段落不参与机器比对」= 未守卫漂移源）/ compatibility_matrix.md:76「第 8 个 provider」历史计数 / CLAUDE.md:116 组件表「(7)」。③ **修复**: 前 4 处 → 10（7 built-in + 3 plugins：Doubao + DeepSeek×2），L76 历史计数去数字化「均经此路径接入」+ 补 deepseek_full 样例（退役漂移源, V37.9.152 method）；CLAUDE.md:116 → 10 with breakdown（对齐 README:212 canonical「10 concrete providers」）。④ **诚实留手动（原则 #34 + V37.9.152）**: CLAUDE.md:23 架构图「LLM (7 Providers)」不动 — 图下方 box 逐字列 7 built-in（Qwen3/GPT-4o/.../GLM）= illustrate built-in core 非枚举 plugin, 且 box 右边框依赖 wide-char 终端渲染（char 位 70/95/84/87 非 char-对齐, 7→10 破坏渲染）→ 图=built-in 视图 / 表+prose=total 计数, 各自诚实无矛盾。⑤ **frozen 不动**: docs/changelog.md 全部条目 + CLAUDE.md changelog（含 V37.9.147「8 Provider API Schema Drift」= reliability_bench scenario #8 名非计数）= point-in-time 历史记录。⑥ **验证**: 三 doc-drift 机器检查全绿（只动 human prose 非机器表 gen_compat 三表一致）/ full_regression 138/0/4950 / governance 91/91 / 安全 95 / 零泄漏 / **VERSION 0.37.9.80 不变**（纯 prose 一致性收敛零业务语义, 同 V37.9.151/152/164 doc-sync 惯例）。**复杂度预算账本**: files/env/jobs/runtime-state-sources 四维度不变 / 净 runtime 机制 0 / 退役 1 个 human-prose 漂移源（L76 去数字化）。⑦ **Mac Mini 待用户**: git fetch+reset 同步（纯 doc 零重启）。⑧ **元价值**: V37.9.208（deepseek_full docstring）+ V37.9.209（provider-count prose）同日两次一致性收敛 = 门控收敛日把「等 gated 信号成熟」窗口用于退役 human-维护 prose 漂移源，正中外部评审2「文档一致性」弱项。 |
| V37.9.208 | 2026-07-01 | **deepseek_full「诚实语义」docstring 块 stale 修复 — 从 V37.9.204 declared-only 措辞收敛到 V37.9.205 feature_verified 现实（一致性收敛 + 原则 #23 诚实，纯 docstring 零业务语义 VERSION 不变）** — ① **触发**: 周三开工健康基线全绿（三 doc-drift 机器检查 gen_readme_badges/gen_jobs_doc/gen_compat_matrix 全 ✅ 无漂移 / governance dev 91/91 / 工作树干净同步 origin/main V37.9.207 #769），实质待办全 gated（LLM-Observer shadow 周 / DeepSeek Phase 2 生产观察 / IEEE·ISSRE in-review / 4.27 push 追踪 — 均 Mac-Mini 或用户动作）。从 grounded 纯 dev 可执行项选一致性收敛（外部评审2 核心指令「从加功能切换到证据刷新与一致性收敛」+ 日落法对齐）。② **诊断（原则 #28 grounded 核实非凭推测）**: 三 doc-drift 机器检查全绿 → 漂移在徽章覆盖不到的 prose/comment。抓到 `providers.d/deepseek_full_provider.py` 类 docstring **内部矛盾**: 顶部 header（L1-4）正确反映 V37.9.205 feature_verified（text/tool_calling/reasoning E2E 3/3），但下方 **「诚实语义 (原则 #23)」块（L18-23）冻结在 V37.9.204 declared-only 措辞** —「全部能力仅声明, 未经 Mac Mini E2E 实测」+「tool_calling/json_mode/reasoning/vision 未实测 → False」+「context_window 保守占位待确认」= **与代码（capabilities: tool_calling=True/reasoning=True/verification_tier=feature_verified）+ 顶部 header 双双矛盾**。块本身标题「诚实」却陈述真相的反面（直接原则 #23 issue，fail-plausible 镜像: 读者读此块会认定 provider 是 declared-only 全 False）。③ **修复（一致性收敛）**: 「诚实语义」块重写为 V37.9.205 feature_verified 现实 + E2E 证据表（text/tool_calling/reasoning verified True + vision/json_mode/streaming/fallback False 各注原因 + context_window 1M V37.9.207 确认），镜像 sibling deepseek 量化版（其块本就正确维护带完整 E2E 证据表）。④ **原则 #31 全量 sweep**: AST 提取全 3 个 provider 插件的 docstring tier 声明 vs 代码 verification_tier — deepseek_full 是唯一漂移（deepseek 量化 feature_verified 匹配 / doubao production_observed 无 stale prose claim），修复完整无 sibling 遗漏；顺带核实 deepseek 量化 context_window=65536 placeholder docstring 与代码一致（不同端点 PENDING 无确认值，正确留 as-is 非漂移）。⑤ **验证**: 156 provider 测试全过（docstring-only 无测试改动，无 test guard docstring 措辞 — tests 断言 capabilities 对象非 prose）/ full_regression 138/0/4950 / governance 91/91 EXIT / 安全 95 / 零凭证泄漏 / 三 doc-drift 机器检查仍绿（未破坏徽章）/ **VERSION 0.37.9.80 不变**（纯 docstring/comment 一致性收敛零业务语义，同 V37.9.164/185 doc-sync 惯例）。**复杂度预算账本**: files/env/jobs/runtime-state-sources 四维度全不变 / suites/tests/checks/VERSION 全不变 / **净 runtime 机制 0**（诚实/证据刷新，退役一个内部 docstring 漂移源）。⑥ **Mac Mini 待用户**: git fetch+reset 同步（纯 docstring 零重启零业务影响；provider 行为完全不变）。⑦ **元价值**: 门控收敛日无强 dev 候选（migration/enforcement 均 premature 违日落法）用于纯收敛——把一个「标题诚实、内容不诚实」的 stale docstring 块从 V37.9.204 declared-only 收敛到 V37.9.205 feature_verified 现实，延续 V37.9.164/185/207 一致性收敛谱系。 |
| V37.9.207 | 2026-07-01 | **deepseek_full context_window 修正 65536→1048576 (1M, 用户/端点确认占位值错误) + Doubao vs DeepSeek 满血版 A/B 实测（用户驱动纠错 + 3 题并列对比）** — ① **触发**: 收工后用户要求「最后做一次 Doubao vs DeepSeek 满血版对比测试」+ 指出「DeepSeek Max context 应是 1M 不是 64K，核实」。② **A/B 实测（Mac Mini，3 题：多步推理/领域分析/结构化 JSON）**: 因 dev 容器无 key，给用户 base64 单行脚本（规避 zsh 长 heredoc 粘贴损坏 + 结尾杂散 `~` typo）+ 从 `com.openclaw.adapter.plist`（非 com.bisdom）PlistBuddy 提取 ARK env。**结果**: 两模型三题全对；**纯文本/推理 DeepSeek 满血略胜**（reasoning tokens 省一半 272 vs 486 / 领域分析洞察更深"从实验走向生产"/ 延迟略低）；**综合能力 Doubao 明显全面**（有视觉 DeepSeek 无 / 256K→实为 262144 / production_observed 已真接管）。**结论（非二选一）**: 当前 fallback 链 `['deepseek_full','doubao','deepseek']` 已结合两者——DeepSeek 满血接文本 + Doubao 兜底扛图片；真替代多模态 Qwen3 只有 Doubao 够格（DeepSeek 永无视觉）。n=3 抽查非严格 benchmark（诚实边界）。③ **context 修正（原则 #23/#28）**: `context_window=65536` 是 V37.9.204 标注的「保守占位待实测」猜测值，从未验证；用户（端点 owner）权威确认 1M → 改 1048576（与项目 K-约定一致，gemini 已用 1048576=1M；ModelInfo + capabilities 两处 + 注明 provenance=端点规格非我 E2E）。max_output_tokens 保持 8192（用户仅提 context）。verified_* 不受影响（context_window 非 verified 维度，是描述性 metadata 不影响路由评分）。④ **同步**: gen_compat_matrix --fix（能力矩阵 deepseek_full 64K→1048K）+ test_load_valid_yaml 的 65536 是合成 PluginLoader fixture 与真 provider 无关**不动**。⑤ **验证**: full_regression 138/0/4950 / governance EXIT=0 / 安全 95 / 零凭证泄漏 / gen_compat --check 三表一致 / **VERSION 0.37.9.79→0.37.9.80**（provider 能力声明修正 + 矩阵 doc 同步）。⑥ **元价值**: 用户领域专家纠错——「保守占位」的诚实标注（原则 #23）让错误值可被识别修正，而非伪装成已验证；A/B 实测把「哪个更好」从能力矩阵推断升级为真输出并列对比（原则 #13 用户视角）。 |

> 📦 **V37.9.206 及更早（268 版，V27 ~ V37.9.206）完整变更历史已归档至 [`docs/changelog.md`](docs/changelog.md)** — 移出每-session-必读热路径（冻结历史每行写入后不再编辑=一物一形，非双源维护；2026-06-17 首轮 190 版 + 2026-07-05 第二轮 78 版）。按需 `read docs/changelog.md` 查阅完整 V27 ~ V37.9.206。

## 常用命令

```bash
# 启动服务
nohup python3 ~/adapter.py > ~/adapter.log 2>&1 &
nohup python3 ~/tool_proxy.py > ~/tool_proxy.log 2>&1 &

# 健康检查
curl http://localhost:5002/health

# 一键重启
bash ~/restart.sh

# 运行单测
python3 test_tool_proxy.py
python3 test_check_registry.py

# 校验任务注册表
python3 check_registry.py

# 一键 smoke test（单测+注册表+连通性）
bash smoke_test.sh

# 全量回归测试（461个用例，发布前必须100%通过）
bash full_regression.sh

# 治理审计（ontology-native，23不变式+7元规则+6元发现）
python3 ontology/governance_checker.py              # dev 模式
python3 ontology/governance_checker.py --full        # Mac Mini（含 crontab/env/服务）
python3 ontology/governance_checker.py --json        # JSON 输出

# 安全评分（7维度100分）
python3 security_score.py
python3 security_score.py --update  # 写入 status.json

# 审计日志
python3 audit_log.py --tail 20      # 查看最近操作
python3 audit_log.py --verify       # 校验链式哈希
python3 audit_log.py --stats        # 统计概览

# 收工前全面体检（dev 环境）
bash preflight_check.sh
# 收工前全面体检（Mac Mini，含部署一致性+环境变量+连通性）
bash preflight_check.sh --full

# KB 搜索 / 摘要
bash kb_search.sh "关键词"         # 全文搜索
bash kb_search.sh --summary        # 统计概览
bash kb_search.sh --source arxiv   # 搜索来源归档
bash kb_inject.sh                  # 手动生成每日摘要

# Multimodal Memory（需要 pip3 install google-genai numpy）
python3 mm_index.py                # 增量索引媒体文件
python3 mm_index.py --reindex      # 重建全部索引
python3 mm_search.py "猫的照片"    # 语义搜索媒体
python3 mm_search.py --stats       # 索引统计

# KB 本地 Embedding + RAG（需要 pip3 install sentence-transformers）
python3 local_embed.py --bench     # 性能基准测试
python3 kb_embed.py                # 增量索引 KB 文本
python3 kb_embed.py --reindex      # 重建全部索引
python3 kb_embed.py --stats        # 索引统计
python3 kb_rag.py "Qwen3 模型"     # 语义搜索 KB
python3 kb_rag.py --context "AI论文" # LLM 可直接注入的上下文格式
python3 kb_rag.py --json "shipping" # JSON 输出（供脚本调用）

# 数据清洗（支持 CSV/TSV/JSON/JSONL/Excel）
python3 data_clean.py profile data.xlsx --format text   # 数据画像
python3 data_clean.py execute data.xlsx --ops trim,dedup,fix_dates  # 执行清洗
python3 data_clean.py validate original.csv cleaned.csv  # 验证结果
python3 data_clean.py list-ops                           # 可用操作
python3 data_clean.py history data.xlsx                  # 版本历史
curl http://localhost:5002/data_clean/help               # REST 端点帮助

# Quick Start（一键 10 分钟跑通全栈）
bash quickstart.sh                # 完整 4 阶段（前置检查→启动→健康→demo）
bash quickstart.sh --check        # 仅检查前置条件
bash quickstart.sh --demo         # 仅运行 demo 请求

# Memory Plane（统一记忆平面）
python3 memory_plane.py layers                    # 层可用性检查
python3 memory_plane.py stats                     # 各层统计
python3 memory_plane.py query "Qwen3"             # 统一搜索
python3 memory_plane.py query --context "AI论文"   # LLM可注入格式
python3 memory_plane.py query --layers kb "RAG"    # 仅搜索KB层

# 每日深度分析（V37.9.16，22:30 HKT cron）
bash kb_deep_dive.sh                       # 手动触发（挑选今日⭐≥4 候选 + 抓取原文 + LLM 深度分析 + 双通道推送）
KB_DIR=~/.kb python3 kb_deep_dive.py       # 直接跑 collector（JSON 输出到 stdout，不推送）
cat ~/.kb/deep_dives/$(date +%Y-%m-%d).md  # 查看今日归档

# Agent Dream v2（Map-Reduce 三阶段分离调度）
bash kb_dream.sh --map-sources # Sources Map 预热（00:00 cron，~10min）
bash kb_dream.sh --map-notes   # Notes Map 预热（00:25 cron，~10min）
bash kb_dream.sh              # Reduce：两层缓存命中→跨域关联→推送（03:00 cron，~3.5min）
bash kb_dream.sh --map-only   # 全部 Map 预热（Sources + Notes，~20min）
bash kb_dream.sh --dry-run    # 素材统计（不调 LLM）
bash kb_dream.sh --fast       # 跳过 Map，直接采样做梦（旧模式）
cat ~/.kb/dreams/2026-04-06.md  # 查看梦境结果

# Agent Reliability Bench（17场景故障评测，V37.9.147 +10）
python3 reliability_bench.py            # Markdown 报告
python3 reliability_bench.py --json     # JSON 格式
python3 reliability_bench.py --save     # 保存到 docs/reliability_bench_report.md
python3 reliability_bench.py --scenario 3  # 运行单个场景

# SLO Benchmark（真实生产数据报告）
python3 slo_benchmark.py          # Markdown 报告
python3 slo_benchmark.py --json   # JSON 格式
python3 slo_benchmark.py --save   # 保存到 docs/slo_benchmark_report.md

# Provider 兼容性矩阵
python3 providers.py              # Markdown 表格
python3 providers.py --json       # JSON 格式（供脚本调用）

# 生成任务文档 / 检测文档漂移
python3 gen_jobs_doc.py           # 输出 markdown 表格
python3 gen_jobs_doc.py --check   # 对比 docs/config.md 检测漂移

# 查询远端当前模型ID（V37.9.211：base_url 走 env REMOTE_BASE_URL，服务器重装后为裸 IP）
curl -s "${REMOTE_BASE_URL:-https://hkagentx.hkopenlab.com/v1}/models" \
  -H "Authorization: Bearer $REMOTE_API_KEY" \
  | python3 -c "import json,sys; [print(m['id']) for m in json.load(sys.stdin)['data'] if 'Qwen3' in m['id']]"

# Mac Mini 同步仓库（禁止用 git pull，会因历史 merge commit 分叉失败）
cd ~/openclaw-model-bridge && git fetch origin main && git reset --hard origin/main

# GitHub push前安全扫描（必须全部为空才允许push）
grep -r "sk-[A-Za-z0-9]\{15,\}" . --include="*.py" --include="*.sh" --include="*.md" | grep -v ".git"
grep -r "BSA[A-Za-z0-9]\{15,\}" . --include="*.py" --include="*.sh" --include="*.md" | grep -v ".git"
```

## 关键规则

### 模型ID规则
| 位置 | 格式 |
|------|------|
| `adapter.py` / `tool_proxy.py` | 裸ID（无前缀） |
| `openclaw.json` agents.defaults.model.primary | **必须带 `qwen-local/` 前缀** |
| `jobs.json` payload.model | **不指定**（继承默认值） |

### 硬性限制
- 工具数量 <= 12（超出导致模型混乱）
- 每任务工具调用 <= 2次（超出超时风险指数级上升）
- 请求体 <= 200KB（硬限制280KB，留buffer）
- `--thinking` 合法值：`off, minimal, low, medium, high, adaptive`（**禁止用 `none`**，这是v26修复的bug #92）

### 双 Cron 归属规则（V27新增）

| 调度器 | 管理方式 | 是否经过 LLM | 登记位置 |
|--------|----------|-------------|----------|
| `system` | macOS `crontab -e` | 否 | `jobs_registry.yaml` scheduler=system |
| `openclaw` | `openclaw cron add` | 是 | `jobs_registry.yaml` scheduler=openclaw |

**原则**：确定性脚本（清理、备份、抓取）用 `system`；需要 LLM 理解/生成的用 `openclaw`。
**新增任务必须先登记到 `jobs_registry.yaml`，运行 `python3 check_registry.py` 通过后才能注册 cron。**

### 安全规则（GitHub push前强制）
- API Key 必须通过环境变量：`os.environ.get("REMOTE_API_KEY")`
- 配置文档（含真实手机号/密钥）永不入库（已在 .gitignore）
- 公开仓库手机号统一用 `+85200000000` 占位

## 工作原则

### 🔴🔴🔴 第 0 号宪法：最强模型，不降配不降速不妥协

> **每次使用 Claude Code 必须使用家族中最强的 Opus 级模型。当前合法模型（按发布时间新旧）：Claude Opus 4.7（`claude-opus-4-7`，2026 发布，当前最强）> Claude Opus 4.6（`claude-opus-4-6`，向下兼容）。**
>
> 禁止为了"快一点"切换到较弱模型家族（Sonnet / Haiku），禁止开启 fast mode 牺牲推理深度，禁止因为 token 用量或响应速度而降级。
>
> 这个项目的核心竞争力是**思考深度和工程质量**，不是速度。用弱模型省下的 10 分钟，会在后续的 bug 修复、连锁故障、返工中付出 10 倍代价。
>
> **1111 个测试、53 个治理不变式、15 条元规则——这个系统的复杂度要求最强的推理能力来驾驭，没有妥协空间。**
>
> **"更强的新版本不是降级"原则**：Anthropic 发布更强的 Opus 版本（4.6 → 4.7 → 4.8 ...）时，自动纳入合法列表。**降级的判定不是"模型 ID 变了"，而是"推理能力变弱了"**。把 Opus 4.6 换成 Opus 4.7 是升级，不是违反第 0 号宪法；把 Opus 换成 Sonnet/Haiku 才是违反。
>
> **执行机制（2026-04-14 强制 + 2026-04-20 升级）**：每次新 session 开工的**第一条回复**必须包含一行模型声明，格式为：
> `🤖 本次 session 模型：Claude Opus <X.Y> (claude-opus-<x-y>) | 最强级别，无降级 | fast mode 状态见 /fast 命令`
> 其中 `<X.Y>` 是当前实际运行的 Opus 版本号（4.6 / 4.7 / 未来更新）。声明放在开工报告之前，独占一行，前面不加任何寒暄。如果运行时检测到模型**不是 Opus 家族**（例如被错配到 Sonnet 4.6 / Haiku 4.5），**立即停止工作并告知用户**。见原则 #30。

### 🔴 三合一宪法

> **用户提供专业深度 + Claude Code 提供高效设计部署 + OpenClaw 提供数据复利 — 三者合一成为有生命的闭环系统。**
>
> 共享状态：`~/.kb/status.json`（三方实时同步优先级、反馈、系统健康）

### 🔴🔴🔴 研究攻关 #1（宪法级 · 2026-06-28 确立）：机械化人眼 — LLM-Observer

> **下一阶段的第一任务,凌驾于一切功能添加之上。** 服务初始目标:成为世界顶级大模型系统专家——而顶级专家的跃迁不在 LOC,在**解掉自己提出的开放问题** + **让框架迁移出自己的机器**。
>
> **靶心问题(来自已发表论文 arXiv:2606.14589 的头条发现 = 也是其最大未解问题)**:`~70% 静默故障靠人看产品发现 / 测试·治理·健康检查 ≈0%`。这个差距是"预防新型故障"的约束本身——4,286 测试 + 827 治理检查全绿,真正的探测器是**一个人在像用户一样看产品**。
>
> **使命**:造一个**自动化的用户视角观察者(LLM-Observer)**,机械化人眼——在用户之前捕获测试/治理/审计栈漏掉的静默故障(尤其 fail-plausible)。把 taxonomy 从**描述性**升级为**预测性/预防性**。
>
> **三个概念锚点(各司其职,不是口号)**:
> 1. **fail-plausible** = 主要打击目标:LLM 把内部错误叙述成流畅可信的假输出——正是测试看不见的语义级故障。Observer 必须读**内容语义**,不是状态码。
> 2. **audit-as-regression** = 理论授权:审计设计上预防 0% 新型故障(只挡回归)→ 新型故障的预防**必须来自别处**;Observer 就是那个"别处"(缺失的预测/早探测引擎)。
> 3. **seams-not-components** = Observer 该看哪里:不是部件(测试已覆盖),是**接缝 + 面向用户的输出**——静默故障活在那里。
>
> **独有资产 = ground truth**:22 个完整 postmortem 语料库作**带标注验证集**(全世界没有第二个人有这个纵向靶子)。Observer 必须能在这 22 个历史事故上被 sabotage-validate(论文 §5.2 铁律:Observer 自己是 LLM 组件 → 继承全部 taxonomy → 需要和它评判的组件同等的治理/provenance/sabotage 验证)。
>
> **产出目标(一箭双雕,正面回应两次外部评审的同一对短板)**:① **研究级贡献**(论文 #2:描述 → 预测,正面回应 audit scorecard 的 0% prevention)+ ② **社区可跑的评测集**(种子 = reliability_bench → 一个别人能跑的 "silent-failure / fail-plausible 检测 bench")= **深度(攻自己提出的开放问题)+ 广度(真实迁移采用)**。
>
> **现有底座(种子,非从零)**:每日 LLM "observer" job(已能发现真回归,含一次 fabrication + 两个它自己的 bug)+ source_credibility 五档 + hallucination_guards 六档 + adversarial_chaos_audit 16/16。
>
> **状态**:🟡 方向已确立,**未开工**。下一步 = 写 design doc 拆子目标(评分函数 / ground-truth 标注协议 / Observer 自验证回路 / 与每日 observer job 的关系)。**警惕**:Observer 是又一个 LLM 组件 = 又一道接缝(日落法 #34)——设计时先问"它能退役什么人工观察",别变成纯加法。

### 🔴 战略定位（2026-04-03 导师评审确立）

> **项目已跨过关键门槛：Stage 1 完成（系统构建者）→ 正在冲刺 Stage 2（被社区认可的系统作者）。**
>
> **主战场**：不再是抽象的"LLM推理系统"，已收敛为两条线：
> 1. **Agent Runtime / Inference Gateway / Control Plane** — 模型接入 + 工具治理 + 可观测性 + 故障恢复
> 2. **Agent Memory Plane / KB-RAG / Job-Orchestrated Intelligence** — 记忆系统 + 知识检索 + 作业编排
>
> **旗舰叙事**：`OpenClaw Runtime Control Plane for Tool-Calling Agents`
>
> **核心洞察**：项目最强的不是单独算法，而是把模型接入、工具治理、可观测性、故障恢复、记忆与作业系统编织成可运行的 agent runtime。顶级专家不是死守最初设想，而是顺着已证明的能力继续放大。
>
> **12 个月路线图**：
> - **V1（0-4月）别人能跑**：安装稳定 / 配置清晰 / 文档闭环 / 最小 demo / golden test trace
> - **V2（4-8月）别人敢用**：benchmark / SLO dashboard / incident drill / 兼容矩阵 / semver
> - **V3（8-12月）别人会扩展**：provider plugin / tool policy plugin / memory plane plugin / SDK / extension guide
>
> **距离世界顶级的三块差距**：可迁移性（去硬编码场景依赖）/ 证据密度（benchmark+演练+案例）/ 话语权输出（代码+文档+评测+方法论=完整叙事）
>
> **详细战略文档**：`docs/strategic_review_20260403.md`
> **🔴 中长期技术纲领（2026H2→2028，V37.9.246 确立，长期保存 + 季度复核）**：`docs/technical_charter_20260705.md` — 双轨战略（研究轨×工程轨）+ 三地平线（H1 收敛解耦/H2 产品化生态/H3 标准影响力）+ 8 判据门控 + 反目标清单。V1/V2 路标已完成，V3 收口进纲领 H1/H2；"要不要做 X"的战略决策以纲领判据门控表（§8）为准。
> **🔴 纲领执行计划（V37.9.248 确立，配套运行手册）**：`docs/charter_execution_plan_20260705.md` — Part 1 H1 地平线 15 任务分解（含 done-criteria/门控/季度/依赖链）+ Part 2 每季度五项固定复核可运行协议（R1 影响力/R2 复杂度/R3 门控/R4 修订/R5 反目标，带命令+模板）。**每季度末照此协议跑一次复核**；"这季度做什么"以本执行计划 §1 + §11 季度实例为准。

### 🔴 每次必查（35条，优先级最高）

| # | 原则 | 一句话 |
|---|------|--------|
| 1 | **开工刷新 OpenClaw 架构（先读决策再评估）** | 先读 `docs/config.md` 中现有的升级 hold 决策和版本状态，再查 OpenClaw 最新 release；如已有明确 hold 决策且上游无新版本，跳过重复评估；有新版本时对比决策条件是否变化，变化则重新评估，否则沿用。**禁止"上游已修复"就改代码——必须确认本地已部署该版本**（#48703教训） |
| 2 | **开工先读 config** | 读 `docs/config.md` 获取系统状态 + 踩坑记录，避免重复犯错 |
| 3 | **开工先读/收工必写 status.json** | `python3 status_update.py --read --human` 查看三方共享状态（优先级、反馈、系统健康）；收工时更新 priorities + recent_changes：`python3 status_update.py --add recent_changes '{"date":"...","what":"...","by":"claude_code"}' --by claude_code` |
| 4 | **改完先测** | 新脚本手动验证 → 新任务先写 `jobs_registry.yaml` 并 `python3 check_registry.py` 通过 → 才能注册 cron |
| 5 | **push前必扫描** | 安全扫描（见上方命令）全部为空才允许 push |
| 6 | **新功能必须 Mac Mini E2E 验证** | dev 环境单测通过不算完成；**必须提醒用户在 Mac Mini 上运行 `bash preflight_check.sh --full` + `bash job_smoke_test.sh` + 手动触发目标 job**，确认端到端有效果（消息到达 WhatsApp / 文件生成 / 日志正常）。dev 通过 ≠ 生产工作。 |
| 7 | **故障先查自身代码** | 排查问题时默认从我们自己的代码和架构中找 bug（shell 数据传递、cron 环境、进程管理等），不归因于上游服务不稳定（#97教训） |
| 8 | **做减法不做加法**（→ #34 日落法战略升级版） | 新增防护/监控前先问"谁已经在管这件事"；每加一层保险 = 多一个故障源（#95教训）。**2026-06-05 升级**：这条的战略化北极星是 #34 日落法——不只"别加"，是"加之前先问能否退役"。 |
| 9 | **🔴 收工必须执行完整检查清单（不可跳过）** | "结束今天的工作"时，**必须逐项执行以下清单，每项完成后打勾确认，禁止跳过或合并**：**A. 全量回归** `bash full_regression.sh`（必须 0 fail，完成后自动更新 status.json test_count）**B. 安全评分** `python3 security_score.py --update` **C. 安全扫描** API key + 手机号 + BSA 泄漏扫描 **D. 治理审计** `python3 ontology/governance_checker.py` **E. 文档刷新**（逐一 check）：`status.json`（recent_changes/session_context/focus）→ `CLAUDE.md`（版本/文件表/changelog/待办交叉校验）→ `docs/config.md`（如有配置变更）→ `README.md`（如有架构变更）→ `SOUL.md`（如有 PA 行为变更）→ `docs/ontology/`（如有 ontology 变更）**F. 交叉校验** 原则#17：commits vs CLAUDE.md 待办一致性 **G. 全部提交推送** **H. 提醒 Mac Mini 验证** `preflight_check.sh --full` + `job_smoke_test.sh` — **不是"提醒"而是必须等用户执行并确认结果** **I. 遗留问题登记** 未完成事项写入 session_context.unfinished。**J. 复杂度预算（V37.9.150）** changelog "验证"字段照常报账本增量（suites/tests/inv/MR/checks/VERSION）；**额外**：后四维度若变化显式记一行（files/env/jobs/runtime-state-sources 的 +/-），并对每个净新增*机制*（非证据/文档）自问"退役了一个等价旧机制吗？还是真加价值的新能力而非又一道接缝？"——VERSION 不变大概率=证据刷新（免费），VERSION bump 须解释退役了什么。**刻意无守卫测试**（MR-22 示范：用新 check 守护复杂度约定本身就是新复杂度），靠 changelog 留痕 + 收工自问执行。详见 `docs/complexity_budget.md`。（2026-04-08教训：长 session 后"赶紧结束"心态导致跳过 6 项检查，用户两次追问才补齐） |
| 10 | **相信 OpenClaw，用好 OpenClaw** | 优先利用 OpenClaw 已有能力（Multi-Agent、contextPruning、workspace SOUL.md/CLAUDE.md、memory、sessions_spawn 等），而非重新造轮子；遇到新需求先查 OpenClaw 文档和 release notes |
| 11 | **🆕 结果验证优先于功能建设** | 先定义"从用户视角，成功长什么样"，再写代码。status.json 的成功标准不是"能写入"，而是"PA 能正确回答项目进展"。（2026-03-28教训：393个单测通过但 PA 说"没有项目"） |
| 12 | **🆕 上下文工程是一等公民** | SOUL.md = 宪法级（身份+关键状态，LLM 注意力最高），CLAUDE.md = 手册级（工具+详情）；信息放哪里、占多少 token、LLM 能否注意到——都是架构决策，和 API 设计同等严肃。（2026-03-28教训：SOUL.md 空置数月，17KB CLAUDE.md 信息被"lost in the middle"） |
| 13 | **🆕 定期像用户一样使用系统** | 不是跑单测，而是在 WhatsApp 上实际问 PA 问题。每次涉及 PA 行为的变更后，必须清空 session（`echo '{"sessions":[]}' > sessions.json`）+ 重启 Gateway + WhatsApp 实测。单测验证组件内部，系统价值在组件之间的接缝处。 |
| 14 | **🔴 合并 PR 后下一步首先同步 Mac Mini (主动给命令不等用户问)** | **这是卡点，不是提醒。** GitHub PR 合并到 main 后，**下一步必须是 Mac Mini 同步，不做任何其他操作**。同步命令：`cd ~/openclaw-model-bridge && git fetch origin main && git reset --hard origin/main`。同步后立即验证：`bash preflight_check.sh --full`。**不要在 Mac Mini 上测试未同步的代码**——多次返工都是因为合并后没同步就直接跑旧代码。不要等 auto_deploy 轮询。**🔴 执行机制 (2026-05-13 升级)**：每次 push commit 到 PR 分支后，或检测到 PR 合并 (用户说"已合并"/"merged"/`git log` 看到 main 有新 merge commit) 时，**Claude 必须立即主动给出同步命令 + 验证命令** (上方两条 bash 块)，不等用户问"是否需要同步". 这是原则 #29 "零遗漏" 的具体兑现——同步是已知必须的下一步，不需要每次用户提醒。**禁止反模式**: 回复"请用户决定下一步" 或 "工作已完成, 等用户合并" 而不附同步命令 (用户合并后会再问"需要同步吗?" 是这个反模式的症状). （2026-04-01教训：合并后 preflight 8 项失败全是部署漂移；2026-04-06教训：合并后直接测试跑的是旧代码，浪费时间排查不存在的 bug；2026-05-13教训：V37.9.62 doc commit f70503a push 后未主动提示同步, 用户合并 PR #547 后必须再问"需要同步吗?", 才提示同步命令——证明 #14 执行不彻底, 升级为主动给指令）|
| 15 | **🆕 测试必须全量：单测 + full_regression + WhatsApp 业务验证** | 每次变更后测试三层缺一不可：① `bash full_regression.sh`（394 单测 + 注册表 + 安全扫描 + 代码质量）② `bash preflight_check.sh --full` + `bash job_smoke_test.sh`（Mac Mini 部署验证）③ **WhatsApp 端实际业务测试**（用户视角发消息验证 PA 回复、search_kb 检索、图片理解等核心功能）。只跑单测不算测完——单测验证组件，WhatsApp 验证系统。（2026-04-01教训：394 单测全过但 preflight 8 项失败） |
| 16 | **🆕 所有推送必须经 notify.sh（统一多通道）** | 新增或修改任何消息推送时，**必须经 `notify.sh`** 统一推送（`source notify.sh && notify "msg" --topic X`）+ 重试 3 次 + 失败队列。**禁止直接 `openclaw message send`** 做内容/告警主推。**🔴 V37.9.179 通道现实（contextToken 根因）**：**定时推送主通道 = Discord**（当前唯一可靠）；`notify.sh` 默认 `NOTIFY_CHANNELS=discord`。**openclaw-weixin（微信客服通道）对无人值守 cron 不可靠，不做内容主推**——它是客服/对话通道：CLI 发起的 outbound 无 contextToken，**仅在微信 48h 客服窗口内可投递（窗口外静默丢失）**，窗口靠用户近 48h 内有交互维持（`✅ Sent`/`rc=0` 是发送侧假成功，不代表投递）。**V37.9.182 修正 V37.9.179「永远做不了/架构上不可能」的过头结论**（Mac Mini 日志铁证：无 token 的 deep_dive cron outbound 在窗口开时仍投递成功）；操作结论不变——微信只做交互问答 + 窗口开时尽力投递，不做 cron 内容主推。**WhatsApp 408 缓解后恢复** = `.env_shared` 设 `NOTIFY_CHANNELS="whatsapp,discord"`（无 contextToken 契约，CLI 可直投）。审计：`grep -rn "message send --channel" jobs/ *.sh *.py` 命中应**仅剩 fallback 分支**（`if command -v notify; then notify; else 直发兜底`）或 notify.sh 内部，主推一律 `notify --topic X`（topic→通道映射见 notify.sh：papers/tech/freight/daily/deep_dive/alerts/ontology）。**验证推送以用户端真实看到为准，不看发送侧 rc/✅Sent**（原则 #11/#13）。（2026-04-03 货代遗漏 Discord；**2026-06-18 V37.9.170-178 血案**：WhatsApp 禁用 → 17+ job 硬编码 whatsapp 主推静默失败 → Path B 全收编 notify.sh；**V37.9.179 血案**：误把微信当推送通道，contextToken 根因致 3h 零投递，rc=0/✅Sent 假成功掩盖——详见 `ontology/docs/cases/weixin_contexttoken_push_blocked_case.md`） |
| 17 | **🆕 收工必须交叉校验待办状态** | 收工时不仅更新 `status.json`，还必须**扫描 CLAUDE.md 待办列表**，对照本次 session 的 commits 和 recent_changes，将已实现的任务标记 ✅。实现代码 + 更新待办 = 一个完整的交付，缺一不可。同时检查：① CLAUDE.md 待办 vs 实际代码一致 ② status.json priorities vs CLAUDE.md 待办一致 ③ 版本号/文件表/常用命令是否需要同步更新。（2026-04-03教训：V32 实现了 7 个 P0+P1 任务但 CLAUDE.md 全部未标记，直到 V33 审计才发现） |
| 18 | **🆕 补证据而非补功能** | 下一阶段最该补的不是功能，而是**可对外复述的证据链**：A.兼容性矩阵（provider/模型/模态/工具模式验证 matrix+checklist）B.性能/SLO 实验结果（延迟/成功率/降级恢复时间）C.运维韧性证据（故障注入+恢复时间统计）D.可复现证据（一键启动+demo transcript）。新增功能前先问"这能产出什么证据？"（2026-04-03导师评审：系统已有但证据密度不足） |
| 19 | **🆕 纵向做深不横向铺开** | 沿 `providers.py` 已证明的方向继续放大，不轻易开新战线。每个改动必须对应 V1/V2/V3 路标中的具体目标：V1=别人能跑，V2=别人敢用，V3=别人会扩展。对照 `docs/strategic_review_20260403.md` 和 status.json 路标检查。偏离路标的功能需要明确理由。（2026-04-03导师建议：不是再做更多功能，而是把已有能力做成证据链） |
| 20 | **🆕 话语权输出是一等公民** | 代码只是第一步，真正的顶级专家把代码、文档、评测、方法论、复盘文章串成完整叙事。每个 milestone 完成后考虑：能否产出一篇架构型/证据型/立场型文章？README 里的方法论要持续扩写成观点体系。（2026-04-03导师建议：建立"话语权上层建筑"） |
| 21 | **🆕 对抗审计：问"什么坏了我们发现不了"（含治理自身）** | 每月至少一次 adversarial review：不问"检查了什么"，而问"**什么东西坏了我们会发现不了？**"。**治理系统自己也在这个问题的范围里**——MR-7 (governance-execution-is-self-observable) 要求 check 层所有状态（pass/fail/skip/error）必须在汇总层有观察路径。治理审计（`ontology/governance_checker.py`，每日 07:00 自动执行+失败告警）防止已知漏洞回归；人工对抗思维发现新维度盲区。每发现一个"没人会发现"的答案，就转化为 `governance_ontology.yaml` 的新不变式（ontology-native，不用硬编码）。检查体系最危险的漏洞不是某个检查没写好，而是某个维度从未被纳入检查。（2026-04-09：adversarial_audit.py 9个检查合并入 governance_ontology.yaml；2026-04-11 升级：V37.3 MR-7 治理自观察 + INV-GOV-001 summary 不吞 error，23 不变式 / 7 元规则 / 6 元发现，首次实现"治理系统治理自己"） |
| 22 | **🆕 顺势设计：适配模型行为，不对抗** | LLM 工具使用服从训练分布，不服从"应该"。通用工具（write/read/exec）自然使用，专用工具（memory/sessions）需 SOUL.md 强制规则才触发。设计系统时**顺着模型的自然行为**：PA 自然用 write 写 MEMORY.md → 我们把 MEMORY.md 接入 KB 索引；PA 不会主动调 kb_write.sh → 我们在 proxy 层静默捕获对话。对抗模型倾向的设计必然失败。（2026-04-08教训：等了一个月让 Qwen3 调 memory_create，从未成功；改为顺势捕获后一天闭环） |
| 23 | **🆕 链式幻觉防范：LLM 链路中每一跳都会放大幻觉** | 多个 LLM 共享上下文时，一个 LLM 的幻觉会被下游 LLM 当作事实执行。Dream LLM 编造文件名 → PA LLM 尝试读取 → 失败。防范方法：① 讨论密度必须等于文档密度（KB 里高频讨论的主题必须有对应文档，否则 LLM 会补全出不存在的文件名）② LLM 生成的行动建议不能直接执行，需要 grounding 检查（文件是否存在、工具是否可用）③ 上下文中提供明确的文件清单，让 LLM 知道"有什么"和"没有什么"。（2026-04-08教训：industrial_ai_paradigm.md 幻觉链——Dream 生成→PA 执行→文件不存在） |
| 24 | **🆕 SOUL.md 触发词是唯一可靠的工具调用机制** | 当前 Qwen3 不会自主决定调用专用工具。唯一可靠的强制方式是 SOUL.md 的"遇到X必须调Y"规则+具体触发词列表。ops agent 的 sessions_spawn 成功不是因为 Qwen3 学会了，而是触发词"排查/超时/告警"命中了 SOUL.md 硬规则。新增专用工具时，必须同步更新 SOUL.md 触发词规则，否则工具永远不会被调用。（2026-04-08教训：memory 工具上线数周零调用，ops spawn 靠触发词 100% 成功） |
| 25 | **🆕 对话数据是最高质量信号源** | 用户与 PA 的对话包含决策、偏好、专业洞察、领域判断——这些是 cron 抓取的论文/新闻无法替代的一手数据。必须确保对话数据被捕获并进入 KB 索引（`tool_proxy.py` 热路径捕获 → `kb_harvest_chat.py` 冷路径提炼 → KB notes）。同时关注 PA 自主写入的文件（如 `MEMORY.md`），将其纳入索引范围。数据流失 = 系统失忆。（2026-04-08教训：240 条 KB notes 全是机器抓取，零对话数据；PA 自主写入的 MEMORY.md 也是孤岛） |
| 26 | **🔴 异常分析宪法：必须输出完整因果链架构图** | 见下方独立章节"异常分析宪法"。（2026-04-10 升级为宪法级） |
| 27 | **🆕 X/Twitter 是高质量实时数据的第一选择（但账号健康 ≠ HTTP 200）** | 当 RSS 源失效（官方取消、CDN 封锁、RSSHub 公共实例 403）时，**X/Twitter Syndication API 是最可靠的替代通道**——无需认证、零成本、覆盖全球权威机构官方账号。Twitter Syndication API (`syndication.twitter.com/srv/timeline-profile/screen-name/{handle}`) 返回 HTML 内含 `__NEXT_DATA__` JSON，可解析出完整推文内容+时间+ID。新增数据抓取任务时，优先评估目标机构的 X 账号是否活跃，再考虑 RSS/API。实践证明：14 个财经 X 账号 100% 成功率 vs 10 个 RSSHub 路由 100% 失败率。**🔴 V37.8.4 血案补充：X 账号健康 ≠ HTTP 200**——Syndication API 对已停更账号返回 stale 快照（HTTP 200 + 可解析 HTML + 推文数据，但最新推文可能是几百天甚至几年前），embed-disabled 账号则返回 2KB 空 stub。**账号健康必须通过最新推文时间戳验证**（`newest_tweet_dt > now - N 天`），不能依赖 HTTP 状态码或 HTML 大小。新增账号前必须先抽查最新推文时间；生产脚本必须对"抓到内容但 0 条过时间窗"的账号打 ZOMBIE 嫌疑标记，连续多天命中触发告警。（2026-04-13 教训：finance_news 12/20 RSS 源失败；2026-04-14 教训：V37.8.3 修复把 3 个正常 handle 改名成僵尸账号——caixin/yicaichina/straits_times 分别停更 2227/3364/420 天，Reuters 253 天，BrookingsInst 585 天，WorldBank 返回 stub。这是 MR-10 violation：看到失败先猜原因改代码而不是先验证账号真实状态） |
| 28 | **🔴 理解再动手：修复前必答三问** | 看到报错/失败时，**禁止直接开始"修"**，必须先回答三个问题：**(1) 这个问题之前存在吗？** 如果之前不存在，那是我的改动引入的，不是系统本身的 bug。**(2) 是我的哪个改动引入的？** 精确定位因果链，不猜测。**(3) 最小修复方案是什么？** 不引入新概念、新模式、新复杂度。三个问题答不上来 = 还没理解问题，禁止动手。**每次"修复"都可能制造下一个 bug——连锁修复是最危险的系统破坏模式。**（2026-04-13教训：preflight 一个部署同步问题被误诊为"缺少配置"，5 轮连锁修复、4 层新复杂度、用户 Mac Mini 手动测试 5 次，才修好一个原本只需 `cp` 一下的问题） |
| 29 | **🔴 开工/收工清单逐项执行，零遗漏** | 开工和收工的检查清单**每一项都必须逐一执行并确认**，不允许"差不多就行"、不允许合并跳过、不允许因为 session 太长而"赶紧结束"。系统已达 951 测试 / 35 不变式 / 34 cron job 的复杂度，任何一项遗漏都可能造成连锁故障。**开工时**：逐项执行原则 #1~#3（读 config → 读 status.json → 检查 OpenClaw 版本），不跳过任何一步。**收工时**：原则 #9 的 A~I 九项清单逐一执行并打勾，每完成一项立即标记，禁止批量处理。如果某项因客观原因无法执行，必须**显式说明原因并登记到 session_context.unfinished**，而不是静默跳过。（2026-04-13教训：开工/收工流程逐渐敷衍，原则执行不彻底导致遗留问题积累；2026-04-08教训：长 session 后"赶紧结束"心态跳过 6 项检查） |
| 30 | **🔴🔴🔴 开工必须主动声明模型（第 0 号宪法执行机制）** | 每次新 session 开工的**第一条回复必须以模型声明开头**，独占一行，格式为：`🤖 本次 session 模型：Claude Opus <X.Y> (claude-opus-<x-y>) \| 最强级别，无降级 \| fast mode 状态见 /fast 命令`。`<X.Y>` 是当前实际运行的 Opus 版本号（4.6 / 4.7 / 未来更新），读取 `$0` 或环境注入信息确定。**用户无需询问也必须声明**，这是第 0 号宪法的自动执行机制，不是可选项。**合法模型家族 = Opus**（4.6/4.7/更新版本均合规）；如果运行时检测到模型**不是 Opus 家族**（即被错配到 Sonnet / Haiku 或其他），**立即停止工作，把错配告知用户，等待处理后再继续**。升级到更新的 Opus 版本（4.6 → 4.7 → …）自动合规无需告警——那是升级不是降级。禁止"先干活再说"的侥幸思维——弱模型写的代码会在后续造成连锁故障。声明位置：在任何寒暄、分析、工具调用之前。（2026-04-14教训：用户必须手动追问"是否最强模型"才能确认，声明应主动前置而非被动回答；2026-04-20升级：Opus 4.7 发布后原"硬编码 4.6"规则误把升级当降级，改为"Opus 家族"通配） |
| 31 | **🆕 跨消费者豁免必须全量同步** | 同一配置数据（如 FILE_MAP）被多个脚本独立消费时（preflight / auto_deploy / job_smoke_test），**任何豁免规则必须在所有消费者中同步落实**，不得只改一个。发现新豁免需求时，必须**系统性扫描所有消费者**并打包修复，禁止单点修复。（2026-04-15教训 V37.8.11：V37.8.1 给 preflight 加了 status.json 豁免但 auto_deploy 漏改，导致每小时 drift 告警噪声长期潜伏；2026-04-16 又因 FILE_MAP 自复制行触发 cp 非零 + set -e 杀脚本，是同一原理的第二次演出。**正确做法**：每次改 FILE_MAP 豁免前 `grep -l "FILE_MAP\|for mapping" ` 找出所有消费者 → 打包一起改 → 加单测守卫跨文件一致性。）|
| 32 | **🆕 每周用户视角观察制度（30min）** | 每周固定时段（建议周一早）做一次**纯用户视角**的系统观察，**不写代码不修 bug**，专门看四个维度：① **告警噪声**（WhatsApp + Discord #告警 频道里告警频率/重复度/可读性）② **推送延迟**（cron job 推送到达手机的延时分布）③ **信息密度**（推送内容是否真有用 / 有没有水货）④ **用户感知**（自己使用时 PA 是否答非所问 / 慢 / 错乱）。每发现一个问题立刻登记到 status.json unfinished 而不是当场修。这是原则 #13 (定期像用户用) 和 #15 (测试全量) 的运营层兑现 — 单测/治理只能验证组件正确，**用户感知质量只能用户视角观察**。（2026-04-15 V37.8.11 教训：drift 噪声告警长期存在但开发环境永远绿灯，用户半天 WhatsApp 观察就发现；2026-04-16 V37.8.13 教训：Gateway 宕 9h 静默，原因之一就是没人主动巡视双通道告警频道。**执行机制**：周一早开工时第一件事执行此观察，30min 时间盒，发现项写入 status.json `--add unfinished` + `recent_changes` 标记为 weekly_observation 类目，本周内择机修复。） |

| 33 | **🆕 信源可达性必须在部署环境验证，SPA 必查 API** | 判定一个网站"不可抓取"前，必须完成三步验证：**(1) Mac Mini curl 测试**（不是 dev 环境）— 带 User-Agent 请求首页，dev 环境的 403 可能只是 IP/地域限制 **(2) 读 HTML 源码找 API** — SPA 网站的后端 API 必然在 JS 里（如 chaspark `/chasiwu/v1/`），读 `<script>` 标签找 `fetch/XMLHttpRequest/makeRequest` 调用 **(3) 只有三步都失败，才考虑复杂方案**（Puppeteer/间接渠道/放弃）。**禁止在 dev 环境得到 403 就下"全站封锁"的结论。X 账号是僵尸 ≠ 该机构停止发布 — 检查其官网 RSS/API。**（2026-04-17 教训：chaspark.com 在 dev 环境 403，被误判为"全站封锁+SPA 不可抓取"，提议 Puppeteer 重方案。实际 Mac Mini 首页 200，HTML 源码里 API 明文写着，5 分钟就抓到 39 篇文章。同日审计发现 Brookings Atom + WorldBank JSON API 也可直达，此前从未尝试。） |
| 34 | **🔴 日落法（Sunset Law）：降低复杂度优先于增加功能（北极星）** | 2026-06-05 确立为项目未来发展北极星。**系统演进的默认动作是"退役/合并"，不是"新增"**。意外频发的真因不是"系统复杂"(部件难懂)，是"系统组合"(简单正确的部件，交互面积超线性增长超过测试覆盖)——**复杂关乎部件，意外关乎接缝**。三条操作规则：**(1) 加之前先问能否退役** — 每加一层(脚本/不变式/MR/框架/表示/路径)前必须先问"能否退役一个等价的现有机制"(原则 #8 的战略升级，不只是"别加"是"主动退役") **(2) 一物一形** — 同一逻辑事物只应有一个物理表示，多重表示(路径/文件/身份)必然漂移 bug 住在缝里(auto_deploy 两份文件两条 crontab 路径 / ~/ontology symlink vs repo 路径) **(3) 防御本身是事故面** — "再加一层治理"往往不是答案，"缩小接缝"才是(减少一物多形、缩小 dev-production 差距、让观察者只读)。**治理验证"代码符合规则"，验证不了"规则符合现实/部署符合假设"——87 不变式全在仓库内层验证仓库自己，事故住在仓库与真实 Mac Mini 的接缝里(无测试覆盖)。** 平衡视角：很多"意外"是免疫系统开火(暴露潜伏债)，零意外可能只是免疫系统瞎了(MOVESPEED 潜伏 60 天)。目标不是"零意外"，是"快速数据驱动根因诊断 + 持续退役复杂度"。**复杂度预算（V37.9.150 惯例化）= 规则 1 的操作化**：每个版本收工报账本增量 + 对净新增*机制*自问"退役了什么"，账本=既有徽章（零新机器，刻意无守卫，详见 `docs/complexity_budget.md` + 收工清单 J 项）。详见 `ontology/docs/cases/why_so_many_incidents_2026_06_05_reflection.md` + MR-22(sunset-over-accretion) + MR-23(audit-observes-never-mutates)。（2026-06-05 深度反思：一天五版意外频发，逐层剖析五根因——组合非复杂/地图≠领土/观察者改被观察者/不完整修复伪装完整/潜伏条件累积。） |
| 35 | **🔴 给用户的终端命令块禁止注释行（2026-06-11 用户最终指令）** | 任何贴给用户复制粘贴执行的 shell 命令块,**绝对不带 `#` 注释行、不带行内中文括号说明**——zsh `interactive_comments` 默认 OFF 把 `#` 当命令、把 `（(` 当 glob 限定符,已多次报错（2026-06-11 升级阶段 1 连炸两次 `command not found: #` + `unknown file attribute`；V37.9.56-hotfix2 / V37.9.32 同源 quirk 家族）。说明文字写在命令块**外面**的正文里；命令块内只有纯命令。多步操作拆成多个独立命令块,每块可整段粘贴零报错。这同样适用于写给用户的文档中的可执行示例块。 |

### 🔴 异常分析宪法（原则 #26 展开，无例外强制执行）

> **修代码之前先画图。画不清因果链 = 还没理解问题。**

遇到任何 bug/异常/故障，必须按以下流程输出，**不可跳过任何步骤**：

#### 步骤一：完整因果链架构图（强制，必须是第一个输出）

按**时间线 × 层级 × 逻辑 × 架构**四维度，画从上游触发到下游用户感知的全链路 ASCII 图：

```
HH:MM  [组件A] 事件描述
       │
       ├─ [组件B] 具体行为 → 具体错误码/现象
       ├─ [组件C] 因为B的错误 → 触发什么后果
       │
HH:MM  [组件D] 下游事件
       ├─ 调用链: 组件X(:端口) → 组件Y(:端口)
       ├─ 组件Y 尝试A → 失败（具体原因）
       ├─ 组件Y 尝试B → 失败（具体原因）
       ├─ 组件Y 返回错误码
       │
       ├─ [消费方] 收到错误 → 但未正确处理
       ├─ 具体代码逻辑: variable = "" ← 标注关键 bug
       ├─ 具体检查: if condition → TRUE/FALSE ← 标注为什么没拦住
       └─ [用户] 最终感知到的现象
```

**四维度要求**：
- **时间线**：左侧标注精确到分钟的时间戳
- **层级**：标注每个组件名称和端口（如 Adapter:5001、Proxy:5002）
- **逻辑**：标注关键代码分支的 TRUE/FALSE 走向和变量值
- **架构**：标注组件间调用关系和错误传播路径

#### 步骤二：三层根因（触发器 → 放大器 → 掩护者）

| 层级 | 问题 | 发现 |
|------|------|------|
| **触发器** | 什么外部事件引爆？ | （填写） |
| **放大器** | 什么架构缺陷让影响扩散？ | （填写） |
| **掩护者** | 什么缺失让问题被隐藏到用户发现？ | （填写） |

#### 步骤三：时间线还原表

| 时间 | 事件 | 影响 |
|------|------|------|
| HH:MM | ... | ... |

#### 步骤四：为什么以前没发生（条件组合分析）

| 条件 | 以前 | 现在 |
|------|------|------|
| 条件A | ... | ... |

必须找到**多条件组合**：哪些条件单独出现不会触发，组合才触发。

#### 步骤五：喂养本体工程

- 案例文档：写入 `ontology/docs/cases/`
- 提炼 governance 不变式（INV-xxx）加入 `governance_ontology.yaml`
- 更新相关原则（如有新认知）

**参考案例**：`ontology/docs/cases/dream_quota_blast_radius_case.md`

### 🟡 按需查阅（操作 & 架构参考）

<details>
<summary>展开查看完整原则列表（16条）</summary>

**操作类**
- **故障先回滚** — 线上故障 → `git checkout v26-snapshot` 恢复服务 → 再排查根因
- **收工全量同步** — "今天工作结束" → `bash preflight_check.sh` 全面体检 → 扫描全部文档同步当日变更 → 提交推送
- **每日文档刷新** — `CLAUDE.md` + `docs/config.md` + `docs/openclaw_architecture.md` 在开工/收工时强制 read → write
- **纯推理绕过Gateway** — 不需要工具的LLM任务直接 curl 调 API，禁止用 `openclaw agent`（#94）
- **macOS sed禁用OR语法** — `\|` 在 BSD sed 不支持，用 Python 替代
- **禁用交互式编辑器** — git merge 用 `--no-edit`，commit 用 `-m`
- **crontab 安全操作** — **严禁 `echo ... | crontab -`**（2026-03-25事故：清空全部 cron），必须用 `bash crontab_safe.sh add '<行>'`
- **分支合并由用户在GitHub操作** — 推送到 `claude/xxx` 分支 → 用户合并 PR → **⚠️ 合并后下一步首先同步 Mac Mini，不做任何其他操作**（`git fetch origin main && git reset --hard origin/main`）→ 同步后再测试（见必查 #14，2026-04-06 再次踩坑）
- **Mac Mini 同步用 reset 不用 pull** — `git fetch origin main && git reset --hard origin/main`（Mac Mini 是纯消费端，无本地 commit；`git pull` 会因历史 merge commit 导致分叉失败）
- **全量测试三层标准** — 单测通过 ≠ 测完，必须 full_regression + preflight + **WhatsApp 业务验证**（见必查 #15）

**战略类（导师建议 2026-04-03）**
- **不再是桥接器，是控制平面产品** — 项目已远超 "bridge"，实际是 agent runtime control plane。旗舰叙事：`OpenClaw Runtime Control Plane for Tool-Calling Agents`
- **沿此仓库继续长 12 个月** — 不轻易切换去做完全不同的大项目，把这个仓库做成第一性代表作
- **下次汇报准备 5 样东西** — 版本变化 + 1-2 个关键架构图 + benchmark 数据 + 一次真实故障案例 + 对下一阶段的取舍判断

**架构类**
- **进程管理单一主控** — Gateway 由 launchd 管理，禁止再加 cron watchdog（#95）
- **cron 脚本显式声明 PATH** — 首行 `export PATH="/opt/homebrew/bin:/opt/homebrew/sbin:$PATH"`
- **健康检查只检目标组件** — `curl localhost:18789`，不走完整 LLM 链路
- **`--thinking` 参数** — 合法值：`off, minimal, low, medium, high, adaptive`（禁止 `none`，#92）
- **工具数量 <= 12** — 超出导致模型混乱；每任务工具调用 <= 2次
- **双 cron 职责分工** — 确定性脚本用 system crontab；需 LLM 参与的用 openclaw cron
- **开发流程** — Claude Code 只推 `claude/` 分支，Mac Mini 只从 main 拉取，避免双向提交同一分支

</details>

## 系统定位：三平面架构

> 来源：2026-04-01 外部专业评审反馈 + 2026-04-03 导师战略复盘

```
控制平面（先做强 → 60%→90%）：治理、限流、降级、观测、审计
  → V34: Provider Compatibility Layer (providers.py) 已实现
能力平面（持续演进 → 80%）    ：模型路由、工具编排、多模态能力
  → V34: 兼容性矩阵 + 能力声明 (ProviderCapabilities) 已实现
记忆平面（长期投入 → 60%）    ：知识沉淀、冲突消解、可信度评分
  → V2 路标: Memory Plane v1 统一叙事（将 5 个散落组件统一）
```

**核心原则：控制平面先行。否则能力越强，系统越难控。**

### 导师确立的三个高价值模块（V34+）

| 模块 | 目标 | 当前基础 | 路标 |
|------|------|----------|------|
| **Provider Compatibility Layer** | auth/chat/tool-calling/multimodal/streaming/fallback 标准接口 | `providers.py`+`adapter.py` 重构完成 | **V1 (in progress)** |
| **Agent Reliability Bench** | 系统性可靠性评测（**17场景103检查**，V37.9.147 +10：provider宕机/tool timeout/malformed args/oversized/kb miss-hit/cron drift/state corruption + schema drift/streaming 中断/tool 超大/JSON 修复/全 fallback 失败/index stale/cron dup/config 损坏/DNS fail/long-context 截断质量） | `reliability_bench.py`+`gameday.sh` | **V2 (✅ V36 done, V37.9.147 扩展)** |
| **Memory Plane v1** | 4层统一接口（KB语义/多媒体/偏好/状态）+ query/context/stats + 优雅降级 | `memory_plane.py` + 5个底层组件 | **V2 (✅ V36 done)** |

## 当前待办（按导师 V1/V2/V3 路标组织）

> 来源：2026-04-03 导师战略复盘。路标 = 时间窗口 + 成功标准。详见 `docs/strategic_review_20260403.md`

### V1 路标（0-4 个月）：别人能跑

| 优先级 | 任务 | 状态 |
|--------|------|------|
| **V1-P0** | **Provider Compatibility Layer**：`providers.py` BaseProvider 抽象 + **7 个实现** + ProviderRegistry + 能力声明 + CLI 兼容性矩阵（V34 实现，V35+ 扩展至 7 provider） | ✅ V35+ 完成，7 provider 生产验证通过 |
| **V1-P0** | **兼容性矩阵 + SLO Benchmark 证据**：`docs/compatibility_matrix.md` + `slo_benchmark.py` + `docs/slo_benchmark_report.md`（5/5 PASS，p95=459ms） | ✅ V35 完成 |
| **V1-P1** | **一键启动 + 最小 demo + golden test trace**：`quickstart.sh` 4阶段（前置检查→启动→健康→demo），19/19 通过，`docs/golden_trace.json` | ✅ V35 完成 |
| **V1-P1** | **可复现证据**：Quick Start + golden trace + SLO benchmark report 均已入库 | ✅ V35 完成 |

### V2 路标（4-8 个月）：别人敢用

| 优先级 | 任务 | 状态 |
|--------|------|------|
| **V2-P0** | **Agent Reliability Bench**（导师建议模块二）：`reliability_bench.py` **17场景103检查**（V37.9.147 外部评审2 P2(b) +10 场景），mock-based可在dev运行，59单测，朝行业可引用测试集方向 | ✅ V36 完成→V37.9.147 扩展 |
| **V2-P0** | **Memory Plane v1 统一叙事**（导师建议模块三）：`memory_plane.py` 4层统一接口（KB语义/多媒体/偏好/状态）+ query/context/stats API + 45单测 + 架构文档 | ✅ V36 完成 |
| **V2-P1** | **运维韧性证据**：`docs/resilience_report.md` — 7场景故障注入实验 + Recovery Time 汇总 + Dev Bench vs Production GameDay 对比 + 5项改进建议 | ✅ V36 完成 |
| **V2-P1** | **SLO Dashboard**：`slo_dashboard.py` 历史快照追踪 + sparkline 趋势 + cron 定时采集 + 31 单测 | ✅ V36 完成 |
| **V2-P1** | **semver 版本治理**：`VERSION` 文件 + /health 端点暴露版本 + changelog 格式升级 | ✅ V36 完成 |
| **V2-P1** | **安全边界说明文档**：`docs/security_boundaries.md`（8节：认证/网络/输入验证/数据保护/LLM安全/运维/评分/已知风险） | ✅ V36 完成 |
| **V2-P1** | **Memory Plane v2**：跨层去重（filename+text）+ 置信度加权（4层权重+新鲜度衰减）+ 冲突消解（优先级vs偏好矛盾检测）+ 64单测 | ✅ V36 完成 |

### V3 路标（8-12 个月）：别人会扩展

| 优先级 | 任务 | 状态 |
|--------|------|------|
| **V3** | **Provider Plugin Interface**：YAML/Python 插件 + 合约验证 + 能力路由 + auto-discovery fallback chain + Extension Guide + 128 单测 | ✅ V37 完成 |
| **V3** | **Tool Policy Plugin + Memory Plane Plugin**：可插拔的工具策略和记忆平面扩展 | 待启动 |
| **V3** | **Job Template/Registry SDK + Extension Guide**：让别人能基于框架扩展 | 待启动 |

### 话语权输出（持续推进）

| 类型 | 任务 | 状态 |
|------|------|------|
| 架构型 | **Why Agent Systems Need a Control Plane** — `docs/articles/why_control_plane.md`（问题→三平面架构→7场景证据→5条教训→立场） | ✅ V36 完成 |
| 证据型 | **Benchmark Report** / **Failure Injection Report** / **Lessons from 461-test Regression** | 待写 |
| 立场型 | **为什么 agent 系统首先是治理问题** / **为什么 control plane 必须先于 capability plane** | 待写 |
| **学术论文** | **When Errors Become Narratives: A Longitudinal Taxonomy of Silent Failures in a Production LLM Agent Runtime** — ArXiv 论文（fail-plausible 概念 + 5 类 taxonomy + 22 postmortem 实证），`docs/paper/silent_failures_taxonomy/` | ✅ V37.9.139 第一稿完成，待用户决策 7 项（DECISIONS_NEEDED.md）后投稿 |
| 立场型 | **Why Enterprise AI Needs Ontology Before It Needs More Models** — Ontology+LLM+Agent 三角架构论述 | ✅ V36.2 完成（EN dev.to + ZH 知乎发布） |

### Ontology 子项目（终态架构：Semantic Control Plane）

> 终态架构文档：`ontology/docs/architecture/target_architecture.md`
> 迁移路径：Phase 2 Shadow（完成）→ Phase 3 渐进替换（✅ P0 已切换）→ Phase 4 完全推理 → Phase 5 `pip install ontology-engine`（终极目标）

#### Phase 2 已完成（Shadow 观察，V36.1-V37.1）

<details>
<summary>展开查看 17 项已完成</summary>

| 任务 | 版本 |
|------|------|
| Ontology KB 目录结构 + 初始文件（7 文件） | V36.1 |
| 论文监控加 ontology 关键词（ArXiv/S2/DBLP） | V36.1 |
| X 监控加 ontology 人物（Marcus/Leskovec/Witbrock/Palantir×3） | V36.1 |
| Ontology 独立子项目：engine.py+tool_ontology.yaml+diff.py+宪法+PoC+tests | V36.2 |
| Tool Ontology Engine：81条声明式规则 + 推理引擎 + 一致性校验 | V36.2 |
| Ontology 宪法：6条+最高条款+价值评估矩阵 | V36.2 |
| BFO/DOLCE/UFO 流派对比、Neuro-Symbolic、供应链本体 | V36.1 |
| 语义查询 PoC：从枚举到推理 | V36.2 |
| 特性开关 Phase 1：equivalence proof + 3模式 rollback | V36.2 |
| 验证深度三层模型：声明/运行时/效果 + MR-6 + MRD-LAYER-001 | V36.3 |
| classify_tool_call() 从属性推理 risk_level + policy_tags | V36.3 |
| Feature Flag Phase 2：shadow 模式 Mac Mini 生产上线 | V36.3 |
| 立场文章：Why Enterprise AI Needs Ontology（EN+ZH 发布） | V36.2 |
| Governance Ontology v3：17不变式+35检查+6元规则+执行引擎 | V37.1 |
| Phase 0 元规则自主发现：23 个未覆盖 job | V36.2 |
| 对抗审计 ontology-native 化 + 每日定时审计 + 效果层启动 | V37.1 |
| Ontology 专属信息源（4 RSS + 两层过滤 + LLM 摘要 + Discord） | V37.1 |

</details>

#### Phase 3: 渐进替换（近期，P0-P1）

| 优先级 | 任务 | 状态 | 说明 |
|--------|------|------|------|
| **P0** | **ONTOLOGY_MODE=on 切换**：引擎数据替代硬编码 | ✅ V37.8.14 完成 | 等价验证通过，249 测试，已切换为默认 |
| **P0** | **filter_tools() 改用引擎**：`ALLOWED_TOOLS` 等从引擎加载替代硬编码枚举 | ✅ V37.8.14 完成 | ONTOLOGY_MODE=on 时 proxy_filters 启动即从引擎加载（load-once 形态优于每请求 query，热路径零开销）；V37.9.241 doc 收敛时确认表行 stale |
| **P0** | **fix_tool_args() 改用引擎**：参数别名映射从引擎加载替代硬编码 | ✅ V37.8.14 完成 | 同上（mode=on 加载全部 7 集合含 `TOOL_PARAMS`/`CLEAN_SCHEMAS` = fix_tool_args 的映射源，proxy_filters L463-469 实证） |
| **P1** | **夜间阻止语义化**：从手动维护阻止列表改为 `infer("side_effects==true")` 自动覆盖 | 待启动 | Phase 3 标志性交付 |
| **P1** | **新工具只加 YAML**：推广 V37 Provider Plugin 模式到 Tool，新增工具零 Python 改动 | 待启动 | 需要 Tool Plugin YAML schema |
| **P1** | **元规则扩展**：MR-7 新策略必须有 shadow 观察期 / MR-8 概念变更触发影响分析 | 待启动 | 治理体系自我演进 |
| **P1** | 用本体论视角重新审视 OpenClaw（深度版，含工具语义本体实验） | 进行中 | `cases/openclaw_as_ontology.md` |

#### Phase 4: 完全推理（中期，P1-P2）

| 优先级 | 任务 | 状态 | 说明 |
|--------|------|------|------|
| **P1** | **domain_ontology.yaml**：六域概念模型（Actor/Tool/Resource/Task/Provider/Memory），概念间关系推理 | ✅ V37.9.9 骨架 + V37.9.12 wiring API (`find_by_domain`) | Layer 1 终态：从工具列表到领域模型 |
| **P1** | **policy_ontology.yaml**：策略声明式定义（静态+时序+路由三类策略统一） | ✅ V37.9.9 骨架 + V37.9.12 `evaluate_policy()` + V37.9.13 P2 context evaluator + 2 条 policy 切换 | Layer 2 终态部分兑现：static + 6 条 contextual/temporal 已 wire，剩 4 条待注册 |
| **P1** | **三阶段门控**：Pre-check（前置条件）→ Runtime Gate → Post-verify（后置验证）接入请求管线 | ✅ V37.9.15 shadow wiring 完成（`ontology/three_gate.py` + tool_proxy 两处调用点 + INV-GATE-001 14 checks + 66 新单测） | Layer 3 终态部分兑现：P3.1 (shadow→on 一周观察) / P3.2 (第三条 policy 切换) / P4 (enforcement 行为) 待启动 |
| **P2** | **contextual/temporal policy 的 context evaluator**：实现 hour_of_day / request-context matcher 让 `evaluate_policy().applicable` 对非 static policy 也返回真值 | ✅ V37.9.13 完成 — 6 个 matcher（quiet_hours / task_match / has_alert / has_image / need_fallback / data_clean_keywords）+ `_CONTEXT_EVALUATORS` dispatch + 三档 reason（needs_context / no_evaluator / evaluator_error） | 未注册 policy 走"可扩展路径"不崩溃 |
| **P2** | **第二条 policy 切换**：选 `max-tool-calls-per-task` 或 `max-request-body-size` 做第二次 wiring，复用 V37.9.12 `_resolve_*` 模式 | ✅ V37.9.13 完成 — `_resolve_max_tool_calls_per_task_limit()` 镜像 V37.9.12 5 档 safe-fallback + MR-8 copy-paste 防御源码守卫 | 验证 wiring 模式可扩展性 ✓ |
| **P2** | **审计带规则链**：每条审计记录包含 policy_evaluated + rule_chain + rationale | 待启动 | Layer 4 终态：从"做了什么"到"基于什么规则" |
| **P2** | **策略冲突检测**：策略间矛盾自动发现（如夜间阻止 vs 紧急通知） | 待启动 | 策略引擎高级能力 |
| **P2** | **影响分析工具**：`ontology.impact_analysis("修改 max_tools")` → 受影响策略/工具列表 | 待启动 | 变更安全保障 |
| **P2** | **效果层覆盖率 ≥ 60%**：30+ 不变式中至少 18 个有 L3 效果验证 | 待启动 | MR-9 元规则 |

#### Phase 5: 对外输出（长期，V3 路标对齐）— 终极目标：`pip install ontology-engine`

> **项目终极目标**：将引擎和规则解耦为二层结构，让任何 Agent Runtime 项目只需编写自己的 YAML 就能获得工具治理+语义查询+governance 审计能力。
>
> **Layer 1 — `ontology-engine` 通用 pip 包**（项目无关）：
> - Tool Ontology Engine：YAML → 白名单/schema/参数映射/别名解析/语义查询/分类
> - Governance Checker 框架：5 种 check 执行器 + 元规则自动发现 + 三档特性开关(off/shadow/on)
> - 策略推理引擎：`infer_policy_targets("side_effects==true")` 零硬编码
>
> **Layer 2 — 项目级 YAML 配置**（项目特定）：
> - `tool_ontology.yaml`：工具声明（名称/类别/副作用/参数/别名）
> - `governance_ontology.yaml`：治理规则（不变式/元规则/check 定义）
> - `providers.d/*.yaml`：Provider 插件
>
> **当前基础**：Phase 3 ONTOLOGY_MODE=on 已切换，52 不变式 100% job 覆盖，89 引擎单测。
> **瓶颈**：领域本体(Phase 4) + 引擎包化(Phase 5) 尚未开始。

| 优先级 | 任务 | 状态 | 说明 |
|--------|------|------|------|
| **P1** | **引擎包化**：`ontology-engine` 可独立 `pip install` 的通用治理引擎 | 待启动 | **终极目标核心交付物** |
| **P2** | **Tool Policy Plugin**：`tool_policy.yaml` 声明式工具策略扩展接口 | 待启动 | V3 路标对齐 |
| **P2** | **Memory Policy Plugin**：`memory_policy.yaml` 记忆平面策略扩展 | 待启动 | V3 路标对齐 |
| **P2** | **Ontology Extension Guide**：第三方基于 ontology 框架扩展的指南 | 待启动 | V3 路标对齐 |
| **P3** | 证据型文章：从 17 不变式到 52 的治理演进实战 | 待启动 | 话语权输出 |

### 现有功能任务（V1 稳定后继续推进）

| 优先级 | 任务 | 状态 |
|--------|------|------|
| 中 | **数据清洗 Phase 2**：三 Agent 架构（Profiler/Planner/Executor，用 `sessions_spawn`）、语义去重、自定义规则 | active |
| deferred | **PA 子 Agent 委派**：`sessions_spawn` + `sessions_send`。V35 PoC 完成：链路通（显式触发可用），但 Qwen3 隐式触发不可靠（优先从上下文回答）。等下一代模型 | deferred |
| deferred | **PA 长期记忆**：Qwen3 不主动调用 memory 工具，等模型升级后重新验证 | blocked |
| 低 | **可迁移性抽象**：去除个人/场景定制痕迹，抽象成别人也能迁移的框架（导师指出三块差距之一） | V2-V3 |
| 低 | **记忆系统分层**：短期/长期/任务，含冲突消解和可信度评分 → 纳入 Memory Plane v1 | V2 |
| 低 | **成本-质量-时延联动调度**：根据查询复杂度动态选择模型/参数组合 | V2+ |
| 低 | 知识图谱：AI 大模型领域知识图谱构建（需 6-12 个月数据积累） | V3 |
| 低 | 货代 Watcher V3：Bing News API 替代 GoogleNews | 按需 |
| 低 | 语音消息支持：WhatsApp 语音→STT→LLM 回复 | 按需 |
| 低 | **视频号内容转录分析**：firethinker 视频号监控 → STT 转录 → LLM 提炼 → KB 沉淀 | 按需 |
| 低 | KB 静态加密：status.json / index.json 使用 age/gpg 加密存盘 | 按需 |

### 已完成里程碑（V27-V35）

<details>
<summary>展开查看 25+ 项已完成任务</summary>

| 版本 | 任务 |
|------|------|
| V35 | **V1 路标完成** — SLO Benchmark 实验报告（5/5 PASS, p95=459ms）+ Quick Start 一键 demo（19/19）+ Golden Test Trace + Sub-agent PoC（链路通，deferred）+ 605 测试 |
| V34 | **Provider Compatibility Layer**：providers.py + 48 单测 + 兼容性矩阵 + adapter.py 重构 |
| V33 | SLO 最小集 + 阈值中心化 + 旅程级 E2E 进 CI + 故障快照机制 + Job 分层治理 + Fallback Matrix + 变更影响评估 + GameDay 故障演练 + Discord 双通道 + notify.sh + pip-audit |
| V32 | 配置中心化 + 变更审计 + config_loader.py |
| V30.5 | search_kb 混合检索 + 论文监控矩阵 5 源全覆盖 + DBLP |
| V30.4 | SOUL.md 激活 + 三方宪法闭环 + 方法论进化 |
| V30.3 | 数据清洗 Phase 1 + 自定义工具注入 + WhatsApp E2E |
| V30.2 | 安全评分体系 + 审计日志 + 持续安全机制 |
| V30.1 | status.json 实时刷新 + KB 完整性 + 全量回归 374→461 测试 |
| V30 | crontab 事故修复 + cron 安全三层保护 |
| V29.x | KB 搜索/RAG/趋势/MM 索引/Fallback/备份/Multi-Agent |
| V28.x | preflight 体检 + WhatsApp 保活 + CI + pre-commit |
| V27.x | Proxy 拆层 + 任务注册表 + auto_deploy + 回滚机制 |

</details>

## 远程连接（本机）

```bash
ssh bisdom@10.102.0.23       # 办公室内网（en1 Wi-Fi, V37.9.84+ DHCP 更新）
ssh bisdom@10.120.230.23     # ZeroTier（回家后）
```

## Git 仓库

```
git@github.com:bisdom-cell/openclaw-model-bridge.git
```
Remote 已改为 SSH（v25修复HTTPS认证失败）。
回滚标签：`v26-snapshot`（V27变更前的完整快照）。

### Git 分支规则
- 默认直接在 main 分支上开发提交，除非任务指令明确指定其他分支
- 即使 session 系统指令指定了 `claude/xxx` 分支，仍优先遵守本规则在 main 上开发
