# CLAUDE.md — openclaw-model-bridge 项目背景

> 每次新会话开始时自动读取。当前版本：v37.9.65 / 0.37.9.65（2026-05-13）

---

## 项目简介

将任意大模型（当前：Qwen3-235B + Qwen2.5-VL-72B）接入 OpenClaw（WhatsApp AI助手框架）的双层中间件，支持多模态（图片理解）。
运行于 Mac Mini (macOS)，用户：bisdom。

## 系统架构总览

```
┌─────────────────────────────────────────────────────────────────────┐
│                 用户层 (WhatsApp + Discord 双通道)                    │
└────────────────────────────┬────────────────────────────────────────┘
                             │
┌────────────────────────────▼────────────────────────────────────────┐
│  ① 核心数据通路（实时对话 + 多模态）                                  │
│                                                                     │
│  WhatsApp ←┐                                                        │
│  Discord  ←┼→ Gateway (:18789) ←→ Tool Proxy (:5002) ←→ Adapter (:5001) ←→ LLM (7 Providers) │
│  notify.sh ┘  [launchd管理]        [策略过滤+监控]       [认证+VL路由]     [Qwen3/GPT-4o/     │
│               [媒体存储]           [图片base64注入]      [Fallback降级]     Gemini/Claude/     │
│               [双通道推送]         [自定义工具注入]                         Kimi/MiniMax/GLM]  │
│                  │                [自定义工具注入]          │                               │
│                  │                    │                    │                               │
│                  │               /health ──→ /health       │                               │
│                  │               /stats (token监控)        │                               │
│                  │               /data_clean/* (REST)      │                               │
│                                                                     │
│  图片流程：Gateway存储jpg → Proxy检测<media:image> → base64注入       │
│           → Adapter检测image_url → 路由到Qwen2.5-VL → 图片理解回复    │
│  数据清洗：Gateway存储文件 → LLM调用data_clean工具 → Proxy拦截执行     │
│           → data_clean.py本地处理 → 格式化结果返回 → WhatsApp展示      │
└──────────────────┼────────────────────┼────────────────────┼────────────────────────┘
                   │                    │                    │
┌──────────────────▼────────────────────▼────────────────────▼────────────────────────┐
│  ② 定时任务层（system crontab，不经过 LLM 链路）                                     │
│                                                                                     │
│  每3h    ArXiv论文监控 ──→ KB写入 + WhatsApp+Discord推送                              │
│  每3h    HN热帖抓取 ──→ KB写入 + WhatsApp+Discord推送                                 │
│  每天×3  货代Watcher ──→ LLM分析(直接curl) + KB写入 + WhatsApp+Discord推送             │
│  每天    OpenClaw Releases ──→ LLM富摘要 + KB写入 + WhatsApp+Discord推送              │
│  每小时  Issues监控 ──→ KB写入 + WhatsApp+Discord推送                                 │
│  每天    KB晚间整理                                                                  │
│  每天    KB每日摘要 ──→ ~/.kb/daily_digest.md（LLM对话时可查）                          │
│  每周    KB跨笔记回顾 ──→ LLM深度分析 + WhatsApp+Discord推送                            │
│  每周    健康周报 ──→ WhatsApp+Discord推送                                             │
└─────────────────────────────────────────────────────────────────────────────────────┘
                                       │
┌──────────────────────────────────────▼──────────────────────────────────────────────┐
│  ③ 监控层（多级自动告警）                                                            │
│                                                                                     │
│  每30min  wa_keepalive ──→ 真实发送零宽字符 ──→ 失败则记录日志                         │
│  每小时   job_watchdog ──→ 检查所有job状态文件 + 日志扫描 ──→ 超时/失败→WhatsApp+Discord告警│
│  实时     proxy_stats ──→ token用量 + 连续错误计数 ──→ 阈值告警                       │
│  /health  三层健康端点：Gateway(:18789) → Proxy(:5002) → Adapter(:5001)              │
└─────────────────────────────────────────────────────────────────────────────────────┘
                                       │
┌──────────────────────────────────────▼──────────────────────────────────────────────┐
│  ④ DevOps层（自动部署 + 体检）                                                       │
│                                                                                     │
│  GitHub (main) ──→ auto_deploy.sh (每2min轮询)                                      │
│                     ├─ git fetch + pull                                              │
│                     ├─ 单测验证（proxy_filters变更时）                                 │
│                     ├─ 文件同步（仓库→运行时，31个文件映射）                            │
│                     ├─ 每小时漂移检测（md5全量比对）                                   │
│                     ├─ 按需restart（核心服务文件变更时）                                │
│                     └─ preflight_check.sh --full（部署后自动体检 11项）                │
│                         ├─ 单元测试 (proxy_filters + registry)                       │
│                         ├─ 注册表校验                                                │
│                         ├─ 文档漂移检测                                              │
│                         ├─ 脚本语法 + 权限检查                                       │
│                         ├─ Python语法检查                                            │
│                         ├─ 部署文件一致性（仓库 vs 运行时）                            │
│                         ├─ 环境变量检查（bash -lc 模拟cron）                          │
│                         ├─ 服务连通性（5001/5002/18789）                              │
│                         └─ 安全扫描（API key + 手机号泄漏）                           │
│                                                                                     │
│  开发流程：Claude Code → claude/分支 → GitHub PR → main → auto_deploy → Mac Mini     │
└─────────────────────────────────────────────────────────────────────────────────────┘
                                       │
┌──────────────────────────────────────▼──────────────────────────────────────────────┐
│  ⑤ 三方共享状态层（实时同步）                                                         │
│                                                                                     │
│  ┌──────────┐     ┌─────────────────────┐     ┌──────────────┐                      │
│  │ 用户      │     │ ~/.kb/status.json   │     │ Claude Code  │                      │
│  │ (WhatsApp)│────→│  priorities[]       │←────│ 开工: 读全部   │                      │
│  │ 反馈+决策 │ PA  │  feedback[]         │     │ 收工: 写变更   │                      │
│  │          │写入  │  recent_changes[]   │     │ 更新优先级     │                      │
│  └──────────┘     │  health{}           │     └──────────────┘                      │
│                   │  focus              │                                            │
│                   └────────┬────────────┘                                            │
│                            │                                                         │
│                   ┌────────┴────────────┐                                            │
│                   │ Cron 脚本自动更新     │                                            │
│                   │ auto_deploy → deploy │                                            │
│                   │ preflight → health   │                                            │
│                   │ kb_trend → trend     │                                            │
│                   └─────────────────────┘                                            │
│                                                                                     │
│  宪法：用户提供专业深度 + Claude Code 提供高效设计部署 + OpenClaw 提供数据复利            │
│        三者合一成为有生命的闭环系统                                                     │
└─────────────────────────────────────────────────────────────────────────────────────┘
```

### 核心组件详情

| 组件 | 端口 | 文件 | 功能 | 进程管理 |
|------|------|------|------|----------|
| OpenClaw Gateway | 18789 | npm全局安装 | WhatsApp接入、**媒体存储**、工具执行、会话管理 | launchd (KeepAlive) |
| Tool Proxy | 5002 | `tool_proxy.py` + `proxy_filters.py` | 工具过滤(24→12)、**图片base64注入**、**自定义工具注入+拦截执行**（data_clean、search_kb）、Schema简化、SSE转换、截断、token监控、`/data_clean/*` REST端点 | launchd plist |
| Adapter | 5001 | `adapter.py` | 多Provider转发、认证、**多模态路由**（文本→Qwen3，图片→Qwen2.5-VL）、Fallback降级 | launchd plist |
| LLM Providers (7) | — | `providers.py` | **7 Provider**：Qwen3-235B + VL-72B（主力）/ GPT-4o / Gemini 2.5 / Claude / Kimi / MiniMax / GLM | 外部服务 |

## 关键文件（本仓库）

| 文件 | 用途 |
|------|------|
| `tool_proxy.py` | HTTP 层（收发请求、日志、**自定义工具拦截执行**（data_clean本地执行+search_kb混合检索+followup LLM调用）、`/data_clean/*` REST端点） |
| `proxy_filters.py` | **V27新增→V37.4.3扩展** 策略层（过滤、修复、截断、SSE转换、**自定义工具注入**（data_clean+search_kb）、**V37.4.3 新增 `SYSTEM_ALERT_MARKER` + `filter_system_alerts()` 告警剥离函数**），纯函数无网络依赖 |
| `data_clean.py` | **V30.3新增** 数据清洗 CLI 工具（profile/execute/validate/history，7种操作，支持CSV/TSV/JSON/JSONL/Excel） |
| `test_data_clean.py` | **V30.3新增** 数据清洗单测（80个用例：格式检测/读写/操作/端到端/多格式） |
| `docs/archive/data_clean_poc/` | **V30.3新增→V35归档** Phase 0 验证材料（3个脏数据样本+LLM判断力测试脚本） |
| `SOUL.md` | **V30.4新增→V37.4.3升级** OpenClaw 最高优先级 system prompt（PA身份Wei、三方宪法、行为指令、**规则9批判性思考（反迎合+禁模糊关联+保存验证）**、**规则10告警消息不跟进（2026-04-11 血案规则，主题对齐硬规则+macOS FDA 幻觉防线）**、项目状态实时快照，每小时自动刷新） |
| `ops_soul.md` | **V31新增** Ops Agent 运维助手 SOUL.md（系统健康检查/日志排查/cron诊断/维护操作，部署到 `~/.openclaw/SOUL.md`） |
| `ops_health.sh` | **V31新增** Ops Agent 健康检查包装脚本（Qwen3 拒绝直接 curl localhost，通过脚本包装绕过） |
| `status.json` | **V30.4新增（仓库副本）** 三方共享意识锚点（priorities/feedback/incidents/quality/operating_rules/session_context），Mac Mini 每小时 git push 同步 |
| `providers.py` | **V34新增→V35+扩展** Provider Compatibility Layer（BaseProvider 抽象 + **7 个实现**（Qwen/GPT-4o/Gemini/Claude/Kimi/MiniMax/GLM）+ ProviderRegistry 动态注册 + 能力声明 + CLI 兼容性矩阵输出） |
| `test_providers.py` | **V34新增** providers.py 单测（48个用例：能力声明/模型查找/认证头/注册表/向后兼容/CLI输出） |
| `adapter.py` | API适配层（认证 `$REMOTE_API_KEY`，Fallback降级 `$FALLBACK_PROVIDER`）。V34: 从 `providers.py` 导入 PROVIDERS |
| `docs/strategic_review_20260403.md` | **V34新增** 导师战略复盘文档（Stage判断/主战场定位/V1-V3路标/三个高价值模块/话语权输出/三块差距） |
| `docs/compatibility_matrix.md` | **V34新增** Provider 兼容性矩阵（验证状态/降级路径/添加新 Provider 指南） |
| `slo_benchmark.py` | **V35新增** SLO Benchmark 报告生成器（读取 proxy_stats.json 真实数据→Markdown/JSON 报告：延迟p50/p95/p99、成功率、错误分类、降级率、恢复率） |
| `test_slo_benchmark.py` | **V35新增** SLO Benchmark 单测（17个用例：全通过/各类违规/零请求/格式化/文件读取） |
| `quickstart.sh` | **V35新增** 一键 Quick Start（4阶段：前置检查→启动服务→健康验证→Golden Test Trace，10分钟跑通全栈） |
| `docs/golden_trace.json` | **V35新增** Golden Test Trace（quickstart.sh 生成的真实请求/响应/延迟记录，可复现证据） |
| `docs/slo_benchmark_report.md` | **V35新增** 首份 SLO Benchmark 实验报告（5/5 PASS，p95=459ms） |
| `openclaw_backup.sh` | **V29.1新增** 每日Gateway state备份到外挂SSD（保留7天） |
| `jobs_registry.yaml` | **V27新增** 统一任务注册表（system + openclaw 双 cron） |
| `check_registry.py` | **V27新增** 注册表校验脚本 |
| `ROLLBACK.md` | **V27新增** 回滚指南（30秒恢复到V26） |
| `upgrade_openclaw.sh` | Gateway升级SOP脚本（必须SSH直连执行，禁止WhatsApp触发） |
| `restart.sh` | 一键重启 Proxy + Adapter + Gateway（含 PATH 修复，可在 cron 环境使用）。**V37.9.13 架构清理**：Adapter + Proxy 改用 `launchctl kickstart -k` 走 launchd 管理，消除 V37.9.12.1 双管理血案；plist 缺失时 fallback 到 nohup；V37.8.13 Gateway 健康验证模式扩展到 adapter/proxy（5×2s curl /health 探测） |
| `health_check.sh` | 每周健康周报脚本（V27: +JSON输出） |
| `kb_write.sh` | KB写入脚本（含目录锁+原子写） |
| `kb_review.sh` | **V29升级→V37.5 重写** KB跨笔记回顾 thin wrapper（调用 kb_review_collect.py，fail-fast + [SYSTEM_ALERT]，不再机械 fallback） |
| `kb_review_collect.py` | **V37.5新增** KB 周度回顾数据采集器 + LLM 调用（纯 Python 模块：load_sources_from_registry 从 jobs_registry.yaml 读源 + extract_recent_sections H2 drill-down + call_llm 80-char 最小内容阈值 + run() 接受 llm_caller 注入可单测） |
| `test_kb_review.py` | **V37.5新增** kb_review_collect 单测（44 个用例：registry 发现 / H2 parser 边界 / collect_notes 时间过滤 / call_llm 空短失败 / run() 失败不伪装成功 / 输出契约 / shell 源文件锁） |
| `kb_deep_dive.py` | **V37.9.16新增→V37.9.17 picker tier-aware fallback** 每日深度分析 collector（纯 Python 模块：`import kb_review_collect as rc` 复用 load_sources_from_registry + extract_recent_sections + call_llm；picker 算法 ⭐≥4 门槛 + TOPIC_WEIGHTS 主题加权 + 摘要长度 tie-breaker + **V37.9.17 方案 C tier-aware fallback**（TIER 1/2 优先于 TIER 3，避免 X tweet ⭐5 挤掉 arxiv ⭐4 走 abstract_only）；三档 fetch：一档 arxiv/acl/hf/pwc/s2/dblp PDF（pdfplumber lazy import + References/Acknowledgments 切割 + 图表 caption 去噪 + 30K 截断）/ 二档 rss_blogs/ontology_sources/github_trending HTML（bs4 lazy import + article/main 标签优先）/ 三档 degrade；双 prompt 分支 build_full_text_prompt（论证链+实验对比+局限性）vs build_abstract_only_prompt（禁推测+摘要级标注），两者都含 GROUNDING_CONSTRAINT；fail-fast run() 返回 status: ok/no_candidates/llm_failed/collector_failed） |
| `kb_deep_dive.sh` | **V37.9.16新增** 每日深度分析 thin wrapper（镜像 kb_review.sh V37.5.1 env-var heredoc 模式）：set -euo pipefail + notify.sh source + `--topic deep_dive` 双通道推送（WhatsApp 简版 + Discord #daily 完整版，内容不同）+ `[SYSTEM_ALERT]` fail-fast + last_run_deep_dive.json status 写入 + rsync 备份带事故取证（V37.9.14 INV-BACKUP-001 契约）+ no_candidates 告警但不 exit 1（允许明日再试） |
| `test_kb_deep_dive.py` | **V37.9.16新增→V37.9.17 +6 tier fallback** kb_deep_dive 单测（61 个用例 / 11 类）：TestClassifyTier(7) + TestParseEntriesFromSection(6) + TestScoreEntry(6) + **TestPickTopTierFallback(6) — V37.9.17 方案 C 守卫含 `test_v37_9_16_blood_lesson_scenario` 直接复现 X tweet ⭐5 + arxiv ⭐4 必选 arxiv** + TestPdfUrlDerivation(5) + TestPreprocessPdfText(5) + TestFetchDegrade(3) + TestPromptBuilders(4) + TestOutputBuilders(5) + TestRunOrchestrator(5 端到端：no_candidates/ok full_text/llm_failed 不产出 artifacts/degrade abstract_only/collector_failed) + TestKbDeepDiveShellGuards(9 源码级守卫：[SYSTEM_ALERT]/env-var heredoc 反模式/notify.sh 引用/set -euo pipefail/last_run/deep_dive topic/rsync forensics/fail-fast 顺序锁) |
| `cross_source_signal_aggregator.py` | **V37.9.46新增** Opportunity Radar Stage 1 PoC — 跨 source 弱信号聚合器（纯 Python，6 核心函数：scan_today_notes/embed_notes/cluster_dbscan/filter_cross_source/rank_signals/emit_radar_json + run/main CLI）。**算法管道**：扫今日 ~/.kb/notes/*.md → sentence-transformer 384 维 embedding（lazy import local_embed + numpy）→ DBSCAN 聚类（min_samples=3 / eps=0.35 / metric=cosine, lazy import sklearn）→ filter_cross_source（unique_sources≥2 跨源契约）→ rank_signals（score=source_count×2+note_count+avg_intra_similarity×5, top 10）→ emit ~/.kb/radar/daily_signals_{date}.json。**FAIL-OPEN 契约**（V37.4 同款 lazy import）：dev 无 numpy/sklearn/sentence-transformers 时模块仍可 import + 测试可跑（mock 隔离），run() 缺依赖 → status=fail_open_no_deps + 写空 signals 不阻塞下游。**Cache 策略**：content_hash 16 字符 cache key（V37.4 mtime-stable 同款），命中跳过 sentence-transformer 重算。**MR-11 兑现**：log() 写 stderr 防 $(...) 命令替换污染。**suggested_topic Stage 1 简化版**：cluster 内 titles 最高频关键词（中英文 regex），Stage 4 升级 LLM 一句话总结。**设计文档** docs/opportunity_radar_design.md 第三节锁定常量与算法。 |
| `kb_trend_acceleration.py` | **V37.9.48新增** Opportunity Radar Stage 3 — 趋势加速检测（设计文档 5 节）。5 函数：extract_keywords_per_week (复用 kb_trend.py extract_period_text + extract_keywords, lazy import + FAIL-OPEN)、compute_acceleration (输入 dict {1: w1_kw, 2: w2_kw, 3: w3_kw} 输出加速度 metrics — 用各周占比 pct 而非原始 freq 防总数差距失真 + w2=0 新词 skip + min_freq_pct=0.005 小基数门槛过滤噪声)、classify (5 档锁定 — 🚀 strong: a1≥1.5 AND a2≥1.5 / ⚰️ obs: a1<0.5 AND a2<0.5 / 📈 mild: a1≥1.5 / 💧 decel: a1<0.7 / 📊 stable: 其他 / 顺序锁定 strong > obs > mild > decel > stable / 非数字输入安全降级 STABLE)、rank_signals (按 archetype priority 分桶 + 同桶按 \|a1-1\| desc 排 / top_strong=10 strong+mild 共 / top_stable=5 / top_obs=3 / 总 max 18 signals)、emit_radar_json (~/.kb/radar/weekly_trends_{week}.json + archetype_summary 计数). **不集成 kb_dream/kb_evening** (Stage 4 才集成). **向后兼容**: kb_trend.py 旧接口完全不动. **设计文档 11.2 风险表登记**: 趋势加速度噪声大 (小基数) → MIN_FREQ_PCT 门槛防 0.5% 占比以下噪声参与计算. CLI: `--kb-dir X --output-dir Y --json`. |
| `test_kb_trend_acceleration.py` | **V37.9.48新增** Stage 3 PoC 28 单测（7 测试类）：TestExtractKeywordsPerWeek(4 含 invalid week_offset / dict 类型契约 / FAIL-OPEN 缺 kb_trend / 日期范围注入) + TestComputeAcceleration(5 含 3 周历史正常 / w2=0 新词 skip / w3=0 → accel_2w=None / min_freq_pct 噪声门槛 / 空输入安全) + TestClassify(6 含 strong 必须 a1+a2 / obs 必须 a1+a2 / mild 仅要 a1 / decel 仅要 a1 / stable 中间区 / 非数字 fallback STABLE) + TestRankSignals(4 含 archetype 优先级 strong>mild>decel>obs>stable / top_k 截断 per bucket / 空输入 / 桶内 \|a1-1\| desc) + TestEmitJson(3 含 JSON 完整性 + archetype_summary / weekly_trends_{week}.json 路径 / 嵌套自动 mkdir) + TestBackwardCompat(3 含 kb_trend.extract_period_text/tokenize/extract_keywords 旧接口仍 callable 不 break) + TestSourceLevelGuards(3 V37.9.48 marker / 5 档阈值锁定 1.5/0.7/0.5 + 5 archetype 常量 + 5 emoji / log() file=sys.stderr MR-11). **反向验证守卫真有效**: sed `ACCEL_STRONG_THRESHOLD = 1.5` → `0.5` → test_design_locked_thresholds 立即 AssertionError. 还原后 28/28 全过. |
| `project_alignment_scorer.py` | **V37.9.47新增** Opportunity Radar Stage 2 — 项目对齐度评分 rule_check 验证层（设计文档 4 节）。7 函数：load_project_concepts (lazy YAML parse + 行尾注释剥离 V37.9.47 修)、count_keyword_hits (大小写不敏感 + 中英 substring + dedup + excluded_topics weight × -2 强降权)、compute_expected_range (5 档锁定: <0/0/1-2/3-4/5+ → ⭐1-2/2-3/3-4/4-5)、validate_alignment_score (LLM ⭐ 数 vs rule keyword 一致性 + 偏高/偏低 reason 解释)、parse_6field_output (MR-8 与 hf_papers.sh inline 行为一致 — 仅 📌 字段 inline 提取, 其他字段 LLM 多行格式)、extract_star_count (取最大连续 ⭐ 段)、format_validation_marker (validated=False 时返回 ⚠️ <reason>)。**集成 hf_papers.sh emit-stage**: lazy import + load_project_concepts 一次, 每 paper alignment 段后调 validate_alignment_score(title+tldr+abstract, llm_stars), 不一致时推送加 ⚠️ 标记前缀给用户. **FAIL-OPEN**: project_alignment_scorer 缺失 → log WARN 跳过 rule_check 不阻塞 hf_papers cron. CLI: `--content X --llm-score N --json`. |
| `test_project_alignment_scorer.py` | **V37.9.47新增** Stage 2 PoC 31 单测（7 测试类）：TestProjectConceptsYaml(5 含 yaml schema / weight 范围 [-5,5] / keywords 非空 / FAIL-OPEN 缺文件 → 默认 dict) + TestKeywordHitCounting(6 含多关键词 / 大小写不敏感 / 中英混合 substring / excluded × -2 降权 / 同 keyword dedup / 空内容) + TestExpectedScoreRange(4 含 0/5/边界 1-4/负数命中) + TestValidateAlignmentScore(4 含血案场景 LLM=5+0 命中 fail / LLM in range pass / LLM 偏低 / invalid score type) + TestPromptInjection(3 hf_papers.sh prompt 含 🎚️ + 5 档评分指南 + 一句话原因 ≤30 字) + TestParse6FieldOutput(5 完整 6 字段 / 缺字段 / 顺序乱序 / 空 / **MR-8 grep hf_papers inline 守卫**) + TestSourceLevelGuards(4 V37.9.47 marker / _SCORE_RANGES 5 档锁定 / FAIL-OPEN 注释 / log stderr MR-11). **反向验证已确认守卫真有效**: sed `(5, 999, 4, 5)` → `(5, 999, 1, 2)` → test_high_hits_high_stars 立即 fail. 还原后 31/31 全过. |
| `test_cross_source_signal_aggregator.py` | **V37.9.46新增** Stage 1 PoC 33 单测（8 测试类）：TestScanTodayNotes(5 含 YYYYMMDD 日期过滤 + frontmatter parse + tags 单值/list 双格式 + 缺字段 default unknown + 无效 date) + TestEmbedNotes(4 含 cache hit os.path.isfile mock + force_recompute bypass + 文本截断 ≤500 chars + empty 输入 numpy.zeros (0,384)) + TestClusterDbscan(5 含 lazy import sklearn ImportError + N<min_samples short-circuit 在 import 之前 + 多 cluster + noise -1 + DBSCAN 参数 design-locked 验证) + TestFilterCrossSource(4 含单 source 拒绝 + 跨 2 源保留 + 跨 4 源高信号 + noise label 排除) + TestRankSignals(3 含公式正确性 + top_k 截断 + 空输入) + TestEmitRadarJson(3 含 JSON 字段完整性 + daily_signals_{date}.json 路径 + 自动 mkdir 嵌套目录) + TestFailOpenContract(4 含 no_notes 状态 + numpy 缺失 fail_open + sklearn 缺失 fail_open + 单笔记 OSError 不致命) + TestSourceLevelGuards(5 V37.9.46 marker / DBSCAN_MIN_SAMPLES=3 + DBSCAN_EPS=0.35 设计锁定值 / FAIL-OPEN 契约文档 / lazy import 反 module-top-import 守卫前 60 行禁 numpy/sklearn/sentence_transformers/local_embed / log() 写 stderr MR-11)。**反向验证已确认守卫真有效**: sed 把 `DBSCAN_MIN_SAMPLES = 3` → `1` → test_design_locked_constants 立即 AssertionError; 模块顶部加 `import numpy` → ModuleNotFoundError 立即触发（dev 无 numpy 强证守卫）。还原后 33/33 全过。 |
| `project_concepts.yaml` | **V37.9.45新增** Opportunity Radar #2 PoC 项目本体清单（V37.9.46 Stage 1 复用即将由 project_alignment_scorer.py 完整 Stage 2 消费）：3 core_planes (control/capability/memory plane keywords + weights 5/4/5) + 3 active_research_directions (ontology/agent_reliability/data_compounding) + 2 excluded_topics (pure_finetuning/pure_hardware weight=-2 命中扣分) + scoring_guide 5 档评分指南（⭐⭐⭐⭐⭐=control plane/agent runtime/ontology 直接相关）+ meta_rules（收工时同步本周新增概念）。version=0.1-poc。 |
| `hallucination_guards.py` | **V37.9.57新增** 公共反幻觉守卫 5 档模板 (MR-8 single-source-of-truth, 替代 9 task 各自 inline 守卫). 5 档累积式严格度: **LEVEL_1_MINIMAL** (kb_harvest_chat 闭环数据, 仅"严禁虚构") / **LEVEL_2_STANDARD** (freight/finance/ontology 单源新闻, + 来源标签校验) / **LEVEL_3_STRICT** (kb_review/kb_deep_dive 跨域回顾, + 反链式推论 "X 段提到 Y → 推论 Z" 模式禁止) / **LEVEL_4_PROJECT_AWARE** (8 ALIGNED jobs per-paper alignment 评分, + V37.9.56-hotfix3 6 个血案精确字眼 "OpenClaw 社区发布"/"v26"/"v26/v27/v37 版本更新"/"项目里程碑"/"开源 X 上线"/"[openclaw]") / **LEVEL_5_RADAR_AWARE** (kb_dream Reduce + kb_evening, + Opportunity Radar 三件套信号源契约 + Top 5/cross_source/trend 显式声明"跨多天累积非今日事件"). **公开 API**: `get_guard(level)` / `list_levels()` / `get_blocked_phrases()` / `get_radar_signal_types()`. 未知 level fallback LEVEL_3_STRICT 安全中位数. CLI `python3 hallucination_guards.py --list/--blocked-phrases/--level X`. 纯 stdlib 零外部依赖. |
| `test_hallucination_guards.py` | **V37.9.57新增** 38 单测 / 7 类: TestGuardLevels(8 含 5 档累积式 + V37.9.57 marker + LEVEL_N 不含 LEVEL_(N-1) 之外特性) + TestGetGuardAPI(6 默认/未知/None/非string fallback 全过) + TestBloodLessonBlockedPhrases(5 V37.9.56-hotfix3 5 个核心字眼必须在 LEVEL_4+) + TestRadarSignalTypes(3 三件套 #1/#2/#3 必须在 LEVEL_5 + 不在 LEVEL_4) + TestCliInterface(4 subprocess --list/--blocked-phrases/--level) + TestSourceLevelGuards(9 V37.9.57 marker / V37.9.56-hotfix3 lineage / GUARDS dict 5 key locked / _DEFAULT_FALLBACK 锁 LEVEL_3 / MR-8 documented) + TestV37957Contracts(2 模块顶部 import / 无外部依赖). **反向验证已确认守卫真有效**: sabotage _LEVEL_4 整段赋空 → 4 failures; sabotage _DEFAULT_FALLBACK 改 LEVEL_5 → 1 failure; sabotage _LEVEL_5 整段赋空 → 4 failures. 还原后 38/38 全过. |
| `top_alignment_picker.py` | **V37.9.56新增** Opportunity Radar Sub-Stage 4c Top 5 高对齐推送 picker（设计文档第 7 节 Stage 4c 兑现）。纯 Python 模块 8 个核心函数：scan_source_results / parse_alignment_from_content / collect_all_picks / rank_picks / format_top_picks_block / pick_top_aligned + log()(MR-11 stderr) + main CLI（--json/--block-only/--repo-root/--min-stars/--top-n）。**ALIGNED_SOURCES** 8 个 source (hf_papers prio 1 / s2 prio 2 / arxiv+dblp prio 3 / github_trending prio 4 / rss_blogs prio 5 / ai_leaders_x prio 6 / hn prio 7) 来自 V37.9.45/50/51 全量迁移 6 字段 + 🎚️ 项目对齐度评分. **解析**: lazy import project_alignment_scorer.extract_star_count → _fallback_extract_star_count (clamp [0,5]) → key-based 6-field parser 复用 hf_papers/V37.9.51 同款 (📌/🔑/💡/🎯/⭐/🎚️ 字段头识别 + 🎚 fallback no variation selector)。**排序契约**: ⭐ 数 desc + source_priority asc + cn_title 长度 desc 三档 tie-break。**FAIL-OPEN 契约 (V37.9.46 同款 lazy import 模式)**: cache dir/file 缺失 → 该 source 0 picks 不阻塞其他 source / llm_results.jsonl 损坏行 → skip 继续 / failed=True 跳过 / alignment scorer 缺失 → fallback regex / 无 picks → block="" 调用方静默不推送 / 任何异常 → pick_top_aligned 顶层 try/except 返回 no_picks。**默认配置**: DEFAULT_MIN_STARS=4 / DEFAULT_TOP_N=5 (V37.9.51 收工承诺锁定)。**集成路径** kb_dream.sh Phase 1.5+ + kb_evening_collect.py build_evening_prompt 双处。 |
| `test_top_alignment_picker.py` | **V37.9.56新增** Sub-Stage 4c 55 单测 / 8 类 + 反向验证三层守卫真有效 (sabotage DEFAULT_MIN_STARS=3 / 删 🎚 fallback / log 写 stdout 都立即多 fail，还原后全过). TestParseAlignmentFromContent(11 含 6 字段完整 / ⭐≥4 vs ⭐3 阈值 / 🎚 emoji fallback / 中文标题 / 5 字段向后兼容 / 空内容 / 非 string / ⭐ clamp 到 5 / rating vs alignment 不污染 / reason truncate 60 字) + TestFallbackExtractStarCount(5 含多段取最长 / 空输入 / clamp) + TestScanSourceResults(6 含 missing dir + corrupted JSON skip + failed entries skip + empty content skip) + TestCollectAllPicks(3 含 dev 空环境 / min_stars 过滤 / source metadata 注入) + TestRankPicks(6 含 stars desc / priority tie-break / title len tie-break / top_n truncate / 空输入 / 缺字段安全) + TestFormatTopPicksBlock(6 含空输入 / 一行一 pick / 标题 40 字截断 / 无 reason 不加分隔符 / ⭐ clamp) + TestPickTopAlignedOrchestrator(3 含端到端 dev no_picks / ok with picks / multi-source 排序) + TestCliInterface(3 subprocess 真跑 CLI --json + --block-only) + TestSourceLevelGuards(10 V37.9.56 marker / 8 source 全部注册 / DEFAULT_MIN_STARS=4 锁定 / DEFAULT_TOP_N=5 锁定 / log 写 stderr MR-11 / FAIL-OPEN 文档 / 🎚️ + 🎚 双 emoji 兼容 / priority 论文优先 / 反向验证守卫 / 模块顶部反 lazy import 守卫) + TestV9_56IntegrationContracts(2 picks JSON 可序列化 + bash 注入安全契约). |
| `kb_search.sh` | **V29新增** KB按需查询工具（关键词/标签/来源/统计概览） |
| `kb_inject.sh` | **V29新增→V29.4升级** 每日KB摘要+运维知识精华注入workspace CLAUDE.md；文档按需查阅路径 `~/.kb/docs/` |
| `mm_index.py` | **V29.1新增** Multimodal Memory 索引器（Gemini Embedding 2，支持图片/音频/视频/PDF） |
| `mm_search.py` | **V29.1新增** Multimodal Memory 语义搜索（文本查询→cosine similarity→匹配媒体） |
| `mm_index_cron.sh` | **V29.1新增** MM 索引定时任务包装脚本（每2小时） |
| `local_embed.py` | **V29.3新增** 本地 Embedding 引擎（sentence-transformers，中英双语，零API调用） |
| `kb_embed.py` | **V29.3新增** KB 文本向量索引器（notes+sources 分块→本地 embedding→~/.kb/text_index/） |
| `kb_rag.py` | **V29.3新增** KB RAG 语义搜索（--context LLM注入 / --json 脚本调用） |
| `kb_trend.py` | **V29.5新增** KB周趋势报告（本周vs上周关键词+LLM分析+WhatsApp推送） |
| `status_update.py` | **V29.5新增** 三方共享项目状态工具（原子读写 ~/.kb/status.json，Claude Code + PA + cron 共用） |
| `kb_save_arxiv.sh` | ArXiv监控结果写入KB + rsync备份 |
| `auto_deploy.sh` | **V27.1新增** 仓库→部署自动同步 + 漂移检测（md5全量比对+WhatsApp+Discord告警） |
| `test_tool_proxy.py` | proxy_filters 单测（118个用例，含自定义工具注入 + **V37.4.3 新增 21 个告警隔离回归：TestFilterSystemAlerts(14) + TestNotifyShAlertMarker(5) + TestToolProxyImportsAlertFilter(2)**） |
| `test_check_registry.py` | **V28新增** check_registry.py 单测（18个用例） |
| `gen_jobs_doc.py` | **V28新增** 从 registry 自动生成任务文档 + 漂移检测 |
| `smoke_test.sh` | **V28新增** 端到端 smoke test（单测+注册表+连通性） |
| `wa_keepalive.sh` | **V28新增** WhatsApp session 保活（每30分钟真实发送验证） |
| `preflight_check.sh` | **V28新增→V30.3升级** 收工前全面体检（16项检查：单测+注册表+语法+部署一致性+环境变量+连通性+安全扫描+数据流+货代监控+crontab路径一致性+推送通道E2E） |
| `job_smoke_test.sh` | **V30.3新增** 全量 job smoke test（20个启用任务×6维度：脚本存在/crontab注册/运行时路径/日志活跃/状态文件/锁文件+KB完整性+crontab条目数） |
| `docs/config.md` | 完整系统配置文档（含所有历史变更） |
| `docs/GUIDE.md` | 完整中英文集成指南 |
| `docs/openclaw_architecture.md` | **V28.2新增** OpenClaw 开源架构完整参考（每日开工自动刷新） |
| `cron_doctor.sh` | **V30新增** 定时任务全面诊断工具（7项检查：crontab/锁文件/心跳/服务/环境/时效/系统） |
| `cron_canary.sh` | **V30新增** Cron 心跳金丝雀（每10分钟，零依赖，原子写入） |
| `crontab_safe.sh` | **V30新增** 安全 crontab 操作（自动备份+条目数验证+回滚保护） |
| `test_cron_health.py` | **V30新增** 定时任务健康检测单测（94个用例：锁/心跳/告警/原子写入/损坏恢复/daemon检测/完整性/状态刷新） |
| `kb_status_refresh.sh` | **V30.1新增** 每小时刷新 status.json 系统健康字段（三层服务/模型ID/KB统计/过期job），补齐三方宪法实时同步 |
| `kb_integrity.py` | **V30.1新增** KB 文件完整性校验器（SHA256 指纹比对、目录文件数监控、权限检查、status.json 结构验证） |
| `test_status_update.py` | **V30.1新增** status_update.py 全量单测（33个用例：原子读写/嵌套字段/数组操作/优先级CRUD/CLI接口） |
| `test_adapter.py` | **V30.1新增** adapter.py 全量单测（36个用例：Provider注册表/认证头/多模态路由/Fallback/智能路由/健康端点） |
| `test_kb_business.py` | **V30.1新增** KB全业务逻辑单测（44个用例：kb_embed/kb_rag/kb_trend/mm_index/mm_search/kb_integrity/安全模式） |
| `full_regression.sh` | **V30.1新增** 全量回归测试一键运行器（四层：单元测试+注册表文档+安全扫描+代码质量，393个用例，100%通过才允许推送） |
| `audit_log.py` | **V30.2新增** 链式哈希审计日志（JSONL append-only，SHA256 链式校验，篡改/删除可检测） |
| `test_audit_log.py` | **V30.2新增** 审计日志单测（19个用例：写入/链式哈希/篡改检测/删除检测/统计） |
| `governance_audit_cron.sh` | **V37.1新增** 每日定时治理审计（governance_checker --full + engine --check，失败推送 alerts），ontology 从被动验证升级为主动监控 |
| `security_score.py` | **V30.2新增** 系统安全评分（7维度100分：密钥/测试/完整性/部署/传输/审计/可用性） |
| `reliability_bench.py` | **V36新增** Agent Reliability Bench（7场景47检查：Provider宕机/工具超时/畸形参数/超大请求/KB未命中/Cron漂移/状态损坏，mock-based可在dev运行） |
| `test_reliability_bench.py` | **V36新增** Reliability Bench 单测（36个用例：7场景×独立验证+报告格式+CLI） |
| `docs/reliability_bench_report.md` | **V36新增** 首份 Reliability Bench 实验报告（7/7 PASS，47/47 checks） |
| `memory_plane.py` | **V36新增** Memory Plane v1 统一接口（4层：KB语义/多媒体/偏好/状态，统一 query/context/stats） |
| `test_memory_plane.py` | **V36新增** Memory Plane 单测（45个用例：4层×可用性/搜索/统计+统一查询+优雅降级+CLI） |
| `docs/memory_plane.md` | **V36新增** Memory Plane 架构文档（分层设计/API/数据流/CLI） |
| `docs/security_boundaries.md` | **V36新增** 安全边界文档（8节：认证/网络/输入验证/数据保护/LLM安全/运维/评分/已知风险+Checklist） |
| `docs/resilience_report.md` | **V36新增** 运维韧性实验报告（7场景故障注入+Recovery Time+GameDay对比+改进建议） |
| `docs/ontology/` | **V36新增** Ontology KB — 本体论驱动的企业智能架构知识库（16文件：三角架构论述/核心概念/AI治理/OpenClaw本体审视/文献/人物/README/流派对比/供应链本体） |
| `ontology/` | **V36.1新增→V37.9.15 Phase 4 P3 三阶段门控 shadow wiring** Ontology 独立子项目 — Tool Engine（81条声明式规则+推理引擎+**classify_tool_call语义分类**）+ **Governance Ontology v3.32**（**61不变式**+**297 可执行检查**+**16元规则**+**MR-6 critical-invariants-need-depth 从警告升级为 hard-enforcing via INV-LAYER-001**+MR-7 治理自观察+MR-8 copy-paste-is-a-bug-class+MR-9 state-writes-go-through-helper+**INV-GOV-001 summary 不吞 error**+**INV-DREAM-001/002 + INV-CACHE-002 Dream Map 预算+缓存契约**+**INV-PA-001/002 告警污染+SOUL.md 规则 10 双防线**+**INV-LAYER-001 治理深度自我强制**+**INV-GATE-001 三阶段门控 wiring observability (V37.9.15)**+验证深度三层模型+Phase 0元规则自主发现）+ governance_checker.py 执行引擎 + 宪法6条 + **Phase 2 shadow模式**(off→shadow→on) + 立场文章(EN+ZH) + **V37.9.12 engine.py 3 个 Phase 4 P1 纯函数** (`load_domain_ontology()` / `find_by_domain(domain)` / `evaluate_policy(policy_id, context)`) + `proxy_filters.py::filter_tools` 切换读 policy.max-tools-per-agent.limit + **V37.9.13 Phase 4 P2 context evaluator** (6 matcher + `_CONTEXT_EVALUATORS` dispatch + `evaluate_policy()` 三档扩展) + 第二条 policy `_resolve_max_tool_calls_per_task_limit()` 镜像 5 档 safe-fallback + **V37.9.15 Phase 4 P3 `ontology/three_gate.py` 三阶段门控 shadow wiring**：`pre_check/runtime_gate/post_verify` 三纯函数 + `GateFinding` namedtuple + `ONTOLOGY_GATES_MODE` env (off/shadow/on 默认 shadow) + 静态 policy signal 比对（tool_count/tool_call_count/body_bytes vs limit 产 pass/flag verdict） + 与 `ONTOLOGY_MODE` 解耦 + FAIL-OPEN 契约（engine 异常不冒泡，fall back to pass+reason=engine_unavailable）。tool_proxy.py lazy-load three_gate + `_run_gate` 帮助函数（Exception 兜底 + log [gate:*]）+ do_POST 两处调用点（raw 后 pre_check+runtime_gate / rj 后 post_verify 必须在 fix_tool_args 之前）|
| `ontology/domain_ontology.yaml` | **V37.9.9新增（骨架）→V37.9.12 P1 wiring 依据** Phase 4 Layer 1 领域模型 — 六域概念 Actor/Tool/Resource/Task/Provider/Memory + 域间关系模板 + Phase 4 实施路径 P1/P2/P3。V37.9.12: `find_by_domain()` 纯函数读此文件, Actor 6/Resource 7/Task 6/Memory 4 层/Provider 4 type 可查。|
| `ontology/policy_ontology.yaml` | **V37.9.9新增（骨架）→V37.9.15 Phase 4 P3 gates 接入** Phase 4 Layer 2 策略声明 — 10 条 policy 按 static/temporal/contextual 三类分。**V37.9.15 meta 升级** version 0.2-p2-partial → 0.3-p3-partial，status → gates_wired_shadow_mode，新增 `policies_observed_by_three_gate` 清单（pre_check 2 / runtime_gate 3 / post_verify 1）+ `gates_feature_flag` 声明（env_var=ONTOLOGY_GATES_MODE / values=["off","shadow","on"] / default=shadow / decoupled_from=ONTOLOGY_MODE）+ `next_step` 指向 P3.1 (shadow→on 一周观察) / P3.2 (第三条 policy) / P4 (真正 enforcement 路径)。|
| `ontology/three_gate.py` | **V37.9.15新增** Phase 4 P3 三阶段门控纯函数模块（~280 行，零 I/O 零状态）— `pre_check(context)` 评估 alert-context-isolation + quiet-hours-00-07；`runtime_gate(context)` 评估 max-tools-per-agent + max-tool-calls-per-task + max-request-body-size（静态 policy 提取 signal 与 limit 数值比对产 pass/flag verdict）；`post_verify(context, response)` 扫 LLM 响应检测 [SYSTEM_ALERT] echo。`GateFinding` namedtuple (gate/policy_id/verdict/action/reason/enforced)。`ONTOLOGY_GATES_MODE` env 三档 off/shadow/on（默认 shadow，未知值 fall back shadow 观察优先）+ `_VALID_GATE_MODES = ("off", "shadow", "on")` 常量。与 `ONTOLOGY_MODE` 解耦（允许 ONTOLOGY_MODE=on 但 gates=off 的灰度组合）。FAIL-OPEN 契约: `_safe_evaluate_policy` 捕获 engine 所有异常返回 None → gate 降级为 verdict=pass + reason="engine_unavailable"，绝不冒泡到 tool_proxy。`format_findings_for_log()` 产出 `[gate:pre_check] N findings: policy_id=verdict(reason) ...` 单行便于 `grep [gate:]` 运维排查。|
| `ontology/tests/test_three_gate.py` | **V37.9.15新增** three_gate 契约守卫（57 单测）：TestGatesMode(7 含默认 shadow + case-insensitive + whitespace + 未知 fall back shadow)、TestGateFinding(3 含 immutable + 字段顺序稳定)、TestOffModeShortCircuit(4 含 off 模式不调 engine)、TestShadowModeNeverEnforces(2 反证 on 模式会 enforced=True)、TestPreCheck(8 含 alert 检测 + quiet-hours 上下文全分支)、TestRuntimeGate(6 含 signal≤limit boundary 合规 / signal>limit flag / 缺信号 tolerant)、TestPostVerify(5 含 assistant [SYSTEM_ALERT] 回声检测 + malformed response tolerant)、TestFailOpen(4 monkey-patch engine.evaluate_policy 抛异常契约)、TestExtractSignal(7 含 bool 排除于 int) + TestExtractAssistantText(6) + TestFormatFindingsForLog(3) + TestModuleIntegration(2 公开 API + gate name 稳定性) |
| `test_phase4_ontology_skeleton.py` | **V37.9.9新增→V37.9.12扩展** Phase 4 骨架守卫（17 单测）：六域声明/description 长度/Actor 核心实例/source_of_truth 对齐/Memory 四层匹配 memory_plane/policy 三类分类/policy 必填字段/hard_limit 仅 static/alert-isolation ordering_constraint/跨 ontology 引用一致性 + **V37.9.12 hard_limit 必须有 `limit` 字段 + max-tools-per-agent.limit == 12 横向守卫** |
| `ontology/tests/test_engine_phase4.py` | **V37.9.12新增** Phase 4 P1 wiring 契约（38 单测）：`load_domain_ontology()` 加载 + path 参数 + 缺文件抛错；`find_by_domain()` Actor/Resource/Task/Memory 归一化 + Provider.types 字符串包装 + Tool 主动返回 []（source_of_truth=tool_ontology）+ 未知域返回 [] + 预加载 ontology 注入 + 纯函数不改输入；`evaluate_policy()` 返回 12-key 稳定结构 + unknown found=False + static applicable=True + contextual/temporal applicable=None+reason=needs_context_evaluator + load_failed 不抛异 + context 参数兼容 + pre-loaded policy_data 注入；**max-tools-per-agent 契约**: limit=12, hard_limit=True, type=static, governance_invariant=INV-TOOL-001, 横向一致 config_loader.MAX_TOOLS; **_parse_limit_from_rule**: ≤/<=/< 提取 + 下划线千位分隔 + None 回退 |
| `ontology/tests/test_governance_cron_matcher.py` | **V37.2+新增** INV-CRON-003/004 匹配器回归测试（18 单测）：① endswith+word-boundary 精确匹配 ② Map-Reduce split 不误报 ③ prefix-subset entries 不混淆 ④ exec() 作用域陷阱正向对照 ⑤ YAML-matcher in-sync guard |
| `ontology/tests/test_governance_summary.py` | **V37.3新增** INV-GOV-001 silent error bug 回归测试（7 单测）：① error 状态注入→汇总不说"所有不变式成立" ② error 状态 exit_code≠0 ③ error 汇总用 💥 ④ pure fail 仍用 ❌ ⑤ all-pass 仍说✅ ⑥ mixed fail+error 各自计数 ⑦ 源码级 grep 守卫 `r["status"] in ("fail","error")` |
| `ontology/docs/cases/governance_silent_error_case.md` | **V37.3新增** Governance 自身三层嵌套盲区案例（子串匹配→exec 作用域→silent error summary）— 观察者的自我盲区 + MR-7 提案 + INV-GOV-001 提案 |
| `ontology/docs/cases/dream_map_budget_overflow_case.md` | **V37.4新增** Dream Map 预算溢出 + cache key 漂移案例（workload 增长 × cache key 对 mtime/sort 敏感 × Reduce 不相信缓存 → 连续超时，Reduce 从未执行）完整因果链架构图 + 三层根因 + 预算/契约/稳定三层修复 |
| `ontology/docs/cases/pa_alert_contamination_case.md` | **V37.4.3新增** PA 告警污染血案案例（系统告警经 Gateway sessions.json 污染对话上下文 × Proxy truncate 无语义理解 × Qwen3 attention 跨主题错误关联 × LLM 编造 macOS FDA 指令）六层根因深挖 + 完整因果链架构图 + 六条件组合爆炸分析 + 结构+行为双防线修复论证 |
| `ontology/docs/cases/heartbeat_md_pa_self_silencing_case.md` | **V37.8.16新增** PA 自残 HEARTBEAT.md 血案案例（2026-04-19 09:09 PA 把"任务完成"写进 OpenClaw 保留文件 HEARTBEAT.md → 13h 潜伏 → OpenClaw heartbeat 机制激活让 LLM 对所有用户消息回 HEARTBEAT_OK → Gateway stripTokenAtEdges 剥离 → 13h 完全静默）完整时间线 + 三层根因 + 六条件组合爆炸分析 + MR-15 元规则提炼 + 4/19 深夜原则 #28 违规复盘（Bad MAC 错归因让用户 auth reset）+ 6 轮假说错五轮的教训 |
| `ontology/tests/test_dream_cache_stability.py` | **V37.4新增→V37.4.2扩展** Dream Fix A+B+C+retry 回归测试（32 单测）：① Fix A cache-only fast path + SKIP_MAP_LOOPS 三处门控 ② Fix B 批次阈值 30/24000 ③ Fix C per-note content_hash 缓存 + 跨平台 md5 fallback + signal dedup ④ 动态 DREAM_TIMEOUT_SEC 预算 ⑤ flush_pending_batch 辅助函数语义 ⑥ Sources 缓存键保持不变 ⑦ **V37.4.1 Retry fallback**：MIN_DREAM_CHARS=3000 / MIN_ACCEPTABLE_CHARS=1500 / BEST_RESULT 追踪 / 温度单调递减 / 致命错误门槛用 MIN_ACCEPTABLE_CHARS ⑧ **V37.4.2 结构+变体**：UNIQ_NOTE_SIG_BLOCKS 数组 / 覆盖统计 header / 编号 cluster header / bland header 已移除 / retry 2+ 变体 prompt / llm_call 引用 \$cur_prompt |
| `kb_dream.sh` | **V36新增→V37.8.6升级** Agent Dream v2 MapReduce 全量 KB 探索引擎（Phase1 Map 14源+293笔记逐一提取信号 → Phase2 Reduce 跨域深度分析；Map-Reduce 分离调度：00:00 Map预热缓存→00:40 Notes 预热→03:00 Reduce 跨域关联+推送）。**V37.4 Fix A+B+C** / **V37.4.1 retry** / **V37.4.2 结构+变体**（见下方 changelog）。**V37.8.6 自引用幻觉防御**：① `log()` 改写 stderr（`>&2`）阻断 `signals=$(llm_call ...)` 命令替换把错误日志捕获进 cache 的通道（血案根因：错误日志→cache→Reduce LLM → 编造 Hugging Face 危机）② heredoc 内嵌 `_sanitize(s)` 清洗 U+D800-U+DFFF 孤立代理 → U+FFFD，防 json.dump 触发 UnicodeEncodeError 导致 body_file 截断 → adapter 400 Bad JSON ③ file open 显式 `encoding='utf-8', errors='replace'` 第二防线 ④ REDUCE/CHUNK1/2/3 system prompt 加反污染守卫，明示禁止把 HTTP 错误码/Python 异常/错误页 HTML/U+FFFD 当外部信号，禁止推断 Hugging Face/GitHub/npm 等平台状态 |
| `gameday.sh` | **V33新增** GameDay 故障演练（5场景：GPU超时/断路器/快照/SLO/Watchdog，`bash gameday.sh --all`） |
| `jobs/dblp/run_dblp.sh` | **V30.5新增** DBLP CS论文监控（多关键词搜索、免费API、每日12:00推送+KB写入） |
| `jobs/hf_papers/run_hf_papers.sh` | **V30.5新增** HuggingFace Daily Papers 监控（热门AI论文、每日10:00推送+KB写入） |
| `jobs/semantic_scholar/run_semantic_scholar.sh` | **V30.5新增** Semantic Scholar 论文监控（引用量排序、每日11:00推送+KB写入） |
| `jobs/acl_anthology/run_acl_anthology.sh` | **V30.5新增** ACL Anthology NLP论文监控（顶会论文、每日09:30推送+KB写入） |
| `jobs/pwc/run_pwc.sh` | **V31新增** Papers with Code 论文+代码监控（免费API、每日13:00推送+KB写入，核心价值：论文+代码仓库关联） |
| ~~`jobs/openreview/`~~ | **V35 已移除**（API 403 post-security-incident，S2 已覆盖顶会论文） |
| `preference_learner.py` | **V30.4新增** 用户偏好自动学习器（从对话历史推断偏好，写入 status.json） |
| `activate_openclaw_features.py` | **V30.5新增** OpenClaw 功能激活脚本（检查+启用 agent 工具配置） |
| `notify.sh` | **V33新增→V35+升级** 统一消息推送（WhatsApp + Discord 双通道 + **自动重试3次指数退避** + **失败队列持久化+自动重放**，`source notify.sh && notify "msg"`） |
| `kb_harvest_chat.py` | **V37新增→V37.1升级** 对话精华提炼器 MapReduce 版（从 proxy 捕获的每日对话中 LLM 分段提取+去重合并，零数据丢失，每日 06:00 cron） |
| `jobs/ontology_sources/run_ontology_sources.sh` | **V37.1新增** Ontology 专属信息源监控（4 RSS: W3C/JWS/DKE/KBS + 两层关键词过滤 + LLM 中文摘要+要点+价值 + Discord #ontology 推送 + KB 归档，cron 10:00/20:00） |
| `jobs/finance_news/run_finance_news.sh` | **V37.8.2新增→V37.8.5升级** 全球财经/政策新闻每日汇总（双通道：8 RSS 直连 + 21 X/Twitter Syndication API，72h 窗口覆盖周末，LLM 结构化分析+价值评级+国内外对比+投资建议+风险提示，cron 07:30）。V37.8.5：僵尸检测从 heredoc 内嵌严格相等升级为导入 `finance_news_zombie.classify_zombie` 模块，三层 tier（stub/stale/alive）+ count 守卫防止低频活跃账号误报 |
| `jobs/finance_news/finance_news_zombie.py` | **V37.8.5新增** Syndication API 僵尸账号三层检测纯函数模块（Tier 1 stub: no_data=0 + total=0 闭合 SingTaoDaily 0-tweet 盲区；Tier 2+3 stale: count=0 + old*10 >= total*9 整数比较闭合 CNS1952 99% 近僵尸盲区；count 参数守卫避免低频活跃账号误报） |
| `test_finance_news_zombie.py` | **V37.8.5新增** 僵尸检测 24 单测：3 层 tier + count 守卫 + V37.8.4 向后兼容 + shell 集成（import/call-with-count/env-export/tier-prefix/禁 inline fallback）+ auto_deploy 映射 + 常量契约 |
| `test_wa_gateway_resilience.py` | **V37.8.13新增** Gateway 宕机韧性三层修复 21 单测：TestQuietAlertDiscordAlways(4) quiet_alert 静默期 Discord 穿透 + TestWaKeepaliveEscalation(9) 连续 WARN 升级 + TestRestartGatewayVerification(5) post-bootstrap 健康验证 + TestCrossFileGuards(3) 跨文件一致性 |
| `test_restart_launchd.py` | **V37.9.13新增** restart.sh 架构清理 20 单测：TestRestartVialaunchdHelper(6) helper 函数契约（kickstart -k / bootstrap fallback / 5×2s 健康验证 / return 2 / launchctl 可用性检测）+ TestAdapterViaLaunchd(3) + TestProxyViaLaunchd(2) + TestSingleManagerInvariant(5) V37.9.12.1 双管理血案防线（nohup 必在 fallback 分支 + V37.8.13 Gateway 逻辑保留 + #48703 hotfix 保留）+ TestShellExecutability(3) + TestRuntimeHelperBehavior(1) subprocess 实跑 helper 验证 return 2 |
| `providers.d/` | **V37新增** Provider Plugin 目录（YAML/Python 插件自动发现，`_example.yaml` + `_example_provider.py` 示例） |
| `docs/provider_plugin_guide.md` | **V37新增** Provider Plugin Extension Guide（60秒添加新 Provider，合约验证，YAML/Python 双模式） |
| `ontology/docs/architecture/industrial_ai_paradigm.md` | **V37新增** 工业AI范式文档（三平面架构+五项工业需求+范式对比，切断 Dream→PA 链式幻觉） |
| `ontology/docs/architecture/target_architecture.md` | **V37.1新增** Ontology 终态架构（四层设计+六域概念模型+策略引擎+三阶段门控+迁移路径 Phase 3-5） |
| `ontology/docs/cases/pa_echo_chamber_case.md` | **V37.1新增** PA 迎合性回复案例分析（三环反馈陷阱根因+SOUL.md 批判性规则修复+Phase 4 结构性修复路径） |
| `ontology/docs/cases/dream_quota_blast_radius_case.md` | **V37.2新增** Dream MapReduce 配额耗尽跨 Job 爆炸链案例（30+ LLM 调用 × Qwen3 宕机 → Gemini 配额耗尽 → HN 垃圾推送，三层修复+2 新不变式） |

## 版本变更历史

> 完整变更记录见 `docs/changelog.md`，按需 read 查阅。

| 版本 | 日期 | 关键变更 |
|------|------|----------|
| V37.9.65 | 2026-05-13 | **crontab_safe.sh 加 `remove` command — V37.9.64 用户痛点闭环 (不需 vim) + convergence framework 双向 sync 下半截** — ① **触发**: V37.9.64 改 freight 一天 1 次 14:00 后, 用户反馈"不需要使用 VIM" — V37.9.64 推荐 `crontab -e` 手动删 2 行需用户进 vim 编辑器, 用户视角不友好。同时这是 V37.9.64 收工承诺登记的 V37.9.65 候选 (convergence machine_sync 加 remove_extra 双向 sync 能力的下半截)。② **设计 cmd_remove 镜像 cmd_add 安全机制**: backup + 严格 count 验证 (count_after = count_before - matched, V37.9.18 cmd_add 同款契约防 35→35 谎报 ✅) + 拒绝全清空操作 (pattern 匹配所有活跃行时拒绝, V30 教训) + 失败自动 cmd_restore 回滚. **接口**: `bash crontab_safe.sh remove "<grep -F 固定字符串 pattern>"` — pattern 用 grep -F 匹配 (不是正则), 出现在行内任何位置都被删除. ③ **修一个 V37.9.65 引入的 bug** (开发期发现): `grep -cF -- "$pattern" \|\| echo 0` 在 grep 无匹配时输出"0\n0" (grep -c 已输出 0 + `\|\|` 触发 echo 0), 导致 matched_count = "0\n0" 整数比较失败. 修: `\|\| true` 仅安抚 pipefail 不再额外 echo. ④ **test_crontab_safe_remove.py 16 单测 4 类**: TestRemoveBasic(5 含真 fake-crontab shim 注入 PATH 隔离 + 单 pattern 单行 + 0 匹配 silent skip + 拒绝全清空 + 空 pattern 用法提示) + TestRemoveMultipleMatches(1 单 pattern 多行严格 count 验证) + TestCliInterface(2 help 含 remove + V37.9.65 marker) + TestSourceLevelGuards(8 V37.9.65 marker / cmd_remove() 函数定义 / remove case 分支 / grep -F 固定字符串 / -ne 严格相等 / 拒绝全清空保护 / cmd_restore 自动回滚 / do_backup 修改前). **反向验证守卫真有效**: sabotage cmd_remove 函数整段删除 → 9 failures + 2 errors, 还原后 16/16 OK. ⑤ **MR-9 正向兑现** (state-writes-go-through-helper-not-raw-redirect): 用户操作 crontab remove 现在走 helper 不再 vim 直编辑, 同 add 模式接到机器化安全机制. ⑥ **V37.9.64 Mac Mini 操作升级** (用户体验改善): 旧"crontab -e 进 vim 删 2 行"→ 新"`bash ~/crontab_safe.sh remove "<pattern>"` 一行命令 + 同款删 20:00", 自动 backup + count 验证 + 失败回滚, 不需 vim. ⑦ **累计**: 82→83 suites (+1 crontab_safe_remove) / 2815→2831 tests (+16) / 0 fail / 安全 95/100 / VERSION 0.37.9.64 → 0.37.9.65. ⑧ **元价值**: 用户视角原则 #13 第 N+1 次正向兑现 — 单测/治理全过, 用户实际操作流程 (vim 体验差) 立即触发改进. 同时把 V37.9.65 候选从"V3 路标 framework 双向 sync 增强"具体化为 "crontab_safe.sh remove 已上线", convergence framework 调用此 helper 即可实现 remove_extra (V37.9.66+ 候选). MR-9 (state-writes-go-through-helper) 在用户操作层兑现, 把"用户改 crontab 走 helper"从纪律建议升级为可用工具. |
| V37.9.64 | 2026-05-13 | **货代 Watcher 推送频率 3次/天 → 1次/天 (用户视角原则 #13 + #32 减少告警疲劳)** — ① **触发**: 用户开工后立即提出"货代商机速报及货代客户画像任务一天有几个？建议一天一次即可 没必要一天几次"。审计现状: freight_watcher 是唯一货代相关 job, 但产出三层 (📊 经济晴雨表 / 🏢 运营信号 / 🚢 商机条目) + 客户画像 deep_dive — 用户看到的"商机速报"+"客户画像"是同一脚本的不同输出段非两个 job。当前 interval `0 8,14,20 * * *` 每天 3 次 (V37.9.33 起)，watchdog 阈值 50400 (14h). ② **原则 #28 三问全过**: Q1 之前存在 ✓ (V37.9.33 起 3 次) / Q2 我引入 ✗ (历史配置) / Q3 最小修复 = 改 interval + 同步阈值 + Mac Mini 手动删多余 crontab 行。③ **用户决策时段**: 14:00 (中午业务空隙看, 与 finance_news 07:30 + 22:00 kb_evening 错开)。④ **改动 3 文件**: (a) jobs_registry.yaml: `0 8,14,20 * * *` → `0 14 * * *` + V37.9.64 注释 (b) job_watchdog.sh: freight 阈值 50400 (14h) → 180000 (50h, 24h max gap + 26h slack) (c) test_cron_health.py: dict 内 `freight_watcher: 50400` → `180000` 同步守卫。⑤ **convergence framework 单向 sync 限制 (新发现)**: V37.9.58 起 jobs_to_crontab spec 真激活 machine_sync, 但实现是单向 (declared missing → add via crontab_safe.sh add), 不删 extra (runtime 有但 declared 没)。**意味着改 yaml 一行后 framework 不会自动删多余的 2 行 cron** — 因 framework 用 `line_contains_identifier` 匹配 entry, 3 个 cron 行都含同一 entry → missing=0 → 不动。用户必须手动 `crontab -e` 删除 freight_watcher 的 08:00 和 20:00 两行。⑥ **未来 framework 增强候选 V37.9.65+** (登记 status.json unfinished): convergence machine_sync 加 `remove_extra` 支持 — declared 缺 + runtime 多 双向 sync, crontab_safe.sh 加 remove command, 让 freight 这类配置变更自动落地不需用户手动操作。⑦ **累计**: 82 suites / 2815 tests / 0 fail / 安全 95/100 / VERSION 0.37.9.63 → 0.37.9.64. ⑧ **Mac Mini 部署后用户操作 (必须手动)**: (1) `cd ~/openclaw-model-bridge && git fetch origin main && git reset --hard origin/main` 同步 yaml (2) **`crontab -e` 删除 freight_watcher 的 08:00 和 20:00 两行** (保留 14:00 行) (3) `crontab -l \| grep freight` 验证只剩 1 行 (4) 下次 governance audit cron (07:00) 跑应报告 jobs_to_crontab declared=1 observed=1 missing=0 零漂移. ⑨ **元价值**: 用户视角原则 #13 (定期像用户用) + #32 (周一观察制度) 第 N 次正向兑现 — 单测/治理全绿但只有用户在 WhatsApp 反复看到 freight 推送才能感知"3 次过频"。同时 V37.9.65 候选揭示 convergence framework 真实 gap (单向 sync), 把"配置变更落地"从靠用户记忆升级为机器化框架功能 — V3 路标 `pip install ontology-engine` 双向 sync 能力候选. |
| V37.9.63 | 2026-05-13 | **MR-8 抽公共 fatal_handler helper — MR-19 framework 化推广后第一次 MR-8 正向兑现 + 顺势修 V37.9.60 6 个 fatal_handler 第二层 FAIL-OPEN CLI 参数 bug** — ① **触发**: V37.9.61 收工承诺登记 V37.9.63+ 候选 "MR-8 抽 cron_monitor_fatal_handler.sh 公共 helper, V37.9.60+V37.9.61 7 governed scripts copy-paste". 用户开工选 MR-8 路径 (vs by design 排除重评估 / docs/config.md 同步 / 用户视角观察). ② **audit 7 个 inline _\<script\>_fatal_handler 真实代码**: 99% 同款 (三层 FAIL-OPEN: stderr 写错误 / 本地 .openclaw_alerts.log 追加 / notify→openclaw 直发) + 6 处差异 (caller name / reason 文案 / log path / bash_x hint / OPENCLAW 变量名 OPENCLAW vs OPENCLAW_BIN / auto_deploy CLI 参数风格 1/7 异端). ③ **意外发现 V37.9.60 6 个 fatal_handler 第二层 FAIL-OPEN CLI 参数 bug**: 用了 `--channel-id` + `--content` 但 canonical CLI (notify.sh / auto_deploy 主告警 / watchdog 主告警 / health_check / diagnose 同款) 是 `--target` + `--message` + `--json`. 6 个 fatal_handler 第二层 FAIL-OPEN 在 notify 不可用时**实际是死代码**. 顺势统一到 canonical CLI. ④ **新文件 cron_monitor_fatal_handler.sh** (~80 行 helper, sentinel `CRON_MONITOR_FATAL_HANDLER_LOADED="V37.9.63"`): 单函数 `_cron_monitor_fatal_handler()` 读 4 个 caller 变量 (`CRON_FATAL_LABEL` / `CRON_FATAL_LOG` / `CRON_FATAL_BASH_X` / `CRON_FATAL_REASON`) + 三层 FAIL-OPEN 同款 + canonical CLI + 三档 OPENCLAW 变量 fallback (`${OPENCLAW_BIN:-${OPENCLAW:-/opt/homebrew/bin/openclaw}}`). ⑤ **7 个 governed scripts 迁移**: 每个删除 ~20 行 inline `_<script>_fatal_handler` 函数 + 加 7 行 (source helper + 4 个 CRON_FATAL_* 变量 + trap '_cron_monitor_fatal_handler $LINENO' ERR), 净减少 ~13 行 × 7 = **~90 行 copy-paste 消除**. watchdog 保留 `_watchdog_exit_handler` (canary heartbeat) 不动 — 它不是 fatal handler. ⑥ **test_cron_monitor_fatal_handler.py 30 单测 7 类** + 反向验证守卫真有效: TestHelperSyntaxAndStructure(6) + TestHelperCanonicalCli(2 含反 V37.9.60 buggy CLI 守卫) + TestHelperRuntimeBehavior(5 含 subprocess 真跑 set -e caller + handler 自身不冒泡契约) + TestSevenScriptsIntegration(7 含 LABEL 唯一性 + helper source 命令真实性) + TestInlineHandlersRemoved(2 含 watchdog 保 EXIT trap) + TestReverseVerificationSabotage(3 含 sabotage helper → 13 failures / sabotage script source → integration 守卫立即抓) + TestV37963SourceLevelMarkers(5 含 FILE_MAP 部署 + MR-8 引用 + 7 scripts 计数锁定). **反向验证已确认守卫真有效**: sabotage helper → 13 failures (含 layer1/layer2/integration/sabotage 自身的元层验证), 还原后 30/30 OK. ⑦ **cron_monitor_scanner.py 升级**: 新增 `uses_shared_fatal_handler_helper()` 纯函数 (检测 source helper + trap helper 函数双条件 + 跳过注释行), scan_script 加 alternation — [SYSTEM_ALERT] marker OR V37.9.63 helper 模式 任一满足即合规. 防止 V37.9.63 抽 helper 后 daily_ops_report (V37.9.63 后无 [SYSTEM_ALERT] inline) 被误判. ⑧ **test_cron_monitor_scanner.py V37.9.61 守卫升级 alternation**: `test_v37_9_61_llm_task_scripts_have_fatal_handler` 接受 inline trap `'_<script>_fatal_handler' $LINENO' ERR` (legacy V37.9.61) OR helper 模式 (source + trap `'_cron_monitor_fatal_handler'`). 52/52 仍全过. ⑨ **INV-CRON-MONITOR-001 扩 +10 checks** (V37.9.63 helper 段): helper 文件存在 + V37.9.63 marker + 公共函数定义 + canonical CLI (`--target/--message/--json` 各 1 check) + MR-8 引用 + scanner V37.9.63 helper 模式 alternation 守卫 (`uses_shared_fatal_handler_helper`) + auto_deploy FILE_MAP 部署守卫 + 1 runtime python_assert subprocess 跑 test_cron_monitor_fatal_handler.py 30 单测全过. ⑩ **INV-WATCHDOG-SELF-001 升级 alternation** (V37.9.63 后 watchdog inline handler 已移除): 2 个原字面量守卫 (`watchdog ERR trap 注册` + `ERR trap 推送 [SYSTEM_ALERT]`) 升级为 `python_assert` 双路径 — 接受 inline 模式 (legacy V37.9.58-hotfix3 watchdog 自身含 `[SYSTEM_ALERT] watchdog FATAL`) OR V37.9.63 helper 模式 (source helper + helper 自身含 `[SYSTEM_ALERT]`). ⑪ **auto_deploy.sh FILE_MAP 补齐**: 加 `cron_monitor_fatal_handler.sh\|$HOME/cron_monitor_fatal_handler.sh` 部署到 Mac Mini ~/. ⑫ **MR-8 single-source-of-truth 兑现** — 修改 fatal_handler 行为现在只需改 helper 一处, 7 个 governed scripts 自动 propagate. 未来加新 governed cron 类聚合监控只需 source helper + 4 变量 + trap, 不再 copy-paste. ⑬ **累计**: 81→82 suites (+1: cron_monitor_fatal_handler) / 2785→2815 tests (+30) / 0 fail / governance v3.40→v3.41 / 75 invariants 不变 (INV-CRON-MONITOR-001 内涵扩) / 609→619 checks (+10) / 19 meta rules 不变 (V37.9.63 是 MR-8 正向兑现, 不立新元规则) / 安全 95/100 / VERSION 0.37.9.62 → 0.37.9.63. ⑭ **Mac Mini 部署后预期行为**: (a) `cd ~/openclaw-model-bridge && git fetch origin main && git reset --hard origin/main` 同步 V37.9.63 (b) auto_deploy 2min 轮询自动同步 cron_monitor_fatal_handler.sh 到 ~/ (c) 下次任一 governed cron 触发 ERR trap 时 (job_watchdog 4h/governance_audit 07:00/daily_ops 08:15/auto_deploy 2min/kb_deep_dive 22:30/kb_evening 22:00/kb_review 周日 21:00) 走 helper 路径, Discord #alerts 推送行为不变, 但 fatal_msg 内容含 caller-provided REASON + LOG path + bash_x hint (与 V37.9.60+V37.9.61 inline 时代等价). ⑮ **元价值**: V37.9.58-hotfix3 单点 MR-19 (watchdog 合规) → V37.9.59 watchdog 覆盖率扩展 (LOG_FRESHNESS) → V37.9.60 MR-19 framework 化推广 (4 cron) → V37.9.61 LLM-task 扩展 (3 LLM-task = 7 总) → **V37.9.63 MR-8 抽公共** — silent monitor 防御 framework 四阶段完整闭环. 同时用 audit 工作意外发现 V37.9.60 隐藏 CLI 参数 bug 顺势统一. 原则 #18 "补证据而非补功能" 兑现 — 不增加 cron 数量, 但 framework 边界更清晰 + 死代码消除 + 治理负担降低 + V3 路标 `pip install ontology-engine` 终极目标的可包化粒度进一步聚焦. ⑯ **下次 V37.9.64+ 候选**: (a) Mac Mini 用户视角观察 V37.9.62 6 脚本真激活推送质量 (V37.9.62 收工承诺登记 candidate a) (b) finance_news/freight/kb_dream/kb_inject 4 个 by design 排除评估 (V37.9.62 candidate b) (c) docs/config.md 头部漂移修复 (停在 v37.9.57, V37.9.58→V37.9.63 累积 6 版本未同步) (d) 一周观察期后立 INV-CRON-MONITOR-HELPER-001 严格守卫"未来新 governed script 必须用 helper 而非 inline" (e) V37.9.30 24-48h ACL/lsof/snapshot 数据回归 + V37.9.35 推送质量观察. |
| V37.9.62 | 2026-05-13 | **6 脚本 batch 升级 6 字段 — 用户视角 100% 彻底处理 (Sub-Stage 4b 续完结, ALIGNED_SCRIPTS 11→17 = 81% LLM cron 候选)** — ① **触发**: 用户视角发现 ACL Anthology 推送只有浅 3 字段, 要求按 S2 (V37.9.39+) 6 字段格式 + "100% 彻底 check 还有哪些任务请一并处理, 不要有遗漏". audit 报告 10 unaligned 中 6 个适合升级 (4 P1 机械迁移 + 2 P2 含定制). ② **batch 6 脚本** (并行 6 agents + 主线 audit + 测试 alternation): (a) `jobs/acl_anthology/run_acl_anthology.sh` (391→728 行) 机械迁移 V37.9.51 dblp papers 模板, ACL 有 abstract (rule_content = title + abstract + venue) (b) `jobs/karpathy_x/run_karpathy_x.sh` (420→773 行) 机械迁移 ai_leaders_x V37.9.51 tweets 模板 (单账号, rule_content = author + text) (c) `jobs/openclaw_official/run.sh` (214→683 行) 机械迁移 rss_blogs V37.9.51 blogs 模板, GitHub releases (rule_content = title + body) (d) `jobs/openclaw_official/run_discussions.sh` (179→728 行) 机械迁移 hn V37.9.51 threads 模板, GitHub Issues+Discussions (rule_content = title + body) (e) `jobs/ontology_sources/run_ontology_sources.sh` (449→678 行) + `ontology_parser.py` 扩, 机械迁移 V37.9.51 但 V37.8.7 separator 切块设计完整保留 + parse_6field_output 扩展, 4 RSS source / 关键词过滤 / Discord #ontology 路由全保留 (f) `jobs/chaspark/run_chaspark.sh` (267→772 行) **混合定制设计** Phase A per-article 6 字段 (V37.9.51 rss_blogs 同款) + Phase B 跨域分析 (V37.9.33 freight 三层启发, 保留 chaspark 跨文章关联洞察特色), API 抓取 + 去重 + KB 归档全保留. ③ **ALIGNED_SCRIPTS 11→17** (audit 报告 21 候选 - 4 by design 排除 = 17 应对齐). by design 排除: finance_news (V37.8.5 定制 Syndication API 多通道) / freight_watcher (V37.9.33 三层结构客户画像) / kb_dream (V36.1 KB 内部 MapReduce 不推送) / kb_inject (KB 内部摘要不推送). ④ **测试 alternation 修**: test_ontology_parser (V37.8.7 24 tests 兼容 6 字段) + test_llm_cron_audit (alignment 计数) + governance INV-ONTOLOGY-001 pattern 升级. ⑤ **累计**: 82 suites 不变 / 2785 tests 不变 (alternation 修, 没新增 batch 测试) / 0 fail / governance v3.40 不变 (无新 INV, alternation 升级) / 75 invariants 不变 / 609 checks 不变 / 19 meta rules 不变 / 安全 95/100 / VERSION 0.37.9.61 → 0.37.9.62. ⑥ **元价值**: V37.9.45 (hf_papers PoC) → V37.9.50 (S2 PoC) → V37.9.51 batch (6 脚本) → **V37.9.62 batch (6 脚本)** = Sub-Stage 4b 完整闭环. 21 LLM cron 候选 17 已对齐 (81%), 其余 4 by design 排除. 用户视角 "100% 彻底处理不遗漏" 兑现. ⑦ **Mac Mini 部署后预期 (用户视角验证)**: ACL Anthology 下次周三 09:30 cron 触发推送应**首次出现** 📌中文标题/🔑核心贡献/💡关键方法/🎯实践启发/⭐评级/🎚️项目对齐度 6 字段 + 高对齐统计 (取代浅 3 字段血案). karpathy_x 09:00,21:00 / openclaw_official 同款 / ontology_sources 10:00,20:00 / chaspark 11:00 都升级. ⑧ **下次 V37.9.63+ 候选**: (a) 全部 6 脚本 Mac Mini 真激活验证用户视角推送质量 (b) finance/freight/kb_dream/kb_inject 4 个 by design 排除评估 (c) MR-8 抽 cron_monitor_fatal_handler.sh 公共 helper (V37.9.61 7 governed scripts copy-paste). |
| V37.9.61 | 2026-05-13 | **MR-19 扩到 LLM-task 类 — V37.9.60-hotfix3 揭露的 framework gap 闭环 (3 LLM-task 脚本合规 + cron_monitor_scanner 监管范围 4→7)** — ① **触发**: V37.9.60-hotfix3 同日早用数据驱动诊断 (fallback_chain + log + bash -x + deep_dives 5 文件 + crontab 5 维度) 定位 V37.9.21 我引入的 silent abort 真因 (send_wa_parts && sleep 短路 → set -e 杀 caller → kb_deep_dive 5/8-5/12 5 天 last_run 不更新). 揭露 V37.9.60 framework scope 设计缺口: SCRIPTS_REQUIRING_ERR_TRAP 只覆盖 cron 聚合监控类 (4 个), 遗漏 LLM-task 类 (kb_deep_dive/kb_dream/kb_evening/kb_review) — 这些脚本同款 set -euo + 推 [SYSTEM_ALERT] + 缺 trap ERR 风险. ② **V37.9.61 framework 化扩展** (严格 #28 最小修复): (a) 修 3 LLM-task 脚本 (kb_deep_dive / kb_evening / kb_review): set -euo → set -eEuo (加 -E errtrace, V37.9.58-hotfix4 教训) + 各加 inline `_<script>_fatal_handler` 函数推 [SYSTEM_ALERT] + 三层 FAIL-OPEN (notify.sh → openclaw 直发 → 本地 .openclaw_alerts.log) + `trap '_<script>_fatal_handler $LINENO' ERR` 注册. (b) cron_monitor_scanner.py SCRIPTS_REQUIRING_ERR_TRAP 4→7 (新增 kb_deep_dive.sh / kb_evening.sh / kb_review.sh). kb_dream.sh 不加 (V37.4.x retry/fallback 已含复杂 fail-fast 自身). ③ **测试**: test_cron_monitor_scanner.py 46 单测 alternation 兼容 SCRIPTS 4→7 + 反向验证 sabotage 3 LLM-task 立即抓 violation. ④ **累计**: 82 suites 不变 / 2785 tests 不变 (alternation 修, 没新增 batch 测试) / 0 fail / governance v3.40 (INV-CRON-MONITOR-001 监管范围 4→7) / VERSION 0.37.9.60 → 0.37.9.61. ⑤ **元价值**: V37.9.60 cron 聚合监控类 MR-19 合规 → V37.9.61 LLM-task 类 MR-19 合规. framework "MR-19 一个契约 (err_trap_handler) 覆盖 cron 类 + LLM-task 类共 7 脚本" 完整闭环. ⑥ **下次 V37.9.62+ 候选**: (a) Mac Mini 部署后 3 个 LLM-task ERR trap 真激活验证 (b) MR-8 抽 cron_monitor_fatal_handler.sh 公共 helper (7 个脚本 copy-paste 已构成 MR-8 反例信号). |
| V37.9.60 | 2026-05-13 | **MR-19 err_trap_handler 契约横向推广 — V37.9.58-hotfix3 立 MR-19 后第一次 framework 化扩展** — ① **触发**: V37.9.58-hotfix3 (2026-05-12) 立 MR-19 (monitor-must-self-alarm-on-silent-abort) 时只把 job_watchdog.sh 升级为合规, 收工候选明确登记 "扫所有 cron 监控脚本是否满足 MR-19 三契约". 用户 5/13 开工选 "MR-19 三契约扫描" 路径 (vs 元监控 cron_canary 外部检测 / 监控覆盖率 100% / 用户视角观察 Mac Mini 真激活). ② **scope 收敛**: V37.9.60 = err_trap_handler **一个契约** 横向推广 (awk_safe 仅适用处理 log 的 awk pipe / canary_writer 仅适用 watchdog 类聚合监控, 都是 watchdog-specific 不普适). MR-19 治理对象 4 个: job_watchdog.sh (V37.9.58-hotfix3 已合规) / governance_audit_cron.sh / daily_ops_report.sh / auto_deploy.sh. **MR-19 不适用** 4 个: wa_keepalive (V37.9.59 LOG_FRESHNESS_JOBS 已通过 log mtime 监控) / cron_canary (本身是 heartbeat writer) / health_check (周报无 set -e) / preflight_check (user-interactive 诊断, fail-fast 给用户看是预期). ③ **新文件 `cron_monitor_scanner.py`** (~200 行纯 Python, 启发式 scanner 不依赖 AST): `SCRIPTS_REQUIRING_ERR_TRAP` 4 个 governed 脚本 + 3 个纯函数 `has_set_e_strict` (regex `^\s*set\s+-[a-zA-Z]*e[a-zA-Z]*`) / `has_err_trap` (单行内 trap 后任意位置出现 ERR 词边界 + 跳过注释行) / `has_system_alert_marker` (整脚本 `[SYSTEM_ALERT]` 字面量出现, 简化检查) + `scan_script` / `scan_repo` / `main` CLI (`--list` / `--file` / 默认全量). **FAIL-CLOSE 契约**: 任一 violation 必须 exit 1. **dev scan 立即抓出 3 个真违反** — governance_audit_cron.sh / daily_ops_report.sh / auto_deploy.sh 都是 `set -e*` + 无 trap ERR, V37.9.58-hotfix3 同款盲点. ④ **3 个脚本修复 (inline 模式, V37.9.60 PoC 选择)**: 每个加 (a) set -e* → set -eE* (加 -E errtrace 让 function 内 fail 也触发 ERR trap, V37.9.58-hotfix4 教训) (b) 定义 `_<script>_fatal_handler` 函数推 [SYSTEM_ALERT] 内含 exit_code / line_no / 排查提示 + 三层 FAIL-OPEN: notify.sh → openclaw 直发 → 本地 ~/.openclaw_alerts.log 写入 (c) `trap '_<script>_fatal_handler $LINENO' ERR` 注册. MR-8 single-source-of-truth 讨论: V37.9.60 PoC 4 个脚本 copy-paste handler 可接受; 未来扩展到 N 个监控时考虑抽 helper (V37.9.61+ 候选). ⑤ **INV-CRON-MONITOR-001** 新增 (meta_rule=MR-19, severity=high, verification_layer=[declaration, runtime], **18 checks**): 16 declaration (scanner V37.9.60 marker / FAIL-CLOSE / MR-19 引用 / 4 个 governed 脚本字面量 + 3 个修复脚本各 3 守卫: fatal_handler 名 + V37.9.60 marker + set -eE*) + 2 runtime python_assert (subprocess 跑 scanner 0 violations + subprocess 跑 46 单测全过). ⑥ **test_cron_monitor_scanner.py 46 单测 8 类** + **反向验证 4 类**: TestHasSetEStrict(11 含 set -e/eo/euo/eEo/eEuo/ex + set -u 单独无 e 不算) + TestHasErrTrap(9 含 trap '' ERR / 含 EXIT 不算 / 多信号 ERR / 注释行豁免) + TestHasSystemAlertMarker(4) + TestScanScript(5 含合规无 findings / missing trap / trap 无 alert / 无 set -e 不强制 / 文件不存在) + TestScanRepoIntegration(2 含 V37.9.60 修复后全合规 + SCRIPTS list 锁定) + **TestReverseVerificationGuardsAreReal(4 反向验证)** sabotage 4 个 governed 脚本立即触发 violation + 还原后再次合规 (job_watchdog/governance_audit/daily_ops/auto_deploy 各一) + TestCliBehavior(3) + TestSourceLevelGuards(8 V37.9.60 marker / MR-19 引用 / FAIL-CLOSE 契约 / SCRIPTS 锁 / V37.9.58-hotfix3 lineage / 3 脚本 V37.9.60 marker + handler 名 + -E errtrace). 反向验证已确认守卫真有效: sabotage governance_audit_cron 删 trap ERR → test 立即抓 missing_err_trap, 还原后合规. ⑦ **接入**: full_regression.sh +1 suite `cron_monitor_scanner` 紧随 V37.9.59 watchdog_coverage. test_convergence.py audit_metadata version alternation 加 `version: "3.39"`. **不加 FILE_MAP**: scanner 是 dev/CI tool, governance_audit_cron 从 repo dir 跑 (`cd $REPO_DIR && python3 ontology/governance_checker.py --full`), scanner 在仓库根目录即可访问 (V37.9.58-hotfix2 heredoc_import_scanner 同款模式). ⑧ **累计**: 81→82 suites (+1: cron_monitor_scanner) / 2757→2803 tests (+46) / 0 fail / governance v3.38→v3.39 / 74→75 invariants (+1 INV-CRON-MONITOR-001) / 582→600 checks (+18) / 19 meta rules 不变 (MR-19 在 V37.9.58-hotfix3 立案) / 安全 95/100 / VERSION 0.37.9.59 → 0.37.9.60. ⑨ **Mac Mini 部署后预期**: (a) `cd ~/openclaw-model-bridge && git fetch origin main && git reset --hard origin/main` 同步 V37.9.60 (b) **3 个修复脚本立即生效**: 下次 governance_audit_cron (每日 07:00) / daily_ops_report (每日 08:15) / auto_deploy (每 2min) 触发时如有 set -e abort, ERR trap 主动推 [SYSTEM_ALERT] 到 Discord (不再 silent), 用户立即可见 (c) governance audit cron 同时跑 INV-CRON-MONITOR-001 runtime check, 任一 governed 脚本未来回退到 silent 模式立即被 audit 抓到. ⑩ **下次 V37.9.61+ 候选**: (a) 一周观察 3 个新 trap ERR 真激活效果 (Discord 应看到首条 FATAL alert if 任一脚本 silent abort) (b) MR-8 single-source-of-truth: 若未来扩展到 N 个监控考虑抽 `cron_monitor_fatal_handler.sh` 公共 helper (c) MR-21 候选 `monitor-coverage-must-track-registry` (V37.9.59 立案准备, baseline 5/12 + 7d = 5/19 一周观察后立 INV-WATCHDOG-COVERAGE-001) (d) 元监控 cron_canary 外部检测 watchdog_canary.json (chicken-and-egg 真闭环) (e) V37.9.30 EPERM 24h 数据回归 + V37.9.35 推送质量观察. ⑪ **元价值**: V37.9.58-hotfix3 立 MR-19 (watchdog 单点合规) + V37.9.59 watchdog 覆盖率扩展 (LOG_FRESHNESS) + **V37.9.60 MR-19 框架化推广** (4 个脚本批量合规 + scanner 主动监控) = 三阶段闭环 silent monitor 防御 framework. 未来新增 cron 类聚合监控只需注册到 SCRIPTS_REQUIRING_ERR_TRAP 即被 governance audit 强制 enforce, V3 路标 `pip install ontology-engine` 核心交付方向 — declarative monitor governance 用机器化契约取代记忆维护成本. mirror V37.9.58-hotfix2 heredoc_import_scanner 同款 framework 化模式 (MR-18 立案后 framework scanner 落地). |
| V37.9.59 | 2026-05-12 | **watchdog 监控覆盖率扩展 47%→74%+** — 用户 5/12 17:30 询问 "watchdog 是否监控所有任务" 驱动. job_watchdog.sh JOBS 数组 +4 jobs (`kb_deep_dive` / `kb_dream` / `chaspark` / `governance_audit`, 都有 last_run.json 写入). 新加 `LOG_FRESHNESS_JOBS` 数组 +11 jobs (`auto_deploy` / `wa_keepalive` / `cron_canary` / `kb_embed` / `kb_trend` / `kb_status_refresh` / `mm_index` / `daily_ops_report` / `openclaw_backup` / `preference_learner` / `health_check`) — 用 log mtime 替代 last_run, 粗粒度但比完全不监控好. `check_log_freshness()` 函数实现 stat -f %m + tier 分级 + ALERTS 追加. STALE_LOCK_DIRS 补 kb_dream + chaspark (18→20). **Watchdog 自监控** (V37.9.58-hotfix3 元监控盲区上游): 检查 `~/watchdog_canary.json` mtime > 12h 触发 [SYSTEM_ALERT] 通过 alerts 频道. **22 单测** test_watchdog_coverage.py 6 类 (JOBS expansion / LOG_FRESHNESS / self-monitor / STALE_LOCK / 覆盖率 ≥25 / source-level marker). 反向验证: V37.9.58-hotfix3 sabotage 测试改 replace 所有出现 (V37.9.59 加重复字面量后单点 replace 失效). 累计 80→81 suites (+1) / 2735→2757 tests (+22) / 0 fail / governance v3.38 不变 (V37.9.60+ 立 INV-WATCHDOG-COVERAGE-001 PoC 阶段) / 安全 95/100 / VERSION 0.37.9.58→0.37.9.59. **元价值**: V37.9.58 五件套主要修 watchdog 自身 silent abort, V37.9.59 治理 watchdog 监控盲区. V37.9.58 + V37.9.59 共同闭环今日用户两个深问 (为什么不告警 + 是否监控所有任务). **下次 V37.9.60+ 候选**: (a) 监控覆盖率达 100% (剩 4 个: openclaw_official/discussions 已 disable / hn_watcher 已含 / 等) (b) 立 INV-WATCHDOG-COVERAGE-001 + MR-21 candidate `monitor-coverage-must-track-registry` (一周观察后) (c) 元监控 cron_canary 外部检测 watchdog_canary.json 真正 chicken-and-egg 防御. |
| V37.9.58-hotfix5 | 2026-05-12 | **V37.9.27 rsync 输出可观察性补丁 — 用户视角发现 watchdog 告警 "(时间戳缺失)" 8 个 jobs 噪声驱动** — ① **触发**: V37.9.58-hotfix4 Mac Mini 真激活后用户 WhatsApp 收到 17 条累积告警, 敏锐发现 8 个 jobs 告警全显示 "(时间戳缺失) → rsync(N): error: unexpected end of file", 反问 "时间戳却是？？？". ② **真因**: V37.9.27 movespeed_rsync_helper.sh line 99 `rsync "$@" 2>&1` 直接转发 rsync 自身错误格式 (e.g. "rsync(47596): error: ..."), **不带时间戳前缀**. 进 caller log 后 V37.9.28 F1 watchdog scan_logs err_ts 提取 `[YYYY-MM-DD HH:MM]` 模式失败 → 告警显示 "(时间戳缺失)". ③ **三问全过 (原则 #28)**: Q1 之前存在 ✓ (V37.9.27 helper 引入时就这样, 4 周前) / Q2 我引入 ✗ (设计漏洞非 hotfix4 引入) / Q3 最小修复 = helper rsync 输出加 awk pipe timestamp prefix + PIPESTATUS[0] 保 rsync exit code. ④ **修复 (2 行)**: `RSYNC_TS="$(date '+%Y-%m-%d %H:%M:%S')"` + `rsync "$@" 2>&1 \| awk -v ts="$RSYNC_TS" '{print "[" ts "] " $0}'` + `EXIT_CODE=${PIPESTATUS[0]}` (rsync exit, 不是 awk). ⑤ **意外验证已通过**: 用户 5/12 17:02 误把示例代码块复制到 zsh 直接执行 (没 args 给 rsync), rsync 输出 usage 30 行, awk 给每行加 `[2026-05-12 17:02:06]` prefix — V37.9.58-hotfix5 awk timestamp pipe **实测工作**. ⑥ **下次 watchdog 告警预期对比**: 旧"(时间戳缺失) → rsync(N): error: ..." → 新"(最早 X, 最新 Y) → [HH:MM:SS] rsync(N): error: ...". ⑦ **接入**: 全量回归 80 suites / 2735 tests / 0 fail (V37.9.27 25 单测全兼容). 不动 governance INV-RETRY-001 (helper 行为契约不变, 只是输出格式改进) + 不立新 MR. ⑧ **今日 V37.9.58 六件套完整闭环**: dc1131d 主 / a4f71a5 hotfix / 69bd3cd hotfix2 / ae84298 hotfix3 / 46bf5a4 hotfix4 / 068eec3 hotfix5 — 一天内连续 6 commits 把 silent failure 从业务层 → 治理层 → 监控层 → 落地兜底 → 可观察性补丁五层逐层闭环. ⑨ **元教训**: 没有用户 5/12 两次深问 ("为什么静默失败不提前告警" + "时间戳却是？？？") → 六件套至少要拖 2-3 天分多次 session 才完成. 用户视角第 N 次正向兑现. |
| V37.9.58-hotfix4 | 2026-05-12 | **修 V37.9.58-hotfix3 两个落地漏洞 — set -E + caller || ALERT (Mac Mini 实测才暴露)** — ① **触发**: V37.9.58-hotfix3 部署 Mac Mini 后用户验证, watchdog 仍 exit 1 + canary 文件未写, **ERR trap 没触发 [SYSTEM_ALERT]** — V37.9.58-hotfix3 设计正确但落地漏点. ② **真因 (按原则 #28 三问)**: 漏洞 1: `set -eo pipefail` 缺 `-E` (errtrace) — bash 默认 `set -e` 在 function 内 fail **不传播 ERR trap** → fatal_handler 不调用 → 仍 silent abort. 漏洞 2: scan_logs 内 V37.9.28 F1 加的 `err_ts=$(... \| grep -iE ... \| grep -oE ... \| sort -u)` 缺 `\|\| true` 容错, recent_window 无错误行时 grep 无匹配 exit 1 + pipefail + set -e → 整 watchdog abort. ③ **三项修复**: (1) `set -eo pipefail` → `set -eEo pipefail` (加 -E errtrace 让 ERR trap 在 function 内生效) (2) err_ts pipe + last_err pipe 加 `2>/dev/null \|\| true` 容错 (3) `for entry in HOME_LOGS: scan_logs ... \|\| ALERTS+=("$job_name: scan_logs internal 异常")` — 让 scan_logs internal fail 可观察但**不让整 watchdog abort**, 让后续 7 维度都能跑. ④ **dev 验证 V37.9.58-hotfix4 真激活**: exit 0 (V37.9.58-hotfix3 是 1) / 56 行输出 (V37.9.58-hotfix3 是 1) / 跑完整 8 维度 / ALERT 真推送 (含 MOVESPEED 48 次 / Qwen context 90% / 多 last_run 不存在等). ⑤ **Mac Mini 实测 17:00 真激活成功**: watchdog exit 0 + 33 行 + canary 文件创建 (checks=22 alerts=17) + WhatsApp 收到 [SYSTEM_ALERT] 17 条 7 天累积告警 (SLO p95 59.5s / MOVESPEED 14 次 / RSS 46h / ai_leaders 46 错误 / 等). 5/5 16:30 起 silent 7 天血案完整闭环. ⑥ **接入**: 31 单测 test_watchdog_self_monitoring (V37.9.58-hotfix3 29 + V37.9.58-hotfix4 2 新守卫: test_set_E_errtrace_present + test_scan_logs_caller_has_alert_fallback) + alternation 接受 set -eEo / set -eo. governance v3.38 不变 (V37.9.58-hotfix4 不新增 INV/MR, 是 hotfix3 落地修复). 累计 80 suites / 2735 tests / 0 fail. ⑦ **副产物 noise**: SLO 检查 line 591 `SLO_ALERT=$(...) && X \|\| Y` idiom 在 set -eE 下产生 false FATAL alert (bash quirk: `&& \|\|` 连接不杀脚本但 ERR trap 仍触发) — V37.9.58-hotfix5+ 候选, 不影响功能只是 noise. ⑧ **元教训 (反思转化为机制)**: V37.9.58-hotfix3 dev 29 单测全过 + 反向验证守卫真有效, **不能替代 Mac Mini 真生产环境实测** — bash 3.2 quirk (set -e + function 内 fail 不传播 ERR trap) 只有真跑才暴露. 原则 #15 测试三层 (单测/dev/Mac Mini E2E) + 用户视角第 N 次正向兑现. MR-20 候选 `framework-level fix must validate in target environment, not dev` 一周观察后立 (MR-15 deployment-layout 同款血案兄弟). |
| V37.9.58-hotfix3 | 2026-05-12 | **治本 watchdog silent abort 7 天血案 — MR-19 立案 + INV-WATCHDOG-SELF-001 framework 级监控自观察 (用户深问"为什么静默失败不提前告警"驱动)** — ① **触发**: V37.9.58-hotfix2 commit 后用户反馈"今天定时任务推送只收到 HN+HF, 其他 5 个没有"+ 深问"这是静默错误为什么不提前告警". 按原则 #28 三问严格诊断: Mac Mini `cat ~/.openclaw/jobs/*/cache/last_run.json` + `ls /tmp/*.lockdir` + `tail ~/job_watchdog.log` 揭示 4 lockdir 残留 (hf_papers/s2/rss_blogs/github_trending, 残留 5h-45h) + watchdog log 从 5/5 16:30 起**只有 heartbeat 没有 ALERT** (累积 28 次 cron 跑全部 silent). 手动跑 `bash ~/job_watchdog.sh` 立即暴露真因: `awk: towc: multibyte conversion failure on: '?'` exit 1 + `set -eo pipefail` + 无 ERR trap = watchdog 整脚本 abort exit 2. ② **完整因果链 (silent 7 天)**: 5/5 16:30 某 log 写入含无效 UTF-8 字节 (V37.8.6 surrogate 类 / X tweet 特殊字符) → macOS bsd awk `tail -200 \| awk` 处理时 `towc multibyte` exit 1 → `pipefail` 让 pipe exit 1 → `set -e` 让 watchdog 整脚本 abort exit 2 → 仅 heartbeat 一行 + awk error stderr 写到 log → 没 ERR trap 主动告警 → cron 不发邮件 → **用户视角 7 天未感知**. 累积影响: 4 lockdir 残留 (没人清), MOVESPEED 24h 21 次 rsync 失败累积告警 (没推送), SLO p95 54s 严重超阈值 (没推送), KB 摘要 29h 未更新 (没推送) — **全部 silent**. ③ **V37.9.58-hotfix3 治本三层 + 框架级 5 项扩展**: **Step A** (核心修): `line 264` awk pipe 加 `LC_ALL=C` 前缀 + `2>/dev/null \|\| true` 容错防 set -e abort. **Step B** (主动告警): 顶部装 `_watchdog_fatal_handler($LINENO)` ERR trap handler 推 `[SYSTEM_ALERT] watchdog FATAL exit=N line=N — 监控自身死亡! 5/5-5/12 silent 7 天血案防回归` 到 Discord. **三层 FAIL-OPEN fallback**: notify 函数 → openclaw 直发 → 本地 .openclaw_alerts.log 写入. **Step C** (元监控): `_watchdog_exit_handler()` 合并 rmdir + canary heartbeat — 正常完成时写 `~/watchdog_canary.json` (含 last_completed/checks/alerts) + ALERTS=0 时推 `✅ [watchdog alive @ HH:MM]` 到 Discord. 让 watchdog 自身死活可被外部检测 (12h 没收到 alert 也没收到 canary → watchdog 死了). **Step D** (监控覆盖补齐): STALE_LOCK_DIRS 加 5 个 jobs (`rss_blogs / github_trending / ai_leaders_x / finance_news / ontology_sources`) — 5/12 诊断发现 rss_blogs.lockdir 残留 45h 实证不在监控列表. **Step E**: `governance INV-WATCHDOG-SELF-001` (meta_rule=MR-19, severity=critical, layer=[declaration, runtime], **12 checks**): 11 declaration (awk LC_ALL=C / 容错 `|| true` / _watchdog_fatal_handler / trap ERR / [SYSTEM_ALERT] / watchdog_canary.json / 'watchdog alive' / rss/gh/ai_leaders STALE 补齐 / V37.9.58-hotfix3 marker) + 1 runtime (subprocess 跑 test_watchdog_self_monitoring 全套). **Step F**: 立 **MR-19** `monitor-must-self-alarm-on-silent-abort` (governance_ontology.yaml meta_rules 段): 任何 silent failure 监控工具必须三契约 — awk_safe + err_trap_handler + canary_writer. 关联 MR-4 (silent-failure 元层) + MR-7 (治理自观察) + MR-17. derivative_invariants=[INV-WATCHDOG-SELF-001]. blood_lessons 详记 V37.9.58-hotfix3 完整因果链 + Q1/Q2/Q3 三问. ④ **test_watchdog_self_monitoring.py (29 单测 / 7 测试类)**: TestStepAAwkMultibyteSafety(3 含 LC_ALL=C 字面量 + `|| true` regex + V37.9.58-hotfix3 注释) + TestStepBErrTrapSilentAbortBecomesLoud(5 含 handler 函数定义 + trap ERR 注册 + [SYSTEM_ALERT] 推送 + 三层 FAIL-OPEN fallback + 血案引用) + TestStepCCanaryHeartbeat(6 含 _watchdog_exit_handler + trap EXIT + canary 文件 + alive 推送 + exit=0 守卫 + JSON 字段完整性) + TestStepDStaleLockListCompleteness(5 含 rss_blogs/github_trending/ai_leaders_x/finance_news/ontology_sources 字面量) + TestWatchdogSyntaxAndIntegration(3 含 bash -n + set -eo pipefail 保留 + trap ERR 必须在 set -e 之后) + **TestReverseVerificationGuardsAreReal(3 反向验证)**: sabotage 移除 LC_ALL=C → 守卫立即 fail / sabotage 移除 trap ERR → 守卫立即 fail / sabotage 移除 canary → 守卫立即 fail (subprocess 真跑各测试类). 还原后全过. + TestGovernanceLinkage(4 含 MR-19 立案 + INV-WATCHDOG-SELF-001 立案 + meta_rule 引用 + audit_metadata v3.38). ⑤ **接入 + audit_metadata**: full_regression.sh +1 suite `watchdog_self_monitoring` 紧随 V37.9.58-hotfix2. audit_metadata version 3.37→3.38 + total_invariants 73→74 (+1 INV-WATCHDOG-SELF-001) + total_checks 570→582 (+12) + meta_rules 18→19 (+1 MR-19). ⑥ **累计**: 79→80 suites (+1) / 2704→2733 tests (+29) / 0 fail / governance v3.37→v3.38 / 73→74 invariants / 570→582 checks (+12) / 18→19 meta rules / 安全 95/100 / VERSION 0.37.9.58 不变 (hotfix 不 bump). ⑦ **元价值 — 今日四件套闭环 (一日内 V37.9.58 主+hotfix+hotfix2+hotfix3 四个版本)**: 每个版本都把 silent failure 向上一层抽象修. **V37.9.58 主交付**: machine_sync 真激活 (业务自动同步). **V37.9.58-hotfix**: 业务层 silent (8 jobs NameError 不推送). **V37.9.58-hotfix2**: 治理层 silent (MR-18 + scanner framework 预防). **V37.9.58-hotfix3 (本)**: 监控层 silent (MR-19 + watchdog 自观察). 最深层 — **监控自身死活监控**. V3 路标 `pip install ontology-engine` 终极目标的核心交付之一. MR-19 让"watchdog 自身死亡 7 天无人发现"从靠运维记忆升级为机器化主动告警 + canary 双层防御. ⑧ **Mac Mini 部署后预期行为**: (a) `cd ~/openclaw-model-bridge && git fetch origin main && git reset --hard origin/main` 同步 V37.9.58-hotfix3 (b) **手动清 4 个残留 lockdir** (rss_blogs/github_trending/hf_papers/semantic_scholar) — V37.9.58-hotfix3 部署后下次 watchdog cron (16:30/20:30) 真激活 STALE 检测时会自动清, 但用户可立即手动清让今晚 18:00+ cron 能跑 (c) 下次 watchdog cron (16:30) 应跑全 8 维度 + 推大量累积告警 (MOVESPEED/SLO/KB 摘要等) (d) 推送 ALERT 后 ALERTS 重置, 之后每次正常 cron 推 canary 'alive' 到 Discord (e) 如未来 watchdog 再因新 silent bug abort → ERR trap 主动推 [SYSTEM_ALERT] 立即可见. ⑨ **下次 V37.9.59+ 候选**: (a) **观察期**: 今晚 16:30 / 20:30 / 明早 08:30 watchdog cron 真激活效果 (推送 canary alive + 累积告警) (b) **`ontology/docs/cases/watchdog_silent_abort_case.md`** 完整四维度因果链案例文档 (V37.9.58-hotfix3 立案后补) (c) **扫所有 cron 类监控脚本** (preflight_check.sh / governance_audit_cron.sh / wa_keepalive.sh) 是否都满足 MR-19 三契约 (d) **trap unlock 8 ALIGNED jobs 普遍不执行 bug** (V37.9.58-hotfix3 候选未修, hf_papers 10:00 推送成功但 lockdir 仍在的现象) — 候选方案 A mkdir 失败时 mtime ≥ 1h 自动清+重试 (e) **ai_leaders X HTTP 429 全部 19 账号** — 5/12 09:00 实测, IP/UA/时间窗口级 ban 调查 + UA 轮换 + backoff (V37.9.59 候选). |
| V37.9.58-hotfix2 | 2026-05-12 | **治理债清零 — MR-18 立案 + INV-HEREDOC-IMPORT-001 framework 级 import 一致性 scanner (V37.9.58-hotfix 反思转化为机制)** — ① **触发**: 用户问"为什么本次错误不应该发生" — V37.9.50-hotfix → V37.9.58-hotfix 同款 import 缺漏 8 天间隔重演证明"单点修复 + 不立元规则 + 不加 framework 守卫" = 必然 MR-4 第 N 次演出. 反思路径：单点 hotfix 是治标，立 framework 级预防才是治本. ② **新文件 heredoc_import_scanner.py** (~280 行纯 Python AST scanner, 零外部依赖): `extract_heredocs` 提取所有 .sh 中 `<< 'PYEOF' ... PYEOF` heredoc body + 行号 + `collect_imported_names` (ast.Import / ast.ImportFrom 含 alias / 子模块 binding `import os.path` → os) + `collect_locally_defined_names` (FunctionDef/AsyncFunctionDef/ClassDef + 函数参数/lambda 参数/Assign/AugAssign/AnnAssign/For/AsyncFor/With/AsyncWith/ExceptHandler/NamedExpr 海象/comprehension/Global/Nonlocal) + `collect_referenced_names` (Name Load + Attribute root chain) + `scan_heredoc_imports` (referenced - imported - defined - builtins = missing) + `scan_repo` 整 repo scan + main CLI `--scan-all` / `--file` / FAIL-CLOSE exit 1 on violation. **FAIL-CLOSE 契约**: 找到任一 violation 必须 exit 1, scanner 自身无法 AST 解析 silently skip 不算 violation 但 verbose 日志 (避免 false positive). ③ **dev scan 立即真有效证据 — 抓出 V37.9.58-hotfix 遗漏的 2 个 LEVEL_2 jobs**: `finance_news/run_finance_news.sh:631` + `ontology_sources/run_ontology_sources.sh:236` 同款 V37.9.57 LEVEL_2 注入 `prompt += os.environ.get('HG_GUARD_TEXT')` 但 heredoc 顶部 `import sys, json, re` 缺 os. scanner 立即抓到 + 修复 + 复扫 0 violations. 还 1 个 false positive (`__import__` builtin 漏豁免) 同时修, 加入 `_PYTHON_BUILTINS` 集合. ④ **MR-18 立案** `auto-batch-injection-must-validate-runtime-semantics` (governance_ontology.yaml meta_rules 段): 任何自动批量改多文件工具必须满足三层契约 — (a) 注入前 dry-run AST 解析 (b) 注入后 semantic validation (c) 失败整批 rollback. 核心论断 "自动批量工具是 bug 放大器" — 单点设计错误立即传播到 N 文件, `bash -n` 永远查不到 Python heredoc 内部 NameError. 元规则关联 MR-4 (silent-failure 上游预防) + MR-6 (critical 需 ≥2 层) + MR-8 (copy-paste-is-a-bug-class 自动版本) + MR-10 (understand-before-fix). 实施路径 Step 1 (V37.9.58-hotfix2) heredoc_import_scanner / Step 2 (V37.9.59+) inject 工具自治化 / Step 3 (V3 路标) ontology-engine pip 包暴露 batch_injection_validator. derivative_invariants=[INV-HEREDOC-IMPORT-001]. blood_lessons 详记 V37.9.50/58-hotfix 谱系. ⑤ **INV-HEREDOC-IMPORT-001** `heredoc-python-imports-must-cover-all-referenced-modules` (meta_rule=MR-18, severity=critical, verification_layer=[declaration, runtime], **5 checks**): declaration 4 (scanner 函数定义 + FAIL-CLOSE 契约 + V37.9.50-hotfix 血案引用 + 单测 TestRepoScanIntegration 类) + runtime 1 (python_assert subprocess 跑 scanner --scan-all 必须 exit 0 = 0 violations). ⑥ **test_heredoc_import_scanner.py** (33 单测 / 6 测试类): TestExtractHeredocs(4 含 unclosed/non-PYEOF skip) + TestCollectImportedNames(4) + TestCollectLocallyDefinedNames(5) + TestCollectReferencedNames(2) + TestScanHeredocImports(7 含 **V37.9.50 + V37.9.58-hotfix 双血案重现** + builtin / 本地定义 / __import__ dynamic / AST parse error 豁免) + **TestRepoScanIntegration(3 含端到端 0 violations + 反向验证 sabotage run_hn_fixed.sh 移除 import os → scanner 立即抓到 missing ['os'] 含 stderr 含 file path 还原 4/4 全过)** + TestCliBehavior(3 含 --help / --file clean / --file violation) + TestSourceLevelGuards(5 V37.9.58-hotfix2 marker + V37.9.50-hotfix lineage + MR-18 引用 + FAIL-CLOSE 契约 + INV-HEREDOC-IMPORT-001 引用). ⑦ **接入**: full_regression.sh +1 suite `heredoc_import_scanner` 紧随 V37.9.58-hotfix 后. audit_metadata 升级 version 3.36→3.37 + total_invariants 72→73 (+1) + total_checks 565→570 (+5) + meta_rules 16→18 (修 V37.9.19 立 MR-17 时漏更新 + 加 MR-18). ⑧ **累计**: 78→79 suites (+1) / 2671→2704 tests (+33) / 0 fail / governance v3.36→v3.37 / 72→73 invariants / 565→570 checks (+5) / 17→18 meta rules / 安全 95/100 / VERSION 0.37.9.58 不变 (hotfix 不 bump). ⑨ **元价值 (反思转化为机制)**: V37.9.58-hotfix 是治标 (单 session 修 8 jobs broken push), V37.9.58-hotfix2 是治本 (立 framework 级预防 + 主动监控). 用户的"为什么本次错误不应该发生"质问驱动: (1) V37.9.50-hotfix 教训没转化为 framework 预防 → V37.9.58-hotfix 重演 (2) 自动批量工具是新 bug 类 → 立 MR-18 (3) scanner 必须 FAIL-CLOSE 主动监控而非 FAIL-OPEN 被动等下次血案. 这是 V3 路标 `pip install ontology-engine` 核心交付 — declarative governance framework 真正进化方向是"用机器化契约取代记忆维护成本". ⑩ **Mac Mini 部署后预期**: (a) git fetch + reset 同步 V37.9.58-hotfix2 (b) 下次 governance_audit_cron (每日 07:00) 自动跑 INV-HEREDOC-IMPORT-001 runtime check, 整 repo 0 violations 则零告警 (c) 任何未来 ALIGNED jobs 或新增 heredoc 引入 import 缺漏 → governance audit 立即抓到 + Discord #alerts 告警, 不再等用户视角发现 (d) inject_level_4_to_aligned_jobs.py 等批量工具下次使用前应先调 scanner --file dry-run (V37.9.59 候选: 自治化). ⑪ **下次 V37.9.59 候选**: (a) inject_level_4_to_aligned_jobs.py 自治化 (注入前后内嵌 scanner check + 失败整批 rollback) (b) 同款 audit 扩展: 扫所有 jobs 是否还有其他批量注入跨多 jobs 但漏 import 的盲点 (c) governance audit cron 集成 INV-HEREDOC-IMPORT-001 主动告警 (Mac Mini 真生效一周后立 INV-AUTO-INJECT-001 上游守卫) (d) Mac Mini V37.9.58 + V37.9.58-hotfix + V37.9.58-hotfix2 全量真激活验证 (一周观察期). |
| V37.9.58-hotfix | 2026-05-12 | **V37.9.57 自动注入工具盲点 — 8 ALIGNED jobs HG_LEVEL_4_TEXT 注入未补 import os 系统性 NameError 血案修复 (与 V37.9.50-hotfix 同款 fix 但 V37.9.57 引入第二处缺漏)** — ① **用户视角发现 (原则 #13 第 18 次正向兑现)**: 用户 WhatsApp 收到 HN 14:45 cron 推送格式异常 — 5 条都是仅 `*英文标题*` + `链接: ...` + `---`, 完全无 V37.9.51 的 6 字段 (📌中文标题/🔑核心要点/💡技术深度/🎯实践启发/⭐评级/🎚️项目对齐度) + 末尾`高对齐 0/5 条`虚假 0 统计. 立即反馈"格式不对"启动诊断. ② **取证 (原则 #28 三问严格执行)**: Mac Mini `tail ~/.openclaw/logs/jobs/hn_watcher.log` 揭示 `Traceback ... NameError: name 'os' is not defined line 70 in <stdin>` × 5 次 (5 条 LLM 调用全部炸) + `[hn] LLM 调用完成: 成功 5/5` 误报 (retry 把 NameError 当成空 content 返回成功) + `[hn] 消息组装: 5 条 (LLM 解析成功 0, degraded 0, 高对齐 0, sent 5)` (parse_6field_output("") 全空 → fallback 用原 title). ③ **真因**: V37.9.57 `inject_level_4_to_aligned_jobs.py` 自动给 8 个 ALIGNED jobs 的 LLM call heredoc (prompt-generation 段, run_hn_fixed.sh line 282 起) 末尾加了 `prompt += os.environ.get('HG_LEVEL_4_TEXT', '')` 但**没补 `import os`**. V37.9.50-hotfix 修过另一处 emit heredoc (line ~388) 补 os, 但 V37.9.57 在 prompt-generation heredoc (line ~282 在 emit 之前) 引入第二处 os 调用未同步补齐. 7/8 jobs 都有同款 bug (只 HN-specific run_hn_fixed.sh 因 V37.9.51 prompt heredoc 起点已含 re 字段巧合不同, 但 V37.9.57 注入后同样缺 os). ④ **三问全过**: Q1 之前存在? ✗ (V37.9.57 之前 prompt heredoc 不调 os.environ) / Q2 我引入? ✓ (V37.9.57 inject 工具系统性) / Q3 最小修复 = 7 个 jobs heredoc 顶部 `import sys, json` → `import sys, json, os` (V37.9.50-hotfix 同款一字不差). ⑤ **批量修复 + 验证**: Python 自动定位每个 jobs 含 `prompt += os.environ.get('HG_LEVEL_4_TEXT'` 的 prompt-generation heredoc, 验证 import line `startswith("import sys, json")` 后插入 `, os` + 行尾 V37.9.58-hotfix marker 注释. 7/7 修复 + 8/8 全部含 os 验证 + bash -n 语法全过. **修复清单**: run_hn_fixed.sh:283 (此前手动 fix) + hf_papers:373 + arxiv:342 + semantic_scholar:328 + dblp:335 + github_trending:328 + rss_blogs:322 + ai_leaders_x:347. ⑥ **`test_v37_9_58_hotfix.py` 4 tests source-level 守卫** (机器化检测 V37.9.57 注入工具盲点): TestV37958HotfixOsImport.(test_v37_9_57_injection_present_in_all_8_jobs 8 jobs 都含 HG_INJECTION 字面量 / test_every_hg_injection_heredoc_has_os_import_at_top 核心守卫 — 每个含 HG 注入的 heredoc 顶部必须含 `import os` (无论组合形式, regex `\bos\b` 在 code 部分检查跳过注释) / test_hn_specific_fix_at_line_283 锁定 HN line 283 / test_v37_9_58_hotfix_marker_in_at_least_one_fix 至少一个修复点含 marker). **反向验证已确认守卫真有效**: sabotage run_hn_fixed.sh line 283 移除 os → test_every_hg_injection_heredoc_has_os_import_at_top 立即 fail + 错误信息精确定位"run_hn_fixed.sh: heredoc@line 282 import line 283 = 'import sys, json, re'", 还原后 4/4 全过. ⑦ **接入**: full_regression.sh +1 suite `v37_9_58_hotfix` 紧随 V37.9.57 hallucination_guards 之后. ⑧ **累计**: 77→78 suites (+1) / 2667→2671 tests (+4) / 0 fail / governance v3.36 不变 / VERSION 0.37.9.58 不变 (hotfix 惯例不 bump) / 安全 95/100. ⑨ **Mac Mini 部署后预期** (用户视角验证): (a) `cd ~/openclaw-model-bridge && git fetch origin main && git reset --hard origin/main` 同步 V37.9.58-hotfix (b) 下次 cron 触发 (HN 20:45 / hf_papers 20:00 / arxiv 每 3h / s2/dblp 明日) 推送应**首次出现** 6 字段格式 (📌中文标题 + 🔑核心要点 + 💡技术深度解读 + 🎯实践启发 + ⭐评级 + 🎚️项目对齐度) + 末尾`本轮高对齐 X/Y 条`真统计 (不再是虚假 0). (c) 用户视角原则 #13 第 18 次正向兑现的硬实证. ⑩ **元教训 — MR-4 silent-failure 第 N 次演出新形态**: 自动批量注入工具引入跨多 jobs 系统性 bug — 单次 fix HN 但需要 source-level 守卫 7 个其他 jobs 才真闭环. V37.9.50-hotfix (修一个) → V37.9.58-hotfix (8 个同款 batch 修复 + 守卫). V37.9.57 自动注入工具是 framework 进化方向但 lint check 缺失 → 引入新 bug 类. **MR-10 understand-before-fix 兑现**: 不只修 HN 一个症状, 取证 → 扫所有同 pattern → 批量修复 + 加守卫防回归. ⑪ **下次 V37.9.58-hotfix2 候选 (a) 部署观察验证 cron 真生效 (b) inject_level_4_to_aligned_jobs.py 加 lint check (注入 os.environ 前先检 heredoc 顶部是否含 import os, 没有则拒绝注入) — 让自动注入工具自治 (c) INV-V37958-HOTFIX-001 governance 一周观察后立 (d) 同款 audit: V37.9.57 是否还有其他 batch 注入跨多 jobs 但漏 import 的盲点 (扫所有 jobs heredoc 内调用的库 vs heredoc 顶部 import 一致性). |
| V37.9.58 | 2026-05-12 | **Plan B 渐进 escalation 终态兑现 — machine_sync 真激活 (5/3-5/11 一周观察期零漂移, V37.9.23/24 收工承诺兑现)** — ① **触发**: V37.9.23 (5/3) jobs_to_crontab drift_action alert_only → machine_sync + 默认 dry-run 上线; V37.9.24 (5/4) named-dispatch 重构 + kb_sources_to_index 同款升级. yaml meta 收工承诺明确登记 "V37.9.24+ (一周后): 切关 CONVERGENCE_DRY_RUN 默认 → 真激活 machine_sync". 一周累积观察期 (5/3-5/11) 数据: jobs_to_crontab declared=36 observed=36 / kb_sources_to_index declared=14 observed=14, 双 spec 全周零漂移零误报零告警, 兑现 Plan B 渐进路径终态. ② **scope (用户决策"立即激活")**: V37.9.58 = `convergence.py::_is_dry_run()` 默认值反转 + yaml 两 spec `dry_run_default: true → false` + meta version 0.7 → 0.8-machine-sync-activated + governance INV-CONVERGENCE-CRON-001 守卫 V37.9.58 升级 + 测试 alternation + 新单测类反向验证. **不引入新功能** — V37.9.23/24 已铺好路径, V37.9.58 仅是 default flip. ③ **convergence.py `_is_dry_run()` 默认值反转** (核心改动 1 行): 旧 `os.environ.get(_DRY_RUN_ENV_VAR, "1") != "0"` (默认 True / 任何非 "0" 字面量都 dry-run, typo→保守) → 新 `os.environ.get(_DRY_RUN_ENV_VAR, "0") == "1"` (默认 False / 仅 "1" 字面量 dry-run, typo→兑现 escalation). **typo-safe direction 反转**: 旧 V37.9.23 默认行为最保守 (任何错误都不动 crontab), 新 V37.9.58 默认行为兑现承诺 (escalation 已经过一周观察期). docstring 升级解释 escalation 兑现 + V37.9.23 历史引用. ④ **yaml 配置同步** (`ontology/convergence_ontology.yaml`): (1) jobs_to_crontab spec `dry_run_default: true → false` + 注释 "V37.9.58 切关 escalation 兑现 (5/3-5/11 一周观察期零漂移, 5/12 切关)" (2) kb_sources_to_index spec 同款 (3) meta version `0.7-fifth-spec-services-to-launchd → 0.8-machine-sync-activated` + status `framework_*_dry_run → framework_*_activated_dry_run_default_off` (4) **加 v37_9_58_changelog 段** 记录完整 Plan B 三阶段时间线 (V37.9.19 alert_only → V37.9.23/24 machine_sync+dry_run → V37.9.58 切关 dry_run 默认) + 一周观察期数据 + typo-safe 反转分析 + MR-17 真正兑现 + Mac Mini 部署预期行为 + 30 秒回滚预案 (cron 加 `export CONVERGENCE_DRY_RUN=1` 或 git revert). ⑤ **governance INV-CONVERGENCE-CRON-001 守卫升级** (3 新 declaration checks): (1) yaml `dry_run_default: false` 字面量守卫 (替代旧 `: true`) (2) convergence.py `_is_dry_run()` 默认值 regex 守卫 `os.environ.get\(_DRY_RUN_ENV_VAR, "0"\) == "1"` (3) v37_9_58_changelog 段守卫 (4) meta version 0.8 守卫. 另外升级 INV-CONVERGENCE-KB-001 + INV-CONVERGENCE-SERVICES-001 的 meta version alternation 接受 0.8. audit_metadata version 3.35 → 3.36 + v3.35→v3.36 changelog 记录终态兑现. ⑥ **测试反转 + alternation** (`test_convergence.py`): (1) TestIsDryRun 5 个测试 (`test_unset/test_value_zero/test_value_true/test_empty_string` 反转为 real apply, 仅 `test_value_one_keeps_dry_run` 保留) — 反映 typo-safe 反转 (2) `TestRealJobsToCrontabSpec::test_real_spec_completes_without_raising` 期望 `apply_dry_run=False` (默认 real apply) + action 前缀接受 `applied:` (real apply) 或 `DRY-RUN would apply:` (显式 dry-run) alternation (3) 3 处 yaml `dry_run_default: true → false` 字面量守卫反转 (4) 2 处 meta version `0.6-0.7` alternation 扩展加入 `0.8-machine-sync-activated`. ⑦ **新单测类 TestV37958DryRunActivation** (14 tests, 7 分类): (1) 3 个源码字面量守卫 (V37.9.58 marker / escalation docstring / `os.environ.get(..., "0") == "1"` 字面量) (2) 1 个反向验证守卫 (防回退 V37.9.23 旧默认 `!= "0"` 反模式) (3) 2 个 yaml `dry_run_default: false` 守卫 (jobs_to_crontab + kb_sources_to_index 两 spec) (4) 4 个 yaml meta 守卫 (version 0.8 + status activated + v37_9_58_changelog 段存在 + 段内含 escalation/5/3/5/11/零漂移/MR-17 关键字) (5) 2 个 governance INV 守卫 (INV-CONVERGENCE-CRON-001 V37.9.58 markers + audit_metadata v3.36) (6) 3 个端到端 (默认无 env → apply_dry_run=False 真激活 / 显式 CONVERGENCE_DRY_RUN=1 → 仍可回到 dry-run / kb_sources_to_index 同款默认 real apply). **反向验证已确认守卫真有效**: sed 把 `os.environ.get(_DRY_RUN_ENV_VAR, "0") == "1"` 改回 `!= "0"` → 4 个 V37.9.58 测试立即失败 (含 test_is_dry_run_default_reversed_to_v37_9_58 字面量守卫 + 3 个 e2e 真激活契约), 还原后 256/256 全过. ⑧ **MR-17 真正兑现** (declared-state-must-converge-to-runtime-via-machine-not-memory): V37.9.19 起步 alert_only (机器**可检测**) → V37.9.23/24 machine_sync + 默认 dry-run (机器**可同步**但不默认同步) → **V37.9.58 切关 dry-run 默认 (机器可同步且默认同步)** ← 终态. Plan B 三阶段路径完整闭环, 从"声明→运行时同步靠 Claude Code 记忆"升级为"机器自动同步", 这是 V3 路标 `pip install ontology-engine` 终极目标的关键里程碑兑现. ⑨ **Mac Mini 部署后预期行为**: (a) `cd ~/openclaw-model-bridge && git fetch origin main && git reset --hard origin/main` 同步 V37.9.58 (b) 下次 governance audit cron 跑 (`python3 ~/ontology/governance_checker.py --full` 每日 07:00 + 任何手动触发): 若 jobs_to_crontab missing=0 / kb_sources_to_index missing=0 → 零操作 (符合一周观察期数据预期), framework 仅日志记录 "DRIFT action=machine_sync missing=0 apply=0"; 任一 spec 漂移 → 立即真同步, 不再 dry-run 等待 (c) crontab_safe.sh add (V37.9.18 加固后 idempotent + safe) 真调用; kb_embed.py 默认增量 (mtime diff) 真调用 (d) operator 可显式 `CONVERGENCE_DRY_RUN=1` 临时回到 dry-run 观察模式. ⑩ **回滚预案 30 秒**: governance_audit_cron.sh 头部加 `export CONVERGENCE_DRY_RUN=1` 恢复 dry-run / git revert V37.9.58 commit 完全回退. crontab 已用 V37.9.18 加固后的 idempotent helper, 无需 crontab 恢复操作. ⑪ **累计**: 77 suites 不变 / 2651 → 2667 tests (+16: TestV37958DryRunActivation 14 + 2 V37.9.23/24 alternation alignment) / 0 fail / governance v3.35 → v3.36 / 72 invariants 不变 (INV-CONVERGENCE-CRON-001 内涵升级非新增) / 562 → 565 checks (+3 V37.9.58 markers) / 17 meta rules / 安全 95/100 / VERSION 0.37.9.57 → 0.37.9.58. ⑫ **元价值**: V37.9.58 不是新功能, 而是 Plan B 渐进 escalation 路径的**终态兑现**. 三个 V37.9.x 版本 (V37.9.19 alert_only 起步 / V37.9.23 machine_sync+dry_run / V37.9.58 切关 dry_run 默认) 形成完整闭环, 证明"通过 dry-run 阶段验证 + 一周观察期 + 数据驱动决策 + 用户授权后真激活"是高 blast-radius 操作的可靠路径。下次 V37.9.59+ 候选: (a) Mac Mini 部署后下次 governance audit 真激活效果观察 (b) 一周真激活后立 INV-CONVERGENCE-ACTIVATED-001 守卫 "default real apply 不可回退" 锁定终态 (c) services_to_launchd (V37.9.25 alert_only) 是否进入 Plan B 升级路径评估 (d) SLO p95 严重超阈值调查 (V37.9.58 候选已登记 → V37.9.59) (e) LEVEL_6 反幻觉守卫 sub-agent / multi-agent dispatch 场景. |
| V37.9.57 | 2026-05-12 | **架构级公共反幻觉守卫模板 + 9 task 全量接入 — MR-8 single-source-of-truth + MR-7 治理自观察兑现 (V37.9.56-hotfix3 血案的真正结构性修复)** — ① **触发**: V37.9.56-hotfix3 修了 kb_evening "OpenClaw 社区发布 v26" 幻觉血案 (原则 #23 链式幻觉典型实证), 但全面审计 9 task 风险矩阵 (Explore agent 完成) 发现同款风险存在于 kb_dream Reduce / 8 ALIGNED jobs (hf/s2/dblp/arxiv/gh/rss/ai_leaders/hn) / kb_review / kb_deep_dive / freight / finance / ontology_sources. 单点修复 evening 等于把同一反幻觉 bug fix 留给每个 task 各自维护 (MR-8 反例 copy-paste-is-a-bug-class). ② **scope (用户决策 "一次性 batch 全量接入 9 task")**: V37.9.57 立公共反幻觉守卫模板 + 9 task 全量集成. 不是"加 1 个 hotfix4"而是"立架构契约 + 横向部署". ③ **新文件 `hallucination_guards.py`** (~250 行, 纯 stdlib 零外部依赖): 5 档严格度模板按场景累积式设计 — (a) **LEVEL_1_MINIMAL**: 仅"严禁虚构" (kb_harvest_chat 闭环数据) (b) **LEVEL_2_STANDARD**: + 来源标签校验 (freight/finance/ontology 单源新闻) (c) **LEVEL_3_STRICT**: + 反链式推论 "X 段提到 Y → 推论 Z" 模式禁止 (kb_review 跨域回顾 + kb_deep_dive 单 paper 全文) (d) **LEVEL_4_PROJECT_AWARE**: + V37.9.56-hotfix3 具体血案字面禁令 (8 ALIGNED jobs 含"OpenClaw 社区发布"/"v26"/"v26/v27/v37 版本更新"/"项目里程碑"/"开源 X 上线"/"[openclaw]" 6 个精确字眼) (e) **LEVEL_5_RADAR_AWARE**: + Opportunity Radar 三件套信号源契约 (kb_dream Reduce + kb_evening, Top 5/cross_source/trend 显式声明"跨多天累积非今日事件" + 反链式 "paper 提到 OpenClaw → 本项目必有相关动态"). **公开 API**: `get_guard(level)` 返回守卫文本块, `list_levels()` / `get_blocked_phrases()` / `get_radar_signal_types()` 辅助查询. **未知 level fallback LEVEL_3_STRICT** 安全中位数. **CLI**: `python3 hallucination_guards.py --list` / `--blocked-phrases` / `--level LEVEL_X` 调试. ④ **`test_hallucination_guards.py`** (38 单测 / 7 类): TestGuardLevels(8 含 5 档累积式 + V37.9.57 marker + LEVEL_N 不含 LEVEL_(N-1) 之外特性) + TestGetGuardAPI(6 默认/未知/None/非string fallback) + TestBloodLessonBlockedPhrases(5 V37.9.56-hotfix3 5 个核心字眼必须在 LEVEL_4+) + TestRadarSignalTypes(3 三件套必须在 LEVEL_5 + 不在 LEVEL_4) + TestCliInterface(4 subprocess --list/--blocked-phrases/--level) + TestSourceLevelGuards(9 V37.9.57 marker / V37.9.56-hotfix3 lineage / GUARDS dict 5 key locked / _DEFAULT_FALLBACK 锁 LEVEL_3 / MR-8 documented) + TestV37957Contracts(2 模块顶部 import / 无外部依赖). **反向验证两层守卫真有效**: (a) sabotage `_LEVEL_4_PROJECT_AWARE = """..."""` 整段赋空 → 4 failures (test_blocked_phrases_in_level_4 + test_level_4_contains_level_3_core + 等) (b) sabotage `_DEFAULT_FALLBACK_LEVEL = "LEVEL_3_STRICT"` 改 LEVEL_5 → test_default_fallback_is_level_3 立即 fail (c) sabotage LEVEL_5 整段赋空 → 4 failures, 还原后 38/38 全过. ⑤ **9 task 全量集成**: **kb_evening_collect.py** (LEVEL_5) 替换 V37.9.56-hotfix3 inline 反幻觉守卫为 `from hallucination_guards import get_guard` + `{get_guard("LEVEL_5_RADAR_AWARE")}` f-string interpolation. V37.9.56-hotfix3 测试 2 处升级接受 V37.9.57 LEVEL_5 wording (语义等价, 字面迁移). **kb_dream.sh** (LEVEL_5) Reduce prompt 头部 `DREAM_HG_GUARD=$(python3 -c "...sys.path...; import hallucination_guards as hg; print(hg.get_guard('LEVEL_5_RADAR_AWARE'))" 2>/dev/null)` + REDUCE_SYSTEM 末尾 append `${DREAM_HG_GUARD}`, 与 V37.8.6 反污染守卫互补 (互补层: V37.8.6 防错误日志被当外部信号 / V37.9.57 防项目动态 + Radar 信号源混淆). **8 ALIGNED jobs** (LEVEL_4 — hf_papers/s2/dblp/arxiv/github_trending/rss_blogs/ai_leaders_x/hn) 用 inject_level_4_to_aligned_jobs.py 批量注入: shell 顶部 `HG_LEVEL_4_TEXT=$(python3 -c "..." 2>/dev/null) + export` + Python heredoc prompt 末尾 `prompt += os.environ.get('HG_LEVEL_4_TEXT', '')`. run_hn_fixed.sh 自动检测插入点失败手动补全 (NOTIFY_SH 加载块之后插入). **freight + finance + ontology jobs** (LEVEL_2) 用 inject_levels_batch.py 批量, freight 因 prompt 走 PROMPT="..." stdin 模式手动补 `${HG_GUARD_TEXT}` interpolation 到 PROMPT 末尾. **kb_review_collect.py + kb_deep_dive.py** (LEVEL_3) 顶部 `from hallucination_guards import get_guard` + 三个 build_*_prompt 函数末尾 `{get_guard("LEVEL_3_STRICT")}` (build_prompt / build_full_text_prompt / build_abstract_only_prompt). ⑥ **集成验证**: 8 ALIGNED jobs `bash -n` 全过 syntax check + HG_LEVEL_4_TEXT shell var 注入 + Python heredoc `prompt += os.environ.get` 注入。kb_evening build_evening_prompt 真渲染输出含 LEVEL_5 守卫 + "OpenClaw 社区发布" 血案字眼 + "Opportunity Radar" 信号源契约. ⑦ **接入**: auto_deploy.sh FILE_MAP 加 `hallucination_guards.py|$HOME/...` (跨 9 task 部署到 Mac Mini) + full_regression.sh 加 `hallucination_guards (V37.9.57 公共反幻觉守卫 5 档模板)` suite. ⑧ **MR-8 single-source-of-truth 兑现**: 公共守卫模板, 不同 task 共享同款守卫文本, 无 copy-paste 漂移. 未来加新 LLM task 只需 `from hallucination_guards import get_guard` + 调 `get_guard(LEVEL_X)`, 自动获得最新反幻觉守卫. 修改守卫文本一处, 9 task 自动 propagate. ⑨ **MR-7 治理自观察兑现**: V37.9.56-hotfix3 评论"测试三层契约 + 用户视角观察是 framework 价值的硬实证"延续 — V37.9.57 把"反幻觉守卫覆盖"从"靠记忆维护"升级为"机器化检查" (TestSourceLevelGuards 9 个守卫 + V37.9.57 marker + V37.9.56-hotfix3 lineage 可追). ⑩ **累计**: 76→77 suites (+1: hallucination_guards) / 2613→2651 tests (+38 hallucination_guards 单测) / 0 fail / VERSION 0.37.9.56 → 0.37.9.57 / 安全 95/100. ⑪ **Mac Mini 部署后预期行为**: (a) `cd ~/openclaw-model-bridge && git fetch origin main && git reset --hard origin/main` 同步 V37.9.57 (b) `bash ~/restart.sh` 不需要 (hallucination_guards.py 是模块, 9 task 在 next cron 触发时自动加载) (c) 22:00 kb_evening cron 推送 `evening_20260513.md` 不再含 V37.9.56-hotfix3 血案字眼编造 + Top 5 段语义重定位为"近期高对齐参考阅读 Top 5 (V37.9.56 #2, ⭐≥4 项目对齐度, 跨多天累积)" + LLM 看到 LEVEL_5 守卫含明确字面禁令 ("OpenClaw 社区发布"/"[openclaw]"等) 立即识别为禁止输出字眼. (d) 03:00 kb_dream Reduce cron 推送的"梦境"段保留 V37.8.6 反污染守卫 + 加 V37.9.57 LEVEL_5 反项目动态编造 + Radar 信号源契约 = 双层防御覆盖. (e) 10:00/20:00 hf_papers + 11:00 s2 + 12:00 dblp + 14:00 github_trending + 09:00/21:00 ai_leaders_x + 18:00 rss_blogs + 每 3h:45 hn + 每 3h arxiv 8 ALIGNED jobs cron 触发后, 每 paper alignment 评分段的"一句话原因 ≤30 字"输出不再含"对 OpenClaw v37 control plane 有启发"等间接项目动态推论 (LEVEL_4 守卫"严禁基于'项目方向词' 推断本项目存在某个未公开发布的功能"). (f) freight/finance/ontology 每日 cron 推送来源标签校验 (LEVEL_2 守卫"严禁创造未在数据源列表中出现的标签"). ⑫ **元价值 (V3 路标 framework 突破)**: V37.9.57 是 V37.9.56-hotfix3 血案的**真正结构性修复** — hotfix3 是单点 prompt 加禁令, V37.9.57 是 framework 级"反幻觉守卫覆盖率"从"靠 prompt 各自记忆"升级为"声明式 5 档模板 + 机器化部署". V3 路标 `pip install opportunity-radar` / `pip install ontology-engine` 终极目标的核心交付闭合 — 任何新 LLM-calling task 引入时, 只需声明所属严格度档位 (LEVEL_1~5) 即可获得全部反幻觉守卫, 零 prompt 复制粘贴, 零血案记忆维护成本. 这是 V37.9.56 PoC pattern (Top 5 alignment 注入) → V37.9.56-hotfix3 (single-task hotfix) → **V37.9.57 (architectural-level fix)** 的三跳升级, 完整闭环原则 #23 链式幻觉防范 + 原则 #18 补证据而非补功能 + MR-8 copy-paste-is-a-bug-class. ⑬ **下次 V37.9.58+ 候选**: (a) **Mac Mini 5/13 22:00 evening + 5/13 03:00 dream 真跑验证 V37.9.57 LEVEL_5 守卫生产效果** (用户视角观察推送内容是否仍含血案字眼) (b) **INV-HALLUCINATION-001 立 governance** (守卫每个 LLM-calling task 必须 import get_guard, 反 inline 反模式) — 当前 V37.9.57 是 PoC 阶段 (与 V37.9.46/47/48 同款 pattern), 一周观察期后立 INV (c) **LEVEL_6 候选** for sub-agent / multi-agent dispatch 场景 (Phase 4 P5 续推进) (d) **5/11 决策窗口续**: V37.9.23/24/25 jobs_to_crontab + kb_sources_to_index + services_to_launchd machine_sync dry-run → 真激活 (一周累积观察期到达). |
| V37.9.56-hotfix3 | 2026-05-12 | **kb_evening LLM 编造 "OpenClaw 社区发布 v26" 幻觉血案闭环 — V37.9.56 链式幻觉副作用修复 (原则 #23 典型实证)** — ① **触发**: V37.9.56-hotfix Mac Mini 部署后 9:41 evening 推送 evening_20260512.md 第 4 条要闻 + 整个明日关注 + 健康度评分理由都是 LLM 编造。具体编造内容: "OpenClaw 社区发布新版本 v26，验证货代Watcher上线及SSH远程部署流程，修复 --thinking 参数问题 [openclaw]"。**用户视角发现** (原则 #13 兑现) — sources_used 只有"🚢 货代动态"+"🔥 HackerNews 热帖"两个，**没有 [openclaw] 源**，"v26"是项目内部 changelog 编号不是 OpenClaw 社区版本。② **因果链 (原则 #23 链式幻觉)**: V37.9.56-hotfix 部署后 picker 找到 31 picks Top 5 注入 evening prompt → LLM 看 Top 5 提到 "OpenClaw 三平面对齐 (control/memory/ontology)" + 今日笔记少 + sources_used.count=2 → 训练倾向 (合理化 + 连接相关概念) 下推断 "用户在做 OpenClaw 项目 + Top 5 必引用 + 必有项目动态" → 编造 "OpenClaw v26 发布" + 自造 [openclaw] 来源标签。原 V37.9.56 alignment_block 指令"在'今日要闻'段优先引用 Top 5"在 LLM 训练倾向下**反向触发幻觉而非引用真 paper**。9:05 evening (V37.9.56-hotfix 前 picker 0 picks) 没编造 v26 → 9:41 (hotfix 后 31 picks) 才编造 v26 = 因果铁证。③ **三层结构修复 (方向 A)**: (a) **alignment_block 软化注入**: 段头 "今日高对齐 Top 5" → "近期高对齐参考阅读 Top 5 (V37.9.56 #2, ⭐≥4 项目对齐度, **跨多天累积**)"; 注入语 "在'今日要闻'段优先引用" → "你**绝不应**在'今日要闻'/'明日关注'段硬性引用上述条目, 除非今日笔记/来源中**实际出现**对应内容. Top 5 仅作为'背景知识参考', 不作为'新闻事件'." 显式说明 Top 5 = 外部累积非今日事件 (b) **prompt 模板加反幻觉具体字面禁令** (即使无 Top 5 也注入, 防 LLM 任何场景下编造项目动态): 禁字面 "OpenClaw 社区发布"/"OpenClaw v26"/"v26/v27/v37 版本更新"/"项目里程碑"/"开源 X 上线"; 禁来源标签 [openclaw]/[OpenClaw]/[社区] 除非 sources_used 真实出现; 禁链式推论"基于 Top 5 推断本项目状态"/"基于 paper 推断 OpenClaw 版本" (c) **源码注释引用具体血案精确字眼便于运维 grep** (kb_evening_collect.py V37.9.56-hotfix3 注释含 "OpenClaw 社区发布 v26" 真实编造字眼). ④ **6 新单测 1 类** TestV9_56Hotfix3AntiHallucinationGuards: test_anti_hallucination_block_in_template (模板级) + test_alignment_block_uses_soft_injection_not_hard_quote_instruction (反模式守卫) + test_alignment_block_new_header_and_semantic_repositioning (新段头 + 跨多天累积说明) + test_blood_lesson_blocked_phrases_must_be_listed (5 个血案精确字眼显式列出) + test_chain_inference_banned (链式推论禁令显式) + test_v9_56_hotfix3_marker_in_source (源码 marker + 血案精确字眼可 grep). 修复同时 3 个老测试 (TestEveningTopAlignmentInjection backward_compat / empty_alignment / coexist) 适应 V37.9.56-hotfix3 新段头. ⑤ **反向验证守卫真有效**: sabotage 删反幻觉字面禁令立即 3 failures (test_anti_hallucination/test_blood_lesson_blocked/test_chain_inference 全 fail), 还原后 54/54 全过. ⑥ **累计**: 76 suites 不变 / 2607→2613 tests (+6) / 0 fail / VERSION 0.37.9.56 不变 (hotfix 不 bump) / 安全 95/100. ⑦ **原则 #23 (链式幻觉) 典型实证案例**: V37.9.56-hotfix3 是 CLAUDE.md 原则 #23 "LLM 链路中每一跳都会放大幻觉" 的教科书级案例 — V37.9.56 让 Top 5 paper 进入 evening prompt 看似"补全 LLM 输入", 实际让 LLM "Top 5 提到项目 + 今日笔记少 + 训练倾向合理化 = 编造项目动态" 三跳幻觉. 防范方法兑现 ③(a)(b): 段头加"跨多天累积" + 显式声明"不是今日事件" + 加具体字面禁令 (告诉 LLM 哪些字眼绝对不能输出). ⑧ **MR-15 候选元规则强化**: V37.9.56 (deployment-layout-must-be-tested-on-target) + V37.9.56-hotfix3 (llm-prompt-injection-must-anticipate-hallucination-vector) 共同提炼: **任何向 LLM prompt 注入新上下文都必须 (i) 显式语义定位 (ii) 配套反幻觉具体字面禁令 (iii) 在生产环境 user-view 实测验证**, 不能假设"补全数据 = 不会幻觉". 是 V3 路标 declarative capability framework 走向"declarative-with-anti-hallucination-contracts" 升级方向. ⑨ **Mac Mini 部署后预期**: 下次 22:00 kb_evening cron 推送 evening_20260513.md 不再含 "OpenClaw 社区发布" 等编造字眼. 用户可继续手动 `bash ~/kb_evening.sh 1` 验证. ⑩ **元教训** (一日 V37.9.56 → hotfix → hotfix2 → hotfix3 四连发 session): 测试三层契约 + 用户视角观察 (原则 #13/#15) 是 framework 价值的硬实证 — dev 单测全过 + Mac Mini 自动测试全过都不能替代"用户真实阅读推送内容". 是 Stage 2 "audit is regression not prevention" 立场文章的下一篇候选素材 — *"LLM prompt 注入即幻觉注入: 当'补全数据'反向触发链式推论"*. |
| V37.9.56-hotfix2 | 2026-05-12 | **kb_evening.sh DAYS 数字校验防御 (V37.9.56 诊断暴露的 zsh # 注释坑 root-cause fix)** — ① **触发**: V37.9.56 部署诊断时用户执行 `bash ~/kb_evening.sh   # 22:00 同款...` 失败，traceback line 413 `int("#")` ValueError. 真因: zsh `interactive_comments` 默认 OFF, `#` 被当 $1 传给 bash → `DAYS="${1:-1}"` 拿到 "#" (非空通过 `:-` 默认) → collector `int(os.environ.get("DAYS"))` 抛 ValueError. ② **原则 #28 root-cause fix**: 不是 V37.9.56 引入但 V37.9.56 诊断暴露 (Q1 之前存在 ✓ V37.7 起 / Q2 我引入 ✗ / Q3 最小修复 = kb_evening.sh 加 regex 数字校验). ③ **修复** (kb_evening.sh log() 定义后, line 47 附近): `if ! [[ "$DAYS" =~ ^[0-9]+$ ]]; then log "WARN: DAYS arg '$DAYS' non-numeric, falling back to 1"; DAYS=1; fi`. 非数字 fallback 1 + WARN 日志含原始值便于运维 grep. 让 cron 继续跑而不是炸 traceback. ④ **5 新单测 1 类** TestV9_57Hotfix2DaysNumericValidation: regex 字面量守卫 + V37.9.57-hotfix2 marker + fallback DAYS=1 顺序锁 + WARN 日志关键字 (non-numeric, DAYS arg) + **runtime subprocess 真跑验证血案场景** (DAYS="#" → fallback 1 + log WARN / DAYS=2 → 正常通过 / 空串 → fallback 1). ⑤ **反向验证两层守卫真有效** (sabotage 删校验块立即 4 failures / sabotage regex 改 ^.+$ 立即 2 failures, 还原后 48/48 全过). ⑥ **累计**: 76 suites 不变 / 2602→2607 tests (+5) / 0 fail / VERSION 0.37.9.56 不变 (hotfix 不 bump, V37.9.43-hotfix/V37.9.50-hotfix2/V37.9.50-hotfix3 同款惯例) / 安全 95/100. ⑦ **元教训**: V37.9.56 部署诊断的 zsh # 坑暴露了一个独立小 bug — 修复成本 10 分钟但消除了未来 caller (含 cron / 第三方 / 误操作) 传非数字 DAYS 的 silent failure 类. 原则 #28 三问严格执行: 不是 V37.9.56 引入但症状暴露窗口在同一诊断中, 同 session 修最高效 (避免下次 session 重新拉起上下文). |
| V37.9.56-hotfix | 2026-05-12 | **V37.9.56 picker cache_paths 多 layout 修复 (Mac Mini 实测 0 picks 死局, MR-10 understand-before-fix 兑现)** — ① **触发**: V37.9.56 部署 Mac Mini 后用户跑 `python3 ~/top_alignment_picker.py` 输出 `no picks (scanned 8 sources)` 而 kb_evening 同时段 sources_used=[HN+货代] 证明 HN 今日 cache 有数据. 单测 + dev 全过, **生产 0 picks**. 按原则 #28 三问诊断: (Q1) 之前不存在 ✓ V37.9.56 引入 (Q2) 我哪个改动: picker 默认 `repo_root = os.path.dirname(__file__) = $HOME`, `cache_dir_rel="jobs/X/cache"` → 找 `~/jobs/X/cache` (Q3) 真根因: **Mac Mini auto_deploy FILE_MAP 真实部署路径是 `~/.openclaw/jobs/X/` 不是 `~/jobs/X/` 也不是 `~/openclaw-model-bridge/jobs/X/`** + HN 例外子目录 `hn_watcher` (FILE_MAP line 164: `run_hn_fixed.sh|$HOME/.openclaw/jobs/hn_watcher/run_hn_fixed.sh`). dev 单测全部用 tempdir 自构造 `jobs/X/cache` layout 不暴露此 bug. ② **最小修复**: ALIGNED_SOURCES schema `cache_dir_rel` (单路径) → `cache_paths` (list 候选, 顺序优先 try). 8 个 source 每个含 dev + Mac Mini 2 候选; HN cache_paths = `["cache", "~/.openclaw/jobs/hn_watcher/cache"]` 处理子目录差异. 新增 `_resolve_cache_dir(repo_root, src)` 纯函数 (~/  展开 + abs 直用 + rel 拼 repo_root + 全空 FAIL-OPEN 返回首个候选). `collect_all_picks` 改用 helper 替代 `os.path.join(repo_root, src["cache_dir_rel"])`. ③ **12 新单测 2 类** TestV9_57MacMiniLayout(6 含 dev layout 解析 + Mac Mini layout 解析 + HN hn_watcher 子目录解析 + FAIL-OPEN 全空返回首个候选 + cache_paths 缺失/空安全 + 端到端 Mac Mini layout picks 真收集) + TestV9_57SchemaGuards(6 含 8 source 全部 cache_paths 字段 + 必须 list + 必须 ≥2 候选 + 禁止残留 cache_dir_rel 反模式 + Mac Mini 路径 `~/.openclaw/jobs/` pattern 必存 + HN 必须 hn_watcher 子目录 + helper 函数定义 + collect_all_picks 调 helper + V37.9.57 marker). **反向验证两层守卫真有效**: sabotage 删 hf_papers Mac Mini path 立即 1 fail / sabotage 改 hn_watcher → hn 立即 1 fail, 还原后 67/67 全过. ④ **MR-10 understand-before-fix 兑现**: 先让 user 跑诊断 `ls ~/openclaw-model-bridge/jobs/hf_papers/cache/llm_results.jsonl` 全 missing → 发现 jobs 不在那里 → 查 auto_deploy FILE_MAP 看真实部署路径 = `~/.openclaw/jobs/X/` 才动代码. **没盲改**, 数据驱动诊断. ⑤ **MR-15 候选元规则反例**: V37.9.56 当 PoC 阶段引入 deployment-specific assumption 但单测全用 tempdir 隔离, 没在 Mac Mini 真实 layout 上 smoke test. 候选元规则 *deployment-layout-must-be-tested-on-target-not-tempdir* — 部署路径假设要在真实部署环境验证 (V37.9.58+ 立). ⑥ **累计**: 76 suites 不变 / 2590→2602 tests (+12 V37.9.57 layout coverage) / 0 fail / governance v3.35 不变 / VERSION 0.37.9.56 不变 (hotfix 不 bump, V37.9.43-hotfix 同款惯例) / 安全 95/100. ⑦ **Mac Mini 部署后预期**: `python3 ~/top_alignment_picker.py` 应找到 HN 今日 cache 输出 picks (如有 ⭐≥4); kb_evening 22:00 cron 推送 Top 5 段含 ⭐≥4 paper/repo/blog. ⑧ **元教训**: 同一 session 内的 V37.9.56 部署 + V37.9.56-hotfix 闭环展示**测试三层契约** (原则 #15) — 单测覆盖逻辑正确 + dev 环境 syntax/structure 正确 + **Mac Mini 真实部署是不可替代的第三层验证**, 否则 deployment-specific 假设错配会让"看似完整"的功能在生产端 0 输出. 这是 V37.9.56 PoC pattern 的延伸 — 不只是写代码, 还要在 target environment 验证 path 假设. |
| V37.9.56 | 2026-05-12 | **Sub-Stage 4c Top 5 高对齐推送 picker 完整兑现 — Opportunity Radar #2 三件套全量集成 (kb_dream + kb_evening 双路注入)** — ① **触发**：V37.9.51 收工承诺 Sub-Stage 4c 是"audit P1/P2 完整批次后聚合高对齐 paper/repo/blog/tweet 到推送顶端", 让用户每日 WhatsApp/Discord 推送一眼看到"今日真正值得读的 5 条". V37.9.46/47/48 PoC + V37.9.49 Sub-Stage 4a 集成 (#1 跨源 + #3 趋势) 已就位, V37.9.56 闭环 #2 项目对齐度 picker. 严格按原则 #28 三问 (Q1 之前不存在 ✓ / Q2 新功能 ✓ / Q3 最小修复 = 新模块 + 双处集成, 不动 8 个上游 ALIGNED 脚本本身). ② **新文件 `top_alignment_picker.py`** (~340 行纯 Python, 零 I/O at import, FAIL-OPEN 契约 V37.9.46 同款 lazy import 模式): 6 个公开纯函数 — `scan_source_results(cache_dir)` 读 llm_results.jsonl 跳 failed/空内容/损坏 JSON 行 + `parse_alignment_from_content(content)` lazy import project_alignment_scorer.extract_star_count → _fallback_extract_star_count (clamp [0,5]) → key-based 6-field state-machine 复用 hf_papers/V37.9.51 同款 (📌/🔑/💡/🎯/⭐/🎚️ 字段头识别 + 🎚 fallback no variation selector + ⭐ 评级与 🎚️ alignment 不污染) + `collect_all_picks(repo_root, min_stars)` 扫 8 ALIGNED source + `rank_picks(picks, top_n)` 三档排序 (⭐ 数 desc + source_priority asc + cn_title 长度 desc) + `format_top_picks_block(picks)` markdown 单行格式 `- ⭐⭐⭐⭐⭐ [HF精选] 标题 / 一句话原因` 标题 40 字截断 + `pick_top_aligned(repo_root, min_stars, top_n)` 端到端 orchestrator 返回 12-key dict (status/picks_total/picks_top/block) 顶层 try/except FAIL-OPEN 返回 no_picks. **常量锁定**: `DEFAULT_MIN_STARS=4` + `DEFAULT_TOP_N=5` (V37.9.51 收工承诺) + `ALIGNED_SOURCES` 8 个 source registry (hf_papers prio 1 / s2 prio 2 / arxiv+dblp prio 3 / github_trending prio 4 / rss_blogs prio 5 / ai_leaders_x prio 6 / hn prio 7) + `RESULTS_FILENAME="llm_results.jsonl"`. **CLI**: `--json` / `--block-only` (适合 bash `BLOCK=$(...)` 注入) / `--repo-root` / `--min-stars` / `--top-n` / 默认 human-readable + log() 写 stderr (MR-11 V37.8.6 stdout 污染血案防御). ③ **kb_dream.sh Phase 1.5 集成** (V37.9.49 Sub-Stage 4a 模式扩展): 新增 `TOP_ALIGNMENT_BLOCK=""` + `ALIGNMENT_PICKER` 路径自发现 ($HOME 或 dirname $0) + 调 `python3 ALIGNMENT_PICKER --block-only` 取 markdown 段 + 注入 REDUCE_DATA 作为 `═══ Opportunity Radar #2 (今日高对齐 Top 5, ⭐≥4) ═══`. 整段在 #1 cross_source + #3 趋势加速度之间, 不破坏 V37.9.49 现有逻辑顺序. ④ **kb_evening_collect.py 集成**: 新增 `collect_top_alignment_picks_for_evening(repo_root, min_stars, top_n)` helper (镜像 V37.9.49 `collect_trend_signals_for_evening`) + `build_evening_prompt` 新增 `top_alignment_picks=None` 可选参数 (向后兼容 V37.9.49) + prompt 末尾注入 `═══ 今日高对齐 Top 5 (V37.9.56 #2, ⭐≥4 项目对齐度) ═══` 段 (在 trend_block 之前) + 提示 LLM 在"今日要闻"段优先引用 + `.strip()` 守卫防 whitespace-only 字符串误注入空 block + run() orchestrator 调 helper 注入 prompt. ⑤ **60 新单测 / 2 文件**: test_top_alignment_picker.py 55 单测 / 8 类 + 反向验证三层守卫真有效 (sabotage DEFAULT_MIN_STARS=3 立即 fail / 删 🎚 fallback 立即 fail / log 写 stdout 立即 fail, 还原后 55/55 全过): TestParseAlignmentFromContent(11) + TestFallbackExtractStarCount(5) + TestScanSourceResults(6) + TestCollectAllPicks(3) + TestRankPicks(6 含 stars desc / priority tie-break / title len tie-break / top_n truncate) + TestFormatTopPicksBlock(6 含 ⭐ clamp / 标题 40 字截断 / 无 reason 不加分隔符) + TestPickTopAlignedOrchestrator(3 含端到端 dev no_picks / ok / multi-source 排序) + TestCliInterface(3 subprocess 真跑 --json + --block-only) + TestSourceLevelGuards(10 含 V37.9.56 marker / 8 source 全部注册 / 默认阈值锁定 / MR-11 stderr / FAIL-OPEN 文档 / 双 emoji 兼容 / 反 lazy import 守卫) + TestV9_56IntegrationContracts(2 JSON 可序列化 + bash 注入安全). test_kb_evening.py 新增 5 测试 TestEveningTopAlignmentInjection(向后兼容无注入 + 提供时注入 + 空白字符串不注入 + 与 trend_signals 共存 + collect helper FAIL-OPEN 返回 str). ⑥ **接入**: auto_deploy.sh FILE_MAP 加 `top_alignment_picker.py|$HOME/top_alignment_picker.py` + full_regression.sh 加新 suite `top_alignment_picker`. ⑦ **设计决策 (按 PoC 阶段惯例)**: 不立 governance INV (V37.9.46/47/48/49 同款 PoC pattern, 设计文档 INV-RADAR-001 留 Stage 6 三件套全量在生产真跑稳定后立). 同时不动 8 个上游 ALIGNED 脚本本身 — V37.9.56 只新建 picker + 集成, 上游 V37.9.45/50/51 已迁移完毕的 cache schema 直接复用. ⑧ **累计**: 75→76 suites (+1: top_alignment_picker) / 2530→2590 tests (+60: 55 picker + 5 evening V37.9.56 integration) / 0 fail / governance v3.35 不变 / 72 invariants 不变 / 562 checks 不变 / VERSION 0.37.9.55→0.37.9.56 / 安全 95/100. ⑨ **Mac Mini 部署效果**: 部署 V37.9.56 后下次 cron 自动生效: (1) 22:00 kb_evening 推送在"今日要闻"段之前**首次出现** "═══ 今日高对齐 Top 5 ═══" 段 + 5 条 ⭐≥4 paper/repo/blog/tweet 简明 listing + LLM 在主分析中优先引用. (2) 03:00 kb_dream Phase 1.5 把 #1+#2+#3 三件套完整注入 Reduce LLM prompt, "梦境"段首次以"今日真正值得读"作为锚点而非 paper 海洋抽样. (3) 用户在 WhatsApp 第一眼看到"今日值得读 5 条"前置位置, 决定是否点开 13 个其他 cron 推送源. ⑩ **元价值**: V37.9.56 是 Opportunity Radar 三件套从"独立组件"(#1+#2+#3 V37.9.46/47/48 各自 PoC) → "信号入推送链"(V37.9.49 #1+#3 集成) → **"完整三件套全量集成"** 的真正闭环. 数据复利路线从"14 source × 50 entries/day 信息洪流"升级为"⭐≥4 高对齐 + 跨源共振 + 趋势加速度 三档过滤后的高价值早期信号 Top 5". 这是 V3 路标 `pip install opportunity-radar` 终极目标的核心交付闭合 — declarative source registry + capability framework + 推送通道三层解耦, 任何新 source 只需迁移到 6 字段 + 加入 ALIGNED_SOURCES 即可参与 Top 5 评选, 零上游脚本侵入. ⑪ **下次 V37.9.57+ 候选**: (a) Mac Mini E2E 验证 — 跑 22:00 kb_evening 看推送中 Top 5 段真实数据 + 03:00 kb_dream Reduce 看 #1+#2+#3 三件套全量信号融合效果 (b) Sub-Stage 4b 续推剩余 P2/P3 audit 脚本 (acl_anthology / openclaw_official / kb_inject / freight_watcher / finance_news 等还未对齐 6 字段) — 完成后 ALIGNED_SOURCES 数从 8 增到 12+ (c) Stage 5 kb_radar.sh 06:00 cron 综合分析 (#1+#2+#3 早间一篇推送) (d) Stage 6 INV-RADAR-001 立 governance 守 (e) 5/11 决策窗口处理 (jobs_to_crontab + kb_sources_to_index machine_sync dry-run 真激活, 需 Mac Mini 一周漂移数据). |
| V37.9.55 | 2026-05-11 | **doubao flip verified_tool_calling + verified_streaming — Mac Mini E2E 双 capability 实测通过 + framework 视角 doubao 超越 Qwen3** — ① **触发**: 用户战略询问 "doubao 性能更好, 后续建议为主". 我推 4 个未验证 capability 风险表 (tool_calling/streaming/fallback/业务一致性) + 推荐渐进切换路径 (V37.9.55 先完成能力实测 → V37.9.56 单 cron 试水 → V37.9.57+ 切 primary). 用户决策路径 = 节奏选 "V37.9.55 完成能力实测 (不切 primary)" + Quota=明确充裕. ② **Mac Mini E2E 双测通过**: (a) **tool_calling**: curl 带 OpenAI tools schema (get_weather function) → 响应 `finish_reason="tool_calls"` + `message.tool_calls=[{id, function:{name:"get_weather", arguments:'{"city": "北京"}'}}]` + reasoning_content 显思考过程. 100% OpenAI Chat Completions tool_calling schema 兼容. (b) **streaming**: curl 带 `stream:true` → 多行 `data: {chat.completion.chunk}` SSE + `delta.content` + `delta.reasoning_content` 双字段流式 (reasoning model 流式 reasoning 字逐字). 100% OpenAI streaming schema 兼容. ③ **flip 2 个 verified flags** in doubao_provider.py: `verified_tool_calling=True` + `verified_streaming=True`. 剩 `verified_fallback=False` 守诚实语义留 V37.9.56+ 生产真 fire 后才能 flip (不能人为造 fallback 触发场景). ④ **cap_score framework 效果**: V37.9.54: 6 base + 3 verified*2 = 12. V37.9.55: 6 base + 5 verified*2 = **16**. **doubao cap_score 16 > Qwen3 cap_score 14** (Qwen3: 4 base + 5 verified*2). **framework 视角 doubao 是 registry 最强 provider**. 但 primary 仍是 Qwen3 (adapter PROVIDER_NAME env=qwen 不动), V37.9.56+ 用户决定是否单 cron 试水或全切. ⑤ **15 新单测 6 类** test_v37_9_55_doubao_more_verified.py: TestVerifiedToolCallingFlagsV9_55(2 含 verified_tool_calling=True + verified_features 含 tool_calling) + TestVerifiedStreamingFlagsV9_55(2 含 verified_streaming=True + verified_features 含 streaming) + TestVerifiedFeaturesV9_55Complete(1 锁定 5 项集合 {text,vision,tool_calling,streaming,reasoning}) + TestCapScoreV9_55(2 含锁定 16 + 必须 > Qwen3 cap_score) + TestRemainingUnverified(2 verified_fallback 守 False + 不在 features) + TestPluginSourceV9_55(5 plugin source: verified_tool_calling=True + verified_streaming=True + verified_fallback=False + V37.9.55 marker + tool_calls/SSE 字面量证据) + TestV37955VersionMarker(1 VERSION 0.37.9.55). ⑥ **修适 V37.9.52/53/54 cascading lock** (V37.9.55 flip 引发 4 个测试文件升级): V37.9.54 3 处 (cap_score 12→16 / verified_features 集合扩 5 项 / VERSION 不等式断言接受 V37.9.55+ 后续版本) + V37.9.53 4 处 (cap_score 12→16 / verified_features {text,vision,reasoning}→5 项 / unverified flags 只剩 verified_fallback / plugin 反验证守卫只剩 verified_fallback) + V37.9.52 1 处 (test_verified_state_evolution 只剩 verified_fallback). ⑦ **额外观察 — doubao reasoning streaming 特性**: streaming 测试发现 doubao 流式输出同时流送 `delta.reasoning_content` (思考过程逐字) + `delta.content` (最终答复) 双字段. 这对 adapter -> OpenClaw -> WhatsApp 路径有产品决策影响: V37.9.56+ 可能需要选择是否 expose reasoning_content 给 OpenClaw (类似 OpenAI o1 隐藏 reasoning vs DeepSeek-R1 显示). 当前 V37 adapter 不处理 reasoning_content, 流式输出会自动 strip 它 (delta.content 才被传到 OpenClaw 显示). ⑧ **累计**: 74→75 suites (+1: v37_9_55_doubao_more_verified) / 2515→2530 tests (+15) / 0 fail / governance v3.35 不变 (V37.9.55 仅 verified flag flip 无新 INV) / 72 invariants 不变 / 562 checks 不变 / VERSION 0.37.9.54 → 0.37.9.55 / 安全 95/100. ⑨ **V37.9.56+ 候选路径** (用户切 primary 决策): (a) **单 cron 试水**: 让 1 个低优先级 cron (如 hf_papers 或 rss_blogs) 显式走 doubao (通过 capability_routing 或 FALLBACK_PROVIDER env), 观察 3-7 天推送质量 + Volcengine quota 消耗. (b) **全切 primary**: adapter PROVIDER_NAME env qwen→doubao + plist 同步 + 重启 adapter. Qwen3 自动退为第 1 fallback (V37 capability routing 推导). (c) **doubao reasoning_content 决策**: 是否 expose 给 OpenClaw / WhatsApp 给用户看 (产品决策, 不是技术问题). (d) **真 fallback fire 等待**: Qwen3 quota 满或宕机时, doubao 自动接管 → 自然 flip verified_fallback=True (诚实语义). ⑩ **元价值**: V37.9.55 是 V37 capability routing framework 的**真实压力测试** — V37.9.52 接入 doubao 起 cap_score=5, V37.9.53 加 reasoning capability 升 10, V37.9.54 加 verified_vision 升 12, V37.9.55 加 verified_tool_calling + verified_streaming 升 16. **每次升级都让 fallback chain 自动重排**, framework 通过 capability 数据驱动而不是 if-then-else 硬编码. doubao 在 4 个 session 内从"未知能力 plugin"演进到"registry 最强 provider", 整个过程零 adapter.py routing 改动 / 零 FALLBACK_PROVIDER 硬指定. V3 路标 "pip install ontology-engine" 终极目标的核心证据之一 — declarative capability framework 真的工作. |
| V37.9.54 | 2026-05-11 | **ABC 三件套同 PR — A restart.sh marker-based plist reload + B doubao verified_vision flip + C INV-PLIST-ENV-001 治理 plist EnvironmentVariables 同步** — ① **触发**: V37.9.53 Mac Mini 实测发现 launchctl kickstart -k 不重读 plist (V37.9.13 后第二次踩同款坑), 需要手动 bootout/bootstrap 才让 ARK_API_KEY 真到 daemon 让 doubao 进 fallback chain. 用户 doubao vision E2E (curl image_url) 200 + 完整中文图片描述 + reasoning_content 同步通过, 路径 A 完全可行. 用户指示 ABC 全部执行: A restart.sh 修, B verified_vision flip, C plist env 治理. ② **A: restart.sh marker-based plist reload** — 新 `PLIST_LOAD_MARKER_DIR=$HOME/.openclaw/restart_markers` + helper 内 `stat -f %m` 比较 plist mtime vs `${label}.loaded` marker mtime. plist 比 marker 新 (含无 marker = 首次启动) → 强制 bootout + sleep 1 + bootstrap 重读 plist 含 EnvironmentVariables; plist 不比 marker 新 → kickstart -k 快路径 (V37.9.13 性能). 加载成功且 HTTP 200 健康验证通过后才 `touch "$marker"` (顺序锁守 daemon 启动失败时不误更新 marker → 下次 restart 跳过 plist reload). ③ **B: doubao verified_vision=True** — Mac Mini curl 实测 image_url + text content 混合 content list 工作: HTTP 200 + finish_reason=stop + 完整中文图片描述 + reasoning_content. plugin 注释加 V37.9.54 章节说明 image_url schema 形式. cap_score 升至 12 (6 base + 3 verified*2: text+vision+reasoning), 仍排 fallback chain 第 1 位. 未测 flags 仍 False: tool_calling/streaming/fallback (留 V37.9.55+). ④ **C: INV-PLIST-ENV-001** 新治理不变式 (meta_rule=MR-4 silent-failure, severity=high, verification_layer=[declaration, runtime]): 守 adapter plist EnvironmentVariables 包含所有 providers.py + providers.d/*.py 声明的 `api_key_env`. 5 checks: providers.py 含 api_key_env 字段定义 + doubao plugin 含 ARK_API_KEY 声明 + V37.9.54 marker + python_assert declared api_key_env 集合 ⊇ V37.9.54 baseline (REMOTE/OPENAI/GEMINI/ANTHROPIC/MOONSHOT/MINIMAX/GLM/ARK 8 个) + **runtime python_assert** Mac Mini --full mode 真读 `~/Library/LaunchAgents/com.openclaw.adapter.plist` 用 `plistlib.load()` 解析 EnvironmentVariables, 比对 critical envs {REMOTE_API_KEY, GEMINI_API_KEY, ARK_API_KEY} 必须全在 plist (dev 环境 plist 不存在时静默通过). ⑤ **INV-RESTART-001 扩展 5 个 V37.9.54 marker reload checks**: PLIST_LOAD_MARKER_DIR 常量定义 + helper plist_mtime 比较 + touch marker 字面量 (regex `touch\s+"\$marker"` escape `$`) + need_full_reload 标识 + runtime python_assert 守 touch marker 必须在 HTTP 200 / healthy 字样之后 (400 chars 内). ⑥ **27 新单测 5 类** test_v37_9_54_abc.py: TestRestartShMarkerLogic(8 含 PLIST_LOAD_MARKER_DIR 定义 / mkdir -p / plist_mtime+marker_mtime stat -f %m / need_full_reload=1 默认 safe / touch marker 顺序锁 / kickstart -k 路径保留 / V37.9.54 + V37.9.13 双 marker) + TestRestartShShellBehavior(2 bash -n 语法 + mkdir -p idempotent) + TestDoubaoVerifiedVision(5 verified_vision=True / verified_features 含 vision / V37.9.54 set [text, vision, reasoning] / 未测 flags 仍 False / plugin 注释含 image_url) + TestDoubaoCapScoreUpRanking(2 cap_score 10→12 / fallback chain 第 1 位仍 doubao) + TestPlistEnvGovernance(6 INV-PLIST-ENV-001 声明 / MR-4 严重性 / plistlib + adapter.plist / critical envs baseline / dev 安全 fallback / V37.9.53 血案引用) + TestV37954VersionMarker(4 VERSION 0.37.9.54 / restart.sh + plugin + governance 全含 V37.9.54). ⑦ **修适 V37.9.53 + V37.9.52 cascading lock**: V37.9.53 4 处 (cap_score 锁 10→12, verified_features 锁 {text,reasoning}→{text,vision,reasoning}, unverified flags 删 verified_vision, plugin 反验证守卫从 4 个→3 个) + V37.9.52 1 处 (test_verified_state_evolution 删 verified_vision 行). 8 ⑧ **累计**: 73→74 suites (+1: v37_9_54_abc) / 2488→2515 tests (+27) / 0 fail / governance v3.34→v3.35 / 71→72 invariants (+1 INV-PLIST-ENV-001) / 552→562 checks (+10: INV-RESTART-001 +5 + INV-PLIST-ENV-001 +5) / VERSION 0.37.9.53→0.37.9.54 / 安全 95/100. ⑨ **Mac Mini 部署后用户验证**: (1) `bash ~/auto_deploy.sh` 同步新 restart.sh + plugin + governance yaml (2) **首次 `bash ~/restart.sh`** marker 不存在 → 默认走 bootout+bootstrap 路径 (慢 ~1s 但保证 plist 最新), `/health` 仍正常 fallback_chain=[doubao, gemini]. (3) **第二次 `bash ~/restart.sh`** marker 已存在且 plist 未变 → 自动走 kickstart -k 快路径. (4) **用户改 plist** (PlistBuddy 加新 env) 后跑 `bash ~/restart.sh` → marker mtime < plist mtime → 自动 bootout+bootstrap 重读, env 自动生效 (不再需要手动 bootout). (5) `python3 ~/ontology/governance_checker.py --full` 跑 governance 应 INV-PLIST-ENV-001 ✅, runtime check 真读 plist 验证 critical envs 都在. ⑩ **元价值**: V37.9.13 双管理修复 + V37.9.53 plist env 缺失 + V37.9.54 ABC 同 PR 修 — 同款 launchd silent failure 第三次浮现后转化为治理. V37.9.54 让"plist 改了 daemon 没重读"从程序员记忆问题升级为 (a) restart.sh 自动检测+修复 (A) (b) governance 静态守 marker 逻辑 (INV-RESTART-001 扩展) (c) governance --full 真读 plist 验证内容 (INV-PLIST-ENV-001). 三层防御覆盖运维盲区, 未来加新 provider 时 plist 同步成为机器化 check 不再靠人记得. MR-4 silent-failure 元规则在 plist 场域的完整闭环. |
| V37.9.53 | 2026-05-11 | **Doubao flip verified_text + reasoning capability 落地 — Mac Mini E2E 实测通过后 framework 升级** — ① **触发**: V37.9.52 接入 doubao 后 Mac Mini 实测: `curl /chat/completions` → **HTTP 200 + 标准 OpenAI Chat Completions 兼容 JSON** + `finish_reason=stop` + 完整 `content` "你好呀😊!我是豆包..." + **额外发现 `reasoning_content` 字段** (doubao seed 2.0 是 reasoning model 类似 o1/DeepSeek-R1)。零 translator 需要, V37.9.52 选择的路径 A (OpenAI Chat Completions 兼容) 完全可行。② **framework 扩展 — ProviderCapabilities 新增 reasoning 维度**: providers.py 加 `reasoning: bool = False` + `verified_reasoning: bool = False` 字段 (默认 False 向后兼容 7 个 built-in)。`verified_features()` 追加 `if self.verified_reasoning: features.append("reasoning")`。`_capability_score()` 加分维度从 7 升 8 (text/vision/audio/video/tool_calling/streaming/json_mode/**reasoning**) + verified 维度从 5 升 6 (加 verified_reasoning)。`_capability_overlap` + 公开 `capability_overlap` 同步扩展 8 维度。③ **doubao plugin verified flags flip**: verified_text=True (Mac Mini 实测) + reasoning=True (响应 reasoning_content 实证) + verified_reasoning=True (同次实测确认). 保持 verified_vision=False / verified_tool_calling=False / verified_streaming=False / verified_fallback=False (V37.9.53 仅基于本次实测项 flip, 未测项守 False 等 V37.9.54+ vision E2E + 生产真 fallback fire)。④ **fallback chain 排名自动前移 — 通过 framework 自然推导达成"与 Qwen3 同级"**: doubao cap_score 升至 10 (6 base + 2 verified*2, 含 reasoning), gemini 9 (5 base + 2 verified*2). build_fallback_chain(qwen) 输出 doubao **第 1 位** (取代 gemini), gemini 退第 2 位。这是用户"和 Qwen3 同级别 LLM"诉求的兑现 — **零 adapter.py 改动 + 零硬编码**, V37 capability routing framework 已具备的自动排序根据真实能力数据自动得出。⑤ **25 新单测 6 类** test_v37_9_53_doubao_verified.py: TestReasoningCapabilityField(3 含 reasoning/verified_reasoning 默认 False + 7 个 built-in 向后兼容) + TestDoubaoVerifiedFlagsV9_53(4 verified_text=True / reasoning=True / verified_reasoning=True / 未测 flags 仍 False 守护) + TestDoubaoCapScoreRanking(3 含 doubao>gemini 必须 + cap_score 锁定 10 + gemini 不变 9) + TestVerifiedFeaturesIncludesReasoning(3 doubao 含 [text, reasoning] / 其他 7 不含 reasoning) + TestFallbackChainDoubaoFirst(2 chain[0]=doubao + chain[1]=gemini) + TestSourceLevelGuardsV9_53(10 含 providers.py reasoning 字段 / V37.9.53 marker / _capability_score 改动 / verified_features 改动 + plugin verified_text=True / reasoning=True / verified_reasoning=True / V37.9.53 marker + Mac Mini E2E 注释 / 未 flip flags 守 False)。⑥ **修适 V37.9.52 test_no_verified_features_yet → test_verified_state_evolution**: 旧 test 锁定全 False, V37.9.53 后无效。改为锁定 unverified flags (vision/tool_calling/streaming/fallback) 仍 False + 注释指引详细 V37.9.53 状态见新文件 test_v37_9_53_doubao_verified.py。⑦ **修 test_providers.py test_overlap_has_all_capability_fields**: expected_keys 从 7 维度集合扩到 8 维度集合 (加 reasoning)。⑧ **重要观察**: adapter.py 的 `_build_fallback_chain()` 已经从 V37 起**全自动**调用 `ProviderRegistry.build_fallback_chain(require_available=True)`, V37.9.53 升级 doubao 排名后 Mac Mini **重启 adapter 即生效** (或等 ADAPTER_HOT_RELOAD=true 配置下的热重载)。完全零 adapter.py 改动 + 零 FALLBACK_PROVIDER env 硬指定。⑨ **累计**: 72→73 suites (+1: v37_9_53_doubao_verified) / 2463→2488 tests (+25) / 0 fail / providers 仍 8 (V37.9.52 已加) / VERSION 0.37.9.52 → 0.37.9.53 / 安全 95/100。⑩ **Mac Mini 部署后行为变化** (用户验证): (a) `python3 ~/providers.py --fallback-chain qwen` 输出 doubao 第 1 位 + Available=yes (env 已配齐) (b) `bash ~/restart.sh` 重启 adapter, `/health` 输出的 fallback_chain 含 doubao 在第 1 位 (c) WhatsApp 测 vision (发图 + "看见了什么") 若触发 fallback (qwen2.5-VL quota 紧张时) 会路由到 doubao — 但 V37.9.54+ 才能真 flip verified_vision=True (需要 image_url content 实测)。⑪ **元价值**: V37.9.53 是 V37 Provider Compatibility Layer 设计的真实压力测试 — 用户 demo 揭示 doubao 是 reasoning model 后, framework 通过新增一个 capability 维度 (reasoning + verified_reasoning) **自动**让 fallback chain 重新排序, 不需要任何 if-then-else 硬编码 "doubao 优先". 这是 V3 路标"pip install ontology-engine"愿景的核心证据 — capability 声明 + 评分函数 + 排序算法的解耦让新 provider 类型 (reasoning model) 接入零改动其他 provider。原则 #18 "补证据而非补功能" 兑现 — 不写"if provider==doubao then rank=1"的硬规则, 只声明"我有 reasoning capability + 我已 verified", 自然进入第 1 位。 |
| V37.9.52 | 2026-05-11 | **Doubao Seed 2.0 Pro 接入第 8 个 provider (主力级 LLM, 与 Qwen3 同级) + V37 Provider Plugin Interface 首个真实插件触发 module 重入 bug 闭环** — ① **触发**: 用户提供 Volcengine Ark 凭证 (API key + endpoint ID `ep-20260511174451-dlhm8` + 模型 ID `doubao-seed-2-0-pro-260215`) 要求接入。doubao seed 2.0 是火山引擎主力 LLM, 用户明确"和 Qwen3 同级别 LLM"。② **API 路径决策**: 用户 demo 用 `client.responses.create()` (Volcengine Ark Responses API, input_image/input_text content type)。当前 7 个 provider 全部 OpenAI Chat Completions 兼容。**选 OpenAI Chat Completions 兼容路径优先** (零 adapter.py 改动, YAML/Python plugin 即可), 失败再走 Responses API translator (V37.9.53+ 候选)。**已 dev 测试无法直接调外网** (`Host not in allowlist` 拦截), 真验证留 Mac Mini。③ **新文件 `providers.d/doubao_provider.py`** (Python plugin, ~60 行): `DoubaoSeedProvider(BaseProvider)` + `name="doubao"` / `base_url="https://ark.cn-beijing.volces.com/api/v3"` / `api_key_env="ARK_API_KEY"` / `auth_style="bearer"`。**__init__ 从 `ARK_ENDPOINT_ID` env 读 endpoint ID 注入 model_id**, 缺 env / 空字符串 / 空白 → fallback 公开标识符 `doubao-seed-2-0-pro` (合约通过 + dev 环境无害)。能力声明: text+vision+tool_calling+streaming+json_mode 全开, **verified_*=False** (V37.9.52 未实战, 待 Mac Mini 实测后 V37.9.53+ flip)。**安全底线**: API key 严格走 env 绝不入代码 + 用户专属 endpoint ID 也走 env (即便用户豁免也守 public repo `bisdom-cell/openclaw-model-bridge` 公开可见安全底线)。④ **V37 Provider Plugin Interface 隐藏 bug 首次暴露**: `python3 providers.py` 直接执行 (__name__='__main__') 时, plugin 内 `from providers import BaseProvider` 触发 providers 模块**二次加载** (Python 视 __main__ 与 providers 为不同 module) → 产生两份独立的 BaseProvider 类对象 (id_a vs id_b) → plugin 子类 issubclass 检查永远失败 "No BaseProvider subclass found"。**之前未暴露原因**: `providers.d/` 目录只有 `_example.*` (下划线前缀自动跳过), 从未加载过真插件; V37.9.52 doubao 是首个真插件触发。**最小修复**: providers.py 顶部加 `if __name__ == "__main__" and "providers" not in sys.modules: sys.modules["providers"] = sys.modules[__name__]` 让 __main__ 模式下 alias 指向同一 module 避免重入。⑤ **32 新单测 5 类** test_v37_9_52_doubao_provider.py: TestDoubaoProviderRegistration(6 plugin 文件存在 / 注册到 default registry / 7+1=8 provider count / 无 plugin_errors / 合约验证 / metadata) + TestDoubaoEndpointIdHandling(4 dev 无 env → fallback / env 注入真实 ID / 空字符串 → fallback / 空白 → fallback) + TestDoubaoCapabilities(9 text/vision/tool_calling/streaming/json_mode + 无 audio/video + 256K context + verified_*=False 守卫 + default model 含 vision) + TestDoubaoInFallbackChain(2 build_fallback_chain(qwen) 含 doubao + 自 fallback 排除自身) + **TestModuleReentryGuard(3 V37.9.52 关键 fix 回归)**: `python3 providers.py --validate` exit 0 + 含 "OK doubao" / 直接执行列 8 providers / 源码守卫 sys.modules alias 行存在 + V37.9.52 注释存在 + TestSourceLevelGuards(8 V37.9.52 marker / base_url Volcengine 字面量 / ARK_API_KEY env / ARK_ENDPOINT_ID env / fallback 必须公开 + 用户专属 endpoint ID 不得硬编码 + API key 任何形式不得硬编码 (regex `ark-[a-f0-9]{8}-` 守卫) + Bearer 字面量禁止 + exactly one BaseProvider subclass + from providers import BaseProvider 契约)。⑥ **反向验证守卫真有效**: (a) sabotage 移除 providers.py sys.modules alias → `test_providers_py_has_sys_modules_alias_fix` 立即 AssertionError; (b) sabotage 把用户 endpoint ID `ep-20260511174451-dlhm8` 写入 plugin → `test_fallback_model_id_is_public_identifier` 立即 fail。还原后 32/32 全过。⑦ **接入**: auto_deploy.sh FILE_MAP 加 `providers.d/doubao_provider.py|$HOME/providers.d/doubao_provider.py` (auto_deploy line 374 已有 `mkdir -p $(dirname $DST)`, 部署到 `$HOME/providers.d/` 时目录自动创建) + full_regression.sh 加 suite `v37_9_52_doubao_provider`。**V37 deployment debt 闭环**: V37 Provider Plugin Interface 上线时 `providers.d/` 目录从未在 FILE_MAP, plugin 加载从未在 Mac Mini 真激活, V37.9.52 doubao 首次暴露并修复。⑧ **fallback chain 状态**: `python3 providers.py --fallback-chain qwen` 输出 doubao 在第 6 位 (cap_score=5 与其他后注册 provider 同, 按 registration order 落后)。用户要求"与 Qwen3 同级"暗示 verified_* flag 应 flip 后排名前移, 但 V37.9.52 守 verified=False 因为还没真测过 (不可在等价证明前提升优先级 — V35 原则)。⑨ **累计**: 71→72 suites (+1: v37_9_52_doubao_provider) / 2431→2463 tests (+32) / 0 fail / governance v3.34 不变 (本次无新 INV — 候选 INV-PROVIDER-PLUGIN-REENTRY-001 守卫 sys.modules alias 留 V37.9.53+ 一周观察后立) / 71 invariants / 552 checks / 16 meta rules / 安全 95/100 / VERSION 0.37.9.51 → 0.37.9.52 / **providers 总数 7→8**。⑩ **Mac Mini 部署后用户验证步骤**: (1) `export ARK_API_KEY="ark-..."` + `export ARK_ENDPOINT_ID="ep-20260511174451-dlhm8"` 到 ~/.zshrc (2) `cd ~/openclaw-model-bridge && git fetch origin main && git reset --hard origin/main` (3) `python3 ~/providers.py --validate` 应显示 `OK doubao` (4) `python3 ~/providers.py --fallback-chain qwen` 应看到 doubao + `Available: yes` (api key 配齐) (5) `curl -sS https://ark.cn-beijing.volces.com/api/v3/chat/completions -H "Authorization: Bearer $ARK_API_KEY" -H "Content-Type: application/json" -d '{"model":"'"$ARK_ENDPOINT_ID"'","messages":[{"role":"user","content":"你好"}]}' \| head -c 500` 真探测 Chat Completions 端点。**预期路径 A 成功** → 推送 V37.9.53 flip verified_text=True + 加入生产 fallback chain。**若 404/415**: 进入 V37.9.53 Responses API translator 工作量 3-4h。⑪ **元价值**: V37.9.52 是 **V37 Provider Plugin Interface 第一次产生新插件 + 第一次暴露 module 重入 bug + 第一次填补 providers.d/ FILE_MAP debt** — 三个"第一次"在同 session 解决, 让 V3 路标 "pip install ontology-engine" 终极目标的 Plugin 系统从"理论可行"升级为"真插件验证"。MR-7 兑现 — 治理框架的真实压力测试由用户提的"第 8 个 provider" 引爆, 而不是由我自检发现。 |
| V37.9.51 | 2026-05-11 | **Sub-Stage 4b 续 batch: 6 论文/repo/tweet 类脚本批量 5→6 字段 + rule_check 机械迁移 (V37.9.45 hf_papers / V37.9.50 semantic_scholar PoC 模板横向应用)** — ① **触发**: V37.9.50 PoC 收工后 ALIGNED_SCRIPTS 标记 11/21, 用户开工选 "Sub-Stage 4b 续 batch" 路径 (vs 决策窗口主硬卡 / 用户视角观察). 单 session 完成 6 脚本机械迁移 + 28 新单测 + 5 老测 alternation 更新 + governance 守卫升级 + 反向验证. ② **6 脚本迁移**: (1) `rss_blogs` (blog 场景, rule_content=title+description); (2) `dblp` (论文无 abstract, rule_content=title+venue 元数据兜底); (3) `arxiv_monitor` (论文有 abstract, rule_content=title+abstract); (4) `github_trending` (repo 场景, rule_content=full_name+description+topics); (5) `ai_leaders_x` (tweet 场景, rule_content=author+text); (6) `hn` (HN 帖子场景, rule_content=title+desc 清 HTML). 每个脚本统一改动 8 处: prompt 5 字段 → 6 字段 (加 🎚️ 项目对齐度 + OpenClaw 5 档评分指南 + 反幻觉守卫 "而非泛泛 AI 相关") / emit heredoc imports 加 `os` (V37.9.50-hotfix 防 NameError 同款) / `parse_5field_output` → `parse_6field_output` (加 alignment 字段 + 🎚️/🎚 双 emoji fallback) / lazy import project_alignment_scorer (4 helper + FAIL-OPEN try/except + 独立 log prefix `[rss]/[dblp]/[arxiv]/[gh_trending]/[ai_leaders]/[hn]`) / per-paper rule_check (LLM ⭐ vs keyword 一致性, validation invalid → format_validation_marker → ⚠️ marker 加入推送) / `high_alignment_count` 计数器 (⭐≥4 才累加) / 末尾"本轮高对齐 ⭐≥4: N/M" 统计行. ③ **新增 test_v37_9_51_sub_stage_4b_batch.py (28 单测 / 3 类)**: TestV37951SharedGuards(17 含 V37.9.51 marker / 🎚️ 字段定义 / 5 档评分指南 / 反幻觉守卫 / 6 字段声明替换 5 字段 / parse_6field_output 函数 + 老函数清除 / alignment 字段 + 🎚 fallback / emit 调 parse_6field + 显示 🎚️ + high_alignment_count / 4 helper lazy import / V37.9.50-hotfix emit heredoc os import 守卫 / rule_check 4 helper 守卫 + try/except / 评分门槛 llm_stars>=4 顺序锁 / V37.9.36 占位符严禁) + TestV37951PerScriptRuleContent(7 每脚本特有 rule_content 拼接 + total_<domain> 变量名约定) + TestV37951AlignedScriptsAudit(4 ALIGNED_SCRIPTS V37.9.51 锚点 + count ≥11 + 6 脚本路径都映射 V37.9.51 + audit_script aligned=True). **反向验证已确认守卫真有效**: sabotage rss_blogs 全局替换 V37.9.51 → V_BROKEN_ 立即 3 个 FAIL (rule_check 缺失 / emit os 缺 / marker 缺), 还原后 28/28 全过. ④ **ontology/llm_cron_audit.py ALIGNED_SCRIPTS**: 6 个脚本统一从 V37.9.36-37/V37.9.40/V37.9.41/V37.9.43/V37.9.44 升级到 V37.9.51, 总 11 条 (V37.5/V37.8.10/V37.9.16/V37.9.45/V37.9.50/V37.9.51×6) + 每条 comment 明确 Sub-Stage 4b 序号 (1/6, 2/6 等). ⑤ **5 老测试 alternation 更新 (MR-8 单一真理源, V37.9.50 同款模式)**: test_llm_cron_audit.py (V37.9.36-37 或 V37.9.51) / test_v37_9_40_dblp_ailead.py (V37.9.40 或 V37.9.51, 含 DBLP + AI Leaders X 双脚本) / test_v37_9_41_hn.py (V37.9.41 或 V37.9.51) / test_v37_9_43_arxiv.py (V37.9.43 或 V37.9.51) / test_v37_9_43_hotfix.py ("V37.9.43" 或 "V37.9.51" 字面量) / test_v37_9_44_github_trending.py (V37.9.44 或 V37.9.51). 不删老测试 (向后兼容防御作用), 改为 `self.assertIn(version, ("V37.9.X", "V37.9.51"))` alternation. ⑥ **governance INV-LLMCRON-AUDIT-001 守卫升级**: 合并 V37.9.36-37/V37.9.40/V37.9.41/V37.9.43/V37.9.44 5 个老锚点为 1 个 V37.9.51 file_contains + 1 个 V37.9.51 python_assert 端到端验证 (subprocess 跑 importlib 加载 ALIGNED_SCRIPTS 校验 6 路径 + 版本). audit_metadata.total_checks 555 → 552 (净 -3, 8 entries 替换 5 entries). ⑦ **累计**: 70→71 suites (+1: v37_9_51_sub_stage_4b_batch) / 2403→2431 tests (+28) / 0 fail / governance v3.34 不变 / 71 invariants 不变 (内涵升级非新增) / 555→552 checks (-3 合并老锚点) / 16 meta rules / 安全 95/100 / VERSION 0.37.9.50 → 0.37.9.51. ⑧ **Mac Mini 部署效果**: 部署 V37.9.51 后 6 脚本下次 cron 触发自动用新 6 字段格式: rss_blogs 18:00 / dblp 12:00 / arxiv 每 3h:00 (8 次/天) / github_trending 14:00 / ai_leaders_x 09:00+21:00 / hn 每 3h:45 (6 次/天). 用户 WhatsApp 应**首次看到 🎚️ 项目对齐度 + ⚠️ rule_check marker + 末尾"高对齐 ⭐≥4: N/M" 统计**横向覆盖所有论文/repo/tweet 类推送 (V37.9.45 hf_papers 单点 PoC → V37.9.50 semantic_scholar 双 PoC → V37.9.51 全量 8/21 ALIGNED 覆盖 audit P1+P2 路径). ⑨ **下次 (V37.9.52+) 候选**: (a) Sub-Stage 4c Top 5 高对齐推送段 (聚合所有今日 ⭐≥4 alignment 推 Dream/Radar/Evening 综合段) (b) Stage 5 kb_radar.sh 06:00 cron 综合分析 LLM prompt + 红/黄/蓝三档机会点推送 (c) 决策窗口续: Mac Mini 验证零漂移后激活 machine_sync (用户今日保留 dry-run 默认让 Mac Mini 实测可控) (d) SLO p95 50380ms 调查 (V37.9.50 候选). ⑩ **元价值**: V37.9.51 是 V37.9.36 (rss_blogs 血案 PoC) → V37.9.39 (S2 第 1 PoC) → V37.9.40-44 (5 字段批量) → V37.9.45 (hf_papers 6 字段 PoC) → V37.9.50 (semantic_scholar 续 PoC) → **V37.9.51 (6 脚本批量横向覆盖)** 的连环 16 commits 兑现, ALIGNED_SCRIPTS 4→5→7→8→9→10→11→**11** (V37.9.51 数量不变但 6 个脚本升级到 V37.9.51 同款 6 字段模板, 是真正的"加深"非"加宽"). MR-8 copy-paste-is-a-bug-class 兑现 — V37.9.45 hf_papers / V37.9.50 semantic_scholar 双 PoC 模板已机械化, V37.9.51 6 脚本约 30min/脚本完成. 原则 #18 "补证据而非补功能" 兑现 — 不增 cron job 数量, 但每个 paper/repo/tweet 推送的信息密度跃升一个数量级 (5 字段 → 6 字段 + rule_check 防 hallucination + 高对齐统计 = 真正可用的"信号过滤" framework). |
| V37.9.50-hotfix | 2026-05-10 | **emit heredoc 缺 `os` import 导致 rule_check FAIL-OPEN 跳过 (Mac Mini 实测发现, 高对齐统计虚假为 0)** — ① **触发**: V37.9.50 部署 Mac Mini 后用户手动跑 `bash jobs/semantic_scholar/run_semantic_scholar.sh` (12:21 触发), 看到 stderr `[s2] V37.9.50 project_alignment_scorer 缺失或失败: name 'os' is not defined (rule_check 跳过, FAIL-OPEN)` + `(LLM 解析成功 10, degraded 0, 高对齐 0)`. WhatsApp/Discord 推送 6 字段 + 🎚️ 项目对齐度内容**完美**(3 篇 ⭐⭐⭐⭐⭐ 全部 alignment 评分准确), **但**末尾"高对齐 0"统计是虚假值——LLM 输出 alignment 字段正确但 rule_check 完全没跑因 os not defined. ② **真根因**: emit heredoc (line 419 `python3 - "$PAPERS_FILE" "$RESULTS_FILE" "$DAY" "$MSG_FILE" << 'PYEOF'`) 顶部 `import sys, json, re` **缺 `os`**. V37.9.50 加的 lazy import `sys.path.insert(0, os.environ.get('HOME', os.path.expanduser('~')))` 调用 `os.environ` 时模块未导入 → NameError. try/except 捕获走 FAIL-OPEN 路径 → rule_check 完全跳过. ③ **MR-4 silent-failure 第 N 次演出新形态**: V37.9.50 自动化测试 + dev 环境 21/21 全过 + Mac Mini Preflight 80/0/2/1 全过, 但**实跑生产时 stderr WARN 才暴露真问题**. 这是原则 #15 "测试三层 (单测/preflight/真业务验证)" 的第 N 次正向兑现 — dev import 解析时不会触发 lazy import 块的 NameError, 只有真跑到 try/except 内部才会暴露. ④ **最小修复 (按 MR-10 三问)**: Q1 之前存在 ✓ V37.9.50 部署即有 / Q2 我引入 ✓ V37.9.50 lazy import 时 emit heredoc imports 没同步加 os / Q3 = emit heredoc line 420 `import sys, json, re` → `import sys, json, re, os` + 注释解释 V37.9.50-hotfix 用途. ⑤ **1 新单测** test_v37_9_50_semantic_scholar.TestV37950ShellGuards.test_v37_9_50_hotfix_emit_heredoc_imports_os: 找 emit heredoc 起点 + 检查后续 200 字符内 import 行必须含 os (regex `^import\s+[\w,\s]+` 多行模式 + alternation 兼容 `import os` / `import sys, json, re, os` / `import os, sys, ...` 等格式). 反向验证: 改回 `import sys, json, re` 立即 test fail. ⑥ **累计**: 70 suites 不变 / 2402→2403 tests (+1) / 0 fail / governance 555 checks 不变 / 71 invariants / 16 meta rules / 安全 95/100 / VERSION 0.37.9.50 不变 (hotfix 不 bump). ⑦ **Mac Mini 部署后预期行为**: 5/11 08:00 HKT 下次 S2 cron 触发后, **首次真正看到 V37.9.50 完整效果** — rule_check 真跑 + LLM ⭐ 评分与 keyword 不一致时显示 ⚠️ <reason> + 末尾"高对齐论文 ⭐≥4: N/M 篇"统计**真实**(非虚假 0). ⑧ **元价值**: V37.9.50 hotfix 验证用户视角观察原则 #13 第 17 次正向兑现 — 用户实跑 stderr 立即抓到 dev 单测漏掉的 NameError. 单测改进路径: 静态 grep import 行只能查 import 字面量, 真要防回归需要 mock `_concepts/_validate_alignment_score` 都成功 + run() 端到端跑 emit heredoc 真验证 high_alignment_count 计数, 但工程量高于本次 hotfix 必要性. |
| V37.9.50 | 2026-05-10 | **Sub-Stage 4b 1 脚本 PoC: semantic_scholar 5→6 字段升级 + rule_check 集成 (V37.9.45 hf_papers PoC 横向迁移)** — ① **触发**: V37.9.49 Sub-Stage 4a (#1 + #3 信号入推送链) 完成后用户选 "Sub-Stage 4b 续推进 (11 脚本)" 路径. 按 MR-10 三问"最小修复方案"严格执行 — V37.9.45 hf_papers 单脚本 488 insertions / 638 行变化数据揭示: 一次性 7 脚本 batch ≈ 3500+ 行 / 5-10h 超出剩余 session 时间. 用户 push back 决策选 "1-2 脚本 PoC 验证" 路径. ② **scope V37.9.50 = 1 脚本 PoC (semantic_scholar)**: V37.9.39 5 字段 → V37.9.50 6 字段 + rule_check, V37.9.45 hf_papers 同款模板横向迁移. 选 semantic_scholar 因 (a) 论文类与 hf_papers 结构最近 (b) daily cron (08:00 HKT) 真实推送数据多用户能立即感知效果 (c) 已 V37.9.39 fail-fast 对齐成熟. ③ **prompt 升级 (line 327-364)**: 顶部声明 "5 字段中文分析" → "6 字段中文分析 + OpenClaw 项目对齐评估师" / 加 🎚️ 项目对齐度 第 6 字段 + 评分指南 5 档锁定 (⭐⭐⭐⭐⭐ 直接相关 control plane / agent runtime / ontology / governance / convergence framework / fail-fast / memory plane / multimodal routing / opportunity radar / ⭐⭐⭐⭐ 间接相关 tool plugin / KB RAG / semantic search / drift detection / declarative policy / agent reliability / ⭐⭐⭐ 一般 AI/ML 趋势 / ⭐⭐ 无明显关联 / ⭐ 完全无关) + 反幻觉守卫 "项目对齐度评分必须基于'是否能为 OpenClaw 控制平面 / 记忆平面 / ontology engine 提供有价值的借鉴', 而非泛泛 AI 相关" + 输出格式举例新增 "🎚️ 项目对齐度: ⭐⭐⭐ / <一句话原因, ≤ 30 字>" 行. ④ **parse_5field_output → parse_6field_output (line 433-513)**: 函数定义重命名 + fields dict 加 'alignment' 键 + 字段头识别加 🎚️ 分支 (含 🎚 fallback 不依赖 variation selector U+FE0F) + state machine current_field='alignment' 处理 + regex `r'.*🎚️?\s*(?:项目)?对齐度?\s*[:：]?\s*(.*)'` 兼容多种 LLM prefix 变体. 5 字段函数体内的 cn_title / highlights / insight / practice / rating 字段全部保留 (V37.9.50 不删字段, 只加第 6 个). ⑤ **emit alignment + rule_check (line 519-580)**: msg_lines.append `🎚️ 项目对齐度: {fields['alignment']}` 行 + lazy import project_alignment_scorer (4 helper: load_project_concepts / validate_alignment_score / extract_star_count / format_validation_marker) + try/except FAIL-OPEN (模块缺失 / yaml 缺失 → 跳过 rule_check 不阻塞 cron + log 提示) + per-paper rule_check 调用 (extract_star_count(fields['alignment']) → llm_stars; validate_alignment_score(rule_content=title+tldr/abstract, llm_stars, _concepts); validation invalid → format_validation_marker → ⚠️ <reason> 行加入 msg_lines) + high_alignment_count 统计 (llm_stars ≥ 4 计数). 末尾追加 "本轮高对齐论文 (项目对齐度 ⭐≥4): N/M 篇" 统计行. ⑥ **21 新单测 3 测试类** test_v37_9_50_semantic_scholar.py: TestV37950ShellGuards(15 含 V37.9.50 marker / 🎚️ 字段定义 / 5 档评分指南 / 反幻觉守卫 "而非泛泛 AI 相关" / 6 字段声明 + 老 5 字段清除 / parse_6field_output 函数 + 老 parse_5field 清除 / 'alignment' 字段定义 / 🎚 fallback / parse_6field_output 调用 / 🎚️ 项目对齐度 emit / high_alignment_count + 高对齐论文统计 / lazy import + 4 helper / rule_check 调用 + try/except + rule_content 拼接 / format_validation_marker → ⚠️ marker / V37.9.36 占位符反模式守卫 line-by-line 跳过注释) + TestV37950LlmDegradedFallback(3 含 [LLM_DEGRADED] 保留 + tldr 优先 abstract 兜底 + **顺序锁** high_alignment_count += 1 必须在 if llm_stars >= 4 守卫之后 + 必须在 LLM_DEGRADED 块之后 防止误计 degraded 路径) + TestV37950InAuditAligned(3 含 ALIGNED_SCRIPTS V37.9.50 锚点 + count ≥11 + audit_script(s2) returns aligned=True+version=V37.9.50+findings=0 端到端验证). ⑦ **ontology/llm_cron_audit.py ALIGNED_SCRIPTS 升级**: jobs/semantic_scholar/run_semantic_scholar.sh V37.9.39 → V37.9.50 (6 字段 + alignment + rule_check, V37.9.45 hf_papers PoC 横向 Sub-Stage 4b 模板验证). ⑧ **governance INV-LLMCRON-AUDIT-001 守卫升级**: ALIGNED_SCRIPTS 含 V37.9.39 semantic_scholar 锚点 → V37.9.50 锚点 (升级守卫匹配 semantic_scholar 新版本号). ⑨ **V37.9.39 老 test 套兼容 (alternation)**: test_run_semantic_scholar_v37_9_39.py 4 失败测试 (parse_5field_output 函数定义 / parse 返回 dict 5 keys / S2 ALIGNED_SCRIPTS 字面量 / audit_script aligned_version) 升级为 V37.9.50 alternation 模式 (接受 parse_5field_output 或 parse_6field_output / V37.9.39 或 V37.9.50 字符串). MR-8 单一真理源兑现 — 老 test 不删除, 而是升级为兼容老/新两种实现. ⑩ **接入**: full_regression.sh +1 suite (auto_deploy FILE_MAP 不动 — semantic_scholar 已在 FILE_MAP). ⑪ **累计**: 69→70 suites (+1) / 2381→2402 tests (+21) / 0 fail / governance 555 checks 不变 (INV-LLMCRON-AUDIT-001 V37.9.39 守卫升级为 V37.9.50 字面量, 守卫数不变) / 71 invariants / 16 meta rules / 安全 95/100 / VERSION 0.37.9.49→0.37.9.50. ⑫ **下次 (V37.9.51+) 候选**: (a) **Sub-Stage 4b 续 batch** — 用 V37.9.50 semantic_scholar 验证模板成功后批量推剩 6 个论文/repo/tweet 类脚本 (rss_blogs / dblp / arxiv_monitor / github_trending / ai_leaders_x / hn). 每脚本同款迁移工作 30-45min, 总 3-5h. (b) **Sub-Stage 4c** — Top 5 高对齐推送段 (聚合所有今日 ⭐≥4 alignment 推到 Dream/Radar/Evening). 中工作量 1-2h. (c) **Stage 5 V37.9.52** — kb_radar.sh 06:00 cron + 综合分析 LLM prompt + 红/黄/蓝三档机会点推送. (d) **Mac Mini 验证 (硬卡)**: 装 sklearn + 5/11 决策窗口 + V37.9.49 部署后 03:00 kb_dream + 22:00 kb_evening cron 看 Radar 信号注入推送效果 + V37.9.50 部署后 08:00 semantic_scholar cron 看 6 字段 + ⚠️ rule_check marker 推送效果. ⑬ **Mac Mini 部署效果**: V37.9.50 部署后下次 cron (5/11 08:00 HKT) 推送 S2 高引论文时**首次出现** 🎚️ 项目对齐度 字段 + 末尾"本轮高对齐论文 ⭐≥4: N/10 篇"统计 + LLM 评分与 rule_check keyword 不一致时显示 ⚠️ 标记 (V37.9.45 hf_papers 同款 user-visible 验证模式). ⑭ **元价值**: V37.9.45 hf_papers PoC → V37.9.50 semantic_scholar PoC 横向迁移完整闭环 — 验证 V37.9.45 同款 6 字段 + rule_check 模板可机械迁移到论文类同结构脚本. **下次 V37.9.51+ batch 6 个脚本时不需要重新设计模板**, 直接复用 + 适配各脚本特定结构 (如 dblp 无 abstract / hn RSS desc / github_trending repo metadata / ai_leaders_x tweet 上下文 / arxiv_monitor abstract 兜底 / rss_blogs description 兜底). 用户视角原则 #13 第 16 次正向兑现 — V37.9.49 Sub-Stage 4a 部署后立即 Sub-Stage 4b 推进, 单 session 内闭环 4a (#1+#3 信号入推送链) + 4b PoC (单脚本模板验证) 双交付. |
| V37.9.49 | 2026-05-10 | **Stage 4a 三件套 cascade 集成: #1 + #3 接入 kb_dream/evening 推送链 (信号入推送链)** — ① **触发**: V37.9.48 Stage 3 收工后用户选 "Stage 4 三件套集成 (cascade)" 路径. 设计文档 7.1 节 Stage 4 范围 = 三件套协同集成 (kb_dream Phase 1.5 + kb_dream/evening 加速主题段 + 11 脚本批量 6 字段 + Top 5 段). 按 MR-10 三问"最小修复方案"严格执行不一次性大爆炸, 拆 4a/4b/4c 子阶段. ② **scope V37.9.49 = Sub-Stage 4a only**: (a) kb_dream.sh Phase 1.5 集成 #1 cross_source + #3 trend_acceleration → REDUCE_DATA 注入 "Opportunity Radar #1/#3" 双段 (b) kb_evening_collect.py 集成 #3 trend_acceleration → build_evening_prompt 加 trend_signals 参数 + collect_trend_signals_for_evening helper. **不做** (Sub-Stage 4b/4c): 11 脚本批量 6 字段 / Top 5 高对齐推送段 / kb_dream Phase 1.5 升级到 LLM-driven topic summary. ③ **kb_dream.sh Phase 1.5 (~80 行新增 line 869-955)**: 在 Phase 2 Reduce 之前调 `python3 cross_source_signal_aggregator.py --json` + `python3 kb_trend_acceleration.py --json` 输出 daily/weekly JSON, 解析 top 5 共振 + 各 archetype top 3 加速度信号格式化为 RADAR_SIGNALS_BLOCK / TREND_SIGNALS_BLOCK bash 变量. **FAIL-OPEN 契约**: scorer 文件不存在 → 跳过 / python3 调用失败 → 信号设空 / JSON 解析失败 → 信号设空 / 任何环节失败不阻塞 Reduce. RADAR_SCORER / TREND_SCORER 双路径查找 ($HOME 优先 fallback dirname $0). REDUCE_DATA 注入双 section header "═══ Opportunity Radar #1/#3 ═══" + 注意提示 "标 🚨 早期机会点但只有今日笔记/sources 有具体支持才能展开" 防 LLM 把 Radar 信号无中生有. ④ **kb_evening_collect.py 集成 #3** (~70 行新增): (a) `collect_trend_signals_for_evening(radar_dir, max_strong=3, max_decel=2)` helper — 读最新 weekly_trends_*.json (lazy + FAIL-OPEN 缺文件返空) + 按 archetype 分组 (strong+mild / decel+obs) + 格式化 "加速主题/减速主题" 双段. (b) `build_evening_prompt` 加 `trend_signals=None` 可选参数 (向后兼容 V37.6/7 调用) + trend_block 注入 "本周趋势加速度" 段在 prompt 末尾 + 注意提示 "辅助'明日关注'段判断方向, 不能直接当今日要闻输出". (c) run() orchestrator 调 collect_trend_signals_for_evening + 注入 build_evening_prompt. ⑤ **9 新单测 2 测试类** test_v37_9_49_radar_integration.py: TestEveningCollectTrendIntegration(4 含 missing radar_dir / no weekly_trends files / real data 双段格式化 / build_evening_prompt with trend_signals + 向后兼容 None) + TestKbDreamPhase15ShellGuards(5 源码守卫 V37.9.49 Sub-Stage 4a marker / Phase 1.5 块 / RADAR_SCORER + RADAR_SIGNALS_BLOCK / TREND_SCORER + TREND_SIGNALS_BLOCK / Opportunity Radar #1+#3 双 section header + 早期机会点提示 / FAIL-OPEN 契约 + scorer 存在性检查). ⑥ **接入**: full_regression.sh +1 suite (auto_deploy FILE_MAP 不动 — kb_dream/kb_evening_collect 已在 FILE_MAP). ⑦ **累计**: 68→69 suites (+1) / 2372→2381 tests (+9) / 0 fail / governance 555 checks 不变 / 71 invariants / 16 meta rules / 安全 95/100 / VERSION 0.37.9.48→0.37.9.49. ⑧ **下次 (V37.9.50+) 候选**: (a) **Sub-Stage 4b** — 11 脚本批量 5→6 字段 + 集成 rule_check (acl_anthology / karpathy_x / openclaw_official×2 / dblp / arxiv / s2 / github_trending / hn / rss_blogs 同款 V37.9.45 hf_papers 模式机械迁移). 每脚本 ~30-60min, 总 5-10h 大工作量. (b) **Sub-Stage 4c** — Top 5 高对齐推送段 (聚合所有今日 ⭐≥4 alignment 推到 Dream/Radar/Evening). 中工作量 1-2h. (c) **Stage 5 V37.9.51** — kb_radar.sh 06:00 cron + 综合分析 LLM prompt + 红/黄/蓝三档机会点推送. (d) **Mac Mini 验证 (硬卡)**: 装 sklearn + 5/11 决策窗口 jobs_to_crontab + kb_sources_to_index machine_sync 真激活 + V37.9.49 部署后下一次 03:00 kb_dream + 22:00 kb_evening cron 看 Radar 信号注入推送效果. ⑨ **Mac Mini 部署效果**: V37.9.49 部署后下次 cron 触发: (1) 03:00 kb_dream Phase 1.5 自动调 cross_source/kb_trend_acceleration 生成 JSON → 注入 Reduce LLM prompt → 用户在 03:00 推送的 "梦境" 中**首次出现**"Opportunity Radar #1/#3" 双段 (含 top 5 共振信号 + 加速主题). (2) 22:00 kb_evening 自动读 weekly_trends_*.json 注入 evening prompt → 用户在 22:00 推送的"晚间整理"中**首次出现** "明日关注"段含本周加速主题判断方向. ⑩ **元价值**: V37.9.46-49 四阶段 PoC + 集成完结的 Stage 4a — Opportunity Radar 三件套从"独立模块"升级到"信号入推送链"的关键拐点. **下一阶段 4b 是机械迁移工作 (11 脚本)**, 4c 是简单聚合, Stage 5 是新 cron job — 都是 Sub-Stage 4a 这个 cascade 链路打通后的延伸. 用户可在 V37.9.49 Mac Mini cron 真跑数据验证后再决定继续 4b/4c 节奏 (按 MR-10 三问"先验证再 cascade" 谨慎推进). |
| V37.9.48 | 2026-05-10 | **Opportunity Radar Stage 3 — 趋势加速检测 (4 周历史关键词加速度 + 5 档分类)** — ① **触发**：V37.9.47 Stage 2 收工后用户选 "Stage 3 续推进 (#3 趋势加速)" 路径. 设计文档 docs/opportunity_radar_design.md 5 节锁定: #3 趋势加速度 = kb_trend.py 仅算"本周 vs 上周"频率变化, 加速度是二阶导数, 反映动态 (是否连续多周加速). 三件套第三维度补全 = 时间维度. ② **scope (MR-10 三问)**: Q1 ✓ V37.9.46-47 Stage 1+2 同款渐进路径 (#1 + #2 已就位, #3 是补全缺失维度) / Q2 ✗ Stage 3 是规划路径 / Q3 = kb_trend_acceleration.py + 28 单测 + 不集成下游 (Stage 4 才集成 kb_dream/kb_evening, MR-10 三问严格执行不 cascade). ③ **新文件 kb_trend_acceleration.py (~390 行纯 Python, 5 函数 + run/main CLI)**: (a) `extract_keywords_per_week(week_offset, kb_dir, today)` — 复用 kb_trend.py extract_period_text + extract_keywords (lazy import, FAIL-OPEN 缺模块 → 空 dict). week_offset=1 → 今天-7~今天-1 (上周), week_offset=2 → 今天-14~今天-8 (上上周), etc. (b) `compute_acceleration(week_keywords, min_freq_pct=0.005)` — 输入 dict {1: w1_kw, 2: w2_kw, 3: w3_kw}, 用各周占比 pct 而非原始 freq 防总数差距失真. **关键契约**: w2=0 (新词) → skip (无 baseline) / w3=0 → accel_2w=None (但 accel_1w 仍计) / freq < min_freq_pct → skip 小基数门槛 (设计文档 11.2 风险表). 输出 dict {kw: {freq_w1/w2/w3, pct_w1/w2/w3, accel_1w (=pct_w1/pct_w2), accel_2w (=pct_w1/pct_w3 或 None), classification}} (c) `classify(accel_1w, accel_2w=None)` — **5 档锁定** (设计文档 5.1): 🚀 strong: a1≥1.5 AND a2≥1.5 (连续 2 周加速 = 高信号) / ⚰️ obsolescence: a1<0.5 AND a2<0.5 (连续 2 周衰退) / 📈 mild: a1≥1.5 (本周加速 / a2 弱或 None) / 💧 deceleration: a1<0.7 (本周减速) / 📊 stable: 其他 (0.7≤a1<1.5). 顺序锁定 strong > obs > mild > decel > stable. 非数字输入安全降级 STABLE. (d) `rank_signals(metrics, top_strong=10, top_stable=5, top_obs=3)` — 按 archetype priority 分桶 (high_priority=strong+mild / stable / decel_obs) + 同桶按 |a1-1| desc 排 (偏离 1 越远越显著) + 各桶截断. 总 max 18 signals. (e) `emit_radar_json(signals, week, output_dir)` — ~/.kb/radar/weekly_trends_{week}.json + archetype_summary 计数 + tmp+os.replace 原子写. (f) CLI: `--kb-dir X --output-dir Y --json`. ④ **FAIL-OPEN 契约 (V37.9.46/47 同款 lazy import)**: 模块顶部只 stdlib (os/sys/json/glob/datetime/Counter/timedelta), kb_trend lazy import 包 try/except. 任何环节失败 → emit_radar_json([]) 不阻塞. **MR-11 兑现**: log() file=sys.stderr 防 $(...) 命令替换污染. ⑤ **28 新单测 7 测试类** test_kb_trend_acceleration.py: TestExtractKeywordsPerWeek(4 含 invalid week_offset / dict 类型契约 / FAIL-OPEN 缺 kb_trend / 日期范围 today 注入) + TestComputeAcceleration(5 含 3 周历史正常 ontology 强加速 / w2=0 新词 skip / w3=0 → accel_2w=None / min_freq_pct 噪声门槛 / 空输入安全) + TestClassify(6 含 strong 必须 a1+a2 / obs 必须 a1+a2 / mild 仅要 a1 / decel 仅要 a1 / stable 中间区 0.7≤a1<1.5 / 非数字 fallback STABLE) + TestRankSignals(4 含 archetype 优先级 strong>mild>decel>obs>stable / top_k 截断 per bucket 18 max / 空输入 / 桶内 |a1-1| desc 同档内排序) + TestEmitJson(3 含 JSON 完整性 + archetype_summary / weekly_trends_{week}.json 路径 / 嵌套自动 mkdir) + TestBackwardCompat(3 含 kb_trend.extract_period_text/tokenize/extract_keywords 旧接口仍 callable 不 break, V29.5 现有功能完全兼容) + TestSourceLevelGuards(3 V37.9.48 marker / 5 档阈值锁定 1.5/0.7/0.5 + 5 archetype 常量 + 5 emoji 🚀📈⚰️💧📊 / log() file=sys.stderr MR-11). **反向验证守卫真有效**: sed `ACCEL_STRONG_THRESHOLD = 1.5` → `0.5` → test_design_locked_thresholds 立即 AssertionError. 还原后 28/28 全过. ⑥ **Smoke test E2E**: 构造 4 keyword 测试集 (cot pruning 加速 1.92x/3.54x → strong / speculative decoding 1.54x/1.77x → strong / agent 0.92x → stable / kernel cuda 0.32x/0.20x → obsolescence) — 全部按设计文档 5.1 节预期分类工作. ⑦ **接入**: auto_deploy.sh FILE_MAP +1 entry (`kb_trend_acceleration.py|$HOME/...`) + full_regression.sh +1 suite. ⑧ **累计**: 67→68 suites (+1) / 2344→2372 tests (+28) / 0 fail / governance 555 checks 不变 (本次无新 INV — 设计文档 9 节 INV-RADAR-001 留 Stage 6 三件套全部就位后立) / 71 invariants / 16 meta rules / 安全 95/100 / VERSION 0.37.9.47→0.37.9.48. ⑨ **下次 (V37.9.49+) 候选** (按设计文档 7.1 节 Stage 4-6): (a) **Stage 4 V37.9.49** — 三件套协同集成: #1 cross_source 集成 kb_dream Phase 1.5 (Reduce LLM prompt 加"今日跨 source 共振信号"段) + #3 trend_acceleration 集成 kb_dream/kb_evening 推送加"加速主题/减速主题"段 + #2 批量推 6 字段到所有 11 已对齐脚本 + 集成 rule_check 全脚本 + 推送加"高对齐 Top 5"段 (b) **Stage 5 V37.9.50** — kb_radar.sh 06:00 cron 新 job + 综合分析 LLM prompt 设计 + 红/黄/蓝三档机会点推送 + Mac Mini cron 注册 (c) **Stage 6 V37.9.51** — 数据可视化 (kb_concept_map.py + 周生成 HTML) + governance INV-RADAR-001 立 + 路线图收工文档 (d) **Mac Mini 验证**: 装 scikit-learn (sentence-transformers V29.3 已装) → 跑 cross_source_signal_aggregator + V37.9.45+ hf_papers cron 看真实 alignment + ⚠️ 标记效果 + V37.9.48 kb_trend_acceleration 跑出真实 4 周历史关键词加速度 (e) **5/11 决策窗口 (明天)**: jobs_to_crontab + kb_sources_to_index machine_sync dry-run 真激活. ⑩ **Mac Mini 部署效果**: V37.9.48 部署后 kb_trend_acceleration.py 可手动触发 `python3 ~/kb_trend_acceleration.py --json` 看 4 周真实 KB 数据加速度. cron 注册留给 Stage 5 kb_radar.sh 06:00 (设计文档 6.1 节). ⑪ **元价值**: V37.9.48 完成三件套 #1+#2+#3 PoC 三维度全部就位. **下一阶段 Stage 4 集成 = "广度 14 sources × 290 notes/day → 深度 #1 跨源共振 ∩ #2 项目对齐 ∩ #3 趋势加速 = 高价值早期信号"完整链路** — 这是 V3 路标 `pip install opportunity-radar` 终极目标的算法基础全部就位. 但 V37.9.48 严格按 MR-10 三问"最小修复方案"执行不 cascade — Stage 4 集成留给下次 session, 当前 PoC 阶段不引入下游耦合让单独验证三件套独立性. |
| V37.9.47 | 2026-05-10 | **Opportunity Radar Stage 2 — 项目对齐度 rule_check 验证层 + hf_papers 集成** — ① **触发**：V37.9.46 Stage 1 收工后用户选 "Stage 2 续推进 (#2)" 路径. 设计文档 docs/opportunity_radar_design.md 4 节锁定 = #2 项目对齐度评分 = LLM 主观 ⭐ 数 + 离线 keyword-based rule_check 双层验证, 防 V37.9.45 hf_papers PoC 单 LLM 评分的 hallucination. ② **scope (MR-10 三问)**: Q1 ✓ (V37.9.45 inline parser + LLM prompt 已加 🎚️ 字段, 但**没有 rule_check 验证层**) / Q2 ✗ (Stage 2 是 V37.9.45 PoC 后规划的下一步, 设计就位) / Q3 = project_alignment_scorer.py + 31 单测 + hf_papers.sh emit-stage 集成 (灰度 1 个脚本 — 设计文档 7.1 节 Stage 2 范围, **不**批量推 11 脚本 / **不**加 Dream/Radar Top 5 段, 这些留 Stage 4). MR-10 三问严格执行 — 不 cascade. ③ **新文件 project_alignment_scorer.py (~430 行纯 Python, 7 函数)**: (a) `load_project_concepts(path)` — 轻 YAML parse (kb_review_collect 同款 fallback 模式避新增 PyYAML 依赖) + V37.9.47 修 bug: 行尾注释 `weight: 5  # 最高优先级` 让 int() 抛 ValueError 退化为 0, 修复用 `val.split("#",1)[0].strip()` 剥离注释. FAIL-OPEN 缺文件 → 默认 dict (b) `count_keyword_hits(content, concepts)` — 大小写不敏感 + 中英 substring (in 操作不分词) + 同 keyword dedup (set semantics) + excluded_topics weight × -2 强降权; total_score = positive - 2 × negative (c) `compute_expected_range(score)` — 5 档锁定 (设计文档 4.2): score<0 → ⭐1-2 / score=0 → ⭐1-2 / 1-2 → ⭐2-3 / 3-4 → ⭐3-4 / 5+ → ⭐4-5 (d) `validate_alignment_score(content, llm_score, concepts)` — LLM ⭐ 数 vs expected_range 一致性: in_range → validated=True / 偏高 → reason "LLM 评 ⭐{N} 偏高 (rule 预期 ⭐{min}-{max}, 仅命中 {N} 关键词)" / 偏低 → reason "LLM 评 ⭐{N} 偏低 (rule 预期 ⭐{min}-{max}, 命中 {N} 关键词暗示更高对齐)" / invalid llm_score (非 1-5 int) → validated=False + reason 解释 (e) `parse_6field_output(content)` — **MR-8 单一真理源**: 行为与 hf_papers.sh inline parse_6field_output 完全一致 (仅 📌 字段做 inline 提取, 其他字段必须 LLM 多行格式). 测试 `test_module_parser_matches_hf_papers_inline_behavior` grep hf_papers.sh 守卫两侧不漂移 (f) `extract_star_count(text)` — 取最大连续 ⭐ 段 (如 "⭐⭐⭐⭐ / 直接相关" → 4) (g) `format_validation_marker(validation)` — validated=False 时返回 "⚠️ <reason>" 给推送显示, validated=True 返回空. ④ **hf_papers.sh emit-stage 集成 (line 466-480 + 633-650)**: (1) Python heredoc 顶部 lazy import project_alignment_scorer + load_project_concepts (一次, FAIL-OPEN 缺模块 → log WARN 跳过) (2) 每 paper alignment 段展示后调 extract_star_count(fields['alignment']) → validate_alignment_score(title+tldr+abstract, llm_stars, concepts) → format_validation_marker → 不一致时 msg_lines.append(marker). 用户在 WhatsApp 看 hf_papers 推送时, alignment 行下面**直接出现** ⚠️ 标记 + reason — 立即识别 LLM hallucination. ⑤ **31 新单测 7 测试类** test_project_alignment_scorer.py: TestProjectConceptsYaml(5 含真 yaml 加载 / 三 section 结构 / weight 范围验证 / keywords 非空 / FAIL-OPEN 缺文件) + TestKeywordHitCounting(6 含多关键词 / 大小写不敏感 / 中英混合 / excluded × -2 降权 / dedup / 空内容) + TestExpectedScoreRange(4 含 0/5+/边界/负数) + **TestValidateAlignmentScore(4 含 V37.9.47 关键血案场景**: LLM=5 + 0 命中 → not validated + "LLM 评 ⭐5 偏高 仅命中 0 关键词" / LLM in range → validated / LLM 偏低 / invalid score (0/6/99/-1/None/3.5) 各种类型) + TestPromptInjection(3 hf_papers.sh prompt 含 🎚️ + 评分指南 + 一句话原因 30 字) + TestParse6FieldOutput(5 完整 / 缺字段 / 顺序乱序 / 空 / **MR-8 grep hf_papers inline 守卫**: assertIn `def parse_6field_output(content):` + 6 字段 keys + 6 emoji 字段头) + TestSourceLevelGuards(4 V37.9.47 marker / _SCORE_RANGES 5 档锁定 (`(0, 0, 1, 2)` + `(5, 999, 4, 5)` + `(-999, -1, 1, 2)`) / FAIL-OPEN 注释 / log() file=sys.stderr MR-11). **反向验证两层守卫真有效**: (a) sed `(5, 999, 4, 5)` → `(5, 999, 1, 2)` → test_high_hits_high_stars 立即 AssertionError; (b) test_module_parser_matches_hf_papers_inline_behavior grep hf_papers.sh 6 emoji 字段头 + 6 字段 keys 确保 MR-8 单一真理源不漂移. 还原后 31/31 全过. ⑥ **Smoke test E2E**: 构造 paper 1 (高对齐 content + LLM=5 → validated=True 无 marker) + paper 2 (低对齐 content "ImageNet ResNet" + LLM=5 → validated=False + marker `⚠️ LLM 评 ⭐5 偏高 (rule 预期 ⭐1-2, 仅命中 0 关键词)`) — 两个 case 全部按设计文档 4.2 节预期工作. ⑦ **接入**: auto_deploy.sh FILE_MAP +1 entry (`project_alignment_scorer.py|$HOME/...`) + full_regression.sh +1 suite. ⑧ **累计**: 66→67 suites (+1) / 2313→2344 tests (+31) / 0 fail / governance 555 checks 不变 (本次无新 INV — 设计文档 9.2 MR-18 候选"opportunity-radar-must-cross-validate-llm-vs-rule"待 Stage 2 多日实例积累后立) / 71 invariants / 16 meta rules / 安全 95/100 / VERSION 0.37.9.46→0.37.9.47. ⑨ **下次 (V37.9.48+) 候选** (按设计文档 7.1 节 Stage 4-6): (a) Stage 4 — 三件套协同集成: #1 cross_source 集成 kb_dream Phase 1.5 (Reduce LLM prompt 加"今日跨 source 共振信号"段) + #2 批量推 6 字段到所有 11 已对齐脚本 (acl_anthology / karpathy_x / openclaw_official×2 / dblp / arxiv / s2 / github_trending / hn / rss_blogs) + 集成 rule_check 全脚本 + 推送加"高对齐 Top 5"段 (b) Stage 3 V37.9.48 — kb_trend_acceleration.py 28 单测 (4 周历史关键词加速度 + 5 档分类: 🚀 strong 1.5x×2w / 📈 mild 1.5x×1w / 💧 deceleration / ⚰️ obsolescence / 📊 stable) (c) Stage 5 — kb_radar.sh 06:00 cron + 综合分析 LLM prompt + 红/黄/蓝三档机会点推送 (d) Mac Mini 验证: 装 scikit-learn (sentence-transformers V29.3 已装) → 跑 cross_source_signal_aggregator + V37.9.45+ hf_papers cron (10:00/20:00 HKT) 看真实 alignment + ⚠️ 标记效果 (e) **5/11 决策窗口 (明天)**: jobs_to_crontab + kb_sources_to_index machine_sync dry-run 真激活. ⑩ **Mac Mini 部署效果**: V37.9.47 部署后 hf_papers 下次 cron (5/10 20:00 HKT) 推送时, alignment 段下方**首次出现** ⚠️ 标记 (如 LLM 评分与 rule 不一致). 用户在 WhatsApp 第一时间识别 LLM hallucination, 验证 #2 PoC 完整价值. ⑪ **元价值**: V37.9.47 是 V37.9.45-46 PoC 三件套的关键拐点 — V37.9.45 立 #2 LLM 主观评分, V37.9.46 立 #1 跨源 weak signal, **V37.9.47 立 #2 rule_check 验证层让主观评分可被机器交叉验证**. 三层"LLM 主观 + 工程量化 + 跨 source 共振"协同 = "高价值早期信号"识别 framework 雏形, 是 V3 路标 `pip install opportunity-radar` 的核心算法基础. MR-10 三问严格执行不 cascade — Stage 4 集成 / 批量推 / Top 5 推送等留给后续 session. |
| V37.9.46 | 2026-05-10 | **Opportunity Radar Stage 1 PoC — 跨 source 弱信号聚合器（V37.9.45 收工承诺"下次 session 干净启动 Stage 1"兑现）** — ① **触发**：V37.9.45 收工时本周焦点明确登记 "下次 session 干净启动 Opportunity Radar Stage 1 (#1 跨 source 弱信号聚合, 33 单测, sentence-transformer + DBSCAN)". 用户开工选 Stage 1 路径 (vs audit P2 续 / 观察期等待 / 其他方向). ② **scope (设计文档 docs/opportunity_radar_design.md 第 7.1 节锁定)**: V37.9.46 = Stage 1 PoC 仅交付独立模块 + 33 单测，**不集成下游** (kb_dream Phase 1.5 集成留 Stage 4 / project_alignment_scorer rule_check 留 Stage 2 / kb_radar.sh 06:00 cron 留 Stage 5 / kb_concept_map 可视化留 Stage 6). MR-10 三问严格执行 — 不 cascade. ③ **新文件 cross_source_signal_aggregator.py (~410 行纯 Python, 6 核心函数 + run/main CLI)**: (a) `scan_today_notes(date, kb_dir)` — 扫 ~/.kb/notes/{YYYYMMDD}*.md, 解析 frontmatter (kb_write.sh 同款格式: date/tags/source/type), tags[0] 作 source_name (向后兼容 kb_save_arxiv 写入的"arxiv-ai-models"/"hf-papers"等 tag)+ extract title (第一行 #) + abstract (后续段落 ≤500 chars, skip ## 子标题) (b) `embed_notes(notes, cache_dir)` — lazy import numpy + local_embed.embed_texts (sentence-transformer 384d), content_hash 16-char cache key (V37.4 mtime-stable 同款), cache 命中跳过 sentence-transformer 重算 (c) `cluster_dbscan(embeddings, min_samples=3, eps=0.35)` — short-circuit N<min_samples 在 lazy import sklearn **之前** (避免 dev 无 sklearn 时小输入也抛 ImportError), DBSCAN(metric=cosine) (d) `filter_cross_source(labels, notes, embeddings)` — unique_sources>=2 跨源契约, _avg_intra_similarity 用 numpy 矩阵 dot (cosine since L2-normalized), _suggest_topic_keyword 中英文 regex 抽 cluster 内最高频 top-3 keywords (Stage 4 升级 LLM 一句话总结) (e) `rank_signals(signals, top_k=10)` — score=source_count×2+note_count+avg_intra_similarity×5 (f) `emit_radar_json(signals, date)` — 写 ~/.kb/radar/daily_signals_{date}.json + tmp+os.replace 原子写, 简化 notes 字段防大对象. ④ **FAIL-OPEN 契约 (V37.4 同款 lazy import 模式)**: 模块顶部只 import stdlib (os/sys/json/glob/re/hashlib/datetime/Counter), 任何重依赖 (numpy/sklearn/sentence_transformers/local_embed) 都 lazy import 包 try/except, 失败 raise ImportError. run() orchestrator 顶层 try/except ImportError → status='fail_open_no_deps' + emit_radar_json([]) 不阻塞下游. 单笔记 OSError → log WARN 跳过, 其他笔记继续. CLI exit 0 for ok/no_notes/fail_open_no_deps (FAIL-OPEN 不阻塞), exit 1 仅 unexpected failure. ⑤ **MR-11 兑现**: log() `print(..., file=sys.stderr)` 防 $(...) 命令替换污染 (V37.8.6 Dream 自引用幻觉血案教训扩展). source-level guard 在 TestSourceLevelGuards.test_log_writes_to_stderr_mr11 验证 log() 函数体含 `file=sys.stderr`. ⑥ **33 新单测 8 测试类** test_cross_source_signal_aggregator.py: TestScanTodayNotes(5 含 YYYYMMDD 日期过滤 / frontmatter 完整解析 / 缺字段 default unknown / tags 单值 OR list 双格式 / 无效 date 安全返回空) + TestEmbedNotes(4 含 empty 输入 numpy.zeros (0,384) / 文本截断 ≤500 / cache hit os.path.isfile mock 模拟 / force_recompute bypass) + TestClusterDbscan(5 含 empty 返回空 / N<min_samples short-circuit 在 sklearn import 之前 (V37.9.46 顺序锁) / lazy import sklearn ImportError / 单 cluster / 多 cluster + DBSCAN 参数 design-locked 验证 metric=cosine) + TestFilterCrossSource(4 含单 source 拒绝 / 跨 2 源保留 / 跨 4 源高信号 + suggested_topic 含 agent/runtime / noise label -1 排除) + TestRankSignals(3 含公式正确性 / top_k 截断 / 空输入) + TestEmitRadarJson(3 含 JSON 字段完整性 / daily_signals_{date}.json 路径 / 自动 mkdir 嵌套目录) + TestFailOpenContract(4 含 no_notes status / numpy 缺失 fail_open / sklearn 缺失 fail_open (用 builtins.__import__ patch 真抛 ImportError) / 单笔记 OSError 不致命 mock builtins.open). + TestSourceLevelGuards(5 V37.9.46 marker / DBSCAN_MIN_SAMPLES=3 + DBSCAN_EPS=0.35 设计锁定 / FAIL-OPEN 契约文档 / 反 module-top-import 守卫前 60 行禁 numpy/sklearn/sentence_transformers/local_embed / log() stderr MR-11). **反向验证守卫真有效**: sed `DBSCAN_MIN_SAMPLES = 3` → `1` → test_design_locked_constants 立即 AssertionError; 模块顶部加 `import numpy` → ModuleNotFoundError 在 import 时立即触发 (dev 无 numpy 强证守卫 — 即使没跑测试也防住). 还原后 33/33 全过. ⑦ **接入**: auto_deploy.sh FILE_MAP 加 `cross_source_signal_aggregator.py|$HOME/...` (project_concepts.yaml 已在 V37.9.45 加) + full_regression.sh 加 suite. ⑧ **设计文档 docs/opportunity_radar_design.md** 第三节锁定常量 (DBSCAN_MIN_SAMPLES=3 / DBSCAN_EPS=0.35 sentence-transformer all-MiniLM-L6-v2 同主题阈值经验值 / MIN_UNIQUE_SOURCES=2 跨源契约 / TOP_K_SIGNALS=10 / TEXT_TRUNCATE_CHARS=500). ⑨ **累计**: 65→66 suites (+1: cross_source_signal_aggregator) / 2280→2313 tests (+33) / 0 fail / governance 555 checks 不变 (本次无新 INV — 设计文档 9.1 Stage 6 立 INV-RADAR-001, V37.9.46 PoC 阶段不立) / 71 invariants 不变 / 16 meta rules / 安全 95/100 / VERSION 0.37.9.45→0.37.9.46. ⑩ **下次 (V37.9.47+) 候选**: (a) **Stage 1 完成验证**: Mac Mini 装 scikit-learn (`pip3 install scikit-learn`, sentence-transformers 已装 V29.3) + 用户合并后 cd ~/openclaw-model-bridge git fetch+reset → 跑 `python3 ~/cross_source_signal_aggregator.py --date today` 看真实数据 (~290 notes/14 sources) 出 ≥3 跨源共振信号即 Stage 1 成功定义达标 (b) **Stage 2 V37.9.46-Stage2 候选**: 完整 #2 项目对齐度评分 — project_alignment_scorer.py rule_check 验证层 + 批量推 6 字段到所有 11 已对齐脚本 + Dream/Radar 推送的"高对齐 Top 5"段 (c) **Stage 3 V37.9.47**: kb_trend_acceleration.py + 28 单测 + 集成 kb_dream/kb_evening 推送 (d) **5/11 决策窗口** (明天): jobs_to_crontab + kb_sources_to_index machine_sync dry-run 真激活 (V37.9.23/V37.9.24/V37.9.25 累积观察期到达). ⑪ **Mac Mini 部署效果**: V37.9.46 PoC 阶段 cross_source_signal_aggregator.py 部署后**不会自动触发**（无 cron 注册）, 必须用户手动跑 `python3 ~/cross_source_signal_aggregator.py --date $(date +%Y%m%d) --json` 验证. cron 注册留给 Stage 5 kb_radar.sh 06:00 (设计文档 6.1 节). ⑫ **元价值**: V37.9.46 是数据复利路线 (V37 起累积 26000+ chunks / 14 sources / 290 notes/day) 从"广度收集"升级为"深度合成"的关键拐点. **不是再加 1 个 cron**, 而是把已有数据用 sentence-transformer + DBSCAN 算法做横向 weak-signal 检测 — 让用户在每天 14 sources 50 条独立推送中识别"今日 ≥2 source 同时关注的早期信号". 配合 Stage 2 #2 (项目对齐度) + Stage 3 #3 (趋势加速度) 三件套交集 = "高价值早期机会点雷达". MR-10 三问"最小修复方案"严格执行 — 不 cascade 不引入 Stage 2-6 复杂度. |
| V37.9.45 | 2026-05-09 | **hf_papers 6 字段深度迁移（audit P1 完结 + Opportunity Radar #2 PoC 单点验证, ALIGNED_SCRIPTS 10→11）** — ① **触发**：用户 V37.9.44 收工后明确指令"audit P1 续做 hf_papers V37.9.45 机械迁移（同步加 #2 6 字段一并推进）现在就开始做不留尾巴, 下次 session 开始执行 Stage 1"。同 session 双交付: audit P1 最后一个未对齐脚本 + Opportunity Radar Stage 2 #2 单点 PoC（先验证 6 字段设计模式可行性, V37.9.46 Stage 2 再批量推到所有脚本 + 加 rule_check 验证层）。② **scope (MR-10 三问)**: Q1 ✓（audit P1 已识别 hf_papers 是剩余唯一 0/6 score / 3 placeholder findings）/ Q2 ✗（历史 V37.8 copy-paste from arxiv 老版本）/ Q3 = V37.9.43 arxiv 同款机械迁移 + 加 #2 第 6 字段 PoC（不含 rule_check 验证层, 留给 Stage 2）。③ **新文件**: `project_concepts.yaml`（Stage 2 设计文档 4.1 节 schema, version 0.1-poc, 含 3 core_planes 各 weight 5/4/5 + 3 active_research_directions 各 weight 5/4/5 + 2 excluded_topics weight -2 + scoring_guide 5 档评分指南 + meta_rules）。④ **Write 全量重写 hf_papers (433→771 行 +78%)**: (A) 头部 V37.9.45 marker + NOTIFY_SH + send_alert helper（同款）(B) **保留 Step 2.5 GitHub repo enrichment**（HF 独有, 通过 ArXiv ID 搜 GitHub 仓库, github_url/github_stars/github_lang 字段不动）(C) call_llm_single_with_retry helper（V37.9.39 同款 5/10/20s × 3 + 三层检测）+ per-paper 主循环 (D) **6 字段深度 prompt** (V37.9.39 5 字段 + V37.9.45 新增第 6 字段 🎚️ 项目对齐度): 📌 中文标题 / 🔑 核心贡献 / 💡 关键方法 / 🎯 实践启发 / ⭐ 评级 + 推荐场景 / **🎚️ 项目对齐度 ⭐×N + 一句话原因 (≤30 字)** + 5 档评分指南 (⭐⭐⭐⭐⭐ control plane / agent runtime / ontology 直接相关 → ⭐ 完全无关) + V37.8.6 同款反幻觉守卫 + 严禁项目对齐评分泛泛 AI 相关必须基于"是否能为 OpenClaw 控制平面 / 记忆平面 / ontology engine 提供有价值借鉴" (E) **parse_5field_output → parse_6field_output**: 新增 alignment 字段, key-based parser 含 🎚️ 字段头识别 (含 fallback `🎚️` 或 `🎚`) + emit 显示 `🎚️ 项目对齐度: <内容>` + 末尾"本轮高对齐论文 (项目对齐度 ⭐≥4): N/M 篇"统计 (V37.9.45 PoC 简化版, Stage 2 加专门 Top 5 段) (F) LLM_DEGRADED 用 abstract 兜底 (与 V37.9.43 arxiv 同款) + emit 显示 GitHub repo metadata (HF-specific 保留) (G) 多窗口切片 (>8000 字 + sleep 1s + [i/N] + 续段, V37.9.21 同款) + 三档 status (ok/partial_degraded/llm_failed). ⑤ **27 新单测 4 类** test_v37_9_45_hf_papers.py: TestHfPapersV9_45ShellGuards(15 含 V37.9.45 marker / source notify / send_alert / [SYSTEM_ALERT] hf_papers / __LLM_HTTP_ERROR__ / __LLM_PARSE_FAIL__ / call_llm_single_with_retry / backoffs=(5 10 20) / 主循环 per-paper / 三档 status / [LLM_DEGRADED] / **6 字段 emoji 全套含 🎚️** / 严禁虚构 / 评级动态长度 / 多窗口阈值 8000+4000+sleep 1s+续 / 全部失败 500 字符内 exit 1 顺序锁 / V37.9.36 占位符严禁 / V37.8 老 3 字段 emit 已清除 / **HF-specific Step 2.5 GitHub enrichment 必须保留**) + TestHfPapersLlmDegradedFallback(3 含 abstract 兜底 + 无 abstract 显式说明 + HF paper URL 格式) + TestHfPapersInAuditAligned(3 含 ALIGNED_SCRIPTS V37.9.45 锚点 + count ≥11 + audit_script aligned=True+version=V37.9.45+findings=0) + **TestProjectAlignmentField(5 — V37.9.45 PoC 新类)**: project_concepts.yaml 文件存在 + schema 完整 (project/core_planes/active_research/excluded_topics/scoring_guide/control_plane/memory_plane/ontology) / prompt 含 🎚️ 字段定义 + 5 档评分指南 + OpenClaw 项目方向词 / emit 含 parse_6field_output + alignment 字段初始化 + 🎚️ 显示 / emit 末尾含 high_alignment_count 统计. **反向验证两层守卫真有效**: sed 把 LLM_DEGRADED 改回 `贡献：AI领域相关研究` → test_no_placeholder_fallback_text 立即 fail / Python 全局替换 `🎚️ 项目对齐度` → `_REMOVED_alignment` → test_prompt_has_alignment_field 立即 fail, 还原后 27/27 全过. ⑥ **INV-LLMCRON-AUDIT-001 扩展 18→19 checks**: 新增 ALIGNED_SCRIPTS 含 V37.9.45 hf_papers 锚点 file_contains 守卫. ⑦ **audit 报告刷新**: 21 候选 / 10→11 已对齐 / 11→10 未对齐 / placeholder findings 7→4 (hf_papers 移除 3). **audit P1 路径完结** (剩 P2: acl_anthology / karpathy_x / openclaw_official×2 各 1 finding + P3 8 个无 placeholder finding 但缺 fail-fast 标志). ⑧ **累计**: 64→65 suites (+1) / 2253→2280 tests (+27) / 0 fail / governance 554→555 checks (+1) / 71 invariants 不变 / 16 meta rules / 安全 95/100 / VERSION 0.37.9.44→0.37.9.45. ⑨ **Opportunity Radar Stage 2 PoC 关键设计决策**: V37.9.45 单点验证模式 — 仅 hf_papers 加 6 字段, 暂不加 project_alignment_scorer.py rule_check 验证层 (Stage 2 真正 PoC 时实现) + 暂不批量推到所有脚本 (Stage 2 完整时一并). 这样 V37.9.45 是"机械迁移 + 设计验证", 风险最小. 如 V37.9.45 6 字段在生产环境 (HF cron 10:00/20:00 HKT) 表现良好 → V37.9.46 Stage 2 信心十足全量推. ⑩ **MR-10 三问严格执行**: V37.9.45 不引入 project_alignment_scorer.py rule_check (Stage 2 责任) + 不批量改其他脚本 (Stage 2 责任) + 不加 Dream/Radar 推送的"高对齐 Top 5"段 (Stage 2 责任) — V37.9.45 = audit P1 续 + #2 PoC 单点验证, 不 cascade. ⑪ **元价值**: V37.9.36 audit 立 → V37.9.39 S2 → V37.9.40 DBLP+AI Leaders X → V37.9.41 HN → V37.9.43 arxiv → V37.9.44 github_trending → **V37.9.45 hf_papers = 48h 内七阶段 audit P1+P2 续机械迁移连环兑现完结**, ALIGNED_SCRIPTS **4→5→7→8→9→10→11**. **同时 V37.9.45 是 Opportunity Radar #2 PoC 单点验证起点**, Stage 2 (V37.9.46) 全量推 + rule_check, Stage 1 (V37.9.46+ 跨 source 弱信号聚合) 下次 session 单独干净启动. ⑫ **Mac Mini 部署效果**: hf_papers cron 每天 10:00/20:00 HKT (2 次/天). 部署 V37.9.45 后下次 cron 自动用新 6 字段格式. 用户在 WhatsApp 看到 hf_papers 推送时**首次出现 🎚️ 项目对齐度评分** + 末尾"本轮高对齐论文" 统计 — 验证 PoC 模式可观察性. |
| V37.9.44 | 2026-05-09 | **github_trending 5 字段深度迁移（用户视角原则 #13 第 15 次兑现 — WhatsApp 收到旧 4 字段浅表 GitHub 推送后立即提"按 S2 同款格式优化"）** — ① **触发**：用户 V37.9.43-hotfix2 收工后立即在 WhatsApp 收到 14:00 cron 推送的"GitHub 热门 AI/ML 仓库"老 V37.8 4 字段格式（标签/亮点/推荐⭐⭐⭐⭐），明确要求"请把这个任务也升级成 S2 相同格式的任务 深入分析"。**audit 报告中 github_trending 是 P3 1/6 score**（无 placeholder finding 但缺 fail-fast 标志），但用户视角 = 用户感知缺口优先于 audit 优先级。② **scope (MR-10 三问)**: Q1 ✓（audit P3 已识别 1/6 score）/ Q2 ✗（历史 V37.8 copy-paste from arxiv pattern）/ Q3 = 完整 V37.9.39 / V37.9.43 同款机械迁移（5 字段 + LLM_DEGRADED + fail-fast entangled）。③ **3 阶段迁移（423→677 行 +60%, 用 Write 全量重写避开 Edit 工具对大块 emoji+CJK 替换的诡异行为）**：(A) 头部加 V37.9.44 marker + NOTIFY_SH 自发现 + send_alert helper（[SYSTEM_ALERT] github_trending LLM 失败 走 Discord #alerts）(B) 替换 line 203-394（192 行 → 386 行 V37.9.39 同款代码）：call_llm_single_with_retry helper（5/10/20s × 3 + V37.9.36 三层检测）+ per-repo 主循环 + 5 字段深度 prompt 适配 GitHub 项目场景：📌 中文项目名 / 🔑 核心功能（3-5 条 bullet）/ 💡 技术亮点（按评级 ⭐⭐⭐→100-150 / ⭐⭐⭐⭐→250-400 / ⭐⭐⭐⭐⭐→500-800 字）/ 🎯 实践启发（fork/集成/学习架构建议）/ ⭐ 评级 + 推荐场景 + V37.8.6 反幻觉守卫（严禁虚构 / 严禁推断仓库内部实现 / 严禁把错误日志当外部信号）(C) parse_5field_output emit + LLM_DEGRADED 用 GitHub repo description 兜底（替代 V37.9.36 占位符反模式 `亮点：AI/ML相关项目`+`推荐：⭐⭐⭐`）+ 多窗口切片 ≤8000 单段直发 / >8000 mktemp + sleep 1s + `[i/N]` + `(续)` 续段 + 三档 status（ok / partial_degraded / llm_failed → exit 1 fail-fast 顺序锁）。④ **github_trending-specific 适配**：URL 用 `html_url`（GitHub API 返回，已是 `https://github.com/{owner}/{repo}` 完整格式）/ MAX_REPOS=10 保留 / GitHub API 完整 description → LLM_DEGRADED fallback 用 `description[:300]`（vs S2 用 tldr+abstract / DBLP 用 title+venue / arxiv 用 abstract / HN 用 RSS desc）/ Discord 频道 `DISCORD_CH_TECH`（vs S2/arxiv 用 PAPERS — 技术 vs 论文场景区分）/ Repo metadata badge 显示 `⭐ stars \| 主语言 \| 创建于YYYY-MM-DD` / 保留 NEW_IDS_FILE → SEEN_FILE 推送成功后才标记 invariant。⑤ **23 新单测 3 类** test_v37_9_44_github_trending.py: TestGhTrendingV9_44ShellGuards(15 含 V37.9.44 marker / source notify / send_alert / [SYSTEM_ALERT] github_trending / __LLM_HTTP_ERROR__ / __LLM_PARSE_FAIL__ / call_llm_single_with_retry / backoffs=(5 10 20) / for attempt in 0 1 2 / 主循环 per-repo / status:llm_failed / status:partial_degraded / all_failed_ / [LLM_DEGRADED] / 5 字段 emoji 全套 / 严禁虚构 / 评级动态长度 / 多窗口阈值 8000 + 4000 + sleep 1s + [i/N] + 续 / 全部失败分支 500 字符内 exit 1 顺序锁 / V37.9.36 占位符 line-by-line 跳过注释 / V37.8 老 3 字段 emit `msg_lines.append(rec_stars)` 已清除 / Discord 用 TECH 不用 PAPERS) + TestGhTrendingLlmDegradedFallback(4 含 description 兜底 + 无 description 显式说明 + html_url 保留 + repo metadata 显示) + TestGhTrendingInAuditAligned(3 含 ALIGNED_SCRIPTS V37.9.44 锚点 + count ≥10 + audit_script aligned=True+version=V37.9.44+findings=0). **反向验证已确认守卫真有效**：sed 把 LLM_DEGRADED 改回 `亮点：AI/ML相关项目` → `test_no_placeholder_fallback_text` 立即在 L518 报失败 + 行号定位精确, 还原后 23/23 全过. ⑥ **INV-LLMCRON-AUDIT-001 扩展 17→18 checks**: 新增 `ALIGNED_SCRIPTS 含 V37.9.44 github_trending 锚点` file_contains 守卫. ⑦ **audit 报告刷新**: 21 候选 / 9→10 已对齐 / 12→11 未对齐 / placeholder findings 7 不变 (github_trending 之前就 0 findings, 这次升级是补 fail-fast 标志 1/6→6/6). ⑧ **累计**: 63→64 suites (+1: v37_9_44_github_trending) / 2230→2253 tests (+23) / 0 fail / governance 553→554 checks (+1: V37.9.44 ALIGNED_SCRIPTS 锚点) / 71 invariants 不变 / 16 meta rules / 安全 95/100 / VERSION 0.37.9.43→0.37.9.44. ⑨ **元价值**: V37.9.36 audit 立 → V37.9.39 S2 → V37.9.40 DBLP+AI Leaders X → V37.9.41 HN → V37.9.43 arxiv_monitor → **V37.9.44 github_trending = 48h 内六阶段连环兑现**, ALIGNED_SCRIPTS **4→5→7→8→9→10** 是机器化进度证据 vs"靠记忆". MR-8 跨 job 同步 fail-fast 兑现. 用户视角原则 #13 第 15 次正向兑现 — V37.9.43-hotfix2 刚部署用户立即在 WhatsApp 看到下一个浅表推送, 单 session 内闭环 (用户提需求 → 完整迁移 + 单测 + 反向验证 + audit 刷新 + 推送 < 30 分钟). ⑩ **Mac Mini 部署效果**: github_trending cron 每天 14:00 HKT (1 次/天). 部署 V37.9.44 后下次 cron (5/10 14:00) 自动用新 5 字段格式. 如 LLM 故障 → 推 [SYSTEM_ALERT] + status:llm_failed + exit 1 (与 S2/DBLP/AI Leaders X/HN/arxiv/rss_blogs 同款行为). ⑪ **下次 V37.9.45+ 候选** (按 audit 路线图剩余优先级): (a) **P1 续 (3 findings)**: hf_papers — 与 V37.9.39 S2 / V37.9.43 arxiv 同款机械迁移 (b) **P2 (1 finding)**: acl_anthology / karpathy_x / openclaw_official×2 (c) **P3 (0 findings 但缺 fail-fast 标志)**: chaspark / ontology_sources / kb_inject / kb_dream / freight_watcher / finance_news. |
| V37.9.43-hotfix2 | 2026-05-09 | **wa_e2e_test.sh macOS bash 3.2 全角括号变量解析 bug（V37.9.43-hotfix W3 部署后 Mac Mini 实测苏醒，PR #458 沉睡）** — Mac Mini 部署 wa_e2e_test.sh 后首次运行报 `line 140: CHUNK_COUNT�: unbound variable` exit 1。**真根因**: L140 `pass "KB 索引可用（$CHUNK_COUNT）"` + L146 `skip "KB 索引检查跳过（$KB_RESULT）"` 全角 `）` (U+FF09) 紧贴变量, macOS bash 3.2 把 UTF-8 字节当作变量名一部分, `set -u` 触发 unbound variable. **历史溯源**: 自 PR #458 即存在但因脚本长期未部署 (V37.9.43-hotfix W3 修复 FILE_MAP 缺失才首次部署), 从未在 Mac Mini 真跑过 — "长期沉睡的真 bug 在新部署时苏醒"典型案例, **部署是验证不是终点**. **修复**: `${VAR}` 显式 brace 定界变量名 (`${CHUNK_COUNT}` / `${KB_RESULT}`), 防 bash 跨 ASCII/UTF-8 边界混淆. **4 新单测** TestW4WaE2eBashCjkVarParsing: test_no_unbraced_var_adjacent_to_fullwidth_paren (regex 全局扫禁止 `$VAR` 紧贴 `[U+4E00-U+9FFF]\|[U+FF00-U+FFFF]`) + test_v37_9_43_hotfix2_marker_present + test_chunk_count_uses_brace + test_kb_result_uses_brace. **反向验证守卫真有效**: sed `${CHUNK_COUNT}` → `$CHUNK_COUNT` → test_chunk_count_uses_brace 立即 fail, 还原后 16/16 全过. **MR-10 三问严格执行**: Q1 ✓ (PR #458 起即存在) / Q2 ✗ (V37.9.43-hotfix2 未改脚本内容) / Q3 = `${VAR}` 显式 brace, 不引入新概念. **累计**: 63 suites / 2226→2230 tests (+4) / 0 fail / 安全 95/100 / VERSION 0.37.9.43 不变. **Mac Mini 部署后 preflight 79/0/3/1**: W4 ✅ E2E 测试通过 (7 项验证), W1 货代等 20:00 freight cron 自愈, W2 KB lag 结构性自愈, W5 SLO p95 46s long-term unfinished 登记. **元教训**: MR-15 候选元规则 *"shell-string-with-cjk-must-brace-vars"* — 任何 shell 字符串里 `$VAR` 紧贴 CJK / 全角字符必须 `${VAR}` brace, 防 macOS bash 3.2 UTF-8 字节边界混淆. 用户视角原则 #13 第 14 次兑现 (单测 + governance + dev preflight 全绿但 Mac Mini set -u 实跑才暴露). |
| V37.9.43-hotfix | 2026-05-09 | **Mac Mini preflight 81/0/3 三警告闭环（W1 freight schema + W3 wa_e2e FILE_MAP, W2 KB lag 不修）** — V37.9.43 部署 Mac Mini 后 preflight 81/0/3, 3 warnings 按原则 #28 三问审视: W1 货代 deep_dive=skipped (V37.9.31 漏改 Step 9 条件分支, 真 bug 但非 V37.9.43 引入) / W2 KB 索引 98% (结构性 lag, kb_embed cron 自愈, 不修) / W3 wa_e2e_test.sh 未部署 (PR #458 即存在但 FILE_MAP 缺 entry, 长期漏部署观察项). **W1 修复**: jobs/freight_watcher/run_freight.sh line 639 generic `DEEP_DIVE_STATUS="skipped"` → `"skipped_no_high_star"` (语义不变, Step 9 by-design 不进入 = 无 ⭐≥4 高星条目, 合法跳过) + preflight_check.sh 加新 case `skipped_no_high_star) → pass` (V37.9.31 三档 status 之前补齐). **W3 修复**: auto_deploy.sh FILE_MAP 加 `wa_e2e_test.sh|$HOME/wa_e2e_test.sh` (原则 #15 测试三层第三层 WhatsApp E2E 业务验证脚本本体). **12 新单测 3 类** test_v37_9_43_hotfix.py: TestW1FreightSkippedSchemaFix(7 含源码守卫禁止 generic skipped / 用 skipped_no_high_star / V37.9.43-hotfix marker / preflight pass case / 顺序锁 在 V37.9.31 三档之前 / 必须 pass 不能 warn) + TestW3WaE2eTestDeployment(3 含 FILE_MAP entry / 文件存在 / 周围 V37.9.43-hotfix 注释溯源) + TestHotfixDoesNotBreakV37943Main(2 含 V37.9.43 主交付 arxiv_monitor 仍正常 / ALIGNED_SCRIPTS 仍含 V37.9.43). **反向验证守卫真有效**: W1 sabotage `skipped_no_high_star → 'skipped'` → test_freight_no_generic_skipped_status 立即 L639 fail / W3 完整删除 FILE_MAP 行 → test_wa_e2e_test_in_file_map 立即 fail, 还原后 12/12 全过. **V37.9.31 测试同步更新**: test_freight_schema_v9_31.test_step_9_status_values_complete 断言 `skipped` → `skipped_no_high_star` + 顶部 docstring 同步说明 V37.9.43-hotfix 重命名. **MR-10 三问严格执行**: 都是历史真 bug 非 V37.9.43 引入, 同 session 全修 (用户选项 1) — 测试守卫 + 反向验证 + 单一 commit, 不引入 cascade. **累计**: 62→63 suites (+1) / 2214→2226 tests (+12) / 0 fail / 安全 95/100 / VERSION 0.37.9.43 不变 (hotfix 不 bump). **后续 V37.9.43-hotfix2** 修 W4 wa_e2e bash CJK 解析 bug 完成 W3 真 E2E 闭环. |
| V37.9.43 | 2026-05-09 | **arxiv_monitor 5 字段深度迁移 (V37.9.41 audit P1 续 — 机械迁移 V37.9.39 S2 / V37.9.40 DBLP+AI Leaders X / V37.9.41 HN 同款模式, ALIGNED_SCRIPTS 8→9)** — ① **触发**: V37.9.42 收工 audit 报告 P1 剩 hf_papers + arxiv_monitor (各 3 placeholder findings, daily cron 高频用户可见). 用户开工选 arxiv_monitor 优先 (vs hf_papers 顺序对调) — V37.9.41 同款机械迁移路径已成熟. ② **scope (MR-10 三问)**: Q1 ✓ (audit P1 已识别 0/6 score) / Q2 ✗ (历史 V37.8 copy-paste from S2 老版本) / Q3 = 完整 V37.9.39 模式 (5 字段 + LLM_DEGRADED + fail-fast entangled). ③ **3 阶段迁移 (443→704 行 +59%)**: (A) 头部加 V37.9.43 marker + NOTIFY_SH 自发现 + send_alert helper (`[SYSTEM_ALERT] arxiv_monitor LLM 失败` 走 Discord #alerts) (B) **替换 line 216-393 (217 行 → 330 行)**: call_llm_single_with_retry helper (5/10/20s × 3 + V37.9.36 三层检测 `__LLM_HTTP_ERROR__:` / `__LLM_PARSE_FAIL__:bad_json:` / `__LLM_PARSE_FAIL__:no_choices:` / empty content) + per-paper 主循环 `for ((i=0; i<TOTAL_NEW; i++))` + 5 字段深度 prompt 适配论文 metadata `📌 中文标题 / 🔑 核心贡献 / 💡 关键方法 / 🎯 实践启发 / ⭐ 评级` + 按评级动态长度 ⭐⭐⭐→100-150 / ⭐⭐⭐⭐→250-400 / ⭐⭐⭐⭐⭐→500-800 字 + V37.8.6 同款反幻觉守卫 (严禁虚构 / 严禁推断平台状态 / 严禁把错误日志当外部信号) (C) parse_5field_output emit + LLM_DEGRADED 用 arxiv abstract 兜底 (替代 V37.9.36 占位符反模式 `贡献：AI领域相关研究`+`价值：⭐⭐⭐`) + 多窗口切片 ≤8000 单段直发 / >8000 mktemp + sleep 1s + `[i/N]` + `(续)` 续段 + 三档 status (ok / partial_degraded / llm_failed → exit 1 fail-fast 顺序锁) + L2 解析率<0.5 检查废弃 (partial_degraded 替代). ④ **arxiv-specific 适配**: URL 格式 `https://arxiv.org/abs/{arxiv_id}` 保留 / MAX_PAPERS=10 保留 (vs HN 5) / arxiv API 完整 abstract → LLM_DEGRADED fallback 用 `abstract[:300]` (S2 用 tldr+abstract / DBLP 用 title+venue 元数据 / HN 用 description) / 保留 ontology_filter Discord #ontology 单独推送 (line 622+ 不动, 它读 PAPERS_FILE 不读 LLM 输出) / 保留 KB write source name 'arxiv-ai-models' / 保留 NEW_IDS_FILE → SEEN_FILE 推送成功后才标记 invariant. ⑤ **22 新单测 3 类** test_v37_9_43_arxiv.py: TestArxivV9_43ShellGuards(17 含 V37.9.43 marker / source notify / send_alert / [SYSTEM_ALERT] arxiv_monitor / __LLM_HTTP_ERROR__ / __LLM_PARSE_FAIL__ / call_llm_single_with_retry / backoffs=(5 10 20) / for attempt in 0 1 2 / 主循环 per-paper / status:llm_failed / status:partial_degraded / all_failed_ / [LLM_DEGRADED] / 5 字段 emoji 全套 / 严禁虚构 / 评级动态长度 / 多窗口阈值 8000 + 4000 + sleep 1s + [i/N] + 续 / 全部失败分支 500 字符内 exit 1 顺序锁 / V37.9.36 占位符 line-by-line 跳过注释 / V37.8 老 3 字段 emit `msg_lines.append(stars)` 已清除 / 老 L2 `parse_quality_low` status 废弃) + TestArxivLlmDegradedFallback(3 含 abstract 兜底 + 无 abstract 显式说明 + arxiv URL 格式保留) + TestArxivInAuditAligned(3 含 ALIGNED_SCRIPTS V37.9.43 锚点 + count ≥9 + audit_script aligned=True+version=V37.9.43+findings=0). **反向验证已确认守卫真有效**: sed 把 `⚠️ [LLM_DEGRADED] 深度分析失败...` 改回 `贡献：AI领域相关研究` → `test_no_placeholder_fallback_text` 立即在 L519 报失败 + 行号定位精确, 还原后 22/22 全过. ⑥ **INV-LLMCRON-AUDIT-001 扩展 16→17 checks**: 新增 `ALIGNED_SCRIPTS 含 V37.9.43 arxiv_monitor 锚点` file_contains 守卫. ⑦ **audit 报告刷新**: 21 候选 / 8→9 已对齐 / 13→12 未对齐 / 10→**7 placeholder findings** (arxiv 移除 3). 下次 V37.9.44+ 候选: hf_papers (P1 剩唯一 / 3 findings, 同款机械迁移) → P2 acl_anthology / karpathy_x / openclaw_official×2. ⑧ **累计**: 61→62 suites (+1: v37_9_43_arxiv) / 2192→2214 tests (+22) / 0 fail / governance 552→553 checks (+1: V37.9.43 ALIGNED_SCRIPTS 锚点) / 71 invariants 不变 / 16 meta rules / 安全 95/100 / VERSION 0.37.9.42→0.37.9.43. ⑨ **元价值**: V37.9.36 audit 立 → V37.9.39 S2 → V37.9.40 DBLP+AI Leaders X → V37.9.41 HN → V37.9.43 arxiv_monitor = **48h 内五阶段连环兑现**, ALIGNED_SCRIPTS **4→5→7→8→9** 是机器化进度证据 vs"靠记忆". MR-8 跨 job 同步 fail-fast 兑现 — V37.9.36/37/38/39/40/41 的工作让 V37.9.43 不需要重新设计 fail-fast 模式, 直接复用 + arxiv-specific 适配 (abstract fallback / URL 格式 / 保留 ontology_filter). 用户视角原则 #13 第 13 次正向兑现. ⑩ **Mac Mini 部署效果**: arxiv_monitor cron 每 3h 整点 HKT (8 次/天). 部署 V37.9.43 后下次 cron 自动用新 5 字段格式. 如 LLM 故障 → 推 [SYSTEM_ALERT] + status:llm_failed + exit 1 (与 S2/DBLP/AI Leaders/HN/rss_blogs 同款行为). |
| V37.9.42 | 2026-05-09 | **HN V37.9.41 三个 bash 整数处理 hotfix（Mac Mini 实测发现）** — V37.9.41 部署后 23:51 用户手动跑 HN 报 (a) `调用 LLM 分析条 1/       1` (BSD wc -l 含前导空格) (b) `Traceback IndexError list index out of range line 13` (dedup `print('\n'.join([]))` 输出 '\n' 让 wc -l=1 但实际无 JSON, 后续 prompt Python `items[0]` 越界) (c) `已推送 0\n0 条` (grep -c 无匹配 exit 1 → `\|\| echo 0` 双输出). 三处最小修复: line 151 `wc -l` 改 `tr -d '[:space:]'` + 新增 `NEW_JSON_COUNT=$(grep -c '^{' "$NEW_FILE")` 精确计数 valid JSON 行 + early-exit 守卫防 `print('\n'.join([]))` 空行陷阱; line 481 `SENT_COUNT=$(grep -c ...) \|\| SENT_COUNT=0` 替代 `\|\| echo 0` 单 stdout. **MR-10 三问严格执行**: Q1 部分历史问题 (BSD wc, grep exit code) 但 V37.9.41 per-paper iteration 让其首次实际触发 / Q2 V37.9.41 引入了主要血案路径 / Q3 最小修复 = 3 处 + early-exit, 不引入新概念. **bash -n + full_regression 61/0/2192 通过**. 上游 LLM 502 (Qwen3+gemini 双故障 5/8 23:51 复发, 同 V37.9.36 / 5/7 18:00 / 5/8 07:30 同款 — 这是上游 infra 问题非 V37.9.41 代码 bug, fail-fast 正确触发). 累计: 61 suites / 2192 tests / 0 fail / 552 checks / 71 inv / 安全 95/100 / VERSION 0.37.9.41 → 0.37.9.42.|
| V37.9.41 | 2026-05-08 | **HN 5 字段深度迁移 (V37.9.40 同款扩展, ALIGNED_SCRIPTS 7→8)** — 用户视角原则 #13 第 12 次正向兑现 — V37.9.40 部署后用户立即看 HN 推送也是浅 4 字段格式（zh_title/链接/要点/价值⭐⭐⭐），明确要求"HN 也参照 S2 详细分析"。① **触发**: HN 老 V38 batch 单调 LLM 调用 + 4 字段输出 + `stars='⭐⭐⭐'` silent fallback (V37.9.36 反模式 audit 没抓到的特殊形态 — pattern 是 `'⭐⭐⭐'` 字面量不带"价值："前缀, 不在 PLACEHOLDER_PATTERNS). ② **scope 决策**: 完整 V37.9.40 同款 fail-fast + 5 字段, 但 HN-specific 适配 (RSS fetch/dedup/inbox 全部保留, 替换 LLM 调用 + emit + push 段, 大型 Python heredoc 重写). MR-10 三问严格执行: 之前存在 ✓ (audit P3 score 3/6) / 我引入 ✗ (历史 V38) / 最小修复 = 完整 V37.9.40 模式 (5 字段 + LLM_DEGRADED + 多窗口 + send_alert helper entangled). ③ **3 阶段迁移 (385→578 行 +50%)**: (A) 头部 NOTIFY_SH + send_alert helper (与 S2/DBLP 同款) (B) **替换 line 131-298 大块 Python heredoc**: call_llm_single_with_retry helper (V37.9.40 同款 5/10/20s × 3 + 三层检测) + per-tweet 主循环 + 5 字段 prompt 适配 HN posts (📌中文标题/🔑核心要点/💡技术深度解读/🎯实践启发/⭐评级 + "(基于标题与摘要推断)" caveat 类似 DBLP 因 HN 摘要常缺失) + V37.9.36 同款反幻觉守卫 + Qwen3 <think>/ANSI 清理保留 (HN 早期 输出兼容) (C) 替换 emit (parse_5field_output + LLM_DEGRADED 用 HN description 兜底替代老 stars='⭐⭐⭐' 占位符 + 多窗口 [i/N] (续) 触发 >8000 字 + sleep 1s 防乱序 + KB source line 用 rating 而非旧 4 字段). ④ **HN-specific 适配**: max 5 items/run (HN limit) + RSS hnrss.org/frontpage 不变 + KEYWORDS 60+ AI/tech 关键词不变 + Python dedup + inbox 写入提前 (Fix1 V23) 不变 + 老 ERR_MSG ('HN Watcher LLM调用失败') 替换为 send_alert helper ([SYSTEM_ALERT] hn_watcher LLM 失败). ⑤ **21 新单测 3 类** test_v37_9_41_hn.py: TestHnV9_41ShellGuards(15 含 V37.9.41 marker / source notify / send_alert / [SYSTEM_ALERT] / __LLM_HTTP_ERROR__ / __LLM_PARSE_FAIL__ / call_llm_single_with_retry / backoffs=(5 10 20) / for attempt in 0 1 2 / per-item 主循环 / 三档 status / all_failed_ / [LLM_DEGRADED] / 5 字段 emoji 全套 / 严禁虚构 / HN-specific (基于标题与摘要推断) caveat / 评级动态长度 / 多窗口 8000+4000+sleep 1s+(续)+[i/N] / V37.9.36 占位符 stars='⭐⭐⭐' 严禁 + point=title[:40] 严禁 / V37.8 老 4 字段 emit 已清除) + TestHnLlmDegradedFallback(2 含 desc 兜底 + 无 desc 显式说明) + TestHnInAuditAligned(3 含 ALIGNED_SCRIPTS V37.9.41 锚点 + count ≥8 + audit_script aligned=True+version=V37.9.41+findings=0). ⑥ **V37.4.3 历史 test 兼容**: test_tool_proxy.TestNotifyShAlertMarker.test_hn_err_path_has_marker 放宽断言接受 V37.9.41 send_alert helper 字面量 ("hn_watcher LLM 失败") 或 V37.4.3 旧 inline ("HN Watcher LLM调用失败"), 维持向后兼容. ⑦ **governance INV 修复 LLM_FAILED legacy pattern**: 旧 INV-QUOTA-001 用 file_contains pattern: "LLM_FAILED" 检 V38 老 __LLM_FAILED__ marker, V37.9.41 替换为 V37.9.36 三层 markers, pattern 升级为 regex `(?:__LLM_HTTP_ERROR__|__LLM_PARSE_FAIL__|"status":"llm_failed")` 接受 V37.9.41 任一标志. ⑧ **INV-LLMCRON-AUDIT-001 扩展 16→17 checks**: 新增 ALIGNED_SCRIPTS 含 V37.9.41 锚点守卫. ⑨ **audit 报告刷新**: 21 候选 / 7→8 已对齐 / 14→13 未对齐 / 10 placeholder findings 不变 (HN 之前 audit 0 findings 因 pattern miss). ⑩ **累计**: 60→61 suites (+1: v37_9_41_hn) / 2171→2192 tests (+21) / 0 fail / governance 551→552 checks (+1) / 71 invariants 不变 / 17 meta rules / 安全 95/100 / VERSION 0.37.9.40→0.37.9.41. ⑪ **下次 V37.9.42+ 候选** (按 audit 报告剩余优先级): (a) **P1 续 (3 findings each)**: hf_papers / arxiv_monitor — 与 V37.9.39 S2 完全同款 copy-paste 模式机械迁移 (b) **P2 (1 finding)**: acl_anthology / karpathy_x / openclaw_official×2 (c) **P3 (0 findings 但缺 fail-fast 标志)**: chaspark / github_trending / ontology_sources / kb_inject / kb_dream / freight_watcher / finance_news (d) **auto_deploy cp 原子化** (V37.9.41+ P1 timing race 实证修复, today commit `9715256` 候选已登记) (e) **运行时验证**: 5/9 早 V37.9.30 EPERM 数据回归 + 5/11 决策窗口. ⑫ **Mac Mini 部署效果**: HN cron 每 3h:45 (8:45/11:45/14:45/17:45/20:45/23:45 HKT 6 次/天). 部署 V37.9.41 后下次 cron 自动用新 5 字段格式. 如 LLM 故障 → 推 [SYSTEM_ALERT] + status:llm_failed + exit 1 (与 S2/DBLP/AI Leaders/rss_blogs 同款). ⑬ **元价值**: 同日 V37.9.38 audit 立 → V37.9.39 S2 → V37.9.40 DBLP+AI Leaders → V37.9.41 HN = **24h 内四阶段连环兑现**, ALIGNED_SCRIPTS **4→5→7→8** 是机器化进度证据 vs"靠记忆"。MR-8 跨 job 同步 fail-fast 兑现 — V37.9.36/37/38/39/40 的工作让 V37.9.41 不需要重新设计 fail-fast 模式, 直接复用 + HN-specific 适配 (基于标题与摘要推断 caveat 因 HN 摘要常缺失). audit 模块从"视野工具"升级为"驱动器"持续兑现 — 每次 PoC fix 推 ALIGNED_SCRIPTS 数, 每次都有 user feedback 立即兑现。|
| V37.9.40 | 2026-05-08 | **DBLP + AI Leaders X 双脚本批次 5 字段深度迁移 (V37.9.39 同款 V37.9.36-37 模式横向扩展, 用户视角原则 #13 第 11 次正向兑现)** — ① **触发**: V37.9.39 部署 + S2 真实推送验证后用户立即提"DBLP CS论文精选 和 AI Leaders 技术洞察 也按 S2 格式优化". ② **scope 决策**: 双脚本批次 (V37.9.39 单脚本 PoC 已验证 + 同款机械迁移 + 用户明确双脚本要求 + 单 commit + Mac Mini E2E 一次验证两个脚本). 按 MR-10 三问严格执行: Q1 之前存在 ✓ (audit P2 已识别) / Q2 我引入 ✗ (历史 copy-paste) / Q3 最小修复 = 完整 V37.9.36-37 模式横向迁移 (5 字段 + LLM_DEGRADED + fail-fast entangled, 不能拆开). ③ **DBLP 迁移 (3 阶段 edit, 451→657 行 +46%)**: (A) 头部 NOTIFY_SH 自发现 + send_alert helper (B) call_llm_single_with_retry helper (5/10/20s × 3) + per-paper 主循环 + V37.9.36 三层检测 + 5 字段 prompt 适配无 abstract (DBLP 数据库无 abstract, prompt 加显式"基于标题推断"caveat + 推断时必须显式标注 + 严禁虚构论文中的具体数据/实验结果) (C) parse_5field_output emit + LLM_DEGRADED 用 标题+venue 元数据兜底 (DBLP 无 abstract 时是最低保障 + 用户至少看到论文 metadata + 链接) + 多窗口切片 [i/N] (续). ④ **AI Leaders X 迁移 (3 阶段 edit, 454→665 行 +46%)**: (A) 头部 NOTIFY_SH + send_alert (与 DBLP/S2 同款) (B) per-tweet retry helper + 5 字段 prompt 适配 tweet 上下文: 📌 中文主题 (≤15 字, twitter 上下文短) / 🔑 核心观点 (推文作者论点) / 💡 技术深度解读 (按评级动态长度: 揭示作者立场+技术背景+为什么重要+与已有讨论关联+局限性) / 🎯 系统启示 (对 Agent Runtime/Control Plane/Memory 的启示, 1-3 条) / ⭐ 评级 + 推荐场景. 替代老 V37.8 文字 prefix (主题/深度分析/系统启示/行动建议/价值) batch 调用 (C) emit 重写: parse_5field_output 适配 ⭐ tweet header (━━━ [author] title ━━━) + LLM_DEGRADED 用推文原文 (text_preview 200 chars) 兜底 + 多窗口 [i/N]. ⑤ **17 新单测 3 类** test_v37_9_40_dblp_ailead.py: TestDblpV9_40Guards(6 含 V37.9.40 共用守卫 + 占位符严禁 + 基于标题推断 caveat + 元数据兜底 + DOI 链接优先 + 📚 emoji + 多窗口 header) + TestAiLeadersV9_40Guards(6 含共用守卫 + 占位符严禁 + 老 5 行格式清除 + tweet metadata 保留 + 推文原文兜底 + 🧠 emoji + 多窗口 header) + TestV9_40InAuditAlignedScripts(5 含 ALIGNED_SCRIPTS ≥7 + DBLP V37.9.40 锚点 + AI Leaders X V37.9.40 锚点 + audit_script DBLP aligned + audit_script AI Leaders X aligned). 共用 _common_v37_9_40_guards() helper 测两脚本 fail-fast 模式 (V37.9.40 marker / source notify / send_alert / [SYSTEM_ALERT] / __LLM_HTTP_ERROR__ / __LLM_PARSE_FAIL__ / call_llm_single_with_retry / backoffs=(5 10 20) / for attempt in 0 1 2 / per-item 主循环 / status:llm_failed / status:partial_degraded / all_failed_ / [LLM_DEGRADED] / 5 字段 emoji 全套 / 严禁虚构 / 多窗口阈值 8000+4000+sleep 1s+续 / 全部失败 500 字符内 exit 1 顺序锁 / 老 3 字段 prompt 已清除). ⑥ **INV-LLMCRON-AUDIT-001 扩展 15→16 checks**: 新增 ALIGNED_SCRIPTS 含 V37.9.40 锚点 file_contains 守卫. ⑦ **audit 报告刷新**: 21 候选 / 5→7 已对齐 / 16→14 未对齐 / 12→10 placeholder findings (DBLP -3 + AI Leaders X -1 = -4 但 audit 之前在 AI Leaders X 已 silent fail 模式所以净 -2 真实). ⑧ **累计**: 59→60 suites (+1 v37_9_40_dblp_ailead) / 2154→2171 tests (+17) / 0 fail / governance 550→551 checks (+1 V37.9.40 ALIGNED_SCRIPTS 锚点) / 71 invariants 不变 / 17 meta rules / 安全 95/100 / VERSION 0.37.9.39→0.37.9.40. ⑨ **下次 V37.9.41+ 候选** (按 audit 报告剩余优先级): (a) **P1 续 (3 findings each)**: hf_papers / arxiv_monitor — 与 V37.9.39 S2 完全同款 copy-paste 模式, 机械迁移 (b) **P2 (1 finding)**: acl_anthology / karpathy_x / openclaw_official×2 (c) **P3 (0 findings 但缺 fail-fast 标志)**: chaspark / github_trending / ontology_sources / kb_inject / kb_dream / freight_watcher / finance_news / run_hn_fixed (d) **运行时验证**: 5/9 早 V37.9.30 EPERM 24h 数据回归 + 5/11 决策窗口 jobs_to_crontab + kb_sources_to_index machine_sync 真激活. ⑩ **Mac Mini 部署效果**: DBLP cron 12:00 HKT (今日已跑) + AI Leaders X cron 09:00/21:00 HKT. 部署 V37.9.40 后下一次 cron 自动用新 5 字段格式. 如 LLM 故障 → 推 [SYSTEM_ALERT] + status:llm_failed + exit 1 (与 S2/rss_blogs 同款行为). ⑪ **元价值**: 同日内 V37.9.38 audit 立 → V37.9.39 第一 PoC fix → V37.9.40 双脚本批次扩展 = 24h 内三阶段连环兑现 framework 价值. **audit 模块从"视野工具"升级为"驱动器"**, 每次 PoC fix 推 ALIGNED_SCRIPTS 数 (4→5→7) 是机器化进度证据 vs"靠记忆"。MR-8 跨 job 同步 fail-fast 兑现 — V37.9.36/37/38/39 的工作让 V37.9.40 不需要重新设计 fail-fast 模式, 直接复用 + 适配 (DBLP 无 abstract caveat / AI Leaders X tweet metadata). ⑫ **下次 V37.9.41+ 用户视角观察项**: (a) DBLP 5 字段在无 abstract 限制下深度是否够用 (caveat "基于标题推断" LLM 是否真的标注) (b) AI Leaders X 5 字段在 tweet 短文本上下文是否过度膨胀 (推文 280 字符 → 5 字段输出 800-2000 字, 比例 5-10x) (c) S2 V37.9.39 + DBLP/AI Leaders V37.9.40 三 task 多日推送质量 (LLM variance / 缺字段 / prompt 字面量泄漏) 收敛模式. |
| V37.9.39 | 2026-05-08 | **V37.9.38 audit 工具识别 P1 第一个 PoC fix — semantic_scholar 5 字段深度迁移（V37.9.36-37 rss_blogs 模式横向扩展, 用户视角原则 #13 第 10 次正向兑现）** — ① **触发**：V37.9.38 audit 工具部署到 Mac Mini 后, 同日下午用户在 WhatsApp 看到当日 S2 高引论文推送内容浅薄（标题+作者+引用+一句话贡献+`价值：⭐⭐⭐⭐`）, 直接反馈"这个分析的也比较浅，请按照昨天 rss_blogs 任务格式继续优化"。② **scope 决策（按 MR-10 三问）**：Q1 之前存在? ✓（audit 报告中 S2 是 P1, 3 placeholder findings + 0/6 score） / Q2 是否新引入? ✗（历史 copy-paste from arxiv_monitor pattern） / Q3 最小修复? = **完整 V37.9.36-37 rss_blogs 模式横向迁移**（不只是改 prompt 加深度, 因为 5 字段 + LLM_DEGRADED + fail-fast 是 entangled — 不加 fail-fast 意味着 LLM partial parse 失败时 placeholder 仍会回归, 不加 LLM_DEGRADED 意味着失败篇没有用户可读 fallback）。③ **5 阶段迁移（incremental edit, bash -n 验证 + audit 工具实时验证）**：(A) 头部加 NOTIFY_SH 自发现 + send_alert helper（含 `[SYSTEM_ALERT]` 前缀, V37.4.3 PA 上下文隔离 + V37.9.36 同款模式）(B) 替换 LLM 调用 (line 179-249 → V37.9.37 同款 call_llm_single_with_retry helper, 5/10/20s 指数退避 × 3 + V37.9.36 三层检测 `__LLM_HTTP_ERROR__:` / `__LLM_PARSE_FAIL__:bad_json:` / `__LLM_PARSE_FAIL__:no_choices:` / empty content + per-paper 主循环 `for ((i=0; i<TOTAL_NEW; i++))` + 5 字段深度 prompt 适配论文 metadata `📌 中文标题 / 🔑 核心贡献 / 💡 关键方法 / 🎯 实践启发 / ⭐ 评级` + 按评级动态长度 ⭐⭐⭐→100-150 / ⭐⭐⭐⭐→250-400 / ⭐⭐⭐⭐⭐→500-800 字 + V37.8.6 同款反幻觉守卫 严禁虚构 + 严禁推断平台状态 + 严禁把错误日志当外部信号）(C) 替换 emit (line 251-373 → 5 字段 key-based parser `parse_5field_output()` 容忍字段顺序错乱/单字段缺失/prefix 变体 + LLM_DEGRADED fallback 含 `[LLM_DEGRADED] 深度分析失败, 论文摘要供参考:` + tldr/abstract 兜底 替代 V37.9.36 `贡献：AI领域相关研究`+`价值：⭐⭐⭐` 占位符反模式) (D) 多窗口切片 (≤8000 单段直发 / >8000 触发 mktemp + sleep 1s 防 WhatsApp 乱序 + `[i/N]` 标识 + `(续)` 续段) (E) status_file schema 升级 三档 `ok` / `partial_degraded` (失败篇 ≥1 但 <N 全部) / `llm_failed` (全部失败 → exit 1)。④ **S2 特定适配**：保留引用次数（论文 metadata 不变）+ 优先 arxiv 链接 + tldr 优先 abstract（S2 native TLDR 更简洁）+ first_author 字段 + 保留 ontology_filter Discord #ontology 单独推送（line 375-393 不动, 它读 PAPERS_FILE 不读 LLM 输出）。⑤ **37 单测 4 类** test_run_semantic_scholar_v37_9_39.py: TestS2ShellGuardsV9_39(28 含 V37.9.39 marker / source notify.sh / send_alert / [SYSTEM_ALERT] / __LLM_HTTP_ERROR__ / __LLM_PARSE_FAIL__ / call_llm_single_with_retry / backoffs=(5 10 20) / for attempt in 0 1 2 / 主循环 per-paper / status:llm_failed / status:partial_degraded / all_failed_ prefix / [LLM_DEGRADED] / 5 字段 emoji 全套 / 严禁虚构 / 评级动态长度 / 多窗口阈值 8000 + 4000 + sleep 1s + [i/N] + 续 / 全部失败分支 500 字符内 exit 1 顺序锁 / V37.9.36 占位符 line-by-line 跳过注释 / V37.8 老 3 字段 prompt 已清除) + TestEmit5FieldParser(2 parse_5field_output 函数 + 5 key dict 完整性) + TestActualBloodLessonScenarioRegression(2 V37.9.36 反模式三个变体严格清除 + LLM_DEGRADED + tldr/abstract 兜底逻辑) + TestS2InAuditAlignedScripts(3 ALIGNED_SCRIPTS 含 S2 V37.9.39 + count ≥5 + audit_script(S2) returns aligned=True+version=V37.9.39+findings=0). **反向验证已确认守卫真有效**：sed 把 `贡献：AI领域相关研究` 注入回 LLM_DEGRADED 块 → `test_no_placeholder_fallback_text` 立即 L511 抓到 + `test_v37_9_39_fallback_uses_explicit_degraded_marker` 立即报失败, 还原后 37/37 全过。⑥ **INV-LLMCRON-AUDIT-001 扩展守卫 14→15 checks**：新增 `ALIGNED_SCRIPTS 含 V37.9.39 锚点` file_contains 守卫防止未来 V37.9.40+ 误删 S2 entry。⑦ **audit 报告刷新**：21 候选 / 4 已对齐→**5 已对齐** / 17 未对齐→**16 未对齐** / 15 placeholder findings→**12** (S2 移除 3)。下次 V37.9.40+ 候选按报告 P1: hf_papers / arxiv_monitor 各 3 findings (与 S2 同款 pattern 应用机械迁移)。⑧ **累计**：58→59 suites (+1: run_semantic_scholar_v9_39) / 2117→2154 tests (+37) / 0 fail / governance 549→550 checks (+1: V37.9.39 ALIGNED_SCRIPTS 锚点守卫) / 71 invariants 不变 / 17 meta rules / 安全 95/100 / VERSION 0.37.9.38 → 0.37.9.39。⑨ **元价值**：V37.9.38 audit 工具部署后 < 6 小时即驱动 V37.9.39 第一个 PoC fix — 验证"audit + 视野 + 路线图"三件套不是空头, 而是真实驱动产品质量提升的 framework。原则 #13 第 10 次正向兑现 — 用户在 WhatsApp 实际收到当日推送 → 立即识别质量缺口 → 立即对齐已建立模式（rss_blogs reference）→ 单 session 完整闭环（migrate + 守卫 + 反向验证 + commit）。MR-8 跨 job 同步 fail-fast 兑现 — V37.9.36/37/38 的工作让 V37.9.39 不需要重新设计 fail-fast 模式, 直接复用。⑩ **下次 V37.9.40+ 候选**：(a) **P1 续**: hf_papers / arxiv_monitor (各 3 findings, 与 S2 完全同款 copy-paste 模式, 机械迁移即可) (b) **P2**: dblp / acl_anthology / ai_leaders_x / karpathy_x / openclaw_official×2 (1 finding each, 同款适配) (c) **P3**: 8 个无 finding 但缺 fail-fast 标志脚本 (d) **运行时验证**: V37.9.30 EPERM 24h 数据回归 (5/9 早 截止, 还剩 1 天) + V37.9.35 推送质量观察 + 5/11 决策窗口 jobs_to_crontab + kb_sources_to_index machine_sync 真激活。⑪ **Mac Mini 部署效果**：S2 cron 每天 11:00 HKT 触发, 部署 V37.9.39 后立即生效。下次（明天 5/9 11:00）用户应在 WhatsApp 看到 5 字段深度分析的 S2 推送（10 篇旗舰可能触发多窗口分片为 2 段）。如 LLM 故障 → 推 [SYSTEM_ALERT] + status:llm_failed + exit 1（与 rss_blogs V37.9.36 同款）。|
| V37.9.38 | 2026-05-08 | **双交付：① status_update.py 双路径 bug 闭环 (V37.9.36 候选) + ② LLM cron fail-fast audit 视野+预防+路线图三件套 (V37.9.36 教训横向扩展)** — ① **P1 status_update 双路径 bug**（V37.9.36 候选登记的开工/收工闭环 bug）：实测 `data["unfinished"]` (顶层 32 项) vs `data["session_context"]["unfinished"]` (13 项 schema 真理源) 长期分叉 45 项。`--add unfinished <item>` 写顶层但 `--read --human` 读 session_context → 收工 add 的 todo 开工读不到（V37.9.35→V37.9.36 实证）。**最小修复**：(a) 新增 `_resolve_array_target(data, name)` 把 unfinished 重定向到 session_context（其他数组照常顶层）(b) 新增 `_migrate_top_level_unfinished(data)` 在 `load_status` 时合并去重 + 删顶层（in-memory，下次 save 持久化为单一路径）(c) `args.add/pop/clear` 三处统一走 helper（V37.9.36 反模式禁止）(d) `format_human` 把 unfinished list 逐项展开（≤220 字符截断 + "还有 N 项"）(e) 历史字符串 session_context.unfinished 自动转 list（DEFAULT_STATUS legacy）。**26 新单测 4 类 + 反向验证守卫真有效**（sabotage --add 回退到 data[array_name] → TestSourceLevelGuards 立即失败 + TestEndToEndAddUnfinishedSubprocess 真 subprocess 跑也立即失败）：TestResolveArrayTargetSpecialCase(6) + TestMigrationFromTopLevel(8) + TestEndToEndAddUnfinishedSubprocess(4 含**血案场景复现** 收工 add 3 项 → 开工 read 必含 3 项 + 计数) + TestSourceLevelGuards(8)。② **P2 V37.9.38 LLM cron audit 三件套**（V37.9.37 收工 73b2248 已登记 → V37.9.36 教训"扫所有调 LLM 的 cron job 是否对齐 fail-fast 模式" + MR-8 跨 job 不同步是 copy-paste-is-a-bug-class 兑现）：发现 5 个 Tier-A 脚本（hf_papers/arxiv_monitor/finance_news/run_hn_fixed/openclaw_official）共享同款 V37.9.36 反模式（占位符 fallback `价值：⭐⭐⭐` + `贡献：AI领域相关研究` + `要点：技术深度文章`）。今日交付：(a) **`ontology/llm_cron_audit.py` 扫描器**（pure Python 行级启发式 + prompt 模板豁免 + 三种字面量上下文 quoted_inline / shell_multiline_close / py_assignment_open）扫 21 候选脚本 → 4 已对齐（V37.5 kb_review / V37.8.10 kb_evening / V37.9.16 kb_deep_dive / V37.9.36-37 rss_blogs）+ 17 未对齐含 15 placeholder findings (b) **`docs/llm_cron_fail_fast_audit.md`** 完整报告（21 候选 P1/P2/P3 三档优先级 + V37.9.39+ 修复路线图） (c) **INV-LLMCRON-AUDIT-001 守卫**（meta_rule=MR-8, severity=high, verification_layer=[declaration, runtime], **13 checks**）：11 declaration file_contains/python_assert（audit 模块 V37.9.38 标记 / 4 个公开 API 函数定义 / ALIGNED_SCRIPTS 含 4 个版本锚点 / PLACEHOLDER_PATTERNS 含 V37.9.36 血案 3 个核心字符串 / audit 报告 21 候选 + V37.9.38+ 路线图 / test 文件 V37.9.38 标记）+ 2 runtime python_assert（subprocess 真跑 audit --report 验证 4 个 ALIGNED 版本被识别 / subprocess 跑 26 单测全过）(d) **26 新单测 6 类**：TestPlaceholderPatterns(2) + TestFindPlaceholderFindings(9 含 prompt 模板豁免 / shell 多行字符串闭合 / Python 三引号块豁免 / 注释豁免 / quote 包裹三种上下文) + TestAuditScript(3) + TestAlignedScriptsRecognition(3 含 endswith 路径归一化让绝对路径也能匹配) + TestMarkdownReport(1) + TestCliCheckMode(3) + TestSourceLevelGuards(5)。③ **arxiv_monitor PoC fix 推迟 V37.9.39+**（MR-10 三问严格执行）：Q1 之前存在 ✓ / Q2 非新引入 ✓ / Q3 最小修复 = **不引入新 bug** — 350+ 行脚本一次性大改风险高（V37.9.34 cascade hotfix 教训），audit + 防御 + 路线图已是完整 V37.9.38 deliverable，PoC 留给 V37.9.39 新 session 全力投入 + Mac Mini E2E 真跑数据回归。④ **MR-8 兑现**：未来 V37.9.39+ 每次升级 N 个 LLM cron 时，`python3 ontology/llm_cron_audit.py --report` 立即知道剩余多少不合规 + 风险分级，从"靠记忆"升级为"机器化 visibility"。⑤ **累计**：56→58 suites (+2) / 2065→2117 tests (+52: status_update_v9_38 26 + llm_cron_audit 26) / 0 fail / governance 535→549 checks (+14: INV-LLMCRON-AUDIT-001) / 70→71 invariants (+1) / 17 meta rules / **MRD-LAYER-002 41/41 high 全部 ≥2 层** / 安全 95/100 / VERSION 0.37.9.37 → 0.37.9.38. ⑥ **下次 V37.9.39+ 候选**（按 audit 报告优先级）：(a) **P1 (≥2 findings)**: arxiv_monitor / hf_papers / semantic_scholar 各 3 findings — 最高优先级，daily cron 高频 (b) **P2 (1 finding)**: dblp / acl_anthology / ai_leaders_x / karpathy_x / openclaw_official×2 (c) **P3 (0 findings 但缺 fail-fast 标志)**: chaspark / github_trending / ontology_sources / kb_inject / kb_dream / freight_watcher / finance_news / run_hn_fixed (d) **运行时验证**: V37.9.30 EPERM 24h 数据回归 (5/9 早 截止) + V37.9.35 推送质量 24-48h 观察 + 5/11 决策窗口 jobs_to_crontab + kb_sources_to_index machine_sync 真激活。⑦ **元价值**: 一日内同时收敛"开工/收工流程内部 bug" (P1) + "跨 job 系统性 silent failure 视野" (P2) — 验证 V37.9.36 fail-fast 模式不只是 rss_blogs 单点修复, 而是项目级架构契约。原则 #18 "补证据而非补功能" 兑现 — 不增 cron 不增功能, 但每次 LLM cron 升级现在有了"机器化 visibility". 原则 #28 "理解再动手" 严格执行 — PoC 推迟避免 V37.9.34 cascade 教训复现。|
| V37.9.37 | 2026-05-07 | **rss_blogs 深度分析升级（用户反馈"博客精选都是值得读的好文章, 不能只是题目摘要"）+ V37.9.36 fail-fast 契约保留** — ① **触发**: V37.9.36 闭环后用户立即提产品深度需求 — "请把生成的内容和分析的内容尽量多一些 不能仅仅是题目或摘要", 同时强调 V37.9.35 已把 WhatsApp budget 升到 4000 + 多窗口能力具备, 可以放开手脚生成长内容. 用户视角原则 #13 第 9 次正向兑现 (V37.9.36 闭环后立即触发产品升级). ② **设计选择 (用户决策)**: 深度档位 = "混合 5 字段 + 按评级动态调长度" (⭐⭐⭐→100-150字 / ⭐⭐⭐⭐→250-400字 / ⭐⭐⭐⭐⭐→500-800字 旗舰级充分展开); LLM 失败行为 = "每篇独立 retry, 部分失败用 RSS description fallback 标 [LLM_DEGRADED]". ③ **5 字段 prompt 重写** (line 217-282): 📌 中文标题 (信达雅翻译 ≤25字) / 🔑 核心要点 (3-5 条 bullet 每条 ≤60字) / 💡 关键洞察 (按评级动态长度, 揭示作者立场+方法论+行业趋势关联+与已有工作对比+局限性) / 🎯 实践启发 (1-3 条对 AI 工程师/创业者/架构师的具体行动建议) / ⭐ 评级 + 推荐场景 (谁应该读 / 何时读 / 配什么场景). 含 V37.8.6 同款反幻觉守卫 ("严禁虚构作者未提及的事实/数据/链接 / 严禁推断 Hugging Face/OpenAI/GitHub 等平台的具体内部状态除非原文提及 / 严禁把 HTTP 错误码/Python 异常/错误日志当外部信号"). 原文摘要长度从 300 → 600 字给 LLM 更多上下文. ④ **LLM 调用策略重写**: V37.9.36 单次调用全部 N 篇 → V37.9.37 每篇独立调用 + 独立 retry (5s/10s/20s 指数退避). 新 helper `call_llm_single_with_retry()` 函数封装单篇 LLM 调用 + 三层失败检测 (V37.9.36 contract 复用) + 3 attempts. 主循环 `for ((i=0; i<TOTAL_NEW; i++))` 调用 helper, 收集结果到 `$RESULTS_FILE` (jsonl: idx/content/failed/fail_reason). max_tokens 从 4096 → 2500 (单篇够用 + 节省 token). ⑤ **失败语义细化**: V37.9.36 任一失败 → fail-fast → V37.9.37 三档 status: (a) 全部成功 → `status:ok` (b) 部分失败 → `status:partial_degraded` + `failed:N` 字段 + `[SYSTEM_ALERT]` 通知 + 仍推送其余成功篇 (c) 全部失败 → `status:llm_failed` + `reason:all_failed_<原因>` + `[SYSTEM_ALERT]` + `exit 1` (V37.9.36 fail-fast 契约保留, 但仅当 ALL 失败才触发). ⑥ **emit 端 5 字段 key-based parser** (V37.8.7 ontology_parser 同款模式): `parse_5field_output()` 容忍字段顺序错乱 / 单字段缺失 / prefix 变体 (识别 📌/🔑/💡/🎯/⭐ emoji marker 作为字段头, state-machine 累积内容到 flush). 失败篇用 `[LLM_DEGRADED] 深度分析失败, 原文摘要供参考:` + RSS description 前 300 字兜底, 让用户至少知道有什么文章值得点链接看 (替代 V37.9.36 "（本篇 LLM 摘要缺失）"的更弱信号). 严禁占位符回归 (V37.9.36 反模式硬规则保留). ⑦ **多窗口分片复用 V37.9.21 同款** (>8000 字才触发, 否则单段直发利用 V37.9.35 客户端折叠机制): 总长 ≤4000 单段不折叠 / 4000-8000 单段客户端自动折叠 2 气泡 / >8000 多窗口切片每段 ≤4000 + `mktemp -d` + sleep 1s 防 WhatsApp 消息乱序 + 段间 header 含 `[i/N]` + `(续)` 标识让用户知道是连续的. ⑧ **44 单测 6 类** (V37.9.36 27 → V37.9.37 +17 = 44): TestRssBlogsShellGuards(24 含 V37.9.37 marker / V37.9.36 [SYSTEM_ALERT] / send_alert / source notify.sh / __LLM_HTTP_ERROR__ / __LLM_PARSE_FAIL__ / call_llm_single_with_retry helper 定义 / backoffs=(5 10 20) / for attempt in 0 1 2 / TOTAL_NEW 主循环 / status:partial_degraded / all_failed_ prefix / [LLM_DEGRADED] 标记 / [LLM_DEGRADED] 深度分析失败 完整字串 / 5 字段 emoji marker / 严禁虚构 反幻觉 / ⭐⭐⭐⭐⭐→500-800字 评级长度 / TOTAL_LEN -le 8000 阈值 / MAX_CHUNK = 4000 / 多窗口分支 sleep 1s / [i/N] header 标识 / 全部失败分支 exit 1 顺序锁 / 占位符 line-by-line 扫描跳过注释) + TestLlmResponseDetection(5 V37.9.36 detection snippet 在 retry helper 内复用语义不变) + TestEmit5Field(8 含 5 字段完整解析 / 部分失败 LLM_DEGRADED + RSS description / 全部失败标 LLM_DEGRADED / RSS description 空时提示点链接 / 5 字段容忍单字段缺失 / 字段顺序错乱不影响解析 / 任何 LLM 输入下严禁占位符回归) + TestStatusSchema(2 partial_degraded + all_failed_) + TestMultiWindowSplit(4 短消息单 chunk / 多篇 ≤4000 单 chunk / 每篇 1500x3 必触发切片 / [i/N] 标识) + TestActualBloodLessonScenario(2 502 触发 fail-fast marker + 实际 articles 部分失败用 LLM_DEGRADED 不写占位符). **反向验证已确认守卫真有效**: sed 把 emit Python 内 `[LLM_DEGRADED] 深度分析失败` 改回 `要点：技术深度文章` → `test_no_placeholder_fallback_text` 立即在 line 488 抓到 "要点：技术深度文章 unexpectedly found", 还原后 44/44 全过. ⑨ **INV-RSSBLOGS-001 扩展 14→28 checks**: V37.9.36 declaration 9 + V37.9.37 declaration 11 (V37.9.37 marker / call_llm_single_with_retry helper / backoffs=(5 10 20) / status:partial_degraded / all_failed_ prefix / [LLM_DEGRADED] 标记 / 5 字段 emoji 全套 / 严禁虚构 / 评级动态长度 / 多窗口阈值 8000 + 4000 / 多窗口 sleep 1s) + V37.9.36 python_assert 3 + V37.9.37 python_assert 2 + V37.9.36 runtime 2 + V37.9.37 runtime 2 + V37.9.36 旧 `LLM 摘要缺失` declaration 1 删除 (被 V37.9.37 [LLM_DEGRADED] 等效替代) + V37.9.36 RUNTIME emit python_assert 升级为 V37.9.37 5 字段 + LLM_DEGRADED 端到端验证 (read RESULTS_FILE marker / parse_5field_output 函数存在 / 端到端 2 篇全失败 → 两篇都标 [LLM_DEGRADED] + RSS desc fallback / 占位符严禁回归). ⑩ **累计**: 56 suites 不变 / 2048→2065 tests (+17) / 0 fail / governance 523→535 checks (+12 INV-RSSBLOGS-001 V37.9.37 守卫) / 70 invariants 不变 (扩展非新增) / 17 meta rules / 安全 95/100 / VERSION 0.37.9.36 → 0.37.9.37. ⑪ **典型预期推送长度**: 3 篇混合 (⭐⭐⭐ ~400 字 + ⭐⭐⭐⭐ ~800 字 + ⭐⭐⭐⭐⭐ ~1500 字) ≈ 2700 字, 单段直发 WhatsApp 不折叠. 3 篇都旗舰 ⭐⭐⭐⭐⭐ ≈ 4500 字, 触发多窗口分片 2 段. ⑫ **元价值**: V37.9.36 闭环 fail-fast 修复, V37.9.37 闭环产品深度升级 — 同日内"用户视角发现质量缺口 → 立即修复 + 立即升级" double 兑现, 验证 V37.9.35 budget 升级 + V37.9.21 多窗口框架的真实价值 (没有这两个底层框架就无法承载本次深度内容升级). 原则 #18 "补证据而非补功能" 兑现 — 不增加 cron 数量, 但 rss_blogs 单 job 产出价值从"标题+一句话+⭐⭐⭐"升级为"标题+核心要点+关键洞察+实践启发+评级", 信息密度跃升一个数量级. |
| V37.9.36 | 2026-05-07 | **rss_blogs LLM fail-fast 闭环（MR-4 silent-failure 第 26 次演出 + 用户视角 #13 第 8 次正向兑现）** — ① **触发**: V37.9.35 budget 升级当晚 (周四) 用户在 WhatsApp 发现 18:00 cron 推送的"📖 博客精选"3 篇文章全部显示 `要点：技术深度文章 / 价值：⭐⭐⭐` 占位符 + 来源含括号注释 `Simon Willison(LLM工具/实践)`. 单测 / 治理 / preflight 全过, 但用户视角推送质量为零. ② **Mac Mini 取证 (原则 #28 第三问"最小修复"前先看真实数据)**: `cat ~/.openclaw/jobs/rss_blogs/cache/llm_*.txt + last_run.json` 揭示 (a) `llm_raw_last.txt = '{"error":"HTTP Error 502: Bad Gateway \| upstream: primary: HTTP Error 301 Moved Permanently 无限重定向; gemini: HTTP Error 503 Service Unavailable"}'` (b) `llm_content.txt = 1 byte` (空) (c) `rss_message.txt 全部 3 篇 fallback "技术深度文章 ⭐⭐⭐"` (d) `last_run.json {"status":"ok","sent":true}` ← **谎报成功**. 真因 = Qwen3 主+gemini fallback 双 provider 同时故障 + run_rss_blogs.sh 三层宽容 (`except pass` 吞 KeyError → `log WARN 不 exit` → emit 硬编码 `要点：技术深度文章 + 价值：⭐⭐⭐` 占位符) + last_run.json 谎报 status:ok + 零 [SYSTEM_ALERT] 推送. ③ **MR-4 silent-failure 第 26 次演出新形态**: 错误日志完整保留在 llm_raw_last.txt 里 (可见层有记录), 但每一层都拒绝把它升格为告警, 最后用户成了告警链. rss_blogs 是 V36 之前老代码, V37.5 (kb_review) / V37.8.10 (kb_evening) / V37.9.16 (kb_deep_dive) 三次 fail-fast 升级时都没同步扫描 rss_blogs — MR-8 反例提醒 (跨 job 没及时同步 fail-fast 升级也是 copy-paste-is-a-bug-class). ④ **结构修复 (与 V37.5/V37.8.10/V37.9.16 同款 fail-fast 模式对齐)**: (1) `run_rss_blogs.sh` 头部加 source notify.sh + `send_alert()` helper (走统一 `[SYSTEM_ALERT]` 通道 / topic alerts → Discord #alerts) (2) LLM 调用后三层检测 (替换 line 224-236 宽容处理): **层 1** HTTP 错误响应 (`isinstance(d, dict) and 'error' in d` → `__LLM_HTTP_ERROR__:` marker) **层 2** JSON 解析失败 (KeyError/IndexError/TypeError → `__LLM_PARSE_FAIL__:no_choices:`) + bad JSON (JSONDecodeError → `__LLM_PARSE_FAIL__:bad_json:`) **层 3** 空 content. 任一触发 → `send_alert "..."` + `printf '{"status":"llm_failed","reason":"%s"}' > "$STATUS_FILE"` + `exit 1` (3) emit 端 (line 304-306) 删除 `要点：技术深度文章` + `价值：⭐⭐⭐` 占位符 fallback, 改为 `（本篇 LLM 摘要缺失，参见原文链接）` 显式标记 — 让用户感知层不被欺骗 (4) error 消息 `\n` squash 为空格防止破坏 marker 行 / truncated 500 chars 防 logspam. ⑤ **27 新单测** test_run_rss_blogs.py 4 类: TestRssBlogsShellGuards(15 含 V37.9.36 marker / [SYSTEM_ALERT] / send_alert / source notify.sh / __LLM_HTTP_ERROR__ / __LLM_PARSE_FAIL__ / status:llm_failed / "LLM 摘要缺失" / 不含"使用原始标题推送" / 不含"要点：技术深度文章" 占位符 line-by-line 跳过注释 / HTTP error 分支 500 字符内 exit 1 顺序锁 / 空 content 分支 500 字符内 exit 1 顺序锁 / emit 端显式缺失标记) + TestLlmResponseDetection(9 端到端 subprocess 真跑提取的 detection snippet 含**血案场景复现** `test_actual_502_blood_lesson_response` 输入 2026-05-07 18:00 实际 LLM_RESP 断言 stderr 含 `__LLM_HTTP_ERROR__` + primary 301 + gemini 503 / no choices / bad json / empty / 正常 / choices 空 list IndexError / choices 非 list / 多行 error 消息 squash / 500 char truncation) + TestEmitParser(4 含 full parse / partial parse 显式缺失 / 空 LLM content 全部缺失 / 任何 LLM 输入下严禁占位符) + TestActualBloodLessonScenario(2 复现完整链路: 502 触发 fail-fast 检测 + 实际 articles + 空 content → 不写占位符). **反向验证已确认守卫真有效**: sed 把 `（本篇 LLM 摘要缺失）` 改回 `要点：技术深度文章` → `test_no_placeholder_fallback_text` 立即报失败 + 行号定位, 还原后 27/27 全过. ⑥ **INV-RSSBLOGS-001 新增** (meta_rule=MR-4, severity=high, verification_layer=[declaration, runtime], 15 checks): 9 个 declaration file_contains/file_not_contains 守卫 (V37.9.36 marker / [SYSTEM_ALERT] / send_alert / source notify.sh / `__LLM_HTTP_ERROR__` / `__LLM_PARSE_FAIL__` / status:llm_failed / "LLM 摘要缺失" / 不含 "使用原始标题推送") + 3 个 declaration python_assert (line-by-line 占位符扫描跳过注释 / HTTP error 分支顺序锁 / 空 content 分支顺序锁) + 3 个 runtime python_assert (subprocess 真跑 detection snippet 复现 502 血案 / subprocess 真跑 emit code 验证空 content 输出含 "LLM 摘要缺失" 不含占位符 / subprocess 跑 27 单测端到端). ⑦ **auto_deploy.sh FILE_MAP** 已含 `jobs/rss_blogs/run_rss_blogs.sh` (无需补齐). full_regression.sh 接入 `run_rss_blogs` suite. ⑧ **累计**: 55→56 suites (+1) / 2021→2048 tests (+27) / 0 fail / governance 506→523 checks (+17: INV-RSSBLOGS-001 15 + 2 audit-of-audit metric 漂移自适应) / 69→70 invariants (+1) / 17 meta rules / **MRD-LAYER-002 40/40 high 全部 ≥2 层** / 安全 95/100 / VERSION 0.37.9.35 → 0.37.9.36. ⑨ **Mac Mini 部署后预期行为**: 下次 LLM 双 provider 双故障时 → 18:00 cron 立即推 `[SYSTEM_ALERT] rss_blogs LLM 失败 / 原因: HTTP Error 502 \| upstream: primary 301; gemini 503 / 降级处理: 今日未推送博客精选 (避免占位符污染) / 建议: 查 Adapter/Proxy 状态 + llm_raw_last.txt` 到 Discord #alerts + WhatsApp, **不再静默推送占位符消息**. last_run.json 写 `{"status":"llm_failed","reason":"HTTP Error 502 ..."}` 让 watchdog/preflight 能识别真实失败. ⑩ **元教训**: V37.5 / V37.8.10 / V37.9.16 三次 fail-fast 升级时都应做"扫所有 LLM 调用 job 检查同款反模式"的 audit, 而不是单点修复. MR-8 copy-paste-is-a-bug-class 不只跨文件重复造 bug, **跨 job 没及时同步架构升级也是 bug**. 候选 V37.9.37 audit: 扫所有调 LLM 的 cron job (~10 个) 是否都对齐 fail-fast 模式 (检测项: 含 [SYSTEM_ALERT] / 不含 silent fallback 占位符 / status_file 不谎报 ok). |
| V37.9.35 | 2026-05-07 | **WhatsApp 客户端显示折叠架构发现 → budget 1400→4000 (Path A 保守路径)** — ① **触发**: V37.9.33 freight 三层 LLM 输出产生 **8131 字符**单条消息, 用户观察 WhatsApp 出现"两个窗口"+ **时间戳完全相同 (都是 12:26)**. ② **5 层切片排查 (原则 #28 不凭推测)**: (a) OpenClaw `auth-profiles-*.js` 主 bundle grep `splitMessage/chunkMessage/maxLength` 全部空命中 (b) `runtime-whatsapp-outbound.runtime-*.js` lazy loader 实现 grep 空 (c) `~/.openclaw/openclaw.json` channels.whatsapp 无 maxTextLength 字段 (d) Baileys 7.0.0-rc.9 已知不分片 (单条 65,536 上限) (e) **WhatsApp 手机客户端 ~4000-5000 字符自动分气泡显示** ✅ 真切片层. ③ **关键证据**: 时间戳相同 = 单条消息 (协议层若真切片两次 API 调用至少差 1 秒); 8131÷2≈4065 字/气泡 = 折叠阈值约 4000-4500 (与 Telegram 4096 限制接近). ④ **Path A 保守路径实施 (用户决策)**: budget 1400→4000 在 3 source + 3 test 文件 — `kb_review_collect.py:518` `body[:1400]→[:4000]` / `kb_evening_collect.py:182` 同 / `kb_deep_dive.py:596` `_WA_BUDGET_PER_PART = 1400→4000` + line 601 `_WA_BODY_BUDGET_PER_PART = 1200→3800` (header 200 buffer 保留). 测试 5 处 assertion + 输入长度调整 (`x*5000→8000`, `段落内容。x500→1500` 触发新 budget multi-part) + 1 处 test_body_truncated_at_1400 重命名 _at_4000. **保留**手动分屏代码作为防御 fallback (V37.9.21 `_split_text_into_chunks` + kb_dream max_chunk=4000 不动) — 极长内容 (>8000) 仍触发 + 跨平台一致性保险 (WhatsApp Web/Desktop 折叠阈值可能不同) + MR-7 治理不删除已验证安全边界. ⑤ **影响矩阵**: kb_review/kb_evening/kb_deep_dive 信息密度立即提升 **2.86x** (1400→4000); freight 已无 budget 不变; kb_dream max_chunk=4000 已合理不变; 论文监控 task 无 1400 截断不变 — 实际只 3 task 受益, 证明 1400 是 V37.9.21 单点假设非全局. ⑥ **完整案例文档** `ontology/docs/cases/whatsapp_client_display_folding_case.md` (TL;DR + 完整发现链路 + 5 层排查矩阵 + V37.9.21 假设来源 + Path A 实施 + 跨任务影响 + 4 条元教训 + 与其他案例关系). ⑦ **元教训**: (a) 字符数限制要看真实生产数据不是 documented limit — V37.9.21 1400 是没实测的"安全值" (b) 用户视角观察是不可替代的最终验证层 — 同日 V37.9.31/32/34/35 四次同源教训 (c) 保守路径 (Path A) 边际效用 > 激进路径 (Path C) — 信息密度 2.86x 已足够 + 跨平台保险 (d) 不删除已工作的防御代码即便上层假设修正后部分失效. ⑧ **累计**: 55 suites 不变 / 2021 tests 不变 (源码改 + 测试 input 调整, net 0) / 0 fail / governance 506 checks / 17 meta rules / 安全 95/100 / VERSION 0.37.9.34 → 0.37.9.35. ⑨ **Mac Mini 部署**: 5 文件 cp (kb_review_collect.py + kb_evening_collect.py + kb_deep_dive.py + VERSION + CLAUDE.md). 下次 22:00 kb_evening / 22:30 kb_deep_dive cron 自动用新 budget. 周日 kb_review 同样. ⑩ **下次 (V37.9.36+) 候选**: (a) 24-48h V37.9.35 推送质量观察 — kb_evening / kb_deep_dive 是否产生更深度内容 (b) 跨平台一致性测试 (WhatsApp Web/Desktop/旧版手机) (c) 如跨平台都一致 → 评估 Path B 移除 kb_deep_dive 手动分屏 (d) V37.9.30 24-48h ACL/lsof/snapshot 真实数据回归 (仍是 EPERM 真根因调查的硬卡).|
| V37.9.34 | 2026-05-07 | **V37.9.33 hotfix: PROMPT 内嵌 ASCII 双引号导致 bash 字符串提前终止 (Mac Mini 实测发现, MR-4 silent-failure 第 25 次演出)** — ① **触发**: V37.9.33 部署后用户立即跑 `bash run_freight.sh` 验证三层输出, 终端报 `line 222: 本期无显著经济晴雨表信号）... No such file or directory`. RSS 抓取成功 15 条但 LLM 调用从未发生 (脚本中段崩). ② **真根因**: V37.9.33 LLM prompt 内嵌入两处 ASCII 双引号 `输出"📊 本期无显著经济晴雨表信号"` (line 222) + `输出"🏢 本期无显著运营信号"` (line 229). bash 在 `PROMPT="..."` 内部解析时, 把这些 ASCII `"` 当成 PROMPT 字符串的关闭符, 导致字符串提前终止 → 后续中文段落被当成 shell 命令解析 → "No such file or directory". V25 原 prompt 用单引号 `'行业信号 — 描述'` 没此 bug, V37.9.33 引入双引号导致回归. ③ **MR-4 silent-failure 第 25 次演出新形态**: V37.9.33 的 26 个单测全过 + dev 环境 `bash -n` 语法检查通过, 但实际 `bash` 运行才暴露问题. **`bash -n` 只检查语法不检查变量展开/字符串嵌套**, 实际运行时的 PROMPT="..." 内部嵌套引号问题在静态语法检查中**不可见**. 这是 V37.9.31/32 同款"自动化测试全过 + 真用户跑才暴露 bug"模式的第三次演出. ④ **最小修复 (原则 #28 三问全过)**: line 222 + 229 把 `输出"📊 X"` 改为 `输出\"📊 X\"` (反斜杠转义双引号). bash 在 `"..."` 内看到 `\"` 当作字面量 `"` 字符传给 LLM, 不再把它当字符串关闭符. ⑤ **2 新 source-level 守卫单测** TestV37934BashQuoteEscaping: `test_no_unescaped_double_quote_after_chu` 用负向回顾正则 `(?<!\\\\)输出"` 扫源码禁止任何未转义的 `输出"` 字面量 + `test_section_empty_signals_use_escape` 验证三层 "本期无显著X信号" 短语周边必须有 `\"` / 单引号 / 全角引号包围 (5 字符上下文检查). 防 V37.9.34 hotfix 后未来重构再次回退到 ASCII 双引号反模式. ⑥ **元教训**: bash 字符串嵌套是**运行时语义层级**问题, 不是语法层级问题. `bash -n` 看不到, source-level grep 在 V37.9.33 没设计这个守卫. 这是测试三层"WhatsApp 业务验证" + "用户视角观察" (原则 #13/#15) 的第 6 次正向兑现 — 自动化测试 + dev 环境 `bash -n` 都过, 用户 Mac Mini 实跑 stderr 才告诉我们真问题. ⑦ **累计**: 55 suites 不变 / 2019→2021 tests (+2) / 0 fail / governance 506 checks 不变 / 17 meta rules / 安全 95/100 / VERSION 0.37.9.33 → 0.37.9.34. ⑧ **Mac Mini 部署**: 仅 cp 1 文件 (run_freight.sh), 立即重跑 freight 应看到完整三层输出 + 客户画像生成正常.|
| V37.9.33 | 2026-05-07 | **货代任务深度优化：Tier 1/2 权威源扩展 + LLM 三层深度分析（"全球发货是经济晴雨表"用户论点兑现）** — ① **触发**: 用户论点 — 全球发货信息（海运/陆运/空运）是经济**领先指标**（运价 1-3 个月领先 GDP），比咨询机构或专家智囊事后判断更值得信赖。V37 freight_watcher 仅用 Tier 3 行业新闻 RSS + Google News 通用检索，权威 Tier 1/2 数据源（运价指数 / 班轮公告 / 港口吞吐 / 海关数据）完全没接入。用户明确"不新增任务，直接优化货代"。② **设计选择**: Tier 1/2 直连权威源（Drewry / 上海航交所 / Marine Traffic / 海关总署）面临反爬 + GeoIP 限制，V37.9.31/32 stealth 升级仍不保证全部可达。改用 **Google News RSS 精确搜索 indexed 内容**（100% 可达，间接覆盖权威源被引用的内容），代价是延迟 1-2 天但保证可靠性。③ **SOURCES 扩展 12 → 27**: (a) 集装箱运价指数 4 (SCFI/CCFI/Drewry WCI/FBX Freightos Baltic) — 周更经济温度计 (b) 散货 1 (BDI Baltic Dry) — 大宗商品全球晴雨表 (c) 空运 1 (TAC Index/IATA CASS) — 周更 (d) 班轮公司动态 3 (MAERSK / CMA CGM+MSC+Hapag-Lloyd / COSCO+Evergreen+Yang Ming) — 缩舱/空班/路线调整领先 1-2 个月反映需求 (e) 港口吞吐 3 (LA+Long Beach / Shanghai+Ningbo / 全球 congestion+TEU) (f) 海关 2 (China exports+trade balance / US imports+trade deficit Census Bureau)。④ **关键词集 +4**: INDEX_KW (16 词中英双语) / CARRIER_KW (14 词，10 班轮 + 4 中文) / PORT_KW (14 词) / CUSTOMS_KW (10 词)。⑤ **抓取上限 10→15**: 给新增 Tier 1/2 留空间，防 Tier 3 行业新闻挤占运价指数（这些源每周才出 1 条但价值最高）。⑥ **LLM prompt 三层结构化升级** (V25 单层 → V37.9.33 三层): **📊 第一层：经济晴雨表** (用 `指数：` 前缀，输出指数名+方向+经济解读+评级) **🏢 第二层：运营信号** (用 `动作：` 前缀，输出公司/港口动作+影响+评级) **🚢 第三层：商机条目** (V25 原 `企业信号：` 前缀**完整保留**，Step 8 ImportYeti 提取 regex 仍工作)。每段最多 5 条防 WhatsApp 超长。LLM prompt 含反幻觉守卫"严禁虚构具体数字（运价/港口吞吐量）必须来自原文"。⑦ **MSG_FILE 三段式 passthrough**: 新增 section-aware regex `(?=\\n📊\|\\n🏢\|\\n🚢\|\\n\\d+\\.)` 切块，`current_section` 跟踪当前段（`index`/`ops`/`business`），ImportYeti link **仅在 `current_section == "business"` + ⭐≥4 + 含 `企业信号：`** 三条件全满足时附加。📊 段含 SCFI 数字 / 🏢 段含 MAERSK 公司名都不会误触发 ImportYeti。⑧ **末尾追加完整新闻链接段** "📚 本期数据来源 (15 条权威信源)"，让 KB 归档可追溯到原始权威源（Drewry/MAERSK/上海航交所等）。⑨ **Step 8 ImportYeti 向后兼容守卫**: line 451-452 regex `re.search(r'企业信号：(.+?)[\\s]*[—–\\-]', block)` 完全保留 V25 格式 — 实测 simulated 3-section input 仅提取 🚢 段企业（"Acme Corp"），📊 段 "SCFI" 数字 + 🏢 段 "MAERSK" 公司名都被自动过滤。⑩ **26 新单测 6 类** test_freight_v9_33.py: TestV37933SourcesExpanded(7 验证 12 新源 + total ≥25) + TestV37933KeywordSets(5 验证 4 新 KW + 中英双语) + TestV37933CapBumped(1 cap=15 + 守卫 V37 旧 cap=10 不残留) + TestV37933LLMPromptThreeLayer(6 三段 emoji + 各段独立前缀 `指数:`/`动作:`/`企业信号:` + 反幻觉守卫 + 每段 5 条限制) + TestV37933MsgFileAssembly(5 section-aware 切块 + current_section 跟踪 + ImportYeti scoping + 数据源 footer + extract_company V25 兼容) + TestV37933Step8BackwardCompat(2 含 source-level grep 守卫 Step 8 不能匹配 `指数:`/`动作:` + simulated 3-section 输入仅提取 🚢 段企业)。⑪ **累计**: 54→55 suites (+1) / 1993→2019 tests (+26) / 0 fail / governance 506 checks 不变 / 17 meta rules / 安全 95/100 / VERSION 0.37.9.32 → 0.37.9.33。⑫ **Mac Mini 部署**: 仅需 cp run_freight.sh + VERSION + CLAUDE.md（无新增 cron / 无新依赖 / 无 ImportYeti scraper 改动）。下次 8:00/14:00/20:00 cron 自动用新源。⑬ **下次 (V37.9.34+) 候选**: (a) 24-48h 后看 freight 推送质量（经济晴雨表段是否有 SCFI/BDI 实数据 vs 空段）(b) Marine Traffic / Vessel Finder AIS 直连（V37.9.32 stealth 实战，需 API key 评估）(c) 海关总署 / Census Bureau 月度数据直连（RSS 可能可达）(d) 增加分行业指数（汽车 freight / 电子产品 freight / 服装 freight）。⑭ **元价值**: V37.9.33 是 V3 路标"别人会扩展"的方法论体现 — 不创建新 cron 而是**深化现有 job** + LLM prompt 工程让单一 job 输出多层结构化分析。用户视角看到"货代速报"从单层"商机条目"升级为三层"经济晴雨表+运营信号+商机条目"，信息密度大幅提升。原则 #18 "补证据而非补功能"兑现 — 没增加新 job 数量，但 freight 这个 job 的产出价值翻倍。|
| V37.9.32 | 2026-05-07 | **playwright-stealth 1.x → 2.x API 兼容修复（V37.9.31 Mac Mini 实测发现）** — ① **触发**: 用户合并 V37.9.31 后 Mac Mini 手动跑 freight 验证全链路成功 (`"deep_dive":"ok"` 历史性首次出现 — 5/4 之后第一次), 但 scraper 输出 `WARN: playwright-stealth 未安装 ... 使用 manual stealth fallback`. 实际 `pip3 install playwright-stealth` 显示 `Requirement already satisfied (2.0.2)`, 矛盾根因 = V37.9.31 代码用 `from playwright_stealth import stealth_sync` (1.x API), 但 2.x 改为 `Stealth` class. ② **API 变化考证**: `pip install playwright-stealth==2.0.3` + `python3 -c "from playwright_stealth import stealth_sync"` → ImportError; `from playwright_stealth import Stealth` → OK; `Stealth().apply_stealth_sync(page)` 是 2.x 推荐入口 (返回 None, in-place patch). ③ **修复**: importyeti_scraper.py main() 函数内 import 时三档 fallback chain — 先 try 2.x `from playwright_stealth import Stealth` + `_stealth_apply = lambda page: Stealth().apply_stealth_sync(page)` (`stealth_api_version = "2.x"`), ImportError 则 try 1.x `from playwright_stealth import stealth_sync` + `_stealth_apply = lambda page: stealth_sync(page)` (`"1.x"`), 都失败则 `_stealth_apply = None` + `"none"` + manual STEALTH_JS fallback. 调用点从 `stealth_sync(page)` → `_stealth_apply(page)` (API 版本无关). 启动日志加 `stealth_lib={api_version}` 可观测哪个分支生效. ④ **3 新单测 source-level 守卫** TestSourceLevelGuards 扩展: `test_v37_9_32_uses_2x_api_first` (Stealth import 必先于 stealth_sync) + `test_v37_9_32_uses_apply_stealth_sync_method` (必含 `apply_stealth_sync(page)`) + `test_v37_9_32_call_site_uses_api_agnostic_helper` (regex 锁定 `if stealth_available:` 块内调 `_stealth_apply` 而非 `stealth_sync`). 防止未来重构回退到 1.x-only API. ⑤ **Mac Mini E2E 数据点 (V37.9.31 同时验证)**: ✅ `last_run.json` 含 `"deep_dive":"ok"` (历史性首次, 5/4 之后第一次) ✅ Step 8-10 全跑完 (高星 OMI → ImportYeti 查询 → 客户画像) ✅ rsync helper 错峰 sleep 178s + 36s 后脚本继续 (V37.9.31 fail-open 真生效) ✅ Preflight 通过 82 / 失败 0 (历史新高, 之前 4 deployment fail 全消) ✅ WhatsApp 推送收到 OMI 客户画像 (Changzhou Zweal Office Equipment 供应商 + 上海港→洛杉矶港路径 + USD 2,500-4,000 月预算). ⑥ **累计**: 54 suites 不变 / 1990→1993 tests (+3) / 0 fail / governance 506 checks 不变 / 17 meta rules / 安全 95/100 / VERSION 0.37.9.31 → 0.37.9.32. ⑦ **下次 (V37.9.33+) 候选**: (a) Mac Mini 重跑 freight 应输出 `stealth_lib=2.x (on)` 而非 manual fallback warn (b) V37.9.30 24-48h ACL/lsof/snapshot 数据回归 (c) 5/11 决策窗口 jobs_to_crontab + kb_sources_to_index machine_sync dry-run 真激活. ⑧ **元教训**: 用户视角观察 (原则 #13) 第 5 次正向兑现 — V37.9.31 自动化测试单测全过 + Mac Mini Preflight 0 failure, 但用户实际跑 freight 看 stderr 才暴露 import 路径错配. dev 环境跑测试时 `pip install playwright-stealth` 装的是最新 2.x, 但 V37.9.31 代码 grep 仅守卫 `playwright_stealth` 字面量出现 (没守卫 `Stealth` 或 `stealth_sync` 哪个函数被 import), 所以 source-level test 全过. 实际生产数据告知真问题. 这是原则 #15 测试三层 "WhatsApp 业务验证" 的进一步兑现.|
| V37.9.31 | 2026-05-07 | **V37.9.27 rsync_helper 回归修复 (fail-open exit 0) + 货代 schema 完整 + ImportYeti 反爬升级 (MR-4 第 24 次演出: 修复引入更深 silent failure)** — ① **触发**: 用户 5/7 preflight 报告"货代 deep_dive: last_run.json 无 deep_dive 字段（旧版本脚本？）"+ 明确要求"彻底解决包括反爬". 真实数据揭示 Mac Mini Step 8-10 文件 mtime 卡在 5/4 14:00 (今天 8:00 run 没到达). ② **真根因 (非反爬)**: V37.9.27 movespeed_rsync_helper.sh line 116 `exit "$EXIT_CODE"` 透传 rsync 失败码. run_freight.sh line 9 `set -eo pipefail` 启用 errexit. V37.9.30 EPERM 100% 持平后, 每次 rsync_helper 都失败 exit 非 0 → 20 个 set -e caller 每天被杀 mid-script (freight Step 8-10 / kb_dream Reduce / kb_evening / kb_inject 等等). last_run.json 在 Step 5 写了 status=ok+sent=true 但**无 deep_dive 字段**, preflight 误报 "missing 旧脚本". ③ **MR-4 silent-failure 第 24 次演出新形态**: 前 23 次都是错误处理失败, 第 24 次是**修复 (V37.9.27 retry helper) 引入更深的 silent failure** — 每天 ~20 caller step 8+ 全部静默丢失. V37.9.27 之前的 inline pattern `rsync ... 2>&1 \|\| echo WARN` 用 `\|\|` 保证 exit 0 给 caller, V37.9.27 helper 破坏了这个 invariant. ④ **F1 (核心修复) movespeed_rsync_helper.sh fail-open exit 0**: line 116 `exit "$EXIT_CODE"` → `exit 0` + 详细注释解释 set -e contract + V37.9.4-V37.9.26 invariant 恢复. fail-loud 仍通过 (1) stderr "WARN: SSD" 字面量 (INV-BACKUP-001) (2) JSONL forensic record (3) V37.9.26 watchdog 24h ≥5 alert. 影响 20 callers 的 set -e 安全 — 它们想要的是 best-effort backup 不杀脚本, 这才是 V37.9.4 之前的 invariant. ⑤ **F2 run_freight.sh 4 exit path 都写 deep_dive**: NEW_COUNT=0 → `skipped_no_news` / LLM 失败 exit 1 → `skipped_llm_failed` / 解析率<50% exit 2 → `skipped_parse_low` / Step 5 推送成功 → `pending` (Step 9 后续覆盖为 `ok`/`no_data`/`skipped`) / Step 5 推送失败 → `pending`. 永不再有 missing 字段. ⑥ **F3 preflight_check.sh 容忍 V37.9.31 新 status**: skipped_no_news → pass / skipped_llm_failed → warn (含告警链路) / skipped_parse_low → warn / pending → warn (含 V37.9.31 上下文指向 set -e 修复) / missing → 升级为 fail (V37.9.31 后是真异常). ⑦ **F4 importyeti_scraper.py 反爬升级 (用户选 playwright-stealth 专业库方案)**: USER_AGENTS 4 modern Chrome (130/131 macOS+Windows+Edge 跨平台轮换) / VIEWPORTS 5 真实尺寸随机化 (1280-1920 × 720-1080) / EXTRA_HEADERS 完整 sec-ch-ua / sec-fetch-* / accept-language q-value / Cloudflare 检测 _detect_cloudflare_challenge() 纯函数 (Just a moment / challenge / cf-chl / verifying you are human / block page <500 chars 启发) / 重试 backoff [30,60,120]s 替代 V3 fixed 10s / 公司间 delay 5-12s 随机替代 V3 fixed 3s / playwright-stealth 50+ patches 自动 fallback 到 manual STEALTH_JS (扩展 hardwareConcurrency/deviceMemory/platform/chrome[runtime,loadTimes,csi]). Mac Mini 需 `pip3 install playwright-stealth` (用户已确认). IMPORTYETI_TEST_MODE=1 跳过 sleep 用于确定性测试. ⑧ **F5 53 新单测**: TestRsyncAllRetriesFail 重写 (test_all_fail_emits_warn_ssd_and_exits_zero_v37_9_31 + test_caller_with_set_e_survives_rsync_failure_v37_9_31 真 subprocess 跑 set -e caller + helper 验证 POST_RSYNC_REACHED 字面量) / test_freight_schema_v9_31.py 13 单测 2 类 (TestFreightStatusFileSchemaV9_31 8 + TestPreflightStatusToleranceV9_31 5) / test_importyeti_scraper_anti_crawl.py 39 单测 9 类 (TestUserAgentRotation 4 + TestViewportRandomization 4 + TestSecChUaHeaders 5 + TestCloudflareDetection 8 + TestCloudflareBackoff 4 + TestInterCompanyDelay 3 + TestTestModeBehavior 2 + TestEmptyResultHelper 1 + TestFormatResult 2 + TestSourceLevelGuards 6). 全部禁止 V3 反模式字面量 (3s sleep / 10s backoff / fixed 1280x720). full_regression.sh +2 suites 接入. ⑨ **governance INV-RETRY-001 扩展**: declaration 段加 V37.9.31 fail-open contract 修正叙事 (引用 V37.9.30 EPERM 100% 持平证据) + 2 新 declaration check (`V37.9.31: fail-open exit 0` + `set -e contract` 字面量). MR-4 演出史更新到 24 次. ⑩ **累计**: 52→54 suites (+2) / 1937→1990 tests (+53) / 0 fail / governance 504→506 checks (+2 V37.9.31 fail-open + set -e contract) / 17 meta rules 不变 / 安全 95/100 / VERSION 0.37.9.30 → 0.37.9.31. ⑪ **下次 (V37.9.32+) 候选**: (a) **主硬卡**: Mac Mini `pip3 install playwright-stealth` 安装 + 等明日 8:00 freight cron 真跑观察 stealth 效果 + 检查 last_run.json 是否含 `"deep_dive":"ok"` (V37.9.31 全链路真闭环) (b) 同时观察 V37.9.30 24-48h ACL/lsof/snapshot 真实数据回归 (c) 5/11 决策窗口三 spec 同步切关 dry-run (V37.9.23/24/25 累积观察期到达). ⑫ **元教训**: V37.9.31 是 **MR-4 第 24 次演出** — 修复引入更深 silent failure. 与 V37.9.27 同源 (修复假设错), 但 V37.9.27 是"修非主因" (UID 错位是真 bug 但非 EPERM 主因), V37.9.31 是"修复破坏 invariant" (helper exit 行为改变破坏 caller 的 set -e 假设). 数据驱动诊断方法论第 7 步: 每次"修复成功"必须验证**所有 caller 行为契约**未被破坏. 原则 #28 第三问"最小修复方案"反向兑现 = 不要让修复成为下个 silent failure 的源头.|
| V37.9.30 | 2026-05-07 | **V37.9.29 假说部分证伪 → 取证扩展第 9-11 维度 (ACL/xattr + lsof handles + TM snapshots) + 35 新单测** — ① **触发**: 5/7 早 8:00 HKT 24h 验证窗口截止, Mac Mini `incident_analyzer --window 24h` 输出: 21 incidents (>15 R1 阈值) / **19/21 records 显示 ownership=501:20 bisdom:staff** (chown 真生效) / EPERM **100% 持平 V37.9.28** / 5 daemon (Spotlight/backupd/fseventsd/mds/mds_stores) **100% 共现**. **关键解读**: V37.9.29 path D' chown 修复了真存在的 UID 错位 bug (V37.9.4 引入, 60 天潜伏), 但 **UID 错位不是 EPERM 的根因 (或不是唯一根因)** — chown 后 EPERM 完全不降. ② **决策**: 用户选 Path C (不回滚, 进入新诊断) 而非严格 R1 — 理由 (a) chown 修了真 bug 没引入副作用 (rsync 67MB 0 EPERM 验证通过) (b) 历史数据证明 noowners + disableOwnership 不会让 EPERM 减少 (V37.9.4-V37.9.28 都是 ~19/24h) (c) R1 会失去 V37.9.29 b ownership 诊断字段. **预定 R1 规则原意是"修复有副作用让情况变糟立即恢复"**, 实际是"修复无副作用 + 没解决主问题" — 严格执行 R1 是僵化, Path C 更符合规则原意. ③ **3 类新取证字段 capture.sh +30 行**: (1) `acl_top` / `acl_kb` 用 `ls -le@` 抓 macOS ACL + xattr (chown 改 owner 但 ACL/xattr 留下, 这是 chown 不能清的强阻塞模式) (2) `lsof` 用 `lsof /Volumes/MOVESPEED 2>/dev/null \| head -50` 抓开放文件句柄 (head -50 双重保护: 防 lsof macOS hang + 限输出体积) (3) `snapshots` 用 `tmutil listlocalsnapshots / 2>/dev/null \| head -20` 抓 TM 本地快照 (即使 TM exclude 也可能有快照锁 metadata). 全部 best-effort, 缺工具 / 非 macOS = 空字段, 永不阻塞 caller. ④ **3 个新分类纯函数 analyzer.py +90 行**: `classify_acl_anomaly(acl_str)` 优先级排序 acl_deny > acl_present > xattr_only > normal > empty (基于 macOS `ls -le@` 输出格式: `0: group:everyone deny ...` / 制表符前缀的 xattr 行 / 普通 ls -l 行) / `classify_handle_holders(lsof_str)` 区分 daemon_dominated / user_only / mixed / empty (LSOF_DAEMON_KEYWORDS 含 mds_stores/backupd/fseventsd/Spotlight/mds/mdworker 注意顺序: 长字符串先 防 mds 子串误命中 mds_stores) / `classify_snapshot_count(snap_str)` 三档桶 snap_0 / snap_1_5 / snap_6_plus / empty (基于 `com.apple.TimeMachine.YYYY-MM-DD-HHMMSS.local` 字面量行计数). ⑤ **3 个新 analyzer 维度** (8→11): `by_acl_anomaly` 取 top+kb 强信号 (priority 字典选 max) + `by_handle_pattern` + `by_snapshot_bucket`. ⑥ **3 个新决策提示分支** (text 报告): (a) **🛡️ ACL deny 警告** 当任一 incident 有 ACL deny → 提示 `sudo chmod -RN /Volumes/MOVESPEED` 清除所有 ACL (这是 V37.9.29 假说证伪后的强候选根因) (b) **📂 句柄持有警告** 当 daemon_dominated ≥3 且 > user_only → 验证 daemon contention 假说 + 提示 path B 调度避峰 (12:00 UTC = HKT 20:00 是峰值, 移开避峰时段) (c) **📸 Snapshot 警告** 当 snap_6_plus > 0 → 提示 `sudo tmutil deletelocalsnapshots /` + `sudo tmutil disablelocal`. ⑦ **35 新单测** test_movespeed_incident_analyzer.py 5 类 (镜像 V37.9.29 b TestV37929B* 模式): TestV37930ClassifyAclAnomaly(7 含 empty/whitespace/非 string 健壮性 / 显式 deny / xattr only / normal / acl_present 无 deny) + TestV37930ClassifyHandleHolders(6 含 empty / 仅 header 行不计 / 仅 daemon / 仅 user / mixed / mds 子串不误判 md5sum) + TestV37930ClassifySnapshotCount(6 含 empty / 0 / 1 / 5 boundary / 6 boundary / 非 TM 行忽略) + TestV37930ForensicAnalysisIntegration(10 含 backward compat 缺字段 → empty 桶 / acl_deny 计数 / daemon_dominated 计数 / snap_6_plus 计数 / 文本报告含 3 新段 + V37.9.30 marker / ACL deny 警告 fire + chmod -RN 字面量 / daemon dominated 阈值 ≥3 fire path B / 阈值不达不 fire / snapshot 警告 fire + deletelocalsnapshots 字面量 / 全 clean 不 fire 任何警告) + TestV37930CaptureScriptIntegrity(6 含 4 capture 字面量守卫 + lsof head -50 守卫 + rec_dict 4 字段 + V37.9.30 marker + partially falsified attribution). _make_record helper 加 4 新可选 kwargs (acl_top/acl_kb/lsof/snapshots) 保持向后兼容. ⑧ **V37.9.29 假说被证伪后的新假说候选** (待 24-48h 真实数据验证): (a) **ACL deny 假说** — chown 不清 ACL 是 macOS 机制硬规则, 是最合理候选 (b) **APFS 内部锁/快照假说** — exfat→APFS 转换可能未稳定, 内部 metadata 锁 (c) **kernel ACL/xattr ACL 假说** — chown 改 owner 但内核 ACL 仍按旧规则做检查. **关键**: 让 24-48h 的新数据告诉我们哪个假说成立, 不再凭推测改代码 (原则 #28 兑现 + V37.9.27 retry 实战破证为 F2 数据驱动诊断 同款方法论). ⑨ **MR-4 silent-failure 第 23 次演出新形态**: V37.9.29 是"修复确实生效但修错了 bug"——前 22 次 silent 都是"错误被吞 / 错误被稀释 / 错误推送编造", 第 23 次是 **"修复成功 + 验证生效 + 但目标问题没改善"** — 完全合规的修复路径 + 数据自我证伪. 这正是 V3 路标 framework "数据从不撒谎"的硬实证场景. ⑩ **累计**: 52 suites 不变 (test_movespeed_incident_analyzer.py 内继续扩展) / 1902→1937 tests (+35) / 0 fail / governance 69 inv / 423 checks 不变 (V37.9.30 选择测试层守卫优先, 镜像 V37.9.29 b 节奏, 候选 INV-FORENSIC-001 待 24-48h 真实数据后再考虑) / 17 meta rules / 安全 95/100 / VERSION 0.37.9.29 → 0.37.9.30. ⑪ **下次 (V37.9.31+) 候选**: (a) **主硬卡**: 24-48h 后用户跑 `python3 ~/movespeed_incident_analyzer.py --window 24h` 把 V37.9.30 新维度数据贴回, 基于真实 ACL/lsof/snapshot 模式做精确根因判定 (b) 若 ACL deny 警告 fire → 立 INV-ACL-001 + 加 chmod -RN 修复路径文档 (c) 若 daemon_dominated 主导 → 实施 path B 调度避峰 (d) 5/11 决策窗口三 spec 同步切关 dry-run 真激活 machine_sync (V37.9.23/V37.9.24/V37.9.25 累积观察期到达). ⑫ **元价值**: V37.9.30 是 V37.9.x 系列"数据驱动诊断"方法论的进一步加固 — V37.9.14 取证 → V37.9.26 主动告警 → V37.9.27 主动修复 → V37.9.28 数据驱动诊断工具 → V37.9.29 ownership 维度 → V37.9.30 ACL/handle/snapshot 维度. **每次假说被证伪都不靠盲改代码而靠扩取证维度**, 这是与"看到失败就改代码"反模式的本质区别. 原则 #28 (理解再动手) 第三问 "最小修复方案" 兑现到 framework 层级.|
| V37.9.29 | 2026-05-06 | **MOVESPEED EPERM 60 天 silent failure 真因终于触底 — noowners + UID 错位 (path D' chown + enableOwnership + remount 闭环) + V37.9.29 (a) incident_capture mount 截断 bug + 24h 验证窗口至 5/7 早 8:00** — ① **触发**: V37.9.28 收工时承诺 5/6 24h 数据决定 EPERM 真因。开工跑 `incident_analyzer --window 24h` 揭示 macOS 修复 (Spotlight off + TM exclude) **完全无效** — 24h 仍 21 incidents (vs 之前 ~19/24h), 100% EPERM (Operation not permitted, errno 1), 100% 5 daemon 共现 (mds_stores/backupd/mds/Spotlight/fseventsd), 23:30 兜底 cron 也 EPERM 失败。**Spotlight 真 disabled** (mdutil -s) **TM 真 excluded** (tmutil), 但 daemon 进程仍跑 → 排除 V37.9.28 假说。② **Path A 4 轮诊断锁定真凶**: (a) 第 1 轮 mdutil/tmutil 状态 (排除"修复未生效") (b) 第 2 轮 diskutil info APFS Read-Write + tmutil snapshots 空 + **mount flag 含 noowners** ⚠️ (c) 第 3 轮 probe_top_err = "Operation not permitted" errno 1 (排除 EAGAIN/EACCES, 锁定 ACL 拒绝) (d) 第 4 轮 stat -f %Su:%Sg → **/Volumes/MOVESPEED = root:wheel (0:0) / KB = _unknown:_unknown (99:99)**, bisdom 是 UID 501 → **完全错位**！noowners flag 让所有 file 显示成 bisdom 完全掩盖 UID 错位 (8 天潜伏期), macOS daemon (mds_stores UID 89, backupd UID 200) metadata access 触发跨 UID ACL 拒绝 → 内核 inode lock → bisdom cron 同时段 rsync 命中 EPERM。③ **Path D' 第一次错序 (第 6 次诊断错误)**: 顺序 chown(noowners 模式) → enableOwnership → mount owners → ❌ touch test "Permission denied" — bisdom 完全失访 ~10 分钟。**根因**: macOS APFS 隐含规则 — **noowners 模式下 sudo chown 被静默吃掉** (教科书 "sudo bypass" 假设错), chown 没改 UID 但 ownership 被启用让 mount 走 owners path, bisdom UID 501 ≠ root 0 / UID 99 → 立即失访。④ **R2 抢救 (正确顺序)**: 现在 ownership 已启用, 重跑 chown → 真生效, UID 全变 bisdom:staff (501:20), touch test 成功, rsync 真运行 67MB sent 0 EPERM ✅。**关键发现**: macOS APFS chown 操作顺序硬规则 = enableOwnership → mount owners → chown (反过来 chown 在 noowners 模式下 silent no-op)。⑤ **V37.9.29 (a) incident_capture mount 字段截断 bug 修复 (parallel 独立工作)**: 昨日 V37.9.28 发现 incident_analyzer 报 21/21 "other_or_unmounted" 实是 read_file 400 char 截断 — `grep -i -e MOVESPEED -e Volumes` 同时匹配 /System/Volumes/Data 等无关行撑爆 400 char 窗口。修复: line 63 `grep -i MOVESPEED` (移除 `-e Volumes` 通用上下文) + line 120 limit 400→800 defense-in-depth。**4 新单测** TestV37929MountFieldFix 3 source-level + 1 fake-mount behavior。analyzer.py 不动 (其逻辑正确, bug 只在 capture 端)。30/30 test_movespeed_incident_capture.py 通过。Mac Mini 部署后未来 incident 数据准确, 不再被截断系统性误读为 unmount (V37.9.28 4 次诊断错误链第 4 环根除)。⑥ **完整案例文档** `ontology/docs/cases/movespeed_noowners_uid_mismatch_case.md` (~520 行): 四维度因果链架构图 (60 天潜伏 → 7 轮诊断 → V37.9.29 闭环) + 三层根因 (触发器 V37.9.4 APFS 默认 noowners / 放大器 root + UID 99 ownership 错位 / 掩护者 macOS daemon ACL 拒绝) + 时间线还原表 + 6 条件组合分析 + chown 操作顺序 macOS APFS 隐含规则记录 + MR-4 第 22 次演出 (架构假设级 silent failure 60 天) + 5 条元教训。⑦ **可证伪 24h 验证窗口至 2026-05-07 早 8:00 HKT**: incidents < 5 = ✅ 假说正确 立 INV-OWNERSHIP-001 / 5-10 = ⚠️ 部分缓解观察一周 / 15+ = ❌ 立即 R1 回滚 (`sudo mount -u -o noowners + sudo diskutil disableOwnership`) + 进 path B 调度避峰。回滚保障 `/tmp/movespeed_before_v37_9_29d.txt` 已备份原始 diskutil info。⑧ **元教训 (一日内 6 次诊断错误)**: V37.9.28 错误 #1-#4 + V37.9.29 错误 #5 (chown 顺序假设) + #6 (sudo chown noowners 假设) — 每次都因为"凭推测脱离数据"。终结诊断错误的是 stat -f 直接看 UID 数据。MR-10 understand-before-fix 反向兑现 6 次. **真正修复需要数据驱动质疑假设而不是修单点 bug**: V37.9.4 整团队基于"APFS 重建解决 EPERM"假设运作 60 天, 比单个错误更难发现。这是 V3 路标 framework "数据从不撒谎"的硬实证 — V37.9.14 INV-INCIDENT-001 取证 + V37.9.26 INV-INCIDENT-CONSUMPTION-001 主动告警 + V37.9.27 INV-RETRY-001 retry helper + V37.9.29 数据驱动诊断 = 完整自我修正闭环。⑨ **V37.9.29 (b) incident_capture 加 ownership 字段（同日加固，让明日 24h 验证数据自带 UID 维度）**: capture.sh 加 `stat -f "%u:%g" /Volumes/MOVESPEED` + `/KB` 两个真实 UID:GID 收集（不受 noowners 显示掩码影响，能直接暴露 ownership 错位）+ Python rec dict 加 `ownership_top` / `ownership_kb` 两字段 (limit 50 chars 各)。analyzer.py `analyze()` 加第 8 维度 `by_ownership` (`ownership_pair_counter` 统计 `top=X kb=Y` pair 分布，向后兼容旧 records 归入 `empty (pre-V37.9.29(b) records)` 桶) + `format_text_report()` 加 🔐 Ownership 分布段 + 决策提示新增 ownership misalignment 警告（检测到 `top=0:0` / `kb=0:0` / `top=99:99` / `kb=99:99` 任一 → 提示"V37.9.29 path D' 修复后理应仅看 cron user，仍有此 pattern 立即 R1 回滚 + 具体命令"）。**14 新单测** TestV37929BOwnershipAnalysis(10) + TestV37929BCaptureScriptIntegrity(4)：包括 backward compat (pre-V37.9.29 b records → empty bucket) / 错位 pair 计数 (top=0:0 kb=99:99) / 一致 pair 计数 (top=501:20 kb=501:20) / partial field handling / non-string field 健壮性 / 8th dimension display / 警告触发场景 / 仅 kb 错位也警告 / capture.sh source-level guards。**意义**: 让未来类似 ownership 错位问题在 1 天内发现而不是 60 天 (V37.9.4→V37.9.29 silent failure 长度)，这是 V3 路标 framework "数据从不撒谎"在诊断粒度的进一步加固。⑩ **累计**: 52 suites 不变 / 1884→1902 tests (+18, V37.9.29 (a) 4 + V37.9.29 (b) 14) / 0 fail / governance 69 invariants 不变 (INV-OWNERSHIP-001 等 24h 验证后立) / 423 checks 不变 / 17 meta rules / 安全 95/100 / VERSION 0.37.9.28 → 0.37.9.29。⑪ **下次 (V37.9.30+) 候选**: (a) **主硬卡**: 5/7 早 8:00 24h 验证窗口跑 incident_analyzer (现已含 V37.9.29 b 第 8 维度 ownership 真实 UID:GID 显示) 决定 INV-OWNERSHIP-001 是否立案或 R1 回滚 (b) MR-18 候选 `mount-flag-must-not-mask-actual-ownership` 立元规则 (c) 立场文章第 6 篇 *"我以为闭环了" — 60 天 silent failure 的架构假设级教训* (V37.9.4 → V37.9.26 → V37.9.27 → V37.9.28 → V37.9.29 完整时间线案例素材已齐) (d) 5/11 决策窗口三 spec 同步切关 dry-run 真激活 machine_sync。|
| V37.9.28 | 2026-05-05 | **周一用户视角观察 (周二补做) 5 finding 单日全闭环 + V37.9.27 helper 实战破证为 F2 数据驱动诊断 + V37.9.22 Tripwire 框架首次复评** — ① **触发**：原则 #32 周一用户视角观察制度 (V37.8.11 教训立的元规则)，今日 (周二) 补做 30min。用户体感 3/5 分，单条 watchdog WhatsApp 告警截图同时露出 5 类问题。同时执行 V37.9.22 Tripwire 框架首次"复评"实践——上游 v2026.4.26→v2026.5.3-1 一周 5 个新 stable + 主版本号跳 5.x。② **Tripwire 0/6 触发自动 hold + 实证 WebFetch 关键发现**：(a) #73358 codex/gpt-5.5 silent default V37.9.22 三次评估硬阻塞 dealbreaker **已在 v2026.4.27 release notes 显式修复** (`require explicit skills.entries.coding-agent.enabled`) — V37.9.22 hold 主因解除 (b) v2026.4.29 新增 `tools.exec`/`tools.fs` restrictive profile 收紧 (中风险) (c) v2026.5.2 强制 plugin manifest `contracts.tools` ownership (中风险) (d) #59265 仍无 verified fix (e) 5.x 是日历版本号 (年.月.patch) **非** semver major bump。**战略局面变化**: 4/29 hold indefinitely → 5/5 hold tactically (路径已开)。**推荐方案 C**: 等 4-8 周 (~2026-06-15) 让 5.x 沉淀后阶梯升 v2026.4.27。文档刷新 5 文件 (eval doc 第十三节 + 13.9 时间表 / check_upgrade.sh LAST_EVAL_DATE 4/29→5/5 / config.md / SOUL.md / status.json)。③ **5 finding 单日修复**：**F1** [V37.9.28 commit 4aa9aa0] job_watchdog.sh `scan_logs()` 告警子条目暴露错误时间戳分布 — 旧只显示 "12条错误 → last_err"，新加 `(最早 X, 最新 Y)` / `(@ X)` / `(时间戳缺失)` 三档格式。最小修复 = 仅扩 ALERTS+= 拼接，不动 awk filter (V37.9.6 INV-WATCHDOG-FRESHNESS-001 互补)。9 单测 (TestF1AlertFormatTimestampRange 5 含血案场景 + TestF1AlertFormatShellGuards 4 源码守卫)。**F2** [commit 7d19abe] V37.9.27 helper 实战破证 (24h 仍 20 次, 错误模式 EPERM→EOF, MR-4 silent-failure 第 19 次演出新形态)。按原则 #28 第三问"最小修复方案"目前未知 → 构建数据驱动诊断工具 `movespeed_incident_analyzer.py` (~330 行, 7 维度: 时间覆盖/caller/exit_code/probe 矩阵分类/并发进程/小时分布/mount 状态 + 决策提示自动生成)。34 单测 9 类 + auto_deploy FILE_MAP + full_regression 集成。让 Mac Mini 端 JSONL 数据决定 helper 修复策略 (e.g. EOF 不重试 / 检测 tmutil status / 增长 backoff)，**不再凭推测**。**F3** [commit 0666885] HN watchdog 阈值 7h→14h 对齐 schedule (V37.8.7 ACL 同款 schedule-vs-threshold drift)。注释错写"每3小时→7h"实际 jobs_registry.yaml 是 `45 8,14,20 * * *` 每天 3 次最大 gap 12h，7h 阈值导致 overnight 必报警。改 25200→50400 (12h max gap + 2h slack)，与 freight_watcher 同款 8/14/20 schedule 对齐。1 单测守卫。**F4** [commit e21813e] slo_checker.py format_alert direction 显式声明 (修 3/5 metric 显示反向)。旧 line 120-122 三层 if-else 推断 direction 互相矛盾: latency_p95 显示 ">" 应 "<" / tool_success_rate 显示 "<" 应 ">" / auto_recovery_rate 显示 "<" 应 ">"。新方案: 每个 metric 在 check_slo 创建 result dict 时显式写 `"direction": "<" or ">"` 字段, format_alert 直接读 `v["direction"]` 不再推断。17 单测 3 类 (TestDirectionField 6 metric 方向 / TestFormatAlertUsesDirection 7 含血案场景反向断言 + 向后兼容缺字段 / TestSourceLevelGuards 4 旧推断 pattern 禁止)。**F5** [本提交] F1 修复后自动解决 — 用户登记的"ai_leaders 4 天前 429 仍显示"是 F1 symptom: F1 grep `[YYYY-MM-DD HH:MM]` 正确提取 ai_leaders 时间戳，alert 现显示 `(最早 2026-05-04 09:00, 最新 ...)` 让用户立刻判断是同时段突发还是持续。1 集成测试 `test_f5_ai_leaders_format_user_5_5_observation` 锁定 ai_leaders 实际格式被 F1 grep 正确识别。④ **因果链发现**: F1 (timestamp 显示) + F2 (V37.9.27 破证) **互锁** — 不修 F1 看不见 F2 真相。先修 F1 让用户能看清错误时间分布，才能基于证据决定 F2 helper 修复策略。⑤ **元价值**: 周一用户视角观察发现"系统监控产出降级到无法判断真伪"，比单点修复重要。原则 #32 兑现 — 单测/治理全绿但用户实际使用才暴露 UX 缺口。MR-7 (用户视角观察→24h 闭环) 第 4 次正向兑现 (V37.9.16 → V37.9.17 → V37.9.21 → V37.9.28)。⑥ **下次 V37.9.29+ 候选**: (a) **主硬卡**: Mac Mini 同步后用户跑 `python3 ~/movespeed_incident_analyzer.py --window 24h` 把输出贴回，基于真实 incident 模式做 F2 helper 修复 (e.g. EOF 不重试 / 检测 tmutil status / 增长 backoff) (b) F4 follow-up: Mac Mini 真实 proxy_stats.json p95 是否真 54s 严重超阈值，还是 stale data 调查 (c) 5/11 决策窗口三 spec 同步切关 dry-run 真激活 machine_sync (V37.9.23/V37.9.24 jobs_to_crontab + kb_sources_to_index + V37.9.25 services_to_launchd) (d) 立场文章第 6 篇候选: *"我以为闭环了" — 60 天 silent failure 的架构假设级教训* (V37.9.4 → V37.9.26 → V37.9.27 → V37.9.28 时间线全栈)。⑦ **累计**: 50→52 suites (+2) / 1822→1883 tests (+61, F1 9 + F2 34 + F3 1 + F4 17 + F5 1, 部分单测含 source-level 守卫) / 0 fail / governance 69 invariants 不变 (本次无新 INV, 候选 INV-WATCHDOG-THRESHOLD-001 守卫 schedule-vs-threshold 一致性留 V37.9.29+) / 423 checks 不变 / 17 meta rules / 安全 95/100 / VERSION 0.37.9.27 → 0.37.9.28。⑧ **MR-7 用户视角→24h 闭环正向兑现** (第 4 次)：原则 #32 周一观察制度作为元规则建立的工程价值首次实证 — 30min 用户观察发现 5 类问题同日修 4 个根因 + 1 个 F1-symptom 自愈。这是 framework "用户感知 = 一等可观测信号" 的硬证据。|
| V37.9.27 | 2026-05-04 | **B 问题真正修复 — movespeed_rsync_helper.sh (jitter+retry+fail-loud+capture wrapper) 替代 20 个 inline rsync site / V37.9.26 主动告警立即抓真问题 / V37.9.4 闭环假设失败的 60 天 silent failure 终结** — ① **触发**：V37.9.26 收工后 user 跑 `python3 movespeed_incident_monitor.py` 立即抓到 **24h 19 条 incident** (远超阈值 5)，5 个生产 cron 受影响 (run_hn_fixed/run_freight/run_discussions/run_arxiv/run_rss_blogs). 深度审计 incidents.jsonl 历史数据 (212 条总量, 分析时间分布) 揭示**架构假设级 silent failure**: V37.9.4 APFS 重建只解决文件系统层 fskit EPERM, 但 OS 调度层 SSD I/O 竞争仍每天 ~19 次 transient 失败影响 11 个 KB cron jobs (60 天潜伏期, MR-4 第 18 次演出新形态). 错误时段集中: 00-06 (8 条/8h) + 12 + 19 整点尖峰 = macOS 日级维护 (Time Machine / Spotlight / mds_stores) 抢占 SSD I/O 总线. ② **三层主动修复设计**: Phase 1 错峰 30-180s 抖动 (避多 cron 同秒触发抢 SSD I/O 总线 — 避开整点尖峰) / Phase 2 retry 3 次 10s/20s 指数退避 (transient EPERM 通常 30s 内自愈) / Phase 3 全部失败才 fail-loud + 调 movespeed_incident_capture.sh (V37.9.4/V37.9.14 契约保留). **设计权衡**: 起步设计 5-15min jitter 太长导致 cron 推迟严重, 改为 30-180s 错开多 cron 同秒触发同时保持 cron 漂移可接受. 预期 incident 从 ~19/24h 降到 <1/24h (95%+ 减少). ③ **新文件 movespeed_rsync_helper.sh** (~115 行, 6 行接口): 接口 `bash $HOME/movespeed_rsync_helper.sh <caller> -- <rsync args>` (用 `--` 分隔避免与 rsync args 混淆). 4 个 env 覆盖 (NO_SLEEP / NO_RETRY / MAX_ATTEMPTS / BACKOFF_BASE) 让单测可确定性. 防御 clamp MAX_ATTEMPTS 到 [1, 10] 避免 env typo 导致 runaway loop. MR-11 兑现: 所有 diagnostics 写 stderr (避 `$(...)` 命令替换污染). ④ **批量替换 20 个 site** (Python regex automation): 18 个 site 自动替换成功 (含 freight 2 处, 共 19 处) + kb_save_arxiv.sh 字面量 caller `[kb_save_arxiv]` 模式不同手动替换 1 处. 旧模式 `rsync ... 2>&1 || { _rc=$?; echo "WARN..."; capture; }` (1 长行) → 新模式 `bash "$HOME/movespeed_rsync_helper.sh" "$0" -- <args>` (1 清爽行). 验证: 旧模式残留 0 个 (`grep` 无输出), 新 helper 调用 20 个 (1:1 替换). ⑤ **24 新单测** test_movespeed_rsync_helper.py 7 测试类: TestUsageErrors(3 含缺参/缺 -- 分隔符 → exit 2) + TestEnvOverrides(5 含 NO_SLEEP / NO_RETRY 严格 1 次 / MAX_ATTEMPTS=2 严格 2 次 / 999 clamp 到 10 / 非数字 fallback 3) + TestRsyncSuccess(1 第一次成功 0 retry) + TestRsyncRetryRecovers(2 第 2/3 次恢复, 含 'recovered on attempt N' 字面量) + TestRsyncAllRetriesFail(1 含 WARN: SSD + exit 23 propagation) + TestCaptureHelperWired(1 端到端 mock SCRIPT_DIR + capture stub + 验证 marker 写入) + TestSitesMigrationGuard(2 全部 20 site 用 helper + 0 legacy pattern 残留) + TestSourceLevelGuards(9 helper 字面量守卫: V37.9.27 标记 / jitter 30-180s 公式 / MAX_ATTEMPTS=3 / BACKOFF_BASE=10 / clamp 10 / WARN: SSD / capture / stderr 写入 / 可执行权限). **fake rsync 注入策略**: tempfile + PATH 注入 fake bash script 模拟各种 rsync 行为 (always success / always fail / fail-then-success / count attempts), 实证 helper 内部行为不依赖真实 rsync 是否安装. ⑥ **INV-RETRY-001 新增** (meta_rule=MR-4, severity=high, verification_layer=[declaration, runtime], **10 checks**): 8 declaration helper 字面量 + auto_deploy FILE_MAP 部署 + 1 runtime python_assert 跑 24 单测端到端. 与 INV-BACKUP-001 + INV-INCIDENT-001 形成**三阶段防御链**: 失败时取证 (V37.9.4 fail-loud) → 累积时告警 (V37.9.26 watchdog 主动) → 主动修复减少失败 (V37.9.27 jitter+retry). 三 INV 一起覆盖 detection → forensic → alert → recovery 完整链路. ⑦ **INV-BACKUP-001 检测器扩展豁免** (V37.9.27 兼容): 含 `movespeed_rsync_helper.sh` 字面量的 site 自动豁免 `WARN: SSD` 直接字面量检查 + `incident_capture` 直接调用检查 (MR-8 单一真理源: site 通过 helper 间接获得 capture wiring 视为合规). 同步更新 governance yaml 检测器 + test_movespeed_incident_capture.py 单测 + test_kb_deep_dive.py 单测. **不破坏 V37.9.4 检测目的** (生产 rsync 站点必须有 capture wiring), 只改"如何获得 wiring"的接受形式 (直接 OR 通过 helper). ⑧ **MR-4 silent-failure 第 18 次演出新形态**: 前 17 次都在代码层 (单点 silent), V37.9.27 是**架构假设级** silent failure — V37.9.4 整个团队基于"B 问题闭环"假设继续 60 天, 直到 V37.9.26 watchdog 主动告警识破. **原则 #28 反向兑现**: 修复前必答三问"这问题之前存在吗" — 之前**以为**不存在, 实际**一直存在** (V37.9.14 数据从不撒谎). 跳过这一步 = 半年时间被浪费. ⑨ **生产 governance audit 输出对比** (V37.9.26 → V37.9.27): V37.9.26 (1 跑就抓): `19|1|run_github_trending.sh/run_freight.sh/run_hn_fixed.sh/run_rss_blogs.sh/run_arxiv.sh` (24h 19 条触发阈值). V37.9.27 修复后预期一周观察: `<1|0|` (95%+ 减少). 真实修复效果待 5/11 决策窗口前一周 Mac Mini 数据验证. ⑩ **累计**: 49→50 suites (+1) / 1798→1822 tests (+24, V37.9.27 24 单测) / 0 fail / governance 68→69 invariants (+1 INV-RETRY-001) / 413→423 checks (+10) / 17 meta rules / 安全 95/100 / VERSION 0.37.9.26 → 0.37.9.27. ⑪ **下次 V37.9.28+ 候选**: (a) **主硬卡**: 5/11 决策窗口三 spec 切关 dry-run 真激活 machine_sync (jobs_to_crontab + kb_sources_to_index + services_to_launchd). 同时观察 V37.9.27 retry 实战效果 (24h incident 数应 < 1). (b) 立场文章第 6 篇候选: *"我以为闭环了" — 60 天 silent failure 的架构假设级教训* (V37.9.4 → V37.9.26 → V37.9.27 时间线 + 监控发现真相 + 三阶段防御链, Stage 2 第 6 篇资产). (c) V37.9.15 候选剩余: kb_status_refresh.sh 写 24h incident 计数到 status.json. ⑫ **元价值**: V37.9.27 闭环 V37.9.26 立即抓出的真问题, **整个 V37.9.x 系列形成完整自我修正闭环**: V37.9.14 取证 → V37.9.26 主动告警 → V37.9.27 主动修复. 这不是"加监控"故事, 是 framework 自我治理的硬证据 — declared (helper 设计契约) → runtime (24h incident <1) 的 convergence framework 同款模式应用到运维层.|
| V37.9.26 | 2026-05-04 | **watchdog 主动告警升级 — 24h 窗口 ≥5 条 MOVESPEED incident 推 [SYSTEM_ALERT]（V37.9.15 候选闭环 watchdog 部分 + 从被动证据收集升级为主动告警链）** — ① **触发**：V37.9.14 已落地 20 处 rsync fail-loud + JSONL 取证, 但 incidents.jsonl 仅被动累积 (operator 不会主动 cat 看). V37.9.26 升级让 watchdog 每小时 cron 主动消费 incidents.jsonl, 24h 窗口 ≥5 条 → 推 [SYSTEM_ALERT] 含计数 + 最近 5 个 caller basename. 阈值 5 来自 V37.9.4 案例数据 (一周 18 次 = 24h 平均 ~3 次为噪声基线; ≥5 = 噪声 +2σ 异常爆发). ② **新文件 movespeed_incident_monitor.py** (~140 行纯 Python): `parse_iso_to_epoch(ts)` 时间戳解析 (Z suffix / 含 offset / naive UTC fallback) + `extract_caller_basename(path)` basename 提取 + `count_recent_incidents(file, now_epoch, window_seconds=86400) -> (count, callers, parse_errors)` 24h 窗口纯函数 + `format_watchdog_output(count, threshold, callers) -> "{count}|{hit}|{callers}"` pipe 分隔输出 + CLI `python3 movespeed_incident_monitor.py <file> <now_epoch> <threshold>`. **FAIL-OPEN 契约**: 文件 IO 失败 → 输出 `0|0|file_read_error` exit 0 让 watchdog 降级; 损坏 JSON 行 silent skip + parse_errors 计数; 缺 timestamp_iso 字段 silent skip. ③ **MR-8 单一真理源**: 不内嵌 watchdog heredoc, 抽到独立 Python 模块, 单测可直接 import 函数 (不需 subprocess 跑 watchdog), 防漂移. job_watchdog.sh 调 `python3 movespeed_incident_monitor.py "$INCIDENT_FILE" "$NOW_EPOCH" "$INCIDENT_24H_THRESHOLD"` 替代旧 heredoc 模式. ④ **job_watchdog.sh 第 9 维度集成**: 从 8 大维度扩展到 9 (新增 MOVESPEED incidents 累积监控). 双层文件存在性守卫 (`[ -f "$INCIDENT_FILE" ] && [ -f "$INCIDENT_MONITOR" ]`) 确保 dev 环境 / 部署不完整时静默通过. 阈值常量 `INCIDENT_24H_THRESHOLD=5` 明确定义. 触发时 `ALERTS+=("MOVESPEED 24h 内 N 次 rsync 失败...")` 推入 watchdog 标准告警链路 (统一 notify.sh 双通道推送). ⑤ **INV-INCIDENT-001 新增** (meta_rule=MR-4 silent-failure 反向变种延续 V37.9.6 的"过度报"修到 V37.9.26 "升级为主动报", severity=high, verification_layer=[declaration, runtime], **10 checks**): 4 monitor.py declaration (count_recent_incidents / parse_iso_to_epoch / extract_caller_basename / format_watchdog_output 函数定义) + 5 watchdog.sh declaration (调 monitor.py 不内嵌 / 阈值常量 5 / 双层文件存在性 / ALERTS 推送字面量) + 1 auto_deploy FILE_MAP 部署守卫 + 1 runtime python_assert subprocess 跑 36 单测端到端. **与 INV-BACKUP-001 是 sibling**: 前者守证据收集 (V37.9.4/14 fail-loud + JSONL), 后者守证据被消费产告警. 两 INV 一起覆盖 declared → runtime → alert 完整链路. ⑥ **36 新单测** test_movespeed_incident_monitor.py 7 测试类: TestParseIsoToEpoch(7 含 Z suffix / explicit offset / naive UTC / 等价性 / 空字符串 raise / 非字符串 raise / 损坏 raise) + TestExtractCallerBasename(5 含绝对路径 / 相对路径 / 空字符串→? / 嵌套路径 / trailing slash 边界) + TestCountRecentIncidents(7 含空文件 / 24h 窗口边界 1min before/after / 损坏 JSON 行不阻塞 / 缺 timestamp 字段 silent skip / 顶层非 object 计 parse_error / 空白行忽略) + TestFormatWatchdogOutput(4 含阈值命中/不命中 / 0 count 无 callers / caller 截断 5 个) + TestCliBehavior(5 端到端 subprocess 含 happy path / 阈值不命中 / 缺参 exit 2 / invalid epoch exit 2 / FAIL-OPEN file_read_error exit 0) + TestThresholdBoundary(3 反向验证: 恰好 5 触发 / 恰好 4 不触发 / 100 条不崩溃) + TestWatchdogIntegrationGuards(5 源码守卫: watchdog 调 monitor.py / 阈值 5 字面量 / 文件存在性双 guard / **反 inline heredoc 守卫** `parse_iso_to_epoch` 不在 watchdog / `window_24h_start = now_epoch - 86400` 历史 inline 痕迹清除 / monitor.py 文件存在). ⑦ **INV-BACKUP-001 检测器豁免 watchdog (V37.9.26 同款修复)**: V37.9.4 INV-BACKUP-001 检测器扫所有 .sh 含 "rsync" + "MOVESPEED" 字样视为 rsync 站点, V37.9.26 watchdog 加告警消息 "MOVESPEED 24h 内 N 次 rsync 失败" 引入字样导致误判. 按原则 #28 最小修复 = 给检测器加 "monitor consumer 豁免": (a) `test_movespeed_incident_capture.py::test_no_extra_undeclared_sites` 加 `if rel == "job_watchdog.sh": continue` (b) governance INV-BACKUP-001 yaml 内嵌检测器 2 处同款豁免 (WARN log 检查 + forensic capture 检查). 不破坏 V37.9.14 的检测目的 (生产 rsync 站点) 且语义清晰 (watchdog 是消费者不是生产者). ⑧ **auto_deploy.sh FILE_MAP 加 movespeed_incident_monitor.py** + **full_regression.sh 接入** test_movespeed_incident_monitor.py suite. ⑨ **累计**: 48→49 suites (+1) / 1762→1798 tests (+36, V37.9.26 36 单测) / 0 fail / governance 67→68 invariants (+1 INV-INCIDENT-001) / 403→413 checks (+10) / 17 meta rules / 安全 95/100 / VERSION 0.37.9.25 → 0.37.9.26. ⑩ **Mac Mini 部署后预期行为**: dev 环境无 incidents.jsonl → 整段 if [ -f ... ] 跳过, watchdog 8 大维度照常 (新增第 9 维度静默). 生产环境如有 incidents 累积: 24h 内 ≥5 条触发推送 "MOVESPEED 24h 内 N 次 rsync 失败 (≥5 阈值, B 问题连续性复发) callers: run_freight.sh/kb_dream.sh/...". 这是 MR-4 silent-failure 反向变种延续 — V37.9.6 修"过度报", V37.9.26 升级"被动报变主动报". ⑪ **MR-8 单一真理源正向兑现**: monitor.py 是独立纯函数模块, 单测可直接 `from movespeed_incident_monitor import count_recent_incidents` 测试, 不需要 subprocess 跑 watchdog. **5 个反 inline heredoc 守卫** (TestWatchdogIntegrationGuards) 防止未来 watchdog 内嵌副本 → 漂移 → bug. ⑫ **下次 V37.9.27+ 候选** (V37.9.15 候选剩余部分): kb_status_refresh.sh 也消费 movespeed_incidents.jsonl, 把 24h 计数写进 status.json 让三方共享状态可见. 工作量 ~30min, 不阻塞 5/11 决策窗口. 当前阶段重点仍是 5/11 三 spec 同步切关 dry-run.|
| V37.9.25 | 2026-05-04 | **第 5 个 convergence spec services_to_launchd — V37.9.13 候选 launchd plist 持久化方向闭环 + framework 第 5 次扩展验证** — ① **触发**: V37.9.13 status.json unfinished 长期登记 "Mac Mini 重装 / plist 误删后 V37.9.12.1 给 proxy plist 加的 ONTOLOGY_MODE=on env 会丢失" 候选, 等待数版后 V37.9.25 决定不直接守 plist 入仓 (敏感 env vars handling 是更大 deployment 工作), 而是守"声明的 service 是否真在 launchd 注册"上游事实层 — convergence framework 完美适配场景. ② **新文件 services_registry.yaml** (与 jobs_registry.yaml 平行的独立 declared source): 3 个 service 初始声明 (com.openclaw.adapter port 5001 / com.openclaw.proxy port 5002 / ai.openclaw.gateway port 18789) + label/port/plist/description 字段. **设计决策**: 不扩 jobs_registry.yaml schema 因 jobs (scheduled run-and-exit cron) 与 services (persistent supervised launchd) 是不同概念, 独立 source 让 schema 校验 + 文档 + 操作语义清晰分离, 也是 minimal-extension 路径第 5 次实证 (V37.9.20 named-dispatch 第 5 次承诺兑现). ③ **convergence.py 扩展** (零 framework 改动): 新增 `_extract_services_from_registry(spec)` 纯函数读 `data.get("services") or []` → 提取每个 entry 的 `label` 字段作 set[str] 返回 (skip 空 label 防御); 注册到 `_DECLARED_EXTRACTORS["services_from_registry"]` (5 个 extractor: registry_enabled_system_jobs / registry_kb_source_files / providers_from_registry / json_file_paths / services_from_registry). 复用 V37.9.19 `_observe_shell_command` (`launchctl list`) + V37.9.19 `_parse_line_contains_identifier` — 零新 observer + 零新 parser. ④ **convergence_ontology.yaml** spec `services_to_launchd`: declaration.source=services_registry.yaml + extractor=services_from_registry + identifier_field=label / runtime_observable.method=shell_command + command="launchctl list" + parser=line_contains_identifier / drift_action=alert_only (V37.9.19 同款保守路线) / convergence_method.planned=machine_sync_via_helper (V37.9.26+ 升级路径已设计: bash $HOME/restart.sh launchctl kickstart -k 模式 + per-service plist 解析). meta version 0.6-named-dispatch-apply-functions → 0.7-fifth-spec-services-to-launchd, related_invariants 升级到 5 个 INV. v37_9_25_changelog 段记录第 5 spec 上线 + V37.9.13 闭环理由. ⑤ **INV-CONVERGENCE-SERVICES-001** 新增 (meta_rule=MR-17, severity=high, verification_layer=[declaration, runtime], **16 checks**): 4 services_registry.yaml structural 守卫 (services 段 / 3 个 label 字面量) + 2 convergence.py declaration (extractor 注册 + 函数定义) + 8 convergence_ontology.yaml structural 守卫 (spec id / governance_invariant 引用 / extractor / observer / parser / meta version / related_invariants / changelog) + 1 governance_ontology.yaml MR-17 derivative_invariants 引用 + 1 runtime python_assert subprocess 跑 V37.9.25 3 测试类端到端. MR-17 derivative_invariants 终态 [CRON, PROVIDERS, OPENCLAW, KB, INTEGRATION, SERVICES]. ⑥ **23 新单测 3 类**: TestExtractServicesFromRegistry(4 含 真 yaml 解析 / set[str] 类型 / 空 label 跳过 / default source 指向 services_registry.yaml) + TestVerifyServicesToLaunchdIntegration(6 含 dev 不崩溃 / drift_action=alert_only / extractor 名 / command="launchctl list" / parser / dispatch table 注册) + TestServicesSpecSourceGuards(13 含 services_registry 文件存在 / 3 个 service / label 字段计数 / extractor 注册 / 函数定义 / 函数体读 services 段 + label 字段 / yaml spec 声明 / meta version 升级 / related_invariants 5 个 / v37_9_25_changelog / drift_action 块内 alert_only / command 字面量 / governance INV 引用). **反向验证已确认守卫真有效**: 故意 yaml label 改坏 (`com.openclaw.adapter → com.openclaw.adapter_RENAMED`) → `test_extracts_service_labels_from_registry` 立即失败, 还原后 23/23 全绿. ⑦ **auto_deploy.sh FILE_MAP 加 `services_registry.yaml|$HOME/services_registry.yaml`** 部署到 Mac Mini ~/. dev 环境跑通: `declared=3 (com.openclaw.adapter/proxy/gateway), observer_failed (launchctl 不存在), missing=3 alert_only` 输出. ⑧ **Mac Mini 部署后预期输出**: 如 launchd 已 load 3 个 service → `✅ [services_to_launchd] declared=3 observed=3 (no drift)` 第 5 个 spec zero drift baseline; 任一 service 漂移 (plist 误删 / bootstrap 失败) → `⚠️ [services_to_launchd] declared=3 observed=N missing=M (drift_action=alert_only)` 触发告警让 operator 决定是否手动修. ⑨ **累计**: 47→48 suites 不变 (test_convergence.py 内继续扩展类) / 1739→1762 tests (+23) / 0 fail / governance 66→67 invariants (+1 INV-CONVERGENCE-SERVICES-001) / 387→403 checks (+16) / 17 meta rules / 安全 95/100 / VERSION 0.37.9.24 → 0.37.9.25 / **convergence specs 4→5**. ⑩ **下次 (V37.9.26) 候选**: (a) **主硬卡**: services_to_launchd 一周观察期 (5/4 baseline → 5/11 决策窗口) 收集 declared vs observed 数据, 如零漂移升级 drift_action: alert_only → machine_sync + 注册 `_apply_services_launchctl_bootstrap` 到 `_APPLY_FUNCTIONS` dict (per-service launchctl bootstrap "gui/$(id -u)" "<plist_path>", 复用 V37.9.13 restart.sh kickstart -k 模式), Plan B 渐进 dry-run 安全网保留 (b) jobs_to_crontab + kb_sources_to_index 同款 5/11 决策窗口切关 dry-run 真激活 machine_sync (从 V37.9.23/V37.9.24 累积观察期到达) (c) 第 6 个 spec 候选 (X 监控账号声明 / launchd plist 文件 mtime 监控). ⑪ **元价值**: V37.9.25 是 V37.9.20 named-dispatch 设计承诺的**第 5 次实证** (V37.9.20 第 1 / V37.9.22 第 2+3 / V37.9.24 第 4 / V37.9.25 第 5) — 添加新 spec 类型零 framework 改动这条承诺已被 5 个独立 spec + 多种扩展粒度 (full 三件套 / mid-extension 抽 helper / minimal 1 件 / named-dispatch refactor / 独立 source minimal-extension) 实测验证, V3 路标 `pip install ontology-engine` 终极目标的扩展性论证已是硬证据非空头. 同时本 spec 闭环 V37.9.13 长期登记的 plist 持久化候选, 把候选项从 backlog 转为 production audit 主动消费.|
| V37.9.24 | 2026-05-04 | **三件套并行：kb_sources_to_index machine_sync 升级 (named-dispatch 重构) + openclaw_config_to_runtime mock 化加固 + 立场文章 Why Control Plane = Convergence Engine** — ① **任务 B (中优先级 - 主交付)**: kb_sources_to_index spec drift_action `alert_only → machine_sync` (Plan B 渐进 dry-run 同款 V37.9.23). 引入 named-dispatch 重构: 把 V37.9.23 的 `_apply_machine_sync` 重构为顶层 dispatcher, 按 spec yaml `convergence_method.apply_function` 字段路由, 镜像 V37.9.20 `_DECLARED_EXTRACTORS` / V37.9.13 `_CONTEXT_EVALUATORS` 同款模式. **新增 `_APPLY_FUNCTIONS` dict** = `{"jobs_to_crontab_per_entry": _apply_jobs_to_crontab_per_entry, "kb_embed_incremental": _apply_kb_embed_incremental}`. **新增 `_apply_kb_embed_incremental` (one-shot pattern)**: 单次 `python3 ~/openclaw-model-bridge/kb_embed.py` subprocess call 覆盖所有 missing sources (与 jobs_to_crontab 的 per-entry crontab_safe.sh add 模式不同 — kb_embed 启动加载 embedding 模型 ~3s 不应 amortize per-entry, 内部 mtime-diff 已是 batch 处理逻辑, V37.8 锁机制保证 idempotent). 新增 `_KB_EMBED_TIMEOUT_SEC=300` (5min, kb_embed LLM 调用密集). **闭环 V37.9.22 blocker**: V37.9.22 误以为 kb_embed.py 是全量重建, V37.9.24 实证默认无参 = 增量 (mtime diff + 只重 embed 变化文件), 不需新增 CLI args. **V37.9.23 jobs_to_crontab 路径迁移**: `_apply_machine_sync` 主体逻辑移到 `_apply_jobs_to_crontab_per_entry`, 顶层成为 dispatcher; legacy spec.id-based fallback 保 V37.9.23 spec 向后兼容(spec yaml 缺 apply_function 字段时按 spec.id="jobs_to_crontab" 路由). spec yaml 同步: jobs_to_crontab 加 `apply_function: jobs_to_crontab_per_entry` + apply_path `_apply_machine_sync → _apply_jobs_to_crontab_per_entry`; kb_sources_to_index `drift_action: machine_sync` + `apply_function: kb_embed_incremental` + `implemented: machine_sync_via_helper` + `dry_run_default: true`. yaml meta `version: 0.5-machine-sync-dry-run → 0.6-named-dispatch-apply-functions`. 新增 v37_9_24_changelog 段记录三件套. ② **任务 C (低优先级 - 加固)**: openclaw_config_to_runtime spec 同款 V37.9.22 hotfix 教训横向扩展. **新增 `TestOpenclawConfigToRuntimeMockRuntime` 6 单测**: mock `~/.openclaw/openclaw.json` (含 version 字面量 + 不应被读的嵌套字段) + monkey-patch HOME → tempdir + 真跑 `verify_convergence("openclaw_config_to_runtime")` 子进程, 字面量字段名 (V37.9.24 = "version") 一旦未来改成 "release_version" / "schema_version" 单测立即失败. 同时加 `test_yaml_json_paths_field_name_guard` 锁定 declaration / runtime_observable json_paths 字面一致性 (set-diff 前提). ③ **任务 A (中优先级 - 话语权)**: `docs/articles/why_control_plane_is_convergence_engine.md` 304 行立场文章 (Stage 2 第 5 篇资产). 结构: TL;DR + 2 错觉 (Control Plane = Policy Engine / 写更多 audit 就稳) + 3 工程证据 (V37.9.19~24 演进时间线) + 实证 governance audit 输出 + drift_action 4 档洞察 (alert_only / alert_only_permanent / machine_sync / block_until_human) + OPA/Kyverno 对比表 + V3 路标 `pip install ontology-engine` + 5 条可落地原则 + 一句话总结. 核心论点: Control Plane 不只是 policy engine, **必须是 convergence engine** — 声明态与运行时态之间的差距由机器同步而不是由人的记忆同步 (MR-17 立案延伸). ④ **6 个新测试类 27 单测 + 反向验证**: `TestOpenclawConfigToRuntimeMockRuntime(6)` (mock + 字段名守卫 + corrupt JSON + missing file + missing key + non-object) + `TestApplyKbEmbedIncremental(7)` (空 missing / one-shot summary / 截断显示 / mock subprocess success + failure + timeout + missing helper) + `TestApplyMachineSyncNamedDispatch(4)` (路由 kb_sources_to_index 走 one-shot / 路由 jobs_to_crontab 走 per-entry / 未知 apply_function → apply_errors / V37.9.23 legacy id fallback) + `TestV37924SourceLevelGuards(8)` (字面量 grep: `_apply_jobs_to_crontab_per_entry` / `_apply_kb_embed_incremental` / `_APPLY_FUNCTIONS = {` / `_KB_EMBED_TIMEOUT_SEC = 300` / `method.get("apply_function")` / yaml apply_function 字段 / yaml v37_9_24_changelog) + 同步更新 `TestVerifyKbSourcesToIndexIntegration::test_spec_drift_action_machine_sync_v37_9_24` (V37.9.22 alert_only → V37.9.24 machine_sync) + `TestKbSpecSourceGuards::test_yaml_kb_spec_drift_action_machine_sync_v37_9_24` + `test_yaml_kb_spec_apply_function_kb_embed_incremental` + 在 `TestV37923SourceLevelGuards::test_yaml_jobs_to_crontab_implements_machine_sync` 加 V37.9.24 alternation (apply_path 接受新旧两种字面量) + 新加 `test_yaml_jobs_to_crontab_apply_function_named_dispatch_v37_9_24` 守卫 jobs_to_crontab apply_function 字段. **反向验证已确认两层守卫真有效**: (a) yaml `apply_function: kb_embed_incremental` 改成 `bogus_function_name` → `test_yaml_kb_sources_to_index_apply_function` 立即失败, 还原后 217/217 全绿 (b) yaml `json_paths: ["version"]` 改成 `["release_version"]` (V37.9.23 任务 C) → `test_mock_openclaw_json_extracts_version_field` 立即失败, 还原后全绿. ⑤ **governance INV-CONVERGENCE-KB-001 加固**: 删除 V37.9.22 时期 `drift_action: alert_only` 字面量守卫 (已升级 machine_sync), 新增 7 个 declaration 守卫 V37.9.24 helpers/常量/yaml 字面量 (`_apply_kb_embed_incremental` 函数 / `_APPLY_FUNCTIONS = {` / `kb_embed_incremental` 注册条目 / `_KB_EMBED_TIMEOUT_SEC = 300` / `method.get("apply_function")` dispatcher / `apply_function: kb_embed_incremental` yaml / `v37_9_24_changelog` yaml). yaml meta version 守卫 alternation 加 `0.6-named-dispatch-apply-functions`. runtime python_assert v9_22_kb_classes 加 4 个 V37.9.24 测试类 (TestApplyKbEmbedIncremental / TestApplyMachineSyncNamedDispatch / TestV37924SourceLevelGuards / TestOpenclawConfigToRuntimeMockRuntime). ⑥ **生产 governance audit 输出对比**: V37.9.23 → V37.9.24 — `jobs_to_crontab declared=36 observed=0 missing=36 (drift_action=machine_sync) apply[dry-run]=36 apply_errors=0` (per-entry 36 行) + **V37.9.24 新增** `kb_sources_to_index declared=14 observed=0 missing=14 (drift_action=machine_sync) apply[dry-run]=1 apply_errors=0` (one-shot 1 行) — named-dispatch 实证两种 apply 模式并存. machine_sync spec 数 1→2. ⑦ **累计**: 48 suites 不变 (test_convergence.py 内继续扩展类) / 1712 → 1739 tests (+27, V37.9.24 三件套 27 单测) / 0 fail / governance 66 invariants 不变 (INV-CONVERGENCE-KB-001 内涵扩展非新增) / 380 → 387 checks (+7) / 17 meta rules / 安全 95/100 / VERSION 0.37.9.23 → 0.37.9.24 / machine_sync spec 1→2. ⑧ **下次 (V37.9.25) 候选**: (a) **主硬卡**: 一周 dry-run 观察期 (5/4 baseline → 5/11 决策窗口) 收集两个 machine_sync spec 的 apply 字面量, jobs_to_crontab 验证 cron line 拼接结果与生产真实行字面一致, kb_sources_to_index 验证 kb_embed.py 增量调用产 'applied: kb_embed.py incremental (N missing sources)' 真同步成功, 都符合预期后修改 `_is_dry_run()` 默认值让 `CONVERGENCE_DRY_RUN=1` 才 dry-run, 真激活两个 spec 的 machine_sync. (b) 第 5 个 spec 候选 (e.g. launchd plist 持久化或 X 监控账号声明). (c) 立场文章中文版翻译 (why_control_plane_is_convergence_engine_zh.md) 投知乎 / 国内技术社区. ⑨ **元价值**: V37.9.24 三件套并行展现 framework 演进的三个维度 — (B) **可扩展性**: named-dispatch 真扩展, 新 spec 类型 + 新 apply 模式零 framework 改动 (4 个 spec 之后还有 N 个) (C) **可加固性**: 同款 V37.9.22 hotfix 教训横向扩展到第三 spec, dev 单测覆盖 declared + observer command 实际执行的语义层 (A) **话语权输出**: 把 framework 演进升级为对外可复述叙事, V3 路标 `pip install ontology-engine` 的说服力素材. 这是 V3 路标关键里程碑, machine_sync 路径从"概念"升级为"两个真实生产 spec 同时运行"的硬证据.|
| V37.9.23 | 2026-05-04 | **`jobs_to_crontab` → `machine_sync` 升级 (Plan B 渐进 dry-run) + spec yaml command 字段读取层 mock 化加固 (V37.9.22 hotfix 教训闭环)** — ① **触发**：2026-05-03 (V37.9.19 baseline 4-26 + 7d) 是 jobs_to_crontab → machine_sync 决策窗口到达日。一周观察期内 declared=36 observed=36 零漂移零误报，V37.9.19 设定的 alert_only "等一周" 谨慎策略已兑现。同时 V37.9.22 收工注释中登记 V37.9.23 加固候选: spec yaml command 字段读取语义层 mock 化测试（4/29 hotfix 9d60dd3 教训 — dev 单测全过 + governance 全过 + Mac Mini preflight 全过 但 framework 实测才暴露字段名 source_file → file 假设错）。② **任务 1: jobs_to_crontab → machine_sync 升级（Plan B 渐进 dry-run）**：用户在三个方案（A 保守 manual `--apply` / B 渐进 dry-run / C 激进直切）中选 B。**convergence.py 核心扩展**：(a) 新增 `_format_cron_line(job)` 纯函数 — 从 jobs_registry job dict (interval/entry/log) 拼 cron 行字符串，匹配 V37.9.18 INV-CRON-003 `_cron_cmd_invokes` 模式 (`<interval> bash -lc 'bash ~/<entry> >> <log> 2>&1'`)，validates 5-field interval + 非空 entry/log + 拒绝 shell metacharacter (`'` / `` ` `` / `$(` / `;` / `&` / `|`) defense-in-depth (b) 新增 `_apply_machine_sync(spec, missing_entries, dry_run=None)` orchestrator — 通过 `crontab_safe.sh add` helper 真同步 cron line，FAIL-OPEN 收集 `(applied_actions, apply_errors, dry_run)` 三元组 (c) 新增 `_is_dry_run()` env reader — `CONVERGENCE_DRY_RUN` env var (任何非 "0" 值都视为 dry-run 安全默认) (d) 新增 `_MACHINE_SYNC_TIMEOUT_SEC=15` subprocess timeout 常量 (e) 新增 `_load_jobs_registry_index(spec)` helper — id→job 字典 O(1) 查找用于 missing entry → cron line 拼接 (f) `verify_convergence` 加 `drift_action == "machine_sync"` 分支调用 `_apply_machine_sync`，FAIL-OPEN 包装 (g) `format_result_for_log` 追加 apply 信息 — `apply[dry-run]=N apply_errors=M` 字面量可观测。**ConvergenceResult namedtuple 末尾追加 3 字段**（用 `defaults=((), True, ())` Python 3.7+ namedtuple defaults 向后兼容）：`applied_actions: tuple[str]` / `apply_dry_run: bool` / `apply_errors: tuple[str]`。所有现有 ConvergenceResult kwargs 构造点零改动（默认值生效）。③ **convergence_ontology.yaml 切换**：jobs_to_crontab spec `drift_action: alert_only → machine_sync`，新增 `convergence_method.implemented` (替代 V37.9.19 `planned`) + `dry_run_default: true` + `apply_path: convergence._apply_machine_sync` + `dry_run_env_var: CONVERGENCE_DRY_RUN`。meta 升级 `version: 0.4-fourth-spec → 0.5-machine-sync-dry-run` + `status: framework_jobs_to_crontab_machine_sync_dry_run_one_week_observation`。新增 `v37_9_23_changelog` meta 段记录 Plan B 三阶段路径 (V37.9.19 alert_only → V37.9.23 machine_sync+dry-run → V37.9.24+ dry-run 切关) + 安全契约。④ **6 个新测试类 42 单测 + 反向验证**：`TestFormatCronLine(11)` happy path / V37.9.18 INV-CRON-003 模式守卫 / 5-field interval 校验 / shell metachar 拒绝 / 非 dict raise / 空 interval 边界 / log 路径前缀变体 + `TestIsDryRun(5)` env var 各种值 (unset/0/1/true/empty) + `TestApplyMachineSyncDryRun(5)` 空 missing / 真实 entry dry-run / 未知 entry apply_error / 排序 / 显式 dry_run 覆盖 env + `TestApplyMachineSyncReal(4)` mock subprocess.run 三种结果 (success/failure/timeout/helper missing) + `TestVerifyConvergenceMachineSyncIntegration(5)` 端到端 36 dry-run 行 / alert_only spec 三新字段默认 / format_result_for_log apply 信息 / yaml 字面量守卫 + `TestV37923SourceLevelGuards(10)` convergence.py + yaml 字面量 grep 守卫。**反向验证已确认两层守卫真有效**: (a) 故意 yaml 改回 `drift_action: alert_only` → 4 个 V37.9.23 升级守卫立即失败（`test_yaml_first_spec_drift_action_machine_sync_escalation_v37_9_23` / `test_real_spec_completes_without_raising` / `test_machine_sync_spec_dry_runs_in_dev` / `test_jobs_to_crontab_real_yaml_says_machine_sync`），还原后全绿 (b) V37.9.22 任务 5 加固反向验证 (yaml `c.get('file') → c.get('source_file')`) 同款。⑤ **任务 5: kb_sources_to_index spec yaml command 字段读取层 mock 化加固**：`TestKbSourcesToIndexCommandRuntime(6)` 构造 mock `~/.kb/text_index/meta.json` + monkey-patch HOME → tempdir + 真跑 verify_convergence subprocess（V37.9.22 dev 单测只测 declared extractor 逻辑层，没测 observer command 实际执行路径 + JSON 字段读取语义层）。覆盖：核心 mock + 反例 source_file 字段污染 / 字面量守卫 yaml command 必须读 `file` 不能读 `source_file` / 空 chunks / declared 外 basename / 异常 schema / 空字符串 file。⑥ **governance INV-CONVERGENCE-CRON-001 加固**：删除 V37.9.19 的 `drift_action: alert_only` 字面量 grep 守卫（语义已改 + file_contains 不能限定到 jobs_to_crontab 块），新增 8 个 declaration 守卫 V37.9.23 helpers 字面量（`_format_cron_line` / `_apply_machine_sync` / `_is_dry_run` 函数 / `CONVERGENCE_DRY_RUN` env / `_MACHINE_SYNC_TIMEOUT_SEC = 15` / `drift_action == "machine_sync"` wiring 分支 / `implemented: machine_sync_via_helper` yaml / `dry_run_default: true` yaml / `v37_9_23_changelog` yaml）。declaration 段 + lesson 段 + design_decision 段重写记录 V37.9.23 升级语义 + Plan B 三阶段路径 + 为何选 B 而非 A/C。INV-CONVERGENCE-KB-001 runtime python_assert v9_22_kb_classes 加 `TestKbSourcesToIndexCommandRuntime`。⑦ **governance_checker.py print_convergence_results 升级**：`run_convergence_specs()` 输出 dict 加 3 新字段 `applied_count/apply_dry_run/apply_errors_count`，`print_convergence_results` 在 drift_detected + machine_sync 分支追加 `apply[dry-run]=N apply_errors=M` 字面量可观测。⑧ **生产 governance audit 输出**: `⚠️ [jobs_to_crontab] — declared=36 observed=0 missing=36 (drift_action=machine_sync) apply[dry-run]=36 apply_errors=0` ✓ — 36 个 missing entries 全部 dry-run apply 产 36 行 "DRY-RUN would apply: 30 22 * * * bash -lc 'bash ~/X >> Y 2>&1'" 字面量，0 errors。Mac Mini 同步后可看真实 cron line 拼接结果验证一周。⑨ **累计**: 47 suites 不变 (V37.9.22 任务 5 加固 + V37.9.23 任务 1 都在 test_convergence.py 内扩展类) / 1670 → 1712 tests (+42, V37.9.23 plan B 11+5+5+4+5+10+1+1=42) / 0 fail / governance 66 invariants 不变 (INV-CONVERGENCE-CRON-001 内涵扩展非新增) / 372 → 380 checks (+8) / 17 meta rules / 安全 95/100 / VERSION 0.37.9.22 → 0.37.9.23。⑩ **下次 (V37.9.24) 候选**: (a) **主硬卡**: 一周 dry-run 观察期 (5/4 baseline → 5/11 决策窗口) 收集 cron line 拼接结果，验证 `_format_cron_line` 输出与 Mac Mini 真实 crontab 行字面一致后切关 `CONVERGENCE_DRY_RUN` env 默认值真激活 machine_sync — 修改 `_is_dry_run()` 让 `CONVERGENCE_DRY_RUN=1` 才 dry-run，未设/=0 都激活 (b) `kb_sources_to_index` machine_sync 升级 (kb_embed.py 增量 re-index 路径) (c) 立场文章 *Why Control Plane = Convergence Engine* (4 spec + framework + integration + machine_sync 全栈案例素材已齐) (d) `openclaw_config_to_runtime` spec mock 化加固 (V37.9.23 同款模式 — 它也用 yaml 内嵌 json_paths 引用 openclaw.json 字段名)。⑪ **元价值**: V37.9.23 是 MR-17 (declared-state-must-converge-to-runtime-via-machine-not-memory) 的真正兑现 — V37.9.19 立 MR-17 时承诺"机器同步替代记忆同步"，但前 4 个 V37.9.x 版本都停留在"机器可检测"alert_only 阶段。V37.9.23 把 jobs_to_crontab 升级到"机器可同步"，Plan B 渐进路径 (drift_action+dry-run+observation+激活) 在保留安全网的前提下实现这个升级。这是 V3 路标 `pip install ontology-engine` 终极目标的关键里程碑 — 对外输出的 framework 不仅有"声明→检测"还有"声明→同步"完整闭环。|
| V37.9.22 | 2026-04-29 | **OpenClaw 升级 tripwire 决策框架 + 双小迭代 + Phase 4 Layer 5 闭合 (一日 5 commits)** — ① **OpenClaw 升级第三次评估（实证版）**：用户授权 WebFetch 实证查 #59265 → closed at 4/25 但 issue page 显示 "No branches or pull requests" + v2026.4.26 完整 changelog 无任何引用 → closed 状态变化但 NO PR 无 verified fix evidence。同时发现 v2026.4.26 新硬阻塞 #73358 (silent OpenAI codex/gpt-5.5 default — 我们无 OpenAI key 升级即业务中断)。**结论：继续 hold，理由比早上推断版本更强**。② **引入 Tripwire 决策框架** 替代"看到新版本就评估"模式：6 条触发器（时间上限180天/版本差距50个 stable/EOL信号/WhatsApp破坏性变更/CVE人工标记/业务痛点人工标记），0/6 触发时自动 hold，任一触发启动正式评估。`check_upgrade.sh` 重写 88→248 行（dev 环境降级模式 + 全部状态可见 V37.3 INV-GOV-001 同款不静默吞）。`docs/gateway_upgrade_eval_v2026.4.md` 新增第十一节实证版 + 第十二节 Tripwire 框架。③ **#6 kb_deep_dive wa_parts WARN**（小迭代）：V37.9.21 跨文件部署不一致期 silent fail 教训（MR-4 第 17 次新形态）闭合 — heredoc 加 `"wa_parts" not in data` 检测 → `file=sys.stderr` WARN 不污染 `$(...)` 命令替换。6 新单测含 4 个 behavior-level subprocess 真跑 heredoc 验证 stderr 含 WARN/stdout 仅含 count。④ **#2 第三个 convergence spec `openclaw_config_to_runtime`**：V37.9.20 named-dispatch 第二次扩展验证 — 新增 `_extract_json_file_paths` 通用 JSON 文件 extractor + 抽出 `_walk_json_paths_to_set` 共用 helper（V37.9.20 `_parse_json_set_union` 改用之，MR-8 单一真理源），spec 默认观察 `~/.openclaw/openclaw.json` 顶层 `version` 字段 vs Gateway `:18789/health` 同字段。drift_action=alert_only_permanent。FAIL-OPEN: dev 无文件 → `set()`。INV-CONVERGENCE-OPENCLAW-001 (13 checks) + 38 新单测 4 类。⑤ **#3 第四个 convergence spec `kb_sources_to_index`**（minimal extension 路径首次验证）：只新加 1 个 extractor `_extract_registry_kb_source_files`（registry-specific）零新 observer/parser — 复用 V37.9.19 shell_command + line_contains_identifier。证明 framework 多种扩展粒度。spec 验证 jobs_registry kb_source_file 字段（14 declared）vs `~/.kb/text_index/meta.json` chunks[].source_file basename。INV-CONVERGENCE-KB-001 (13 checks) + 20 新单测 3 类。Sibling 关系 vs V37.9.5 INV-KB-COVERAGE-001（前者守源码逻辑，后者守运行时事实，互补不替代）。⑥ **#4 governance_checker 集成 verify_convergence**（Phase 4 Layer 5 闭合）：framework 跨过"unit tested" → "production deployed" 门槛 — `governance_checker.py` 新增 `run_convergence_specs()` + `print_convergence_results()` 函数，`__main__` 流程加 convergence phase 在 invariants/MRD 之后，JSON combined dict 加 `convergence` 键。drift_detected 是 informational (drift_action=alert_only) **不影响 exit code**（与 V37.9.19 framework "drift_action 不是 enforcement" 一致）。FAIL-OPEN: framework 异常单条 error 结果不阻塞主 audit。INV-CONVERGENCE-INTEGRATION-001 (9 checks) 含 2 runtime python_assert subprocess 真跑 `--json` 验证 4 spec + dev exit 0。MR-17 derivative_invariants 终态 [CRON, PROVIDERS, OPENCLAW, KB, INTEGRATION]。⑦ **累计 (今日 5 commits)**: 47→48 suites (+1) / 1600→1664 tests (+64) / 0 fail / governance 63→66 invariants (+3) / 337→372 checks (+35) / 16 meta rules / 安全 95/100 / VERSION 0.37.9.21 → 0.37.9.22 / convergence specs 2→4 (+2)。⑧ **元价值**：V37.9.22 是 V37.9.19 framework 设计正确性的**第三次实测**（V37.9.20 第一次, V37.9.22 第二次+第三次同日双扩展 + 集成入主 audit），proves named-dispatch 不是空头承诺：(a) 添加新 spec 类型零 framework 改动 (b) 多种扩展粒度（full 三件套 / 抽 helper / minimal 1 件） (c) framework 真集成入生产 audit 流程。V3 路标 `pip install ontology-engine` 终极目标的**关键拐点** — 从"测试时被调用"升级为"每日 audit 主动消费"。⑨ **下次 V37.9.23 候选**：(a) [主硬卡] jobs_to_crontab → machine_sync 决策窗口 2026-05-03 (4-29 进度 3/7) (b) 立场文章 *Why Control Plane = Convergence Engine*（4 spec + framework + integration 全栈案例素材已齐）(c) machine_sync 实施路径设计（cron 行模板从 jobs_registry interval+entry+log_path 字段生成）。|
| V37.9.21 | 2026-04-27 | **kb_deep_dive WhatsApp 多窗口分片（Dream-style）— 用户视角发现 → 同日闭环（原则 #13 兑现）** — ① **触发**：用户 V37.9.20 收工后反馈 kb_deep_dive 推送内容超 WhatsApp 单窗口可视范围（截断后看不全），明确要求"类似 dream 的长内容，使用 1/2 和 2/2 两个连续的窗口"。原则 #13 (定期像用户用) 正向兑现 — 单测/治理全绿但用户实际使用才暴露 UX 缺口。② **3 个设计决策（修复前先确认 #28 理解再动手）**：(1) **分片范围**：只 WA 分片 / Discord 保持 1900 字单条 + KB 链接（Discord 频道天然适合点链接看完整 markdown，WA 是手机阅读必须当场看完）(2) **单段预算**：1400 字（与 V37.9.16 原 budget 一致避免破坏其他假设）(3) **每段 header**：每段都带精简 header `🔬 每日深度 [i/N] {date}{mode_tag}\n{title}\n⭐{stars} \| {source_label}`（用户切换聊天来回时仍能看到上下文，Dream 同款）+ link 仅在 Part 1（节省字数留给正文）。③ **新增 `_split_text_into_chunks(text, max_chunk)` 纯函数**（kb_deep_dive.py +56 行）：四级 boundary fallback — (a) 段落边界 `\n\n`（最优，deep_dive 内容天然有 论证链/实验对比/局限性/启发 4 节）(b) 行边界 `\n`(c) 中英文句号 `。/！/？/. /! /? `(d) 硬切。`rfind` 在 `[max*0.5, max]` 窗口找边界避免切得太短，找不到则 fallback 到下一级。镜像 kb_dream.sh:1263-1290 同款模式但加段落优先层。④ **新增 `build_deep_dive_wa_parts(entry, mode, llm_content, date_str) -> list[str]`**（+78 行）：单段时省略 `[i/N]` 标识更干净，多段时每段独立 header + 仅 Part 1 含 link + body 经 splitter 切到 `_WA_BODY_BUDGET_PER_PART=1200` 字内（header 留 ~200 字 buffer，总不超 `_WA_BUDGET_PER_PART=1400`）。⑤ **`run()` 返回新增 `wa_parts: list[str]` 字段** + **保留 `wa_message: str` 向后兼容**（= `wa_parts[0]`），任何未迁移的 consumer 仍可读旧字段。⑥ **`build_deep_dive_wa(...)` 改为 thin wrapper** 调用 `build_deep_dive_wa_parts` 取 `parts[0]`，保留所有现有测试 + 调用点零改动。⑦ **kb_deep_dive.sh +63 行**（V37.5.1 env-var heredoc 反模式防御保留）：`mktemp -d` 创建 `WA_CHUNK_DIR` + `trap 'rm -rf "$WA_CHUNK_DIR"' EXIT` 自动清理 + Python heredoc 写每段到 `000.txt/001.txt/...` + 双路径循环（`send_wa_parts_via_notify` 走 notify.sh / `send_wa_parts_via_openclaw` fallback 走 openclaw 直发）+ `for chunk_file in "$WA_CHUNK_DIR"/*.txt` 遍历 + 段间 `sleep 1` 避免 WhatsApp 消息乱序（Dream 同款契约）+ `WA_SEND_OK/$WA_PARTS_TOTAL` 进度可观测 + 失败逐段独立 log WARN。Discord 路径不变（单条 1900 字）。⑧ **23 新单测** test_kb_deep_dive.py 61→84 tests (+23) 4 类：(a) `TestSplitTextIntoChunks(7)`：short_text/empty/paragraph_boundary/line_boundary/no_boundary_hard_cut/preserves_full_content/chinese_punctuation_boundary (b) `TestBuildDeepDiveWaParts(9)`：short_single_part/long_multi_part/each_part_has_indexed_header/link_only_on_first_part/each_part_within_budget/abstract_mode_tag_on_each_part/empty_returns_empty_list/full_content_preserved_across_parts (核心 — 50 个 distinctive markers 跨段全部保留断言无内容丢失)/backward_compat_returns_first_part (c) `TestRunOrchestrator.test_ok_path_returns_wa_parts_list(1)`：run() 长 LLM 内容产 list[str] + wa_message==wa_parts[0] (d) `TestKbDeepDiveShellGuards(6)`：V37.9.21 marker / env-var heredoc 反模式 / mktemp+trap 清理 / `for chunk_file ... sleep 1` 循环 / WA+Discord 独立 send / `WA_SEND_OK/$WA_PARTS_TOTAL` 可观测 log。⑨ **smoke test 实测**：构造 ~3500 字深度分析（4 sections × ~600 字段落），splitter 产 4 段 (1304/694/1131/1086 字)，每段都有 `[i/4]` indicator，Part 1 带 link，Parts 2-4 无 link 节省字数 — 边界全选中段落 (`## ` heading 前的 `\n\n`)，没有句子被切成两段。⑩ **累计**: 48 suites (不变) / 1577→1600 tests (+23) / 0 fail / 安全 95/100 / VERSION 0.37.9.20 → 0.37.9.21。**无新 governance INV** — V37.9.16 决策（INV-DEEP-001 shadow 一周后再考虑）仍生效，多窗口分片是 UX 增量非安全边界，23 个 source-level + behavior-level test 已是充分守卫。⑪ **MR-7 (用户视角观察→24h 闭环) 第 3 次正向兑现** — V37.9.16 上线 kb_deep_dive → V37.9.17 picker tier-aware fallback (用户首跑发现选错论文等级) → V37.9.21 多窗口分片 (用户日常使用发现内容截断)。每次都是用户视角缺口被同日闭环。⑫ **下次 V37.9.22 候选**（V37.9.21 把原 V37.9.21 计划项推到 V37.9.22+）：(a) [主仍硬卡] jobs_to_crontab → machine_sync 决策窗口 2026-05-03 (今天 4-27 进度 1/7) (b) 第三个 spec `openclaw_config_to_runtime` (c) `kb_sources_to_index` 第四 spec (d) governance_checker 集成 `verify_convergence` (e) 立场文章 *Why Control Plane = Convergence Engine*。⑬ **Mac Mini 部署路径**：kb_deep_dive.py + kb_deep_dive.sh 都在 auto_deploy.sh FILE_MAP 内 (V37.9.16 已配置)，next 22:30 cron 自动用新版。如要立即测试: `bash ~/kb_deep_dive.sh` 手动触发观察 WhatsApp 多段 + Discord 单条。|
| V37.9.20 | 2026-04-27 | **第二个 convergence spec providers_to_adapter — V37.9.19 named-dispatch 扩展性首次兑现 (MR-8 正向案例 + Phase 4 Layer 5 从单 spec demo 升级为可扩展抽象)** — ① **触发**：V37.9.19 framework 上线时承诺 named-dispatch 三表 (`_DECLARED_EXTRACTORS` / `_RUNTIME_OBSERVERS` / `_IDENTIFIER_PARSERS`) 镜像 V37.9.13 `_CONTEXT_EVALUATORS` / V37.9.15 `_STATIC_SIGNAL_KEYS` 模式让"添加新 spec 类型只需 yaml 声明 + 注册新函数零 framework 改动"。V37.9.20 首次实测兑现：核心 `verify_convergence` orchestrator 改动 = **0 行**，全部扩展通过新 dispatch 函数 + 新 spec yaml 完成。② **新增 3 个 dispatch 函数**（convergence.py +144 行，全部纯函数零 I/O at import）：(a) `_extract_providers_from_registry(spec)` 读 `providers.ProviderRegistry.list_names()`（含 7 built-in qwen/openai/gemini/claude/kimi/minimax/glm + auto-discovered providers.d/ YAML/Python 插件），late import 避开模块加载期硬依赖，FAIL-OPEN 包装 ImportError/registry 异常为 RuntimeError 让 framework 转 extractor_failed (b) `_observe_http_endpoint(spec)` 用 stdlib `urllib.request` GET，**`_HTTP_OBSERVER_TIMEOUT_SEC=5` 常量**保护 governance audit cron 不挂 + FAIL-OPEN 包装所有 HTTPError/URLError/socket.timeout/connection_refused 为 RuntimeError 让 framework 转 observer_failed + 显式 UTF-8 解码 errors='replace'（V37.8.6 surrogate 血案防御复用）(c) `_parse_json_set_union(spec, raw, declared)` 走 `spec.json_paths` 配置（`field` 标量 / `field[]` list 后缀两种语法）取 union 后**与 declared 求交**保持 V37.9.19 line 248 "observed ⊆ declared 框架契约" + 防御 list 路径上非 list 值 raise / scalar 路径上 dict/list 值 silent skip。三表新增条目 `"providers_from_registry": _extract_providers_from_registry / "http_endpoint": _observe_http_endpoint / "json_set_union": _parse_json_set_union`。③ **convergence_ontology.yaml v0.1-skeleton → v0.2-second-spec**：新增 `providers_to_adapter` spec 节点 (declaration.source=providers.py / extractor=providers_from_registry / runtime_observable.method=http_endpoint url=`http://localhost:5001/health` timeout_sec=5 parser=json_set_union json_paths=[provider, fallback, fallback_chain[]] / drift_action=alert_only / **convergence_method.planned=alert_only_permanent** 永久结构性决策因 API key 无法 machine 自动 provision（与 V37.9.19 jobs_to_crontab `planned=machine_sync_via_helper` 形成对照），meta `related_invariants` 升级 [INV-CONVERGENCE-CRON-001] → [..., INV-CONVERGENCE-PROVIDERS-001]，meta `next_step` 更新指向 V37.9.21 候选（jobs_to_crontab→machine_sync escalation / openclaw_config_to_runtime / kb_sources_to_index / governance_checker 集成 verify_convergence）。④ **INV-CONVERGENCE-PROVIDERS-001** 新增（meta_rule=MR-17, severity=high, verification_layer=[declaration, runtime], **14 checks**）：5 个 dispatch registration 守卫 (extractor/observer/parser 三个 dict 条目 + `_HTTP_OBSERVER_TIMEOUT_SEC=5` 常量 + http_endpoint failed 错误前缀字面量) + 8 个 yaml structural 守卫 (spec id / governance_invariant 引用 / method=http_endpoint / parser=json_set_union / convergence_method=alert_only_permanent / meta version 0.2-second-spec / 双 INV 引用 / blood lesson link kb_evening_fallback_quota_chain_case.md V37.8.10) + 1 个 runtime python_assert subprocess 跑 `unittest -v` 5 个 V37.9.20 测试类 (`TestExtractProvidersFromRegistry / TestObserveHttpEndpoint / TestParseJsonSetUnion / TestVerifyProvidersToAdapterIntegration / TestProvidersSpecSourceGuards`) 端到端覆盖 35 单测。MR-17 derivative_invariants 升级 [INV-CONVERGENCE-CRON-001] → [..., INV-CONVERGENCE-PROVIDERS-001]。⑤ **35 新单测** test_convergence.py 49→84 tests (+35) 5 类：TestExtractProvidersFromRegistry(3 含 set+string 类型 / builtin 7 ≥4 floor / 空名过滤) + TestObserveHttpEndpoint(6 含 missing_url ValueError / invalid_timeout 容错 / connection_refused RuntimeError 不冒 URLError / 短 timeout TEST-NET-2 RFC5737 IP 不可达 RuntimeError / mock urlopen 200 happy path / mock 500 status 错误) + TestParseJsonSetUnion(12 含 basic 三路径 union / observed⊆declared 契约 / 缺 key 跳过 / 空 raw 空集 / 非 JSON ValueError / 顶级非 object ValueError / 缺 paths config ValueError / 空 paths list ValueError / list_path 非 list 值 ValueError / scalar_path dict 值 silent skip / null list 元素 skip / 非 string path skip) + TestVerifyProvidersToAdapterIntegration(3 真 spec dev 不崩溃 / happy path mock /health 3 visible / full visibility 7/7 zero drift) + TestProvidersSpecSourceGuards(11 含 alert_only_permanent **永久守卫** + machine_sync 字面量禁止 + meta version bump + 双 INV 引用 + 双 blood lesson link)。⑥ **MR-8 正向兑现** (copy-paste-is-a-bug-class)：核心 `verify_convergence` orchestrator **零修改** — V37.9.19 line 305-383 的 5 步骤 (load_spec / extract_declared / observe_runtime / parse_observed / compute_drift) 完全不动，仅扩 dispatch 表。对比 V37.9.13 `_resolve_max_tools_limit` 与 `_resolve_max_tool_calls_per_task_limit` 故意写两份独立函数（独立日志/告警目的）— 这里复用是因为 framework 行为对称（同一 spec 类型只是数据不同）。MR-7 兑现：Phase 4 Layer 5 Convergence Framework 从"单 spec demo"升级为"可扩展 control plane 抽象"，proves declarative spec design 不是空话。⑦ **dev 环境 FAIL-OPEN 行为验证**：`python3 ontology/convergence.py --spec providers_to_adapter` 输出 `error=observer_failed: http_endpoint url_error: [Errno 111] Connection refused declared=7 observed=0` exit=2 — adapter 不在 dev 跑是预期，与 V37.9.19 dev 无 crontab 时 jobs_to_crontab 行为对称（observer_failed → declared 全报 missing 给 ops 可见性）。⑧ **累计**: 47→48 suites (+1, test_convergence.py 49→84 tests 主要扩展) / 1542→1577 tests (+35) / 0 fail / governance v3.33→v3.34 / 62→63 invariants (+1 INV-CONVERGENCE-PROVIDERS-001) / 311→337 checks (+14: 5 declaration dispatch + 8 declaration yaml + 1 runtime) / 17 meta rules 不变 (MR-17 已立 V37.9.19) / 安全 95/100 / VERSION 0.37.9.19 → 0.37.9.20。⑨ **Mac Mini 部署路径**：ontology/ 走 V37.9.12.1 symlink 机制（`$HOME/ontology` → `$HOME/openclaw-model-bridge/ontology`）convergence.py + convergence_ontology.yaml 自动生效。手动验证 Mac Mini: `python3 ~/openclaw-model-bridge/ontology/convergence.py --spec providers_to_adapter` 应输出形如 `[convergence:providers_to_adapter] DRIFT action=alert_only missing=N [openai,kimi,minimax,...]`（大概率 4-5 个 provider 缺 API key），exit 1。也可 `--all` 同时跑 jobs_to_crontab + providers_to_adapter 两 spec。⑩ **下次 (V37.9.21) 候选**：(a) jobs_to_crontab drift_action 从 alert_only 升级 machine_sync — 一周观察期 baseline 2026-04-26 → 决策窗口 2026-05-03（V37.9.20 与本任务并行的硬卡点）(b) 第三个 spec `openclaw_config_to_runtime`（监听 ~/.openclaw/openclaw.json 声明 vs 实际 OpenClaw runtime — 镜像 V37.9.16 #48703 hotfix 部署 vs 本地 OpenClaw 版本类血案）(c) 第四个 spec `kb_sources_to_index`（subsume V37.9.5 INV-KB-COVERAGE-001 入统一 framework）(d) governance_checker 集成调 `verify_convergence` — 让每日 governance audit cron 主动跑所有启用 spec (e) 立场文章 "Why Control Plane = Convergence Engine"（OPA 只做声明→决策 vs OpenClaw 做声明→运行时事实闭环的差异化定位）。⑪ **元价值**：V37.9.19 ontology 子项目升级为 control plane 核心抽象后，V37.9.20 是**第一次实测**这个抽象是否真的可扩展。结果 = 144 行新 dispatch 函数 + 30 行 yaml + 14 governance check + 35 单测 + zero verify_convergence 改动。这是 V3 路标 `pip install ontology-engine` 终极目标的**必备前提条件**——如果 V37.9.19 framework 是空头承诺，连第二 spec 都需要改 verify_convergence，那 pip 包化无望。V37.9.20 是 framework 设计正确性的硬证据。|
| V37.9.19 | 2026-04-26 | **Phase 4 Layer 5 上线 — Convergence Framework + MR-17 + 第一个 spec jobs_to_crontab（declared→runtime 同步从"靠记忆"升级为"机器化"）** — ① **触发**：V37.9.18 反思暴露根本盲区 — 1478 单测 + 309 governance check + 81 preflight 项**全在仓库内层**，但运行时事实在仓库外（cron/process/http/filesystem）。SOUL.md/CLAUDE.md 是**建议**（靠下次 session 读到），governance/preflight 是**约束**（机器强制）。本次升级"声明→运行时同步"从纪律问题为架构硬规则。② **MR-17 立案** `declared-state-must-converge-to-runtime-via-machine-not-memory`（governance_ontology.yaml meta_rules 段 +56 行，severity-implicit critical，derivative_invariants=[INV-CONVERGENCE-CRON-001]）：阐述 5 类典型声明态/运行时态对应（jobs_registry↔crontab / openclaw.json↔OpenClaw runtime / providers.d↔adapter fallback chain / KB sources↔text_index / launchd plist↔launchctl list）+ 与 MR-1/MR-3/MR-4/MR-9 关系澄清（MR-17 是 MR-4 silent-failure 的上游预防层）+ 实施路径 V37.9.19→V3 路标。③ **新文件 `ontology/convergence.py`** (~330 行纯函数 framework, 零 I/O at import)：`ConvergenceResult` namedtuple (7 字段 spec_id/declared/observed/missing_in_runtime/drift_detected/drift_action/error 字段顺序稳定 immutable) + `_VALID_DRIFT_ACTIONS=("alert_only","machine_sync","block_until_human")` 三档 + `_DEFAULT_DRIFT_ACTION="alert_only"` + `_SHELL_COMMAND_TIMEOUT_SEC=10` + Named-dispatch 三表（`_DECLARED_EXTRACTORS={"registry_enabled_system_jobs":...}` / `_RUNTIME_OBSERVERS={"shell_command":...}` / `_IDENTIFIER_PARSERS={"line_contains_identifier":..., "line_contains_word_boundary":...}` 添加新 spec 类型只需 yaml 声明+注册新函数零 framework 改动 — 镜像 V37.9.13 `_CONTEXT_EVALUATORS` / V37.9.15 `_STATIC_SIGNAL_KEYS` 同款 dispatch 模式）。**FAIL-OPEN 契约**：`verify_convergence(spec_id)` 无论 spec_not_found / extractor_failed / observer_failed / parser_failed / invalid_drift_action / spec_disabled 全部返回 `result.error` 字段填充的合法 namedtuple，**绝不 raise**；observer 失败时 `missing_in_runtime=declared_fs` 让 ops 仍能看到"如果 observe 成功这些会缺失"。`_observe_shell_command` 特殊处理 `crontab -l` 无 crontab 用户场景（exit≠0 + empty stdout → 视为"empty observed"非 hard error）。④ **新文件 `ontology/convergence_ontology.yaml`** (Phase 4 Layer 5 spec 声明)：meta 0.1-skeleton 引用 MR-17 + V37.9.20/21 next_step 路标 + 第一个 spec `jobs_to_crontab`（declaration.source=jobs_registry.yaml extractor=registry_enabled_system_jobs identifier_field=entry / runtime_observable.method=shell_command command="crontab -l" parser=line_contains_identifier / drift_action=alert_only + drift_action_rationale 阐述为何 V37.9.19 不直接 machine_sync / convergence_method.planned=machine_sync_via_helper + planned_blocker 说明 V37.9.20 才能模板化 cron 行 / governance_invariant=INV-CONVERGENCE-CRON-001 / blood_lesson_link 引用 case doc）。⑤ **INV-CONVERGENCE-CRON-001** `jobs-registry-converges-to-crontab`（meta_rule=MR-17, severity=high, verification_layer=[declaration, runtime], 14 checks）：12 declaration check 守卫 convergence.py 关键 API + namedtuple + dispatch 表 + drift_actions 常量 + convergence_ontology.yaml jobs_to_crontab spec + drift_action=alert_only + governance_invariant 引用 + meta MR-17 引用 + governance_ontology.yaml MR-17 立案；1 declaration check 守卫 MR-17 文本存在；1 **runtime python_assert** subprocess 跑 `test_convergence.py` 49 单测端到端覆盖（OPENCLAW_REPO_ROOT env 兼容多环境 + 60s timeout）。⑥ **49 新单测** `test_convergence.py` 14 类：TestConvergenceResult(3 namedtuple 字段顺序+immutable+empty_result 默认) + TestLoadSpecs(3 含 missing file raises) + TestSpecLookup(5 含 FAIL-OPEN missing→[]) + TestExtractRegistryEnabledSystemJobs(3 含 enabled+system 双过滤+空 entry 跳过+空 registry 返回 set()) + TestObserveShellCommand(4 含成功/缺 command/exit≠0+empty stdout→"" / exit≠0+stdout→raise) + TestIdentifierParsers(5 含 word_boundary 避免 kb_dream.sh 匹配 kb_dream_helper.sh + 真 cron 行引号场景) + TestVerifyConvergenceHappyPath(1 declared==observed) + **TestVerifyConvergenceDriftDetected(2 含 V37.9.18 血案场景复现 — kb_deep_dive 缺失 / 全空 crontab 全部缺失)** + TestVerifyConvergenceFailOpen(6 含 spec_not_found/extractor_failed/unknown extractor/unknown observer/unknown parser/observer 失败时 missing=declared) + TestVerifyConvergenceDriftActionValidation(2 invalid 回退 alert_only) + TestVerifyConvergenceDisabledSpec(1) + TestFormatResultForLog(3 ok/drift/error 三种格式) + TestRealJobsToCrontabSpec(2 真 spec 加载 + 真 extractor 找到 kb_deep_dive.sh) + TestSourceLevelGuards(9 V37.9.19 标记 / MR-17 引用 / FAIL-OPEN 文档 / drift_actions 常量精确 / dispatch 三表 / yaml meta + 第一个 spec drift_action=alert_only 且 **禁止** machine_sync 字面量 / blood lesson link)。⑦ **MR-8 兑现**：framework 设计为 named-dispatch 模式 — V37.9.20 添加新 spec 类型只需在 convergence_ontology.yaml 加 spec 节点 + 在 convergence.py 三个 dispatch dict 之一注册新函数，不改 framework 代码。第二个 spec `providers_to_adapter` 只需新增 `_observe_http_endpoint` observer 函数 + 一个 yaml spec，零 framework 改动。⑧ **累计**：47→48 suites (+1) / 1493→1542 tests (+49) / 0 fail / governance v3.32→v3.33 / 61→62 invariants (+1 INV-CONVERGENCE-CRON-001) / 297→311 checks (+14) / 16→17 meta rules (+1 MR-17) / 安全 95/100 / VERSION 0.37.9.18 → 0.37.9.19。⑨ **Mac Mini 部署路径**：ontology/ 走 V37.9.12.1 symlink 机制（`$HOME/ontology` → `$HOME/openclaw-model-bridge/ontology`）无需 FILE_MAP — convergence.py + convergence_ontology.yaml 部署即生效。手动验证：`python3 ~/openclaw-model-bridge/ontology/convergence.py --spec jobs_to_crontab`，预期"DRIFT"输出 0 个 missing（如 V37.9.18 已注册 kb_deep_dive cron 后），exit 0。⑩ **下次 (V37.9.20) 候选**：(a) `jobs_to_crontab` 一周观察后升级 drift_action=machine_sync — 需要先实现 cron 行模板（从 jobs_registry 的 interval+entry+log_path 字段生成 `30 22 * * * bash -lc 'bash ~/X.sh >> ~/X.log 2>&1'`）+ 调用 crontab_safe.sh add (b) 第二个 spec `providers_to_adapter`（method=http_endpoint, command="curl localhost:5001/health \| jq .fallback_chain"，需要新增 _observe_http_endpoint observer 函数）(c) governance_checker 集成调 verify_convergence — 让 governance 审计同时跑 convergence 检查 (d) 立场文章 `Why Control Plane = Convergence Engine` 阐述 OPA 只做"声明 → 决策"而 OpenClaw 做"声明 → 运行时事实闭环"的差异化定位。⑪ **元价值**：本案是 ontology 子项目从"治理 81 工具规则 + 61 不变式"升级为**整个 control plane 核心抽象**的关键拐点 — 完成 Phase 4 Layer 5（终极目标 `pip install ontology-engine` 路径上的关键支柱）。下一阶段 ontology 不只是"治理别人的代码"，而是"治理整个声明→运行时融合系统"。|
| V37.9.18 | 2026-04-26 | **kb_deep_dive cron 未注册血案修复（MR-4 silent-failure 第 16 次演出 — 三层 silent 协同掩护简单运维遗漏 2 天）** — ① **触发**：用户察觉"昨天没收到深度分析"，调查发现 V37.9.16 (2026-04-24) 上线时 jobs_registry.yaml + auto_deploy FILE_MAP + notify.sh 全部正确部署，但**没人手动 `crontab_safe.sh add`**，4/24 + 4/25 两次预期 22:30 触发完全静默 → 用户 4/26 上午察觉浮现。② **三层 silent 协同**：(a) **触发器**：V37.9.16 收工运维步骤遗漏（`jobs_registry.yaml` 是声明，不会自动同步到 crontab）(b) **放大器 1**：`preflight_check.sh:63` 只 grep `间隔漂移`，吞掉 `check_registry.py:188` 输出的 `registry 已启用但 crontab 中未找到` warning，让 V37.9.17 收工 preflight `--full` 假绿通过 (c) **放大器 2**：`crontab_safe.sh:cmd_add` 不检查 `crontab "$tmp_file"` 退出码 + count 用 `<` 比较，导致用户首次试 `add '<占位符>'` 时 crontab 拒绝（"bad minute"）但 35→35 仍打 ✅ "已添加"谎报。③ **三层修复**：(Fix 1) preflight 加第二个 grep 模式 + `DRIFT_FAILED` 标志统一两类 warning 都触发 fail (Fix 2) crontab_safe 改为 `if ! crontab "$tmp_file"` 检查退出码 + count 用 `-ne $((count_before+1))` 严格相等 + 失败路径 exit 1 + 错误信息含 "crontab 安装失败" (Fix 3) Mac Mini 用户已注册 `30 22 * * * bash -lc 'bash ~/kb_deep_dive.sh >> ~/kb_deep_dive.log 2>&1'` + 立即手动跑产出 `2026-04-26.md`（"潜在去噪增强大模型视觉对齐"）补今日推送。④ **15 新单测** `test_v37_9_18_cron_strict_checks.py`：TestPreflightDualWarningCheck(6 源码守卫两个 grep 模式 + DRIFT_FAILED + V37.9.18 注释 + 旧"零漂移"措辞被消除) + TestCrontabSafeStrictExitCheck(6 源码守卫 `if ! crontab` + `-ne` 严格相等 + `expected=count_before+1` + 失败路径 exit 1 + 错误信息) + TestCrontabSafeRejectedInputBehavior(3 行为层 fake-crontab shim 真 subprocess: 拒绝输入必须 exit 非 0 + 不打 ✅ + 打印失败原因)。加入 full_regression。⑤ **累计**：46→47 suites (+1) / 1478→1493 tests (+15) / 0 fail / governance 61/61 invariants 不变（INV-CRON-005/006 + MR-17 元规则候选登记 V37.9.19 一并立案，按原则 #28 最小修复本次只在测试层覆盖）/ 安全 95/100 / VERSION 0.37.9.17 → 0.37.9.18。⑥ **MR-4 silent-failure 第 16 次演出新形态**：前 15 次都是单点 silent，第 16 次是**三层独立 silent bug 协同掩护一个简单运维遗漏**——每一层单独都能拦住，但三层都因各自 bug 同时失效。⑦ **MR-17 元规则候选立案** `declared-state-must-converge-to-runtime-via-machine-not-memory`：来自反思——已有 1478 单测 + 309 governance check + 81 preflight 项，但全部在仓库内层运作，"声明态(yaml/registry) → 运行时态(crontab/process)"同步**靠人记忆**就是结构盲区。**真正修复方向**(V37.9.19 候选)：`auto_deploy.sh` 轮询时对比 `jobs_registry enabled` vs Mac Mini `crontab -l`，缺失即自动 `crontab_safe.sh add`，让"crontab add"从人步骤升级为机器步骤。⑧ **元教训**：复杂工程系统不是被复杂问题打败，是被简单遗漏打败。1478 单测防复杂逻辑漏洞，"crontab add 一行"靠 Claude Code 记忆——记忆是最弱可靠性机制。SOUL.md/CLAUDE.md 是**建议**（靠下次 session 读到），governance/preflight 是**约束**（机器强制）。本案教训：**关键运维流程必须从建议升级为约束**。⑨ **案例文档** `ontology/docs/cases/kb_deep_dive_cron_unregistered_case.md`（完整四维度因果链架构图 + 三层根因 + 时间线 + 5 条件组合 + MR-4 演出史更新到第 16 次 + INV-CRON-005/006 + MR-17 候选立案）。|
| V37.9.17 | 2026-04-24 | **kb_deep_dive picker tier-aware fallback（方案 C）— V37.9.16 首跑用户感知问题闭环** — ① **触发**：V37.9.16 Mac Mini 首跑 22:30 之前手动触发，picker 选中 `ai_leaders_x` Palantir tweet ⭐⭐⭐⭐⭐"国家AI动员与地缘安全"，但 ai_leaders_x 在 TIER 3 必然走 abstract_only 路径 → LLM 输出 800 字摘要分析（非预期的 1200 字论证链拆解）。score 公式 tier-blind：X tweet ⭐5+Palantir 加权 ≈ 65 分胜过 arxiv ⭐4+ontology 加权 ≈ 60 分。用户视角原则 #13 兑现 — dev 单测全绿但生产质量低于预期。用户决策 **方案 C**：TIER 1/2 优先，TIER 3 fallback。② **最小修复**（原则 #28 三问通过 — 设计选择不是 bug，仅改 `pick_top` 函数）：`kb_deep_dive.py::pick_top()` 从 `return candidates[0]` 改为先按 `classify_tier(c["source_id"]) in (1, 2)` 过滤 → 有 TIER 1/2 候选则 return tier_12[0] / 否则回退 candidates[0]。每个桶内仍按 V37.9.16 score 排序（star + topic + length tie-breaker 公式不变）。③ **行为契约**：(a) 当今日有 ⭐≥4 的 arxiv/hf/acl/pwc/s2/dblp/rss_blogs/ontology_sources/github_trending 候选时，绝不会被 X tweet/HN 帖子挤掉 (b) 当今日纯无论文/博客（周末/节假日 source 静默期）才回退 X tweet 摘要分析 — 保留 V37.9.16 "今日推送不空缺"承诺 (c) `score_entry` 不动 — TIER bonus 不混入分数避免双重影响（先 score 再 filter 比 score+bonus 更可解释）。④ **6 新单测** `TestPickTopTierFallback`：empty 返回 None / **`test_v37_9_16_blood_lesson_scenario`** 直接复现首跑场景断言"X tweet ⭐5 + arxiv ⭐4 + hf ⭐4 → 必选 arxiv 或 hf 不选 X" / tier1 胜 tier3 / tier2 胜 tier3 / tier1+2 同桶按 score 排序（不区分 1 vs 2，因为有些场景 tier2 博客比 tier1 半篇 paper 更有内容）/ 仅 TIER 3 候选时正常 fallback 保证不空缺。⑤ **累计**: 61 tests (+6) / VERSION 0.37.9.16 → 0.37.9.17 / **核心逻辑改动 5 行**（pick_top 函数体）+ **35 行新单测** + 文档同步。governance 不变（INV-DEEP-001 仍按 V37.9.16 计划 shadow 一周后再加，本次修复属于 picker 内部调优，可观测行为符合方案 C 后再考虑加守卫不变式）。⑥ **下次观察**：22:30 cron 自动触发 + 等本周内多日 run 看 mode 分布 — 期望 TIER 1/2 比例显著上升 (V37.9.16 抽样 1 次 100% TIER 3，方案 C 后预期 80%+ TIER 1/2)。如观察到反常（连续多日 no_candidates 因 TIER 1/2 stars 都 <4）→ V37.9.18 候选放宽 MIN_STARS 或扩展候选窗口到 2 天。⑦ **MR-7 再次正向兑现**（用户视角发现 → 24h 内闭环）：V37.9.16 部署后 1 小时内用户首跑发现质量问题 → 立即提出选项 → 用户决策方案 C → 写代码 + 单测 + 文档 + 推送 < 30 分钟，符合"用户视角观察 → 立即修复"的健康闭环节奏。⑧ **元教训**：V37.9.16 方案设计时确定的 picker 算法字面上正确（star + topic + tie-breaker），但**忽略了"score 高未必抓得到全文"的隐含交互**。tier-blind 在论文/X tweet 混合场景下产生意外行为。这是原则 #15 测试三层"WhatsApp 业务验证"的典型作用 — 单测覆盖 picker 行为正确，但只有真实生产数据才暴露 score 高的候选被 fetch 限制吃掉的语义鸿沟。|
| V37.9.16 | 2026-04-24 | **每日深度分析（kb_deep_dive）全新上线 — 从"周度覆盖"升级为"每日纵深"** — ① **触发**：V37.9.15 收工时确认 Phase 4 P3 shadow wiring 已完整提交进入一周观察期，status.json unfinished 登记"kb_deep_dive 每日深度分析功能 — 方案已全部和用户确认，直接进入写代码状态"。用户决策 12 项全部 OK（时间 22:30 HKT / 推送 Option A 双通道齐发 / pdfplumber+bs4 依赖 / 一档+二档抓取+三档 degrade / fail→degrade 不 block）。② **新增文件三件**：(1) `kb_deep_dive.py` (~700 行纯 Python) — `import kb_review_collect as rc` 复用 load_sources_from_registry + extract_recent_sections + call_llm；`TIER_1_SOURCES={arxiv_monitor/hf_papers/acl_anthology/pwc/semantic_scholar/dblp}` / `TIER_2_SOURCES={rss_blogs/ontology_sources/github_trending}` / 其余三档；`parse_entries_from_section` 按 `*title*` 块边界 + 链接/星级/摘要行识别（state machine，V37.8.7 ontology_parser 同款模式）；`score_entry` 星级×10 + TOPIC_WEIGHTS 主题加权（ontology/本体/knowledge graph/agent runtime/llm infra/control plane 各 +10 分等）+ 摘要长度 tie-breaker（封顶 +10）；`fetch_pdf_text` arxiv/acl URL 派生 PDF URL + pdfplumber lazy import + `_urlopen` 加 UA header 突破 403 + `preprocess_pdf_text` 切 References/Acknowledgments/参考文献/致谢（中英文）+ Figure/Table caption 正则去噪 + 30K 截断；`fetch_html_text` bs4 lazy import + charset 探测 + article/main 标签优先 + 清 script/style/nav/footer/header/aside；`fetch_full_text` 分档 dispatch + 任何失败都 degrade 到 abstract_only 不 raise；`build_full_text_prompt`（论证链+实验对比+局限性+对本项目启发五结构）vs `build_abstract_only_prompt`（禁推测方法细节+明确"基于摘要的分析"标注），两者共享 GROUNDING_CONSTRAINT（违反即作废）；`run()` orchestrator 返回 status: ok/no_candidates/llm_failed/collector_failed，llm_failed 路径**不产出** markdown/wa_message/discord_message 成功-only 字段（MR-8 "silent-failure-is-a-bug" 契约）。(2) `kb_deep_dive.sh` (~150 行 thin wrapper) — 镜像 kb_review.sh V37.5.1 env-var heredoc 模式（禁 `echo \| python3 -` pipe+heredoc 冲突，V37.5.1 血案防御）；set -euo pipefail + notify.sh source + double-push（WhatsApp 简版 + Discord 完整版，**内容不同**）通过 `--channel whatsapp --topic deep_dive` + `--channel discord --topic deep_dive` 两次独立调用；no_candidates 推 [SYSTEM_ALERT] 但 exit 0（明日再试契约）+ llm_failed/collector_failed 推告警并 exit 1（fail-fast）；`last_run_deep_dive.json` status 写入 + rsync 备份带 V37.9.14 `movespeed_incident_capture.sh` 事故取证钩子。(3) `test_kb_deep_dive.py` (55 单测 / 10 类) — TestClassifyTier(7) + TestParseEntriesFromSection(6 含 multiple_entries/empty/no_stars/fallback_url/abstract_accumulates) + TestScoreEntry(6 含 low_stars_negative / 多主题加权叠加 / tie_breaker 封顶) + TestPdfUrlDerivation(5 含 arxiv abs→pdf 变体 + acl URL) + TestPreprocessPdfText(5 含切英文+中文 References/截断/Figure caption) + TestFetchDegrade(3 含 tier3/无 link/HTTP 失败 degrade 契约，真 monkey-patch `_urlopen` 抛 URLError) + TestPromptBuilders(4 含 grounding 约束守卫) + TestOutputBuilders(5 含 WA 长度<=1450 + Discord markdown + abstract_mode 推送标记) + TestRunOrchestrator(5 端到端：no_candidates 不产 artifacts / ok full_text 产全套 / llm_failed 不产 markdown-wa-discord / degrade abstract_only mode + degrade_reason 透传 / collector_failed on missing registry) + TestKbDeepDiveShellGuards(9 源码级：脚本存在 / [SYSTEM_ALERT] 前缀 / 禁 `\| python3 -` 反模式 / source notify.sh / set -euo pipefail / last_run_deep_dive.json / --topic deep_dive / rsync+movespeed_incident_capture / llm_failed 分支 500 字符内 exit 1 顺序锁)。③ **配置面三处改动**：(a) `jobs_registry.yaml` 新 job `kb_deep_dive`（scheduler=system / interval="30 22 * * *" HKT / tier=auxiliary / needs_api_key=true），check_registry 通过 40 jobs validated。(b) `auto_deploy.sh` FILE_MAP 双文件补齐 `kb_deep_dive.sh|$HOME/kb_deep_dive.sh` + `kb_deep_dive.py|$HOME/kb_deep_dive.py`（置于 kb_review/kb_evening 行相邻位置，保持群组一致）。(c) `notify.sh` `_notify_discord_target_for_topic()` 加入 `deep_dive) echo "${DISCORD_CH_DAILY:-}" ;;`（V37.9.16 复用 #daily 频道，语义独立便于未来分离）+ header 注释块登记新 topic。(d) `full_regression.sh` 加 `run_suite "kb_deep_dive ..." "python3 test_kb_deep_dive.py"` 置于 kb_evening 之后。④ **时间窗设计**：22:00 kb_evening → 22:30 kb_deep_dive → 00:40 Dream Map，三者零撞车；22:30 选择理由：数据最完整（全天 32 jobs 已跑）+ LLM 相对空闲（凌晨前窗口）+ 不撞 Phase 4 P3 quiet-hours-00-07 policy + 不与 22:00 kb_evening 并发争 LLM。⑤ **失败策略四种 status 分流**：(a) **no_candidates** — 无 ⭐≥4 候选（今日论文不够好）→ 推 [SYSTEM_ALERT] 提示但 exit 0，明日自动再试 (b) **PDF/HTML 抓取失败** — degrade 到 abstract_only + `build_abstract_only_prompt` 明示禁推测 + 推送内容前缀"（摘要级分析）"用户可见 + **不 block 不单独告警** (c) **LLM 失败** — [SYSTEM_ALERT] + exit 1 fail-fast（V37.5 血案防御）(d) **collector_failed** — 部署/注册表缺失 → 同 LLM 失败路径。⑥ **设计亮点**：(a) **依赖 lazy import** 让 dev 环境可跑单测（pdfplumber 和 bs4 不必装），Mac Mini 需 `pip3 install pdfplumber beautifulsoup4`。(b) **MR-8 兑现** — `import kb_review_collect as rc` 复用 5 个原语零 copy-paste，与 V37.6 kb_evening_collect.py 同款"单一真理源"模式。(c) **双通道内容差异**：WhatsApp 简版 1400 字（手机阅读优先）vs Discord 完整版 1900 字（Discord 频道归档优先），通过两次独立 notify 调用。(d) **V37.5.1 env-var heredoc 血案防御**：测试有 source 级守卫 `test_script_uses_env_var_heredoc_pattern` grep 禁止 `\| python3 -` 反模式。⑦ **累计**: 45→46 suites (+1) / 1411→1466 tests (+55) / 0 fail / governance v3.32 不变（本次无新不变式，V37.9.16 首批上线后 shadow 观察一周再考虑加 INV-DEEP-001，与 V37.9.15 three_gate 同款渐进路径）/ 安全 95/100 / VERSION 0.37.9.15 → 0.37.9.16。⑧ **Mac Mini 部署路径**：auto_deploy 2min 轮询同步 kb_deep_dive.{sh,py}，22:30 首次触发产出 `~/.kb/deep_dives/2026-04-24.md` + WhatsApp + Discord #daily 推送。首次运行预期：22:30:00 读 sources → picker 选出 ⭐≥4 候选 → 如属 arxiv/hf/acl/pwc 尝试 PDF 抓取（若 pdfplumber 未装 degrade 到 abstract）→ LLM 分析 → 3 通道产出。⑨ **下次 (V37.9.17) 候选**：(a) Mac Mini `pip3 install pdfplumber beautifulsoup4` 安装观察首批 PDF 抓取成功率（dev 环境 fetch 路径单测覆盖完整但生产 URL 多样性未验证）(b) INV-DEEP-001 `daily-deep-dive-fails-loud-not-silent`（shadow 观察一周后加 meta_rule=MR-8/MR-9 的治理不变式，同 INV-EVENING-001 / INV-REVIEW-001 模式）(c) picker 调优：观察一周选出的 topic 分布，调整 TOPIC_WEIGHTS (d) 如 no_candidates 告警过频 → 放宽 MIN_STARS=4 至 3 或扩展候选池到最近 2 天 (e) Phase 4 P3 shadow→on 推进（V37.9.15 延续观察）。|
| V37.9.15 | 2026-04-24 | **Phase 4 P3 三阶段门控 shadow wiring — policy 引擎从"可查询"升级为"在请求路径上被调用并产可观测 log"** — ① **触发**：V37.9.12 Phase 4 P1 (`evaluate_policy()` 三纯函数 + max-tools-per-agent 首切换) + V37.9.13 Phase 4 P2 (6 个 context evaluator + max-tool-calls-per-task 第二切换) 只在启动期解析 static policy 阈值缓存到模块常量；contextual/temporal policy 的 6 个 evaluator 已注册但**生产环境零调用**—只被单测覆盖。下次 session focus 明确登记 "[首选] Phase 4 P3 三阶段门控"。② **新文件 `ontology/three_gate.py`**（~280 行，零 I/O 零状态纯函数模块）：`GateFinding` namedtuple (gate/policy_id/verdict/action/reason/enforced) + 3 个 gate 函数 `pre_check/runtime_gate/post_verify` + `_collect_findings()` 共享 helper 遍历 policy_pairs → `_safe_evaluate_policy()` 捕获 engine 异常 → 提取 static policy signal 与 limit 比对（`_STATIC_SIGNAL_KEYS` 映射 tool_count/tool_call_count/body_bytes 三指标）→ 产出 pass/flag verdict。`ONTOLOGY_GATES_MODE` env 三档 off/shadow/on（默认 `shadow`，未知值 fall back shadow 观察优先），`_VALID_GATE_MODES = ("off", "shadow", "on")` 常量。与 `ONTOLOGY_MODE` 解耦（允许 ONTOLOGY_MODE=on 但 gates=off 的灰度组合）。`format_findings_for_log()` 产 `[gate:pre_check] N findings: policy_id=verdict(reason) ...` 单行便于 `grep [gate:]` 运维排查。**FAIL-OPEN 契约**：engine 异常经 `_safe_evaluate_policy` 吃掉返回 None → gate 降级 verdict=pass + reason="engine_unavailable"，绝不冒泡到 tool_proxy。③ **三 gate policy 分配**：pre_check→{alert-context-isolation, quiet-hours-00-07} / runtime_gate→{max-tools-per-agent, max-tool-calls-per-task, max-request-body-size} / post_verify→{alert-context-isolation (扫响应检测 [SYSTEM_ALERT] echo)}。④ **`tool_proxy.py` wiring**（最小入侵，两处调用点）：(a) `importlib.util.spec_from_file_location` lazy-load three_gate（ontology/ 缺失时 log WARN 不崩 + _three_gate = None 哨兵）(b) `_run_gate(gate_fn_name, context, rid, response=None)` 帮助函数：None 哨兵检查 + getattr(fn) + try/except Exception + log `[{rid}] {findings_line}` / `WARN: gate X raised` (c) do_POST 第一处：`raw = json.dumps(body).encode()` 后构造 context {messages, tool_count=len(body.tools), body_bytes=len(raw), hour=datetime.now().hour} 调 `_run_gate("pre_check", ...)` + `_run_gate("runtime_gate", ...)` (d) do_POST 第二处：`rj = json.loads(resp_body)` 后 `_run_gate("post_verify", {"messages": body.messages}, rid, response=rj)` 必须在 fix_tool_args 之前（V37.9.15 顺序锁，观察 LLM 原始响应未被 alias 修复改写）。⑤ **`policy_ontology.yaml::meta` 升级**：version 0.2-p2-partial → 0.3-p3-partial，status → gates_wired_shadow_mode，新增 `policies_observed_by_three_gate` 分 gate 清单 + `gates_feature_flag` 声明（`values: ["off", "shadow", "on"]` 引号避 YAML 1.1 bool 陷阱 + decoupled_from ONTOLOGY_MODE）+ `next_step` 指向 P3.1 (shadow→on 一周观察) / P3.2 (第三条 policy 切换) / P4 (真正 enforcement 路径)。⑥ **`INV-GATE-001 three-gate-wiring-observability`**（meta_rule=MR-7, severity=high, verification_layer=[declaration, runtime], **14 checks**）：7 declaration 守卫 three_gate.py（Phase 4 P3 头注释 / `def pre_check(context)` / `def runtime_gate(context)` / `def post_verify(context, response=None)` / `_DEFAULT_GATE_MODE = "shadow"` / `_VALID_GATE_MODES = ("off", "shadow", "on")` / `def _safe_evaluate_policy`）+ 5 declaration 守卫 tool_proxy.py（`spec_from_file_location("_three_gate"` / `def _run_gate(` / `_run_gate("pre_check"` / `_run_gate("runtime_gate"` / `response=rj`）+ 1 declaration 守卫 policy_ontology.yaml P3 meta + **1 runtime python_assert** subprocess 跑 `unittest ontology.tests.test_three_gate test_tool_proxy.TestThreeGateWiring` 覆盖 66 场景端到端。⑦ **66 新单测**：`ontology/tests/test_three_gate.py` 57 tests (TestGatesMode×7 含默认 shadow + case-insensitive + whitespace + 未知 fall back shadow + TestGateFinding×3 含 immutable + 字段顺序稳定 + TestOffModeShortCircuit×4 含 off 不调 engine + TestShadowModeNeverEnforces×2 反证 on 模式 enforced=True + TestPreCheck×8 含 alert 检测 + quiet-hours 上下文全分支 + missing hour→verdict pass + None context tolerant + TestRuntimeGate×6 含 signal≤limit boundary 合规 / signal>limit flag / 缺信号 tolerant + TestPostVerify×5 含 assistant [SYSTEM_ALERT] 回声检测 + malformed response tolerant + TestFailOpen×4 真 monkey-patch engine.evaluate_policy 抛异常 + TestExtractSignal×7 含 bool 排除于 int + TestExtractAssistantText×6 + TestFormatFindingsForLog×3 + TestModuleIntegration×2 公开 API + gate name 稳定性) + `test_tool_proxy.TestThreeGateWiring` 9 tests（lazy-load importlib.util 模式 / `_run_gate` 定义 + fail-open / 三处调用点 + context 字段 / post_verify 顺序锁 source 级位置比较 / Phase 4 P3 日志标记 / 默认 shadow / shadow 不 enforce）。**全量回归 45 suites / 1411 tests (+66) / 0 fail**。⑧ **MR-8 正面兑现**：`_collect_findings` 共享 helper 让三 gate 复用同一遍历逻辑，policy 列表通过 `_PRE_CHECK_POLICIES / _RUNTIME_GATE_POLICIES / _POST_VERIFY_POLICIES` 数据驱动 — 不 copy-paste 三份遍历循环 (对比 V37.9.12/13 的 `_resolve_max_tools_limit` 与 `_resolve_max_tool_calls_per_task_limit` 故意写两份是因为需要独立 policy-specific 启动日志，这里复用是因为三 gate 行为对称)。⑨ **MR-7 兑现**：policy 引擎从"可查询但不在请求路径"升级为"被请求路径调用并产 `[gate:*]` 可观测 log"。每请求 stdout 将出现形如 `[proxy] ... [rid] [gate:pre_check] 2 findings: alert-context-isolation=pass(applicable=False) quiet-hours-00-07=pass(context_missing_hour)` 的 runtime 观察数据。⑩ **累计**: 44→45 suites (+1) / 1345→1411 tests (+66) / 0 fail / governance v3.31→v3.32 / 60→61 invariants (+1 INV-GATE-001) / 273→297 checks (+14 INV-GATE-001 + 10 audit_metadata 漂移回填) / 15→16 meta rules (MR-16 计入) / 安全 95/100 / VERSION 0.37.9.14 → 0.37.9.15。⑪ **Mac Mini 部署路径**：`ontology/three_gate.py` 走 V37.9.12.1 发现的 symlink 机制（`$HOME/ontology` → `$HOME/openclaw-model-bridge/ontology`）无需 FILE_MAP。`tool_proxy.py` 在 FILE_MAP 里下次 auto_deploy 2min 轮询自动同步。同步后 proxy 启动日志将出现 `[proxy] three_gate loaded (mode=shadow) (Phase 4 P3 shadow wiring active)`，每次 `/v1/chat/completions` 请求末尾出现三行 `[gate:*]` findings 供 Mac Mini 运维层 `grep [gate:]` 观察一周。⑫ **下次 (V37.9.16) 候选**：(a) shadow → on 推进（基于一周生产观察），需要 enforcement 行为定义（flag verdict 时 proxy 做什么 — reject request / truncate further / 只 alert 不阻塞？） (b) 第三条 policy 切换 max-request-body-size 或 alert-context-isolation 从 proxy_filters.filter_system_alerts 等现有 enforcement 点**查询**而非**硬编码** (c) Phase 4 P4 四耦合点（Pre→Runtime→Post 加入 audit_log 带 policy_evaluated + rule_chain）。|
| V37.9.14 | 2026-04-23 | **MOVESPEED SSD rsync 失败取证埋点（证据导向修复，不盲猜根因）** — ① **触发**：V37.9.4 APFS 重建理论上闭环 exfat fskit EPERM，但 2026-04-22 ACL cron 日志再次出现 `Operation not permitted`，顶层 `/Volumes/MOVESPEED/KB/` 权限 OK 但 rsync 子操作失败 — V37.9.4 假说被证伪，但根因不明（可能 cron FDA / APFS mount option / USB 节能 / Time Machine 并发等多种假设）。② **方法论选择**：按原则 #28 理解再动手 + 原则 #26 异常分析宪法，在证据不足时**拒绝盲目修复**。transient EPERM 一旦自愈就抓不到现场（V37.9.4 的 4/21 11:40 手动 rsync 立即成功即是证据），所以选"埋取证点"而非"猜根因修"。③ **新 helper `movespeed_incident_capture.sh`**（bash 3.2 兼容 macOS 默认，never-fail 契约，~130 行）：rsync 失败时追加一行 JSONL 到 `~/.kb/movespeed_incidents.jsonl`，字段含 timestamp / caller basename / exit_code / `mount` 输出 / `diskutil info` 摘要 / `ls -la` 顶层和 KB 子目录 / `df -h` / **顶层和 KB 子目录独立 touch 权限探测**（区分"全盘 EPERM"vs"部分子目录 EPERM"—— V37.9.4 血案之后真正需要的那块证据）/ `rsync/backupd/mds/fseventsd/tmutil/Spotlight` 进程快照（并发竞争诊断）/ `sw_vers` + env 摘要。Python3 argv 参数传递避开 shell 转义坑，所有诊断命令 `2>/dev/null` + 缺失字段为空串，`mktemp -d` 失败走 last-ditch minimal record 分支。10MB 自动 rotation 到 `.1`。④ **20 处 rsync fail-loud 站点批量升级**（18 文件 20 处，freight 2 处）：旧 `rsync ... 2>&1 \|\| echo "WARN: SSD ... (exit=$?)" >&2` → 新 `rsync ... 2>&1 \|\| { _rc=$?; echo "WARN: SSD ... (exit=$_rc)" >&2; "$HOME/movespeed_incident_capture.sh" "$_rc" "$0"; }`。关键 `_rc=$?` 必须在 echo 之前捕获（echo 执行后 $? 变成 0）。脚本 `set -e` 安全（helper 契约 never-fail exit 0）。覆盖 kb_save_arxiv/kb_dream/kb_evening/kb_inject/kb_review/run_hn_fixed + jobs/{hf_papers,semantic_scholar,dblp,ontology_sources,karpathy_x,rss_blogs,arxiv_monitor,freight_watcher×2,acl_anthology,github_trending,ai_leaders_x,openclaw_official×2}。⑤ **INV-BACKUP-001 扩展 check 4 + check 5**（meta_rule 从 MR-4 继承，添加 MR-9 正面兑现维度）：(a) declaration python_assert 全局扫 rsync+MOVESPEED 的 .sh 必须调用 `movespeed_incident_capture.sh`（helper 自身无 rsync 自动豁免，避开 V37.3 exec scope trap 用 iterative loop） (b) runtime python_assert 端到端 subprocess 跑 mock caller → rsync 失败 → 断言 caller exit 0 + WARN 在 stderr + JSONL 行写入 + JSON 解析成功 + caller basename 正确。⑥ **MR-9 正面兑现扩展**：V37.9.13 把 MR-9 应用到进程管理层（launchctl 单一 manager），V37.9.14 把 MR-9 应用到取证层（事故现场捕获必须走单一 helper，20 处不再各自 inline 自己的取证逻辑）—— 从 MR-8 copy-paste-is-a-bug-class 反例升级为 MR-9 正面兑现。⑦ **26 新单测** `test_movespeed_incident_capture.py` 加入 full_regression：TestHelperExists(4) + TestHelperContract(8: 无 args 不崩 / shell metachar 不执行 / tmpdir 坏不挂 / JSONL 有效 / append 语义 / basename 抽取 / ISO8601 UTC) + TestRotation(2: MAX_SIZE 触发 + 小文件不触发) + TestBashCompat(6: 禁 ${var@Q} / ${^^} / declare -A / mapfile + WARN: SSD 字样合规 + 反模式不自伤，注释行智能豁免) + TestInvariantCoverage(4: 20 个预期站点存在 + 都调用 helper + helper 在 fail-loud 分支内 + 无未声明新增站点) + TestFileMapCoverage(1: auto_deploy FILE_MAP 含 helper) + TestGovernanceExtension(1: governance_ontology.yaml 引用 helper)。⑧ **auto_deploy.sh FILE_MAP 补齐**：`movespeed_incident_capture.sh | $HOME/movespeed_incident_capture.sh`。注意 comment wording 避免"rsync"+"MOVESPEED"co-occur 误触 INV-BACKUP-001 discovery scan。⑨ **accumulated**: 43 → **44 suites (+1)** / 1319 → **1345 tests (+26)** / 0 fail / governance 60 invariants（不变，INV-BACKUP-001 扩展 2 checks）/ 安全 95/100 / VERSION 0.37.9.13 → 0.37.9.14。⑩ **下次 transient EPERM 复发**（可能 1 天也可能数周）：`cat ~/.kb/movespeed_incidents.jsonl` 将给出完整现场数据 — 顶层 probe exit 码 vs KB probe exit 码的差异即可立即判定是"全盘 EPERM"还是"子目录特定 EPERM"，`procs` 字段显示并发 Time Machine/Spotlight 证据，`mount` 输出显示当时是否 readonly remount，`df` 显示是否空间耗尽。有这份数据后，V37.9.15 可以精确修根因而非继续猜。⑪ **原则 #28 正向兑现**：本次修复严格遵守"修复前必答三问"——(1) 这问题之前存在吗？V37.9.4 已修过部分，4/22 复发证明假说不完整（回答：是，但 V37.9.4 的修复不完整） (2) 我的哪个改动引入的？不是我的改动，是底层环境 transient 问题（回答：非代码引入） (3) 最小修复是什么？埋取证点而非盲修（回答：收证据层，不改行为层）。禁止了"看到报错就改代码"的反模式。⑫ **备份监控延伸**：该 JSONL 未来可被 `job_watchdog.sh` 或 `kb_status_refresh.sh` 消费 — 如累积 ≥ 5 条 incident 在 24h 内即触发连续性告警（登记为 V37.9.15 候选）。|
| V37.9.13+arch-cleanup | 2026-04-23 | **restart.sh 架构清理 — 单一 manager 契约（消除 nohup + launchd 双管理血案）** — ① **触发**：V37.9.12.1 Mac Mini 实测发现 `com.openclaw.adapter.plist` + `com.openclaw.proxy.plist` KeepAlive 管理的 launchd 进程，与 `restart.sh nohup python3 adapter.py/tool_proxy.py` 启动的 manual 进程同时抢占 :5001 / :5002 端口，launchd 侧持续显示 crash-loop（两路进程 ExitCode ≠ 0 反复重启）。status.json unfinished 登记 "V37.9.13 候选 (架构清理): restart.sh 改用 launchctl kickstart 替代 nohup python 启动 adapter/proxy"。② **修复方案**：restart.sh 新增 `restart_via_launchd <label> <port> <plist> <display>` helper 函数（返回 0=成功 / 1=launchd 失败 / 2=launchctl 或 plist 不可用，signal to caller to fallback），主路径用 `launchctl kickstart -k gui/$(id -u)/<label>` 触发 launchd 内部重启（modern idempotent API），首次加载时 `bootout + bootstrap` 兜底。③ **健康验证扩展**：V37.8.13 Gateway 的 5×3s 健康验证循环从 Gateway-only 扩展到 adapter/proxy — helper 内置 `for _attempt in 1 2 3 4 5; sleep 2; curl http://localhost:$port/health` 循环，HTTP 200 判定健康。Gateway 的验证逻辑完整保留（`V37.8.13 Post-bootstrap health verification` 注释 + `GATEWAY_HEALTHY=false` + 5×3s curl 循环 + 失败不 exit 1）。④ **向后兼容 fallback**：plist 缺失（dev 环境 / 未装 plist / 恢复场景）时 helper 返回 2，调用方用 `_ad_rc=$?` / `_px_rc=$?` 捕获并走 `nohup python3 adapter.py` 原路径；set -e 下用 `|| _ad_rc=$?` 安全捕获避免脚本整个退出。⑤ **#48703 hotfix 段完整保留**：V37.9.13 重构与该段无关，不得误删 `const listeners = /* @__PURE__ */ new Map()` 的 sed 补丁逻辑。⑥ **INV-RESTART-001 新增**（meta_rule=MR-9 state-writes-go-through-helper，severity=high，verification_layer=[declaration, runtime]）8 checks：restart_via_launchd helper 定义 + kickstart -k 源码模式 + adapter/proxy 调用参数精确匹配（`"com.openclaw.adapter" 5001` / `"com.openclaw.proxy" 5002`）+ V37.9.13 注释存在 + **2 个 runtime python_assert**：regex 扫所有 `nohup python3 ...adapter.py` / `...tool_proxy.py` 出现位置，前 300 字符内必须含 `plist not found` 或 `_ad_rc`/`_px_rc` 字样（证明在 fallback 分支内，V37.9.12.1 双管理血案的源码级防线）+ **set -e 安全模式守卫** `restart_via_launchd "com.openclaw.adapter" \|\| _ad_rc=$?` 必须用 `\|\|` 捕获返回码。⑦ **20 新单测** `test_restart_launchd.py`：TestRestartVialaunchdHelper(6 测试 kickstart -k / bootstrap fallback / 健康验证循环 / return 2 契约 / launchctl 可用性检测) + TestAdapterViaLaunchd(3) + TestProxyViaLaunchd(2) + TestSingleManagerInvariant(5 测试 nohup adapter/proxy 必在 fallback 分支 + V37.9.13 注释 + V37.8.13 Gateway 逻辑保留 + #48703 hotfix 保留) + TestShellExecutability(3 bash -n + set -euo pipefail + `\|\|` 模式) + **TestRuntimeHelperBehavior(1)** 实际 subprocess 跑 helper，用 awk 提取函数体 eval 后调用空 plist 场景，断言返回 2 不崩溃（用环境变量 `RESTART_SH` 注入路径避免 Python `.format()` 与 awk `/^}/` 花括号冲突）。⑧ **full_regression.sh 接入** `restart_launchd` suite。⑨ **accumulated**: 42 → **43 suites (+1)** / 1299 → **1319 tests (+20)** / 0 fail / governance 59→60 invariants / 安全 95/100 / VERSION 保持 0.37.9.13（同日累积改动）。⑩ **MR-9 正面兑现**：本次清理是 MR-9 state-writes-go-through-helper 的扩展 — 原 MR-9 针对 KB sources 写入（14 个 cron 走 `kb_append_source.sh` 不直 `>>` 重定向），V37.9.13 把"单一 manager"原则应用到进程管理层 — 进程启停必须走 launchd 唯一管理通道，不允许脚本同时用两种机制管理同一服务。⑪ **下次 Mac Mini 验证**：用户合并本 PR 后在 Mac Mini 同步 + `bash ~/restart.sh`，应看到 `[restart] Adapter: launchctl kickstart -k gui/501/com.openclaw.adapter` 日志 + `[restart] Adapter healthy (HTTP 200 after Nx2s)`；`launchctl list \| grep openclaw` 应显示 com.openclaw.adapter / proxy 的 PID 稳定不再 crash-loop。|
| V37.9.13 | 2026-04-23 | **Phase 4 P2 context evaluator + 第二条 policy 切换（max-tool-calls-per-task）** — ① **触发**：V37.9.12 Phase 4 P1 完成后 status.json 焦点明确登记两项 P2 任务 (context evaluator + 第二条 policy 切换)，验证 V37.9.12 wiring 模式可扩展而非一次性 hack。② **engine.py 扩展**：新增 6 个 context 匹配器纯函数 — `_eval_quiet_hours`（temporal：`context.hour ∈ [0,7)` 或 `context.now.hour`，含 int 类型转换 / 范围 [0,23] 校验 / 半开区间 7 点严格判否）/ `_eval_task_match`（temporal：`task==expected`）/ `_eval_has_alert`（contextual：messages 中任一 content 含 `[SYSTEM_ALERT]`，支持 str content 和 OpenAI content blocks 双格式）/ `_eval_has_image`（contextual：优先 `has_image` flag，否则扫 messages 里的 `image_url`/`image` 类型 block）/ `_eval_need_fallback`（contextual：显式 `need_fallback` flag）/ `_eval_data_clean_keywords`（contextual：10 个中英文关键词大小写不敏感）。③ **`_CONTEXT_EVALUATORS` dispatch table** 把 6 条 policy 映射到 matcher：quiet-hours-00-07 / dream-map-budget / alert-context-isolation / multimodal-routing / fallback-chain-capability / data-clean-tool-injection。未注册 policy 走 `reason="no_context_evaluator_registered"`——P2 承诺是"可扩展路径"不是"全部做完"。④ **`evaluate_policy()` 三档扩展**：static 仍 applicable=True；temporal/contextual 分三档 — context=None → `needs_context_evaluator`（向后兼容 P1）/ evaluator 未注册 → `no_context_evaluator_registered` / evaluator 抛异常 → `evaluator_error: <type>` 不冒泡。每个 evaluator 缺字段都有具体 reason（`context_missing_hour` / `context_hour_invalid_type` / `context_messages_must_be_list` 等）便于运维调试。⑤ **第二条 policy wiring**：`config_loader.py` 加 `MAX_TOOL_CALLS_PER_TASK`（读 config.yaml 已声明但从未加载的 `max_tool_calls_per_task: 2`）；`proxy_filters.py` 新增 `_resolve_max_tool_calls_per_task_limit()` 纯镜像 V37.9.12 5 档 safe-fallback 模式（off / load_failed / policy_miss / shadow / on+valid）+ `_MAX_TOOL_CALLS_PER_TASK_RESOLVED` 启动期一次性计算常量。启动日志 `[proxy] max-tool-calls-per-task: using ontology limit=2 (Phase 4 P2 wiring active)` 与 P1 日志配对。⑥ **enforcement 与 wiring 解耦**：当前 Python 尚无 tool_call 计数 enforcement 点（config.yaml 声明但未落实），P2 只做"阈值 wiring"不做"新 enforcement"——有意设计让 wiring 模式先稳定再考虑 enforce 何时何地。⑦ **policy_ontology.yaml::meta 升级**：version 0.1-skeleton → 0.2-p2-partial，status → wired_static_and_registered_contextual，新增 policies_with_context_evaluator 6 条 + policies_wired_via_proxy_filters 2 条清单，next_step 指向 Phase 4 P3 三阶段门控。⑧ **47 新单测**：test_engine_phase4.py +37（TestContextEvaluatorQuietHours×9 含 7 点 boundary + datetime 解析 + 类型/越界 reason / TestContextEvaluatorAlertIsolation×5 含 content blocks 双格式 / TestContextEvaluatorMultimodal×5 / TestContextEvaluatorDreamBudget×3 / TestContextEvaluatorDataCleanKeywords×4 / TestContextEvaluatorFallbackChain×3 / TestContextEvaluatorUnregistered×1 契约"未注册 policy 不崩溃" / TestContextEvaluatorExceptionSafe×1 契约"evaluator 抛异常不冒泡" / TestMaxToolCallsPolicyWiring×6）+ test_tool_proxy.py +9（TestPolicyDrivenMaxToolCallsPerTask 5 档 safe-fallback 各自 monkey-patch + 契约"查询正确 policy_id" + **MR-8 copy-paste 防御源码守卫** `assertNotIn("max-tools-per-agent", src)` 防止 P2 resolver 意外指向 P1 policy）+ test_phase4_ontology_skeleton.py +1（`test_max_tool_calls_per_task_is_2` 横向一致性）。⑨ **accumulated**: 42 suites / 1252 → **1299 tests (+47)** / 0 fail / 安全 95/100 / VERSION 0.37.9.12 → 0.37.9.13。⑩ **MR-8 正面案例兑现**：`_resolve_max_tool_calls_per_task_limit` 与 `_resolve_max_tools_limit` 95% 逻辑相同，但写两份独立函数 + 源码级 grep 守卫优于泛化 `_resolve(policy_id, default)` —— 每条 policy 启动期日志要独立字符串便于运维 grep、未来可能为特定 policy 加自定义日志/告警/指标、5 档 fallback 分支抛出的 WARN 要追溯到具体 policy 名字。⑪ **Phase 4 P3 候选**：(a) 三阶段门控（pre-check → runtime-gate → post-verify）把 `evaluate_policy(policy_id, context)` 接入请求管线 (b) 第三条 policy 切换候选 `max-request-body-size`（已有 explicit limit）或 `alert-context-isolation`（已有 ordering_constraint）(c) MR-7 兑现：为每个 context evaluator 添加 governance_ontology.yaml 对应 INV-* 让 wiring 本身被治理监控。|
| V37.9.12 | 2026-04-22 | **Phase 4 P1 wiring 落地 — engine.py 三纯函数 API + max-tools-per-agent policy 首切换** — ① **触发**：V37.9.9 完成 Phase 4 骨架（domain_ontology.yaml + policy_ontology.yaml + 15 单测）但 `engine.py` 未实现 wiring API，两个 YAML 停留在声明层没有消费者，status.json unfinished 登记 "Phase 4 P1+ wiring: engine.py 新增 load_domain_ontology() + find_by_domain() + evaluate_policy() 三个纯函数 API"。② **三纯函数 API 实现**（engine.py 第 650~800 行）: (a) `load_domain_ontology(path=None) -> dict` 加载 YAML 无缓存 (b) `find_by_domain(domain_name, ontology=None, path=None) -> list` 六域归一化: Actor→instances / Resource→categories / Task→taxonomy / Memory→layers / Provider.types 字符串包装成 `{"id":s}` / Tool 主动返回 [] 因 source_of_truth=tool_ontology.yaml / 未知域返回 [] 不抛 (c) `evaluate_policy(policy_id, context=None, policy_data=None, path=None) -> dict` 12-key 稳定返回: policy_id / found / type / hard_limit / limit / applicable / rule / rationale / enforcement_site / governance_invariant / scope / reason；Phase 4 P1 覆盖 static 策略（applicable=True），contextual/temporal 返回 applicable=None+reason=needs_context_evaluator 留给 Phase 4 P2；load 失败返回 found=False+reason=load_failed 不抛异。(d) `_parse_limit_from_rule()` helper 从 rule 文本提取 ≤/<=/< 数值（下划线千位分隔支持）作为 YAML 未显式声明 `limit` 字段时的回退。③ **policy_ontology.yaml 3 条 hard_limit policy 加 `limit` 字段**：max-tools-per-agent=12 / max-tool-calls-per-task=2 / max-request-body-size=200000，作为机器可读阈值，`evaluate_policy().limit` 优先读此字段>`value`>regex-parse rule。④ **proxy_filters.py 首切换**: filter_tools 的硬性截断阈值从 `_CFG_MAX_TOOLS` 改为 `_MAX_TOOLS_RESOLVED`（模块启动时由 `_resolve_max_tools_limit()` 解析）。Safe-fallback 5 档: (i) ONTOLOGY_MODE=off → config (ii) _onto_mod 未加载 → config+log warn (iii) policy not found / limit=None → config+log warn (iv) shadow mode → config 但 log observe ontology vs config 差异 (v) on mode + valid limit → ontology limit+log drift if any。启动日志: `[proxy] max-tools-per-agent: using ontology limit=12 (Phase 4 P1 wiring active)`。改 12 只需改 policy_ontology.yaml 一处（Phase 4 终态部分兑现）。⑤ **38 新单测** `ontology/tests/test_engine_phase4.py`: TestLoadDomainOntology(4)/TestLoadPolicyOntology(1)/TestFindByDomain(9)/TestEvaluatePolicyContract(7)/TestMaxToolsPolicyWiring(6)/TestParseLimitFromRuleFallback(7)/TestEvaluatePolicyLimitFallbackChain(4)。**11 新单测** `test_tool_proxy.TestPolicyDrivenMaxTools`: 5 档回退路径每档独立 monkey-patch 测试 + filter_tools 截断/custom tools 保留 + 源码守卫 `>\s*_CFG_MAX_TOOLS` 禁止回退模式。**2 扩展单测** `test_phase4_ontology_skeleton.py`: hard_limit 必须有 `limit` 字段 + max-tools-per-agent.limit==12 横向一致。⑥ **full_regression.sh 接入** `engine_phase4` suite。⑦ **accumulated**: 41→42 suites / 1201→**1252 tests (+51)** / 0 fail / 38/38 invariants / 安全 95/100 / VERSION 0.37.9.11 → 0.37.9.12。⑧ **部署策略（Mac Mini 实测结果修正 V37.9.12.1）**：`$HOME/ontology` 是 **symlink → `$HOME/openclaw-model-bridge/ontology`**，`ontology/` 未入 FILE_MAP 但通过 symlink 跨越——`$HOME/proxy_filters.py` 能直接找到 `$HOME/ontology/engine.py`。**Phase 4 P1 wiring 在 Mac Mini 真实生效**（不是 safe-fallback），`/health` 返回 `version: 0.37.9.12`，日志确认 `[proxy] max-tools-per-agent: using ontology limit=12 (Phase 4 P1 wiring active)`。宪法"删除后原系统正常"仍成立（只需删 symlink，系统走 config fallback）。**同时发现 launchd plist 配置债**：`com.openclaw.proxy.plist` 无 `ONTOLOGY_MODE` env 但 launchd 进程仍使用 shadow 模式（来源未追到，疑似 bytecode cache 或系统 env 继承），通过 `PlistBuddy Add :EnvironmentVariables:ONTOLOGY_MODE string on` 显式覆盖已修复；launchd 进程 PID 73456 Python 3.9 现以 on 模式 serve（Phase 4 wiring active 日志生效）。附带发现 adapter plist 同款双管理冲突（restart.sh manual 进程 + launchd KeepAlive 抢端口），登记 V37.9.13 架构清理。⑨ **元价值**: Phase 4 P1 是 V37.9.9 骨架到 V3 路标"pip install ontology-engine"终极目标的关键桥梁 — 从"本体只描述"升级为"本体可查询"。Phase 4 P2 下次推进: (a) contextual/temporal policy 的 context evaluator 实现（hour_of_day / request context matcher）(b) 选第二条 policy 做切换（max-tool-calls-per-task 最相似路径）(c) `evaluate_policy().applicable` 真值覆盖到 100% 策略。|
| V37.9.11 | 2026-04-22 | **MRD-RESERVED-FILES-001 闭环：BOOTSTRAP.md + SKILL.md 加入保留文件列表** — ① **触发**：V37.9.9 Mac Mini governance --full 扫 OpenClaw dist/*.js 639 个 JS 文件，发现 3 个 runtime 保留文件模式（`f.name === "X.md"`），但 `proxy_filters.RESERVED_FILE_BASENAMES` 只登记了 HEARTBEAT.md，漏掉 BOOTSTRAP.md + SKILL.md。MRD 连续 warn 提示未闭环。② **同类风险**：BOOTSTRAP.md / SKILL.md 虽未发生血案，但 MRD 源码扫描证实它们与 HEARTBEAT.md 同属 OpenClaw runtime 保留文件 —— LLM 写入会触发 runtime bootstrap/skill 机制，同款 13h 静默风险。③ **四层扩展**：(a) `proxy_filters.py::RESERVED_FILE_BASENAMES` 从 1 个扩到 3 个 basename (b) `RESERVED_FILE_SAFE_CONTENT` 从 HEARTBEAT-specific 改为通用 comment-only 骨架（首行从 `# HEARTBEAT.md` 改为 `# OpenClaw runtime reserved file — comments-only safe placeholder`，3 文件共享同一安全占位） (c) `SOUL.md 规则 11` 从单文件扩展到 3 文件条款，加入 basename 明细表 + 更新禁止行为 (d) **INV-HB-001 扩展 4 checks**：2 个 file_contains（BOOTSTRAP.md / SKILL.md 字面量存在）+ 1 个 runtime python_assert（for-loop 遍历 2×basename × 3×path_prefix = 6 场景 + edit 工具同款覆盖，避开 V37.3 exec scope trap）。④ **8 新 test_tool_proxy 单测** `TestReservedFileWriteBlock` 扩展：basename 常量守卫 ×2 / 精确集合契约（禁止漂移必须走 MRD） / BOOTSTRAP workspace 路径拦截 / SKILL home 路径拦截 / 2 文件 edit 工具拦截 / fix_tool_args BOOTSTRAP 恶意内容（`exec('rm -rf /')`）完全清除 / SAFE_CONTENT 文件无关性（首行不硬编码特定文件名）。⑤ **累计**: governance 59 inv / 283→287 checks (+4) / 41 suites / **1201 tests (+8)** / 0 fail / 安全 95/100 (Mac Mini 应保持 100/100). ⑥ **元价值**: MR-15 从"血案后 post-hoc 响应"进化为"源码扫描 pre-emptive 覆盖"— MRD-RESERVED-FILES-001 主动发现 OpenClaw 新增的 runtime 保留文件而不是等血案。此次闭环是 MR-7 (governance-execution-is-self-observable) 的生产级兑现：治理工具主动告警 → 人类修复 → 治理自证闭环。|
| V37.9.10 | 2026-04-22 | **audit_log 原子导出 + fsync 持久化加固（数据完整性 13→15 满分）** — ① **触发**：V37.9.9 Mac Mini 生产验证 security_score 跃升 93→98/100（bandit 已装 + 32 test suites），但 `数据完整性 13/15` 仍是唯一未满分项（`atomic_count=2` 因 audit_log.py 使用 append-only JSONL 未含 `os.replace`）。② **有意义的修复（非凑分数）**：(a) `audit()` 写入后加 `f.flush() + os.fsync(f.fileno())` + try/except OSError 兜底 tmpfs 不支持 fsync —— 防止 cron 环境进程崩溃时内核缓冲区尾部记录丢失 (b) 新增 `snapshot(dest_path)` 函数 — 用 `tmp + os.replace` 真原子模式导出审计日志快照到目标路径，保证"完整有效 JSONL 或根本不修改"，适用于备份/跨主机同步 (c) 新增 `--snapshot DEST` CLI 模式。③ **7 新单测 TestSnapshot**：缺审计文件返回 ok=False（非抛异常）/ snapshot 字节级一致副本 / 不留 .tmp 残余（atomic pattern 契约）/ 自动创建目标目录 / 覆盖已存在目标（os.replace 语义）/ 源码级守卫 os.replace + def snapshot / 源码级守卫 os.fsync。④ **累计**: security_score 数据完整性 13→15 / 总分 93→95（dev）/ 98→100（Mac Mini 预期）/ 41 suites / **1193 tests (+7)** / 0 fail / VERSION 0.37.9.9 → 0.37.9.10。⑤ **元价值**: 原则 #18 "补证据而非补功能"的正向兑现 — 每个改动对应真实运维场景（fsync=durability / snapshot=backup），不是为了让 grep 检查通过。|
| V37.9.9 | 2026-04-22 | **路线 C Step 4: security_score library 直调 + Ontology Phase 4 骨架** — ① **任务 A（Mac Mini V37.9.8 验证）**：ACL cron 今日 HKT 9:30:00 准时触发 + 10 篇论文推送成功（4/15 失败的任务复活；V37.9.8 调宽 28d 阈值策略生效）；auto_deploy heartbeat 每小时整点完美出行（`Wed Apr 22 06:00:06 ~ 10:00:06 HKT heartbeat: no change`）；watchdog 真实日志名为 `gateway_watchdog.log`（非 job_watchdog，对清单做修正）；**新发现 MOVESPEED rsync Operation not permitted 间歇性复发**（V37.9.4 APFS 重建后理论闭环但又报错，顶层权限 OK 但 rsync 子操作失败，登记观察）。② **任务 C（security_score 库化）**：INV-SEC-001 check 2 从 `subprocess.run(["python3","security_score.py"])` + 中文正则 parse `"安全评分：(\d+)/100"` 升级为 `from security_score import compute_score` 直调 + 从 `security_config.total_threshold` 读阈值。三问题一并解决：(a) 消除 ~200ms fork 开销每次 audit (b) 消除正则 parse 失败静默 pass 的塌陷盲区 (c) 消除硬编码 90 与 YAML 阈值漂移风险。三个 INV-SEC-001 runtime check 现全部 library + YAML 真理源统一。**3 新 regression test** `TestInvSec001UsesLibraryDirectCall`：(1) `test_no_subprocess_call_*` 锁定 active code 禁止 subprocess 反模式（智能跳过注释/文档块） (2) `test_inv_sec_001_imports_security_score_library` 契约 (3) `test_inv_sec_001_reads_total_threshold_from_yaml` 禁硬编码 90。③ **任务 B（Phase 4 骨架）**：新增 `ontology/domain_ontology.yaml`（六域概念模型 Actor/Tool/Resource/Task/Provider/Memory + 域间关系 + 实施路径 Phase 4 P1/P2/P3 清单）+ `ontology/policy_ontology.yaml`（10 条声明式策略 = 3 static + 2 temporal + 3 contextual + INV-PA-001 ordering_constraint + 元规则 MR-7/8/16 + 策略冲突检测 stub）+ `test_phase4_ontology_skeleton.py`（15 单测：六域声明/description 长度/Actor 核心实例/source_of_truth 对齐/Memory 四层匹配 memory_plane/policy 三类分类/policy 必填字段/hard_limit 仅 static/alert-isolation ordering_constraint/跨 ontology 引用一致性）。**状态**: declaration_only，未 wiring。下次 B 推进从 `engine.py::load_domain_ontology()` + `evaluate_policy()` 开始。④ **governance v3.30 → v3.31**: 58 inv / 272→273 checks。⑤ **全量回归**: 41 suites / **1186 tests** / 0 fail / 安全 93/100。⑥ **元价值**: 本次 C 是 MR-16（security-governance 双轨统一）的终态 — security_config YAML 真正成为 security_score.py + governance_checker 共同读的唯一数据源，观察者盲区彻底闭合。|
| V37.9.6 | 2026-04-21 | **告警噪声治理 + watchdog 行级时间戳过滤 (MR-4 反向变种修复)** — ① **触发**：12:30 watchdog 推送 12 项告警，**6 项是陈旧错误** (kb_evening 4/14-15 已 V37.8.10 闭环 / openclaw_discussions 4/8 推送失败 / kb_inject gateway 1006 / 等)。**告警疲劳是真实成本**——用户每天看到一半"幽灵告警"会逐渐麻木真告警。② **MR-4 反向变种**：前 14 次 silent-failure 都是"漏报"，第 15 次是"过度报"——同样违反"告警必须信号化"原则。③ **根因**：`job_watchdog.sh:scan_logs()` 只过滤文件 mtime（24h 内文件才扫），但 `tail -50 grep` 扫到的行内可能含 13 天前错误（持续更新的日志文件历史尾部）。④ **修复 awk 行级时间戳过滤**：解析 `[YYYY-MM-DD ...]` 前缀，仅保留 24h 内行；`in_recent` 状态机让 Python Traceback 多行延续跟随上一时间戳行状态（旧 Traceback 自动丢弃）。⑤ **失效源清理**：(a) `rss_blogs.sh` 注释掉 LangChain RSS（持续 9 次 HTTP 404 死链） (b) `job_watchdog.sh` ACL Anthology 阈值 8d → 14d（ACL 学术源更新频率本就低，4/8 后真无新内容不应报噪）。⑥ **INV-WATCHDOG-FRESHNESS-001 新增**（meta_rule=MR-4, severity=high, layer=[declaration, runtime]）7 checks：6 declaration scan watchdog 源码（V37.9.6 标记/cutoff_date/in_recent/recent_window grep/1209600 ACL 阈值/LangChain 注释）+ 1 runtime subprocess 调外部 `test_watchdog_freshness.py` 12 单测。⑦ **12 个新单测**：TestTimestampFilter(7) 锁定今日/昨日保留 + 13d 前过滤 + 旧 Traceback 多行延续过滤 + 新 Traceback 保留 + state 切换正确 + TestWatchdogShellInvariants(4) 源码守卫 + TestRssBlogsCleanup(1) LangChain 死链清理。⑧ **关键澄清**：08:28 端口冲突初判为 silent failure 实为**误读**——incident_snapshot.py `_tail_file` 读 proxy.log **历史尾部 100 行**含 traceback，proxy_stats.last_success_time=08:15:16 证明 8:15 后 proxy 正常运行。watchdog 在 8:30:03 因 11 alerts 触发 incident 写入。**告警通道工作正常**，问题在告警内容质量。⑨ **累计**：governance v3.29→v3.30 (57→58 inv +1 INV-WATCHDOG-FRESHNESS-001 / 265→272 checks +7) / 40 suites / 1159 tests (+12) / 0 fail / 安全 93/100 / VERSION 0.37.9.5 → 0.37.9.6。|
| V37.9.5 | 2026-04-21 | **数据全景审计 + workspace .md 接入 KB 索引（接缝盲区闭合）** — ① **数据全景审计**：用户直觉"~/.kb 67M 数月业务积累太少"驱动深度审计。发现：(a) ~/.kb 67M 含 text_index 44M 派生 → 真业务 23M；(b) ~/.openclaw/workspace 109M 中 109M 几乎全是 venv 53M+venv_pptx 56M（Python 包不是业务），真业务 .md ~50KB；(c) ~/.openclaw/logs 105M 中 90M 是 gateway.err.log 噪声，真业务 ~11M；(d) ~/.openclaw/media 41M 由 mm_index 单独管。**真业务总数 ~75MB**——精炼模式合理，但发现 workspace 顶层 PA 自主 .md（AGENTS.md / IDENTITY.md / SOUL.md / OPENCLAW_*_SUMMARY 等）从未接入 text_index。② **kb_harvest 压缩比验证**：4/20 提炼 78 条对话 84KB，合理无需修改。③ **任务 3 实施**（`kb_embed.py:scan_kb_files`）：扩展从只索引 `MEMORY.md` 单文件到扫描整个 `~/.openclaw/workspace/*.md`（顶层）。新增 `WORKSPACE_EXCLUDE_BASENAMES = {"MEMORY.md", "HEARTBEAT.md"}` —— MEMORY.md 已单独索引为 `source_type=memory`（向后兼容），HEARTBEAT.md 严格排除（V37.8.16 INV-HB-001 OpenClaw 控制文件不得当业务知识，避免下个血案）。`.bak` / `~` 排除（备份/临时文件）。其他 .md 用新 `source_type=workspace`。④ **INV-KB-COVERAGE-001 新增**（meta_rule=MR-4, severity=high, layer=[declaration, runtime]）4 checks：3 declaration scan kb_embed.py（含 workspace_dir 扫描 + HEARTBEAT.md 排除 + .bak 过滤）+ 1 runtime subprocess 调外部 `test_kb_embed_workspace.py` 单测（避开 V37.3 exec scope trap 的 YAML 内嵌闭包陷阱）。⑤ **10 个新单测** `test_kb_embed_workspace.py`：TestWorkspaceIndexing(7) + TestExclusionList(3) 锁定 AGENTS/IDENTITY 被索引 + HEARTBEAT.md/.bak 排除 + MEMORY.md `source_type=memory` 向后兼容 + 其他 .md `source_type=workspace` + 目录不存在不崩溃 + 常量 + V37.8.16 注释引用。⑥ **MR-4 silent-failure 第 15 次演出**：本次新形态 = 数据接缝盲区——~50KB workspace .md 数月以来 PA 自己搜不到自己写的内容，因 kb_embed.py 范围限定遗漏。每次新增类似 PA 文档源都需要回扫——INV-KB-COVERAGE-001 守卫的就是这个。⑦ **累计**：governance v3.28→v3.29 (56→57 inv +1 INV-KB-COVERAGE-001 / 261→265 checks +4) / 39 suites / 1147 tests (+10) / 0 fail / 安全 93/100 / VERSION 0.37.9.4 → 0.37.9.5。|
| V37.9.4 | 2026-04-21 | **B 问题闭环 + MR-4 silent-failure 第 14 次演出系统化预防** — ① **MOVESPEED exfat → APFS 转换**（SSH 远程 `diskutil eraseDisk APFS MOVESPEED disk6`，新挂载 `/dev/disk4s1 (apfs, journaled)`，POSIX 3/3 通过 mkdir/chmod/touch）解决 6 天 cron rsync 静默失败盲区。② **20 处 cron rsync 反模式批量修复**：旧 `rsync ... 2>/dev/null \|\| true` → 新 `rsync ... 2>&1 \|\| echo "[XXX] WARN: SSD rsync failed (exit=$?)" >&2`。覆盖 5 个根目录脚本（kb_dream/kb_save_arxiv/kb_evening/kb_inject/kb_review）+ run_hn_fixed.sh + 13 个 jobs/ 脚本（含 freight 2 处）= 共 18 文件 20 处。设计：失败时 stderr 出 WARN 进 cron log（可见+可 grep），exit 0 保留（不杀 cron 主流程），写 `>&2` 避免污染命令替换（顺势 MR-11）。③ **INV-BACKUP-001 新增**（meta_rule=MR-4, severity=high, layer=[declaration, runtime]）3 checks: declaration 全局扫 .sh 禁止 `2>/dev/null \|\| true` 反模式 + 任何含 rsync MOVESPEED 必须有 "WARN: SSD" 字样 + runtime 真跑模拟 rsync 失败断言 stderr 含 WARN + stdout 不含。**部署即时验证**：第 1 次跑就抓出第 20 个漏网（run_hn_fixed.sh），声明层守卫真有效。④ **案例文档** `ontology/docs/cases/movespeed_exfat_silent_backup_failure_case.md`：完整四维度因果链架构图 + 三层根因（exfat fskit transient EPERM 触发 / 18 处复制粘贴反模式放大 / 监控盲区掩护）+ 时间线还原 + 6 条件组合分析 + MR-4 14 次演出对照表 + MR-8 copy-paste-is-a-bug-class 反例兑现 + 6 条元教训。⑤ **元洞察**：本次是 MR-4 silent-failure 的**新形态**——前 13 次都是单点静默，第 14 次是**多脚本统一反模式协同沉默 6 天**。修复证明 MR-8 反例：复制粘贴 18 次的反模式 = 系统性 bug 类，需要工具发现（INV-BACKUP-001 全局 scan）而非靠程序员记忆。⑥ **累计**：governance v3.27→v3.28 (56 inv (+1 INV-BACKUP-001) / 261 checks (+3) / 15 meta rules / 13 MRD scanners) / 38 suites / 1137 tests / 0 fail / 安全 93/100 / 对抗审计 16/16 / VERSION 0.37.9.3 → 0.37.9.4。|
| V37.9.3 | 2026-04-21 | **四项顺序推进：X 僵尸告警可观测性 + MR-6 9 项 high 补齐 + audit-of-audit 4 维度扩展 + 路线 C Step 3 终态** — ① **#1 X 僵尸告警路径诊断**（`jobs/finance_news/run_finance_news.sh`）：4/20 07:30 三天连续命中但 WhatsApp+Discord #alerts 零到达。Mac Mini 日志证实执行到 `notify` 调用但静默，手动测试证明 notify+前缀+topic 路径正常工作 → 定性为 Gateway 瞬时故障（已自愈）。最小修复：(a) DAY/TS 去掉显式 `TZ=Asia/Hong_Kong`，与 YESTERDAY/DAY_BEFORE 统一用系统默认 TZ，消除时区不一致漏洞 (b) 告警路径每个分支加诊断 log（5 种跳过原因识别：日期解析失败/昨-前日文件不存在/文件为空/交集为空/NOTIFY_LOADED=false）(c) `notify` 调用加 `\|\| log "exit=$?"` 捕获失败 exit code。下次再发故障能留下决定性证据。② **#2 MR-6 9 个单层 high 全部补齐** — MRD-LAYER-002 从 ⚠️ 9 个 warn 转为 ✅ pass。分三批：Batch A 纯 metadata 修正（`INV-TOOL-003 / INV-CRON-002 / INV-NOTIFY-003` 已有 python_assert 性质为 runtime，只标签错）；Batch B 真缺 runtime（`INV-NOTIFY-002 / INV-HEALTH-001 / INV-NOTIFY-004 / INV-JOB-GOVAUDIT-001 / INV-JOB-HARVEST-001` 各加一个 subprocess 或 AST 级 runtime python_assert，notify 空 channel stderr ERROR / 空 channel token 捕获 / AST MapReduce 函数存在 / governance_audit_cron 因果链 + alerts topic 路由）；Batch C 缺 declaration（`INV-CRON-004` 加 YAML 源码 `_cron_cmd_invokes` + `cmd.endswith(entry)` 守卫，与 INV-CRON-003 共享策略）。25 个 high 不变式全部 ≥2 层验证。③ **#3 audit-of-audit 从 1 维扩展到 4 维**（`ontology/governance_checker.py`）— 原 `MRD-AUDIT-PERF-001` 仅 `wall_time_ms`，扩展为：`wall_time_ms`（保留 1.3x+300ms）+ `peak_memory_mb`（新，1.5x+10MB，跨平台 `resource.ru_maxrss`）+ `bootstrap_ms`（新，2.0x+500ms，SESSION_START 到第一个 check 的启动开销）+ `skip_rate_pct`（新，绝对 +20pct 突增）。`_AUDIT_FIRST_CHECK_TIME` 全局变量在 `run_all` 第一次 iteration 时 stamp。老 history 缺字段时 median=0 自动跳过该维度判定（向后兼容）。新增 14 单测 `ontology/tests/test_audit_perf_dimensions.py`：4 维度独立阈值边界 + 向后兼容 + 跨平台内存转换 + 3 new fields 写入完整性。④ **#4 路线 C Step 3 数据源统一到 ontology** — V37.9 Step 1 `INV-SEC-001` 总分 ≥ 90 / V37.9.1 Step 2 per-dimension MIN_THRESHOLDS 硬编码在 YAML python_assert / V37.9.3 Step 3 阈值上升到 `governance_ontology.yaml` 顶层 `security_config.dimensions` 作为**唯一真理源**。INV-SEC-001 从 `security_config` 读阈值（不再硬编码），新增"ontology 维度名 vs security_score.py 实际输出对齐"守卫（防改名漂移）。`security_score.py` 新增 `load_ontology_thresholds()` 纯函数 + `check_ontology_thresholds(data)` 纯函数 + `--check-ontology-thresholds` CLI 模式（读 YAML 对比 exit 0/1）。新增 12 单测 `test_security_ontology_alignment.py`：YAML 段存在 + 7 维度声明 + 名字跨源对齐 + 阈值 ≤ 满分 + INV-SEC-001 不得重新硬编码 + CLI 模式 + 纯函数 pass/fail 三场景。⑤ **累计**: governance v3.26→v3.27 (55 inv / 258 checks / 15 meta rules / 13 MRD scanners, 新增 security_config 顶层段) / 38 suites / 1137 tests (+26) / 0 fail / 安全 93/100 / 对抗审计 16/16 (100%) / VERSION 0.37.9.2 → 0.37.9.3 / Opus 4.7。|
| V37.9 / V37.9.1 / V37.9.2 | 2026-04-20 | **Stage 2 验证者阶段四轮交付：路线 A 血案回填 + 路线 B 对抗审计 + V37.8.18 盲区修复 + V37.9 结构加固 + V37.9.1 安全统一 + V37.9.2 MR-6 深度补齐** — 从"修血案"进入"建制度"。① **路线 A 实证交付** `ontology/docs/audit_coverage_retrospective.md` (618 行): 15 个真实血案逐一回填 Q1/Q2/Q3 三问 → **0% 预防率 + 87% 回归率 + 80% 空白类别**。② **路线 B 对抗审计** `ontology/docs/adversarial_audit_report.md` (177 行) + `ontology/tests/adversarial_chaos_audit.py` (572 行, 16 场景): Cat A 10/10 (已知血案回归 100% 防御) + Cat B 6/6 (探测盲区全部闭合，V37.8.17 前 0/6 → 今日 6/6)。③ **V37.8.17 MR-14/MR-15 MRD 三步跃迁** (单点修复 → 元规则 → MRD 扫描器): MRD-ALERT-INDEPENDENCE-001 + MRD-RESERVED-FILES-001。④ **V37.8.18 P1+P2 修复 5/6 盲区**: C14 INV-KB-001 扩展 dict 防御 / C11 MRD-SILENT-EXCEPT-001 新增 / C15 MRD-PUSH-ROUTE-001 新增 / C12 INV-COST-001 retry 上限 / C13 INV-OBSERVABILITY-001 扩展 last_run 一致性。改进 adversarial counter 从 boolean → 按 "N 处" 数字解析 (工具自身递归 MR-7 自观察)。⑤ **V37.9 四项结构加固**: (a) adversarial Cat A 集成 full_regression layer 3.5 (每 PR 前跑防御力回归) (b) MRD-LAYER-002 渐进 warn severity=high 12 项技术债 (c) MRD-AUDIT-PERF-001 audit-of-audit (SESSION_START 实时 wall + 中位数双阈值 1.3x+300ms, C16 对抗 sleep 0.5→1.5s UNEXPECTED catch) (d) MR-16 + INV-SEC-001 security+governance 双轨统一 (score ≥ 90 守卫)。⑥ **V37.9.1 路线 C Step 2**: INV-SEC-001 按 7 维度 MIN_THRESHOLDS 细化 (密钥 15/测试 13/数据 10/部署 15/传输 9/审计 15/可用 8), 防"单维度塌陷但总分被其他拉高"。⑦ **V37.9.1 Stage 2 话语权** `docs/articles/audit_is_regression_not_prevention.md`: 立场型文章（第 4 份公开资产）论证 "audit 不是 prevention 是 regression engine"，6 条可落地原则。⑧ **V37.9.2 MR-6 深度补齐** (12→9 单层 high): INV-PA-002 runtime 解析规则 10 完整块 / INV-DREAM-001 runtime 解析 DREAM_TIMEOUT_SEC 动态映射 / INV-DEPLOY-003 runtime 扫双侧 status.json 邻近 continue。全部避开 V37.3 exec scope trap 用 iterative for。⑨ **原则 #30 升级 Opus 4.7**: 合法模型家族 = Opus (4.6/4.7/更新)，升级不是降级。⑩ **今日 X 僵尸清理**: ForeignPolicy/Carnegie_Endow/PDChina/CNS1952/SingTaoDaily 5 账号 4/18-19-20 三天连续命中从 FINANCE_X_ACCOUNTS 移除 + 6 个 file_not_contains 守卫。⑪ **KB 数据闭环验证**: chaspark.md 35 chunks + finance_daily.md 116 chunks 全部进入 text_index，kb_dream 今日提及 4 次，数据复利正向循环确认。⑫ **累计数字**: governance v3.19→v3.26 (55 inv / 256 checks / 15 meta rules / 13 MRD scanners) / 37 suites / 1111 tests / 安全 93/100 / 对抗审计 16/16 (100%) / VERSION 0.37.8.15 → 0.37.9.2 / 今日 30+ commits (e21d086 → 10a8492)。|
| V37.8.16 | 2026-04-20 | **PA 自残 HEARTBEAT.md 血案闭环（MR-4 silent-failure 第 12 次演出 + MR-15 新元规则立案）** — ① **血案**：2026-04-19 09:09 PA (Wei) 处理 HN 告警对话时调用 write 工具把"[SYSTEM_ALERT] HN热帖抓取已恢复 / 任务完成 / 下一步监控"三行写进 `~/.openclaw/workspace/HEARTBEAT.md`（把它当普通 TODO 文件名用）。13 小时潜伏期后（4/19 22:09）用户开始发 WhatsApp 消息，OpenClaw heartbeat 机制 (`auth-profiles-*.js:48796 isHeartbeatContentEffectivelyEmpty=false`) 激活 `runKind="heartbeat"` 模式，system prompt 变成默认 "Read HEARTBEAT.md if it exists. Follow it strictly. Do not infer or repeat old tasks from prior chats. If nothing needs attention, reply HEARTBEAT_OK." → Qwen3 严格执行 + 看到 HEARTBEAT.md 里"任务完成"判定无需关注 → 对每条用户消息都回 "HEARTBEAT_OK"（12 字符）→ Gateway `stripTokenAtEdges` 剥离 12 字符 → outbound 空 → 用户完全看不到 PA 回复 13 小时直到人肉察觉。② **六条件组合爆炸**：(1) WhatsApp plugin 4/10 14:53 被 OpenClaw 自动安装（WhatsApp routing 从 core 改为 plugin）(2) PA 有 write 工具 + workspace 访问（一直有）(3) HEARTBEAT.md 是 OpenClaw 保留文件（一直是）(4) PA 不知道它有特殊语义（一直不知道）(5) `agents.defaults.heartbeat.prompt` 未配置 → fallback 默认 prompt（一直未配置）(6) HEARTBEAT.md 非空非注释 ← **4/19 09:09 PA 自写触发第 6 项，前 5 项长期潜伏**。③ **反面教训 (原则 #28 违规记录)**：4/19 深夜我（Claude Code）初诊时看到 proxy /stats 502 + p99 126s → 先猜 primary/fallback 问题；看到 Gateway auth 日志 "Bad MAC" → 立刻联想 auth corruption → 让用户 `rm -rf auth` + QR 扫码重新登录。**这是不可逆操作**，幸好用户回滚无损失。正确路径是今天早上走的：读 status.json 焦点 → 查 git log 排除自己改动 → 读 OpenClaw 源码 `resolveHeartbeatPrompt` → diff 4/3 vs 4/10 openclaw.json → 读 HEARTBEAT.md 实际内容（锁定 PA 自写）— **六轮假说错五轮才到达根因**。④ **最小修复**：`cat > ~/.openclaw/workspace/HEARTBEAT.md` 置为只含注释（3 行 # 开头），`isHeartbeatContentEffectivelyEmpty()` 立即返回 true → heartbeat 不再激活。2026-04-20 09:16 WhatsApp E2E 实测"早上好 你在吗" → PA 5 秒延迟回复"早上好，我在。系统状态正常" ✅。⑤ **MR-15 新元规则立案** `reserved-files-must-not-be-writable-by-llm`：任何被底层 runtime（OpenClaw/Gateway/Proxy/Adapter）赋予特殊语义的文件路径，不得暴露给 LLM 的 write/edit 工具作无约束写入目标。典型违反 = HEARTBEAT.md。合规做法四层联合防御：(1) 声明层 `RESERVED_FILES` 明细表 (2) 行为层 SOUL.md 规则 11 + 血案注释 (3) Proxy 拦截层 `detect_reserved_file_write()` 纯函数 + `fix_tool_args` 集成改写 `args.content` 为 `RESERVED_FILE_SAFE_CONTENT`（只含 # 注释行）(4) 测试层单测覆盖每条保留路径 + 每种 path alias。**MR-15 与 MR-4 关系**：MR-4 是问题分类（静默故障是 bug），MR-15 是上游预防层（别让 LLM 碰能制造静默故障的 runtime 文件）。⑥ **INV-HB-001 新增** `heartbeat-md-reserved-file-not-llm-writable`（meta_rule=MR-15, severity=critical, verification_layer=[declaration, runtime]）12 checks：proxy_filters.py `RESERVED_FILE_BASENAMES`/`RESERVED_FILE_SAFE_CONTENT`/`detect_reserved_file_write`/`fix_tool_args` 调用 + SOUL.md 规则 11 三个 grep 守卫（"禁止写 OpenClaw 保留文件"/"2026-04-19 血案规则"/"MR-15"）+ runtime python_assert 真跑血案场景（构造 PA 写入 HEARTBEAT.md 的 tool_call JSON 断言 content 被改写为 SAFE_CONTENT、原"任务完成"字样消失、SAFE_CONTENT 所有非空行以 # 开头）。⑦ **SOUL.md 规则 11 新增**（位置：规则 10 "告警消息不跟进"之后，`## 我的性格` 之前，避开 `## 当前项目状态` 自动刷新区）：禁止路径列表、识别规则（精确名 HEARTBEAT.md）、禁止行为（write/edit 任何内容）、替代方案（kb_write.sh / status_update.py）、血案时间与字符串锚点、四层结构修复引用。⑧ **16 新单测** `TestReservedFileWriteBlock`：纯函数 detect_reserved_file_write 11 测（blocked/allowed/case-sensitive/alias/trailing-slash/非 dict 参数/空字符串/非字符串 path 等）+ fix_tool_args 集成 3 测（write content 被改写 / edit new_text 被改写 / 正常 write 不受影响）+ 常量守卫 2 测（RESERVED_FILE_BASENAMES 含 HEARTBEAT.md / SAFE_CONTENT 只含 # 注释）。⑨ **governance v3.19 → v3.20**：52→53 不变式，236→248 checks，14→15 meta rules。**全量回归 36 suites / 1111 tests / 0 fail / 53/53 invariants / 234/234 checks / 15/15 meta rules / 安全评分 93/100**。⑩ **案例文档** `ontology/docs/cases/heartbeat_md_pa_self_silencing_case.md`（完整四维度因果链架构图 + 三层根因 + 时间线还原 + 六条件组合爆炸 + MR-15 元规则提炼 + 4/19 深夜原则 #28 违规复盘 + 6 轮假说错五轮的教训）。⑪ **未解决项登记到 unfinished**：WhatsApp 入站自 4/10 起路由到 research agent 而非 main（main/sessions.json mtime 停留在 Mar 30），但 HEARTBEAT.md 修复后 research agent 正常响应，不致命；长期应考虑 `channels.whatsapp.agent` 显式配置或 `agents.list` 顺序调整。⑫ **元教训**：MR-4 silent-failure 第 12 次演出的新形态——不是错误被吞、不是错误被稀释、不是告警路径失效，是 **LLM 正确响应系统 prompt + Gateway 正确剥离 ack token + 但 LLM 误读了 system prompt 的适用范围**。整条链路每一环都"按设计工作"，但设计本身有语义 gap：HEARTBEAT.md 作为 runtime 控制文件 vs LLM 视为普通 TODO 文件 —— 这个 gap 没被任何一层填补。**MR-15 正是填补这种 gap**：把 runtime 边界的保留语义显式声明给所有可能触碰的层。|
| V37.8.15 | 2026-04-17 | **preflight push test 垃圾消息修复 + Mac Mini 分支错位修复 + GitHub 全面刷新 + 外部评审吸收** — ① **触发**：用户报告每 2 分钟收到 `🔧 preflight push test` WhatsApp 消息（22 分钟内 12 条）。② **双层根因**：(a) Mac Mini repo 在 `claude/start-daily-work-Mql3p` 分支（非 main），`git fetch origin claude/...` 拿到旧 tip，与 HEAD（merge commit）永远不等 → `HAS_NEW_COMMITS=true` 无限循环 (b) `preflight_check.sh` step 16 push test 无速率限制，每次 `--full` 都发 WhatsApp。③ **三层修复**：Fix A Mac Mini `git checkout main` 切回正确分支（根因）/ Fix B `auto_deploy.sh` 传 `SKIP_PUSH_TEST=1` 给 preflight（auto_deploy 有自己的 quiet_alert）/ Fix C `preflight_check.sh` push test 加每小时速率限制（defense-in-depth）。④ **GitHub 全面刷新**：README v37.8.15（治理 52 inv / 35 jobs / Phase 3 ontology / 81 FILE_MAP / 15 案例）+ Issue #132 进度更新（393→1093 tests 增长表）。⑤ **OpenClaw 升级评估**：上游 v2026.4.15（+6 版本），#59265 经 14 版本仍 OPEN，继续 hold。⑥ **外部代码级评审吸收**：六优势（控制平面意识/Provider 抽象/韧性/阈值/门禁/战略）+ 五劣势（脚本特征/环境耦合/PyYAML/Memory 接口/文档过载）。录入 4 项 P0/P1 优先级：可迁移性闭环 + 证据链产品化 + 插件生态接口 + 可观测闭环。核心三字诀：**标准化 + 可比较 + 生态协议**。⑦ **Nikkei Asia 遗留关闭**：X Syndication 17/21 条正常，非僵尸。⑧ **全量回归**：36 suites / **1095 tests** / 0 fail / 52/52 inv / 安全 93/100。|
| V37.8.13 | 2026-04-16 | **Gateway 宕 9h 静默血案闭环 + auto_deploy FILE_MAP 自复制修复 + status.json 形状修复（MR-4 第 11 次演出）** — ① **触发**：2026-04-16 00:30 HKT Gateway 进程死亡（restart.sh bootstrap 后 21 秒崩溃 → launchd rapid-crash jettison），WhatsApp 全断 9 小时。② **三层放大器全失**：(a) auto_deploy `quiet_alert` 凌晨静默期 **同时跳过 WhatsApp 和 Discord**，3 次 CRITICAL preflight 失败全被 `[QUIET]` 吞（主谋）(b) `wa_keepalive` 每 30min 写 WARN 到日志**但不推送任何告警**（18 次 WARN 沉默 9h）(c) `restart.sh` 报 "Done!" 但不验证 Gateway 是否真活。③ **三层修复**：Fix A `quiet_alert` 静默期仅跳过 WhatsApp、Discord 始终推送 / Fix B `wa_keepalive` 连续 2 次 WARN 自动升级 Discord #alerts（告警链不依赖失效主体自身——Gateway 宕则 WhatsApp 不通，必须走 Discord）/ Fix C `restart.sh` post-bootstrap 5×3s 健康验证循环。④ **INV-WA-001**（meta_rule=MR-4, severity=critical, verification_layer=[declaration, runtime]）7 checks + **INV-QUIET-001** 4 checks。⑤ **V37.8.12 修复**：auto_deploy FILE_MAP 自复制行移除 + 双循环自复制守卫（`$REPO_DIR/$SRC == $DST` 跳过，防 macOS cp 非零 + set -e 杀脚本）。⑥ **status.json 形状修复**：`format_human` 防崩（非 dict 条目防御） + 数据修正（`session_context.unfinished` str→list、扁平 key 清理、`recent_changes` 损坏条目重建）。⑦ **21 单测** `test_wa_gateway_resilience.py`：TestQuietAlertDiscordAlways(4) + TestWaKeepaliveEscalation(9) + TestRestartGatewayVerification(5) + TestCrossFileGuards(3)。⑧ **governance v3.17→v3.18**：40→42 不变式，199→210 checks，12/12 MR。⑨ **全量回归**：37 suites / **1092 tests** / 0 fail / 42/42 inv / 196/207 checks / 安全 93/100。⑩ **案例文档** `whatsapp_silent_death_case.md`：完整四维度因果链 + MR-4 第 11 次演出叙事 + 元教训"告警链不得依赖失效主体自身"。⑪ **MR-14 候选**：`alert-path-must-not-depend-on-failing-subject`（本案核心教训升级为元规则）。|
| V37.8.11 | 2026-04-15 | **auto_deploy 每小时漂移告警噪声闭环（MR-4 第 10 次演出：expected-behavior 被错分类为 error）** — ① **触发**：用户反馈"几乎每小时都收到 `[SYSTEM_ALERT] 漂移检测: 修复 1 个部署文件不一致，已自动覆盖`"。用户确认**问题存在很久了，只是没注意**——V37.4.3 `[SYSTEM_ALERT]` 前缀让长期潜伏的告警在 WhatsApp 显性化。② **根因**：V37.8.1 曾给 preflight_check.sh drift 循环加过 `if SRC == status.json then continue` 豁免（因为仓库 status.json 是 Claude Code 快照，runtime `~/.kb/status.json` 被 kb_status_refresh cron 每小时重写 health/quality 字段，两侧设计上永远不一致），但 **auto_deploy.sh 的等价 drift 循环没有同步加豁免**——遗漏至今。每小时整点（minute<2）auto_deploy 轮询 → md5 比对发现不一致 → `cp` 覆盖 runtime（清空 runtime 数据） → `quiet_alert` 推送 [SYSTEM_ALERT] → 下个整点 kb_status_refresh 重写 runtime → 恶性循环。③ **MR-4 第 10 次演出新形态**：前 9 次 silent-failure 都是"错误发生了但不可见/被稀释"。第 10 次是**expected-behavior 被系统错分类为 error，然后产出噪声告警**——不是错误被吞，是**正常行为被当错误**。④ **结构修复**：auto_deploy.sh drift 循环（line 317 起）镜像 preflight 同款豁免 `if [[ "$SRC" == "status.json" ]]; then continue; fi`，保留 **new-commit 同步路径**（上方 CHANGED_FILES 循环）继续把 Claude Code 的 priorities/unfinished/recent_changes intent 单向下传。契约："one-way intent flow (repo → runtime via new-commit) + exempt from two-way drift detection"。⑤ **INV-DEPLOY-003 新增**（meta_rule=MR-4, severity=high, verification_layer=[declaration]）5 checks：preflight 豁免守卫×2 + auto_deploy 豁免关键字 + V37.8.11 注释标记 + FILE_MAP 仍含 status.json（intent 通道不能丢）。⑥ **案例扩展**：`kb_evening_fallback_quota_chain_case.md` 新增 V37.8.11 章节登记此血案类（"告警噪声也是 observability 问题"）。⑦ **governance v3.16 → v3.17**：39→40 不变式，194→199 checks。⑧ **全量回归**：36 suites / **1071 tests** / 0 fail / 40/40 invariants / 185/196 checks / 12/12 MR / 安全 93/100。⑨ **Mac Mini E2E**：下一个整点（2026-04-16 00:00 HKT）drift 告警应不再触发（只要 auto_deploy 已通过 git pull 拉到 V37.8.11）。⑩ **关键原则反思**：本案印证 **原则 #15 "定期像用户一样使用系统"**——这个 bug 如果我们自己用 WhatsApp 实际观察日常推送，半天就能发现，而不是等用户受够了主动上报。观察盲区的体现：**开发环境所有单测/治理/preflight 全绿，但生产环境用户每天被 24 条噪声告警折磨**。⑪ **元教训**：每当一个类别的 check 被加豁免（如 V37.8.1 的 preflight），应该**系统性扫描**同类 drift 检测脚本是否都需要同款豁免——auto_deploy 和 preflight 是两份独立的 FILE_MAP 消费者，V37.8.1 只修了一半。|
| V37.8.10 | 2026-04-15 | **kb_evening 连续 2 天 22:00 告警"HTTP 502: Bad Gateway"血案闭环 — LLM 错误链三层稀释（MR-4 第 9 次演出）** — ① **触发**：2026-04-14 首次 + 2026-04-15 再次，用户同一时段收到 `[SYSTEM_ALERT] kb_evening 失败 原因: LLM 晚间整理失败: HTTP 502: Bad Gateway`。连续 2 天定性为结构性。② **三层根因**：(a) **触发器**：2026-04-14 某时刻起 primary Qwen3 (hkagentx.hkopenlab.com) 开始不稳定，`adapter._CircuitBreaker.consecutive_errors` 累积到阈值 → breaker OPEN（22:00 kb_evening 请求到达时直接 skip primary，2026-04-15 23:53 primary last_success_time 证实时好时坏非宕机） (b) **放大器**：`FALLBACK_CHAIN = ["gemini"]` **只配 1 个 fallback**。gemini 2.5-flash 免费层 daily/rate quota 被白天 ~20 个 LLM cron (kb_inject/finance_news/arxiv/daily_ops/ai_leaders_x/hf_papers/ontology_sources/s2/dblp/pwc/github_trending/rss_blogs/freight…) 持续磨耗 → 22:00 gemini 也 429，整条链路死亡 (c) **掩护者（本血案核心）**：**三层错误链稀释**——adapter `_send_json(502, {"error": "ALL 1 FALLBACKS FAILED: gemini 429"})` → proxy `except Exception as e: str(e)` 只拿"HTTP Error 502: Bad Gateway"，**从未调 `e.read()`** → client `rc.call_llm` HTTPError `f"HTTP {e.code}: {e.reason}"`，body 再次丢弃。真实原因"gemini 429 quota 耗尽"经 3 跳稀释完全不可见。③ **结构修复**（两侧缺一不可）：(1) `proxy_filters.py` 新增 `compose_backend_error_str(exc)` 纯函数 + `MAX_UPSTREAM_BODY_CHARS=500` 常量，对 HTTPError 读 `e.read()` body + JSON `error` 字段提取 + 截断 + fail-open try/except（observability 增强绝不能成为新故障源）(2) `tool_proxy.py except Exception` 分支 import 并调 `compose_backend_error_str(e)` 替代 `str(e)` (3) `kb_review_collect.py` 镜像 helper `_compose_http_reason(http_error)` + `MAX_UPSTREAM_BODY_CHARS=400` + `call_llm` HTTPError 分支 `return False, "", _compose_http_reason(e)` (4) **架构契约 MR-8 正向兑现**：helper 放在 `proxy_filters.py`（纯函数无网络依赖，符合文件表架构）而非 `tool_proxy.py`（import 即启动 HTTP server 不可测），让 `test_tool_proxy` 可直接导入测试。④ **INV-OBSERVABILITY-001 新增**（meta_rule=MR-4, severity=high, verification_layer=[declaration, runtime]）12 checks：proxy 侧 4 check（helper 定义 / MAX 常量 / read body / fail-open return base）+ tool_proxy 侧 3 check（import compose_backend_error_str / 不重新定义 helper 守卫×2）+ client 侧 3 check（helper 定义 / MAX 常量 / HTTPError 分支调 helper）+ 2 runtime python_assert 真跑血案场景（proxy helper 拼 upstream body 含"gemini 429" + kb_review helper fail-open on e.read 抛异常）。⑤ **21 新单测** `test_kb_review.TestComposeHttpReason`(10) + `test_tool_proxy.TestComposeBackendErrorStr`(11)：JSON error 提取 / 血案实际场景复现 / 非 JSON raw text fallback / empty body 向后兼容 / 超长 body 截断 / utf8 decode errors 替换 / e.read 抛异常 fail-open / 端到端 call_llm integration / tool_proxy.py import guard 禁止重新定义 helper。⑥ **Fix D 配套**：Mac Mini adapter 进程自 V36 (0.36.0) 起**首次重启**到 0.37.8.9——`bash ~/restart.sh` 后 `/health version: "0.37.8.9"` 生效（之前 V37.1~V37.4 三次 adapter.py commit 一直未在运行时）。但 `fallback_chain` 仍只显示 `["gemini"]`，说明 V3 capability routing 真跑但 `providers.d/` 下其他 provider 缺 API key env var → **Fix B 扩展 fallback 是独立的运维决策**，登记为 V37.8.11 候选。⑦ **MR-4 silent-failure 第 9 次演出（新形态）**：前 8 次都是"错误发生了但没被发现"。第 9 次是**错误被发现了但原因不可见**——用户看到告警，误以为观察机制工作正常，实际信息密度已经是零。这是更隐蔽的变种。⑧ **governance v3.15 → v3.16**：38→39 不变式，168+12→194 checks，meta rules 12/12 不变。⑨ **全量回归**：36 suites / **1071 tests** / 0 fail / 39/39 invariants / 180/180 checks (11 skip runtime-only) / 12/12 meta rules / 安全 93/100。⑩ **登记未解决项（不和本血案捆绑）**：(a) 扩展 fallback_chain 到 ≥2 provider — 需用户决策 API key (b) 其他 17+ LLM cron 加 `last_run_*.json` — 统一 observability 入口 (c) proxy_stats `auto_recovery_rate_pct=194.1%` bug（>100% 数学错误）(d) MR-13 候选元规则 `error-chain-must-preserve-upstream-cause-across-layers` — 把本血案教训升级为元规则 (e) gemini quota 消耗审计：白天 ~20 cron 哪些可降频。⑪ **案例文档** `ontology/docs/cases/kb_evening_fallback_quota_chain_case.md`（TL;DR + 完整因果链架构图 + 三层根因 + 时间线还原 + 七条件组合分析 + MR-4 演出史更新 + 5 条结构教训）。⑫ **Mac Mini E2E 待观察**：明日 2026-04-16 22:00 下次 kb_evening 告警（若再失败）应显示完整 upstream chain，形如"HTTP 502: Bad Gateway \| upstream: ... \| upstream: ALL 1 FALLBACKS FAILED: gemini HTTP 429"。|
| V37.8.9 | 2026-04-15 | **MR-11 / MR-12 运行时检测落地 — 元规则从"声明层"升级为"CI 可检测层"** — ① **触发**：V37.8.8 完成 MR-11 + MR-12 立案但两条元规则停留在 governance_ontology.yaml 的 `meta_rules:` 段声明层，无运行时扫描器，未来任何新代码违反不会被 CI 捕获。用户要求"#4 + #5 MRD 落地"把两条元规则从**被动声明**升级为**主动检测**。② **MRD-LOG-STDERR-001 实现**：`_discover_log_stderr_violations()` 在 governance_checker.py 扫所有 shell 文件（`*.sh` + `jobs/**/*.sh`）查找 log/debug/status/warn/info/notice/err/err_log 等诊断函数定义，识别单行 `log() { echo ...; }` + 多行 `log() {\\n ... echo ... \\n}` 两种形态。`_is_echo_to_stdout()` 纯函数判定：echo 行有 `>&2` 或 `1>&2` = stderr 合规；`>> file` 或 `> file` = 重定向到文件（不污染 stdout）；其余 = 违规。**白名单豁免 11 个用户交互诊断工具**（`cron_doctor/preflight_check/job_smoke_test/full_regression/daily_ops_report/health_check/smoke_test/quickstart/gameday/governance_audit_cron/kb_status_refresh`）因为它们 stdout 是用户终端的合法输出目标，不会被命令替换捕获。③ **MRD-LLM-PARSER-POSITIONAL-001 实现**：`_discover_llm_parser_positional_violations()` 扫 LLM 调用脚本集合（`jobs/*/run_*.sh` + `kb_*.{py,sh}` + `run_hn_fixed.sh` + `jobs/**/*.py` 共 37 个）检测三种反模式：`lines\[i+N\]` (任何 N≥1) / `i += N` (N≥2, 排除合法 while 遍历的 `i += 1`) / `(content\|text\|response\|result\|output\|raw\|llm_content).split()[N]`（白名单变量名避免误报）。**跨行 docstring 状态机豁免**（V37.8.9 edge case）：进入 `"""` 或 `'''` 块后跳过所有行直到遇到配对关闭符，这样历史血案注释里提到的 `lines[i+1] / lines[i+2]` 字样（V37.8.7 case doc）不被误判。**test_\*.py 文件豁免**、**assertNotIn/assertRaises 行豁免**、**注释行豁免**。④ **批量修复 19 个 job 脚本 log() 写 stderr**：用 sed 把 `log() { echo "..."; }` 替换为 `log() { echo "..." >&2; }` 覆盖 jobs/（acl_anthology/ai_leaders_x/arxiv_monitor/dblp/finance_news/freight_watcher/github_trending/hf_papers/karpathy_x/ontology_sources/openclaw_official×2/pwc/rss_blogs/semantic_scholar） + 主仓库（kb_evening/kb_inject/kb_review/run_hn_fixed）。格式统一的单行 log 定义使批量修复安全可行。修复后重新 scan：`所有 38 个 shell 文件的 log/debug/status/warn 函数均写 stderr（11 个诊断工具豁免）`。⑤ **governance_checker.py mr_used 计数器扩展**：原实现只从 `results` (invariants) 收集 meta_rule，V37.8.9 新增同时收集 `meta_rule_discovery` 引用的元规则——从 `元规则: 10/12` 升到 **`12/12`** 覆盖率。未来 MR-13/MR-14 加入后，只要任一 invariant 或 MRD 引用它们就会进入 used 集合。⑥ **32 单测** `test_governance_mrd_v8_9.py` 加入 full_regression：TestIsEchoToStdout(6) + TestScanShellLogFunctions(7) + TestDiscoverLogStderr(3) + TestPositionalPatterns(9) + TestDiscoverLlmParser(2) + TestMRDDeclarationInYaml(4) + 1 集成。关键边界：`i += 1` 豁免（合法 while 遍历）、`i += 2/3/4+` 捕获（跳多行即 MR-12 违反）、`echo ... >> file` 不报（重定向到文件）、多行函数体整体 `} >&2` 后置重定向合规、跨行 docstring 内的反模式字样豁免（避免血案注释被误判）。⑦ **governance v3.14 → v3.15**，38 不变式不变（MR 是元规则不是 invariant），168 checks 不变；两个新 MRD 加入 `meta_rule_discovery:` 段作为 Phase 0 可执行元规则发现。⑧ **全量回归**：35 suites / **1050 tests** / 0 fail / 38/38 invariants / 168/179 checks / **12/12 元规则**（从 V37.8.8 的 10/12 升满） / 安全 93/100。⑨ **未来保障**：任何新增 shell 脚本的 log/debug 函数写 stdout 会被 MRD-LOG-STDERR-001 自动 warn；任何新增 LLM 调用脚本用 `lines[i+N]` 或 `i += N` (N≥2) 解析会被 MRD-LLM-PARSER-POSITIONAL-001 自动 warn。两条元规则从**依赖人类记忆**的纪律问题变成了**CI 自动 enforce** 的架构约束，V37.8.6 和 V37.8.7 类血案未来不会再发生。|
| V37.8.8 | 2026-04-15 | **双元规则正式立案：MR-11 (log→stderr) + MR-12 (LLM-output-key-based)** — ① **触发**：V37.8.6 (Dream) + V37.8.7 (ontology_sources) 同日双血案都是 MR-4 silent-failure 的演出，但两个血案的**防御机制**是独立的跨领域架构规则，不该靠个别 INV 级别 check 一事一议。用户要求"立即立案"把两条元规则从"案例教训"正式升级为 ontology 治理体系的硬规则。② **MR-11 shell-function-output-must-go-to-stderr-if-not-returned-value**：shell 函数内的诊断/日志/状态输出（log / debug / status / warn）必须写到 stderr（`>&2`），不得写 stdout。原因：调用方常用 `RESULT=$(func ...)` 命令替换捕获 stdout 作为"返回值"，如果 log 也写 stdout 会污染 RESULT → 被当作业务数据向下游传递 → cache → 下游 LLM 幻觉。血案教训来自 V37.8.6：kb_dream.sh `log() { echo ...; }` 写 stdout 让 llm_call 失败时的错误日志被 `signals=$(llm_call ...)` 捕获 → cache 污染 → LLM 编造"Hugging Face 危机"。**一个 `>&2` 阻断整条幻觉链**，证明 MR-11 不是纪律问题而是架构硬规则。③ **MR-12 llm-output-parser-must-be-key-based-not-positional**：解析 LLM 文本输出的代码不得用严格位置索引（`lines[i] / lines[i+1] / lines[i+2] / i += N / content.split()[0/1/2]`）。允许模式按推荐度：(1) JSON/YAML schema 校验 (2) key-based prefix dict (3) 语义 state-machine + pending_* + 特征字符块边界 (4) 正则直接提取。强制要求：每个字段缺失有 sentinel 默认值 + 输出层有兜底（如 `cn_title = paper['title']`） + 块相互独立无级联。血案教训来自 V37.8.7：ontology_sources `i += 3` 步进 + 无 fallback → 级联错位 → 用户 WhatsApp 看到 `*---*` 作 cn_title。④ **V37.8.7 MR-12 扫描结果**：扫完 15 个 LLM 输出解析器，除已修的 kb_dream/ontology_sources，其他都合规：7 个论文类 job（arxiv/semantic_scholar/hf_papers/dblp/pwc/acl_anthology/github_trending/rss_blogs）共享 state-machine + key-based + 三重 fallback 模式（`pending_title` / `'⭐' in line` 块边界 / `paper['title']` 兜底），ai_leaders_x 用 pure dict key-based，run_hn_fixed 用 JSON+regex fallback。**无紧急修复任务**。⑤ **违反模式正则**（为未来 CI 扫描预埋）：MR-11 扫 `^\s*(log\|debug\|status\|warn\|info)\(\)\s*\{\s*echo\s+.*[^&]2?`；MR-12 扫 `lines\[i\s*[+]\s*[0-9]+\]` / `^\s*i\s*\+=\s*[0-9]+\s*$` / `content\.split\([^)]*\)\[[0-9]+\]`。⑥ **audit_metadata.meta_rules 10→12**（MR-1~MR-12，新增 MR-11/MR-12 作为 MR-4 silent-failure 的上游预防层）。governance_checker 自动跟随 audit_metadata 动态计数，显示 `元规则: 10/12`（12 条声明 / 10 条被现存 INV 引用）— MR-11/MR-12 暂无 invariant 关联，等 V37.8.9 迭代新增 MRD 扫描器落地运行时检测。⑦ **governance v3.13 → v3.14**：38 不变式不变（MR 是元规则不是 invariant），168 checks 不变。⑧ **全量回归**：34 suites / **1018 tests** / 0 fail / 38/38 invariants / 168/179 checks / 安全 93/100。⑨ **遗留登记（V37.8.9 候选）**：(a) MRD-LOG-STDERR-001：扫所有 shell 函数定义确认 log/debug/status 用 >&2 (b) MRD-LLM-PARSER-POSITIONAL-001：扫所有 Python heredoc 识别 `lines[i+N] / i+=N` 反模式 (c) 这两个 MRD 一旦落地就把 MR-11/MR-12 从"声明层"升级为"CI 可检测层"，符合 MR-6 ≥2 层验证深度要求。|
| V37.8.7 | 2026-04-15 | **ontology_sources 推送格式错位血案闭环（MR-4 第 8 次演出，同日双血案）** — ① **触发**：用户 WhatsApp 收到 ontology_sources 推送出现严重错位 — 第 2 篇 cn_title 显示为 `*---*`（分隔符被当成中文标题）、中文标题串到 highlight 槽、第 3 篇 cn_title 显示为 `*价值：⭐⭐⭐⭐*`（前一篇的"价值"行错位上来）。同一天 V37.8.6 修了 Dream 血案，下午又遇 ontology_sources 血案 — 双血案同源 MR-4 silent-failure。② **三层根因**：(a) **触发器**：LLM 偶尔漏一行（如第 2 篇缺"要点"）— Qwen3 的指令遵循训练让它倾向"提供有用输出"而非严格遵循三行格式 (b) **放大器**：原解析器 `run_ontology_sources.sh:300-313` 用严格位置 `lines[i], lines[i+1], lines[i+2]` + `i += 3` 步进 — LLM 漏一行后所有后续条目的 (cn_title, highlight, stars) 全部右移一格**级联污染** (c) **掩护者**：emit 端 `*{cn_title}*` 直接照 parse 结果输出，没有任何字段语义校验（如"cn_title 不应是 ---"、"cn_title 不应以'价值：'开头"）— 错位输出毫无 degraded 标记直接推送给用户。③ **结构修复**：(1) 抽到独立模块 `jobs/ontology_sources/ontology_parser.py`（纯函数 `parse_llm_blocks(content) -> list[(cn,hl,stars)]` 可单测，避开 V37.5 heredoc-only 不可测血案）。 (2) **separator 切块**：用 `re.split(r'(?:^\|\n)\s*[-=*_]{3,}\s*(?:\n\|$)', cleaned)` 按 `---/===/***` 等分隔符切块，单块缺行不影响其他块。 (3) **块内 key-based 解析**：按前缀（"中文标题：" / "标题：" / "要点：" / 含 "⭐"）识别字段而非依赖位置；缺字段留空字符串。 (4) **shell `export ONTOLOGY_JOBS_DIR=$JOB_DIR`** + heredoc `from ontology_parser import parse_llm_blocks`（heredoc 走 `python3 - <<EOF` stdin 不带脚本目录到 sys.path，需主动注入）。 (5) auto_deploy FILE_MAP 补齐 `ontology_parser.py`。④ **INV-ONTOLOGY-001 新增**（meta_rule=MR-4, severity=high, verification_layer=[declaration, runtime]）7 checks：parse_llm_blocks 函数定义 + `_SEPARATOR_RE.split` 用法 + 块内 startswith('中文标题') key-based 识别 + shell `export ONTOLOGY_JOBS_DIR` + shell `from ontology_parser import parse_llm_blocks` + auto_deploy FILE_MAP 部署 + **runtime python_assert** 真跑用户 2026-04-15 实际看到的污染场景：构造"3 篇但第 2 篇缺要点"输入，断言 (a) 解析出 3 个 block (b) block 0 完整 (c) block 1 highlight 为空但其他正确 (d) block 2 完全不被 block 1 缺行影响 (e) 任何 cn_title 不含 `---` 或"价值"字样。⑤ **24 单测** `test_ontology_parser.py` 加入 full_regression：TestNormalCases(4) + **TestBloodLessonRegression(5)**（第 2 篇缺要点不级联 / 缺价值 / 缺标题 / 超额空行 / 连续分隔符）+ TestSeparatorVariants(5) + TestLLMOutputVariants(5) + TestShellScriptIntegration(4) + TestActualBloodLessonScenario(1) 端到端复现用户血案场景。⑥ **MR-4 第 8 次演出**：演出史 V37.3 → V37.4 → V37.4.3 → V37.5 → V37.6 → V37.7 → V37.8.6 → **V37.8.7 ontology_sources 位置解析**（同日双血案，证明 silent failure 在系统中持续以不同形态出现）。⑦ **元洞察**：V37.8.6 (Dream) + V37.8.7 (ontology_sources) 同日双血案揭示——**LLM 输出永远不能假设格式严格遵守**，所有 LLM 输出解析必须 (a) 容忍单条缺/多/重排 (b) 用语义键（如前缀/特征字符）而非位置定位 (c) 字段独立提取，不级联 (d) emit 前做最小语义校验（如"cn_title 不应是分隔符"）。⑧ **governance v3.12 → v3.13**：37→38 不变式（+INV-ONTOLOGY-001），175→182 检查。⑨ **全量回归**：34 suites / **1018 tests** / 0 fail / 38/38 invariants / 168/179 checks / 10/10 meta rules / 安全 93/100。⑩ **遗留登记**：(a) emit 端字段语义校验（"cn_title 不应是分隔符 / 不应以已知前缀开头"）作为 V37.8.8 候选 (b) 同模式审查其他 LLM 输出解析器（finance_news/dblp/hf_papers/semantic_scholar/freight_watcher 是否有类似位置解析）。|
| V37.8.6 | 2026-04-15 | **Dream 自引用幻觉血案闭环（MR-4 第 7 次演出 + 原则 #23 链式幻觉典型实证）** — ① **触发**：V37.8.5 Mac Mini E2E 同步后用户察觉 2026-04-15 03:00 Dream run 推送的"Hugging Face 平台危机"分析存在违和 — 信号一是"Papers with Code 沉默"但行动一却是"监控 Hugging Face"，主题断裂。日志里同时出现 4 次 `adapter.py:386 send_error(400, "Bad JSON")` 但 Dream 仍产出完整 6 章节推送。② **三层根因深挖**：(a) **触发器**：某个 source/note 抓取内容含 U+D800-U+DFFF 孤立 UTF-16 代理码点（smoke_test 已报 UnicodeEncodeError × 8）→ `llm_call` 内 `python3 -c` heredoc 的 `json.dump(body, f, ensure_ascii=False)` + 文件默认 utf-8 编码 → 孤立代理写入时 UnicodeEncodeError → body_file 截断为不完整 JSON → curl 上传破损 JSON 到 adapter :5001 → `adapter.py:383` json.loads 炸 JSONDecodeError → 返回 456 bytes HTML 错误页 "Bad JSON 400"。(b) **放大器（本次血案核心）**：`kb_dream.sh:109` 的 `log() { echo ...; }` 用 plain echo 写 stdout，而 Map 循环用 `signals=$(llm_call "$prompt" ... || true)` 命令替换捕获 stdout → log() 发出的 "LLM raw response: <HTML>Error code: 400 Bad JSON..." 错误日志全部被捕获进 `$signals` 变量 → `if [ -n "${signals// }" ]` 非空检查通过 → `echo "$signals" > "$cache_file"` 把错误日志写入 cache file。(c) **掩护者**：Reduce cache-only fast path (V37.4 Fix A) 读 `$MAP_DIR/2026-04-15_*.txt` 把错误日志作为"信号"注入 REDUCE_MATERIAL → LLM 看到 "Bad JSON 400" 字样后基于训练数据里"平台报错 + 用户在讨论分析"的分布，自动编造"某平台出了问题"的叙事 → 最高概率的 AI 平台 = Hugging Face → 输出虚假的"Hugging Face 平台危机"分析并经 WhatsApp+Discord 推送给用户 = 原则 #23 链式幻觉三跳累积（第 0 跳 adapter 错误 → 第 1 跳污染 cache → 第 2 跳 LLM 编造 → 第 3 跳推送用户）。③ **四层结构修复（defense-in-depth）**：(1) **log() 改写 stderr**：`log() { echo ... >&2; }` 阻断 stdout 污染 `$(...)` 命令替换的通道，四层中最根本的一层，一个 `>&2` 直接阻断整条幻觉链。(2) **heredoc 内嵌 `_sanitize(s)`**：U+D800-U+DFFF 孤立代理 → U+FFFD 替换字符，保持字符边界与 token 对齐，同时应用于 `prompt` 和 `system_msg`，防 json.dump 触发 UnicodeEncodeError。(3) **file open 双属性**：`open(..., encoding='utf-8', errors='replace')` 作为第二防线，万一 sanitize 漏网其他无效字节也能 replace 不炸裂。(4) **REDUCE/CHUNK1/2/3 system prompt 反污染守卫**：明示禁止把 HTTP 错误码/Python 异常/错误页 HTML/U+FFFD 连续片段/工具链名(curl/jq/grep) 当作外部信号，禁止基于这些字样推断 Hugging Face/GitHub/npm 等平台状态，禁止在行动建议中针对"平台错误"给建议。即使前三层全失效，LLM 也被明示约束。④ **INV-DREAM-003 新增**（meta_rule=MR-4, severity=critical, verification_layer=[declaration, runtime]，**首次登记即符合 MR-6 hard-enforcing ≥2 层要求**）16 checks：log() 源码模式 + V37.8.6 血案注释 + _sanitize 函数定义 + U+D800-U+DFFF 范围 + U+FFFD 替换 + 双字段应用 + errors='replace' + encoding='utf-8' + REDUCE_SYSTEM 反污染守卫 + Bad JSON 禁令 + Hugging Face/GitHub/npm 举例 + CHUNK1/2/3 各自反污染 + runtime python_assert 真跑 sanitize 五场景（孤立高位/低位代理/正常串/代理范围边界/sanitize 后 json+utf-8 编码不炸）。⑤ **19 单测** `test_dream_surrogate_sanitize.py` 加入 full_regression.sh 第一层：TestLogGoesToStderr(2) + TestSanitizeSurrogates(7) + TestShellSanitizeImplementation(5) + TestAntiPollutionSystemPrompt(3) + TestShellScriptIntegrity(1) + 含 negative-control "raw surrogate would crash json dump" 证明问题真实存在。⑥ **MR-4 第 7 次演出**（silent-failure-is-a-bug）：演出史 V37.3 governance summary 吞 error → V37.4 Dream Map budget → V37.4.3 PA 告警污染 → V37.5 kb_review 空 prompt → V37.6 KB dedup → V37.7 双跑审计 → **V37.8.6 Dream 自引用幻觉（迄今最隐蔽：错误不仅被掩盖，还被 LLM 加工成看似合理的业务分析再推送给用户）**。⑦ **原则 #23 典型实证**：本案例成为 CLAUDE.md 原则 #23 "LLM 链路中每一跳都会放大幻觉"的教科书级案例，未来引用可指向 `ontology/docs/cases/dream_self_referential_hallucination_case.md`（详细四维度因果链 + 三层根因 + LLM 合理化本能分析 + 四层防御逻辑 + 7 次 MR-4 演出对照表）。⑧ **潜伏态血案元洞察**：V37.1+ 四个条件（surrogate 累积 + log→stdout + `$(cmd)` 捕获 + cache 非空检查）一直存在，Dream 可能已经静默输出幻觉内容数天或数周而无人察觉，只有条件 5"LLM 编造被用户发现"首次触发才让血案浮现。⑨ **governance v3.11 → v3.12**：36→37 不变式（+INV-DREAM-003），159→175 检查。⑩ **全量回归**：33 suites / **994 tests** / 0 fail / 37/37 invariants / 161/172 checks / 10/10 meta rules / 安全 93/100。⑪ **遗留登记（下次迭代）**：(a) MR-11 候选 `shell-function-output-must-go-to-stderr-if-not-returned-value`（扫所有 shell 函数的 log/debug/status 输出）(b) 系统化 surrogate 清洗（爬虫/KB 写入源头先清洗，不只 LLM 边界）(c) LLM 输出 grounding allow-list（平台名/公司名必须在信号里出现才能提）(d) Dream 失败率可观测（Map 批次失败 > 阈值 → `[DEGRADED]` 标记或跳过推送）(e) 审计今日 pwc_daily / 其他 source 文件是否真的有 surrogate 字符。|
| V37.8.5 | 2026-04-15 | **僵尸检测边缘盲区闭合（V37.8.4 修复的修复，MR-10 第 2 次演出）** — ① **触发**：V37.8.4 收工 Mac Mini E2E 用户主动验证 finance_news 时，系统独立发现 SCMPNews 95/95 超窗口成功标记，但用户肉眼比对时发现同一 run 有两个边缘账号被漏标：**CNS1952 98/99 超窗口 + 1 过短**（严格 `old == total` 放过 99% 老化率）+ **SingTaoDaily 0-tweet stub**（`total > 0` 前置门槛直接排除空 HTML 骨架）。V37.8.4 作为"登记 unfinished"延后处理。② **MR-10 第 2 次演出**：V37.8.4 引入的检测器**本身**是 bug 源——第三问"最小修复"被浅化为"严格相等 + total>0 就够了"，但严格相等是所有 `old/total >= X` 的最窄特例，0-tweet stub 完全不在 `total > 0` 的论域。③ **结构修复**：(1) 把 heredoc 内嵌逻辑提炼为独立纯函数模块 `jobs/finance_news/finance_news_zombie.py`，签名 `classify_zombie(diag, count) -> (bool, tier)`，返回 `stub`/`stale`/`alive` 可区分 (2) Tier 1 "stub"：`no_data == 0 and total == 0` 闭合 SingTaoDaily (3) Tier 2+3 "stale"：`count == 0 and total > 0 and old * 10 >= total * 9`，整数比较避免浮点，阈值 9/10 编码为常量 `ZOMBIE_STALE_NUM/DEN`，CNS1952 98/99 触发 (4) `count == 0` 守卫防止"99% 老化但还产出 1 条新鲜"的低频活跃账号误报 (5) heredoc 改为 `from finance_news_zombie import classify_zombie`，禁止 inline fallback。④ **治理升级**：INV-X-001 `verification_layer` 从 `[declaration]` → `[declaration, runtime]`（主动遵循 MR-6 建议，即使 severity=high 不强制），新增 `python_assert` runtime check 真跑 5 个 tier 场景（Tier 1 stub / Tier 2 V37.8.4 原始 / Tier 3 CNS1952 / count 守卫 / no_data=1 false positive）。13 check → 20 check 净增 7（-1 旧严格谓词，+8 新检查含 import/call-with-count/env-export/模块存在/Tier 1/Tier 2+3/auto_deploy/runtime）。⑤ **24 单测** `test_finance_news_zombie.py` 加入 full_regression.sh 第一层：TestTier1Stub(3) + TestTier2Stale100(2) + TestTier3NearZombie(4) + TestCountGuard(3) + TestAliveAccounts(3) + TestConstants(2) + TestShellScriptIntegration(6) + TestAutoDeployMapping(1)。⑥ **MR-8 兑现**：`test_script_has_no_inline_zombie_fallback` 单测 grep shell 源码禁止 `def classify_zombie` 内嵌（防模块缺失时静默退回 V37.8.4 行为 = copy-paste-is-a-bug-class 反面）。⑦ **诊断可观测**：`⚠️ ZOMBIE嫌疑[stub]` / `⚠️ ZOMBIE嫌疑[stale]` 日志前缀 tier 可区分。⑧ **案例文档** `ontology/docs/cases/zombie_detection_edge_case_closure.md`（四维度因果链 + 三层根因 + 时间线 + 五条件组合分析 + MR-10/MR-6/MR-8/MR-4 四元规则兑现 + 本体喂养清单）。⑨ **auto_deploy FILE_MAP** 补齐 `jobs/finance_news/finance_news_zombie.py` 部署映射。⑩ **governance v3.10 → v3.11**：36 不变式不变（INV-X-001 升级非新增），152 → 159 检查。⑪ **全量回归**：32 suites / **975 tests** / 0 fail / 36/36 invariants / 145/156 checks（11 skip 运行时层）/ 10/10 meta rules / 安全 93/100。⑫ **VERSION** 文件 0.37.5 → 0.37.8.5（V37.5 后未同步的 rot 修复）。⑬ **元洞察**：血案二次演出证明"修复会埋坑"；登记 unfinished 是人类 TODO 不是 CI——原则 #28 理解再动手 + #29 收工零遗漏 两者缺一不可。|
| V37.8.4 | 2026-04-14 | **finance_news X 僵尸账号血案闭环 + INV-X-001 + 原则 #27 升级** — ① **触发**：V37.8.3 修改 3 个 X handle（CaixinGlobal→caixin, YicaiGlobal→yicaichina, STcom→straits_times）**上线第二天（V37.8.4 开工验证）发现三个改名后的 handle 全是僵尸账号**，Syndication API 返回最新推文分别是 2227/3364/420 天前。② **全量审计 22 账号** 7 个僵尸（~32% 污染率）：caixin 2227d/yicaichina 3364d/straits_times 420d/Reuters 253d/BrookingsInst 585d/WorldBank 2KB stub/ChannelNewsAsia 2955d。③ **Path C 最小修复 + 全量审计**：删 7 僵尸 → 剩 15 handle（IMFNews/business/WSJ/ReutersBiz/TheEconomist/XHNews/PDChina/CGTNOfficial/ChinaDaily/globaltimesnews/CNS1952/SCMPNews/NikkeiAsia/SingTaoDaily/asahi），依赖生产 cron 每天自然暴露剩余 14 个（429 限流绕过）。④ **静默失败检测**：`is_zombie_suspect = (diag["total"] > 0 and diag["old"] == diag["total"])` 写入 `cache/zombies_${DAY}.txt` + `⚠️ ZOMBIE嫌疑` 诊断前缀 + 3 天连续命中告警（macOS `date -v-Nd` / Linux `date -d 'N days ago'` 兼容 fallback）。⑤ **INV-X-001 新增**（meta_rule=MR-10, severity=high, verification_layer=[declaration]）13 checks：ZOMBIE_FILE 声明 + is_zombie_suspect 解析 + zombie_file append + ⚠️ 前缀 + `comm -12 - <\(sort -u "\$Y_FILE"\)` 3 天匹配 + 7 个 file_not_contains 守卫（禁止 Reuters/WorldBank/BrookingsInst/caixin/yicaichina/ChannelNewsAsia/straits_times 任一 handle 回归）+ CLAUDE.md "账号健康 ≠ HTTP 200" 声明。⑥ **CLAUDE.md 原则 #27 升级**：标题从"X/Twitter 是高质量实时数据的第一选择"变为"**X/Twitter 是高质量实时数据的第一选择（但账号健康 ≠ HTTP 200）**"，强制条款："账号健康必须通过最新推文时间戳验证（newest_tweet_dt > now - N 天），新增账号前必须先抽查最新推文时间，生产脚本必须对"抓到内容但 0 条过时间窗"的账号打 ZOMBIE 嫌疑标记，连续多天命中触发告警"。⑦ **MR-10 正向兑现**：V37.8.3 引入 MR-10 understand-before-fix 后**第二天就被违反**（第三问"最小修复"被浅层化为"改到字面上正确的 handle"而不是"改到可用信源"），V37.8.4 把违规事件本身固化为 case doc + INV-X-001 让下次改 X handle 必须先验证最新推文时间。⑧ **元洞察**：协议层绿灯 ≠ 内容层健康（本项目第 3 次：V37.4 Dream reduce cache bland 重复 → V37.5 kb_review 空 prompt → V37.8.4 X Syndication 僵尸快照）。⑨ **Mac Mini E2E 验证**：finance_news 手动运行 27 篇推送，系统自主发现 **SCMPNews** 为第 8 个僵尸嫌疑（95/95 条超 72h，ZOMBIE 嫌疑标记生效），zombies_2026-04-14.txt 写入 SCMPNews，等 3 天连续命中（2026-04-16 决策窗口）。⑩ **案例文档** `ontology/docs/cases/finance_news_syndication_zombie_case.md`（TL;DR + 完整因果链架构图 + 三层根因 + 时间线 + 五条件组合爆炸分析 + MR-10 正向兑现叙事）。⑪ **governance v3.9 → v3.10**：35→36 不变式，139→152 checks。⑫ **遗留登记**（3 项）：SCMPNews 3 天连续观察 / CNS1952 近僵尸检测阈值差距（98/99 被 strict equality 放过，需 ≥90% 阈值）/ SingTaoDaily 0 推文 stub 检测路径缺失（total=0 绕过 total>0 阈值）。⑬ **全量回归**：31 suites / 951 tests / 0 fail / 36/36 invariants / 138/149 checks / 10 meta rules / 安全 93/100。|
| V37.8.3 | 2026-04-13 | **连锁修复血案教训 + 方法论升级 + 运维修复** — ① **preflight 连锁修复闭环**：原始问题是 `~/auto_deploy.sh` HOME 副本未同步（只需 `cp`），被误诊为"缺少 FILE_MAP 配置"，触发 5 轮连锁修复（加 FILE_MAP 条目→双目标部署→SCRIPT_DIR HOME 检测→dict→list 解析器→job_smoke_test 同 fix），最终 preflight 77/0/4 + job_smoke_test 34/182/0/16 全部通过 ② **MR-10 understand-before-fix 新元规则**：修复前必答三问（之前存在吗/哪个改动引入的/最小修复是什么），连锁修复是最危险的系统破坏模式，governance v3.9→v3.10（10 元规则）③ **原则 #28 理解再动手**：禁止条件反射式修复 ④ **原则 #29 开工收工零遗漏**：清单逐项执行不允许"差不多就行" ⑤ **第 0 号宪法**：最强模型不降配不降速不妥协 ⑥ **finance_news X 账号修复**：CaixinGlobal→caixin, YicaiGlobal→yicaichina, STcom→straits_times ⑦ **Dream MIN_ACCEPTABLE_CHARS** 1500→3000 bytes + BEST_CHARS 未定义变量修复 ⑧ **kb_evening E2E 测试时区闪烁修复** ⑨ **案例文档** `ontology/docs/cases/preflight_cascading_fix_case.md` ⑩ **全量回归**：951 tests / 35/35 invariants / 125/125 checks / 10 meta rules / 安全 93/100 |
| V37.8.2 | 2026-04-12 | **finance_news 全球财经/政策新闻 job 上线 + 反幻觉修复 + 运维巩固** — ① **preflight 三修复**：(1) status.json 仓库 vs 运行时合法分叉豁免（仓库快照 vs cron 每小时刷新）(2) KB 索引阈值从 count-based（<=5 stale）改为 percentage-based（>=90% 覆盖率 = WARN，<90% = FAIL），33 cron 日产 15 新 notes + 21 modified sources 导致假 FAIL (3) MRD-NOTIFY-002 activity 层假阳性根治：`_discover_silent_channels()` 重写为 TOPIC_JOB_MAP 映射 + registry-driven job log mtime 检测，替代原 grep `--topic` 字符串（28/32 job bypass notify.sh 永不产生该字符串）② **kb_evening/kb_review LLM 反幻觉约束**：两个 prompt builder 加入 `⚠️ 严格约束（违反则整份输出作废）`——只使用明确出现的信息 / 每条标注来源标签 / 严禁虚构发布公告+开源事件+人物言论 / 无数据跳过不编造。触发：evening digest 编造"OpenClaw 开源发布 openclaw-model-bridge"（私有仓库从未公开发布）③ **finance_news 全新 job**：双通道 8 RSS 直连（Fed/NBER/FT/ECB/BIS/Yahoo/SCMP/36Kr）+ 21 X/Twitter Syndication API（7 国际：Reuters/IMFNews/WorldBank/BrookingsInst/Bloomberg/WSJ/ReutersBiz + 14 亚太：XHNews/PDChina/CGTNOfficial/ChinaDaily/globaltimesnews/CNS1952/CaixinGlobal/YicaiGlobal/SCMPNews/NikkeiAsia/SingTaoDaily/ChannelNewsAsia/STcom/asahi）。72h 时间窗口覆盖周末 + 按天自动重置 seen 缓存 + LLM 结构化分析（价值⭐评级/国内外对比/投资建议/风险提示）+ KB 归档 + Discord+WhatsApp 推送。诊断模式：0 条账号打印过滤原因（纯RT/过短/已见/超窗口/无数据）。Mac Mini 验证：32 篇（16 国际 + 16 国内），0 源失败 ④ **CLAUDE.md 原则 #27**：X/Twitter 是高质量实时数据的第一选择——Syndication API 无需认证/零成本/100% 成功率 vs RSSHub 公共实例 100% 失败率 ⑤ **auto_deploy FILE_MAP** 补齐 finance_news 部署映射 ⑥ **全量回归**：930 tests / 35/35 invariants / 125/125 checks / 9/9 meta rules / 安全评分 93/100 |
| V37.8 | 2026-04-11 | **MR-6 深度补齐 + MRD-NOTIFY-002 语义修复（治理自观察第二轮：治理深度从"建议"升级为"硬强制"）** — ① **触发**：V37.7 双跑审计后 `governance_checker --full` 的 Phase 0 MRD 段持续报 4 个 warning，其中 `MRD-LAYER-001` 发现 **7 个 critical 不变式只有单层验证**（MR-6 "critical-invariants-need-depth" 被静默违反），`MRD-NOTIFY-002` 报 6 个 Discord 频道有 4 个"7 天无活动"但 Mac Mini 实际每天都在推送 — 即**检测器假阳性 + 治理元规则未被强制**双重漏洞。② **MR-6 深度补齐 7 个 critical 单层不变式**：(1) **INV-TOOL-001 tool-count-limit**：原 python_assert 实际跑 filter_tools() 是运行时，但 verification_layer 错标 `[declaration]` — 新增 `file_contains` 声明层 check (proxy_filters.py 静态引用 MAX_TOOLS) + 修正标签为 `[declaration, runtime]` (2) **INV-TOOL-002 max-tools-config-imported**：已有 file_contains + python_assert 双层但 verification_layer 错标 — 纯元数据修复 (3) **INV-DEPLOY-002 auto-deploy-never-modifies-crontab**：原单层静态文本扫描 — 新增 `shlex.shlex(posix=True)` AST-tokenize 运行时 check 遍历 auto_deploy.sh 拆出 token 序列检测 `crontab -e/-i/-r/-` 或 `crontab_safe.sh add/remove/set/replace` 危险组合 (4) **INV-CRON-003 bash-lc-enforcement**：原单层运行时 (--full 模式 crontab -l 解析) 在 dev 环境完全 skip — 新增 `file_contains` 声明层守卫 V37.2+ 的 `_cron_cmd_invokes` 精确匹配器 + `cmd.endswith(entry)` 词边界模式，防止未来重构回退到子串匹配 (5) **INV-ENV-002 cron-env-smoke-test**：原单层运行时 (bash -lc 读 OPENCLAW_PHONE) dev 环境 skip — 新增声明层 2 个 check：preflight_check.sh `REQUIRED_VARS=(...)` 数组必含 `OPENCLAW_PHONE` (regex 提取数组字面量) + YAML 源码必须保留占位号 `+85200000000` 字面量作为 runtime 比较的反例守卫 (6) **INV-DREAM-002 reduce-path-must-not-re-run-map-loops**：原三 file_contains 声明层可证 `SKIP_MAP_LOOPS=true` 字面量和门控存在，但无法证明**门控在循环之前生效** — 新增运行时顺序锁 python_assert 读 kb_dream.sh 行号断言 `SKIP_MAP_LOOPS=true` 赋值 `assign_line < gate_1a_line` 且 `< gate_1b_line`，防止重构把赋值移到循环之后的回归 (7) **INV-CACHE-002 notes-cache-key-stable-under-mtime-drift**：原四 file_contains 只能证明字面量存在，无法证明"同内容不同 mtime 真的产生相同 hash" — 新增运行时 python_assert 用 `tempfile.TemporaryDirectory` 构造 3 个文件 (p1/p2 同内容不同 mtime, p3 不同内容)，`os.utime` 人为分离时间戳模拟 mm_index/kb_embed 触碰，inline `hashlib.md5(content).hexdigest()[:12]` 断言 `h1==h2` 且 `h1!=h3`。**注意**：`hashlib.md5` 调用必须 inline，**不能** 包成 `def _content_hash(s): ...` 嵌套函数——exec() 作用域陷阱下嵌套 def 看不到 exec locals 的 import（第一次运行因此 NameError）。③ **INV-LAYER-001 新增 (自我强制 meta-invariant)**：`critical-invariants-have-two-layers`, meta_rule=MR-6, severity=critical, verification_layer=[declaration, runtime]，把 MRD-LAYER-001 的 `severity_when_missing: warn` 升级为 fail-stopping hard invariant —— python_assert 遍历自身 YAML，任何 `severity=="critical"` 但 `len(verification_layer) < 2` 的不变式都让 governance_checker 直接 `exit 1`，未来任何新增 critical 不变式只要少于 2 层就被 CI 拦截。④ **MRD-NOTIFY-002 检测器重写 (根治假阳性)**：原 `_discover_silent_channels()` 只 grep `~/*.log` 里的 `--topic X` 字符串，但 32 个 job 中**只有 4 个用 notify.sh** (arxiv/dblp/semantic_scholar/ontology_sources)，其余 28 个直接 `openclaw message send --channel-id "$DISCORD_CH_X"` bypass notify.sh 永不产生 `--topic` 字样 → ontology/tech/freight 等频道误报 silent。重写为两层检测：**source layer** (dev + full 都跑，扫 `jobs/*/run_*.sh` + `*.sh`，每个 topic 的 caller 可以是 `--topic T` 或 `DISCORD_CH_<T_upper>` 任一形式) + **activity layer** (仅 --full 且 `~/.kb` 存在，用 `notify_queue/*.json` 和 `jobs/*/cache/*.log` 的 **mtime** 而非字符串 grep；只对"源码有 caller 但运行时静默"的 topic 报警)。⑤ **governance_ontology.yaml v3.8 → v3.9**：34→35 不变式 (+INV-LAYER-001)，126→139 检查 (+10: TOOL-001/DEPLOY-002/DREAM-002/CACHE-002 各 +1, CRON-003/ENV-002 各 +2, LAYER-001 +2)，9 meta rules 不变。⑥ **全量回归**：31 suites / **927 tests** / 0 fail / 35/35 invariants / 125/125 checks in dev (136 total, 11 skip runtime-only) / **9/9 meta rules** (从 V37.7 的 8/9 提升) / MRD-LAYER-001 **✅ "所有 15 个 critical 不变式都有 ≥2 层验证深度"** / MRD-NOTIFY-002 **✅ "所有 6 个频道均有 source-layer caller"** / 安全评分 93/100。⑦ **血案元规则兑现**：V37.8 是 MR-7 (governance-execution-is-self-observable) 的第二轮兑现 — V37.3 MR-7 首次引入 + INV-GOV-001 修复 summary 吞 error；V37.8 把 MR-6 从"警告层"提升为"强制层"，同时修复检测器本身的假阳性 (MRD-NOTIFY-002)。治理系统真正做到"不仅治理别人，也治理自己"。 |
| V37.7 | 2026-04-11 | **双跑审计闭环 — 三合一修复 + MR-8/MR-9 元规则形式化（MR-4 第 4/5 次演出 + 元规则预防层首次加入）** — ① **Bucket 1 (P0) `run_discussions.sh` V37.6 第 16 处漏网** (`jobs/openclaw_official/run_discussions.sh:104`)：V37.6 migration 14 个 cron 到 `kb_append_source.sh` helper 时漏掉 discussions（4×/天 08:15/12:15/16:15/20:15 的直接 `>> "$KB_SRC"` append），MR-4 silent-failure 第 5 次演出。修复删除 loop 内直写 + post-loop 走 helper + **`SLOT_TAG="$(date +%H:%M)"`** 区分同日多 run 避免 H2 section dedup 误吞第二次。② **Bucket 2A `audit_log.verify_chain` JSON parse error prev_hash 级联 bug**：V37.6 verify_chain 在 JSON parse 失败时**未更新 `prev_hash`** → 单行损坏后所有下游有效行都被误报为 prev-mismatch。修复改 `prev_hash: "str \| None"`，parse error 时置 None + 下行跳过 prev 指针检查（仍独立验证自身 hash），首个有效 hash 后重新续链。③ **Bucket 2B `kb_dedup.find_duplicate_notes` dangling index refs 假重复**：V37.6 find_duplicate_notes 把"index 有 entry 但磁盘无文件"的孤儿条目当作正常 entry → 按 summary 分组 → 和健在兄弟被误判为重复 → `--apply` 时删掉**活着**的兄弟文件（因为 dangling 排第一个被 kept，健在文件被 removed），MR-4 第 4 次演出。修复 (a) 新增 `find_dangling_index_entries(index)` 独立 reporter (b) `find_duplicate_notes` 内部 V37.7 pre-filter 用 `os.path.exists()` 剔除 dangling refs 后再分组 (c) `main()` 在 `--apply` 时把 dangling 从 `index.entries` 直接移除（**不**动磁盘文件），dry-run 时打印 warning。④ **Bucket 2C `kb_evening_collect` label semantics 修复**：V37.6 WA header `今日笔记 298 篇` 把 `note_count`（`rc.read_index_stats` 返回的**历史累计**笔记总数）错标为"今日"——evening 报告整个定位就是"今日"但数字是 298 天累计。修复新增 `import glob` + `count_today_notes(kb_dir, today)` 扫 `notes/` 前缀匹配 `YYYYMMDD` 的 .md 文件 + `build_evening_prompt/markdown/wa_message` 全部加 `today_note_count` 参数（分列"笔记总数 298"和"今日新增 5"），evening.sh wrapper 同步提取 `TODAY_NOTE_COUNT` 并打印到日志。⑤ **Bucket 3A MR-8 `copy-paste-is-a-bug-class` 从 candidate 正式形式化**：V37.6 changelog 提过 MR-8 candidate 但从未进入 `meta_rules:` 段——V37.7 正式加入，description 阐述"跨模块复制相似逻辑 = 引入 bug 类"+ "正确做法是提取公共原语为 import 模块 + 差异注入扩展"+ "正向兑现 = V37.6 `kb_evening_collect` 通过 `import kb_review_collect as rc` 复用 5 个原语 + 只覆写 evening-specific 行为"，INV-EVENING-001 从 MR-4 重归属到 MR-8。⑥ **Bucket 3B MR-9 `state-writes-go-through-helper-not-raw-redirect` 新增元规则**：把"shell `>>` 直写持久化 state 文件必须走 helper"从"14 个 caller 都要记得"的纪律问题升级为"ontology 架构硬规则"，包含 enforcement (INV-SRC-001 声明 scan + 运行时 15 job 校验) + derivative_invariants [INV-SRC-001, INV-DEDUP-001, INV-DEDUP-002] + blood_lessons（438 行 sources 污染 + run_discussions 第 16 处漏网），MR-9 是 MR-4 silent-failure 的**上游预防层**——让 CI/governance 自动拦截而不是靠程序员记忆。INV-SRC-001 从 MR-4 重归属到 MR-9。⑦ **INV-GOV-001 check 2 去 global mutation 重构**：V37.6 把 `governance_checker.JSON_MODE` 全局变量 save/mutate/restore 来注入 fake 状态测 summary——MR-8 的反面例子"靠 global state 做 dependency injection"。修复 `print_results(results, json_mode=None)` 加显式参数（None 时 fallback 到 module-level global 保持向后兼容），INV-GOV-001 check 2 改为 `print_results(fake_results, json_mode=False)` 直接注入。⑧ **INV-SRC-001 catch-all scan 加固 (Bucket 1)**：新增 2 个运行时 check — `V37.7 全量扫描 jobs/**/*.sh 不得有任何 >> "$KB_SRC" 直写`（catches 任何位置，不限 loop-tail）+ `V37.7 catch-all 任何 >> .*sources/.*\\.md 路径必须通过 helper`（路径级 regex，future-proof 对抗新增 sources 变量名）+ job_sites 列表扩到 15 job（加入 run_discussions.sh 的 1 处写入点）。⑨ **INV-DEDUP-002 新增不变式** `kb-dedup-pre-filters-dangling-index-entries`（meta_rule=MR-4, verification_layer=[declaration, runtime], severity=high）4 个 check：声明层 grep `def find_dangling_index_entries` + `V37.7` 注释标记；运行时层 2 个 python_assert 用 tempfile 隔离 KB_BASE/NOTES_DIR/SOURCES_DIR/INDEX_FILE/REPORT_JSON — (a) 构造 2 entries 同 summary 一死一活，assert exact_dupes=0 + dangling=1 (b) 端到端 main `--apply` 路径测 monkey-patched `sys.argv`，assert dangling 从 index 移除且健在文件仍存在。⑩ **governance_checker.py meta_rules counter 动态化**：原硬编码 `元规则: {len(mr_used)}/6`，V37.7 改为 `_load().get("audit_metadata",{}).get("meta_rules", len(mr_used))`，跟随 audit_metadata 自动刷新。⑪ **governance_ontology.yaml v3.7 → v3.8**：33→34 不变式，119→126 checks，7→9 meta rules，v3.8 changelog 记录 8 项闭环。⑫ **新增 12 单测**：test_kb_dedup.py `TestDanglingIndexRefs` 4 测试 (find_dangling / no-dangling when all exist / dangling not reported as duplicate / apply cleans dangling from index) + test_audit_log.py `test_json_parse_error_does_not_cascade` 1 测试（parse error 1 行，line 3 不被 prev-mismatch 误报）+ test_kb_evening.py 更新 `TestBuildEveningPrompt/Markdown/WaMessage` 3 类测试到新签名 + 新增 `TestCountTodayNotes` 3 测试 + `test_markdown_distinguishes_total_vs_today_counts` + `test_header_distinguishes_total_vs_today`。⑬ **`full_regression.sh` 接入 test_kb_evening.py**（V37.6 新增但从未登记）。⑭ **全量回归**：31 suites / **927 tests** / 0 fail / 34/34 invariants / 115/126 checks / 8/9 meta rules / 安全评分 93/100 |
| V37.6 | 2026-04-11 | **数据层 P0 三合一修复 + P1 kb_evening 架构对齐（MR-4 第三次演出 + MR-8 正向兑现）** — ① **P0-1 Content-blocks repr bug**：`proxy_filters.extract_user_text()` 把 OpenAI content blocks 列表直接 `str()` 成 `[{'type': 'text', 'text': '...'}]` 字面量被当"用户输入"写进 KB，发现于 kb_dedup dry-run 报告。引入 `flatten_content(content)` 只提取 text 字段，tool_proxy.py 对话捕获路径同步调用。② **P0-2 sources H2 repeat-append**：14 个 RSS/cron 抓取 job 各自独立 `>> sources/*.md` 把同一天数据追加多次，滚动窗口同条目在 `## $DAY` 下重复出现 N 次。新增 `kb_append_source.sh` helper（`$SLOT_TAG` 支持多 run/day 如 ontology_sources 10:00/20:00），14 个 job 全部迁移。③ **P0-3 kb_dedup.py 二次演出**：(a) file-level `seen` 集合把跨 H2 section 的合法日期复现误判为 duplicate 并在 `--apply` 时删除历史 (b) `find_duplicate_notes` 只扫 `index.json`，漏掉未索引的 `notes/` 孤儿。修复：`seen` 在每个 `## ` H2 边界重置；find_duplicate_notes 改为扫 NOTES_DIR 目录。④ **P1 kb_evening 架构对齐**：原 kb_evening.sh 用"文件名前 80 字"作假摘要 + 硬编码 7 源枚举 + 零 LLM 智能 + 无 fail-fast。新建 `kb_evening_collect.py` via `import kb_review_collect as rc` **复用**所有采集原语（load_sources_from_registry/extract_recent_sections H2 drill-down/collect_notes/collect_sources/call_llm），只覆写三件事：①DAYS 默认 1（今日窗口）②build_evening_prompt 改为"今日要闻/一条行动/明日关注/健康度"行动导向结构 ③build_evening_wa_message 🌙 emoji。kb_evening.sh 重写为 thin wrapper 遵循 V37.5 kb_review.sh 同款模式（fail-fast + [SYSTEM_ALERT] + env var heredoc 无 stdin 冲突 + 保留 kb_dedup 健康附注拼接 + 日志轮转）。⑤ **auto_deploy FILE_MAP 补齐 V37.5 部署缺口**：kb_review_collect.py 在 V37.5 发布时**从未被加入 FILE_MAP**（能跑纯靠 auto_deploy 的 git pull + kb_review.sh 的 $HOME/openclaw-model-bridge/ fallback 路径），V37.6 一并补齐 `kb_dedup.py` / `kb_review_collect.py` / `kb_evening_collect.py` 三个 Python 模块。⑥ **INV-EVENING-001** `kb-evening-fails-loud-not-silent`（meta_rule=MR-4, severity=high, verification_layer=[declaration, runtime]）17 个 check：reuse 契约（import kb_review_collect + 禁止 load_sources_from_registry/extract_recent_sections/call_llm 重定义）+ thin wrapper 契约（V37.6 标记/[SYSTEM_ALERT]/不含 review_${DATE}.md 粒度错配）+ FILE_MAP 部署契约 + 反模式 python_assert 行扫 `\| python3 -`（regex 精确区分 `python3 -c 'inline'` 安全形式）+ fail-fast 顺序锁（llm_failed 分支 500 字符内必须 exit 1）+ 运行时 DAYS=1 过滤验证（直接调 `rc.collect_notes` 避免 V37.2+ exec() 闭包陷阱）+ mock_fail 不伪造产物契约。⑦ **新增 31 单测** `test_kb_evening.py`（8 类）：TestReusesKbReviewHelpers(3) + TestOneDayWindow(2) + TestBuildEveningPrompt(4) + TestBuildEveningMarkdown(4) + TestBuildEveningWaMessage(3) + TestRunOrchestrator(4) + TestKbEveningShellGuards(10) + TestKbEveningShellRuntime(1，真 subprocess + stub collector E2E）。⑧ **test_kb_dedup.py 陈旧测试同步**：原测试把跨 H2 section 的同一行视为 duplicate，V37.6 H2-scoped 后这是合法的滚动窗口复现，更新 2 个 test case（test_duplicate_lines/test_apply_source_dedup）改为 section 内重复。⑨ **governance v3.5.1 → v3.7**：32→33 不变式，102→119 checks（新增 INV-KB-001/INV-SRC-001/INV-DEDUP-001/INV-EVENING-001 四个不变式 17 checks）。⑩ **案例文档** `ontology/docs/cases/kb_content_and_sources_dedup_case.md` 记录 MR-4 第三次演出（P0）+ MR-8 候选"copy-paste-is-a-bug-class"正向兑现（P1 via import reuse）。⑪ **全量回归**：30 suites / **886 tests** / 0 fail / 33/33 invariants / 108/119 checks / 安全评分 93/100 |
| V37.5.1 | 2026-04-11 | **kb_review.sh `pipe+heredoc` stdin 冲突热修复（V37.5 第一次 Mac Mini E2E 即触发的子血案）** — ① **触发**：V37.5 commit db6136f 推送后 Mac Mini 首次 `bash ~/kb_review.sh` 即崩溃 → `File "<stdin>", line 3, in <module>` → `json.JSONDecodeError: Expecting value: line 1 column 1 (char 0)`（stdin 读到空串） ② **根因**（经典 shell 反模式）：`echo "$COLLECTOR_OUTPUT" \| python3 - "$REVIEW_FILE" << 'PYEOF' ... json.load(sys.stdin) ... PYEOF` — pipe 把 JSON 喂进 `python3` 的 stdin，heredoc **同时**把代码喂进同一 stdin；`python3 -` 优先消费 heredoc 作为代码源，pipe 的 JSON 被丢弃，`json.load(sys.stdin)` 读到空串直接炸 ③ **为什么 V37.5 的 44 单测 + 14 governance checks 全过却漏掉**：测试全部是声明层 grep guard（文件包含 `fail-fast`、包含 `[SYSTEM_ALERT]`、包含 H2 parser 函数…），**从未真正 `subprocess.run()` 跑一次 shell 脚本** → 直接喂养 **MR-6 (grep 守卫不足，验证深度必须包含运行时层)** ④ **三层修复**：(1) **代码层** `kb_review.sh:154-163`：删除 `echo ... \| python3 - << PYEOF` 反模式，改用 `COLLECTOR_OUTPUT="$COLLECTOR_OUTPUT" REVIEW_FILE="$REVIEW_FILE" python3 << 'PYEOF'` 环境变量模式 — heredoc 只承载代码，零 stdin 竞争 (2) **治理层** `INV-REVIEW-001` +2 checks：`V37.5.1 反模式` python_assert 行扫禁止 `\| python3 -`（跳过 `#` 注释以允许文档引用反模式）+ `V37.5.1 runtime` python_assert **真实 `subprocess.run()`** 跑 kb_review.sh（mock collector 喂 canned JSON，断言 review 文件落盘 + stderr 不含 `JSONDecodeError`）(3) **测试层** `test_kb_review.py` +4 单测（44→48）：`test_no_pipe_heredoc_stdin_collision`（静态守卫）+ `TestKbReviewShellRuntime` 新类 3 测试：`test_env_var_heredoc_pattern_writes_file`（正向隔离验证）+ `test_pipe_heredoc_antipattern_actually_fails`（负向反证 JSONDecodeError）+ `test_kb_review_sh_end_to_end_mock_collector`（真 E2E subprocess + 临时目录 + mock collector） ⑤ **Mac Mini 生产 E2E 验证通过**：`bash ~/kb_review.sh` → `开始 LLM 深度分析（7 天回顾，from registry）... 回顾文件已生成: /Users/bisdom/.kb/daily/review_20260411.md ... 回顾已推送（WhatsApp + Discord #daily） ... 知识回顾 20260411 \| 覆盖 12 源 \| 本期笔记 299 篇 \| LLM: ✓` — 验证了 (a) registry-driven 发现 12 源（governance 断言 `>=12` ✓）(b) H2 drill-down 从 7 天窗口提取 299 notes (c) LLM 调用成功未触发 fail-fast (d) V37.5.1 env var 模式写入 review 文件零 JSONDecodeError (e) notify.sh 双通道推送 WhatsApp + Discord #daily 成功 ⑥ **governance_ontology.yaml v3.5 → v3.5.1**（29 不变式不变，81→83 checks）⑦ **血案元认知**：同一个 MR-4 血案类三天内第二次演出（V37.5 主血案 → V37.5.1 子血案），子血案证明**主血案修复本身也可能埋坑**，`test_kb_review.py::TestKbReviewShellGuards` 全部 grep 通过但从未运行脚本 → MR-6 不再是"建议"而是"基础假设" |
| V37.5 | 2026-04-11 | **kb_review 6-issue silent degradation 血案闭环修复（fail-fast + registry-driven + H2 drill-down）** — ① **血案类归属**：与 V37.4.3 PA 告警污染同属 MR-4 (silent failure is a bug) 效果层静默降级血案类——声明层检查全过、status.json 全绿，但端到端效果完全失效 ② **6 个相互掩护的 bug**：(1) shell `export NOTES_CONTENT` 在 `python3 -c` 子进程调用**之后**才写入 → subprocess fork 时 env 快照已定型，`os.environ.get()` 永远拿空字符串 → LLM 收到空壳 prompt (2) 硬编码源枚举 7 项，漏掉 V37 新增 `ai_leaders_x` + V37.1 新增 `ontology_sources` (3) LLM 失败后**机械 fallback** 把"行级日期匹配"的容器标题残渣（例如 `## 今日arXiv精选(2026-04-10)`）直接写入 review 文件伪装成功 (4) `status.json` 永远硬写 `"llm": true`，不区分 ok/failed (5) 行级日期匹配只保留含日期的单行，把整个 H2 section 的论文 body 全部过滤掉 — 结构化 markdown 粒度错配 (6) 悬空承诺"回复任何话题可深入讨论"但 LLM 从未真正看过回顾内容 ③ **结构性修复**：(1) **全 Python 化** `kb_review_collect.py` 作为纯模块（load_sources_from_registry + extract_recent_sections H2 drill-down + call_llm 80-char 最小阈值 + run(llm_caller=...) 可注入 mock），消除 shell scope bug class (2) **jobs_registry.yaml 扩展 `kb_source_file` + `kb_source_label` 字段到 12 个 enabled source-producing job**（arxiv/hf_papers/semantic_scholar/dblp/acl_anthology/github_trending/rss_blogs/ai_leaders_x/freight/openclaw_run/hn/ontology_sources），registry-driven 消除硬编码漂移 (3) **kb_review.sh 重写为 thin wrapper**：调用 collector 后按 status 分派 — `ok` 写 review 文件 + 推送 daily topic，`llm_failed`/`collector_failed`/`send_failed` 均推送 `[SYSTEM_ALERT]` + `exit 1` fail-fast (4) status.json 诚实字段 `llm_status: ok\|failed\|unknown`，不再永远写 true (5) 移除悬空 follow-up 承诺 ④ **INV-REVIEW-001** `kb-review-fails-loud-not-silent`（severity=critical, verification_layer=[declaration, runtime]）：14 个 check 覆盖声明层（V37.5 版本标记/`[SYSTEM_ALERT]` 存在/不含机械 fallback/不含悬空承诺/三个核心函数定义/jobs_registry.yaml kb_source_file 字段/**fail-fast 顺序锁 python_assert**（llm_failed 分支 500 字符内必须 exit 1））+ 运行时层（**registry 真实发现 >=12 源含 ai_leaders_x + ontology_sources**/**H2 parser drill-down 过滤正确性**（窗口内 Paper A 保留 + 窗口外 Paper B 剔除 + 今日 Paper C 保留）/**mock LLM 失败契约**（status=llm_failed 不伪装 ok + 不产出 review_markdown/wa_message 伪造产物）/call_llm 最小内容阈值声明） ⑤ **新增 44 单测** `test_kb_review.py`：TestLoadSourcesFromRegistry(7) + TestExtractRecentSections(8) + TestCollectNotes(4) + TestCallLlm(6, urlopen mock) + TestRunOrchestrator(6, llm_caller 注入) + TestBuildOutputs(4) + TestCollectSources(1) + TestKbReviewShellGuards(8) ⑥ **案例文档** `ontology/docs/cases/kb_review_silent_degradation_case.md`（四维度完整因果链架构图 + 三层根因 + 时间线还原 + 6-bug 条件组合分析 + V37.4.3 血案类对照表 + MR-4 持续扩展 + 元规则 MR-8 候选"跨文件枚举必须声明单一真理源"） ⑦ **governance_ontology.yaml v3.4 → v3.5**（28→29 不变式，67→81 checks） ⑧ 接入 full_regression.sh 第一层 |
| V37.4.3 | 2026-04-11 | **PA 告警污染血案闭环修复（A+C 双防线）** — ① **事件**：13:06 用户问"AI Agent 终极架构：本体×随机×贝叶斯"，PA 回复"已收到系统告警跟进任务，请打开系统偏好设置添加 /usr/sbin/cron 到完全磁盘访问权限"——完全答非所问 + 编造错误的 macOS FDA 指令（launchd 管理的 cron 从不需要 FDA）② **根因六层深挖**：(a) 12:30 job_watchdog 通过 `notify.sh --topic alerts` 推送 WARNING (b) Gateway 把推送写入 `sessions.json` 作为 `assistant` role 消息——**不区分"PA 主动回答"和"系统告警推送"** (c) Proxy `truncate_messages()` 保留"最近 N 条"，36 min 内的告警仍在窗口内 (d) Qwen3 attention 读到 assistant 告警中的 "排查建议" + "cron_doctor.sh"，把它当"PA 自己说过的话" (e) 用户新问题被跨主题错误关联为"告警跟进响应" (f) LLM 编造 macOS FDA 指令（training data 模板填空）③ **Path A 结构性隔离**（主防线）：(1) 推送侧注入标记——`notify.sh` 在 `topic=alerts` 分支、`auto_deploy.sh` 在 `quiet_alert()`、`run_hn_fixed.sh`/`jobs/openclaw_official/run.sh`/`run_discussions.sh` 的直接 send 路径，统一在告警消息首行加 `[SYSTEM_ALERT]`  (2) 消费侧剥离——`proxy_filters.py` 新增 `SYSTEM_ALERT_MARKER = "[SYSTEM_ALERT]"` 常量、`filter_system_alerts(messages, log_fn)` 函数（str content + OpenAI content blocks 双格式支持，system role 保留例外，`_message_starts_with_alert_marker` 只在首行匹配避免误伤）(3) **顺序硬约束**——`tool_proxy.py` 在 `truncate_messages()` **之前**调用 `filter_system_alerts()`（单测 `test_integration_with_truncate` + INV-PA-001 的 python_assert 检查源码中位置 `filter_idx < trunc_idx`，双重锁定）④ **Path C 治理+行为双防线**：(1) **SOUL.md 规则 10 "告警消息不跟进（2026-04-11 血案规则）"**——最高优先级的问答对齐规则，包含：识别标志（`[SYSTEM_ALERT]` + 告警模式）、禁止行为（已收到跟进/系统偏好指令/编造 macOS FDA）、**主题对齐硬规则**（回复必须与用户最新消息主题直接对齐）、幻觉防线（launchd 不需要 FDA）、13:06 案例警示原文 (2) **INV-PA-001** `alert-does-not-pollute-chat-context`（severity=critical, verification_layer=[declaration, runtime]）：10 个 check 覆盖 SYSTEM_ALERT_MARKER 定义 + filter_system_alerts 函数 + tool_proxy 导入 + **顺序锁 python_assert**（filter_idx < trunc_idx）+ 5 个推送脚本标记注入 + **运行时 python_assert**（真实告警消息注入 → filter 正确剥离，4 个 sub-assertion：普通 assistant 告警剥离/system role 保留/OpenAI content blocks/标记只在开头匹配不误伤）(3) **INV-PA-002** `soul-rule-10-present`（severity=high, verification_layer=[declaration]）：5 个 check 确保 SOUL.md 包含规则 10 核心声明（"告警消息不跟进" / "2026-04-11 血案规则" / "主题对齐" / "完全磁盘访问权限" / `[SYSTEM_ALERT]`）⑤ **案例文档** `ontology/docs/cases/pa_alert_contamination_case.md`（六层根因深挖 + 完整因果链架构图 + 时间线 + **六条件组合爆炸分析**：告警频率/时间差/可执行触发词/话题跳变/SOUL 无隔离规则/Proxy 无剥离——六个条件同时出现才爆炸 + 纵深防御论证）⑥ **新增 21 单测**：`TestFilterSystemAlerts`（14 个：marker 常量/assistant 剥离/user 剥离/system 保留/content blocks/leading whitespace/空消息/缺失 content/log_fn 调用/truncate 集成/多告警全剥离等）+ `TestNotifyShAlertMarker`（5 个：notify.sh/auto_deploy/run_hn_fixed/run_discussions/run.sh grep）+ `TestToolProxyImportsAlertFilter`（2 个：import + 顺序）⑦ **governance_ontology.yaml v3.3 → v3.4**（26→28 不变式，52→67 checks）⑧ **全量回归**：29 suites / **807 tests** / 0 fail / 28/28 invariants / 56/67 checks / 安全评分 93/100 |
| V37.4.2 | 2026-04-11 | **Dream Reduce 短输出 bug 二段闭环修复** — ① 触发：V37.4.1 Mac Mini 14:01 run 三次重试全是 876/876/927 chars，完全一致的失败输出 → 定位根因是 Qwen3 server-side prompt caching + 缓存读取路径 bland 重复 header 让 LLM 陷入"简短总结"收敛模式 ② **结构化修复**（cache-only 读取路径 line 414-465）：用 bash 数组 `UNIQ_NOTE_SIG_BLOCKS=()` 收集去重后的 signal blocks → 拼接带覆盖统计的 meta header（`> 覆盖 N 条用户笔记，提取出 M 个独立信号簇`）→ 每簇编号 `### 笔记信号簇 i / N` 赋予独立身份；Phase 1b cache-hit header 同步改为 `## 用户笔记缓存命中: $name`（用文件名区分）③ **重试变体修复**（line 1011-1024）：retry=1 用原 `REDUCE_PROMPT`，retry≥2 prepend `【第 N 次尝试 — 上一次响应过短，不合格】` + 显式硬性要求"6 章节全覆盖 + 不少于 2500 汉字"，变体 prompt prefix 改变 hash 直接绕过任何 server cache + 显式长度约束顶破收敛 ④ 新增 6 单测 `TestV37_4_2_CacheReadStructureAndRetryVariation`（UNIQ_NOTE_SIG_BLOCKS 数组/覆盖 meta header/编号簇/bland header 移除/变体 prompt/llm_call 用 $cur_prompt）⑤ **Mac Mini 15:02 生产验证**：retry 1 = 1402 chars 短仍被 server cache 命中（证据确凿）→ retry 2 变体 prompt = **21871 chars / 7000 汉字**，6 章节完整展开，9m24s 总耗时，3/3 WhatsApp+Discord 推送成功，内容识别出"本体+LLM+Agent 深度融合"主题并给出可执行建议 ⑥ 32 suites / 32 Dream 单测 / 0 fail |
| V37.4.1 | 2026-04-11 | **Dream Reduce 重试降级机制 第一次尝试** — ① 触发：V37.4 Mac Mini Run 1 (c957075) Fix A cache-only fast path 生效（11s 缓存加载），但 Phase 2 Reduce retry 1 = 3967 chars → retry 2 = 906 chars 直接砸穿，用户反馈"已经超过5min 触发LLM重试" ② **重试降级修复**（line 975-1045）：引入 `BEST_RESULT` 跟踪历次最长结果 + 温度衰减数组 `REDUCE_TEMPS=(0.85 0.6 0.4)` + 双阈值 `MIN_DREAM_CHARS=3000`（目标 ≈1000 汉字）/ `MIN_ACCEPTABLE_CHARS=1500`（真正失败底线）+ fallback `DREAM_RESULT=$BEST_RESULT` 保留最佳尝试 + fatal exit 用 MIN_ACCEPTABLE_CHARS 而非 MIN_DREAM_CHARS 以避免可接受输出被错误丢弃 ③ 新增 6 单测 `TestReduceRetryFallback`（MIN_DREAM_CHARS 阈值/REDUCE_TEMPS 衰减/BEST_RESULT 跟踪/fallback 赋值/fatal 底线阈值/retry 循环结构）④ **验证失败**：Mac Mini Run 2 (8dd0260) 三次重试 876/876/927 chars 完全一致 → 暴露"简单重试无法突破 server-side prompt caching"这一更深层根因，为 V37.4.2 铺路 |
| V37.4 | 2026-04-11 | **Dream Map 预算溢出 + cache key 稳定性 三层修复** — ① 触发：2026-04-11 `--map-notes`(00:40) + full run(03:00) 连续 60 min 超时，Reduce 从未执行，WhatsApp 告警"Dream 失败"。根因三层：(a) 286 notes × 76s/batch ÷ 4.4 = **82 min > 60 min** workload 数学溢出 (b) Reduce full run 重跑 Phase 1a/1b LLM 循环，不相信缓存 (c) Notes cache key = md5(批次拼接) 对 mtime/sort/batch 组合敏感，00:40 写的缓存 03:00 全部 miss ② **Fix A (契约)**：Reduce path 新增 cache-only fast path — 扫 `$MAP_DIR` 填充 MAP_SIGNALS / NOTES_SIGNALS，`SKIP_MAP_LOOPS=true` 门控 Phase 1a + Phase 1b + Fast-mode elif，03:00 full run 从 60 min 压到 <5 min ③ **Fix B (效率)**：Notes 批次 15 条/12000B → **30 条/24000B**，LLM 调用次数减半（~47→~24） ④ **Fix C (稳定)**：per-note cache，key=`md5(content)` 前 12 位（+ macOS `md5 -q` fallback），同 batch N 个 note 共享 signals（dedup via signal hash），mtime/sort/batch 组合漂移都不再触发 miss；第二天只有新增 notes 是 cache miss ⑤ **预算动态化**：`DREAM_TIMEOUT_SEC` 按 MAP_ONLY 5400s (90min) / 3600s (60min) 分配；`DREAM_TIMEOUT_SEC_OVERRIDE` env 支持调试 ⑥ 案例文档 `ontology/docs/cases/dream_map_budget_overflow_case.md`（四维度因果链 + 三层根因 + 时间线 + 条件组合 + 设计决策）⑦ 3 新不变式 `INV-DREAM-001` (map-budget-scales-with-mode) + `INV-DREAM-002` (reduce-path-must-not-re-run-map-loops, severity=critical) + `INV-CACHE-002` (notes-cache-key-stable-under-mtime-drift, severity=critical)，governance v3.2 → v3.3, 23→26 不变式 / 43→52 检查 ⑧ `test_dream_cache_stability.py` 20 单测锁定三 fix + 预算动态 + flush 辅助函数 + Sources 键不变 ⑨ 接入 full_regression.sh → **29 suites / 774 tests / 0 fail / 26/26 invariants / 41/52 checks** |
| V37.3 | 2026-04-11 | **Governance 自观察元规则 MR-7 + INV-GOV-001 summary-counts-all-non-pass** — ① 案例文档 `ontology/docs/cases/governance_silent_error_case.md`（四维度因果链架构图 + 三层根因 + 时间线 + 条件组合 + 本体喂养）完整分析 V37.2+ 三层嵌套盲区：子串匹配→exec 作用域陷阱→silent error summary，提炼"观察者的自我盲区"元洞察（治理系统从未对自己应用 MR-4） ② 新增 **MR-7 governance-execution-is-self-observable**：治理系统自身是一等被治理对象，check 层所有状态（pass/fail/skip/error）必须在汇总层有观察路径，不允许聚合时被透明化 ③ 新增 **INV-GOV-001 governance-summary-counts-all-non-pass**（meta_rule: MR-7，severity: critical，verification_layer: [declaration, runtime]）：声明层 grep 守卫 `r["status"] in ("fail","error")` + 运行时层构造 error 注入 probe 验证汇总不说"所有不变式成立"（含 JSON_MODE 全局状态 save/restore，解决嵌套调用视角污染） ④ 新增 `ontology/tests/test_governance_summary.py` 7 单测专门锁定 bug 3：error 状态不被吞、💥 图标正确、pure fail 仍用 ❌、all-pass 仍说 ✅、mixed fail+error 各自计数、源码级 grep 守卫 ⑤ 接入 full_regression.sh，**28 suites / 754 tests / 0 fail, governance 23/23 invariants / 32/43 checks** ⑥ governance_ontology.yaml 升级 v3.1 → v3.2（total_invariants: 22→23, meta_rules: 6→7, total_checks: 41→43） |
| V37.2+ | 2026-04-11 | **Governance INV-CRON 三层 bug 修复 + silent error detection 加固** — ① INV-CRON-003/004 子串匹配 false-positive（`kb_dream.sh` 被 Map-Reduce split 误报 ×3，`sname in l` 无法区分 prefix-subset entries）→ `endswith(entry)` + word-boundary `cmd[idx-1] in "/ \t\"'"` 精确匹配 ② YAML `_exec_python_assert` exec() 作用域陷阱（`def _cron_cmd_invokes` 在 exec 局部，`sum(1 for l in lines if ...)` 生成器表达式新作用域看不到，`NameError` 每次都炸）→ for-loop 替代生成器表达式 ③ `governance_checker.py` silent error bug（`failed_invs` 只数 `status=="fail"`，`error` 状态被忽略，💥 icon 藏在正文，汇总行显示 ✅ 所有不变式成立）→ 区分 `fail`/`error` 并在汇总行显式标注（`❌ N 违反, 💥 M 出错`） ④ 2 个新回归测试锁定：`test_yaml_no_generator_expression_over_cron_cmd_invokes`（grep 守卫）+ `test_exec_scope_trap_is_real`（正向对照，自带 Python 语义文档） ⑤ Mac Mini 生产验证：governance `--full` 40/41+💥 → **41/41 ✅**，preflight 75/1/2 → **76/0/2**（`kb_embed.py` 补齐 3 未索引 notes + 2 过期文件）⑥ 27 suites / **747 tests** / 0 fail |
| V37.2 | 2026-04-10 | **系统韧性加固 + Adapter Hot-Reload + 幻觉工具过滤 + 异常分析宪法** — ① Dream 配额爆炸链修复（智能退避 429/524 + Map 熔断 3 次 + inter-call 节流）② HN 垃圾推送修复（RSS 描述注入 + JSON 输出 + `__LLM_FAILED__` 信号 + 3 次重试）③ Adapter FALLBACK_CHAIN hot-reload（`_build_fallback_chain()` 提取 + daemon 线程 + feature flag `ADAPTER_HOT_RELOAD` + /health 暴露）④ Proxy 幻觉工具过滤（`<tool_call>` XML 清理：主响应路径 + search_kb followup 路径）⑤ 原则 #26 升级为宪法级异常分析五步法 ⑥ Dream 配额爆炸链案例文档 + 2 新治理不变式（INV-QUOTA-001/INV-PUSH-001，19 不变式）⑦ 718 测试 |
| V37.1 | 2026-04-09 | **Ontology 信息源 + 对话数据零丢失 + 治理主动监控 + PA 批判性思考** — ① Ontology 专属信息源（4 RSS: W3C/JWS/DKE/KBS + 两层关键词过滤 + LLM 摘要 + Discord #ontology + KB 归档，cron 10:00/20:00）② kb_harvest_chat MapReduce 升级（分段提炼+去重，零对话数据丢失，28 单测）③ DBLP/S2 加 ontology 关键词 ④ X 监控加 4 位 ontology 先驱（Barry Smith/Guizzardi/Hitzler/Horrocks）⑤ adversarial_audit 合并入 governance_ontology（17 不变式）⑥ 每日 governance_audit cron（07:00 自动执行+失败告警）⑦ SOUL.md 规则 9 批判性思考（反迎合+禁模糊关联+PA 回声室案例分析）⑧ Ontology 终态架构文档（四层+三阶段门控+迁移 Phase 3-5）⑨ ScienceDirect 描述正则修复 ⑩ 692 测试 |
| V37 | 2026-04-08 | **V3 路标启动 + LLM 协作方法论 + 对话数据闭环** — ① Provider Plugin Interface（YAML/Python 插件+合约验证+Extension Guide+128单测）② Capability-Based Routing（find_by_capability+build_fallback_chain+auto-discovery fallback chain 接入 adapter.py）③ 对话数据闭环（proxy 热路径捕获→kb_harvest_chat.py 冷路径提炼→MEMORY.md 索引→KB 可检索）④ KB 统一（HN+Freight 双写 notes 对齐 12/12 job）⑤ LLM 协作 4 条新原则（#22 顺势设计/#23 链式幻觉防范/#24 触发词机制/#25 对话数据一等公民）⑥ industrial_ai_paradigm.md 切断幻觉链 ⑦ 692 测试 |
| V36.3 | 2026-04-08 | **Runtime Governance + Ontology Shadow Mode** — ① 遗留修复(crontab漂移3job+重复清理35→28+DBLP/Dream推送恢复+notify.sh zsh兼容+smoke test python3检测) ② Governance v3(12→15不变式: 运行时层INV-CRON-003/004+INV-ENV-002, MR-6多层深度要求) ③ 验证深度三层模型(声明/运行时/效果, governance自我意识盲区, MRD-LAYER-001) ④ 语义推理落地(classify_tool_call从属性推理risk_level+policy_tags) ⑤ Phase 2 shadow模式(off→shadow→on三档, Mac Mini生产上线) ⑥ 832测试 |
| V36.2 | 2026-04-07 | **Governance Ontology + 对抗审计体系** — ① Dream修复(printf注入+残留锁) ② 凌晨静默期(00-07零推送) ③ Crontab漂移检测(registry vs crontab) ④ 对抗审计22个声明-实际断裂→adversarial_audit.py(9项) ⑤ governance_ontology.yaml v2(12不变式+28可执行检查+5元规则) ⑥ Phase 0元规则自主发现(23个未覆盖job) ⑦ 工具数量硬断言(MAX_TOOLS import+截断) ⑧ 立场文章(EN+ZH) + Ontology子项目深度建设(Tool Engine+宪法+PoC+特性开关) |
| V36.1 | 2026-04-06 | **Agent Dream v2 + Ontology KB** — MapReduce 全量 KB 探索（14源+226笔记）+ Notes 一等信号 + Ontology KB 创建（7文件）+ 论文监控加 ontology 关键词 + X 监控 9→15 人（+Palantir）+ Cron 调度优化（~55→30 次/天）+ 凌晨 GPU 黄金窗口 |
| V36 | 2026-04-05 | **V2 路标双P0完成** — Agent Reliability Bench（7场景47检查） + Memory Plane v1（4层统一接口+45单测+架构文档） + 560 测试 |
| V35 | 2026-04-05 | **V1 路标冲刺** — SLO Benchmark 实验报告 + Quick Start 一键 demo + Golden Test Trace + Sub-agent PoC（链路通，deferred 等模型升级）+ 605 测试 |
| V34 | 2026-04-03 | **Stage2 启动** — Provider Compatibility Layer + 导师战略复盘嵌入治理体系 + V1/V2/V3 路标 + 461 测试 |
| V33 | 2026-04-03 | Discord 双通道支持 + 统一推送 notify.sh + Gateway 不升级可用 |
| V30.5 | 2026-03-31 | search_kb 混合检索 + 论文监控矩阵 5 源全覆盖 + DBLP 上线 |
| V30.4 | 2026-03-28 | SOUL.md 激活 + 三方宪法闭环 + 方法论进化（结果验证优先） |
| V30.3 | 2026-03-27 | 数据清洗 Phase 1 + 自定义工具注入机制 + WhatsApp E2E |
| V30.2 | 2026-03-26 | 安全评分体系 + 审计日志 + 持续安全机制 |
| V30.1 | 2026-03-26 | status.json 实时刷新 + KB 完整性 + 全量回归 374 测试 |
| V30 | 2026-03-26 | crontab 事故修复 + cron 安全三层保护 + 原子写入加固 |
| V29.5 | 2026-03-25 | KB 周趋势报告 + 模型智能路由 + 三方共享状态 |
| V29.4 | 2026-03-25 | 多模态图片理解（Qwen2.5-VL-72B 自动路由） |
| V29.3 | 2026-03-24 | 本地 Embedding + KB RAG 语义搜索 |
| V29.1 | 2026-03-14 | Fallback 降级链 + 每日备份 + Multi-Agent + MM 索引 |
| V29 | 2026-03-13 | KB 搜索/LLM 回顾/每日摘要注入 |
| V28.x | 2026-03-11~13 | 线程化 + preflight 体检 + WhatsApp 保活 + OpenClaw 架构同步 |
| V27.x | 2026-03-10 | Proxy 拆层 + 任务注册表 + auto_deploy + 回滚机制 |

## 常用命令

```bash
# 启动服务
nohup python3 ~/adapter.py > ~/adapter.log 2>&1 &
nohup python3 ~/tool_proxy.py > ~/tool_proxy.log 2>&1 &

# 健康检查
curl http://localhost:5002/health

# 一键重启
bash ~/restart.sh

# 运行单测
python3 test_tool_proxy.py
python3 test_check_registry.py

# 校验任务注册表
python3 check_registry.py

# 一键 smoke test（单测+注册表+连通性）
bash smoke_test.sh

# 全量回归测试（461个用例，发布前必须100%通过）
bash full_regression.sh

# 治理审计（ontology-native，23不变式+7元规则+6元发现）
python3 ontology/governance_checker.py              # dev 模式
python3 ontology/governance_checker.py --full        # Mac Mini（含 crontab/env/服务）
python3 ontology/governance_checker.py --json        # JSON 输出

# 安全评分（7维度100分）
python3 security_score.py
python3 security_score.py --update  # 写入 status.json

# 审计日志
python3 audit_log.py --tail 20      # 查看最近操作
python3 audit_log.py --verify       # 校验链式哈希
python3 audit_log.py --stats        # 统计概览

# 收工前全面体检（dev 环境）
bash preflight_check.sh
# 收工前全面体检（Mac Mini，含部署一致性+环境变量+连通性）
bash preflight_check.sh --full

# KB 搜索 / 摘要
bash kb_search.sh "关键词"         # 全文搜索
bash kb_search.sh --summary        # 统计概览
bash kb_search.sh --source arxiv   # 搜索来源归档
bash kb_inject.sh                  # 手动生成每日摘要

# Multimodal Memory（需要 pip3 install google-genai numpy）
python3 mm_index.py                # 增量索引媒体文件
python3 mm_index.py --reindex      # 重建全部索引
python3 mm_search.py "猫的照片"    # 语义搜索媒体
python3 mm_search.py --stats       # 索引统计

# KB 本地 Embedding + RAG（需要 pip3 install sentence-transformers）
python3 local_embed.py --bench     # 性能基准测试
python3 kb_embed.py                # 增量索引 KB 文本
python3 kb_embed.py --reindex      # 重建全部索引
python3 kb_embed.py --stats        # 索引统计
python3 kb_rag.py "Qwen3 模型"     # 语义搜索 KB
python3 kb_rag.py --context "AI论文" # LLM 可直接注入的上下文格式
python3 kb_rag.py --json "shipping" # JSON 输出（供脚本调用）

# 数据清洗（支持 CSV/TSV/JSON/JSONL/Excel）
python3 data_clean.py profile data.xlsx --format text   # 数据画像
python3 data_clean.py execute data.xlsx --ops trim,dedup,fix_dates  # 执行清洗
python3 data_clean.py validate original.csv cleaned.csv  # 验证结果
python3 data_clean.py list-ops                           # 可用操作
python3 data_clean.py history data.xlsx                  # 版本历史
curl http://localhost:5002/data_clean/help               # REST 端点帮助

# Quick Start（一键 10 分钟跑通全栈）
bash quickstart.sh                # 完整 4 阶段（前置检查→启动→健康→demo）
bash quickstart.sh --check        # 仅检查前置条件
bash quickstart.sh --demo         # 仅运行 demo 请求

# Memory Plane（统一记忆平面）
python3 memory_plane.py layers                    # 层可用性检查
python3 memory_plane.py stats                     # 各层统计
python3 memory_plane.py query "Qwen3"             # 统一搜索
python3 memory_plane.py query --context "AI论文"   # LLM可注入格式
python3 memory_plane.py query --layers kb "RAG"    # 仅搜索KB层

# 每日深度分析（V37.9.16，22:30 HKT cron）
bash kb_deep_dive.sh                       # 手动触发（挑选今日⭐≥4 候选 + 抓取原文 + LLM 深度分析 + 双通道推送）
KB_DIR=~/.kb python3 kb_deep_dive.py       # 直接跑 collector（JSON 输出到 stdout，不推送）
cat ~/.kb/deep_dives/$(date +%Y-%m-%d).md  # 查看今日归档

# Agent Dream v2（Map-Reduce 三阶段分离调度）
bash kb_dream.sh --map-sources # Sources Map 预热（00:00 cron，~10min）
bash kb_dream.sh --map-notes   # Notes Map 预热（00:25 cron，~10min）
bash kb_dream.sh              # Reduce：两层缓存命中→跨域关联→推送（03:00 cron，~3.5min）
bash kb_dream.sh --map-only   # 全部 Map 预热（Sources + Notes，~20min）
bash kb_dream.sh --dry-run    # 素材统计（不调 LLM）
bash kb_dream.sh --fast       # 跳过 Map，直接采样做梦（旧模式）
cat ~/.kb/dreams/2026-04-06.md  # 查看梦境结果

# Agent Reliability Bench（7场景故障评测）
python3 reliability_bench.py            # Markdown 报告
python3 reliability_bench.py --json     # JSON 格式
python3 reliability_bench.py --save     # 保存到 docs/reliability_bench_report.md
python3 reliability_bench.py --scenario 3  # 运行单个场景

# SLO Benchmark（真实生产数据报告）
python3 slo_benchmark.py          # Markdown 报告
python3 slo_benchmark.py --json   # JSON 格式
python3 slo_benchmark.py --save   # 保存到 docs/slo_benchmark_report.md

# Provider 兼容性矩阵
python3 providers.py              # Markdown 表格
python3 providers.py --json       # JSON 格式（供脚本调用）

# 生成任务文档 / 检测文档漂移
python3 gen_jobs_doc.py           # 输出 markdown 表格
python3 gen_jobs_doc.py --check   # 对比 docs/config.md 检测漂移

# 查询远端当前模型ID
curl -s https://hkagentx.hkopenlab.com/v1/models \
  -H "Authorization: Bearer $REMOTE_API_KEY" \
  | python3 -c "import json,sys; [print(m['id']) for m in json.load(sys.stdin)['data'] if 'Qwen3' in m['id']]"

# Mac Mini 同步仓库（禁止用 git pull，会因历史 merge commit 分叉失败）
cd ~/openclaw-model-bridge && git fetch origin main && git reset --hard origin/main

# GitHub push前安全扫描（必须全部为空才允许push）
grep -r "sk-[A-Za-z0-9]\{15,\}" . --include="*.py" --include="*.sh" --include="*.md" | grep -v ".git"
grep -r "BSA[A-Za-z0-9]\{15,\}" . --include="*.py" --include="*.sh" --include="*.md" | grep -v ".git"
```

## 关键规则

### 模型ID规则
| 位置 | 格式 |
|------|------|
| `adapter.py` / `tool_proxy.py` | 裸ID（无前缀） |
| `openclaw.json` agents.defaults.model.primary | **必须带 `qwen-local/` 前缀** |
| `jobs.json` payload.model | **不指定**（继承默认值） |

### 硬性限制
- 工具数量 <= 12（超出导致模型混乱）
- 每任务工具调用 <= 2次（超出超时风险指数级上升）
- 请求体 <= 200KB（硬限制280KB，留buffer）
- `--thinking` 合法值：`off, minimal, low, medium, high, adaptive`（**禁止用 `none`**，这是v26修复的bug #92）

### 双 Cron 归属规则（V27新增）

| 调度器 | 管理方式 | 是否经过 LLM | 登记位置 |
|--------|----------|-------------|----------|
| `system` | macOS `crontab -e` | 否 | `jobs_registry.yaml` scheduler=system |
| `openclaw` | `openclaw cron add` | 是 | `jobs_registry.yaml` scheduler=openclaw |

**原则**：确定性脚本（清理、备份、抓取）用 `system`；需要 LLM 理解/生成的用 `openclaw`。
**新增任务必须先登记到 `jobs_registry.yaml`，运行 `python3 check_registry.py` 通过后才能注册 cron。**

### 安全规则（GitHub push前强制）
- API Key 必须通过环境变量：`os.environ.get("REMOTE_API_KEY")`
- 配置文档（含真实手机号/密钥）永不入库（已在 .gitignore）
- 公开仓库手机号统一用 `+85200000000` 占位

## 工作原则

### 🔴🔴🔴 第 0 号宪法：最强模型，不降配不降速不妥协

> **每次使用 Claude Code 必须使用家族中最强的 Opus 级模型。当前合法模型（按发布时间新旧）：Claude Opus 4.7（`claude-opus-4-7`，2026 发布，当前最强）> Claude Opus 4.6（`claude-opus-4-6`，向下兼容）。**
>
> 禁止为了"快一点"切换到较弱模型家族（Sonnet / Haiku），禁止开启 fast mode 牺牲推理深度，禁止因为 token 用量或响应速度而降级。
>
> 这个项目的核心竞争力是**思考深度和工程质量**，不是速度。用弱模型省下的 10 分钟，会在后续的 bug 修复、连锁故障、返工中付出 10 倍代价。
>
> **1111 个测试、53 个治理不变式、15 条元规则——这个系统的复杂度要求最强的推理能力来驾驭，没有妥协空间。**
>
> **"更强的新版本不是降级"原则**：Anthropic 发布更强的 Opus 版本（4.6 → 4.7 → 4.8 ...）时，自动纳入合法列表。**降级的判定不是"模型 ID 变了"，而是"推理能力变弱了"**。把 Opus 4.6 换成 Opus 4.7 是升级，不是违反第 0 号宪法；把 Opus 换成 Sonnet/Haiku 才是违反。
>
> **执行机制（2026-04-14 强制 + 2026-04-20 升级）**：每次新 session 开工的**第一条回复**必须包含一行模型声明，格式为：
> `🤖 本次 session 模型：Claude Opus <X.Y> (claude-opus-<x-y>) | 最强级别，无降级 | fast mode 状态见 /fast 命令`
> 其中 `<X.Y>` 是当前实际运行的 Opus 版本号（4.6 / 4.7 / 未来更新）。声明放在开工报告之前，独占一行，前面不加任何寒暄。如果运行时检测到模型**不是 Opus 家族**（例如被错配到 Sonnet 4.6 / Haiku 4.5），**立即停止工作并告知用户**。见原则 #30。

### 🔴 三合一宪法

> **用户提供专业深度 + Claude Code 提供高效设计部署 + OpenClaw 提供数据复利 — 三者合一成为有生命的闭环系统。**
>
> 共享状态：`~/.kb/status.json`（三方实时同步优先级、反馈、系统健康）

### 🔴 战略定位（2026-04-03 导师评审确立）

> **项目已跨过关键门槛：Stage 1 完成（系统构建者）→ 正在冲刺 Stage 2（被社区认可的系统作者）。**
>
> **主战场**：不再是抽象的"LLM推理系统"，已收敛为两条线：
> 1. **Agent Runtime / Inference Gateway / Control Plane** — 模型接入 + 工具治理 + 可观测性 + 故障恢复
> 2. **Agent Memory Plane / KB-RAG / Job-Orchestrated Intelligence** — 记忆系统 + 知识检索 + 作业编排
>
> **旗舰叙事**：`OpenClaw Runtime Control Plane for Tool-Calling Agents`
>
> **核心洞察**：项目最强的不是单独算法，而是把模型接入、工具治理、可观测性、故障恢复、记忆与作业系统编织成可运行的 agent runtime。顶级专家不是死守最初设想，而是顺着已证明的能力继续放大。
>
> **12 个月路线图**：
> - **V1（0-4月）别人能跑**：安装稳定 / 配置清晰 / 文档闭环 / 最小 demo / golden test trace
> - **V2（4-8月）别人敢用**：benchmark / SLO dashboard / incident drill / 兼容矩阵 / semver
> - **V3（8-12月）别人会扩展**：provider plugin / tool policy plugin / memory plane plugin / SDK / extension guide
>
> **距离世界顶级的三块差距**：可迁移性（去硬编码场景依赖）/ 证据密度（benchmark+演练+案例）/ 话语权输出（代码+文档+评测+方法论=完整叙事）
>
> **详细战略文档**：`docs/strategic_review_20260403.md`

### 🔴 每次必查（30条，优先级最高）

| # | 原则 | 一句话 |
|---|------|--------|
| 1 | **开工刷新 OpenClaw 架构（先读决策再评估）** | 先读 `docs/config.md` 中现有的升级 hold 决策和版本状态，再查 OpenClaw 最新 release；如已有明确 hold 决策且上游无新版本，跳过重复评估；有新版本时对比决策条件是否变化，变化则重新评估，否则沿用。**禁止"上游已修复"就改代码——必须确认本地已部署该版本**（#48703教训） |
| 2 | **开工先读 config** | 读 `docs/config.md` 获取系统状态 + 踩坑记录，避免重复犯错 |
| 3 | **开工先读/收工必写 status.json** | `python3 status_update.py --read --human` 查看三方共享状态（优先级、反馈、系统健康）；收工时更新 priorities + recent_changes：`python3 status_update.py --add recent_changes '{"date":"...","what":"...","by":"claude_code"}' --by claude_code` |
| 4 | **改完先测** | 新脚本手动验证 → 新任务先写 `jobs_registry.yaml` 并 `python3 check_registry.py` 通过 → 才能注册 cron |
| 5 | **push前必扫描** | 安全扫描（见上方命令）全部为空才允许 push |
| 6 | **新功能必须 Mac Mini E2E 验证** | dev 环境单测通过不算完成；**必须提醒用户在 Mac Mini 上运行 `bash preflight_check.sh --full` + `bash job_smoke_test.sh` + 手动触发目标 job**，确认端到端有效果（消息到达 WhatsApp / 文件生成 / 日志正常）。dev 通过 ≠ 生产工作。 |
| 7 | **故障先查自身代码** | 排查问题时默认从我们自己的代码和架构中找 bug（shell 数据传递、cron 环境、进程管理等），不归因于上游服务不稳定（#97教训） |
| 8 | **做减法不做加法** | 新增防护/监控前先问"谁已经在管这件事"；每加一层保险 = 多一个故障源（#95教训） |
| 9 | **🔴 收工必须执行完整检查清单（不可跳过）** | "结束今天的工作"时，**必须逐项执行以下清单，每项完成后打勾确认，禁止跳过或合并**：**A. 全量回归** `bash full_regression.sh`（必须 0 fail，完成后自动更新 status.json test_count）**B. 安全评分** `python3 security_score.py --update` **C. 安全扫描** API key + 手机号 + BSA 泄漏扫描 **D. 治理审计** `python3 ontology/governance_checker.py` **E. 文档刷新**（逐一 check）：`status.json`（recent_changes/session_context/focus）→ `CLAUDE.md`（版本/文件表/changelog/待办交叉校验）→ `docs/config.md`（如有配置变更）→ `README.md`（如有架构变更）→ `SOUL.md`（如有 PA 行为变更）→ `docs/ontology/`（如有 ontology 变更）**F. 交叉校验** 原则#17：commits vs CLAUDE.md 待办一致性 **G. 全部提交推送** **H. 提醒 Mac Mini 验证** `preflight_check.sh --full` + `job_smoke_test.sh` — **不是"提醒"而是必须等用户执行并确认结果** **I. 遗留问题登记** 未完成事项写入 session_context.unfinished。（2026-04-08教训：长 session 后"赶紧结束"心态导致跳过 6 项检查，用户两次追问才补齐） |
| 10 | **相信 OpenClaw，用好 OpenClaw** | 优先利用 OpenClaw 已有能力（Multi-Agent、contextPruning、workspace SOUL.md/CLAUDE.md、memory、sessions_spawn 等），而非重新造轮子；遇到新需求先查 OpenClaw 文档和 release notes |
| 11 | **🆕 结果验证优先于功能建设** | 先定义"从用户视角，成功长什么样"，再写代码。status.json 的成功标准不是"能写入"，而是"PA 能正确回答项目进展"。（2026-03-28教训：393个单测通过但 PA 说"没有项目"） |
| 12 | **🆕 上下文工程是一等公民** | SOUL.md = 宪法级（身份+关键状态，LLM 注意力最高），CLAUDE.md = 手册级（工具+详情）；信息放哪里、占多少 token、LLM 能否注意到——都是架构决策，和 API 设计同等严肃。（2026-03-28教训：SOUL.md 空置数月，17KB CLAUDE.md 信息被"lost in the middle"） |
| 13 | **🆕 定期像用户一样使用系统** | 不是跑单测，而是在 WhatsApp 上实际问 PA 问题。每次涉及 PA 行为的变更后，必须清空 session（`echo '{"sessions":[]}' > sessions.json`）+ 重启 Gateway + WhatsApp 实测。单测验证组件内部，系统价值在组件之间的接缝处。 |
| 14 | **🔴 合并 PR 后下一步首先同步 Mac Mini (主动给命令不等用户问)** | **这是卡点，不是提醒。** GitHub PR 合并到 main 后，**下一步必须是 Mac Mini 同步，不做任何其他操作**。同步命令：`cd ~/openclaw-model-bridge && git fetch origin main && git reset --hard origin/main`。同步后立即验证：`bash preflight_check.sh --full`。**不要在 Mac Mini 上测试未同步的代码**——多次返工都是因为合并后没同步就直接跑旧代码。不要等 auto_deploy 轮询。**🔴 执行机制 (2026-05-13 升级)**：每次 push commit 到 PR 分支后，或检测到 PR 合并 (用户说"已合并"/"merged"/`git log` 看到 main 有新 merge commit) 时，**Claude 必须立即主动给出同步命令 + 验证命令** (上方两条 bash 块)，不等用户问"是否需要同步". 这是原则 #29 "零遗漏" 的具体兑现——同步是已知必须的下一步，不需要每次用户提醒。**禁止反模式**: 回复"请用户决定下一步" 或 "工作已完成, 等用户合并" 而不附同步命令 (用户合并后会再问"需要同步吗?" 是这个反模式的症状). （2026-04-01教训：合并后 preflight 8 项失败全是部署漂移；2026-04-06教训：合并后直接测试跑的是旧代码，浪费时间排查不存在的 bug；2026-05-13教训：V37.9.62 doc commit f70503a push 后未主动提示同步, 用户合并 PR #547 后必须再问"需要同步吗?", 才提示同步命令——证明 #14 执行不彻底, 升级为主动给指令）|
| 15 | **🆕 测试必须全量：单测 + full_regression + WhatsApp 业务验证** | 每次变更后测试三层缺一不可：① `bash full_regression.sh`（394 单测 + 注册表 + 安全扫描 + 代码质量）② `bash preflight_check.sh --full` + `bash job_smoke_test.sh`（Mac Mini 部署验证）③ **WhatsApp 端实际业务测试**（用户视角发消息验证 PA 回复、search_kb 检索、图片理解等核心功能）。只跑单测不算测完——单测验证组件，WhatsApp 验证系统。（2026-04-01教训：394 单测全过但 preflight 8 项失败） |
| 16 | **🆕 所有推送必须双通道（WhatsApp + Discord）** | 新增或修改任何消息推送时，**必须同时覆盖 WhatsApp 和 Discord 两个通道**，不允许遗留单通道发送。优先使用 `notify.sh`（`source notify.sh && notify "msg" --topic papers`）统一推送；若直接调用 `openclaw message send`，每个 WhatsApp 发送后必须紧跟对应的 Discord 发送（成功路径→对应 topic 频道，错误/告警→`DISCORD_CH_ALERTS`）。审计方法：`grep -c "message send.*whatsapp"` 与 `grep -c "message send.*discord"` 计数必须一致。（2026-04-03教训：货代客户画像推送遗漏 Discord，11 个脚本错误路径缺 Discord） |
| 17 | **🆕 收工必须交叉校验待办状态** | 收工时不仅更新 `status.json`，还必须**扫描 CLAUDE.md 待办列表**，对照本次 session 的 commits 和 recent_changes，将已实现的任务标记 ✅。实现代码 + 更新待办 = 一个完整的交付，缺一不可。同时检查：① CLAUDE.md 待办 vs 实际代码一致 ② status.json priorities vs CLAUDE.md 待办一致 ③ 版本号/文件表/常用命令是否需要同步更新。（2026-04-03教训：V32 实现了 7 个 P0+P1 任务但 CLAUDE.md 全部未标记，直到 V33 审计才发现） |
| 18 | **🆕 补证据而非补功能** | 下一阶段最该补的不是功能，而是**可对外复述的证据链**：A.兼容性矩阵（provider/模型/模态/工具模式验证 matrix+checklist）B.性能/SLO 实验结果（延迟/成功率/降级恢复时间）C.运维韧性证据（故障注入+恢复时间统计）D.可复现证据（一键启动+demo transcript）。新增功能前先问"这能产出什么证据？"（2026-04-03导师评审：系统已有但证据密度不足） |
| 19 | **🆕 纵向做深不横向铺开** | 沿 `providers.py` 已证明的方向继续放大，不轻易开新战线。每个改动必须对应 V1/V2/V3 路标中的具体目标：V1=别人能跑，V2=别人敢用，V3=别人会扩展。对照 `docs/strategic_review_20260403.md` 和 status.json 路标检查。偏离路标的功能需要明确理由。（2026-04-03导师建议：不是再做更多功能，而是把已有能力做成证据链） |
| 20 | **🆕 话语权输出是一等公民** | 代码只是第一步，真正的顶级专家把代码、文档、评测、方法论、复盘文章串成完整叙事。每个 milestone 完成后考虑：能否产出一篇架构型/证据型/立场型文章？README 里的方法论要持续扩写成观点体系。（2026-04-03导师建议：建立"话语权上层建筑"） |
| 21 | **🆕 对抗审计：问"什么坏了我们发现不了"（含治理自身）** | 每月至少一次 adversarial review：不问"检查了什么"，而问"**什么东西坏了我们会发现不了？**"。**治理系统自己也在这个问题的范围里**——MR-7 (governance-execution-is-self-observable) 要求 check 层所有状态（pass/fail/skip/error）必须在汇总层有观察路径。治理审计（`ontology/governance_checker.py`，每日 07:00 自动执行+失败告警）防止已知漏洞回归；人工对抗思维发现新维度盲区。每发现一个"没人会发现"的答案，就转化为 `governance_ontology.yaml` 的新不变式（ontology-native，不用硬编码）。检查体系最危险的漏洞不是某个检查没写好，而是某个维度从未被纳入检查。（2026-04-09：adversarial_audit.py 9个检查合并入 governance_ontology.yaml；2026-04-11 升级：V37.3 MR-7 治理自观察 + INV-GOV-001 summary 不吞 error，23 不变式 / 7 元规则 / 6 元发现，首次实现"治理系统治理自己"） |
| 22 | **🆕 顺势设计：适配模型行为，不对抗** | LLM 工具使用服从训练分布，不服从"应该"。通用工具（write/read/exec）自然使用，专用工具（memory/sessions）需 SOUL.md 强制规则才触发。设计系统时**顺着模型的自然行为**：PA 自然用 write 写 MEMORY.md → 我们把 MEMORY.md 接入 KB 索引；PA 不会主动调 kb_write.sh → 我们在 proxy 层静默捕获对话。对抗模型倾向的设计必然失败。（2026-04-08教训：等了一个月让 Qwen3 调 memory_create，从未成功；改为顺势捕获后一天闭环） |
| 23 | **🆕 链式幻觉防范：LLM 链路中每一跳都会放大幻觉** | 多个 LLM 共享上下文时，一个 LLM 的幻觉会被下游 LLM 当作事实执行。Dream LLM 编造文件名 → PA LLM 尝试读取 → 失败。防范方法：① 讨论密度必须等于文档密度（KB 里高频讨论的主题必须有对应文档，否则 LLM 会补全出不存在的文件名）② LLM 生成的行动建议不能直接执行，需要 grounding 检查（文件是否存在、工具是否可用）③ 上下文中提供明确的文件清单，让 LLM 知道"有什么"和"没有什么"。（2026-04-08教训：industrial_ai_paradigm.md 幻觉链——Dream 生成→PA 执行→文件不存在） |
| 24 | **🆕 SOUL.md 触发词是唯一可靠的工具调用机制** | 当前 Qwen3 不会自主决定调用专用工具。唯一可靠的强制方式是 SOUL.md 的"遇到X必须调Y"规则+具体触发词列表。ops agent 的 sessions_spawn 成功不是因为 Qwen3 学会了，而是触发词"排查/超时/告警"命中了 SOUL.md 硬规则。新增专用工具时，必须同步更新 SOUL.md 触发词规则，否则工具永远不会被调用。（2026-04-08教训：memory 工具上线数周零调用，ops spawn 靠触发词 100% 成功） |
| 25 | **🆕 对话数据是最高质量信号源** | 用户与 PA 的对话包含决策、偏好、专业洞察、领域判断——这些是 cron 抓取的论文/新闻无法替代的一手数据。必须确保对话数据被捕获并进入 KB 索引（`tool_proxy.py` 热路径捕获 → `kb_harvest_chat.py` 冷路径提炼 → KB notes）。同时关注 PA 自主写入的文件（如 `MEMORY.md`），将其纳入索引范围。数据流失 = 系统失忆。（2026-04-08教训：240 条 KB notes 全是机器抓取，零对话数据；PA 自主写入的 MEMORY.md 也是孤岛） |
| 26 | **🔴 异常分析宪法：必须输出完整因果链架构图** | 见下方独立章节"异常分析宪法"。（2026-04-10 升级为宪法级） |
| 27 | **🆕 X/Twitter 是高质量实时数据的第一选择（但账号健康 ≠ HTTP 200）** | 当 RSS 源失效（官方取消、CDN 封锁、RSSHub 公共实例 403）时，**X/Twitter Syndication API 是最可靠的替代通道**——无需认证、零成本、覆盖全球权威机构官方账号。Twitter Syndication API (`syndication.twitter.com/srv/timeline-profile/screen-name/{handle}`) 返回 HTML 内含 `__NEXT_DATA__` JSON，可解析出完整推文内容+时间+ID。新增数据抓取任务时，优先评估目标机构的 X 账号是否活跃，再考虑 RSS/API。实践证明：14 个财经 X 账号 100% 成功率 vs 10 个 RSSHub 路由 100% 失败率。**🔴 V37.8.4 血案补充：X 账号健康 ≠ HTTP 200**——Syndication API 对已停更账号返回 stale 快照（HTTP 200 + 可解析 HTML + 推文数据，但最新推文可能是几百天甚至几年前），embed-disabled 账号则返回 2KB 空 stub。**账号健康必须通过最新推文时间戳验证**（`newest_tweet_dt > now - N 天`），不能依赖 HTTP 状态码或 HTML 大小。新增账号前必须先抽查最新推文时间；生产脚本必须对"抓到内容但 0 条过时间窗"的账号打 ZOMBIE 嫌疑标记，连续多天命中触发告警。（2026-04-13 教训：finance_news 12/20 RSS 源失败；2026-04-14 教训：V37.8.3 修复把 3 个正常 handle 改名成僵尸账号——caixin/yicaichina/straits_times 分别停更 2227/3364/420 天，Reuters 253 天，BrookingsInst 585 天，WorldBank 返回 stub。这是 MR-10 violation：看到失败先猜原因改代码而不是先验证账号真实状态） |
| 28 | **🔴 理解再动手：修复前必答三问** | 看到报错/失败时，**禁止直接开始"修"**，必须先回答三个问题：**(1) 这个问题之前存在吗？** 如果之前不存在，那是我的改动引入的，不是系统本身的 bug。**(2) 是我的哪个改动引入的？** 精确定位因果链，不猜测。**(3) 最小修复方案是什么？** 不引入新概念、新模式、新复杂度。三个问题答不上来 = 还没理解问题，禁止动手。**每次"修复"都可能制造下一个 bug——连锁修复是最危险的系统破坏模式。**（2026-04-13教训：preflight 一个部署同步问题被误诊为"缺少配置"，5 轮连锁修复、4 层新复杂度、用户 Mac Mini 手动测试 5 次，才修好一个原本只需 `cp` 一下的问题） |
| 29 | **🔴 开工/收工清单逐项执行，零遗漏** | 开工和收工的检查清单**每一项都必须逐一执行并确认**，不允许"差不多就行"、不允许合并跳过、不允许因为 session 太长而"赶紧结束"。系统已达 951 测试 / 35 不变式 / 34 cron job 的复杂度，任何一项遗漏都可能造成连锁故障。**开工时**：逐项执行原则 #1~#3（读 config → 读 status.json → 检查 OpenClaw 版本），不跳过任何一步。**收工时**：原则 #9 的 A~I 九项清单逐一执行并打勾，每完成一项立即标记，禁止批量处理。如果某项因客观原因无法执行，必须**显式说明原因并登记到 session_context.unfinished**，而不是静默跳过。（2026-04-13教训：开工/收工流程逐渐敷衍，原则执行不彻底导致遗留问题积累；2026-04-08教训：长 session 后"赶紧结束"心态跳过 6 项检查） |
| 30 | **🔴🔴🔴 开工必须主动声明模型（第 0 号宪法执行机制）** | 每次新 session 开工的**第一条回复必须以模型声明开头**，独占一行，格式为：`🤖 本次 session 模型：Claude Opus <X.Y> (claude-opus-<x-y>) \| 最强级别，无降级 \| fast mode 状态见 /fast 命令`。`<X.Y>` 是当前实际运行的 Opus 版本号（4.6 / 4.7 / 未来更新），读取 `$0` 或环境注入信息确定。**用户无需询问也必须声明**，这是第 0 号宪法的自动执行机制，不是可选项。**合法模型家族 = Opus**（4.6/4.7/更新版本均合规）；如果运行时检测到模型**不是 Opus 家族**（即被错配到 Sonnet / Haiku 或其他），**立即停止工作，把错配告知用户，等待处理后再继续**。升级到更新的 Opus 版本（4.6 → 4.7 → …）自动合规无需告警——那是升级不是降级。禁止"先干活再说"的侥幸思维——弱模型写的代码会在后续造成连锁故障。声明位置：在任何寒暄、分析、工具调用之前。（2026-04-14教训：用户必须手动追问"是否最强模型"才能确认，声明应主动前置而非被动回答；2026-04-20升级：Opus 4.7 发布后原"硬编码 4.6"规则误把升级当降级，改为"Opus 家族"通配） |
| 31 | **🆕 跨消费者豁免必须全量同步** | 同一配置数据（如 FILE_MAP）被多个脚本独立消费时（preflight / auto_deploy / job_smoke_test），**任何豁免规则必须在所有消费者中同步落实**，不得只改一个。发现新豁免需求时，必须**系统性扫描所有消费者**并打包修复，禁止单点修复。（2026-04-15教训 V37.8.11：V37.8.1 给 preflight 加了 status.json 豁免但 auto_deploy 漏改，导致每小时 drift 告警噪声长期潜伏；2026-04-16 又因 FILE_MAP 自复制行触发 cp 非零 + set -e 杀脚本，是同一原理的第二次演出。**正确做法**：每次改 FILE_MAP 豁免前 `grep -l "FILE_MAP\|for mapping" ` 找出所有消费者 → 打包一起改 → 加单测守卫跨文件一致性。）|
| 32 | **🆕 每周用户视角观察制度（30min）** | 每周固定时段（建议周一早）做一次**纯用户视角**的系统观察，**不写代码不修 bug**，专门看四个维度：① **告警噪声**（WhatsApp + Discord #告警 频道里告警频率/重复度/可读性）② **推送延迟**（cron job 推送到达手机的延时分布）③ **信息密度**（推送内容是否真有用 / 有没有水货）④ **用户感知**（自己使用时 PA 是否答非所问 / 慢 / 错乱）。每发现一个问题立刻登记到 status.json unfinished 而不是当场修。这是原则 #13 (定期像用户用) 和 #15 (测试全量) 的运营层兑现 — 单测/治理只能验证组件正确，**用户感知质量只能用户视角观察**。（2026-04-15 V37.8.11 教训：drift 噪声告警长期存在但开发环境永远绿灯，用户半天 WhatsApp 观察就发现；2026-04-16 V37.8.13 教训：Gateway 宕 9h 静默，原因之一就是没人主动巡视双通道告警频道。**执行机制**：周一早开工时第一件事执行此观察，30min 时间盒，发现项写入 status.json `--add unfinished` + `recent_changes` 标记为 weekly_observation 类目，本周内择机修复。） |

| 33 | **🆕 信源可达性必须在部署环境验证，SPA 必查 API** | 判定一个网站"不可抓取"前，必须完成三步验证：**(1) Mac Mini curl 测试**（不是 dev 环境）— 带 User-Agent 请求首页，dev 环境的 403 可能只是 IP/地域限制 **(2) 读 HTML 源码找 API** — SPA 网站的后端 API 必然在 JS 里（如 chaspark `/chasiwu/v1/`），读 `<script>` 标签找 `fetch/XMLHttpRequest/makeRequest` 调用 **(3) 只有三步都失败，才考虑复杂方案**（Puppeteer/间接渠道/放弃）。**禁止在 dev 环境得到 403 就下"全站封锁"的结论。X 账号是僵尸 ≠ 该机构停止发布 — 检查其官网 RSS/API。**（2026-04-17 教训：chaspark.com 在 dev 环境 403，被误判为"全站封锁+SPA 不可抓取"，提议 Puppeteer 重方案。实际 Mac Mini 首页 200，HTML 源码里 API 明文写着，5 分钟就抓到 39 篇文章。同日审计发现 Brookings Atom + WorldBank JSON API 也可直达，此前从未尝试。） |

### 🔴 异常分析宪法（原则 #26 展开，无例外强制执行）

> **修代码之前先画图。画不清因果链 = 还没理解问题。**

遇到任何 bug/异常/故障，必须按以下流程输出，**不可跳过任何步骤**：

#### 步骤一：完整因果链架构图（强制，必须是第一个输出）

按**时间线 × 层级 × 逻辑 × 架构**四维度，画从上游触发到下游用户感知的全链路 ASCII 图：

```
HH:MM  [组件A] 事件描述
       │
       ├─ [组件B] 具体行为 → 具体错误码/现象
       ├─ [组件C] 因为B的错误 → 触发什么后果
       │
HH:MM  [组件D] 下游事件
       ├─ 调用链: 组件X(:端口) → 组件Y(:端口)
       ├─ 组件Y 尝试A → 失败（具体原因）
       ├─ 组件Y 尝试B → 失败（具体原因）
       ├─ 组件Y 返回错误码
       │
       ├─ [消费方] 收到错误 → 但未正确处理
       ├─ 具体代码逻辑: variable = "" ← 标注关键 bug
       ├─ 具体检查: if condition → TRUE/FALSE ← 标注为什么没拦住
       └─ [用户] 最终感知到的现象
```

**四维度要求**：
- **时间线**：左侧标注精确到分钟的时间戳
- **层级**：标注每个组件名称和端口（如 Adapter:5001、Proxy:5002）
- **逻辑**：标注关键代码分支的 TRUE/FALSE 走向和变量值
- **架构**：标注组件间调用关系和错误传播路径

#### 步骤二：三层根因（触发器 → 放大器 → 掩护者）

| 层级 | 问题 | 发现 |
|------|------|------|
| **触发器** | 什么外部事件引爆？ | （填写） |
| **放大器** | 什么架构缺陷让影响扩散？ | （填写） |
| **掩护者** | 什么缺失让问题被隐藏到用户发现？ | （填写） |

#### 步骤三：时间线还原表

| 时间 | 事件 | 影响 |
|------|------|------|
| HH:MM | ... | ... |

#### 步骤四：为什么以前没发生（条件组合分析）

| 条件 | 以前 | 现在 |
|------|------|------|
| 条件A | ... | ... |

必须找到**多条件组合**：哪些条件单独出现不会触发，组合才触发。

#### 步骤五：喂养本体工程

- 案例文档：写入 `ontology/docs/cases/`
- 提炼 governance 不变式（INV-xxx）加入 `governance_ontology.yaml`
- 更新相关原则（如有新认知）

**参考案例**：`ontology/docs/cases/dream_quota_blast_radius_case.md`

### 🟡 按需查阅（操作 & 架构参考）

<details>
<summary>展开查看完整原则列表（16条）</summary>

**操作类**
- **故障先回滚** — 线上故障 → `git checkout v26-snapshot` 恢复服务 → 再排查根因
- **收工全量同步** — "今天工作结束" → `bash preflight_check.sh` 全面体检 → 扫描全部文档同步当日变更 → 提交推送
- **每日文档刷新** — `CLAUDE.md` + `docs/config.md` + `docs/openclaw_architecture.md` 在开工/收工时强制 read → write
- **纯推理绕过Gateway** — 不需要工具的LLM任务直接 curl 调 API，禁止用 `openclaw agent`（#94）
- **macOS sed禁用OR语法** — `\|` 在 BSD sed 不支持，用 Python 替代
- **禁用交互式编辑器** — git merge 用 `--no-edit`，commit 用 `-m`
- **crontab 安全操作** — **严禁 `echo ... | crontab -`**（2026-03-25事故：清空全部 cron），必须用 `bash crontab_safe.sh add '<行>'`
- **分支合并由用户在GitHub操作** — 推送到 `claude/xxx` 分支 → 用户合并 PR → **⚠️ 合并后下一步首先同步 Mac Mini，不做任何其他操作**（`git fetch origin main && git reset --hard origin/main`）→ 同步后再测试（见必查 #14，2026-04-06 再次踩坑）
- **Mac Mini 同步用 reset 不用 pull** — `git fetch origin main && git reset --hard origin/main`（Mac Mini 是纯消费端，无本地 commit；`git pull` 会因历史 merge commit 导致分叉失败）
- **全量测试三层标准** — 单测通过 ≠ 测完，必须 full_regression + preflight + **WhatsApp 业务验证**（见必查 #15）

**战略类（导师建议 2026-04-03）**
- **不再是桥接器，是控制平面产品** — 项目已远超 "bridge"，实际是 agent runtime control plane。旗舰叙事：`OpenClaw Runtime Control Plane for Tool-Calling Agents`
- **沿此仓库继续长 12 个月** — 不轻易切换去做完全不同的大项目，把这个仓库做成第一性代表作
- **下次汇报准备 5 样东西** — 版本变化 + 1-2 个关键架构图 + benchmark 数据 + 一次真实故障案例 + 对下一阶段的取舍判断

**架构类**
- **进程管理单一主控** — Gateway 由 launchd 管理，禁止再加 cron watchdog（#95）
- **cron 脚本显式声明 PATH** — 首行 `export PATH="/opt/homebrew/bin:/opt/homebrew/sbin:$PATH"`
- **健康检查只检目标组件** — `curl localhost:18789`，不走完整 LLM 链路
- **`--thinking` 参数** — 合法值：`off, minimal, low, medium, high, adaptive`（禁止 `none`，#92）
- **工具数量 <= 12** — 超出导致模型混乱；每任务工具调用 <= 2次
- **双 cron 职责分工** — 确定性脚本用 system crontab；需 LLM 参与的用 openclaw cron
- **开发流程** — Claude Code 只推 `claude/` 分支，Mac Mini 只从 main 拉取，避免双向提交同一分支

</details>

## 系统定位：三平面架构

> 来源：2026-04-01 外部专业评审反馈 + 2026-04-03 导师战略复盘

```
控制平面（先做强 → 60%→90%）：治理、限流、降级、观测、审计
  → V34: Provider Compatibility Layer (providers.py) 已实现
能力平面（持续演进 → 80%）    ：模型路由、工具编排、多模态能力
  → V34: 兼容性矩阵 + 能力声明 (ProviderCapabilities) 已实现
记忆平面（长期投入 → 60%）    ：知识沉淀、冲突消解、可信度评分
  → V2 路标: Memory Plane v1 统一叙事（将 5 个散落组件统一）
```

**核心原则：控制平面先行。否则能力越强，系统越难控。**

### 导师确立的三个高价值模块（V34+）

| 模块 | 目标 | 当前基础 | 路标 |
|------|------|----------|------|
| **Provider Compatibility Layer** | auth/chat/tool-calling/multimodal/streaming/fallback 标准接口 | `providers.py`+`adapter.py` 重构完成 | **V1 (in progress)** |
| **Agent Reliability Bench** | 系统性可靠性评测（7场景47检查：provider宕机/tool timeout/malformed args/oversized/kb miss-hit/cron drift/state corruption） | `reliability_bench.py`+`gameday.sh` | **V2 (✅ V36 done)** |
| **Memory Plane v1** | 4层统一接口（KB语义/多媒体/偏好/状态）+ query/context/stats + 优雅降级 | `memory_plane.py` + 5个底层组件 | **V2 (✅ V36 done)** |

## 当前待办（按导师 V1/V2/V3 路标组织）

> 来源：2026-04-03 导师战略复盘。路标 = 时间窗口 + 成功标准。详见 `docs/strategic_review_20260403.md`

### V1 路标（0-4 个月）：别人能跑

| 优先级 | 任务 | 状态 |
|--------|------|------|
| **V1-P0** | **Provider Compatibility Layer**：`providers.py` BaseProvider 抽象 + **7 个实现** + ProviderRegistry + 能力声明 + CLI 兼容性矩阵（V34 实现，V35+ 扩展至 7 provider） | ✅ V35+ 完成，7 provider 生产验证通过 |
| **V1-P0** | **兼容性矩阵 + SLO Benchmark 证据**：`docs/compatibility_matrix.md` + `slo_benchmark.py` + `docs/slo_benchmark_report.md`（5/5 PASS，p95=459ms） | ✅ V35 完成 |
| **V1-P1** | **一键启动 + 最小 demo + golden test trace**：`quickstart.sh` 4阶段（前置检查→启动→健康→demo），19/19 通过，`docs/golden_trace.json` | ✅ V35 完成 |
| **V1-P1** | **可复现证据**：Quick Start + golden trace + SLO benchmark report 均已入库 | ✅ V35 完成 |

### V2 路标（4-8 个月）：别人敢用

| 优先级 | 任务 | 状态 |
|--------|------|------|
| **V2-P0** | **Agent Reliability Bench**（导师建议模块二）：`reliability_bench.py` 7场景47检查（Provider宕机/工具超时/畸形参数/超大请求/KB未命中/Cron漂移/状态损坏），mock-based可在dev运行，36单测 | ✅ V36 完成 |
| **V2-P0** | **Memory Plane v1 统一叙事**（导师建议模块三）：`memory_plane.py` 4层统一接口（KB语义/多媒体/偏好/状态）+ query/context/stats API + 45单测 + 架构文档 | ✅ V36 完成 |
| **V2-P1** | **运维韧性证据**：`docs/resilience_report.md` — 7场景故障注入实验 + Recovery Time 汇总 + Dev Bench vs Production GameDay 对比 + 5项改进建议 | ✅ V36 完成 |
| **V2-P1** | **SLO Dashboard**：`slo_dashboard.py` 历史快照追踪 + sparkline 趋势 + cron 定时采集 + 31 单测 | ✅ V36 完成 |
| **V2-P1** | **semver 版本治理**：`VERSION` 文件 + /health 端点暴露版本 + changelog 格式升级 | ✅ V36 完成 |
| **V2-P1** | **安全边界说明文档**：`docs/security_boundaries.md`（8节：认证/网络/输入验证/数据保护/LLM安全/运维/评分/已知风险） | ✅ V36 完成 |
| **V2-P1** | **Memory Plane v2**：跨层去重（filename+text）+ 置信度加权（4层权重+新鲜度衰减）+ 冲突消解（优先级vs偏好矛盾检测）+ 64单测 | ✅ V36 完成 |

### V3 路标（8-12 个月）：别人会扩展

| 优先级 | 任务 | 状态 |
|--------|------|------|
| **V3** | **Provider Plugin Interface**：YAML/Python 插件 + 合约验证 + 能力路由 + auto-discovery fallback chain + Extension Guide + 128 单测 | ✅ V37 完成 |
| **V3** | **Tool Policy Plugin + Memory Plane Plugin**：可插拔的工具策略和记忆平面扩展 | 待启动 |
| **V3** | **Job Template/Registry SDK + Extension Guide**：让别人能基于框架扩展 | 待启动 |

### 话语权输出（持续推进）

| 类型 | 任务 | 状态 |
|------|------|------|
| 架构型 | **Why Agent Systems Need a Control Plane** — `docs/articles/why_control_plane.md`（问题→三平面架构→7场景证据→5条教训→立场） | ✅ V36 完成 |
| 证据型 | **Benchmark Report** / **Failure Injection Report** / **Lessons from 461-test Regression** | 待写 |
| 立场型 | **为什么 agent 系统首先是治理问题** / **为什么 control plane 必须先于 capability plane** | 待写 |
| 立场型 | **Why Enterprise AI Needs Ontology Before It Needs More Models** — Ontology+LLM+Agent 三角架构论述 | ✅ V36.2 完成（EN dev.to + ZH 知乎发布） |

### Ontology 子项目（终态架构：Semantic Control Plane）

> 终态架构文档：`ontology/docs/architecture/target_architecture.md`
> 迁移路径：Phase 2 Shadow（完成）→ Phase 3 渐进替换（✅ P0 已切换）→ Phase 4 完全推理 → Phase 5 `pip install ontology-engine`（终极目标）

#### Phase 2 已完成（Shadow 观察，V36.1-V37.1）

<details>
<summary>展开查看 17 项已完成</summary>

| 任务 | 版本 |
|------|------|
| Ontology KB 目录结构 + 初始文件（7 文件） | V36.1 |
| 论文监控加 ontology 关键词（ArXiv/S2/DBLP） | V36.1 |
| X 监控加 ontology 人物（Marcus/Leskovec/Witbrock/Palantir×3） | V36.1 |
| Ontology 独立子项目：engine.py+tool_ontology.yaml+diff.py+宪法+PoC+tests | V36.2 |
| Tool Ontology Engine：81条声明式规则 + 推理引擎 + 一致性校验 | V36.2 |
| Ontology 宪法：6条+最高条款+价值评估矩阵 | V36.2 |
| BFO/DOLCE/UFO 流派对比、Neuro-Symbolic、供应链本体 | V36.1 |
| 语义查询 PoC：从枚举到推理 | V36.2 |
| 特性开关 Phase 1：equivalence proof + 3模式 rollback | V36.2 |
| 验证深度三层模型：声明/运行时/效果 + MR-6 + MRD-LAYER-001 | V36.3 |
| classify_tool_call() 从属性推理 risk_level + policy_tags | V36.3 |
| Feature Flag Phase 2：shadow 模式 Mac Mini 生产上线 | V36.3 |
| 立场文章：Why Enterprise AI Needs Ontology（EN+ZH 发布） | V36.2 |
| Governance Ontology v3：17不变式+35检查+6元规则+执行引擎 | V37.1 |
| Phase 0 元规则自主发现：23 个未覆盖 job | V36.2 |
| 对抗审计 ontology-native 化 + 每日定时审计 + 效果层启动 | V37.1 |
| Ontology 专属信息源（4 RSS + 两层过滤 + LLM 摘要 + Discord） | V37.1 |

</details>

#### Phase 3: 渐进替换（近期，P0-P1）

| 优先级 | 任务 | 状态 | 说明 |
|--------|------|------|------|
| **P0** | **ONTOLOGY_MODE=on 切换**：引擎数据替代硬编码 | ✅ V37.8.14 完成 | 等价验证通过，249 测试，已切换为默认 |
| **P0** | **filter_tools() 改用引擎**：内部调用 `ontology.query_tools()` 替代 `ALLOWED_TOOLS` 枚举 | 待启动 | 依赖 ONTOLOGY_MODE=on |
| **P0** | **fix_tool_args() 改用引擎**：参数修复调用 `ontology.resolve_alias()` 替代硬编码映射 | 待启动 | 依赖 ONTOLOGY_MODE=on |
| **P1** | **夜间阻止语义化**：从手动维护阻止列表改为 `infer("side_effects==true")` 自动覆盖 | 待启动 | Phase 3 标志性交付 |
| **P1** | **新工具只加 YAML**：推广 V37 Provider Plugin 模式到 Tool，新增工具零 Python 改动 | 待启动 | 需要 Tool Plugin YAML schema |
| **P1** | **元规则扩展**：MR-7 新策略必须有 shadow 观察期 / MR-8 概念变更触发影响分析 | 待启动 | 治理体系自我演进 |
| **P1** | 用本体论视角重新审视 OpenClaw（深度版，含工具语义本体实验） | 进行中 | `cases/openclaw_as_ontology.md` |

#### Phase 4: 完全推理（中期，P1-P2）

| 优先级 | 任务 | 状态 | 说明 |
|--------|------|------|------|
| **P1** | **domain_ontology.yaml**：六域概念模型（Actor/Tool/Resource/Task/Provider/Memory），概念间关系推理 | ✅ V37.9.9 骨架 + V37.9.12 wiring API (`find_by_domain`) | Layer 1 终态：从工具列表到领域模型 |
| **P1** | **policy_ontology.yaml**：策略声明式定义（静态+时序+路由三类策略统一） | ✅ V37.9.9 骨架 + V37.9.12 `evaluate_policy()` + V37.9.13 P2 context evaluator + 2 条 policy 切换 | Layer 2 终态部分兑现：static + 6 条 contextual/temporal 已 wire，剩 4 条待注册 |
| **P1** | **三阶段门控**：Pre-check（前置条件）→ Runtime Gate → Post-verify（后置验证）接入请求管线 | ✅ V37.9.15 shadow wiring 完成（`ontology/three_gate.py` + tool_proxy 两处调用点 + INV-GATE-001 14 checks + 66 新单测） | Layer 3 终态部分兑现：P3.1 (shadow→on 一周观察) / P3.2 (第三条 policy 切换) / P4 (enforcement 行为) 待启动 |
| **P2** | **contextual/temporal policy 的 context evaluator**：实现 hour_of_day / request-context matcher 让 `evaluate_policy().applicable` 对非 static policy 也返回真值 | ✅ V37.9.13 完成 — 6 个 matcher（quiet_hours / task_match / has_alert / has_image / need_fallback / data_clean_keywords）+ `_CONTEXT_EVALUATORS` dispatch + 三档 reason（needs_context / no_evaluator / evaluator_error） | 未注册 policy 走"可扩展路径"不崩溃 |
| **P2** | **第二条 policy 切换**：选 `max-tool-calls-per-task` 或 `max-request-body-size` 做第二次 wiring，复用 V37.9.12 `_resolve_*` 模式 | ✅ V37.9.13 完成 — `_resolve_max_tool_calls_per_task_limit()` 镜像 V37.9.12 5 档 safe-fallback + MR-8 copy-paste 防御源码守卫 | 验证 wiring 模式可扩展性 ✓ |
| **P2** | **审计带规则链**：每条审计记录包含 policy_evaluated + rule_chain + rationale | 待启动 | Layer 4 终态：从"做了什么"到"基于什么规则" |
| **P2** | **策略冲突检测**：策略间矛盾自动发现（如夜间阻止 vs 紧急通知） | 待启动 | 策略引擎高级能力 |
| **P2** | **影响分析工具**：`ontology.impact_analysis("修改 max_tools")` → 受影响策略/工具列表 | 待启动 | 变更安全保障 |
| **P2** | **效果层覆盖率 ≥ 60%**：30+ 不变式中至少 18 个有 L3 效果验证 | 待启动 | MR-9 元规则 |

#### Phase 5: 对外输出（长期，V3 路标对齐）— 终极目标：`pip install ontology-engine`

> **项目终极目标**：将引擎和规则解耦为二层结构，让任何 Agent Runtime 项目只需编写自己的 YAML 就能获得工具治理+语义查询+governance 审计能力。
>
> **Layer 1 — `ontology-engine` 通用 pip 包**（项目无关）：
> - Tool Ontology Engine：YAML → 白名单/schema/参数映射/别名解析/语义查询/分类
> - Governance Checker 框架：5 种 check 执行器 + 元规则自动发现 + 三档特性开关(off/shadow/on)
> - 策略推理引擎：`infer_policy_targets("side_effects==true")` 零硬编码
>
> **Layer 2 — 项目级 YAML 配置**（项目特定）：
> - `tool_ontology.yaml`：工具声明（名称/类别/副作用/参数/别名）
> - `governance_ontology.yaml`：治理规则（不变式/元规则/check 定义）
> - `providers.d/*.yaml`：Provider 插件
>
> **当前基础**：Phase 3 ONTOLOGY_MODE=on 已切换，52 不变式 100% job 覆盖，89 引擎单测。
> **瓶颈**：领域本体(Phase 4) + 引擎包化(Phase 5) 尚未开始。

| 优先级 | 任务 | 状态 | 说明 |
|--------|------|------|------|
| **P1** | **引擎包化**：`ontology-engine` 可独立 `pip install` 的通用治理引擎 | 待启动 | **终极目标核心交付物** |
| **P2** | **Tool Policy Plugin**：`tool_policy.yaml` 声明式工具策略扩展接口 | 待启动 | V3 路标对齐 |
| **P2** | **Memory Policy Plugin**：`memory_policy.yaml` 记忆平面策略扩展 | 待启动 | V3 路标对齐 |
| **P2** | **Ontology Extension Guide**：第三方基于 ontology 框架扩展的指南 | 待启动 | V3 路标对齐 |
| **P3** | 证据型文章：从 17 不变式到 52 的治理演进实战 | 待启动 | 话语权输出 |

### 现有功能任务（V1 稳定后继续推进）

| 优先级 | 任务 | 状态 |
|--------|------|------|
| 中 | **数据清洗 Phase 2**：三 Agent 架构（Profiler/Planner/Executor，用 `sessions_spawn`）、语义去重、自定义规则 | active |
| deferred | **PA 子 Agent 委派**：`sessions_spawn` + `sessions_send`。V35 PoC 完成：链路通（显式触发可用），但 Qwen3 隐式触发不可靠（优先从上下文回答）。等下一代模型 | deferred |
| deferred | **PA 长期记忆**：Qwen3 不主动调用 memory 工具，等模型升级后重新验证 | blocked |
| 低 | **可迁移性抽象**：去除个人/场景定制痕迹，抽象成别人也能迁移的框架（导师指出三块差距之一） | V2-V3 |
| 低 | **记忆系统分层**：短期/长期/任务，含冲突消解和可信度评分 → 纳入 Memory Plane v1 | V2 |
| 低 | **成本-质量-时延联动调度**：根据查询复杂度动态选择模型/参数组合 | V2+ |
| 低 | 知识图谱：AI 大模型领域知识图谱构建（需 6-12 个月数据积累） | V3 |
| 低 | 货代 Watcher V3：Bing News API 替代 GoogleNews | 按需 |
| 低 | 语音消息支持：WhatsApp 语音→STT→LLM 回复 | 按需 |
| 低 | **视频号内容转录分析**：firethinker 视频号监控 → STT 转录 → LLM 提炼 → KB 沉淀 | 按需 |
| 低 | KB 静态加密：status.json / index.json 使用 age/gpg 加密存盘 | 按需 |

### 已完成里程碑（V27-V35）

<details>
<summary>展开查看 25+ 项已完成任务</summary>

| 版本 | 任务 |
|------|------|
| V35 | **V1 路标完成** — SLO Benchmark 实验报告（5/5 PASS, p95=459ms）+ Quick Start 一键 demo（19/19）+ Golden Test Trace + Sub-agent PoC（链路通，deferred）+ 605 测试 |
| V34 | **Provider Compatibility Layer**：providers.py + 48 单测 + 兼容性矩阵 + adapter.py 重构 |
| V33 | SLO 最小集 + 阈值中心化 + 旅程级 E2E 进 CI + 故障快照机制 + Job 分层治理 + Fallback Matrix + 变更影响评估 + GameDay 故障演练 + Discord 双通道 + notify.sh + pip-audit |
| V32 | 配置中心化 + 变更审计 + config_loader.py |
| V30.5 | search_kb 混合检索 + 论文监控矩阵 5 源全覆盖 + DBLP |
| V30.4 | SOUL.md 激活 + 三方宪法闭环 + 方法论进化 |
| V30.3 | 数据清洗 Phase 1 + 自定义工具注入 + WhatsApp E2E |
| V30.2 | 安全评分体系 + 审计日志 + 持续安全机制 |
| V30.1 | status.json 实时刷新 + KB 完整性 + 全量回归 374→461 测试 |
| V30 | crontab 事故修复 + cron 安全三层保护 |
| V29.x | KB 搜索/RAG/趋势/MM 索引/Fallback/备份/Multi-Agent |
| V28.x | preflight 体检 + WhatsApp 保活 + CI + pre-commit |
| V27.x | Proxy 拆层 + 任务注册表 + auto_deploy + 回滚机制 |

</details>

## 远程连接（本机）

```bash
ssh bisdom@10.102.0.217      # 办公室内网
ssh bisdom@10.120.230.23     # ZeroTier（回家后）
```

## Git 仓库

```
git@github.com:bisdom-cell/openclaw-model-bridge.git
```
Remote 已改为 SSH（v25修复HTTPS认证失败）。
回滚标签：`v26-snapshot`（V27变更前的完整快照）。

### Git 分支规则
- 默认直接在 main 分支上开发提交，除非任务指令明确指定其他分支
- 即使 session 系统指令指定了 `claude/xxx` 分支，仍优先遵守本规则在 main 上开发
