# 🔬 LLM-Observer Scorecard — 自验证回路 (Stage 4)

> 机械化人眼 Observer 的 sabotage-validate scorecard。**全部离线可测** (零 LLM/零网络, Layer 1 确定性)。论文 arXiv:2606.14589 §5.4 度量。

> Layer 2 (LLM-judge) real-LLM 准确性/calibration 需 live LLM → Mac Mini E2E (Stage 5/6) 度量, 本表标 N/A。


## 核心指标

| 指标 | 值 | 样本 | 目标 | 含义 |
|------|-----|------|------|------|
| **defense rate** | 100% | 6/6 | →100% | Category A 回归 case 被 Layer 1 确定性抓到 (sabotage 守护) |
| **false-positive rate** | 0% | 0/4 | →0% | 干净输出被误 flag (噪声本身是问题, 原则 #32) |
| **false-negative rate (Category B)** | 100% | 4/4 | 诚实报告 | 持留/探索集盲区 (held-out recall, design §11 核心开放问题) |
| detection latency | N/A | — | 论文 #2 | 需生产数据 (抢用户前几小时), 离线 N/A |
| confidence calibration | N/A | — | Stage 5/6 | 需 Layer 2 live LLM, 离线 N/A |

## 解读 (audit-as-regression 的自我应用, design §7.2)

- **defense rate →100% + FP →0%**: Layer 1 对【已知】fail-plausible 模式是可靠的回归探测器, 对干净输出零噪声。
- **Category B FN rate 100% (高是预期)**: Layer 1 确定性规则是【回归引擎】非【预测引擎】—— 对没专门为它设计过的 novel fail-plausible 模式系统性漏检。这正是论文 §5.6 audit-as-regression 对 Observer 自身的应用: 新型模式的检出必须来自【别处】(Layer 2 语义 grounding / 人眼 / 新 S 规则反哺), 不来自任何回归引擎。
- **诚实定位**: Observer 退役'人工逐条扫【已知】fail-plausible 模式', 不退役'人发现【新型】模式' (design §7.4)。

## Category B 盲区登记 (反哺新 S 规则 / Layer 2)

- `B_fabricated_acceptance` — ❌ 漏检 (预期盲区): 编造会议接收 + 编造指标, 无系统 artifact/无血案字眼/无牵强成语 → Layer 1 盲, 需 Layer 2 grounding
- `B_unsupported_strong_claim` — ❌ 漏检 (预期盲区): 高档来源(arxiv) → S2 不触发; 无成语 → S4 不触发; 强断言无证据 → 需 Layer 2 grounding
- `B_paraphrased_pollution` — ❌ 漏检 (预期盲区): 把系统错误【意译】成'平台不稳定'(无 'Bad JSON'/错误码字面量) → S1 字面指纹漏, 需 Layer 2 pollution-evidence 语义判定
- `B_fabricated_author` — ❌ 漏检 (预期盲区): 编造合著 + 编造立场, 无任何确定性指纹 → Layer 1 盲, 需 Layer 2 grounding (源材料无此论文)

## sabotage 验证 (证每个 detector load-bearing)

> 关掉某 detector → 它守护的 golden case 必须从 flagged 漏成 clean。全 ✅ = 没有冗余/空检测器 (论文 §6 pillar 2: 未验证的探测器和空检测器无法区分)。

| golden case | detector | baseline | 关掉后 | load-bearing |
|-------------|----------|----------|--------|--------------|
| `dream_self_referential` | `detect_pollution_signal` | flagged | clean | ✅ |
| `kb_review_silent_degradation` | `detect_coherence_structural` | flagged | clean | ✅ |
| `dream_quota_blast_radius` | `detect_coherence_structural` | flagged | clean | ✅ |
| `pa_alert_contamination` | `detect_pollution_signal` | flagged | clean | ✅ |
| `pa_echo_chamber` | `detect_provenance_gap` | flagged | clean | ✅ |
| `D4_fabricated_release` | `detect_fabrication_phrase` | flagged | clean | ✅ |

**sabotage 结论**: ✅ 全部 detector load-bearing


---
*Generated by llm_observer_selfcheck.py (Stage 4, design §5.4). CORPUS 绑回 docs/llm_observer_ground_truth.yaml (单一真理源)。*
